
> dutycheck@0.1.43 mutate:roster-virtualization
> php tests/Mutation/run-roster-virtualization-mutations.php

== baseline roster virtualization tests ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

......                                                              6 / 6 (100%)

Time: 00:00.010, Memory: 40.00 MB

OK (6 tests, 75 assertions)
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.144112
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.129822
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.156252
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.138051
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.104387
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.089922
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.081832
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.126706
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.165746
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 3.164241
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.218948
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.143807
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.154841
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.0841
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.826122
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.103328
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.055475
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.218591
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.131256
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.050814
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.045001
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.293302
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.112817
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 0.744268
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.116145
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.210531
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.278098
      type: 'test'
      ...
    1..5
ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 2.684992
  type: 'suite'
  ...
1..5
# tests 23
# suites 5
# pass 23
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 68.895822
== mutant drop_overscan (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

F.....                                                              6 / 6 (100%)

Time: 00:00.011, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testVirtualWindowHelperIsPureAndFailClosed
Failed asserting that '(function (root) {\n
	'use strict';\n
\n
	/**\n
	 * Pure row-window math for roster virtualization.\n
	 *\n
	 * The GET roster payload stays complete (SF-06). This only decides which\n
	 * rows to put in the DOM. Range `end` is exclusive.\n
	 */\n
\n
	const DEFAULT_ROW_HEIGHT = 44;\n
	const DEFAULT_OVERSCAN = 6;\n
	const UNSIZED_WINDOW_ROWS = 24;\n
\n
	function toNonNegInt(value, fallback) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n) || n < 0) {\n
			return fallback;\n
		}\n
		return Math.trunc(n);\n
	}\n
\n
	function toPositiveNumber(value, fallback) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n) || n <= 0) {\n
			return fallback;\n
		}\n
		return n;\n
	}\n
\n
	function visibleRange(opts) {\n
		const options = opts && typeof opts === 'object' ? opts : {};\n
		const total = toNonNegInt(options.total, 0);\n
		const rowHeight = toPositiveNumber(options.rowHeight, DEFAULT_ROW_HEIGHT);\n
		const overscan = toNonNegInt(options.overscan, DEFAULT_OVERSCAN);\n
		const paintAll = options.paintAll === true;\n
		let viewportHeight = Number(options.viewportHeight);\n
		if (!Number.isFinite(viewportHeight) || viewportHeight < 0) {\n
			viewportHeight = 0;\n
		}\n
		let scrollTop = Number(options.scrollTop);\n
		if (!Number.isFinite(scrollTop) || scrollTop < 0) {\n
			scrollTop = 0;\n
		}\n
\n
		const totalHeight = total * rowHeight;\n
		if (total === 0) {\n
			return {\n
				start: 0,\n
				end: 0,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: 0,\n
				rowHeight,\n
			};\n
		}\n
		if (paintAll) {\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight,\n
				rowHeight,\n
			};\n
		}\n
\n
		let first;\n
		let visible;\n
		if (viewportHeight <= 0) {\n
			first = Math.min(total - 1, Math.floor(scrollTop / rowHeight));\n
			visible = UNSIZED_WINDOW_ROWS;\n
		} else {\n
			first = Math.min(total - 1, Math.floor(scrollTop / rowHeight));\n
			visible = Math.max(1, Math.ceil(viewportHeight / rowHeight));\n
		}\n
		const start = Math.max(0, first);\n
		const end = Math.min(total, first + visible + overscan);\n
		return {\n
			start,\n
			end,\n
			padBefore: start * rowHeight,\n
			padAfter: (total - end) * rowHeight,\n
			totalHeight,\n
			rowHeight,\n
		};\n
	}\n
\n
	function scrollTopToRevealIndex(opts) {\n
		const options = opts && typeof opts === 'object' ? opts : {};\n
		const index = toNonNegInt(options.index, 0);\n
		const rowHeight = toPositiveNumber(options.rowHeight, DEFAULT_ROW_HEIGHT);\n
		const viewportHeight = toPositiveNumber(options.viewportHeight, rowHeight);\n
		const total = Math.max(index + 1, toNonNegInt(options.total, index + 1));\n
		let scrollTop = Number(options.scrollTop);\n
		if (!Number.isFinite(scrollTop) || scrollTop < 0) {\n
			scrollTop = 0;\n
		}\n
		const maxScroll = Math.max(0, total * rowHeight - viewportHeight);\n
		const rowTop = index * rowHeight;\n
		const rowBottom = rowTop + rowHeight;\n
		const viewBottom = scrollTop + viewportHeight;\n
		let next = scrollTop;\n
		if (rowTop < scrollTop) {\n
			next = rowTop;\n
		} else if (rowBottom > viewBottom) {\n
			next = rowBottom - viewportHeight;\n
		}\n
		if (next < 0) {\n
			next = 0;\n
		}\n
		if (next > maxScroll) {\n
			next = maxScroll;\n
		}\n
		return next;\n
	}\n
\n
	function pageStride(opts) {\n
		const options = opts && typeof opts === 'object' ? opts : {};\n
		const rowHeight = toPositiveNumber(options.rowHeight, DEFAULT_ROW_HEIGHT);\n
		const viewportHeight = toPositiveNumber(options.viewportHeight, rowHeight);\n
		return Math.max(1, Math.floor(viewportHeight / rowHeight) - 1);\n
	}\n
\n
	function windowCaption(opts) {\n
		const options = opts && typeof opts === 'object' ? opts : {};\n
		const total = toNonNegInt(options.total, 0);\n
		const start = toNonNegInt(options.start, 0);\n
		let end = toNonNegInt(options.end, 0);\n
		if (end < start) {\n
			end = start;\n
		}\n
		if (end > total) {\n
			end = total;\n
		}\n
		if (total <= 0) {\n
			return { mode: 'empty', from: 0, to: 0, total: 0 };\n
		}\n
		if (start <= 0 && end >= total) {\n
			return { mode: 'all', from: 1, to: total, total };\n
		}\n
		const from = Math.min(total, start + 1);\n
		const to = Math.max(from, end);\n
		return { mode: 'window', from, to, total };\n
	}\n
\n
	const api = {\n
		DEFAULT_ROW_HEIGHT,\n
		DEFAULT_OVERSCAN,\n
		UNSIZED_WINDOW_ROWS,\n
		visibleRange,\n
		scrollTopToRevealIndex,\n
		scrollOffsetToRevealIndex: scrollTopToRevealIndex,\n
		pageStride,\n
		windowCaption,\n
	};\n
\n
	root.DutyCheckVirtualWindow = api;\n
	if (typeof module !== 'undefined' && module.exports) {\n
		module.exports = api;\n
	}\n
})(typeof window !== 'undefined' ? window : globalThis);\n
' contains "first - overscan".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:30

FAILURES!
Tests: 6, Assertions: 67, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.540606
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.193674
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    not ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 1.15106
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:50:2'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        
        20 !== 14
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 14
      actual: 20
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:58:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.229655
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.138616
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    not ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.354145
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:91:2'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        
        100 !== 94
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 94
      actual: 100
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:99:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.138942
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.221745
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.272841
      type: 'test'
      ...
    1..9
not ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 5.61268
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:22:1'
  failureType: 'subtestsFailed'
  error: '2 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.682736
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.090099
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.197368
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.124398
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 2.417398
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.180457
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.132827
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.421
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.257428
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.126644
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.12441
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.656931
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.278414
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 1.570771
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.209288
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.231699
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.504009
      type: 'test'
      ...
    1..5
ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 4.149522
  type: 'suite'
  ...
1..5
# tests 23
# suites 5
# pass 21
# fail 2
# cancelled 0
# skipped 0
# todo 0
# duration_ms 107.296712
killed drop_overscan
== mutant unsized_viewport_paints_nothing (must FAIL) ==
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.861624
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.241653
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.221902
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.166303
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    not ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.735184
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:78:2'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        
        6 !== 30
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 30
      actual: 6
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:87:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    not ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.305009
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:91:2'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        
        0 !== 94
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 94
      actual: 0
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:99:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.141135
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.247273
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.260782
      type: 'test'
      ...
    1..9
not ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 5.499631
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:22:1'
  failureType: 'subtestsFailed'
  error: '2 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.078196
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.086526
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.068143
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.067611
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.520808
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.130382
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.072783
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.268994
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.128499
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.045664
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.039241
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.269369
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.141034
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 0.695423
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.2311
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.207082
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.469259
      type: 'test'
      ...
    1..5
ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 2.952358
  type: 'suite'
  ...
1..5
# tests 23
# suites 5
# pass 21
# fail 2
# cancelled 0
# skipped 0
# todo 0
# duration_ms 109.746544
killed unsized_viewport_paints_nothing
== mutant unsized_ignores_scrolltop (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

......                                                              6 / 6 (100%)

Time: 00:00.014, Memory: 40.00 MB

OK (6 tests, 75 assertions)
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.131242
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.138016
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.154083
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.129718
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.104318
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    not ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.625793
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:91:2'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        
        0 !== 94
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 94
      actual: 0
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:99:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.105839
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.153763
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.193306
      type: 'test'
      ...
    1..9
not ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 3.706461
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:22:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 0.279735
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.067907
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.047757
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.04793
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.48185
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.086456
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.055255
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.189551
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.1094
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.043983
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.038881
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.251998
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.095085
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 0.707993
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.127036
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.189413
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.075892
      type: 'test'
      ...
    1..5
ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 2.365955
  type: 'suite'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 71.724894
killed unsized_ignores_scrolltop
== mutant paint_all_ignored (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

......                                                              6 / 6 (100%)

Time: 00:00.009, Memory: 40.00 MB

OK (6 tests, 75 assertions)
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.802132
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.251836
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.329602
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.257579
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.187141
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.185038
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    not ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.911082
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:105:2'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        
        12 !== 0
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 0
      actual: 12
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:114:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.281986
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.354972
      type: 'test'
      ...
    1..9
not ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 6.265458
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:22:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.195432
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.121136
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.109702
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.101666
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.830628
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.172474
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.107959
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.380167
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.211419
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.109888
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.102305
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.543533
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.180935
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 0.975985
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.241124
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.299246
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.402954
      type: 'test'
      ...
    1..5
ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 3.369483
  type: 'suite'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 101.496225
killed paint_all_ignored
== mutant grid_paints_every_employee (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

..F...                                                              6 / 6 (100%)

Time: 00:00.008, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testRosterJsWindowsRowsAndKeepsKeyboardReach
Failed asserting that '(function () {\n
	'use strict';\n
\n
	const Api = window.DutyCheckApi;\n
	const Msg = window.DutyCheckMessaging;\n
	const C = window.DutyCheckComponents || window.DutyCheckDom || {};\n
	const D = window.DutyCheckDates;\n
	const ConflictLabels = window.DutyCheckConflictLabels;\n
	const create = C.createElement;\n
	if (typeof create !== 'function') {\n
		throw new Error('DutyCheck components failed to load');\n
	}\n
\n
	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n
\n
	const state = {\n
		periods: [],\n
		employees: [],\n
		locations: [],\n
		assignments: [],\n
		absenceBlocks: [],\n
		canCreateAssignments: false,\n
		lastRosterData: null,\n
		defaultBreakMinutes: 0,\n
		planningDefaultsFresh: false,\n
		templates: [],\n
		templatesLoaded: false,\n
		editingAssignmentId: null,\n
		editingAssignmentVersion: null,\n
		copyPreviewReady: false,\n
		preferencesByEmployee: {},\n
		blackoutsByEmployeeDate: {},\n
	};\n
	let assignmentModalInstance = null;\n
	let suggestedBreakMinutes = 0;\n
\n
	function clampBreakMinutes(value) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n)) {\n
			return 0;\n
		}\n
		return Math.max(0, Math.min(720, Math.round(n)));\n
	}\n
\n
	function readStoredBreakMinutes() {\n
		try {\n
			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n
			if (raw === null || raw === '') {\n
				return null;\n
			}\n
			return clampBreakMinutes(raw);\n
		} catch (_) {\n
			return null;\n
		}\n
	}\n
\n
	function rememberBreakMinutes(minutes) {\n
		try {\n
			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n
		} catch (_) {\n
			/* ignore quota / privacy mode */\n
		}\n
	}\n
\n
	function resolveBreakPrefill() {\n
		const org = clampBreakMinutes(state.defaultBreakMinutes);\n
		// Organisation default from Settings always wins when it is greater than zero.\n
		if (org > 0) {\n
			return { minutes: org, source: 'settings' };\n
		}\n
		const stored = readStoredBreakMinutes();\n
		if (stored !== null) {\n
			return { minutes: stored, source: 'last' };\n
		}\n
		return { minutes: 0, source: 'settings' };\n
	}\n
\n
	async function refreshPlanningDefaultFromServer() {\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n
			const minutes = Number(response?.planning?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
			state.planningDefaultsFresh = true;\n
		} catch (_) {\n
			/* Fall back to defaultBreakMinutes from the last roster API load. */\n
		}\n
	}\n
\n
	function breakPrefillMessage(prefill) {\n
		const n = String(prefill.minutes);\n
		if (prefill.source === 'last') {\n
			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n
		}\n
		if (prefill.minutes === 0) {\n
			return t('dutycheck', 'Pre-filled with 0 min — no break.');\n
		}\n
		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n
	}\n
\n
	function updateBreakPrefillHint(userEdited) {\n
		const el = document.getElementById('dc-assignment-break-prefill');\n
		if (!el) {\n
			return;\n
		}\n
		if (userEdited) {\n
			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n
			return;\n
		}\n
		el.textContent = breakPrefillMessage(resolveBreakPrefill());\n
	}\n
\n
	function applyDefaultBreakToForm() {\n
		const prefill = resolveBreakPrefill();\n
		suggestedBreakMinutes = prefill.minutes;\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(prefill.minutes);\n
		}\n
		updateBreakPrefillHint(false);\n
	}\n
\n
	function wireBreakPrefillHint() {\n
		const br = document.getElementById('dc-assignment-break');\n
		if (!br || br.dataset.dcBreakPrefillWired) {\n
			return;\n
		}\n
		br.dataset.dcBreakPrefillWired = '1';\n
		br.addEventListener('input', () => {\n
			const current = clampBreakMinutes(br.value);\n
			updateBreakPrefillHint(current !== suggestedBreakMinutes);\n
		});\n
		br.addEventListener('change', () => {\n
			const current = clampBreakMinutes(br.value);\n
			if (current === suggestedBreakMinutes) {\n
				updateBreakPrefillHint(false);\n
			}\n
		});\n
	}\n
\n
	function dateToDayIndex(isoDate) {\n
		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n
		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n
			return null;\n
		}\n
		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n
	}\n
\n
	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n
		const dayIndex = dateToDayIndex(dutyDate);\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime);\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime);\n
		if (dayIndex === null || start === null || end === null) {\n
			return null;\n
		}\n
		const absoluteStart = (dayIndex * 1440) + start;\n
		let absoluteEnd = (dayIndex * 1440) + end;\n
		if (absoluteEnd <= absoluteStart) {\n
			absoluteEnd += 1440;\n
		}\n
		return [absoluteStart, absoluteEnd];\n
	}\n
\n
	function absoluteRangesOverlap(a, b) {\n
		return a[0] < b[1] && b[0] < a[1];\n
	}\n
\n
	function isDateInAbsenceBlock(employeeId, dutyDate) {\n
		if (!dutyDate) {\n
			return false;\n
		}\n
		for (const block of state.absenceBlocks) {\n
			if (Number(block?.employeeId) !== Number(employeeId)) {\n
				continue;\n
			}\n
			const start = String(block?.startDate || '');\n
			const end = String(block?.endDate || '');\n
			if (dutyDate >= start && dutyDate <= end) {\n
				return true;\n
			}\n
		}\n
		return false;\n
	}\n
\n
	function employeesAvailableOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return [];\n
		}\n
		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n
	}\n
\n
	function countEmployeesBlockedOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return 0;\n
		}\n
		return state.employees.length - employeesAvailableOnDate(dutyDate).length;\n
	}\n
\n
	function appendHintLink(parent, beforeText, linkText, afterText, href) {\n
		parent.appendChild(document.createTextNode(beforeText));\n
		if (href) {\n
			const link = create('a', {\n
				class: 'dc-hint-link',\n
				href,\n
				text: linkText,\n
			});\n
			parent.appendChild(link);\n
			parent.appendChild(document.createTextNode(afterText));\n
		} else {\n
			parent.appendChild(document.createTextNode(linkText + afterText));\n
		}\n
	}\n
\n
	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n
		const hint = document.getElementById('dc-assignment-employee-hint');\n
		if (!hint) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		hint.replaceChildren();\n
		if (!dutyDate) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n
			));\n
			return;\n
		}\n
		if (!available.length && blocked > 0) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n
					.replace('{n}', String(blocked)),\n
				t('dutycheck', 'review absences'),\n
				'.',\n
				String(urls.absences || ''),\n
			);\n
			return;\n
		}\n
		if (!available.length) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'No employees are available. Add an active employee on the '),\n
				t('dutycheck', 'Employees'),\n
				t('dutycheck', ' page first.'),\n
				String(urls.employees || ''),\n
			);\n
			return;\n
		}\n
		if (blocked > 0) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n
					.replace('{available}', String(available.length))\n
					.replace('{hidden}', String(blocked)),\n
			));\n
			return;\n
		}\n
		hint.appendChild(document.createTextNode(\n
			t('dutycheck', 'All {n} active employees can be scheduled on this date.')\n
				.replace('{n}', String(available.length)),\n
		));\n
	}\n
\n
	function refreshEmployeeSelectForDate() {\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const dutyDate = dateInput ? String(dateInput.value || '') : '';\n
		const available = employeesAvailableOnDate(dutyDate);\n
		const blocked = countEmployeesBlockedOnDate(dutyDate);\n
		setEmployeeAvailabilityHint(dutyDate, available, blocked);\n
		fillSelect('dc-assignment-employee', available, {\n
			emptyText: dutyDate\n
				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n
				: t('dutycheck', 'Pick a date first'),\n
		});\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		if (employeeSelect) {\n
			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n
		}\n
	}\n
\n
	function findOverlapAssignment(payload) {\n
		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n
		if (!candidate) {\n
			return null;\n
		}\n
		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		for (const assignment of state.assignments) {\n
			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n
				continue;\n
			}\n
			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n
				continue;\n
			}\n
			const existing = assignmentAbsoluteRange(\n
				String(assignment?.dutyDate || ''),\n
				String(assignment?.startTime || ''),\n
				String(assignment?.endTime || ''),\n
			);\n
			if (existing && absoluteRangesOverlap(candidate, existing)) {\n
				return assignment;\n
			}\n
		}\n
		return null;\n
	}\n
\n
	function setFormFeedbackA11y(el, isError) {\n
		if (!el) {\n
			return;\n
		}\n
		if (isError) {\n
			el.setAttribute('role', 'alert');\n
			el.setAttribute('aria-live', 'assertive');\n
		} else {\n
			el.setAttribute('role', 'status');\n
			el.setAttribute('aria-live', 'polite');\n
		}\n
	}\n
\n
	function focusFirstInvalidAssignmentField(payload) {\n
		if (!payload) {\n
			return;\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		const order = [\n
			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n
			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n
			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n
			{\n
				test: () => period?.startDate && period?.endDate && payload.dutyDate\n
					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n
				id: 'dc-assignment-date',\n
			},\n
			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },\n
			{ test: () => !payload.locationId, id: 'dc-assignment-location' },\n
			{ test: () => !payload.startTime, id: 'dc-assignment-start' },\n
			{ test: () => !payload.endTime, id: 'dc-assignment-end' },\n
			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },\n
			{\n
				test: () => {\n
					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
					return effective === null || effective <= 0;\n
				},\n
				id: 'dc-assignment-end',\n
			},\n
		];\n
		for (const item of order) {\n
			if (item.test()) {\n
				document.getElementById(item.id)?.focus();\n
				return;\n
			}\n
		}\n
	}\n
\n
	function selectedPeriodForPayload(payload) {\n
		const periodId = Number(payload?.periodId);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function clearAssignmentFormSuccess() {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		if (!el || el.dataset.dcSuccess !== '1') {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.dataset.dcSuccess = '';\n
		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
		setFormFeedbackA11y(el, false);\n
	}\n
\n
	function showAssignmentFormSuccess(message, kind) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		const tone = kind === 'info' ? 'info' : 'success';\n
		el.hidden = false;\n
		el.textContent = String(message);\n
		el.dataset.dcSuccess = '1';\n
		el.classList.remove('dc-roster-form__feedback--error');\n
		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');\n
		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');\n
		setFormFeedbackA11y(el, false);\n
		if (submit) {\n
			submit.disabled = submit.dataset.dcLockedBySetup === '1';\n
		}\n
		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });\n
	}\n
\n
	function labelsForAssignmentPayload(payload) {\n
		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));\n
		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		const locationSelect = document.getElementById('dc-assignment-location');\n
		const employeeName = employee\n
			? String(employee.name || employee.displayName || '')\n
			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		const locationName = location\n
			? String(location.name || '')\n
			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		return { employeeName, locationName };\n
	}\n
\n
	function buildAssignmentSavedMessage(payload) {\n
		const { employeeName, locationName } = labelsForAssignmentPayload(payload);\n
		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;\n
		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)\n
			|| `${payload.startTime} – ${payload.endTime}`;\n
		return t(\n
			'dutycheck',\n
			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',\n
		)\n
			.replace('{employee}', employeeName || '—')\n
			.replace('{date}', String(dateLabel || ''))\n
			.replace('{times}', String(times))\n
			.replace('{location}', locationName || '—');\n
	}\n
\n
	function updateAssignmentFormFeedback(payload) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		if (el.dataset.dcSuccess === '1') {\n
			return;\n
		}\n
		if (typeof payload === 'string') {\n
			el.hidden = false;\n
			el.textContent = payload;\n
			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			el.classList.add('dc-roster-form__feedback--error');\n
			setFormFeedbackA11y(el, true);\n
			if (submit) {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		if (!payload) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			return;\n
		}\n
		let message = '';\n
		let isError = false;\n
		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');\n
			isError = true;\n
		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {\n
			const overlap = findOverlapAssignment(payload);\n
			if (overlap) {\n
				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;\n
				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)\n
					|| `${overlap.startTime} – ${overlap.endTime}`;\n
				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')\n
					.replace('{date}', String(when))\n
					.replace('{times}', String(times));\n
				isError = true;\n
			}\n
		}\n
		if (!message) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			if (submit && submit.dataset.dcLockedBySetup !== '1') {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		el.hidden = false;\n
		el.textContent = message;\n
		el.classList.toggle('dc-roster-form__feedback--error', isError);\n
		setFormFeedbackA11y(el, isError);\n
		if (submit && isError) {\n
			submit.disabled = true;\n
		}\n
	}\n
\n
	function refreshAssignmentFormEligibility() {\n
		refreshEmployeeSelectForDate();\n
		updateLocationTimezoneHint();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		updateAssignmentFormFeedback(readForm(form));\n
	}\n
\n
	function updateLocationTimezoneHint() {\n
		const hint = document.getElementById('dc-assignment-location-tz-hint');\n
		const select = document.getElementById('dc-assignment-location');\n
		if (!hint || !select) {\n
			return;\n
		}\n
		const locationId = Number(select.value);\n
		const location = state.locations.find((row) => Number(row.id) === locationId);\n
		const tz = location && String(location.timezone || '').trim();\n
		if (!tz) {\n
			hint.hidden = true;\n
			hint.replaceChildren();\n
			return;\n
		}\n
		const accountTz = D?.currentTimezone?.() || '';\n
		hint.textContent = t(\n
			'dutycheck',\n
			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',\n
		)\n
			.replace('{locationTimezone}', tz)\n
			.replace('{accountTimezone}', accountTz || 'UTC');\n
		hint.hidden = false;\n
	}\n
\n
	function fillSelect(id, items, options) {\n
		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});\n
		const select = document.getElementById(id);\n
		if (!select) return;\n
		const previous = String(select.value || '');\n
		select.replaceChildren();\n
		if (!items.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = opts.emptyText;\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const item of items) {\n
			const option = document.createElement('option');\n
			option.value = String(item.id);\n
			option.textContent = String(item[opts.labelKey] || item.name || item.id);\n
			select.appendChild(option);\n
		}\n
		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {\n
			select.value = previous;\n
		}\n
	}\n
\n
	function fillPeriodSwitcher(periods, selected) {\n
		const select = document.getElementById('dc-roster-period-switcher');\n
		if (!select) return;\n
		const statusLabelMap = {\n
			open: t('dutycheck', 'Open'),\n
			published: t('dutycheck', 'Published'),\n
			closed: t('dutycheck', 'Closed'),\n
		};\n
		select.replaceChildren();\n
		if (!periods.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = t('dutycheck', 'No periods yet');\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const period of periods) {\n
			const option = document.createElement('option');\n
			option.value = String(period.id);\n
			const start = D?.formatDisplayDate(period.startDate) || period.startDate;\n
			const end = D?.formatDisplayDate(period.endDate) || period.endDate;\n
			const statusKey = String(period.status || '').toLowerCase();\n
			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();\n
			option.textContent = `${start} – ${end} · ${status}`;\n
			select.appendChild(option);\n
		}\n
		if (selected) {\n
			select.value = String(selected);\n
		}\n
	}\n
\n
	function severityBadge(severity) {\n
		const sev = String(severity || 'info');\n
		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;\n
		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });\n
		badge.setAttribute('aria-label', label);\n
		return badge;\n
	}\n
\n
	function renderConflicts(conflicts) {\n
		const list = document.getElementById('dc-conflict-list');\n
		const summary = document.getElementById('dc-conflict-summary');\n
		if (!list) return;\n
		list.setAttribute('role', 'list');\n
		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));\n
		list.replaceChildren();\n
		const hard = conflicts.filter((c) => c?.severity === 'hard').length;\n
		const soft = conflicts.filter((c) => c?.severity === 'soft').length;\n
		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;\n
		if (summary) {\n
			summary.textContent = !conflicts.length\n
				? t('dutycheck', 'No planning issues found.')\n
				: (ConflictLabels\n
					? ConflictLabels.countsSummary(hard, soft, softUnack)\n
					: String(hard));\n
		}\n
		if (!conflicts.length) {\n
			return;\n
		}\n
		for (const conflict of conflicts) {\n
			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });\n
			li.appendChild(severityBadge(conflict?.severity));\n
			const conflictMessage = String(conflict?.message || '');\n
			const titleText = conflictMessage\n
				? translateConflictMessage(conflictMessage)\n
				: t('dutycheck', 'Unknown conflict');\n
			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;\n
			const body = create('div', { class: 'dc-conflict__body' });\n
			const title = create('span', { class: 'dc-conflict__title', text: titleText });\n
			title.id = titleId;\n
			body.appendChild(title);\n
			li.setAttribute('aria-labelledby', titleId);\n
			if (conflict?.acknowledged && conflict?.ackReason) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack',\n
					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),\n
				}));\n
			} else if (conflict?.ackInvalidated) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack-invalid',\n
					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),\n
				}));\n
			}\n
			li.appendChild(body);\n
\n
			const actions = create('div', { class: 'dc-conflict__actions' });\n
			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {\n
				const ackBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Confirm'),\n
				});\n
				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));\n
				actions.appendChild(ackBtn);\n
			}\n
			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();\n
			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {\n
				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()\n
					|| t('dutycheck', 'Open ArbeitszeitCheck');\n
				const recoveryLink = create('a', {\n
					class: 'button primary',\n
					href: recoveryUrl,\n
					text: recoveryLabel,\n
					attrs: {\n
						target: '_blank',\n
						rel: 'noopener noreferrer',\n
						'aria-label': recoveryLabel,\n
					},\n
				});\n
				actions.appendChild(recoveryLink);\n
			}\n
			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];\n
			if (conflict?.type === 'absence_collision' && assignmentIds.length) {\n
				const focusBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Show assignment'),\n
				});\n
				focusBtn.addEventListener('click', () => {\n
					revealAssignmentRow(Number(assignmentIds[0]));\n
				});\n
				actions.appendChild(focusBtn);\n
			}\n
			li.appendChild(actions);\n
			list.appendChild(li);\n
		}\n
	}\n
\n
	async function acknowledgeConflict(conflictId) {\n
		const reason = await C.promptReason({\n
			title: t('dutycheck', 'Confirm this exception'),\n
			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),\n
			confirmLabel: t('dutycheck', 'Confirm'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			minLength: 10,\n
		});\n
		if (reason === null) return;\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });\n
			render(response?.data || {});\n
			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');\n
			Msg.announce(ackMsg, 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	const VIRTUAL_FALLBACK = {\n
		DEFAULT_ROW_HEIGHT: 44,\n
		DEFAULT_OVERSCAN: 6,\n
		UNSIZED_WINDOW_ROWS: 24,\n
		visibleRange(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			const rowHeight = 44;\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: total * rowHeight,\n
				rowHeight,\n
			};\n
		},\n
		scrollTopToRevealIndex() {\n
			return 0;\n
		},\n
		pageStride() {\n
			return 1;\n
		},\n
		windowCaption(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			if (total <= 0) {\n
				return { mode: 'empty', from: 0, to: 0, total: 0 };\n
			}\n
			return { mode: 'all', from: 1, to: total, total };\n
		},\n
	};\n
\n
	function virtualApi() {\n
		const api = window.DutyCheckVirtualWindow;\n
		if (api\n
			&& typeof api.visibleRange === 'function'\n
			&& typeof api.scrollTopToRevealIndex === 'function'\n
			&& typeof api.windowCaption === 'function') {\n
			return api;\n
		}\n
		return VIRTUAL_FALLBACK;\n
	}\n
\n
	function cellSelectionKey(employeeId, dutyDate) {\n
		return `${Number(employeeId)}|${String(dutyDate)}`;\n
	}\n
\n
	function prefersReducedMotion() {\n
		try {\n
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n
		} catch (_) {\n
			return false;\n
		}\n
	}\n
\n
	function measureHeight(el) {\n
		if (!(el instanceof HTMLElement)) {\n
			return 0;\n
		}\n
		const h = el.getBoundingClientRect().height;\n
		return Number.isFinite(h) && h > 0 ? h : 0;\n
	}\n
\n
	function assignmentIndex(assignments) {\n
		const byKey = new Map();\n
		for (const a of assignments || []) {\n
			const key = cellSelectionKey(a.employeeId, a.dutyDate);\n
			if (!byKey.has(key)) {\n
				byKey.set(key, []);\n
			}\n
			byKey.get(key).push(a);\n
		}\n
		return byKey;\n
	}\n
\n
	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {\n
		const emptyPanel = document.getElementById('dc-roster-assignments-empty');\n
		if (emptyPanel) {\n
			emptyPanel.hidden = !showEmptyPanel;\n
		}\n
	}\n
\n
	const gridState = {\n
		view: 'grid',\n
		selected: new Set(), // `${employeeId}|${dutyDate}`\n
		focusRow: 0,\n
		focusCol: 0,\n
		paintAll: false,\n
		rowHeight: 44,\n
		headerHeight: 44,\n
		windowStart: -1,\n
		windowEnd: -1,\n
		paintedEmployeeCount: -1,\n
		paintedDateCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	const listState = {\n
		windowStart: -1,\n
		windowEnd: -1,\n
		rowHeight: 48,\n
		paintedCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	function renderAssignments(assignments) {\n
		paintListWindow({ assignments: assignments || [], force: true });\n
		renderRosterGrid(assignments || []);\n
		updateAddAssignmentControl();\n
	}\n
\n
	function buildAssignmentRow(a, periodOpen) {\n
		const overnightHintText = t('dutycheck', 'Continues into the next day.');\n
		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));\n
		const tr = create('tr');\n
		if (a?.id != null) {\n
			tr.dataset.assignmentId = String(a.id);\n
			tr.setAttribute('tabindex', '-1');\n
		}\n
		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;\n
		const tdDate = create('td', { text: date });\n
		tdDate.dataset.cell = t('dutycheck', 'Date');\n
		tr.appendChild(tdDate);\n
\n
		const tdStart = create('td');\n
		tdStart.dataset.cell = t('dutycheck', 'Start');\n
		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));\n
		tr.appendChild(tdStart);\n
\n
		const tdEnd = create('td');\n
		tdEnd.dataset.cell = t('dutycheck', 'End');\n
		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));\n
		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {\n
			tdEnd.title = overnightHintText;\n
			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));\n
		}\n
		tr.appendChild(tdEnd);\n
\n
		const rest = [\n
			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },\n
			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },\n
			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },\n
			{ label: t('dutycheck', 'Note'), value: a.note || '' },\n
		];\n
		for (const cell of rest) {\n
			const td = create('td', { text: String(cell.value ?? '') });\n
			td.dataset.cell = cell.label;\n
			tr.appendChild(td);\n
		}\n
		const tdActions = create('td');\n
		tdActions.dataset.cell = t('dutycheck', 'Actions');\n
		tdActions.className = 'dc-table__actions';\n
		if (periodOpen && a?.id) {\n
			const editBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Edit'),\n
			});\n
			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));\n
			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));\n
			const cancelBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Cancel shift'),\n
			});\n
			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));\n
			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));\n
			tdActions.appendChild(editBtn);\n
			tdActions.appendChild(cancelBtn);\n
		} else {\n
			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));\n
		}\n
		tr.appendChild(tdActions);\n
		return tr;\n
	}\n
\n
	function createListSpacer(px) {\n
		const tr = create('tr', { class: 'dc-virtual-spacer' });\n
		tr.setAttribute('aria-hidden', 'true');\n
		tr.style.height = `${Math.max(0, px)}px`;\n
		const td = create('td');\n
		td.colSpan = 8;\n
		td.style.height = `${Math.max(0, px)}px`;\n
		td.style.padding = '0';\n
		td.style.border = '0';\n
		td.style.lineHeight = '0';\n
		tr.appendChild(td);\n
		return tr;\n
	}\n
\n
	function updateListWindowStatus(range, total) {\n
		const el = document.getElementById('dc-roster-list-status');\n
		if (!el) {\n
			return;\n
		}\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		if (cap.mode === 'all') {\n
			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));\n
			return;\n
		}\n
		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')\n
			.replace('{from}', String(cap.from))\n
			.replace('{to}', String(cap.to))\n
			.replace('{total}', String(cap.total));\n
	}\n
\n
	function paintListWindow(options) {\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		if (!tbody) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		if (!assignments.length) {\n
			tbody.replaceChildren();\n
			setAssignmentsEmptyPanelVisible(true);\n
			listState.windowStart = 0;\n
			listState.windowEnd = 0;\n
			listState.paintedCount = 0;\n
			updateListWindowStatus({ start: 0, end: 0 }, 0);\n
			return;\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const scroller = document.getElementById('dc-assignments-table-wrap');\n
		const VW = virtualApi();\n
		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: assignments.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& listState.windowStart === range.start\n
			&& listState.windowEnd === range.end\n
			&& listState.paintedCount === assignments.length) {\n
			updateListWindowStatus(range, assignments.length);\n
			return;\n
		}\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const periodOpen = canAddAssignment();\n
		const frag = document.createDocumentFragment();\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createListSpacer(range.padBefore));\n
		}\n
		const windowRows = assignments.slice(range.start, range.end);\n
		for (const a of windowRows) {\n
			frag.appendChild(buildAssignmentRow(a, periodOpen));\n
		}\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createListSpacer(range.padAfter));\n
		}\n
		tbody.replaceChildren(frag);\n
		listState.windowStart = range.start;\n
		listState.windowEnd = range.end;\n
		listState.paintedCount = assignments.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
		}\n
		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');\n
		const measured = measureHeight(sample);\n
		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {\n
			listState.rowHeight = measured;\n
			listState.remeasuring = true;\n
			paintListWindow({ assignments, force: true });\n
			listState.remeasuring = false;\n
			return;\n
		}\n
		if (measured > 0) {\n
			listState.rowHeight = measured;\n
		}\n
		updateListWindowStatus(range, assignments.length);\n
	}\n
\n
	function selectedPeriodId() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const fromSwitcher = Number(switcher?.value || 0);\n
		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {\n
			return fromSwitcher;\n
		}\n
		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;\n
	}\n
\n
	function periodDateList() {\n
		const periodId = selectedPeriodId();\n
		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));\n
		if (!period?.startDate || !period?.endDate) {\n
			return [];\n
		}\n
		const out = [];\n
		const cursor = new Date(`${period.startDate}T00:00:00Z`);\n
		const end = new Date(`${period.endDate}T00:00:00Z`);\n
		while (cursor <= end) {\n
			out.push(cursor.toISOString().slice(0, 10));\n
			cursor.setUTCDate(cursor.getUTCDate() + 1);\n
			if (out.length > 62) {\n
				break;\n
			}\n
		}\n
		return out;\n
	}\n
\n
	function updateGridWindowStatus(range, total, dayCount) {\n
		const el = document.getElementById('dc-roster-grid-status');\n
		if (!el) {\n
			return;\n
		}\n
		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		let text = '';\n
		if (cap.mode === 'all') {\n
			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));\n
		} else {\n
			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')\n
				.replace('{from}', String(cap.from))\n
				.replace('{to}', String(cap.to))\n
				.replace('{total}', String(cap.total));\n
		}\n
		if (days > 10) {\n
			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');\n
		}\n
		el.textContent = text;\n
	}\n
\n
	function createGridSpacer(px) {\n
		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });\n
		el.setAttribute('aria-hidden', 'true');\n
		el.setAttribute('role', 'presentation');\n
		el.style.height = `${Math.max(0, px)}px`;\n
		return el;\n
	}\n
\n
	function setGridEmptyState(root, message) {\n
		root.removeAttribute('aria-rowcount');\n
		root.removeAttribute('aria-colcount');\n
		root.removeAttribute('tabindex');\n
		root.style.removeProperty('--dc-roster-day-count');\n
		root.style.removeProperty('--dc-roster-day-min');\n
		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');\n
		root.setAttribute('role', 'status');\n
		root.setAttribute('aria-live', 'polite');\n
		root.replaceChildren(create('p', {\n
			class: 'dc-roster-empty-state__text',\n
			text: message,\n
		}));\n
	}\n
\n
	function prepareGridForRows(root, employees, dates) {\n
		root.setAttribute('role', 'grid');\n
		root.removeAttribute('tabindex');\n
		root.removeAttribute('aria-live');\n
		root.setAttribute('aria-rowcount', String(employees.length + 1));\n
		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));\n
		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));\n
		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));\n
		root.style.setProperty('--dc-roster-day-min', dayMin);\n
		root.classList.toggle('dc-roster-grid--long', dates.length > 10);\n
		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);\n
	}\n
\n
	function buildColHead(day, colIdx, dayMeta) {\n
		const parts = dayMeta || D?.formatRosterColumnParts?.(day);\n
		const full = parts?.full || D?.formatDisplayDate?.(day) || day;\n
		let ariaLabel = parts?.ariaLabel || full;\n
		if (parts?.isToday) {\n
			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;\n
		}\n
		const weekdayShort = parts?.weekdayShort || '';\n
		const dayNum = parts?.day || '';\n
		const classNames = ['dc-roster-grid__colhead'];\n
		if (parts?.isWeekend) {\n
			classNames.push('dc-roster-grid__colhead--weekend');\n
		}\n
		if (parts?.isToday) {\n
			classNames.push('dc-roster-grid__colhead--today');\n
		}\n
		const children = [];\n
		if (weekdayShort) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));\n
		}\n
		if (dayNum) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));\n
		}\n
		const attrs = {\n
			'data-col': String(colIdx),\n
			'aria-colindex': String(colIdx + 2),\n
			title: full,\n
			'aria-label': ariaLabel,\n
		};\n
		if (parts?.iso) {\n
			attrs['data-duty-date'] = parts.iso;\n
		}\n
		if (!children.length) {\n
			return create('div', {\n
				class: classNames.join(' '),\n
				role: 'columnheader',\n
				text: full,\n
				attrs,\n
			});\n
		}\n
		return create('div', {\n
			class: classNames.join(' '),\n
			role: 'columnheader',\n
			attrs,\n
		}, children);\n
	}\n
\n
	function buildDutyDayMeta(dates) {\n
		const map = new Map();\n
		(dates || []).forEach((day) => {\n
			map.set(day, D?.formatRosterColumnParts?.(day) || null);\n
		});\n
		return map;\n
	}\n
\n
	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {\n
		const row = create('div', {\n
			class: 'dc-roster-grid__row',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': String(rowIdx + 2) },\n
		});\n
		const prefs = state.preferencesByEmployee[String(emp.id)] || [];\n
		const prefChips = prefs.slice(0, 2).map((p) => {\n
			const band = String(p.band || '');\n
			const label = band === 'early'\n
				? t('dutycheck', 'Früh')\n
				: (band === 'late' ? t('dutycheck', 'Spät') : band);\n
			return create('span', {\n
				class: 'dc-pref-chip dc-pref-chip--static',\n
				text: label,\n
				attrs: { title: label },\n
			});\n
		});\n
		const headKids = [\n
			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),\n
		];\n
		if (prefChips.length) {\n
			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));\n
		}\n
		row.appendChild(create('div', {\n
			class: 'dc-roster-grid__rowhead',\n
			role: 'rowheader',\n
			attrs: { 'aria-colindex': '1' },\n
		}, headKids));\n
		dates.forEach((day, colIdx) => {\n
			const key = cellSelectionKey(emp.id, day);\n
			const cellAssignments = byKey.get(key) || [];\n
			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);\n
			const cell = create('div', {\n
				class: 'dc-roster-grid__cell',\n
				role: 'gridcell',\n
				attrs: {\n
					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',\n
					'data-employee-id': String(emp.id),\n
					'data-duty-date': day,\n
					'data-row': String(rowIdx),\n
					'data-col': String(colIdx),\n
					'aria-colindex': String(colIdx + 2),\n
					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',\n
				},\n
			});\n
			if (dayParts?.isWeekend) {\n
				cell.classList.add('dc-roster-grid__cell--weekend');\n
			}\n
			if (dayParts?.isToday) {\n
				cell.classList.add('dc-roster-grid__cell--today');\n
			}\n
			if (cellAssignments.length) {\n
				cell.classList.add('dc-roster-grid__cell--filled');\n
				const first = cellAssignments[0];\n
				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;\n
				cell.appendChild(create('span', {\n
					class: 'dc-roster-grid__shift',\n
					text: label,\n
				}));\n
				const editLabel = canAddAssignment()\n
					? t('dutycheck', 'Edit assignment')\n
					: t('dutycheck', 'View assignment (read-only)');\n
				cell.setAttribute('aria-label', `${editLabel}: ${label}`);\n
				if (cellAssignments.length > 1) {\n
					cell.appendChild(create('span', {\n
						class: 'dc-roster-grid__more',\n
						text: `+${cellAssignments.length - 1}`,\n
					}));\n
				}\n
			} else {\n
				cell.classList.add('dc-roster-grid__cell--empty');\n
				const blackoutKey = `${emp.id}|${day}`;\n
				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];\n
				if (hasBlackout) {\n
					cell.classList.add('dc-roster-grid__cell--blackout');\n
					cell.appendChild(create('span', {\n
						class: 'dc-blackout-mark',\n
						attrs: { title: t('dutycheck', 'Kann nicht') },\n
					}, [\n
						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),\n
						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),\n
					]));\n
					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));\n
				} else {\n
					cell.setAttribute(\n
						'aria-label',\n
						canAddAssignment()\n
							? t('dutycheck', 'Empty cell — Space to select for bulk fill')\n
							: t('dutycheck', 'Empty cell'),\n
					);\n
				}\n
			}\n
			if (gridState.selected.has(key)) {\n
				cell.classList.add('is-selected');\n
			}\n
			row.appendChild(cell);\n
		});\n
		return row;\n
	}\n
\n
	function syncVisibleGridChrome() {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {\n
			const empId = cell.getAttribute('data-employee-id');\n
			const day = cell.getAttribute('data-duty-date');\n
			const key = cellSelectionKey(empId, day);\n
			const selected = gridState.selected.has(key);\n
			cell.classList.toggle('is-selected', selected);\n
			cell.setAttribute('aria-selected', selected ? 'true' : 'false');\n
			const rowIdx = Number(cell.getAttribute('data-row'));\n
			const colIdx = Number(cell.getAttribute('data-col'));\n
			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;\n
			cell.setAttribute('tabindex', isFocus ? '0' : '-1');\n
		});\n
	}\n
\n
	function focusGridCellIfVisible(root) {\n
		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
		if (node instanceof HTMLElement) {\n
			node.scrollIntoView({\n
				block: 'nearest',\n
				inline: 'nearest',\n
				behavior: 'auto',\n
			});\n
			node.focus();\n
		}\n
	}\n
\n
	function activateGridCell(cell) {\n
		if (!(cell instanceof HTMLElement)) {\n
			return;\n
		}\n
		const rowIdx = Number(cell.getAttribute('data-row'));\n
		const colIdx = Number(cell.getAttribute('data-col'));\n
		if (Number.isInteger(rowIdx) && rowIdx >= 0) {\n
			gridState.focusRow = rowIdx;\n
		}\n
		if (Number.isInteger(colIdx) && colIdx >= 0) {\n
			gridState.focusCol = colIdx;\n
		}\n
		const empId = cell.getAttribute('data-employee-id');\n
		const day = cell.getAttribute('data-duty-date');\n
		const key = cellSelectionKey(empId, day);\n
		const filled = cell.classList.contains('dc-roster-grid__cell--filled');\n
		if (!filled) {\n
			if (canAddAssignment()) {\n
				toggleGridSelection(key);\n
				syncVisibleGridChrome();\n
				updateBulkBar();\n
			}\n
			return;\n
		}\n
		const byKey = assignmentIndex(state.assignments || []);\n
		const cellAssignments = byKey.get(key) || [];\n
		if (canAddAssignment() && cellAssignments[0]) {\n
			openAssignmentEditModal(cellAssignments[0], cell);\n
			return;\n
		}\n
		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');\n
	}\n
\n
	function ensureGridFocusVisible() {\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const employees = state.employees || [];\n
		if (!scroller || !employees.length) {\n
			return;\n
		}\n
		const VW = virtualApi();\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);\n
		const next = VW.scrollTopToRevealIndex({\n
			index: gridState.focusRow,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop: scroller.scrollTop,\n
			total: employees.length,\n
		});\n
		if (Math.abs(next - scroller.scrollTop) > 0.5) {\n
			scroller.scrollTop = next;\n
		}\n
	}\n
\n
	function renderRosterGrid(assignments) {\n
		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });\n
	}\n
\n
	function paintGridWindow(options) {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		const employees = state.employees || [];\n
		const dates = periodDateList();\n
		const byKey = assignmentIndex(assignments);\n
\n
		if (!employees.length || !dates.length) {\n
			setGridEmptyState(\n
				root,\n
				t('dutycheck', 'Add employees and an open period to use the planning grid.'),\n
			);\n
			gridState.windowStart = 0;\n
			gridState.windowEnd = 0;\n
			gridState.paintedEmployeeCount = employees.length;\n
			gridState.paintedDateCount = dates.length;\n
			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		prepareGridForRows(root, employees, dates);\n
		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));\n
		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));\n
\n
		const VW = virtualApi();\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = scroller && gridState.view === 'grid'\n
			? Math.max(0, scroller.clientHeight - headerHeight)\n
			: 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: employees.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& gridState.windowStart === range.start\n
			&& gridState.windowEnd === range.end\n
			&& gridState.paintedEmployeeCount === employees.length\n
			&& gridState.paintedDateCount === dates.length) {\n
			syncVisibleGridChrome();\n
			updateGridWindowStatus(range, employees.length, dates.length);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const savedLeft = scroller ? scroller.scrollLeft : 0;\n
		const dayMetaByDate = buildDutyDayMeta(dates);\n
		const frag = document.createDocumentFragment();\n
		const header = create('div', {\n
			class: 'dc-roster-grid__row dc-roster-grid__row--head',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': '1' },\n
		});\n
		header.appendChild(create('div', {\n
			class: 'dc-roster-grid__corner',\n
			role: 'columnheader',\n
			text: t('dutycheck', 'Person'),\n
			attrs: { 'aria-colindex': '1' },\n
		}));\n
		dates.forEach((day, colIdx) => {\n
			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));\n
		});\n
		frag.appendChild(header);\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createGridSpacer(range.padBefore));\n
		}\n
		const windowEmployees = employees;\n
		windowEmployees.forEach((emp, offset) => {\n
			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));\n
		});\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createGridSpacer(range.padAfter));\n
		}\n
		root.replaceChildren(frag);\n
		gridState.windowStart = range.start;\n
		gridState.windowEnd = range.end;\n
		gridState.paintedEmployeeCount = employees.length;\n
		gridState.paintedDateCount = dates.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
			scroller.scrollLeft = savedLeft;\n
		}\n
		const headerEl = root.querySelector('.dc-roster-grid__row--head');\n
		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');\n
		const measuredHeader = measureHeight(headerEl);\n
		const measuredRow = measureHeight(bodyRow);\n
		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)\n
			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))\n
			&& !gridState.remeasuring) {\n
			if (measuredHeader > 0) {\n
				gridState.headerHeight = measuredHeader;\n
			}\n
			if (measuredRow > 0) {\n
				gridState.rowHeight = measuredRow;\n
			}\n
			gridState.remeasuring = true;\n
			paintGridWindow({ assignments, force: true });\n
			gridState.remeasuring = false;\n
			return;\n
		}\n
		if (measuredHeader > 0) {\n
			gridState.headerHeight = measuredHeader;\n
		}\n
		if (measuredRow > 0) {\n
			gridState.rowHeight = measuredRow;\n
		}\n
		bindGridInteractions(root);\n
		updateGridWindowStatus(range, employees.length, dates.length);\n
		updateBulkBar();\n
	}\n
\n
	function bindGridInteractions(root) {\n
		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {\n
			return;\n
		}\n
		root.dataset.dcGridBound = '1';\n
		root.addEventListener('click', (ev) => {\n
			const cell = ev.target.closest('[role="gridcell"]');\n
			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {\n
				return;\n
			}\n
			activateGridCell(cell);\n
		});\n
		root.addEventListener('keydown', (ev) => {\n
			const employees = state.employees || [];\n
			const dates = periodDateList();\n
			const rowCount = employees.length;\n
			const colCount = dates.length;\n
			if (!rowCount || !colCount) {\n
				return;\n
			}\n
			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];\n
			if (!gridKeys.includes(ev.key)) {\n
				return;\n
			}\n
			ev.preventDefault();\n
			if (ev.key === 'ArrowUp') {\n
				gridState.focusRow = Math.max(0, gridState.focusRow - 1);\n
			} else if (ev.key === 'ArrowDown') {\n
				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);\n
			} else if (ev.key === 'ArrowLeft') {\n
				gridState.focusCol = Math.max(0, gridState.focusCol - 1);\n
			} else if (ev.key === 'ArrowRight') {\n
				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);\n
			} else if (ev.key === 'Home') {\n
				gridState.focusCol = 0;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = 0;\n
				}\n
			} else if (ev.key === 'End') {\n
				gridState.focusCol = colCount - 1;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = rowCount - 1;\n
				}\n
			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {\n
				const VW = virtualApi();\n
				const scroller = document.getElementById('dc-roster-grid-scroller');\n
				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
				const viewportHeight = scroller\n
					? Math.max(rowHeight, scroller.clientHeight - headerHeight)\n
					: rowHeight;\n
				const stride = typeof VW.pageStride === 'function'\n
					? VW.pageStride({ rowHeight, viewportHeight })\n
					: 1;\n
				if (ev.key === 'PageDown') {\n
					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);\n
				} else {\n
					gridState.focusRow = Math.max(0, gridState.focusRow - stride);\n
				}\n
			} else if (ev.key === 'Enter') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				activateGridCell(cell);\n
				focusGridCellIfVisible(root);\n
				return;\n
			} else if (ev.key === ' ' || ev.key === 'Spacebar') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				const empId = cell?.getAttribute('data-employee-id');\n
				const day = cell?.getAttribute('data-duty-date');\n
				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');\n
				if (empId && day && isEmpty && canAddAssignment()) {\n
					toggleGridSelection(cellSelectionKey(empId, day));\n
					syncVisibleGridChrome();\n
					updateBulkBar();\n
				}\n
				focusGridCellIfVisible(root);\n
				return;\n
			}\n
			ensureGridFocusVisible();\n
			paintGridWindow({ force: false });\n
			focusGridCellIfVisible(root);\n
		});\n
	}\n
\n
	function toggleGridSelection(key) {\n
		if (gridState.selected.has(key)) {\n
			gridState.selected.delete(key);\n
		} else {\n
			gridState.selected.add(key);\n
		}\n
	}\n
\n
	function updateBulkBar() {\n
		const bar = document.getElementById('dc-roster-bulk-bar');\n
		const countEl = document.getElementById('dc-roster-bulk-count');\n
		if (!bar || !countEl) {\n
			return;\n
		}\n
		const n = gridState.selected.size;\n
		if (n < 1 || !canAddAssignment()) {\n
			bar.hidden = true;\n
			return;\n
		}\n
		bar.hidden = false;\n
		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (sel && !(sel.options && sel.options.length)) {\n
			populateBulkTemplates();\n
		}\n
	}\n
\n
	async function populateBulkTemplates() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (!sel) {\n
			return;\n
		}\n
		await loadTemplates();\n
		sel.replaceChildren();\n
		const templates = state.templates || [];\n
		if (!templates.length) {\n
			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));\n
			return;\n
		}\n
		for (const tpl of templates) {\n
			sel.appendChild(create('option', {\n
				value: String(tpl.id),\n
				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,\n
			}));\n
		}\n
	}\n
\n
	function revealAssignmentRow(id) {\n
		const assignmentId = Number(id);\n
		const assignments = state.assignments || [];\n
		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);\n
		if (idx < 0) {\n
			return;\n
		}\n
		applyRosterViewChrome('list');\n
		const run = () => {\n
			const scroller = document.getElementById('dc-assignments-table-wrap');\n
			const VW = virtualApi();\n
			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
			if (scroller) {\n
				scroller.scrollTop = VW.scrollTopToRevealIndex({\n
					index: idx,\n
					rowHeight,\n
					viewportHeight: Math.max(rowHeight, scroller.clientHeight),\n
					scrollTop: scroller.scrollTop,\n
					total: assignments.length,\n
				});\n
			}\n
			paintListWindow({ assignments, force: true });\n
			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);\n
			if (row instanceof HTMLElement) {\n
				row.scrollIntoView({\n
					block: 'nearest',\n
					behavior: 'auto',\n
				});\n
				row.focus();\n
				row.classList.add('dc-row--flash');\n
				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);\n
			}\n
		};\n
		window.requestAnimationFrame(run);\n
	}\n
\n
	function setRosterPaintAll(paintAll) {\n
		gridState.paintAll = paintAll === true;\n
		paintListWindow({ force: true });\n
		paintGridWindow({ force: true });\n
	}\n
\n
	function wireRosterVirtualization() {\n
		const gridScroller = document.getElementById('dc-roster-grid-scroller');\n
		const listScroller = document.getElementById('dc-assignments-table-wrap');\n
		let gridRaf = 0;\n
		let listRaf = 0;\n
		if (gridScroller) {\n
			gridScroller.addEventListener('scroll', () => {\n
				if (gridRaf) {\n
					return;\n
				}\n
				gridRaf = window.requestAnimationFrame(() => {\n
					gridRaf = 0;\n
					paintGridWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		if (listScroller) {\n
			listScroller.addEventListener('scroll', () => {\n
				if (listRaf) {\n
					return;\n
				}\n
				listRaf = window.requestAnimationFrame(() => {\n
					listRaf = 0;\n
					paintListWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		const resize = () => {\n
			if (gridState.view === 'grid') {\n
				paintGridWindow({ force: true });\n
			} else {\n
				paintListWindow({ force: true });\n
			}\n
		};\n
		if (typeof ResizeObserver === 'function') {\n
			const ro = new ResizeObserver(resize);\n
			if (gridScroller) {\n
				ro.observe(gridScroller);\n
			}\n
			if (listScroller) {\n
				ro.observe(listScroller);\n
			}\n
		} else {\n
			window.addEventListener('resize', resize);\n
		}\n
		window.addEventListener('beforeprint', () => setRosterPaintAll(true));\n
		window.addEventListener('afterprint', () => setRosterPaintAll(false));\n
		if (typeof window.matchMedia === 'function') {\n
			try {\n
				const printMq = window.matchMedia('print');\n
				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);\n
				if (typeof printMq.addEventListener === 'function') {\n
					printMq.addEventListener('change', onPrintMq);\n
				} else if (typeof printMq.addListener === 'function') {\n
					printMq.addListener(onPrintMq);\n
				}\n
			} catch (_) {\n
				/* print media query is not available in every test/jsdom shim */\n
			}\n
		}\n
	}\n
\n
	function applyRosterViewChrome(view) {\n
		gridState.view = view === 'list' ? 'list' : 'grid';\n
		const gridWrap = document.getElementById('dc-roster-grid-wrap');\n
		const listPanel = document.getElementById('dc-roster-list-panel');\n
		const gridBtn = document.getElementById('dc-roster-view-grid');\n
		const listBtn = document.getElementById('dc-roster-view-list');\n
		if (gridWrap) {\n
			gridWrap.hidden = gridState.view !== 'grid';\n
		}\n
		if (listPanel) {\n
			listPanel.hidden = gridState.view !== 'list';\n
		}\n
		if (gridBtn) {\n
			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');\n
		}\n
		if (listBtn) {\n
			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');\n
		}\n
	}\n
\n
	function setRosterView(view) {\n
		applyRosterViewChrome(view);\n
		window.requestAnimationFrame(() => {\n
			if (gridState.view === 'list') {\n
				paintListWindow({ force: true });\n
			} else {\n
				paintGridWindow({ force: true });\n
			}\n
		});\n
	}\n
\n
	async function applyBulkFromTemplate() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		const tplId = Number(sel?.value || 0);\n
		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);\n
		if (!tpl || gridState.selected.size < 1) {\n
			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');\n
			return;\n
		}\n
		if (!Number(tpl.locationId || 0)) {\n
			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');\n
			return;\n
		}\n
		const periodId = Number(selectedPeriodId() || 0);\n
		let ok = 0;\n
		let fail = 0;\n
		for (const key of Array.from(gridState.selected)) {\n
			const sep = key.indexOf('|');\n
			if (sep < 1) {\n
				fail += 1;\n
				continue;\n
			}\n
			const employeeId = Number(key.slice(0, sep));\n
			const dutyDate = key.slice(sep + 1);\n
			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {\n
				fail += 1;\n
				continue;\n
			}\n
			const locationId = Number(tpl.locationId || 0);\n
			if (!locationId) {\n
				fail += 1;\n
				continue;\n
			}\n
			try {\n
				await submitAssignment({\n
					periodId,\n
					employeeId: Number(employeeId),\n
					locationId,\n
					dutyDate,\n
					startTime: String(tpl.startTime).slice(0, 5),\n
					endTime: String(tpl.endTime).slice(0, 5),\n
					breakMinutes: Number(tpl.breakMinutes || 0),\n
					note: '',\n
					assignmentId: 0,\n
				}, true);\n
				ok += 1;\n
			} catch {\n
				fail += 1;\n
			}\n
		}\n
		gridState.selected.clear();\n
		await loadRoster(periodId || null);\n
		Msg.announce(\n
			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')\n
				.replace('{ok}', String(ok))\n
				.replace('{fail}', String(fail)),\n
			fail ? 'warning' : 'info',\n
		);\n
	}\n
\n
	function uniqueConflictTypes(conflicts) {\n
		const types = new Set();\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity === 'soft' && conflict?.type) {\n
				types.add(String(conflict.type));\n
			}\n
		}\n
		return Array.from(types);\n
	}\n
\n
	function softConflictSummary(conflicts) {\n
		const lines = [];\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity !== 'soft') {\n
				continue;\n
			}\n
			const raw = String(conflict?.message || '');\n
			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');\n
			if (text && !lines.includes(text)) {\n
				lines.push(text);\n
			}\n
		}\n
		return lines.join('\n');\n
	}\n
\n
	function errorCodeFrom(err) {\n
		return String(err?.code || err?.payload?.error?.code || '');\n
	}\n
\n
	function syncAssignmentPeriodFromSwitcher() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (!switcher || !periodHidden) {\n
			return;\n
		}\n
		const selected = Number(switcher.value);\n
		if (Number.isInteger(selected) && selected > 0) {\n
			periodHidden.value = String(selected);\n
		}\n
	}\n
\n
	function announceAssignmentSaveError(err) {\n
		clearAssignmentFormSuccess();\n
		const friendly = errorMessageFor(err);\n
		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');\n
		updateAssignmentFormFeedback(message);\n
		Msg.announce(message, 'error');\n
	}\n
\n
	async function submitAssignment(payload, retryWithAck = true) {\n
		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		try {\n
			if (editId > 0) {\n
				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);\n
			}\n
			return await Api.post('/apps/dutycheck/api/assignments', payload);\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Save with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return submitAssignment({ ...payload, acknowledgements }, false);\n
			}\n
			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Employee cannot work (blackout)'),\n
					label: t(\n
						'dutycheck',\n
						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',\n
					),\n
					confirmLabel: t('dutycheck', 'Schedule anyway'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
					maxLength: 200,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	function setAssignmentFormBusy(form, busy) {\n
		if (!form) return;\n
		const submit = form.querySelector('button[type="submit"]');\n
		const clear = document.getElementById('dc-assignment-form-clear');\n
		if (submit) {\n
			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';\n
			if (busy) {\n
				submit.setAttribute('aria-busy', 'true');\n
			} else {\n
				submit.removeAttribute('aria-busy');\n
			}\n
		}\n
		if (clear) {\n
			clear.disabled = busy;\n
		}\n
	}\n
\n
	function renderSetupCallout(data) {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		const list = document.getElementById('dc-roster-setup-checklist');\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!callout || !list || !form) return;\n
		const urls = readUrls();\n
		const missing = [];\n
		if (!state.periods.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Create a planning period.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		if (!state.employees.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active employee.'),\n
				url: urls.employees,\n
				cta: t('dutycheck', 'Go to Employees'),\n
			});\n
		}\n
		if (!state.locations.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active location.'),\n
				url: urls.locations,\n
				cta: t('dutycheck', 'Go to Locations'),\n
			});\n
		}\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));\n
		const periodIsOpen = (selectedPeriod?.status || '') === 'open';\n
		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {\n
			missing.push({\n
				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		list.replaceChildren();\n
		if (!missing.length) {\n
			callout.hidden = true;\n
			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {\n
				if (el.dataset.dcLockedBySetup === '1') {\n
					el.disabled = false;\n
					delete el.dataset.dcLockedBySetup;\n
				}\n
			});\n
			updateAddAssignmentControl();\n
			return;\n
		}\n
		for (const item of missing) {\n
			const li = create('li');\n
			li.appendChild(document.createTextNode(item.text + ' '));\n
			if (item.url) {\n
				li.appendChild(create('a', {\n
					class: 'button',\n
					href: item.url,\n
					text: item.cta,\n
				}));\n
			}\n
			list.appendChild(li);\n
		}\n
		callout.hidden = false;\n
		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {\n
			if (!el.disabled) {\n
				el.dataset.dcLockedBySetup = '1';\n
				el.disabled = true;\n
			}\n
		});\n
		updateAddAssignmentControl();\n
	}\n
\n
	function readUrls() {\n
		try {\n
			const raw = document.getElementById('app-content')?.dataset?.dcUrls;\n
			if (!raw) return {};\n
			return JSON.parse(raw) || {};\n
		} catch (_) {\n
			return {};\n
		}\n
	}\n
\n
	function restoreAssignmentFormPanel() {\n
		const host = document.getElementById('dc-assignment-form-host');\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (host && panel && panel.parentElement !== host) {\n
			host.appendChild(panel);\n
		}\n
	}\n
\n
	function selectedPeriodFromState() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;\n
		const periodId = Number(raw);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function isAssignmentSetupBlocking() {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		return !!(callout && !callout.hidden);\n
	}\n
\n
	function canAddAssignment() {\n
		if (!state.canCreateAssignments) {\n
			return false;\n
		}\n
		if (!state.periods.length || !state.employees.length || !state.locations.length) {\n
			return false;\n
		}\n
		const period = selectedPeriodFromState();\n
		return !!(period && String(period.status || '').toLowerCase() === 'open');\n
	}\n
\n
	function addAssignmentDisabledReason() {\n
		if (!state.periods.length) {\n
			return t('dutycheck', 'Create a planning period first.');\n
		}\n
		if (!state.employees.length) {\n
			return t('dutycheck', 'Add at least one active employee before planning shifts.');\n
		}\n
		if (!state.locations.length) {\n
			return t('dutycheck', 'Add at least one active location before planning shifts.');\n
		}\n
		const period = selectedPeriodFromState();\n
		if (!period) {\n
			return t('dutycheck', 'Select a period before adding assignments.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!state.canCreateAssignments) {\n
			return t('dutycheck', 'You cannot add assignments for this period.');\n
		}\n
		return t('dutycheck', 'Adding assignments is not available right now.');\n
	}\n
\n
	function updateAddAssignmentControl() {\n
		const allowed = canAddAssignment();\n
		const reason = addAssignmentDisabledReason();\n
		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {\n
			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.\n
			btn.hidden = !allowed;\n
			btn.disabled = !allowed;\n
			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');\n
			btn.classList.toggle('dc-is-disabled', !allowed);\n
			if (allowed) {\n
				btn.removeAttribute('title');\n
			} else {\n
				btn.setAttribute('title', reason);\n
			}\n
		});\n
		const emptyHint = document.getElementById('dc-roster-empty-add-hint');\n
		if (emptyHint) {\n
			emptyHint.textContent = allowed\n
				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')\n
				: reason;\n
		}\n
	}\n
\n
	function clearAssignmentsSectionSuccess() {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.classList.remove('dc-roster-flash--visible');\n
	}\n
\n
	function showAssignmentsSectionSuccess(message) {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.textContent = String(message);\n
		el.hidden = false;\n
		el.classList.add('dc-roster-flash--visible');\n
	}\n
\n
	function prepareAssignmentFormForModal() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		clearAssignmentFormSuccess();\n
		updateAssignmentFormFeedback(null);\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate && period?.endDate) {\n
			dateInput.min = String(period.startDate);\n
			dateInput.max = String(period.endDate);\n
			if (!dateInput.value) {\n
				dateInput.value = String(period.startDate);\n
			}\n
		}\n
		refreshAssignmentFormEligibility();\n
		applyDefaultBreakToForm();\n
		wireBreakPrefillHint();\n
		fillTemplateSelect();\n
		D?.applyLocaleToTemporalInputs(form);\n
	}\n
\n
	function resetAssignmentFormAfterSave(data) {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		form.reset();\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden && data?.selectedPeriodId) {\n
			periodHidden.value = String(data.selectedPeriodId);\n
		}\n
		applyDefaultBreakToForm();\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate) {\n
			dateInput.value = String(period.startDate);\n
		}\n
		D?.applyLocaleToTemporalInputs(form);\n
		updateAssignmentFormFeedback(null);\n
	}\n
\n
	async function performAssignmentSave() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return false;\n
		}\n
		if (isAssignmentSetupBlocking()) {\n
			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(msg);\n
			Msg.announce(msg, 'error');\n
			return false;\n
		}\n
		const payload = readForm(form);\n
		const validation = validate(payload);\n
		if (validation) {\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(validation);\n
			Msg.announce(validation, 'error');\n
			focusFirstInvalidAssignmentField(payload);\n
			return false;\n
		}\n
		updateAssignmentFormFeedback(null);\n
		setAssignmentFormBusy(form, true);\n
		try {\n
			const response = await submitAssignment(payload, true);\n
			render(response?.data || {});\n
			const savedMsg = buildAssignmentSavedMessage(payload);\n
			clearAssignmentFormSuccess();\n
			showAssignmentsSectionSuccess(savedMsg);\n
			Msg.announce(savedMsg, 'success');\n
			rememberBreakMinutes(payload.breakMinutes);\n
			resetAssignmentFormAfterSave(response?.data || {});\n
			return true;\n
		} catch (err) {\n
			announceAssignmentSaveError(err);\n
			return false;\n
		} finally {\n
			setAssignmentFormBusy(form, false);\n
		}\n
	}\n
\n
	function syncAssignmentModalPrimary(instance) {\n
		if (!instance?.primaryBtn) {\n
			return;\n
		}\n
		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();\n
		instance.primaryBtn.disabled = blocked;\n
		if (blocked) {\n
			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());\n
		} else {\n
			instance.primaryBtn.removeAttribute('title');\n
		}\n
	}\n
\n
	async function openAssignmentCreateModal(triggerEl) {\n
		if (!canAddAssignment()) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
			return;\n
		}\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		renderSetupCallout(state.lastRosterData || {});\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Create assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save assignment'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => {\n
				const ok = await performAssignmentSave();\n
				return ok;\n
			},\n
			onClose: (result) => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				if (!result) {\n
					clearAssignmentFormSuccess();\n
					return;\n
				}\n
				const addBtn = document.getElementById('dc-roster-add-assignment');\n
				if (addBtn && typeof addBtn.focus === 'function') {\n
					addBtn.focus();\n
				}\n
			},\n
			onCancel: () => {\n
				clearAssignmentFormSuccess();\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
		const form = document.getElementById('dc-assignment-form');\n
		if (form && !form.dataset.dcModalSyncWired) {\n
			form.dataset.dcModalSyncWired = '1';\n
			form.addEventListener('change', () => {\n
				if (assignmentModalInstance?._open) {\n
					syncAssignmentModalPrimary(assignmentModalInstance);\n
				}\n
			});\n
		}\n
	}\n
\n
	async function openAssignmentEditModal(assignment, triggerEl) {\n
		if (!canAddAssignment() || !assignment?.id) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			return;\n
		}\n
		state.editingAssignmentId = Number(assignment.id);\n
		state.editingAssignmentVersion = Number(assignment.version ?? 0);\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = String(assignment.id);\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput) {\n
			dateInput.value = String(assignment.dutyDate || '');\n
		}\n
		refreshAssignmentFormEligibility();\n
		const emp = document.getElementById('dc-assignment-employee');\n
		if (emp) {\n
			emp.value = String(assignment.employeeId || '');\n
		}\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (loc) {\n
			loc.value = String(assignment.locationId || '');\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		if (start) {\n
			start.value = String(assignment.startTime || '').slice(0, 5);\n
		}\n
		const end = document.getElementById('dc-assignment-end');\n
		if (end) {\n
			end.value = String(assignment.endTime || '').slice(0, 5);\n
		}\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(assignment.breakMinutes ?? 0);\n
		}\n
		const note = document.getElementById('dc-assignment-note');\n
		if (note) {\n
			note.value = String(assignment.note || '');\n
		}\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Edit assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save changes'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => performAssignmentSave(),\n
			onClose: () => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				clearAssignmentFormSuccess();\n
				if (triggerEl && typeof triggerEl.focus === 'function') {\n
					triggerEl.focus();\n
				}\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
	}\n
\n
	async function cancelAssignmentRow(assignment, triggerEl) {\n
		if (!assignment?.id || !canAddAssignment()) {\n
			return;\n
		}\n
		const ok = window.confirm(\n
			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),\n
		);\n
		if (!ok) {\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});\n
			render(response?.data || {});\n
			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));\n
			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		} finally {\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
		}\n
	}\n
\n
	let templatesInflight = null;\n
\n
	async function ensureAssignmentModalPrereqs() {\n
		const jobs = [loadTemplates()];\n
		if (state.planningDefaultsFresh !== true) {\n
			jobs.push(refreshPlanningDefaultFromServer());\n
		}\n
		await Promise.all(jobs);\n
	}\n
\n
	async function loadTemplates() {\n
		if (state.templatesLoaded) {\n
			fillTemplateSelect();\n
			return;\n
		}\n
		if (templatesInflight) {\n
			await templatesInflight;\n
			fillTemplateSelect();\n
			return;\n
		}\n
		templatesInflight = (async () => {\n
			try {\n
				const response = await Api.get('/apps/dutycheck/api/templates', {});\n
				state.templates = Array.isArray(response?.data) ? response.data : [];\n
				state.templatesLoaded = true;\n
			} catch (_) {\n
				state.templates = [];\n
				state.templatesLoaded = false;\n
			} finally {\n
				templatesInflight = null;\n
			}\n
		})();\n
		await templatesInflight;\n
		fillTemplateSelect();\n
	}\n
\n
	function fillTemplateSelect() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select) {\n
			return;\n
		}\n
		const current = select.value;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));\n
		for (const tpl of state.templates) {\n
			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;\n
			select.appendChild(create('option', { value: String(tpl.id), text: label }));\n
		}\n
		if (current && [...select.options].some((o) => o.value === current)) {\n
			select.value = current;\n
		}\n
	}\n
\n
	function applySelectedTemplate() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select || !select.value) {\n
			return;\n
		}\n
		const tpl = state.templates.find((t) => String(t.id) === String(select.value));\n
		if (!tpl) {\n
			return;\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		const end = document.getElementById('dc-assignment-end');\n
		const br = document.getElementById('dc-assignment-break');\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (start) start.value = String(tpl.startTime || '').slice(0, 5);\n
		if (end) end.value = String(tpl.endTime || '').slice(0, 5);\n
		if (br) br.value = String(tpl.breakMinutes ?? 0);\n
		if (loc && tpl.locationId) loc.value = String(tpl.locationId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		refreshAssignmentFormEligibility();\n
	}\n
\n
	function refreshAcknowledgeStats(periodId) {\n
		const el = document.getElementById('dc-roster-ack-stats');\n
		if (!el) {\n
			return;\n
		}\n
		const id = Number(periodId);\n
		if (!Number.isInteger(id) || id <= 0) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === id);\n
		const status = String(period?.status || '').toLowerCase();\n
		if (status !== 'published' && status !== 'closed') {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const rows = Array.isArray(state.assignments) ? state.assignments : [];\n
		let total = 0;\n
		let acked = 0;\n
		for (const row of rows) {\n
			if (String(row?.status || '').toLowerCase() === 'cancelled') {\n
				continue;\n
			}\n
			total += 1;\n
			if (row?.acknowledged || row?.acknowledgedAt) {\n
				acked += 1;\n
			}\n
		}\n
		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;\n
		el.hidden = false;\n
		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')\n
			.replace('{acked}', String(acked))\n
			.replace('{total}', String(total))\n
			.replace('{pct}', String(pct));\n
	}\n
\n
	function fillCopySourceSelect(selectedPeriodId) {\n
		const select = document.getElementById('dc-roster-copy-source');\n
		const wrap = document.getElementById('dc-roster-copy-period');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!select || !wrap) {\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));\n
		const open = period && String(period.status || '').toLowerCase() === 'open';\n
		wrap.hidden = !open;\n
		state.copyPreviewReady = false;\n
		if (applyBtn) applyBtn.disabled = true;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));\n
		for (const p of state.periods) {\n
			if (Number(p.id) === Number(selectedPeriodId)) {\n
				continue;\n
			}\n
			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;\n
			select.appendChild(create('option', { value: String(p.id), text: label }));\n
		}\n
	}\n
\n
	async function runCopyPeriod(dryRun) {\n
		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);\n
		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);\n
		const status = document.getElementById('dc-roster-copy-status');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!targetId || !sourceId) {\n
			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {\n
				sourcePeriodId: sourceId,\n
				dryRun: !!dryRun,\n
			});\n
			const data = response?.data || {};\n
			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);\n
			const skipped = Number(data.skipped ?? 0);\n
			const msg = dryRun\n
				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')\n
					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))\n
					.replace('{skipped}', String(data.wouldSkip ?? skipped))\n
				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')\n
					.replace('{created}', String(created));\n
			if (status) {\n
				status.hidden = false;\n
				status.textContent = msg;\n
			}\n
			Msg.announce(msg, dryRun ? 'info' : 'success');\n
			if (dryRun) {\n
				state.copyPreviewReady = true;\n
				if (applyBtn) applyBtn.disabled = false;\n
			} else {\n
				state.copyPreviewReady = false;\n
				if (applyBtn) applyBtn.disabled = true;\n
				render(data.roster || data || {});\n
				if (!data.roster && data.assignments) {\n
					render(data);\n
				} else if (!data.roster) {\n
					await loadRoster(targetId);\n
				}\n
			}\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	function updateAdminExportLinks(selectedPeriodId) {\n
		const csv = document.getElementById('dc-roster-export-csv');\n
		const printLink = document.getElementById('dc-roster-export-print');\n
		if (!csv || !printLink) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		const baseCsv = String(urls.rosterExportCsv || '');\n
		const basePrint = String(urls.rosterPrint || '');\n
		const id = Number(selectedPeriodId);\n
		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;\n
		if (!ok) {\n
			csv.href = '#dc-roster-period-section';\n
			printLink.href = '#dc-roster-period-section';\n
			csv.setAttribute('aria-disabled', 'true');\n
			printLink.setAttribute('aria-disabled', 'true');\n
			return;\n
		}\n
		const q = new URLSearchParams({ periodId: String(id) }).toString();\n
		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;\n
		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;\n
		csv.removeAttribute('aria-disabled');\n
		printLink.removeAttribute('aria-disabled');\n
	}\n
\n
	function wireAdminExportGuards() {\n
		const guard = (event) => {\n
			const el = event.currentTarget;\n
			if (el && el.getAttribute('aria-disabled') === 'true') {\n
				event.preventDefault();\n
				Msg.announce(\n
					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),\n
					'warning',\n
				);\n
			}\n
		};\n
		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);\n
		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);\n
	}\n
\n
	let rosterPhpL10nCache;\n
	function getRosterPhpL10n() {\n
		if (rosterPhpL10nCache !== undefined) {\n
			return rosterPhpL10nCache;\n
		}\n
		const el = document.getElementById('dc-roster-php-l10n');\n
		const raw = el && el.dataset ? el.dataset.l10n : '';\n
		if (!raw) {\n
			rosterPhpL10nCache = null;\n
			return null;\n
		}\n
		try {\n
			rosterPhpL10nCache = JSON.parse(raw) || null;\n
		} catch (_) {\n
			rosterPhpL10nCache = null;\n
		}\n
		return rosterPhpL10nCache;\n
	}\n
\n
	function translateConflictMessage(message) {\n
		const raw = String(message || '');\n
		if (!raw) {\n
			return t('dutycheck', 'Unknown conflict');\n
		}\n
		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];\n
		if (fromPhp) {\n
			return fromPhp;\n
		}\n
		return t('dutycheck', raw);\n
	}\n
\n
	function render(data) {\n
		state.lastRosterData = data;\n
		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {\n
			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);\n
			state.planningDefaultsFresh = true;\n
		}\n
		state.periods = data.periods || [];\n
		state.employees = data.employees || [];\n
		state.locations = data.locations || [];\n
		state.assignments = data.assignments || [];\n
		state.absenceBlocks = data.absenceBlocks || [];\n
		state.canCreateAssignments = Boolean(data.canCreateAssignments);\n
		fillPeriodSwitcher(state.periods, data.selectedPeriodId);\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden) {\n
			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));\n
		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {\n
			dateInput.min = String(selectedPeriod.startDate);\n
			dateInput.max = String(selectedPeriod.endDate);\n
			if (!dateInput.value && state.canCreateAssignments) {\n
				dateInput.value = String(selectedPeriod.startDate);\n
			}\n
		} else if (dateInput) {\n
			dateInput.removeAttribute('min');\n
			dateInput.removeAttribute('max');\n
		}\n
		fillSelect('dc-assignment-location', state.locations, {\n
			emptyText: t('dutycheck', 'No active locations yet'),\n
		});\n
		updateLocationTimezoneHint();\n
		refreshAssignmentFormEligibility();\n
		renderAssignments(data.assignments || []);\n
		renderConflicts(data.conflicts || []);\n
		renderSetupCallout(data);\n
		updateAddAssignmentControl();\n
		updateAdminExportLinks(data.selectedPeriodId);\n
		fillCopySourceSelect(data.selectedPeriodId);\n
		refreshAcknowledgeStats(data.selectedPeriodId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
	}\n
\n
	function selectedPeriodIdFromUrl() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const raw = params.get('periodId');\n
		const parsed = Number(raw);\n
		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;\n
	}\n
\n
	function updateUrlPeriodId(periodId) {\n
		const params = new URLSearchParams(window.location.search || '');\n
		if (periodId && Number(periodId) > 0) {\n
			params.set('periodId', String(periodId));\n
		} else {\n
			params.delete('periodId');\n
		}\n
		const query = params.toString();\n
		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);\n
	}\n
\n
	async function loadRoster(periodId) {\n
		if (C.isModalOpen?.()) {\n
			C.dismissOpenModal();\n
		} else {\n
			restoreAssignmentFormPanel();\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		C.setLoadingRow(tbody, 7);\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });\n
			render(response?.data || {});\n
			updateUrlPeriodId(response?.data?.selectedPeriodId || null);\n
			void loadRosterSignals(response?.data || {});\n
			void loadCoverageStrip(response?.data?.selectedPeriodId);\n
			return response?.data || {};\n
		} catch (err) {\n
			const code = String(err?.payload?.error?.code || err?.code || '');\n
			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {\n
				try {\n
					const fallback = await Api.get('/apps/dutycheck/api/roster', {});\n
					render(fallback?.data || {});\n
					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);\n
					void loadRosterSignals(fallback?.data || {});\n
					void loadCoverageStrip(fallback?.data?.selectedPeriodId);\n
					Msg.announce(\n
						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),\n
						'warning',\n
					);\n
					return fallback?.data || {};\n
				} catch (retryErr) {\n
					Msg.handleApiError(retryErr);\n
					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
					return null;\n
				}\n
			}\n
			Msg.handleApiError(err);\n
			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
			return null;\n
		} finally {\n
			C.clearLoadingRow(tbody);\n
		}\n
	}\n
\n
	function dateOverlapsBlackout(day, blackout) {\n
		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);\n
		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);\n
		if (!start || !end) return false;\n
		return day >= start && day <= end;\n
	}\n
\n
	async function loadRosterSignals(data) {\n
		state.preferencesByEmployee = {};\n
		state.blackoutsByEmployeeDate = {};\n
		const employees = data.employees || state.employees || [];\n
		const period = (data.periods || state.periods || []).find(\n
			(p) => Number(p.id) === Number(data.selectedPeriodId),\n
		);\n
		const from = period?.startDate || period?.start_date || '';\n
		const to = period?.endDate || period?.end_date || '';\n
		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);\n
		if (!ids.length) return;\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/roster/signals', {\n
				employeeIds: ids,\n
				from,\n
				to,\n
			});\n
			const prefs = res?.data?.preferences || [];\n
			const blackouts = res?.data?.blackouts || [];\n
			prefs.forEach((p) => {\n
				const eid = String(p.employeeId || p.employee_id || '');\n
				if (!eid) return;\n
				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];\n
				state.preferencesByEmployee[eid].push(p);\n
			});\n
			const days = [];\n
			if (from && to) {\n
				const cur = new Date(from + 'T12:00:00');\n
				const end = new Date(to + 'T12:00:00');\n
				while (cur <= end) {\n
					const pad = (n) => String(n).padStart(2, '0');\n
					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);\n
					cur.setDate(cur.getDate() + 1);\n
				}\n
			}\n
			blackouts.forEach((b) => {\n
				const eid = Number(b.employeeId || b.employee_id || 0);\n
				if (!eid) return;\n
				days.forEach((day) => {\n
					if (dateOverlapsBlackout(day, b)) {\n
						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;\n
					}\n
				});\n
			});\n
			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {\n
				paintGridWindow({ force: true });\n
			}\n
		} catch (_) {\n
			// Optional enrichment — ignore failures.\n
		}\n
	}\n
\n
	async function loadCoverageStrip(periodId) {\n
		const strip = document.getElementById('dc-coverage-strip');\n
		const text = document.getElementById('dc-coverage-strip-text');\n
		if (!strip || !text) return;\n
		const id = Number(periodId || 0);\n
		if (!id) {\n
			strip.hidden = true;\n
			return;\n
		}\n
		try {\n
			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);\n
			const d = res?.data || {};\n
			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);\n
			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);\n
			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);\n
			let message;\n
			if (ready || (hard === 0 && soft === 0)) {\n
				message = t('dutycheck', 'Ready to publish — no blocking issues.');\n
				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--ok');\n
			} else if (hard > 0) {\n
				message = t('dutycheck', '{n} must-fix issues before publish.')\n
					.replace('{n}', String(hard));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');\n
				strip.classList.add('dc-coverage-strip--critical');\n
			} else {\n
				message = t('dutycheck', '{n} items need confirmation.')\n
					.replace('{n}', String(soft));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--warn');\n
			}\n
			text.textContent = message;\n
			strip.hidden = false;\n
		} catch (_) {\n
			strip.hidden = true;\n
		}\n
	}\n
\n
	function currentSuggestLocationId() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const fromUrl = Number(params.get('locationId') || 0);\n
		if (Number.isInteger(fromUrl) && fromUrl > 0) {\n
			return fromUrl;\n
		}\n
		const filterEl = document.getElementById('dc-roster-location')\n
			|| document.getElementById('dc-assignment-location');\n
		const fromUi = Number(filterEl?.value || 0);\n
		if (Number.isInteger(fromUi) && fromUi > 0) {\n
			return fromUi;\n
		}\n
		return null;\n
	}\n
\n
	async function runSuggestFill() {\n
		const periodId = Number(selectedPeriodId() || 0);\n
		if (!periodId) {\n
			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');\n
			return;\n
		}\n
		const btn = document.getElementById('dc-roster-suggest-fill');\n
		if (btn) {\n
			btn.disabled = true;\n
			btn.setAttribute('aria-busy', 'true');\n
		}\n
		const locId = currentSuggestLocationId();\n
		const suggestBody = { locationId: locId || null };\n
		try {\n
			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);\n
			const d = preview?.data || {};\n
			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {\n
				const noLoc = Number(d.skippedNoLocation ?? 0);\n
				Msg.announce(\n
					noLoc > 0\n
						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')\n
						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),\n
					'warning',\n
				);\n
				return;\n
			}\n
			const body = create('div', { class: 'dc-suggest-preview' }, [\n
				create('p', {\n
					text: t('dutycheck', 'Would create {n} shifts from patterns.')\n
						.replace('{n}', String(d.created ?? 0)),\n
				}),\n
				create('ul', { class: 'dc-suggest-preview__counts' }, [\n
					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),\n
				]),\n
			]);\n
			const confirmed = await new Promise((resolve) => {\n
				let settled = false;\n
				const finish = (value) => {\n
					if (settled) return;\n
					settled = true;\n
					resolve(value);\n
				};\n
				C.openModal({\n
					title: t('dutycheck', 'Suggest fill preview'),\n
					primaryLabel: t('dutycheck', 'Apply suggest fill'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					render: () => body,\n
					onSubmit: async () => {\n
						finish(true);\n
						return true;\n
					},\n
					onCancel: () => finish(false),\n
					onClose: () => finish(false),\n
				});\n
			});\n
			if (!confirmed) return;\n
			if ((d.created ?? 0) < 1) {\n
				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');\n
				return;\n
			}\n
			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);\n
			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');\n
			await loadRoster(periodId);\n
		} catch (err) {\n
			const code = String(err?.code || err?.payload?.error?.code || '');\n
			if (code === 'ROTATION_DISABLED') {\n
				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');\n
			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {\n
				Msg.announce(\n
					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),\n
					'warning',\n
				);\n
			} else if (code === 'RATE_LIMITED') {\n
				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');\n
			} else {\n
				Msg.handleApiError(err);\n
			}\n
		} finally {\n
			if (btn) {\n
				btn.disabled = false;\n
				btn.removeAttribute('aria-busy');\n
			}\n
		}\n
	}\n
\n
	function clearAssignmentFormFields() {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) return;\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		const periodVal = periodHidden ? String(periodHidden.value || '') : '';\n
		form.reset();\n
		if (periodHidden && periodVal) {\n
			periodHidden.value = periodVal;\n
		}\n
		applyDefaultBreakToForm();\n
		D?.applyLocaleToTemporalInputs(form);\n
		refreshAssignmentFormEligibility();\n
		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');\n
		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {\n
			showAssignmentFormSuccess(clearedMsg, 'info');\n
		}\n
		Msg.announce(clearedMsg, 'info');\n
	}\n
\n
	function readForm(form) {\n
		const data = new FormData(form);\n
		const rawStart = String(data.get('startTime') || '');\n
		const rawEnd = String(data.get('endTime') || '');\n
		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();\n
		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();\n
		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);\n
		const out = {\n
			periodId: Number(data.get('periodId')),\n
			employeeId: Number(data.get('employeeId')),\n
			locationId: Number(data.get('locationId')),\n
			dutyDate: String(data.get('dutyDate') || ''),\n
			startTime,\n
			endTime,\n
			breakMinutes: Number(data.get('breakMinutes') || 0),\n
			note: String(data.get('note') || '').trim(),\n
			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,\n
		};\n
		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {\n
			out.expectedVersion = Number(state.editingAssignmentVersion);\n
		}\n
		return out;\n
	}\n
\n
	/**\n
	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.\n
	 */\n
	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;\n
		if (start === null || end === null) return null;\n
		let endAdj = end;\n
		if (endAdj <= start) {\n
			endAdj += 24 * 60;\n
		}\n
		return (endAdj - start) - breakMinutes;\n
	}\n
\n
	function validate(payload) {\n
		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {\n
			return t('dutycheck', 'Select a period before saving.');\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		if (!period) {\n
			return t('dutycheck', 'That planning period was not found.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {\n
			return t('dutycheck', 'Select an employee.');\n
		}\n
		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {\n
			return t('dutycheck', 'Select a location.');\n
		}\n
		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {\n
			return t('dutycheck', 'Date, start time, and end time are required.');\n
		}\n
		if (period.startDate && period.endDate\n
			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {\n
			return t('dutycheck', 'Date must fall within the selected period.');\n
		}\n
		if (payload.startTime === payload.endTime) {\n
			return t(\n
				'dutycheck',\n
				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
			);\n
		}\n
		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {\n
			return t('dutycheck', 'Break minutes must be between 0 and 720.');\n
		}\n
		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
		if (effective === null) {\n
			return t('dutycheck', 'Please check the dates and times.');\n
		}\n
		if (effective <= 0) {\n
			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
		}\n
		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			return t('dutycheck', 'This employee has approved leave on the selected date.');\n
		}\n
		if (findOverlapAssignment(payload)) {\n
			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');\n
		}\n
		return null;\n
	}\n
\n
	function errorMessageFor(error) {\n
		const code = errorCodeFrom(error);\n
		switch (code) {\n
			case 'PERIOD_NOT_OPEN':\n
				return t('dutycheck', 'The selected period is not open for planning.');\n
			case 'PERIOD_NOT_FOUND':\n
				return t('dutycheck', 'That planning period was not found.');\n
			case 'DATE_OUTSIDE_PERIOD':\n
				return t('dutycheck', 'Date must fall within the selected period.');\n
			case 'EMPLOYEE_NOT_FOUND':\n
				return t('dutycheck', 'Employee not found or no longer available.');\n
			case 'LOCATION_NOT_FOUND':\n
				return t('dutycheck', 'Location not found or no longer available.');\n
			case 'EQUAL_DUTY_TIMES':\n
				return t(\n
					'dutycheck',\n
					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
				);\n
			case 'INVALID_SHIFT_LENGTH':\n
				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
			case 'INVALID_BREAK_MINUTES':\n
				return t('dutycheck', 'Break minutes are out of the allowed range.');\n
			case 'NOTE_TOO_LONG':\n
				return t('dutycheck', 'Note is too long (maximum 512 characters).');\n
			case 'ASSIGNMENT_OVERLAP':\n
				return t('dutycheck', 'This employee already has an overlapping assignment.');\n
			case 'ASSIGNMENT_DUPLICATE_SLOT':\n
				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');\n
			case 'STALE_VERSION':\n
				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');\n
			case 'CONFLICT_ACK_STALE':\n
				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');\n
			case 'COMPANY_MEMBERSHIP_REQUIRED':\n
				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');\n
			case 'EXPECTED_VERSION_REQUIRED':\n
				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');\n
			case 'ABSENCE_CONFLICT':\n
				return t('dutycheck', 'This employee has an approved absence on that date.');\n
			case 'BLACKOUT_CONFLICT':\n
				return t('dutycheck', 'This employee marked that they cannot work during this time.');\n
			case 'CONFLICT_ACK_REQUIRED':\n
				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');\n
			case 'REASON_TOO_SHORT':\n
				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');\n
			case 'INTERNAL_ERROR':\n
				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');\n
			case 'QUALIFICATION_MISSING':\n
				return t('dutycheck', 'This employee is missing a required qualification for that location.');\n
			case 'ASSIGNMENT_NOT_FOUND':\n
				return t('dutycheck', 'That assignment no longer exists. Reload the page.');\n
			case 'SCHEMA_NOT_READY':\n
				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');\n
			case 'INVALID_DATE':\n
			case 'INVALID_TIME':\n
				return t('dutycheck', 'Please check the dates and times.');\n
			case 'PERIOD_ID_REQUIRED':\n
				return t('dutycheck', 'Select a period before saving.');\n
			case 'EMPLOYEE_ID_REQUIRED':\n
				return t('dutycheck', 'Select an employee.');\n
			case 'LOCATION_ID_REQUIRED':\n
				return t('dutycheck', 'Select a location.');\n
			default:\n
				return null;\n
		}\n
	}\n
\n
\n
	async function loadPendingSwaps() {\n
		const list = document.getElementById('dc-swap-list');\n
		const empty = document.getElementById('dc-swap-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/swaps');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				li.appendChild(create('p', {\n
					text: row.toEmployeeId\n
						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId))\n
							.replace('{to}', String(row.toEmployeeId))\n
						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId)),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });\n
						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');\n
						await loadPendingSwaps();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });\n
						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');\n
						await loadPendingSwaps();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load swap requests.');\n
			}\n
		}\n
	}\n
\n
	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {\n
		try {\n
			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {\n
				acknowledgements,\n
			});\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Approve with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return approveOpenShiftClaim(openShiftId, acks, false);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	async function loadPendingOpenClaims() {\n
		const list = document.getElementById('dc-open-claim-list');\n
		const empty = document.getElementById('dc-open-claim-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;\n
				li.appendChild(create('p', {\n
					text: t('dutycheck', '{date} · employee #{emp}')\n
						.replace('{date}', String(when))\n
						.replace('{emp}', String(row.claimedByEmployeeId || '—')),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await approveOpenShiftClaim(row.id);\n
						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');\n
						await loadPendingOpenClaims();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});\n
						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');\n
						await loadPendingOpenClaims();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load pending claims.');\n
			}\n
		}\n
	}\n
\n
	function wireMarketplace() {\n
		const toggle = document.getElementById('dc-open-shift-create');\n
		const form = document.getElementById('dc-open-shift-form');\n
		toggle?.addEventListener('click', () => {\n
			if (!form) return;\n
			form.hidden = !form.hidden;\n
			const loc = document.getElementById('dc-os-location');\n
			if (loc && state.locations?.length) {\n
				loc.replaceChildren();\n
				for (const l of state.locations) {\n
					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));\n
				}\n
			}\n
			const period = selectedPeriodFromState();\n
			const date = document.getElementById('dc-os-date');\n
			if (date && period?.startDate) date.value = String(period.startDate);\n
		});\n
		document.getElementById('dc-os-save')?.addEventListener('click', async () => {\n
			const period = selectedPeriodFromState();\n
			if (!period?.id) {\n
				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');\n
				return;\n
			}\n
			try {\n
				await Api.post('/apps/dutycheck/api/open-shifts', {\n
					periodId: Number(period.id),\n
					locationId: Number(document.getElementById('dc-os-location')?.value || 0),\n
					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),\n
					startTime: String(document.getElementById('dc-os-start')?.value || ''),\n
					endTime: String(document.getElementById('dc-os-end')?.value || ''),\n
					breakMinutes: 0,\n
				});\n
				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');\n
				if (form) form.hidden = true;\n
			} catch (err) {\n
				Msg.handleApiError(err);\n
			}\n
		});\n
	}\n
\n
	document.addEventListener('DOMContentLoaded', async () => {\n
		D?.applyLocaleToTemporalInputs(document);\n
		wireBreakPrefillHint();\n
		window.addEventListener('dc-planning-defaults-changed', (event) => {\n
			const minutes = Number(event?.detail?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
		});\n
		wireAdminExportGuards();\n
		wireMarketplace();\n
		setRosterView('grid');\n
		wireRosterVirtualization();\n
		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));\n
		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));\n
		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {\n
			gridState.selected.clear();\n
			syncVisibleGridChrome();\n
			updateBulkBar();\n
		});\n
		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {\n
			void applyBulkFromTemplate();\n
		});\n
		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {\n
			void runSuggestFill();\n
		});\n
		await Promise.all([\n
			loadRoster(selectedPeriodIdFromUrl()),\n
			loadPendingSwaps(),\n
			loadPendingOpenClaims(),\n
		]);\n
\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		switcher?.addEventListener('change', async () => {\n
			clearAssignmentsSectionSuccess();\n
			const periodId = Number(switcher.value);\n
			await Promise.all([\n
				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),\n
				loadPendingSwaps(),\n
				loadPendingOpenClaims(),\n
			]);\n
		});\n
\n
		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {\n
			const btn = event.target.closest('.dc-roster-add-assignment-trigger');\n
			if (!btn) {\n
				return;\n
			}\n
			openAssignmentCreateModal(btn);\n
		});\n
\n
		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {\n
			clearAssignmentFormFields();\n
		});\n
\n
		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {\n
			applySelectedTemplate();\n
		});\n
		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {\n
			runCopyPeriod(true);\n
		});\n
		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {\n
			if (!state.copyPreviewReady) {\n
				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');\n
				return;\n
			}\n
			await runCopyPeriod(false);\n
		});\n
		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {\n
			state.copyPreviewReady = false;\n
			const applyBtn = document.getElementById('dc-roster-copy-apply');\n
			if (applyBtn) applyBtn.disabled = true;\n
		});\n
\n
		const form = document.getElementById('dc-assignment-form');\n
		const scheduleEligibilityRefresh = () => {\n
			clearAssignmentFormSuccess();\n
			clearAssignmentsSectionSuccess();\n
			refreshAssignmentFormEligibility();\n
		};\n
		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);\n
		form?.addEventListener('submit', (event) => {\n
			event.preventDefault();\n
			const dialog = form.closest('.dc-modal__dialog--roster-assignment');\n
			if (!dialog) {\n
				return;\n
			}\n
			const primary = dialog.querySelector('.dc-modal__actions .button.primary');\n
			if (primary && !primary.disabled) {\n
				primary.click();\n
			}\n
		});\n
	});\n
})();\n
' contains "employees.slice(range.start, range.end)".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:61

FAILURES!
Tests: 6, Assertions: 58, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.842098
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.232663
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.268419
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.229516
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.189657
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.199081
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.227954
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.252943
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.326729
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 5.531437
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.492152
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.116752
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.093826
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.090884
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 2.072938
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.157094
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.102259
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.349658
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.227196
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.096762
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.088738
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.523194
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.184033
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 2.352975
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /employees\.slice\(range\.start,\s*range\.end\)/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105627 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees;
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:250:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.190523
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.320484
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.524054
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 4.973873
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 102.978916
killed grid_paints_every_employee
== mutant list_paints_every_assignment (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

..F...                                                              6 / 6 (100%)

Time: 00:00.009, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testRosterJsWindowsRowsAndKeepsKeyboardReach
Failed asserting that '(function () {\n
	'use strict';\n
\n
	const Api = window.DutyCheckApi;\n
	const Msg = window.DutyCheckMessaging;\n
	const C = window.DutyCheckComponents || window.DutyCheckDom || {};\n
	const D = window.DutyCheckDates;\n
	const ConflictLabels = window.DutyCheckConflictLabels;\n
	const create = C.createElement;\n
	if (typeof create !== 'function') {\n
		throw new Error('DutyCheck components failed to load');\n
	}\n
\n
	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n
\n
	const state = {\n
		periods: [],\n
		employees: [],\n
		locations: [],\n
		assignments: [],\n
		absenceBlocks: [],\n
		canCreateAssignments: false,\n
		lastRosterData: null,\n
		defaultBreakMinutes: 0,\n
		planningDefaultsFresh: false,\n
		templates: [],\n
		templatesLoaded: false,\n
		editingAssignmentId: null,\n
		editingAssignmentVersion: null,\n
		copyPreviewReady: false,\n
		preferencesByEmployee: {},\n
		blackoutsByEmployeeDate: {},\n
	};\n
	let assignmentModalInstance = null;\n
	let suggestedBreakMinutes = 0;\n
\n
	function clampBreakMinutes(value) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n)) {\n
			return 0;\n
		}\n
		return Math.max(0, Math.min(720, Math.round(n)));\n
	}\n
\n
	function readStoredBreakMinutes() {\n
		try {\n
			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n
			if (raw === null || raw === '') {\n
				return null;\n
			}\n
			return clampBreakMinutes(raw);\n
		} catch (_) {\n
			return null;\n
		}\n
	}\n
\n
	function rememberBreakMinutes(minutes) {\n
		try {\n
			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n
		} catch (_) {\n
			/* ignore quota / privacy mode */\n
		}\n
	}\n
\n
	function resolveBreakPrefill() {\n
		const org = clampBreakMinutes(state.defaultBreakMinutes);\n
		// Organisation default from Settings always wins when it is greater than zero.\n
		if (org > 0) {\n
			return { minutes: org, source: 'settings' };\n
		}\n
		const stored = readStoredBreakMinutes();\n
		if (stored !== null) {\n
			return { minutes: stored, source: 'last' };\n
		}\n
		return { minutes: 0, source: 'settings' };\n
	}\n
\n
	async function refreshPlanningDefaultFromServer() {\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n
			const minutes = Number(response?.planning?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
			state.planningDefaultsFresh = true;\n
		} catch (_) {\n
			/* Fall back to defaultBreakMinutes from the last roster API load. */\n
		}\n
	}\n
\n
	function breakPrefillMessage(prefill) {\n
		const n = String(prefill.minutes);\n
		if (prefill.source === 'last') {\n
			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n
		}\n
		if (prefill.minutes === 0) {\n
			return t('dutycheck', 'Pre-filled with 0 min — no break.');\n
		}\n
		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n
	}\n
\n
	function updateBreakPrefillHint(userEdited) {\n
		const el = document.getElementById('dc-assignment-break-prefill');\n
		if (!el) {\n
			return;\n
		}\n
		if (userEdited) {\n
			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n
			return;\n
		}\n
		el.textContent = breakPrefillMessage(resolveBreakPrefill());\n
	}\n
\n
	function applyDefaultBreakToForm() {\n
		const prefill = resolveBreakPrefill();\n
		suggestedBreakMinutes = prefill.minutes;\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(prefill.minutes);\n
		}\n
		updateBreakPrefillHint(false);\n
	}\n
\n
	function wireBreakPrefillHint() {\n
		const br = document.getElementById('dc-assignment-break');\n
		if (!br || br.dataset.dcBreakPrefillWired) {\n
			return;\n
		}\n
		br.dataset.dcBreakPrefillWired = '1';\n
		br.addEventListener('input', () => {\n
			const current = clampBreakMinutes(br.value);\n
			updateBreakPrefillHint(current !== suggestedBreakMinutes);\n
		});\n
		br.addEventListener('change', () => {\n
			const current = clampBreakMinutes(br.value);\n
			if (current === suggestedBreakMinutes) {\n
				updateBreakPrefillHint(false);\n
			}\n
		});\n
	}\n
\n
	function dateToDayIndex(isoDate) {\n
		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n
		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n
			return null;\n
		}\n
		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n
	}\n
\n
	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n
		const dayIndex = dateToDayIndex(dutyDate);\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime);\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime);\n
		if (dayIndex === null || start === null || end === null) {\n
			return null;\n
		}\n
		const absoluteStart = (dayIndex * 1440) + start;\n
		let absoluteEnd = (dayIndex * 1440) + end;\n
		if (absoluteEnd <= absoluteStart) {\n
			absoluteEnd += 1440;\n
		}\n
		return [absoluteStart, absoluteEnd];\n
	}\n
\n
	function absoluteRangesOverlap(a, b) {\n
		return a[0] < b[1] && b[0] < a[1];\n
	}\n
\n
	function isDateInAbsenceBlock(employeeId, dutyDate) {\n
		if (!dutyDate) {\n
			return false;\n
		}\n
		for (const block of state.absenceBlocks) {\n
			if (Number(block?.employeeId) !== Number(employeeId)) {\n
				continue;\n
			}\n
			const start = String(block?.startDate || '');\n
			const end = String(block?.endDate || '');\n
			if (dutyDate >= start && dutyDate <= end) {\n
				return true;\n
			}\n
		}\n
		return false;\n
	}\n
\n
	function employeesAvailableOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return [];\n
		}\n
		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n
	}\n
\n
	function countEmployeesBlockedOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return 0;\n
		}\n
		return state.employees.length - employeesAvailableOnDate(dutyDate).length;\n
	}\n
\n
	function appendHintLink(parent, beforeText, linkText, afterText, href) {\n
		parent.appendChild(document.createTextNode(beforeText));\n
		if (href) {\n
			const link = create('a', {\n
				class: 'dc-hint-link',\n
				href,\n
				text: linkText,\n
			});\n
			parent.appendChild(link);\n
			parent.appendChild(document.createTextNode(afterText));\n
		} else {\n
			parent.appendChild(document.createTextNode(linkText + afterText));\n
		}\n
	}\n
\n
	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n
		const hint = document.getElementById('dc-assignment-employee-hint');\n
		if (!hint) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		hint.replaceChildren();\n
		if (!dutyDate) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n
			));\n
			return;\n
		}\n
		if (!available.length && blocked > 0) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n
					.replace('{n}', String(blocked)),\n
				t('dutycheck', 'review absences'),\n
				'.',\n
				String(urls.absences || ''),\n
			);\n
			return;\n
		}\n
		if (!available.length) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'No employees are available. Add an active employee on the '),\n
				t('dutycheck', 'Employees'),\n
				t('dutycheck', ' page first.'),\n
				String(urls.employees || ''),\n
			);\n
			return;\n
		}\n
		if (blocked > 0) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n
					.replace('{available}', String(available.length))\n
					.replace('{hidden}', String(blocked)),\n
			));\n
			return;\n
		}\n
		hint.appendChild(document.createTextNode(\n
			t('dutycheck', 'All {n} active employees can be scheduled on this date.')\n
				.replace('{n}', String(available.length)),\n
		));\n
	}\n
\n
	function refreshEmployeeSelectForDate() {\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const dutyDate = dateInput ? String(dateInput.value || '') : '';\n
		const available = employeesAvailableOnDate(dutyDate);\n
		const blocked = countEmployeesBlockedOnDate(dutyDate);\n
		setEmployeeAvailabilityHint(dutyDate, available, blocked);\n
		fillSelect('dc-assignment-employee', available, {\n
			emptyText: dutyDate\n
				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n
				: t('dutycheck', 'Pick a date first'),\n
		});\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		if (employeeSelect) {\n
			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n
		}\n
	}\n
\n
	function findOverlapAssignment(payload) {\n
		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n
		if (!candidate) {\n
			return null;\n
		}\n
		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		for (const assignment of state.assignments) {\n
			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n
				continue;\n
			}\n
			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n
				continue;\n
			}\n
			const existing = assignmentAbsoluteRange(\n
				String(assignment?.dutyDate || ''),\n
				String(assignment?.startTime || ''),\n
				String(assignment?.endTime || ''),\n
			);\n
			if (existing && absoluteRangesOverlap(candidate, existing)) {\n
				return assignment;\n
			}\n
		}\n
		return null;\n
	}\n
\n
	function setFormFeedbackA11y(el, isError) {\n
		if (!el) {\n
			return;\n
		}\n
		if (isError) {\n
			el.setAttribute('role', 'alert');\n
			el.setAttribute('aria-live', 'assertive');\n
		} else {\n
			el.setAttribute('role', 'status');\n
			el.setAttribute('aria-live', 'polite');\n
		}\n
	}\n
\n
	function focusFirstInvalidAssignmentField(payload) {\n
		if (!payload) {\n
			return;\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		const order = [\n
			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n
			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n
			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n
			{\n
				test: () => period?.startDate && period?.endDate && payload.dutyDate\n
					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n
				id: 'dc-assignment-date',\n
			},\n
			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },\n
			{ test: () => !payload.locationId, id: 'dc-assignment-location' },\n
			{ test: () => !payload.startTime, id: 'dc-assignment-start' },\n
			{ test: () => !payload.endTime, id: 'dc-assignment-end' },\n
			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },\n
			{\n
				test: () => {\n
					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
					return effective === null || effective <= 0;\n
				},\n
				id: 'dc-assignment-end',\n
			},\n
		];\n
		for (const item of order) {\n
			if (item.test()) {\n
				document.getElementById(item.id)?.focus();\n
				return;\n
			}\n
		}\n
	}\n
\n
	function selectedPeriodForPayload(payload) {\n
		const periodId = Number(payload?.periodId);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function clearAssignmentFormSuccess() {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		if (!el || el.dataset.dcSuccess !== '1') {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.dataset.dcSuccess = '';\n
		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
		setFormFeedbackA11y(el, false);\n
	}\n
\n
	function showAssignmentFormSuccess(message, kind) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		const tone = kind === 'info' ? 'info' : 'success';\n
		el.hidden = false;\n
		el.textContent = String(message);\n
		el.dataset.dcSuccess = '1';\n
		el.classList.remove('dc-roster-form__feedback--error');\n
		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');\n
		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');\n
		setFormFeedbackA11y(el, false);\n
		if (submit) {\n
			submit.disabled = submit.dataset.dcLockedBySetup === '1';\n
		}\n
		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });\n
	}\n
\n
	function labelsForAssignmentPayload(payload) {\n
		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));\n
		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		const locationSelect = document.getElementById('dc-assignment-location');\n
		const employeeName = employee\n
			? String(employee.name || employee.displayName || '')\n
			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		const locationName = location\n
			? String(location.name || '')\n
			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		return { employeeName, locationName };\n
	}\n
\n
	function buildAssignmentSavedMessage(payload) {\n
		const { employeeName, locationName } = labelsForAssignmentPayload(payload);\n
		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;\n
		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)\n
			|| `${payload.startTime} – ${payload.endTime}`;\n
		return t(\n
			'dutycheck',\n
			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',\n
		)\n
			.replace('{employee}', employeeName || '—')\n
			.replace('{date}', String(dateLabel || ''))\n
			.replace('{times}', String(times))\n
			.replace('{location}', locationName || '—');\n
	}\n
\n
	function updateAssignmentFormFeedback(payload) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		if (el.dataset.dcSuccess === '1') {\n
			return;\n
		}\n
		if (typeof payload === 'string') {\n
			el.hidden = false;\n
			el.textContent = payload;\n
			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			el.classList.add('dc-roster-form__feedback--error');\n
			setFormFeedbackA11y(el, true);\n
			if (submit) {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		if (!payload) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			return;\n
		}\n
		let message = '';\n
		let isError = false;\n
		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');\n
			isError = true;\n
		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {\n
			const overlap = findOverlapAssignment(payload);\n
			if (overlap) {\n
				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;\n
				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)\n
					|| `${overlap.startTime} – ${overlap.endTime}`;\n
				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')\n
					.replace('{date}', String(when))\n
					.replace('{times}', String(times));\n
				isError = true;\n
			}\n
		}\n
		if (!message) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			if (submit && submit.dataset.dcLockedBySetup !== '1') {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		el.hidden = false;\n
		el.textContent = message;\n
		el.classList.toggle('dc-roster-form__feedback--error', isError);\n
		setFormFeedbackA11y(el, isError);\n
		if (submit && isError) {\n
			submit.disabled = true;\n
		}\n
	}\n
\n
	function refreshAssignmentFormEligibility() {\n
		refreshEmployeeSelectForDate();\n
		updateLocationTimezoneHint();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		updateAssignmentFormFeedback(readForm(form));\n
	}\n
\n
	function updateLocationTimezoneHint() {\n
		const hint = document.getElementById('dc-assignment-location-tz-hint');\n
		const select = document.getElementById('dc-assignment-location');\n
		if (!hint || !select) {\n
			return;\n
		}\n
		const locationId = Number(select.value);\n
		const location = state.locations.find((row) => Number(row.id) === locationId);\n
		const tz = location && String(location.timezone || '').trim();\n
		if (!tz) {\n
			hint.hidden = true;\n
			hint.replaceChildren();\n
			return;\n
		}\n
		const accountTz = D?.currentTimezone?.() || '';\n
		hint.textContent = t(\n
			'dutycheck',\n
			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',\n
		)\n
			.replace('{locationTimezone}', tz)\n
			.replace('{accountTimezone}', accountTz || 'UTC');\n
		hint.hidden = false;\n
	}\n
\n
	function fillSelect(id, items, options) {\n
		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});\n
		const select = document.getElementById(id);\n
		if (!select) return;\n
		const previous = String(select.value || '');\n
		select.replaceChildren();\n
		if (!items.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = opts.emptyText;\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const item of items) {\n
			const option = document.createElement('option');\n
			option.value = String(item.id);\n
			option.textContent = String(item[opts.labelKey] || item.name || item.id);\n
			select.appendChild(option);\n
		}\n
		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {\n
			select.value = previous;\n
		}\n
	}\n
\n
	function fillPeriodSwitcher(periods, selected) {\n
		const select = document.getElementById('dc-roster-period-switcher');\n
		if (!select) return;\n
		const statusLabelMap = {\n
			open: t('dutycheck', 'Open'),\n
			published: t('dutycheck', 'Published'),\n
			closed: t('dutycheck', 'Closed'),\n
		};\n
		select.replaceChildren();\n
		if (!periods.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = t('dutycheck', 'No periods yet');\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const period of periods) {\n
			const option = document.createElement('option');\n
			option.value = String(period.id);\n
			const start = D?.formatDisplayDate(period.startDate) || period.startDate;\n
			const end = D?.formatDisplayDate(period.endDate) || period.endDate;\n
			const statusKey = String(period.status || '').toLowerCase();\n
			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();\n
			option.textContent = `${start} – ${end} · ${status}`;\n
			select.appendChild(option);\n
		}\n
		if (selected) {\n
			select.value = String(selected);\n
		}\n
	}\n
\n
	function severityBadge(severity) {\n
		const sev = String(severity || 'info');\n
		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;\n
		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });\n
		badge.setAttribute('aria-label', label);\n
		return badge;\n
	}\n
\n
	function renderConflicts(conflicts) {\n
		const list = document.getElementById('dc-conflict-list');\n
		const summary = document.getElementById('dc-conflict-summary');\n
		if (!list) return;\n
		list.setAttribute('role', 'list');\n
		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));\n
		list.replaceChildren();\n
		const hard = conflicts.filter((c) => c?.severity === 'hard').length;\n
		const soft = conflicts.filter((c) => c?.severity === 'soft').length;\n
		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;\n
		if (summary) {\n
			summary.textContent = !conflicts.length\n
				? t('dutycheck', 'No planning issues found.')\n
				: (ConflictLabels\n
					? ConflictLabels.countsSummary(hard, soft, softUnack)\n
					: String(hard));\n
		}\n
		if (!conflicts.length) {\n
			return;\n
		}\n
		for (const conflict of conflicts) {\n
			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });\n
			li.appendChild(severityBadge(conflict?.severity));\n
			const conflictMessage = String(conflict?.message || '');\n
			const titleText = conflictMessage\n
				? translateConflictMessage(conflictMessage)\n
				: t('dutycheck', 'Unknown conflict');\n
			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;\n
			const body = create('div', { class: 'dc-conflict__body' });\n
			const title = create('span', { class: 'dc-conflict__title', text: titleText });\n
			title.id = titleId;\n
			body.appendChild(title);\n
			li.setAttribute('aria-labelledby', titleId);\n
			if (conflict?.acknowledged && conflict?.ackReason) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack',\n
					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),\n
				}));\n
			} else if (conflict?.ackInvalidated) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack-invalid',\n
					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),\n
				}));\n
			}\n
			li.appendChild(body);\n
\n
			const actions = create('div', { class: 'dc-conflict__actions' });\n
			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {\n
				const ackBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Confirm'),\n
				});\n
				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));\n
				actions.appendChild(ackBtn);\n
			}\n
			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();\n
			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {\n
				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()\n
					|| t('dutycheck', 'Open ArbeitszeitCheck');\n
				const recoveryLink = create('a', {\n
					class: 'button primary',\n
					href: recoveryUrl,\n
					text: recoveryLabel,\n
					attrs: {\n
						target: '_blank',\n
						rel: 'noopener noreferrer',\n
						'aria-label': recoveryLabel,\n
					},\n
				});\n
				actions.appendChild(recoveryLink);\n
			}\n
			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];\n
			if (conflict?.type === 'absence_collision' && assignmentIds.length) {\n
				const focusBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Show assignment'),\n
				});\n
				focusBtn.addEventListener('click', () => {\n
					revealAssignmentRow(Number(assignmentIds[0]));\n
				});\n
				actions.appendChild(focusBtn);\n
			}\n
			li.appendChild(actions);\n
			list.appendChild(li);\n
		}\n
	}\n
\n
	async function acknowledgeConflict(conflictId) {\n
		const reason = await C.promptReason({\n
			title: t('dutycheck', 'Confirm this exception'),\n
			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),\n
			confirmLabel: t('dutycheck', 'Confirm'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			minLength: 10,\n
		});\n
		if (reason === null) return;\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });\n
			render(response?.data || {});\n
			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');\n
			Msg.announce(ackMsg, 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	const VIRTUAL_FALLBACK = {\n
		DEFAULT_ROW_HEIGHT: 44,\n
		DEFAULT_OVERSCAN: 6,\n
		UNSIZED_WINDOW_ROWS: 24,\n
		visibleRange(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			const rowHeight = 44;\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: total * rowHeight,\n
				rowHeight,\n
			};\n
		},\n
		scrollTopToRevealIndex() {\n
			return 0;\n
		},\n
		pageStride() {\n
			return 1;\n
		},\n
		windowCaption(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			if (total <= 0) {\n
				return { mode: 'empty', from: 0, to: 0, total: 0 };\n
			}\n
			return { mode: 'all', from: 1, to: total, total };\n
		},\n
	};\n
\n
	function virtualApi() {\n
		const api = window.DutyCheckVirtualWindow;\n
		if (api\n
			&& typeof api.visibleRange === 'function'\n
			&& typeof api.scrollTopToRevealIndex === 'function'\n
			&& typeof api.windowCaption === 'function') {\n
			return api;\n
		}\n
		return VIRTUAL_FALLBACK;\n
	}\n
\n
	function cellSelectionKey(employeeId, dutyDate) {\n
		return `${Number(employeeId)}|${String(dutyDate)}`;\n
	}\n
\n
	function prefersReducedMotion() {\n
		try {\n
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n
		} catch (_) {\n
			return false;\n
		}\n
	}\n
\n
	function measureHeight(el) {\n
		if (!(el instanceof HTMLElement)) {\n
			return 0;\n
		}\n
		const h = el.getBoundingClientRect().height;\n
		return Number.isFinite(h) && h > 0 ? h : 0;\n
	}\n
\n
	function assignmentIndex(assignments) {\n
		const byKey = new Map();\n
		for (const a of assignments || []) {\n
			const key = cellSelectionKey(a.employeeId, a.dutyDate);\n
			if (!byKey.has(key)) {\n
				byKey.set(key, []);\n
			}\n
			byKey.get(key).push(a);\n
		}\n
		return byKey;\n
	}\n
\n
	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {\n
		const emptyPanel = document.getElementById('dc-roster-assignments-empty');\n
		if (emptyPanel) {\n
			emptyPanel.hidden = !showEmptyPanel;\n
		}\n
	}\n
\n
	const gridState = {\n
		view: 'grid',\n
		selected: new Set(), // `${employeeId}|${dutyDate}`\n
		focusRow: 0,\n
		focusCol: 0,\n
		paintAll: false,\n
		rowHeight: 44,\n
		headerHeight: 44,\n
		windowStart: -1,\n
		windowEnd: -1,\n
		paintedEmployeeCount: -1,\n
		paintedDateCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	const listState = {\n
		windowStart: -1,\n
		windowEnd: -1,\n
		rowHeight: 48,\n
		paintedCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	function renderAssignments(assignments) {\n
		paintListWindow({ assignments: assignments || [], force: true });\n
		renderRosterGrid(assignments || []);\n
		updateAddAssignmentControl();\n
	}\n
\n
	function buildAssignmentRow(a, periodOpen) {\n
		const overnightHintText = t('dutycheck', 'Continues into the next day.');\n
		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));\n
		const tr = create('tr');\n
		if (a?.id != null) {\n
			tr.dataset.assignmentId = String(a.id);\n
			tr.setAttribute('tabindex', '-1');\n
		}\n
		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;\n
		const tdDate = create('td', { text: date });\n
		tdDate.dataset.cell = t('dutycheck', 'Date');\n
		tr.appendChild(tdDate);\n
\n
		const tdStart = create('td');\n
		tdStart.dataset.cell = t('dutycheck', 'Start');\n
		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));\n
		tr.appendChild(tdStart);\n
\n
		const tdEnd = create('td');\n
		tdEnd.dataset.cell = t('dutycheck', 'End');\n
		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));\n
		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {\n
			tdEnd.title = overnightHintText;\n
			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));\n
		}\n
		tr.appendChild(tdEnd);\n
\n
		const rest = [\n
			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },\n
			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },\n
			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },\n
			{ label: t('dutycheck', 'Note'), value: a.note || '' },\n
		];\n
		for (const cell of rest) {\n
			const td = create('td', { text: String(cell.value ?? '') });\n
			td.dataset.cell = cell.label;\n
			tr.appendChild(td);\n
		}\n
		const tdActions = create('td');\n
		tdActions.dataset.cell = t('dutycheck', 'Actions');\n
		tdActions.className = 'dc-table__actions';\n
		if (periodOpen && a?.id) {\n
			const editBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Edit'),\n
			});\n
			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));\n
			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));\n
			const cancelBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Cancel shift'),\n
			});\n
			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));\n
			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));\n
			tdActions.appendChild(editBtn);\n
			tdActions.appendChild(cancelBtn);\n
		} else {\n
			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));\n
		}\n
		tr.appendChild(tdActions);\n
		return tr;\n
	}\n
\n
	function createListSpacer(px) {\n
		const tr = create('tr', { class: 'dc-virtual-spacer' });\n
		tr.setAttribute('aria-hidden', 'true');\n
		tr.style.height = `${Math.max(0, px)}px`;\n
		const td = create('td');\n
		td.colSpan = 8;\n
		td.style.height = `${Math.max(0, px)}px`;\n
		td.style.padding = '0';\n
		td.style.border = '0';\n
		td.style.lineHeight = '0';\n
		tr.appendChild(td);\n
		return tr;\n
	}\n
\n
	function updateListWindowStatus(range, total) {\n
		const el = document.getElementById('dc-roster-list-status');\n
		if (!el) {\n
			return;\n
		}\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		if (cap.mode === 'all') {\n
			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));\n
			return;\n
		}\n
		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')\n
			.replace('{from}', String(cap.from))\n
			.replace('{to}', String(cap.to))\n
			.replace('{total}', String(cap.total));\n
	}\n
\n
	function paintListWindow(options) {\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		if (!tbody) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		if (!assignments.length) {\n
			tbody.replaceChildren();\n
			setAssignmentsEmptyPanelVisible(true);\n
			listState.windowStart = 0;\n
			listState.windowEnd = 0;\n
			listState.paintedCount = 0;\n
			updateListWindowStatus({ start: 0, end: 0 }, 0);\n
			return;\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const scroller = document.getElementById('dc-assignments-table-wrap');\n
		const VW = virtualApi();\n
		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: assignments.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& listState.windowStart === range.start\n
			&& listState.windowEnd === range.end\n
			&& listState.paintedCount === assignments.length) {\n
			updateListWindowStatus(range, assignments.length);\n
			return;\n
		}\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const periodOpen = canAddAssignment();\n
		const frag = document.createDocumentFragment();\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createListSpacer(range.padBefore));\n
		}\n
		const windowRows = assignments;\n
		for (const a of windowRows) {\n
			frag.appendChild(buildAssignmentRow(a, periodOpen));\n
		}\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createListSpacer(range.padAfter));\n
		}\n
		tbody.replaceChildren(frag);\n
		listState.windowStart = range.start;\n
		listState.windowEnd = range.end;\n
		listState.paintedCount = assignments.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
		}\n
		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');\n
		const measured = measureHeight(sample);\n
		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {\n
			listState.rowHeight = measured;\n
			listState.remeasuring = true;\n
			paintListWindow({ assignments, force: true });\n
			listState.remeasuring = false;\n
			return;\n
		}\n
		if (measured > 0) {\n
			listState.rowHeight = measured;\n
		}\n
		updateListWindowStatus(range, assignments.length);\n
	}\n
\n
	function selectedPeriodId() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const fromSwitcher = Number(switcher?.value || 0);\n
		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {\n
			return fromSwitcher;\n
		}\n
		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;\n
	}\n
\n
	function periodDateList() {\n
		const periodId = selectedPeriodId();\n
		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));\n
		if (!period?.startDate || !period?.endDate) {\n
			return [];\n
		}\n
		const out = [];\n
		const cursor = new Date(`${period.startDate}T00:00:00Z`);\n
		const end = new Date(`${period.endDate}T00:00:00Z`);\n
		while (cursor <= end) {\n
			out.push(cursor.toISOString().slice(0, 10));\n
			cursor.setUTCDate(cursor.getUTCDate() + 1);\n
			if (out.length > 62) {\n
				break;\n
			}\n
		}\n
		return out;\n
	}\n
\n
	function updateGridWindowStatus(range, total, dayCount) {\n
		const el = document.getElementById('dc-roster-grid-status');\n
		if (!el) {\n
			return;\n
		}\n
		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		let text = '';\n
		if (cap.mode === 'all') {\n
			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));\n
		} else {\n
			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')\n
				.replace('{from}', String(cap.from))\n
				.replace('{to}', String(cap.to))\n
				.replace('{total}', String(cap.total));\n
		}\n
		if (days > 10) {\n
			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');\n
		}\n
		el.textContent = text;\n
	}\n
\n
	function createGridSpacer(px) {\n
		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });\n
		el.setAttribute('aria-hidden', 'true');\n
		el.setAttribute('role', 'presentation');\n
		el.style.height = `${Math.max(0, px)}px`;\n
		return el;\n
	}\n
\n
	function setGridEmptyState(root, message) {\n
		root.removeAttribute('aria-rowcount');\n
		root.removeAttribute('aria-colcount');\n
		root.removeAttribute('tabindex');\n
		root.style.removeProperty('--dc-roster-day-count');\n
		root.style.removeProperty('--dc-roster-day-min');\n
		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');\n
		root.setAttribute('role', 'status');\n
		root.setAttribute('aria-live', 'polite');\n
		root.replaceChildren(create('p', {\n
			class: 'dc-roster-empty-state__text',\n
			text: message,\n
		}));\n
	}\n
\n
	function prepareGridForRows(root, employees, dates) {\n
		root.setAttribute('role', 'grid');\n
		root.removeAttribute('tabindex');\n
		root.removeAttribute('aria-live');\n
		root.setAttribute('aria-rowcount', String(employees.length + 1));\n
		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));\n
		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));\n
		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));\n
		root.style.setProperty('--dc-roster-day-min', dayMin);\n
		root.classList.toggle('dc-roster-grid--long', dates.length > 10);\n
		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);\n
	}\n
\n
	function buildColHead(day, colIdx, dayMeta) {\n
		const parts = dayMeta || D?.formatRosterColumnParts?.(day);\n
		const full = parts?.full || D?.formatDisplayDate?.(day) || day;\n
		let ariaLabel = parts?.ariaLabel || full;\n
		if (parts?.isToday) {\n
			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;\n
		}\n
		const weekdayShort = parts?.weekdayShort || '';\n
		const dayNum = parts?.day || '';\n
		const classNames = ['dc-roster-grid__colhead'];\n
		if (parts?.isWeekend) {\n
			classNames.push('dc-roster-grid__colhead--weekend');\n
		}\n
		if (parts?.isToday) {\n
			classNames.push('dc-roster-grid__colhead--today');\n
		}\n
		const children = [];\n
		if (weekdayShort) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));\n
		}\n
		if (dayNum) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));\n
		}\n
		const attrs = {\n
			'data-col': String(colIdx),\n
			'aria-colindex': String(colIdx + 2),\n
			title: full,\n
			'aria-label': ariaLabel,\n
		};\n
		if (parts?.iso) {\n
			attrs['data-duty-date'] = parts.iso;\n
		}\n
		if (!children.length) {\n
			return create('div', {\n
				class: classNames.join(' '),\n
				role: 'columnheader',\n
				text: full,\n
				attrs,\n
			});\n
		}\n
		return create('div', {\n
			class: classNames.join(' '),\n
			role: 'columnheader',\n
			attrs,\n
		}, children);\n
	}\n
\n
	function buildDutyDayMeta(dates) {\n
		const map = new Map();\n
		(dates || []).forEach((day) => {\n
			map.set(day, D?.formatRosterColumnParts?.(day) || null);\n
		});\n
		return map;\n
	}\n
\n
	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {\n
		const row = create('div', {\n
			class: 'dc-roster-grid__row',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': String(rowIdx + 2) },\n
		});\n
		const prefs = state.preferencesByEmployee[String(emp.id)] || [];\n
		const prefChips = prefs.slice(0, 2).map((p) => {\n
			const band = String(p.band || '');\n
			const label = band === 'early'\n
				? t('dutycheck', 'Früh')\n
				: (band === 'late' ? t('dutycheck', 'Spät') : band);\n
			return create('span', {\n
				class: 'dc-pref-chip dc-pref-chip--static',\n
				text: label,\n
				attrs: { title: label },\n
			});\n
		});\n
		const headKids = [\n
			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),\n
		];\n
		if (prefChips.length) {\n
			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));\n
		}\n
		row.appendChild(create('div', {\n
			class: 'dc-roster-grid__rowhead',\n
			role: 'rowheader',\n
			attrs: { 'aria-colindex': '1' },\n
		}, headKids));\n
		dates.forEach((day, colIdx) => {\n
			const key = cellSelectionKey(emp.id, day);\n
			const cellAssignments = byKey.get(key) || [];\n
			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);\n
			const cell = create('div', {\n
				class: 'dc-roster-grid__cell',\n
				role: 'gridcell',\n
				attrs: {\n
					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',\n
					'data-employee-id': String(emp.id),\n
					'data-duty-date': day,\n
					'data-row': String(rowIdx),\n
					'data-col': String(colIdx),\n
					'aria-colindex': String(colIdx + 2),\n
					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',\n
				},\n
			});\n
			if (dayParts?.isWeekend) {\n
				cell.classList.add('dc-roster-grid__cell--weekend');\n
			}\n
			if (dayParts?.isToday) {\n
				cell.classList.add('dc-roster-grid__cell--today');\n
			}\n
			if (cellAssignments.length) {\n
				cell.classList.add('dc-roster-grid__cell--filled');\n
				const first = cellAssignments[0];\n
				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;\n
				cell.appendChild(create('span', {\n
					class: 'dc-roster-grid__shift',\n
					text: label,\n
				}));\n
				const editLabel = canAddAssignment()\n
					? t('dutycheck', 'Edit assignment')\n
					: t('dutycheck', 'View assignment (read-only)');\n
				cell.setAttribute('aria-label', `${editLabel}: ${label}`);\n
				if (cellAssignments.length > 1) {\n
					cell.appendChild(create('span', {\n
						class: 'dc-roster-grid__more',\n
						text: `+${cellAssignments.length - 1}`,\n
					}));\n
				}\n
			} else {\n
				cell.classList.add('dc-roster-grid__cell--empty');\n
				const blackoutKey = `${emp.id}|${day}`;\n
				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];\n
				if (hasBlackout) {\n
					cell.classList.add('dc-roster-grid__cell--blackout');\n
					cell.appendChild(create('span', {\n
						class: 'dc-blackout-mark',\n
						attrs: { title: t('dutycheck', 'Kann nicht') },\n
					}, [\n
						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),\n
						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),\n
					]));\n
					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));\n
				} else {\n
					cell.setAttribute(\n
						'aria-label',\n
						canAddAssignment()\n
							? t('dutycheck', 'Empty cell — Space to select for bulk fill')\n
							: t('dutycheck', 'Empty cell'),\n
					);\n
				}\n
			}\n
			if (gridState.selected.has(key)) {\n
				cell.classList.add('is-selected');\n
			}\n
			row.appendChild(cell);\n
		});\n
		return row;\n
	}\n
\n
	function syncVisibleGridChrome() {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {\n
			const empId = cell.getAttribute('data-employee-id');\n
			const day = cell.getAttribute('data-duty-date');\n
			const key = cellSelectionKey(empId, day);\n
			const selected = gridState.selected.has(key);\n
			cell.classList.toggle('is-selected', selected);\n
			cell.setAttribute('aria-selected', selected ? 'true' : 'false');\n
			const rowIdx = Number(cell.getAttribute('data-row'));\n
			const colIdx = Number(cell.getAttribute('data-col'));\n
			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;\n
			cell.setAttribute('tabindex', isFocus ? '0' : '-1');\n
		});\n
	}\n
\n
	function focusGridCellIfVisible(root) {\n
		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
		if (node instanceof HTMLElement) {\n
			node.scrollIntoView({\n
				block: 'nearest',\n
				inline: 'nearest',\n
				behavior: 'auto',\n
			});\n
			node.focus();\n
		}\n
	}\n
\n
	function activateGridCell(cell) {\n
		if (!(cell instanceof HTMLElement)) {\n
			return;\n
		}\n
		const rowIdx = Number(cell.getAttribute('data-row'));\n
		const colIdx = Number(cell.getAttribute('data-col'));\n
		if (Number.isInteger(rowIdx) && rowIdx >= 0) {\n
			gridState.focusRow = rowIdx;\n
		}\n
		if (Number.isInteger(colIdx) && colIdx >= 0) {\n
			gridState.focusCol = colIdx;\n
		}\n
		const empId = cell.getAttribute('data-employee-id');\n
		const day = cell.getAttribute('data-duty-date');\n
		const key = cellSelectionKey(empId, day);\n
		const filled = cell.classList.contains('dc-roster-grid__cell--filled');\n
		if (!filled) {\n
			if (canAddAssignment()) {\n
				toggleGridSelection(key);\n
				syncVisibleGridChrome();\n
				updateBulkBar();\n
			}\n
			return;\n
		}\n
		const byKey = assignmentIndex(state.assignments || []);\n
		const cellAssignments = byKey.get(key) || [];\n
		if (canAddAssignment() && cellAssignments[0]) {\n
			openAssignmentEditModal(cellAssignments[0], cell);\n
			return;\n
		}\n
		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');\n
	}\n
\n
	function ensureGridFocusVisible() {\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const employees = state.employees || [];\n
		if (!scroller || !employees.length) {\n
			return;\n
		}\n
		const VW = virtualApi();\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);\n
		const next = VW.scrollTopToRevealIndex({\n
			index: gridState.focusRow,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop: scroller.scrollTop,\n
			total: employees.length,\n
		});\n
		if (Math.abs(next - scroller.scrollTop) > 0.5) {\n
			scroller.scrollTop = next;\n
		}\n
	}\n
\n
	function renderRosterGrid(assignments) {\n
		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });\n
	}\n
\n
	function paintGridWindow(options) {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		const employees = state.employees || [];\n
		const dates = periodDateList();\n
		const byKey = assignmentIndex(assignments);\n
\n
		if (!employees.length || !dates.length) {\n
			setGridEmptyState(\n
				root,\n
				t('dutycheck', 'Add employees and an open period to use the planning grid.'),\n
			);\n
			gridState.windowStart = 0;\n
			gridState.windowEnd = 0;\n
			gridState.paintedEmployeeCount = employees.length;\n
			gridState.paintedDateCount = dates.length;\n
			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		prepareGridForRows(root, employees, dates);\n
		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));\n
		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));\n
\n
		const VW = virtualApi();\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = scroller && gridState.view === 'grid'\n
			? Math.max(0, scroller.clientHeight - headerHeight)\n
			: 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: employees.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& gridState.windowStart === range.start\n
			&& gridState.windowEnd === range.end\n
			&& gridState.paintedEmployeeCount === employees.length\n
			&& gridState.paintedDateCount === dates.length) {\n
			syncVisibleGridChrome();\n
			updateGridWindowStatus(range, employees.length, dates.length);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const savedLeft = scroller ? scroller.scrollLeft : 0;\n
		const dayMetaByDate = buildDutyDayMeta(dates);\n
		const frag = document.createDocumentFragment();\n
		const header = create('div', {\n
			class: 'dc-roster-grid__row dc-roster-grid__row--head',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': '1' },\n
		});\n
		header.appendChild(create('div', {\n
			class: 'dc-roster-grid__corner',\n
			role: 'columnheader',\n
			text: t('dutycheck', 'Person'),\n
			attrs: { 'aria-colindex': '1' },\n
		}));\n
		dates.forEach((day, colIdx) => {\n
			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));\n
		});\n
		frag.appendChild(header);\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createGridSpacer(range.padBefore));\n
		}\n
		const windowEmployees = employees.slice(range.start, range.end);\n
		windowEmployees.forEach((emp, offset) => {\n
			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));\n
		});\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createGridSpacer(range.padAfter));\n
		}\n
		root.replaceChildren(frag);\n
		gridState.windowStart = range.start;\n
		gridState.windowEnd = range.end;\n
		gridState.paintedEmployeeCount = employees.length;\n
		gridState.paintedDateCount = dates.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
			scroller.scrollLeft = savedLeft;\n
		}\n
		const headerEl = root.querySelector('.dc-roster-grid__row--head');\n
		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');\n
		const measuredHeader = measureHeight(headerEl);\n
		const measuredRow = measureHeight(bodyRow);\n
		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)\n
			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))\n
			&& !gridState.remeasuring) {\n
			if (measuredHeader > 0) {\n
				gridState.headerHeight = measuredHeader;\n
			}\n
			if (measuredRow > 0) {\n
				gridState.rowHeight = measuredRow;\n
			}\n
			gridState.remeasuring = true;\n
			paintGridWindow({ assignments, force: true });\n
			gridState.remeasuring = false;\n
			return;\n
		}\n
		if (measuredHeader > 0) {\n
			gridState.headerHeight = measuredHeader;\n
		}\n
		if (measuredRow > 0) {\n
			gridState.rowHeight = measuredRow;\n
		}\n
		bindGridInteractions(root);\n
		updateGridWindowStatus(range, employees.length, dates.length);\n
		updateBulkBar();\n
	}\n
\n
	function bindGridInteractions(root) {\n
		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {\n
			return;\n
		}\n
		root.dataset.dcGridBound = '1';\n
		root.addEventListener('click', (ev) => {\n
			const cell = ev.target.closest('[role="gridcell"]');\n
			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {\n
				return;\n
			}\n
			activateGridCell(cell);\n
		});\n
		root.addEventListener('keydown', (ev) => {\n
			const employees = state.employees || [];\n
			const dates = periodDateList();\n
			const rowCount = employees.length;\n
			const colCount = dates.length;\n
			if (!rowCount || !colCount) {\n
				return;\n
			}\n
			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];\n
			if (!gridKeys.includes(ev.key)) {\n
				return;\n
			}\n
			ev.preventDefault();\n
			if (ev.key === 'ArrowUp') {\n
				gridState.focusRow = Math.max(0, gridState.focusRow - 1);\n
			} else if (ev.key === 'ArrowDown') {\n
				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);\n
			} else if (ev.key === 'ArrowLeft') {\n
				gridState.focusCol = Math.max(0, gridState.focusCol - 1);\n
			} else if (ev.key === 'ArrowRight') {\n
				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);\n
			} else if (ev.key === 'Home') {\n
				gridState.focusCol = 0;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = 0;\n
				}\n
			} else if (ev.key === 'End') {\n
				gridState.focusCol = colCount - 1;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = rowCount - 1;\n
				}\n
			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {\n
				const VW = virtualApi();\n
				const scroller = document.getElementById('dc-roster-grid-scroller');\n
				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
				const viewportHeight = scroller\n
					? Math.max(rowHeight, scroller.clientHeight - headerHeight)\n
					: rowHeight;\n
				const stride = typeof VW.pageStride === 'function'\n
					? VW.pageStride({ rowHeight, viewportHeight })\n
					: 1;\n
				if (ev.key === 'PageDown') {\n
					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);\n
				} else {\n
					gridState.focusRow = Math.max(0, gridState.focusRow - stride);\n
				}\n
			} else if (ev.key === 'Enter') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				activateGridCell(cell);\n
				focusGridCellIfVisible(root);\n
				return;\n
			} else if (ev.key === ' ' || ev.key === 'Spacebar') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				const empId = cell?.getAttribute('data-employee-id');\n
				const day = cell?.getAttribute('data-duty-date');\n
				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');\n
				if (empId && day && isEmpty && canAddAssignment()) {\n
					toggleGridSelection(cellSelectionKey(empId, day));\n
					syncVisibleGridChrome();\n
					updateBulkBar();\n
				}\n
				focusGridCellIfVisible(root);\n
				return;\n
			}\n
			ensureGridFocusVisible();\n
			paintGridWindow({ force: false });\n
			focusGridCellIfVisible(root);\n
		});\n
	}\n
\n
	function toggleGridSelection(key) {\n
		if (gridState.selected.has(key)) {\n
			gridState.selected.delete(key);\n
		} else {\n
			gridState.selected.add(key);\n
		}\n
	}\n
\n
	function updateBulkBar() {\n
		const bar = document.getElementById('dc-roster-bulk-bar');\n
		const countEl = document.getElementById('dc-roster-bulk-count');\n
		if (!bar || !countEl) {\n
			return;\n
		}\n
		const n = gridState.selected.size;\n
		if (n < 1 || !canAddAssignment()) {\n
			bar.hidden = true;\n
			return;\n
		}\n
		bar.hidden = false;\n
		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (sel && !(sel.options && sel.options.length)) {\n
			populateBulkTemplates();\n
		}\n
	}\n
\n
	async function populateBulkTemplates() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (!sel) {\n
			return;\n
		}\n
		await loadTemplates();\n
		sel.replaceChildren();\n
		const templates = state.templates || [];\n
		if (!templates.length) {\n
			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));\n
			return;\n
		}\n
		for (const tpl of templates) {\n
			sel.appendChild(create('option', {\n
				value: String(tpl.id),\n
				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,\n
			}));\n
		}\n
	}\n
\n
	function revealAssignmentRow(id) {\n
		const assignmentId = Number(id);\n
		const assignments = state.assignments || [];\n
		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);\n
		if (idx < 0) {\n
			return;\n
		}\n
		applyRosterViewChrome('list');\n
		const run = () => {\n
			const scroller = document.getElementById('dc-assignments-table-wrap');\n
			const VW = virtualApi();\n
			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
			if (scroller) {\n
				scroller.scrollTop = VW.scrollTopToRevealIndex({\n
					index: idx,\n
					rowHeight,\n
					viewportHeight: Math.max(rowHeight, scroller.clientHeight),\n
					scrollTop: scroller.scrollTop,\n
					total: assignments.length,\n
				});\n
			}\n
			paintListWindow({ assignments, force: true });\n
			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);\n
			if (row instanceof HTMLElement) {\n
				row.scrollIntoView({\n
					block: 'nearest',\n
					behavior: 'auto',\n
				});\n
				row.focus();\n
				row.classList.add('dc-row--flash');\n
				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);\n
			}\n
		};\n
		window.requestAnimationFrame(run);\n
	}\n
\n
	function setRosterPaintAll(paintAll) {\n
		gridState.paintAll = paintAll === true;\n
		paintListWindow({ force: true });\n
		paintGridWindow({ force: true });\n
	}\n
\n
	function wireRosterVirtualization() {\n
		const gridScroller = document.getElementById('dc-roster-grid-scroller');\n
		const listScroller = document.getElementById('dc-assignments-table-wrap');\n
		let gridRaf = 0;\n
		let listRaf = 0;\n
		if (gridScroller) {\n
			gridScroller.addEventListener('scroll', () => {\n
				if (gridRaf) {\n
					return;\n
				}\n
				gridRaf = window.requestAnimationFrame(() => {\n
					gridRaf = 0;\n
					paintGridWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		if (listScroller) {\n
			listScroller.addEventListener('scroll', () => {\n
				if (listRaf) {\n
					return;\n
				}\n
				listRaf = window.requestAnimationFrame(() => {\n
					listRaf = 0;\n
					paintListWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		const resize = () => {\n
			if (gridState.view === 'grid') {\n
				paintGridWindow({ force: true });\n
			} else {\n
				paintListWindow({ force: true });\n
			}\n
		};\n
		if (typeof ResizeObserver === 'function') {\n
			const ro = new ResizeObserver(resize);\n
			if (gridScroller) {\n
				ro.observe(gridScroller);\n
			}\n
			if (listScroller) {\n
				ro.observe(listScroller);\n
			}\n
		} else {\n
			window.addEventListener('resize', resize);\n
		}\n
		window.addEventListener('beforeprint', () => setRosterPaintAll(true));\n
		window.addEventListener('afterprint', () => setRosterPaintAll(false));\n
		if (typeof window.matchMedia === 'function') {\n
			try {\n
				const printMq = window.matchMedia('print');\n
				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);\n
				if (typeof printMq.addEventListener === 'function') {\n
					printMq.addEventListener('change', onPrintMq);\n
				} else if (typeof printMq.addListener === 'function') {\n
					printMq.addListener(onPrintMq);\n
				}\n
			} catch (_) {\n
				/* print media query is not available in every test/jsdom shim */\n
			}\n
		}\n
	}\n
\n
	function applyRosterViewChrome(view) {\n
		gridState.view = view === 'list' ? 'list' : 'grid';\n
		const gridWrap = document.getElementById('dc-roster-grid-wrap');\n
		const listPanel = document.getElementById('dc-roster-list-panel');\n
		const gridBtn = document.getElementById('dc-roster-view-grid');\n
		const listBtn = document.getElementById('dc-roster-view-list');\n
		if (gridWrap) {\n
			gridWrap.hidden = gridState.view !== 'grid';\n
		}\n
		if (listPanel) {\n
			listPanel.hidden = gridState.view !== 'list';\n
		}\n
		if (gridBtn) {\n
			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');\n
		}\n
		if (listBtn) {\n
			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');\n
		}\n
	}\n
\n
	function setRosterView(view) {\n
		applyRosterViewChrome(view);\n
		window.requestAnimationFrame(() => {\n
			if (gridState.view === 'list') {\n
				paintListWindow({ force: true });\n
			} else {\n
				paintGridWindow({ force: true });\n
			}\n
		});\n
	}\n
\n
	async function applyBulkFromTemplate() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		const tplId = Number(sel?.value || 0);\n
		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);\n
		if (!tpl || gridState.selected.size < 1) {\n
			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');\n
			return;\n
		}\n
		if (!Number(tpl.locationId || 0)) {\n
			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');\n
			return;\n
		}\n
		const periodId = Number(selectedPeriodId() || 0);\n
		let ok = 0;\n
		let fail = 0;\n
		for (const key of Array.from(gridState.selected)) {\n
			const sep = key.indexOf('|');\n
			if (sep < 1) {\n
				fail += 1;\n
				continue;\n
			}\n
			const employeeId = Number(key.slice(0, sep));\n
			const dutyDate = key.slice(sep + 1);\n
			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {\n
				fail += 1;\n
				continue;\n
			}\n
			const locationId = Number(tpl.locationId || 0);\n
			if (!locationId) {\n
				fail += 1;\n
				continue;\n
			}\n
			try {\n
				await submitAssignment({\n
					periodId,\n
					employeeId: Number(employeeId),\n
					locationId,\n
					dutyDate,\n
					startTime: String(tpl.startTime).slice(0, 5),\n
					endTime: String(tpl.endTime).slice(0, 5),\n
					breakMinutes: Number(tpl.breakMinutes || 0),\n
					note: '',\n
					assignmentId: 0,\n
				}, true);\n
				ok += 1;\n
			} catch {\n
				fail += 1;\n
			}\n
		}\n
		gridState.selected.clear();\n
		await loadRoster(periodId || null);\n
		Msg.announce(\n
			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')\n
				.replace('{ok}', String(ok))\n
				.replace('{fail}', String(fail)),\n
			fail ? 'warning' : 'info',\n
		);\n
	}\n
\n
	function uniqueConflictTypes(conflicts) {\n
		const types = new Set();\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity === 'soft' && conflict?.type) {\n
				types.add(String(conflict.type));\n
			}\n
		}\n
		return Array.from(types);\n
	}\n
\n
	function softConflictSummary(conflicts) {\n
		const lines = [];\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity !== 'soft') {\n
				continue;\n
			}\n
			const raw = String(conflict?.message || '');\n
			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');\n
			if (text && !lines.includes(text)) {\n
				lines.push(text);\n
			}\n
		}\n
		return lines.join('\n');\n
	}\n
\n
	function errorCodeFrom(err) {\n
		return String(err?.code || err?.payload?.error?.code || '');\n
	}\n
\n
	function syncAssignmentPeriodFromSwitcher() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (!switcher || !periodHidden) {\n
			return;\n
		}\n
		const selected = Number(switcher.value);\n
		if (Number.isInteger(selected) && selected > 0) {\n
			periodHidden.value = String(selected);\n
		}\n
	}\n
\n
	function announceAssignmentSaveError(err) {\n
		clearAssignmentFormSuccess();\n
		const friendly = errorMessageFor(err);\n
		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');\n
		updateAssignmentFormFeedback(message);\n
		Msg.announce(message, 'error');\n
	}\n
\n
	async function submitAssignment(payload, retryWithAck = true) {\n
		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		try {\n
			if (editId > 0) {\n
				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);\n
			}\n
			return await Api.post('/apps/dutycheck/api/assignments', payload);\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Save with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return submitAssignment({ ...payload, acknowledgements }, false);\n
			}\n
			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Employee cannot work (blackout)'),\n
					label: t(\n
						'dutycheck',\n
						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',\n
					),\n
					confirmLabel: t('dutycheck', 'Schedule anyway'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
					maxLength: 200,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	function setAssignmentFormBusy(form, busy) {\n
		if (!form) return;\n
		const submit = form.querySelector('button[type="submit"]');\n
		const clear = document.getElementById('dc-assignment-form-clear');\n
		if (submit) {\n
			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';\n
			if (busy) {\n
				submit.setAttribute('aria-busy', 'true');\n
			} else {\n
				submit.removeAttribute('aria-busy');\n
			}\n
		}\n
		if (clear) {\n
			clear.disabled = busy;\n
		}\n
	}\n
\n
	function renderSetupCallout(data) {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		const list = document.getElementById('dc-roster-setup-checklist');\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!callout || !list || !form) return;\n
		const urls = readUrls();\n
		const missing = [];\n
		if (!state.periods.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Create a planning period.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		if (!state.employees.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active employee.'),\n
				url: urls.employees,\n
				cta: t('dutycheck', 'Go to Employees'),\n
			});\n
		}\n
		if (!state.locations.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active location.'),\n
				url: urls.locations,\n
				cta: t('dutycheck', 'Go to Locations'),\n
			});\n
		}\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));\n
		const periodIsOpen = (selectedPeriod?.status || '') === 'open';\n
		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {\n
			missing.push({\n
				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		list.replaceChildren();\n
		if (!missing.length) {\n
			callout.hidden = true;\n
			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {\n
				if (el.dataset.dcLockedBySetup === '1') {\n
					el.disabled = false;\n
					delete el.dataset.dcLockedBySetup;\n
				}\n
			});\n
			updateAddAssignmentControl();\n
			return;\n
		}\n
		for (const item of missing) {\n
			const li = create('li');\n
			li.appendChild(document.createTextNode(item.text + ' '));\n
			if (item.url) {\n
				li.appendChild(create('a', {\n
					class: 'button',\n
					href: item.url,\n
					text: item.cta,\n
				}));\n
			}\n
			list.appendChild(li);\n
		}\n
		callout.hidden = false;\n
		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {\n
			if (!el.disabled) {\n
				el.dataset.dcLockedBySetup = '1';\n
				el.disabled = true;\n
			}\n
		});\n
		updateAddAssignmentControl();\n
	}\n
\n
	function readUrls() {\n
		try {\n
			const raw = document.getElementById('app-content')?.dataset?.dcUrls;\n
			if (!raw) return {};\n
			return JSON.parse(raw) || {};\n
		} catch (_) {\n
			return {};\n
		}\n
	}\n
\n
	function restoreAssignmentFormPanel() {\n
		const host = document.getElementById('dc-assignment-form-host');\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (host && panel && panel.parentElement !== host) {\n
			host.appendChild(panel);\n
		}\n
	}\n
\n
	function selectedPeriodFromState() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;\n
		const periodId = Number(raw);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function isAssignmentSetupBlocking() {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		return !!(callout && !callout.hidden);\n
	}\n
\n
	function canAddAssignment() {\n
		if (!state.canCreateAssignments) {\n
			return false;\n
		}\n
		if (!state.periods.length || !state.employees.length || !state.locations.length) {\n
			return false;\n
		}\n
		const period = selectedPeriodFromState();\n
		return !!(period && String(period.status || '').toLowerCase() === 'open');\n
	}\n
\n
	function addAssignmentDisabledReason() {\n
		if (!state.periods.length) {\n
			return t('dutycheck', 'Create a planning period first.');\n
		}\n
		if (!state.employees.length) {\n
			return t('dutycheck', 'Add at least one active employee before planning shifts.');\n
		}\n
		if (!state.locations.length) {\n
			return t('dutycheck', 'Add at least one active location before planning shifts.');\n
		}\n
		const period = selectedPeriodFromState();\n
		if (!period) {\n
			return t('dutycheck', 'Select a period before adding assignments.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!state.canCreateAssignments) {\n
			return t('dutycheck', 'You cannot add assignments for this period.');\n
		}\n
		return t('dutycheck', 'Adding assignments is not available right now.');\n
	}\n
\n
	function updateAddAssignmentControl() {\n
		const allowed = canAddAssignment();\n
		const reason = addAssignmentDisabledReason();\n
		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {\n
			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.\n
			btn.hidden = !allowed;\n
			btn.disabled = !allowed;\n
			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');\n
			btn.classList.toggle('dc-is-disabled', !allowed);\n
			if (allowed) {\n
				btn.removeAttribute('title');\n
			} else {\n
				btn.setAttribute('title', reason);\n
			}\n
		});\n
		const emptyHint = document.getElementById('dc-roster-empty-add-hint');\n
		if (emptyHint) {\n
			emptyHint.textContent = allowed\n
				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')\n
				: reason;\n
		}\n
	}\n
\n
	function clearAssignmentsSectionSuccess() {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.classList.remove('dc-roster-flash--visible');\n
	}\n
\n
	function showAssignmentsSectionSuccess(message) {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.textContent = String(message);\n
		el.hidden = false;\n
		el.classList.add('dc-roster-flash--visible');\n
	}\n
\n
	function prepareAssignmentFormForModal() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		clearAssignmentFormSuccess();\n
		updateAssignmentFormFeedback(null);\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate && period?.endDate) {\n
			dateInput.min = String(period.startDate);\n
			dateInput.max = String(period.endDate);\n
			if (!dateInput.value) {\n
				dateInput.value = String(period.startDate);\n
			}\n
		}\n
		refreshAssignmentFormEligibility();\n
		applyDefaultBreakToForm();\n
		wireBreakPrefillHint();\n
		fillTemplateSelect();\n
		D?.applyLocaleToTemporalInputs(form);\n
	}\n
\n
	function resetAssignmentFormAfterSave(data) {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		form.reset();\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden && data?.selectedPeriodId) {\n
			periodHidden.value = String(data.selectedPeriodId);\n
		}\n
		applyDefaultBreakToForm();\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate) {\n
			dateInput.value = String(period.startDate);\n
		}\n
		D?.applyLocaleToTemporalInputs(form);\n
		updateAssignmentFormFeedback(null);\n
	}\n
\n
	async function performAssignmentSave() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return false;\n
		}\n
		if (isAssignmentSetupBlocking()) {\n
			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(msg);\n
			Msg.announce(msg, 'error');\n
			return false;\n
		}\n
		const payload = readForm(form);\n
		const validation = validate(payload);\n
		if (validation) {\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(validation);\n
			Msg.announce(validation, 'error');\n
			focusFirstInvalidAssignmentField(payload);\n
			return false;\n
		}\n
		updateAssignmentFormFeedback(null);\n
		setAssignmentFormBusy(form, true);\n
		try {\n
			const response = await submitAssignment(payload, true);\n
			render(response?.data || {});\n
			const savedMsg = buildAssignmentSavedMessage(payload);\n
			clearAssignmentFormSuccess();\n
			showAssignmentsSectionSuccess(savedMsg);\n
			Msg.announce(savedMsg, 'success');\n
			rememberBreakMinutes(payload.breakMinutes);\n
			resetAssignmentFormAfterSave(response?.data || {});\n
			return true;\n
		} catch (err) {\n
			announceAssignmentSaveError(err);\n
			return false;\n
		} finally {\n
			setAssignmentFormBusy(form, false);\n
		}\n
	}\n
\n
	function syncAssignmentModalPrimary(instance) {\n
		if (!instance?.primaryBtn) {\n
			return;\n
		}\n
		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();\n
		instance.primaryBtn.disabled = blocked;\n
		if (blocked) {\n
			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());\n
		} else {\n
			instance.primaryBtn.removeAttribute('title');\n
		}\n
	}\n
\n
	async function openAssignmentCreateModal(triggerEl) {\n
		if (!canAddAssignment()) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
			return;\n
		}\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		renderSetupCallout(state.lastRosterData || {});\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Create assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save assignment'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => {\n
				const ok = await performAssignmentSave();\n
				return ok;\n
			},\n
			onClose: (result) => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				if (!result) {\n
					clearAssignmentFormSuccess();\n
					return;\n
				}\n
				const addBtn = document.getElementById('dc-roster-add-assignment');\n
				if (addBtn && typeof addBtn.focus === 'function') {\n
					addBtn.focus();\n
				}\n
			},\n
			onCancel: () => {\n
				clearAssignmentFormSuccess();\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
		const form = document.getElementById('dc-assignment-form');\n
		if (form && !form.dataset.dcModalSyncWired) {\n
			form.dataset.dcModalSyncWired = '1';\n
			form.addEventListener('change', () => {\n
				if (assignmentModalInstance?._open) {\n
					syncAssignmentModalPrimary(assignmentModalInstance);\n
				}\n
			});\n
		}\n
	}\n
\n
	async function openAssignmentEditModal(assignment, triggerEl) {\n
		if (!canAddAssignment() || !assignment?.id) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			return;\n
		}\n
		state.editingAssignmentId = Number(assignment.id);\n
		state.editingAssignmentVersion = Number(assignment.version ?? 0);\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = String(assignment.id);\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput) {\n
			dateInput.value = String(assignment.dutyDate || '');\n
		}\n
		refreshAssignmentFormEligibility();\n
		const emp = document.getElementById('dc-assignment-employee');\n
		if (emp) {\n
			emp.value = String(assignment.employeeId || '');\n
		}\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (loc) {\n
			loc.value = String(assignment.locationId || '');\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		if (start) {\n
			start.value = String(assignment.startTime || '').slice(0, 5);\n
		}\n
		const end = document.getElementById('dc-assignment-end');\n
		if (end) {\n
			end.value = String(assignment.endTime || '').slice(0, 5);\n
		}\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(assignment.breakMinutes ?? 0);\n
		}\n
		const note = document.getElementById('dc-assignment-note');\n
		if (note) {\n
			note.value = String(assignment.note || '');\n
		}\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Edit assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save changes'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => performAssignmentSave(),\n
			onClose: () => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				clearAssignmentFormSuccess();\n
				if (triggerEl && typeof triggerEl.focus === 'function') {\n
					triggerEl.focus();\n
				}\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
	}\n
\n
	async function cancelAssignmentRow(assignment, triggerEl) {\n
		if (!assignment?.id || !canAddAssignment()) {\n
			return;\n
		}\n
		const ok = window.confirm(\n
			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),\n
		);\n
		if (!ok) {\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});\n
			render(response?.data || {});\n
			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));\n
			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		} finally {\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
		}\n
	}\n
\n
	let templatesInflight = null;\n
\n
	async function ensureAssignmentModalPrereqs() {\n
		const jobs = [loadTemplates()];\n
		if (state.planningDefaultsFresh !== true) {\n
			jobs.push(refreshPlanningDefaultFromServer());\n
		}\n
		await Promise.all(jobs);\n
	}\n
\n
	async function loadTemplates() {\n
		if (state.templatesLoaded) {\n
			fillTemplateSelect();\n
			return;\n
		}\n
		if (templatesInflight) {\n
			await templatesInflight;\n
			fillTemplateSelect();\n
			return;\n
		}\n
		templatesInflight = (async () => {\n
			try {\n
				const response = await Api.get('/apps/dutycheck/api/templates', {});\n
				state.templates = Array.isArray(response?.data) ? response.data : [];\n
				state.templatesLoaded = true;\n
			} catch (_) {\n
				state.templates = [];\n
				state.templatesLoaded = false;\n
			} finally {\n
				templatesInflight = null;\n
			}\n
		})();\n
		await templatesInflight;\n
		fillTemplateSelect();\n
	}\n
\n
	function fillTemplateSelect() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select) {\n
			return;\n
		}\n
		const current = select.value;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));\n
		for (const tpl of state.templates) {\n
			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;\n
			select.appendChild(create('option', { value: String(tpl.id), text: label }));\n
		}\n
		if (current && [...select.options].some((o) => o.value === current)) {\n
			select.value = current;\n
		}\n
	}\n
\n
	function applySelectedTemplate() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select || !select.value) {\n
			return;\n
		}\n
		const tpl = state.templates.find((t) => String(t.id) === String(select.value));\n
		if (!tpl) {\n
			return;\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		const end = document.getElementById('dc-assignment-end');\n
		const br = document.getElementById('dc-assignment-break');\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (start) start.value = String(tpl.startTime || '').slice(0, 5);\n
		if (end) end.value = String(tpl.endTime || '').slice(0, 5);\n
		if (br) br.value = String(tpl.breakMinutes ?? 0);\n
		if (loc && tpl.locationId) loc.value = String(tpl.locationId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		refreshAssignmentFormEligibility();\n
	}\n
\n
	function refreshAcknowledgeStats(periodId) {\n
		const el = document.getElementById('dc-roster-ack-stats');\n
		if (!el) {\n
			return;\n
		}\n
		const id = Number(periodId);\n
		if (!Number.isInteger(id) || id <= 0) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === id);\n
		const status = String(period?.status || '').toLowerCase();\n
		if (status !== 'published' && status !== 'closed') {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const rows = Array.isArray(state.assignments) ? state.assignments : [];\n
		let total = 0;\n
		let acked = 0;\n
		for (const row of rows) {\n
			if (String(row?.status || '').toLowerCase() === 'cancelled') {\n
				continue;\n
			}\n
			total += 1;\n
			if (row?.acknowledged || row?.acknowledgedAt) {\n
				acked += 1;\n
			}\n
		}\n
		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;\n
		el.hidden = false;\n
		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')\n
			.replace('{acked}', String(acked))\n
			.replace('{total}', String(total))\n
			.replace('{pct}', String(pct));\n
	}\n
\n
	function fillCopySourceSelect(selectedPeriodId) {\n
		const select = document.getElementById('dc-roster-copy-source');\n
		const wrap = document.getElementById('dc-roster-copy-period');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!select || !wrap) {\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));\n
		const open = period && String(period.status || '').toLowerCase() === 'open';\n
		wrap.hidden = !open;\n
		state.copyPreviewReady = false;\n
		if (applyBtn) applyBtn.disabled = true;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));\n
		for (const p of state.periods) {\n
			if (Number(p.id) === Number(selectedPeriodId)) {\n
				continue;\n
			}\n
			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;\n
			select.appendChild(create('option', { value: String(p.id), text: label }));\n
		}\n
	}\n
\n
	async function runCopyPeriod(dryRun) {\n
		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);\n
		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);\n
		const status = document.getElementById('dc-roster-copy-status');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!targetId || !sourceId) {\n
			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {\n
				sourcePeriodId: sourceId,\n
				dryRun: !!dryRun,\n
			});\n
			const data = response?.data || {};\n
			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);\n
			const skipped = Number(data.skipped ?? 0);\n
			const msg = dryRun\n
				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')\n
					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))\n
					.replace('{skipped}', String(data.wouldSkip ?? skipped))\n
				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')\n
					.replace('{created}', String(created));\n
			if (status) {\n
				status.hidden = false;\n
				status.textContent = msg;\n
			}\n
			Msg.announce(msg, dryRun ? 'info' : 'success');\n
			if (dryRun) {\n
				state.copyPreviewReady = true;\n
				if (applyBtn) applyBtn.disabled = false;\n
			} else {\n
				state.copyPreviewReady = false;\n
				if (applyBtn) applyBtn.disabled = true;\n
				render(data.roster || data || {});\n
				if (!data.roster && data.assignments) {\n
					render(data);\n
				} else if (!data.roster) {\n
					await loadRoster(targetId);\n
				}\n
			}\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	function updateAdminExportLinks(selectedPeriodId) {\n
		const csv = document.getElementById('dc-roster-export-csv');\n
		const printLink = document.getElementById('dc-roster-export-print');\n
		if (!csv || !printLink) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		const baseCsv = String(urls.rosterExportCsv || '');\n
		const basePrint = String(urls.rosterPrint || '');\n
		const id = Number(selectedPeriodId);\n
		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;\n
		if (!ok) {\n
			csv.href = '#dc-roster-period-section';\n
			printLink.href = '#dc-roster-period-section';\n
			csv.setAttribute('aria-disabled', 'true');\n
			printLink.setAttribute('aria-disabled', 'true');\n
			return;\n
		}\n
		const q = new URLSearchParams({ periodId: String(id) }).toString();\n
		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;\n
		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;\n
		csv.removeAttribute('aria-disabled');\n
		printLink.removeAttribute('aria-disabled');\n
	}\n
\n
	function wireAdminExportGuards() {\n
		const guard = (event) => {\n
			const el = event.currentTarget;\n
			if (el && el.getAttribute('aria-disabled') === 'true') {\n
				event.preventDefault();\n
				Msg.announce(\n
					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),\n
					'warning',\n
				);\n
			}\n
		};\n
		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);\n
		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);\n
	}\n
\n
	let rosterPhpL10nCache;\n
	function getRosterPhpL10n() {\n
		if (rosterPhpL10nCache !== undefined) {\n
			return rosterPhpL10nCache;\n
		}\n
		const el = document.getElementById('dc-roster-php-l10n');\n
		const raw = el && el.dataset ? el.dataset.l10n : '';\n
		if (!raw) {\n
			rosterPhpL10nCache = null;\n
			return null;\n
		}\n
		try {\n
			rosterPhpL10nCache = JSON.parse(raw) || null;\n
		} catch (_) {\n
			rosterPhpL10nCache = null;\n
		}\n
		return rosterPhpL10nCache;\n
	}\n
\n
	function translateConflictMessage(message) {\n
		const raw = String(message || '');\n
		if (!raw) {\n
			return t('dutycheck', 'Unknown conflict');\n
		}\n
		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];\n
		if (fromPhp) {\n
			return fromPhp;\n
		}\n
		return t('dutycheck', raw);\n
	}\n
\n
	function render(data) {\n
		state.lastRosterData = data;\n
		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {\n
			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);\n
			state.planningDefaultsFresh = true;\n
		}\n
		state.periods = data.periods || [];\n
		state.employees = data.employees || [];\n
		state.locations = data.locations || [];\n
		state.assignments = data.assignments || [];\n
		state.absenceBlocks = data.absenceBlocks || [];\n
		state.canCreateAssignments = Boolean(data.canCreateAssignments);\n
		fillPeriodSwitcher(state.periods, data.selectedPeriodId);\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden) {\n
			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));\n
		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {\n
			dateInput.min = String(selectedPeriod.startDate);\n
			dateInput.max = String(selectedPeriod.endDate);\n
			if (!dateInput.value && state.canCreateAssignments) {\n
				dateInput.value = String(selectedPeriod.startDate);\n
			}\n
		} else if (dateInput) {\n
			dateInput.removeAttribute('min');\n
			dateInput.removeAttribute('max');\n
		}\n
		fillSelect('dc-assignment-location', state.locations, {\n
			emptyText: t('dutycheck', 'No active locations yet'),\n
		});\n
		updateLocationTimezoneHint();\n
		refreshAssignmentFormEligibility();\n
		renderAssignments(data.assignments || []);\n
		renderConflicts(data.conflicts || []);\n
		renderSetupCallout(data);\n
		updateAddAssignmentControl();\n
		updateAdminExportLinks(data.selectedPeriodId);\n
		fillCopySourceSelect(data.selectedPeriodId);\n
		refreshAcknowledgeStats(data.selectedPeriodId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
	}\n
\n
	function selectedPeriodIdFromUrl() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const raw = params.get('periodId');\n
		const parsed = Number(raw);\n
		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;\n
	}\n
\n
	function updateUrlPeriodId(periodId) {\n
		const params = new URLSearchParams(window.location.search || '');\n
		if (periodId && Number(periodId) > 0) {\n
			params.set('periodId', String(periodId));\n
		} else {\n
			params.delete('periodId');\n
		}\n
		const query = params.toString();\n
		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);\n
	}\n
\n
	async function loadRoster(periodId) {\n
		if (C.isModalOpen?.()) {\n
			C.dismissOpenModal();\n
		} else {\n
			restoreAssignmentFormPanel();\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		C.setLoadingRow(tbody, 7);\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });\n
			render(response?.data || {});\n
			updateUrlPeriodId(response?.data?.selectedPeriodId || null);\n
			void loadRosterSignals(response?.data || {});\n
			void loadCoverageStrip(response?.data?.selectedPeriodId);\n
			return response?.data || {};\n
		} catch (err) {\n
			const code = String(err?.payload?.error?.code || err?.code || '');\n
			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {\n
				try {\n
					const fallback = await Api.get('/apps/dutycheck/api/roster', {});\n
					render(fallback?.data || {});\n
					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);\n
					void loadRosterSignals(fallback?.data || {});\n
					void loadCoverageStrip(fallback?.data?.selectedPeriodId);\n
					Msg.announce(\n
						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),\n
						'warning',\n
					);\n
					return fallback?.data || {};\n
				} catch (retryErr) {\n
					Msg.handleApiError(retryErr);\n
					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
					return null;\n
				}\n
			}\n
			Msg.handleApiError(err);\n
			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
			return null;\n
		} finally {\n
			C.clearLoadingRow(tbody);\n
		}\n
	}\n
\n
	function dateOverlapsBlackout(day, blackout) {\n
		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);\n
		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);\n
		if (!start || !end) return false;\n
		return day >= start && day <= end;\n
	}\n
\n
	async function loadRosterSignals(data) {\n
		state.preferencesByEmployee = {};\n
		state.blackoutsByEmployeeDate = {};\n
		const employees = data.employees || state.employees || [];\n
		const period = (data.periods || state.periods || []).find(\n
			(p) => Number(p.id) === Number(data.selectedPeriodId),\n
		);\n
		const from = period?.startDate || period?.start_date || '';\n
		const to = period?.endDate || period?.end_date || '';\n
		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);\n
		if (!ids.length) return;\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/roster/signals', {\n
				employeeIds: ids,\n
				from,\n
				to,\n
			});\n
			const prefs = res?.data?.preferences || [];\n
			const blackouts = res?.data?.blackouts || [];\n
			prefs.forEach((p) => {\n
				const eid = String(p.employeeId || p.employee_id || '');\n
				if (!eid) return;\n
				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];\n
				state.preferencesByEmployee[eid].push(p);\n
			});\n
			const days = [];\n
			if (from && to) {\n
				const cur = new Date(from + 'T12:00:00');\n
				const end = new Date(to + 'T12:00:00');\n
				while (cur <= end) {\n
					const pad = (n) => String(n).padStart(2, '0');\n
					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);\n
					cur.setDate(cur.getDate() + 1);\n
				}\n
			}\n
			blackouts.forEach((b) => {\n
				const eid = Number(b.employeeId || b.employee_id || 0);\n
				if (!eid) return;\n
				days.forEach((day) => {\n
					if (dateOverlapsBlackout(day, b)) {\n
						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;\n
					}\n
				});\n
			});\n
			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {\n
				paintGridWindow({ force: true });\n
			}\n
		} catch (_) {\n
			// Optional enrichment — ignore failures.\n
		}\n
	}\n
\n
	async function loadCoverageStrip(periodId) {\n
		const strip = document.getElementById('dc-coverage-strip');\n
		const text = document.getElementById('dc-coverage-strip-text');\n
		if (!strip || !text) return;\n
		const id = Number(periodId || 0);\n
		if (!id) {\n
			strip.hidden = true;\n
			return;\n
		}\n
		try {\n
			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);\n
			const d = res?.data || {};\n
			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);\n
			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);\n
			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);\n
			let message;\n
			if (ready || (hard === 0 && soft === 0)) {\n
				message = t('dutycheck', 'Ready to publish — no blocking issues.');\n
				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--ok');\n
			} else if (hard > 0) {\n
				message = t('dutycheck', '{n} must-fix issues before publish.')\n
					.replace('{n}', String(hard));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');\n
				strip.classList.add('dc-coverage-strip--critical');\n
			} else {\n
				message = t('dutycheck', '{n} items need confirmation.')\n
					.replace('{n}', String(soft));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--warn');\n
			}\n
			text.textContent = message;\n
			strip.hidden = false;\n
		} catch (_) {\n
			strip.hidden = true;\n
		}\n
	}\n
\n
	function currentSuggestLocationId() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const fromUrl = Number(params.get('locationId') || 0);\n
		if (Number.isInteger(fromUrl) && fromUrl > 0) {\n
			return fromUrl;\n
		}\n
		const filterEl = document.getElementById('dc-roster-location')\n
			|| document.getElementById('dc-assignment-location');\n
		const fromUi = Number(filterEl?.value || 0);\n
		if (Number.isInteger(fromUi) && fromUi > 0) {\n
			return fromUi;\n
		}\n
		return null;\n
	}\n
\n
	async function runSuggestFill() {\n
		const periodId = Number(selectedPeriodId() || 0);\n
		if (!periodId) {\n
			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');\n
			return;\n
		}\n
		const btn = document.getElementById('dc-roster-suggest-fill');\n
		if (btn) {\n
			btn.disabled = true;\n
			btn.setAttribute('aria-busy', 'true');\n
		}\n
		const locId = currentSuggestLocationId();\n
		const suggestBody = { locationId: locId || null };\n
		try {\n
			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);\n
			const d = preview?.data || {};\n
			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {\n
				const noLoc = Number(d.skippedNoLocation ?? 0);\n
				Msg.announce(\n
					noLoc > 0\n
						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')\n
						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),\n
					'warning',\n
				);\n
				return;\n
			}\n
			const body = create('div', { class: 'dc-suggest-preview' }, [\n
				create('p', {\n
					text: t('dutycheck', 'Would create {n} shifts from patterns.')\n
						.replace('{n}', String(d.created ?? 0)),\n
				}),\n
				create('ul', { class: 'dc-suggest-preview__counts' }, [\n
					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),\n
				]),\n
			]);\n
			const confirmed = await new Promise((resolve) => {\n
				let settled = false;\n
				const finish = (value) => {\n
					if (settled) return;\n
					settled = true;\n
					resolve(value);\n
				};\n
				C.openModal({\n
					title: t('dutycheck', 'Suggest fill preview'),\n
					primaryLabel: t('dutycheck', 'Apply suggest fill'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					render: () => body,\n
					onSubmit: async () => {\n
						finish(true);\n
						return true;\n
					},\n
					onCancel: () => finish(false),\n
					onClose: () => finish(false),\n
				});\n
			});\n
			if (!confirmed) return;\n
			if ((d.created ?? 0) < 1) {\n
				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');\n
				return;\n
			}\n
			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);\n
			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');\n
			await loadRoster(periodId);\n
		} catch (err) {\n
			const code = String(err?.code || err?.payload?.error?.code || '');\n
			if (code === 'ROTATION_DISABLED') {\n
				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');\n
			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {\n
				Msg.announce(\n
					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),\n
					'warning',\n
				);\n
			} else if (code === 'RATE_LIMITED') {\n
				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');\n
			} else {\n
				Msg.handleApiError(err);\n
			}\n
		} finally {\n
			if (btn) {\n
				btn.disabled = false;\n
				btn.removeAttribute('aria-busy');\n
			}\n
		}\n
	}\n
\n
	function clearAssignmentFormFields() {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) return;\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		const periodVal = periodHidden ? String(periodHidden.value || '') : '';\n
		form.reset();\n
		if (periodHidden && periodVal) {\n
			periodHidden.value = periodVal;\n
		}\n
		applyDefaultBreakToForm();\n
		D?.applyLocaleToTemporalInputs(form);\n
		refreshAssignmentFormEligibility();\n
		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');\n
		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {\n
			showAssignmentFormSuccess(clearedMsg, 'info');\n
		}\n
		Msg.announce(clearedMsg, 'info');\n
	}\n
\n
	function readForm(form) {\n
		const data = new FormData(form);\n
		const rawStart = String(data.get('startTime') || '');\n
		const rawEnd = String(data.get('endTime') || '');\n
		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();\n
		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();\n
		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);\n
		const out = {\n
			periodId: Number(data.get('periodId')),\n
			employeeId: Number(data.get('employeeId')),\n
			locationId: Number(data.get('locationId')),\n
			dutyDate: String(data.get('dutyDate') || ''),\n
			startTime,\n
			endTime,\n
			breakMinutes: Number(data.get('breakMinutes') || 0),\n
			note: String(data.get('note') || '').trim(),\n
			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,\n
		};\n
		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {\n
			out.expectedVersion = Number(state.editingAssignmentVersion);\n
		}\n
		return out;\n
	}\n
\n
	/**\n
	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.\n
	 */\n
	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;\n
		if (start === null || end === null) return null;\n
		let endAdj = end;\n
		if (endAdj <= start) {\n
			endAdj += 24 * 60;\n
		}\n
		return (endAdj - start) - breakMinutes;\n
	}\n
\n
	function validate(payload) {\n
		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {\n
			return t('dutycheck', 'Select a period before saving.');\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		if (!period) {\n
			return t('dutycheck', 'That planning period was not found.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {\n
			return t('dutycheck', 'Select an employee.');\n
		}\n
		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {\n
			return t('dutycheck', 'Select a location.');\n
		}\n
		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {\n
			return t('dutycheck', 'Date, start time, and end time are required.');\n
		}\n
		if (period.startDate && period.endDate\n
			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {\n
			return t('dutycheck', 'Date must fall within the selected period.');\n
		}\n
		if (payload.startTime === payload.endTime) {\n
			return t(\n
				'dutycheck',\n
				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
			);\n
		}\n
		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {\n
			return t('dutycheck', 'Break minutes must be between 0 and 720.');\n
		}\n
		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
		if (effective === null) {\n
			return t('dutycheck', 'Please check the dates and times.');\n
		}\n
		if (effective <= 0) {\n
			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
		}\n
		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			return t('dutycheck', 'This employee has approved leave on the selected date.');\n
		}\n
		if (findOverlapAssignment(payload)) {\n
			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');\n
		}\n
		return null;\n
	}\n
\n
	function errorMessageFor(error) {\n
		const code = errorCodeFrom(error);\n
		switch (code) {\n
			case 'PERIOD_NOT_OPEN':\n
				return t('dutycheck', 'The selected period is not open for planning.');\n
			case 'PERIOD_NOT_FOUND':\n
				return t('dutycheck', 'That planning period was not found.');\n
			case 'DATE_OUTSIDE_PERIOD':\n
				return t('dutycheck', 'Date must fall within the selected period.');\n
			case 'EMPLOYEE_NOT_FOUND':\n
				return t('dutycheck', 'Employee not found or no longer available.');\n
			case 'LOCATION_NOT_FOUND':\n
				return t('dutycheck', 'Location not found or no longer available.');\n
			case 'EQUAL_DUTY_TIMES':\n
				return t(\n
					'dutycheck',\n
					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
				);\n
			case 'INVALID_SHIFT_LENGTH':\n
				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
			case 'INVALID_BREAK_MINUTES':\n
				return t('dutycheck', 'Break minutes are out of the allowed range.');\n
			case 'NOTE_TOO_LONG':\n
				return t('dutycheck', 'Note is too long (maximum 512 characters).');\n
			case 'ASSIGNMENT_OVERLAP':\n
				return t('dutycheck', 'This employee already has an overlapping assignment.');\n
			case 'ASSIGNMENT_DUPLICATE_SLOT':\n
				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');\n
			case 'STALE_VERSION':\n
				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');\n
			case 'CONFLICT_ACK_STALE':\n
				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');\n
			case 'COMPANY_MEMBERSHIP_REQUIRED':\n
				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');\n
			case 'EXPECTED_VERSION_REQUIRED':\n
				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');\n
			case 'ABSENCE_CONFLICT':\n
				return t('dutycheck', 'This employee has an approved absence on that date.');\n
			case 'BLACKOUT_CONFLICT':\n
				return t('dutycheck', 'This employee marked that they cannot work during this time.');\n
			case 'CONFLICT_ACK_REQUIRED':\n
				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');\n
			case 'REASON_TOO_SHORT':\n
				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');\n
			case 'INTERNAL_ERROR':\n
				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');\n
			case 'QUALIFICATION_MISSING':\n
				return t('dutycheck', 'This employee is missing a required qualification for that location.');\n
			case 'ASSIGNMENT_NOT_FOUND':\n
				return t('dutycheck', 'That assignment no longer exists. Reload the page.');\n
			case 'SCHEMA_NOT_READY':\n
				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');\n
			case 'INVALID_DATE':\n
			case 'INVALID_TIME':\n
				return t('dutycheck', 'Please check the dates and times.');\n
			case 'PERIOD_ID_REQUIRED':\n
				return t('dutycheck', 'Select a period before saving.');\n
			case 'EMPLOYEE_ID_REQUIRED':\n
				return t('dutycheck', 'Select an employee.');\n
			case 'LOCATION_ID_REQUIRED':\n
				return t('dutycheck', 'Select a location.');\n
			default:\n
				return null;\n
		}\n
	}\n
\n
\n
	async function loadPendingSwaps() {\n
		const list = document.getElementById('dc-swap-list');\n
		const empty = document.getElementById('dc-swap-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/swaps');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				li.appendChild(create('p', {\n
					text: row.toEmployeeId\n
						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId))\n
							.replace('{to}', String(row.toEmployeeId))\n
						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId)),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });\n
						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');\n
						await loadPendingSwaps();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });\n
						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');\n
						await loadPendingSwaps();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load swap requests.');\n
			}\n
		}\n
	}\n
\n
	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {\n
		try {\n
			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {\n
				acknowledgements,\n
			});\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Approve with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return approveOpenShiftClaim(openShiftId, acks, false);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	async function loadPendingOpenClaims() {\n
		const list = document.getElementById('dc-open-claim-list');\n
		const empty = document.getElementById('dc-open-claim-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;\n
				li.appendChild(create('p', {\n
					text: t('dutycheck', '{date} · employee #{emp}')\n
						.replace('{date}', String(when))\n
						.replace('{emp}', String(row.claimedByEmployeeId || '—')),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await approveOpenShiftClaim(row.id);\n
						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');\n
						await loadPendingOpenClaims();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});\n
						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');\n
						await loadPendingOpenClaims();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load pending claims.');\n
			}\n
		}\n
	}\n
\n
	function wireMarketplace() {\n
		const toggle = document.getElementById('dc-open-shift-create');\n
		const form = document.getElementById('dc-open-shift-form');\n
		toggle?.addEventListener('click', () => {\n
			if (!form) return;\n
			form.hidden = !form.hidden;\n
			const loc = document.getElementById('dc-os-location');\n
			if (loc && state.locations?.length) {\n
				loc.replaceChildren();\n
				for (const l of state.locations) {\n
					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));\n
				}\n
			}\n
			const period = selectedPeriodFromState();\n
			const date = document.getElementById('dc-os-date');\n
			if (date && period?.startDate) date.value = String(period.startDate);\n
		});\n
		document.getElementById('dc-os-save')?.addEventListener('click', async () => {\n
			const period = selectedPeriodFromState();\n
			if (!period?.id) {\n
				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');\n
				return;\n
			}\n
			try {\n
				await Api.post('/apps/dutycheck/api/open-shifts', {\n
					periodId: Number(period.id),\n
					locationId: Number(document.getElementById('dc-os-location')?.value || 0),\n
					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),\n
					startTime: String(document.getElementById('dc-os-start')?.value || ''),\n
					endTime: String(document.getElementById('dc-os-end')?.value || ''),\n
					breakMinutes: 0,\n
				});\n
				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');\n
				if (form) form.hidden = true;\n
			} catch (err) {\n
				Msg.handleApiError(err);\n
			}\n
		});\n
	}\n
\n
	document.addEventListener('DOMContentLoaded', async () => {\n
		D?.applyLocaleToTemporalInputs(document);\n
		wireBreakPrefillHint();\n
		window.addEventListener('dc-planning-defaults-changed', (event) => {\n
			const minutes = Number(event?.detail?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
		});\n
		wireAdminExportGuards();\n
		wireMarketplace();\n
		setRosterView('grid');\n
		wireRosterVirtualization();\n
		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));\n
		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));\n
		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {\n
			gridState.selected.clear();\n
			syncVisibleGridChrome();\n
			updateBulkBar();\n
		});\n
		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {\n
			void applyBulkFromTemplate();\n
		});\n
		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {\n
			void runSuggestFill();\n
		});\n
		await Promise.all([\n
			loadRoster(selectedPeriodIdFromUrl()),\n
			loadPendingSwaps(),\n
			loadPendingOpenClaims(),\n
		]);\n
\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		switcher?.addEventListener('change', async () => {\n
			clearAssignmentsSectionSuccess();\n
			const periodId = Number(switcher.value);\n
			await Promise.all([\n
				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),\n
				loadPendingSwaps(),\n
				loadPendingOpenClaims(),\n
			]);\n
		});\n
\n
		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {\n
			const btn = event.target.closest('.dc-roster-add-assignment-trigger');\n
			if (!btn) {\n
				return;\n
			}\n
			openAssignmentCreateModal(btn);\n
		});\n
\n
		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {\n
			clearAssignmentFormFields();\n
		});\n
\n
		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {\n
			applySelectedTemplate();\n
		});\n
		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {\n
			runCopyPeriod(true);\n
		});\n
		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {\n
			if (!state.copyPreviewReady) {\n
				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');\n
				return;\n
			}\n
			await runCopyPeriod(false);\n
		});\n
		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {\n
			state.copyPreviewReady = false;\n
			const applyBtn = document.getElementById('dc-roster-copy-apply');\n
			if (applyBtn) applyBtn.disabled = true;\n
		});\n
\n
		const form = document.getElementById('dc-assignment-form');\n
		const scheduleEligibilityRefresh = () => {\n
			clearAssignmentFormSuccess();\n
			clearAssignmentsSectionSuccess();\n
			refreshAssignmentFormEligibility();\n
		};\n
		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);\n
		form?.addEventListener('submit', (event) => {\n
			event.preventDefault();\n
			const dialog = form.closest('.dc-modal__dialog--roster-assignment');\n
			if (!dialog) {\n
				return;\n
			}\n
			const primary = dialog.querySelector('.dc-modal__actions .button.primary');\n
			if (primary && !primary.disabled) {\n
				primary.click();\n
			}\n
		});\n
	});\n
})();\n
' contains "assignments.slice(range.start, range.end)".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:62

FAILURES!
Tests: 6, Assertions: 59, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.724913
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.210237
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.272991
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.240926
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.183301
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.185919
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.169311
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.238648
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.309942
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 5.198321
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.235543
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.138408
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.099085
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.096606
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.83668
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.16546
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.109465
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.371374
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.218759
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.101231
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.096252
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.53868
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.175492
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 2.413843
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /assignments\.slice\(range\.start,\s*range\.end\)/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105627 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments;
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:251:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.186126
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.316786
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.510205
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 4.964939
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 102.168865
killed list_paints_every_assignment
== mutant skip_print_expand (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

..F...                                                              6 / 6 (100%)

Time: 00:00.009, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testRosterJsWindowsRowsAndKeepsKeyboardReach
Failed asserting that '(function () {\n
	'use strict';\n
\n
	const Api = window.DutyCheckApi;\n
	const Msg = window.DutyCheckMessaging;\n
	const C = window.DutyCheckComponents || window.DutyCheckDom || {};\n
	const D = window.DutyCheckDates;\n
	const ConflictLabels = window.DutyCheckConflictLabels;\n
	const create = C.createElement;\n
	if (typeof create !== 'function') {\n
		throw new Error('DutyCheck components failed to load');\n
	}\n
\n
	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n
\n
	const state = {\n
		periods: [],\n
		employees: [],\n
		locations: [],\n
		assignments: [],\n
		absenceBlocks: [],\n
		canCreateAssignments: false,\n
		lastRosterData: null,\n
		defaultBreakMinutes: 0,\n
		planningDefaultsFresh: false,\n
		templates: [],\n
		templatesLoaded: false,\n
		editingAssignmentId: null,\n
		editingAssignmentVersion: null,\n
		copyPreviewReady: false,\n
		preferencesByEmployee: {},\n
		blackoutsByEmployeeDate: {},\n
	};\n
	let assignmentModalInstance = null;\n
	let suggestedBreakMinutes = 0;\n
\n
	function clampBreakMinutes(value) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n)) {\n
			return 0;\n
		}\n
		return Math.max(0, Math.min(720, Math.round(n)));\n
	}\n
\n
	function readStoredBreakMinutes() {\n
		try {\n
			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n
			if (raw === null || raw === '') {\n
				return null;\n
			}\n
			return clampBreakMinutes(raw);\n
		} catch (_) {\n
			return null;\n
		}\n
	}\n
\n
	function rememberBreakMinutes(minutes) {\n
		try {\n
			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n
		} catch (_) {\n
			/* ignore quota / privacy mode */\n
		}\n
	}\n
\n
	function resolveBreakPrefill() {\n
		const org = clampBreakMinutes(state.defaultBreakMinutes);\n
		// Organisation default from Settings always wins when it is greater than zero.\n
		if (org > 0) {\n
			return { minutes: org, source: 'settings' };\n
		}\n
		const stored = readStoredBreakMinutes();\n
		if (stored !== null) {\n
			return { minutes: stored, source: 'last' };\n
		}\n
		return { minutes: 0, source: 'settings' };\n
	}\n
\n
	async function refreshPlanningDefaultFromServer() {\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n
			const minutes = Number(response?.planning?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
			state.planningDefaultsFresh = true;\n
		} catch (_) {\n
			/* Fall back to defaultBreakMinutes from the last roster API load. */\n
		}\n
	}\n
\n
	function breakPrefillMessage(prefill) {\n
		const n = String(prefill.minutes);\n
		if (prefill.source === 'last') {\n
			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n
		}\n
		if (prefill.minutes === 0) {\n
			return t('dutycheck', 'Pre-filled with 0 min — no break.');\n
		}\n
		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n
	}\n
\n
	function updateBreakPrefillHint(userEdited) {\n
		const el = document.getElementById('dc-assignment-break-prefill');\n
		if (!el) {\n
			return;\n
		}\n
		if (userEdited) {\n
			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n
			return;\n
		}\n
		el.textContent = breakPrefillMessage(resolveBreakPrefill());\n
	}\n
\n
	function applyDefaultBreakToForm() {\n
		const prefill = resolveBreakPrefill();\n
		suggestedBreakMinutes = prefill.minutes;\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(prefill.minutes);\n
		}\n
		updateBreakPrefillHint(false);\n
	}\n
\n
	function wireBreakPrefillHint() {\n
		const br = document.getElementById('dc-assignment-break');\n
		if (!br || br.dataset.dcBreakPrefillWired) {\n
			return;\n
		}\n
		br.dataset.dcBreakPrefillWired = '1';\n
		br.addEventListener('input', () => {\n
			const current = clampBreakMinutes(br.value);\n
			updateBreakPrefillHint(current !== suggestedBreakMinutes);\n
		});\n
		br.addEventListener('change', () => {\n
			const current = clampBreakMinutes(br.value);\n
			if (current === suggestedBreakMinutes) {\n
				updateBreakPrefillHint(false);\n
			}\n
		});\n
	}\n
\n
	function dateToDayIndex(isoDate) {\n
		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n
		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n
			return null;\n
		}\n
		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n
	}\n
\n
	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n
		const dayIndex = dateToDayIndex(dutyDate);\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime);\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime);\n
		if (dayIndex === null || start === null || end === null) {\n
			return null;\n
		}\n
		const absoluteStart = (dayIndex * 1440) + start;\n
		let absoluteEnd = (dayIndex * 1440) + end;\n
		if (absoluteEnd <= absoluteStart) {\n
			absoluteEnd += 1440;\n
		}\n
		return [absoluteStart, absoluteEnd];\n
	}\n
\n
	function absoluteRangesOverlap(a, b) {\n
		return a[0] < b[1] && b[0] < a[1];\n
	}\n
\n
	function isDateInAbsenceBlock(employeeId, dutyDate) {\n
		if (!dutyDate) {\n
			return false;\n
		}\n
		for (const block of state.absenceBlocks) {\n
			if (Number(block?.employeeId) !== Number(employeeId)) {\n
				continue;\n
			}\n
			const start = String(block?.startDate || '');\n
			const end = String(block?.endDate || '');\n
			if (dutyDate >= start && dutyDate <= end) {\n
				return true;\n
			}\n
		}\n
		return false;\n
	}\n
\n
	function employeesAvailableOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return [];\n
		}\n
		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n
	}\n
\n
	function countEmployeesBlockedOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return 0;\n
		}\n
		return state.employees.length - employeesAvailableOnDate(dutyDate).length;\n
	}\n
\n
	function appendHintLink(parent, beforeText, linkText, afterText, href) {\n
		parent.appendChild(document.createTextNode(beforeText));\n
		if (href) {\n
			const link = create('a', {\n
				class: 'dc-hint-link',\n
				href,\n
				text: linkText,\n
			});\n
			parent.appendChild(link);\n
			parent.appendChild(document.createTextNode(afterText));\n
		} else {\n
			parent.appendChild(document.createTextNode(linkText + afterText));\n
		}\n
	}\n
\n
	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n
		const hint = document.getElementById('dc-assignment-employee-hint');\n
		if (!hint) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		hint.replaceChildren();\n
		if (!dutyDate) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n
			));\n
			return;\n
		}\n
		if (!available.length && blocked > 0) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n
					.replace('{n}', String(blocked)),\n
				t('dutycheck', 'review absences'),\n
				'.',\n
				String(urls.absences || ''),\n
			);\n
			return;\n
		}\n
		if (!available.length) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'No employees are available. Add an active employee on the '),\n
				t('dutycheck', 'Employees'),\n
				t('dutycheck', ' page first.'),\n
				String(urls.employees || ''),\n
			);\n
			return;\n
		}\n
		if (blocked > 0) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n
					.replace('{available}', String(available.length))\n
					.replace('{hidden}', String(blocked)),\n
			));\n
			return;\n
		}\n
		hint.appendChild(document.createTextNode(\n
			t('dutycheck', 'All {n} active employees can be scheduled on this date.')\n
				.replace('{n}', String(available.length)),\n
		));\n
	}\n
\n
	function refreshEmployeeSelectForDate() {\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const dutyDate = dateInput ? String(dateInput.value || '') : '';\n
		const available = employeesAvailableOnDate(dutyDate);\n
		const blocked = countEmployeesBlockedOnDate(dutyDate);\n
		setEmployeeAvailabilityHint(dutyDate, available, blocked);\n
		fillSelect('dc-assignment-employee', available, {\n
			emptyText: dutyDate\n
				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n
				: t('dutycheck', 'Pick a date first'),\n
		});\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		if (employeeSelect) {\n
			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n
		}\n
	}\n
\n
	function findOverlapAssignment(payload) {\n
		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n
		if (!candidate) {\n
			return null;\n
		}\n
		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		for (const assignment of state.assignments) {\n
			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n
				continue;\n
			}\n
			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n
				continue;\n
			}\n
			const existing = assignmentAbsoluteRange(\n
				String(assignment?.dutyDate || ''),\n
				String(assignment?.startTime || ''),\n
				String(assignment?.endTime || ''),\n
			);\n
			if (existing && absoluteRangesOverlap(candidate, existing)) {\n
				return assignment;\n
			}\n
		}\n
		return null;\n
	}\n
\n
	function setFormFeedbackA11y(el, isError) {\n
		if (!el) {\n
			return;\n
		}\n
		if (isError) {\n
			el.setAttribute('role', 'alert');\n
			el.setAttribute('aria-live', 'assertive');\n
		} else {\n
			el.setAttribute('role', 'status');\n
			el.setAttribute('aria-live', 'polite');\n
		}\n
	}\n
\n
	function focusFirstInvalidAssignmentField(payload) {\n
		if (!payload) {\n
			return;\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		const order = [\n
			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n
			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n
			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n
			{\n
				test: () => period?.startDate && period?.endDate && payload.dutyDate\n
					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n
				id: 'dc-assignment-date',\n
			},\n
			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },\n
			{ test: () => !payload.locationId, id: 'dc-assignment-location' },\n
			{ test: () => !payload.startTime, id: 'dc-assignment-start' },\n
			{ test: () => !payload.endTime, id: 'dc-assignment-end' },\n
			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },\n
			{\n
				test: () => {\n
					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
					return effective === null || effective <= 0;\n
				},\n
				id: 'dc-assignment-end',\n
			},\n
		];\n
		for (const item of order) {\n
			if (item.test()) {\n
				document.getElementById(item.id)?.focus();\n
				return;\n
			}\n
		}\n
	}\n
\n
	function selectedPeriodForPayload(payload) {\n
		const periodId = Number(payload?.periodId);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function clearAssignmentFormSuccess() {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		if (!el || el.dataset.dcSuccess !== '1') {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.dataset.dcSuccess = '';\n
		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
		setFormFeedbackA11y(el, false);\n
	}\n
\n
	function showAssignmentFormSuccess(message, kind) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		const tone = kind === 'info' ? 'info' : 'success';\n
		el.hidden = false;\n
		el.textContent = String(message);\n
		el.dataset.dcSuccess = '1';\n
		el.classList.remove('dc-roster-form__feedback--error');\n
		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');\n
		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');\n
		setFormFeedbackA11y(el, false);\n
		if (submit) {\n
			submit.disabled = submit.dataset.dcLockedBySetup === '1';\n
		}\n
		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });\n
	}\n
\n
	function labelsForAssignmentPayload(payload) {\n
		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));\n
		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		const locationSelect = document.getElementById('dc-assignment-location');\n
		const employeeName = employee\n
			? String(employee.name || employee.displayName || '')\n
			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		const locationName = location\n
			? String(location.name || '')\n
			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		return { employeeName, locationName };\n
	}\n
\n
	function buildAssignmentSavedMessage(payload) {\n
		const { employeeName, locationName } = labelsForAssignmentPayload(payload);\n
		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;\n
		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)\n
			|| `${payload.startTime} – ${payload.endTime}`;\n
		return t(\n
			'dutycheck',\n
			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',\n
		)\n
			.replace('{employee}', employeeName || '—')\n
			.replace('{date}', String(dateLabel || ''))\n
			.replace('{times}', String(times))\n
			.replace('{location}', locationName || '—');\n
	}\n
\n
	function updateAssignmentFormFeedback(payload) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		if (el.dataset.dcSuccess === '1') {\n
			return;\n
		}\n
		if (typeof payload === 'string') {\n
			el.hidden = false;\n
			el.textContent = payload;\n
			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			el.classList.add('dc-roster-form__feedback--error');\n
			setFormFeedbackA11y(el, true);\n
			if (submit) {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		if (!payload) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			return;\n
		}\n
		let message = '';\n
		let isError = false;\n
		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');\n
			isError = true;\n
		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {\n
			const overlap = findOverlapAssignment(payload);\n
			if (overlap) {\n
				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;\n
				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)\n
					|| `${overlap.startTime} – ${overlap.endTime}`;\n
				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')\n
					.replace('{date}', String(when))\n
					.replace('{times}', String(times));\n
				isError = true;\n
			}\n
		}\n
		if (!message) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			if (submit && submit.dataset.dcLockedBySetup !== '1') {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		el.hidden = false;\n
		el.textContent = message;\n
		el.classList.toggle('dc-roster-form__feedback--error', isError);\n
		setFormFeedbackA11y(el, isError);\n
		if (submit && isError) {\n
			submit.disabled = true;\n
		}\n
	}\n
\n
	function refreshAssignmentFormEligibility() {\n
		refreshEmployeeSelectForDate();\n
		updateLocationTimezoneHint();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		updateAssignmentFormFeedback(readForm(form));\n
	}\n
\n
	function updateLocationTimezoneHint() {\n
		const hint = document.getElementById('dc-assignment-location-tz-hint');\n
		const select = document.getElementById('dc-assignment-location');\n
		if (!hint || !select) {\n
			return;\n
		}\n
		const locationId = Number(select.value);\n
		const location = state.locations.find((row) => Number(row.id) === locationId);\n
		const tz = location && String(location.timezone || '').trim();\n
		if (!tz) {\n
			hint.hidden = true;\n
			hint.replaceChildren();\n
			return;\n
		}\n
		const accountTz = D?.currentTimezone?.() || '';\n
		hint.textContent = t(\n
			'dutycheck',\n
			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',\n
		)\n
			.replace('{locationTimezone}', tz)\n
			.replace('{accountTimezone}', accountTz || 'UTC');\n
		hint.hidden = false;\n
	}\n
\n
	function fillSelect(id, items, options) {\n
		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});\n
		const select = document.getElementById(id);\n
		if (!select) return;\n
		const previous = String(select.value || '');\n
		select.replaceChildren();\n
		if (!items.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = opts.emptyText;\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const item of items) {\n
			const option = document.createElement('option');\n
			option.value = String(item.id);\n
			option.textContent = String(item[opts.labelKey] || item.name || item.id);\n
			select.appendChild(option);\n
		}\n
		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {\n
			select.value = previous;\n
		}\n
	}\n
\n
	function fillPeriodSwitcher(periods, selected) {\n
		const select = document.getElementById('dc-roster-period-switcher');\n
		if (!select) return;\n
		const statusLabelMap = {\n
			open: t('dutycheck', 'Open'),\n
			published: t('dutycheck', 'Published'),\n
			closed: t('dutycheck', 'Closed'),\n
		};\n
		select.replaceChildren();\n
		if (!periods.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = t('dutycheck', 'No periods yet');\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const period of periods) {\n
			const option = document.createElement('option');\n
			option.value = String(period.id);\n
			const start = D?.formatDisplayDate(period.startDate) || period.startDate;\n
			const end = D?.formatDisplayDate(period.endDate) || period.endDate;\n
			const statusKey = String(period.status || '').toLowerCase();\n
			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();\n
			option.textContent = `${start} – ${end} · ${status}`;\n
			select.appendChild(option);\n
		}\n
		if (selected) {\n
			select.value = String(selected);\n
		}\n
	}\n
\n
	function severityBadge(severity) {\n
		const sev = String(severity || 'info');\n
		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;\n
		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });\n
		badge.setAttribute('aria-label', label);\n
		return badge;\n
	}\n
\n
	function renderConflicts(conflicts) {\n
		const list = document.getElementById('dc-conflict-list');\n
		const summary = document.getElementById('dc-conflict-summary');\n
		if (!list) return;\n
		list.setAttribute('role', 'list');\n
		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));\n
		list.replaceChildren();\n
		const hard = conflicts.filter((c) => c?.severity === 'hard').length;\n
		const soft = conflicts.filter((c) => c?.severity === 'soft').length;\n
		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;\n
		if (summary) {\n
			summary.textContent = !conflicts.length\n
				? t('dutycheck', 'No planning issues found.')\n
				: (ConflictLabels\n
					? ConflictLabels.countsSummary(hard, soft, softUnack)\n
					: String(hard));\n
		}\n
		if (!conflicts.length) {\n
			return;\n
		}\n
		for (const conflict of conflicts) {\n
			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });\n
			li.appendChild(severityBadge(conflict?.severity));\n
			const conflictMessage = String(conflict?.message || '');\n
			const titleText = conflictMessage\n
				? translateConflictMessage(conflictMessage)\n
				: t('dutycheck', 'Unknown conflict');\n
			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;\n
			const body = create('div', { class: 'dc-conflict__body' });\n
			const title = create('span', { class: 'dc-conflict__title', text: titleText });\n
			title.id = titleId;\n
			body.appendChild(title);\n
			li.setAttribute('aria-labelledby', titleId);\n
			if (conflict?.acknowledged && conflict?.ackReason) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack',\n
					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),\n
				}));\n
			} else if (conflict?.ackInvalidated) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack-invalid',\n
					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),\n
				}));\n
			}\n
			li.appendChild(body);\n
\n
			const actions = create('div', { class: 'dc-conflict__actions' });\n
			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {\n
				const ackBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Confirm'),\n
				});\n
				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));\n
				actions.appendChild(ackBtn);\n
			}\n
			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();\n
			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {\n
				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()\n
					|| t('dutycheck', 'Open ArbeitszeitCheck');\n
				const recoveryLink = create('a', {\n
					class: 'button primary',\n
					href: recoveryUrl,\n
					text: recoveryLabel,\n
					attrs: {\n
						target: '_blank',\n
						rel: 'noopener noreferrer',\n
						'aria-label': recoveryLabel,\n
					},\n
				});\n
				actions.appendChild(recoveryLink);\n
			}\n
			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];\n
			if (conflict?.type === 'absence_collision' && assignmentIds.length) {\n
				const focusBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Show assignment'),\n
				});\n
				focusBtn.addEventListener('click', () => {\n
					revealAssignmentRow(Number(assignmentIds[0]));\n
				});\n
				actions.appendChild(focusBtn);\n
			}\n
			li.appendChild(actions);\n
			list.appendChild(li);\n
		}\n
	}\n
\n
	async function acknowledgeConflict(conflictId) {\n
		const reason = await C.promptReason({\n
			title: t('dutycheck', 'Confirm this exception'),\n
			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),\n
			confirmLabel: t('dutycheck', 'Confirm'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			minLength: 10,\n
		});\n
		if (reason === null) return;\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });\n
			render(response?.data || {});\n
			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');\n
			Msg.announce(ackMsg, 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	const VIRTUAL_FALLBACK = {\n
		DEFAULT_ROW_HEIGHT: 44,\n
		DEFAULT_OVERSCAN: 6,\n
		UNSIZED_WINDOW_ROWS: 24,\n
		visibleRange(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			const rowHeight = 44;\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: total * rowHeight,\n
				rowHeight,\n
			};\n
		},\n
		scrollTopToRevealIndex() {\n
			return 0;\n
		},\n
		pageStride() {\n
			return 1;\n
		},\n
		windowCaption(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			if (total <= 0) {\n
				return { mode: 'empty', from: 0, to: 0, total: 0 };\n
			}\n
			return { mode: 'all', from: 1, to: total, total };\n
		},\n
	};\n
\n
	function virtualApi() {\n
		const api = window.DutyCheckVirtualWindow;\n
		if (api\n
			&& typeof api.visibleRange === 'function'\n
			&& typeof api.scrollTopToRevealIndex === 'function'\n
			&& typeof api.windowCaption === 'function') {\n
			return api;\n
		}\n
		return VIRTUAL_FALLBACK;\n
	}\n
\n
	function cellSelectionKey(employeeId, dutyDate) {\n
		return `${Number(employeeId)}|${String(dutyDate)}`;\n
	}\n
\n
	function prefersReducedMotion() {\n
		try {\n
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n
		} catch (_) {\n
			return false;\n
		}\n
	}\n
\n
	function measureHeight(el) {\n
		if (!(el instanceof HTMLElement)) {\n
			return 0;\n
		}\n
		const h = el.getBoundingClientRect().height;\n
		return Number.isFinite(h) && h > 0 ? h : 0;\n
	}\n
\n
	function assignmentIndex(assignments) {\n
		const byKey = new Map();\n
		for (const a of assignments || []) {\n
			const key = cellSelectionKey(a.employeeId, a.dutyDate);\n
			if (!byKey.has(key)) {\n
				byKey.set(key, []);\n
			}\n
			byKey.get(key).push(a);\n
		}\n
		return byKey;\n
	}\n
\n
	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {\n
		const emptyPanel = document.getElementById('dc-roster-assignments-empty');\n
		if (emptyPanel) {\n
			emptyPanel.hidden = !showEmptyPanel;\n
		}\n
	}\n
\n
	const gridState = {\n
		view: 'grid',\n
		selected: new Set(), // `${employeeId}|${dutyDate}`\n
		focusRow: 0,\n
		focusCol: 0,\n
		paintAll: false,\n
		rowHeight: 44,\n
		headerHeight: 44,\n
		windowStart: -1,\n
		windowEnd: -1,\n
		paintedEmployeeCount: -1,\n
		paintedDateCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	const listState = {\n
		windowStart: -1,\n
		windowEnd: -1,\n
		rowHeight: 48,\n
		paintedCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	function renderAssignments(assignments) {\n
		paintListWindow({ assignments: assignments || [], force: true });\n
		renderRosterGrid(assignments || []);\n
		updateAddAssignmentControl();\n
	}\n
\n
	function buildAssignmentRow(a, periodOpen) {\n
		const overnightHintText = t('dutycheck', 'Continues into the next day.');\n
		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));\n
		const tr = create('tr');\n
		if (a?.id != null) {\n
			tr.dataset.assignmentId = String(a.id);\n
			tr.setAttribute('tabindex', '-1');\n
		}\n
		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;\n
		const tdDate = create('td', { text: date });\n
		tdDate.dataset.cell = t('dutycheck', 'Date');\n
		tr.appendChild(tdDate);\n
\n
		const tdStart = create('td');\n
		tdStart.dataset.cell = t('dutycheck', 'Start');\n
		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));\n
		tr.appendChild(tdStart);\n
\n
		const tdEnd = create('td');\n
		tdEnd.dataset.cell = t('dutycheck', 'End');\n
		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));\n
		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {\n
			tdEnd.title = overnightHintText;\n
			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));\n
		}\n
		tr.appendChild(tdEnd);\n
\n
		const rest = [\n
			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },\n
			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },\n
			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },\n
			{ label: t('dutycheck', 'Note'), value: a.note || '' },\n
		];\n
		for (const cell of rest) {\n
			const td = create('td', { text: String(cell.value ?? '') });\n
			td.dataset.cell = cell.label;\n
			tr.appendChild(td);\n
		}\n
		const tdActions = create('td');\n
		tdActions.dataset.cell = t('dutycheck', 'Actions');\n
		tdActions.className = 'dc-table__actions';\n
		if (periodOpen && a?.id) {\n
			const editBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Edit'),\n
			});\n
			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));\n
			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));\n
			const cancelBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Cancel shift'),\n
			});\n
			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));\n
			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));\n
			tdActions.appendChild(editBtn);\n
			tdActions.appendChild(cancelBtn);\n
		} else {\n
			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));\n
		}\n
		tr.appendChild(tdActions);\n
		return tr;\n
	}\n
\n
	function createListSpacer(px) {\n
		const tr = create('tr', { class: 'dc-virtual-spacer' });\n
		tr.setAttribute('aria-hidden', 'true');\n
		tr.style.height = `${Math.max(0, px)}px`;\n
		const td = create('td');\n
		td.colSpan = 8;\n
		td.style.height = `${Math.max(0, px)}px`;\n
		td.style.padding = '0';\n
		td.style.border = '0';\n
		td.style.lineHeight = '0';\n
		tr.appendChild(td);\n
		return tr;\n
	}\n
\n
	function updateListWindowStatus(range, total) {\n
		const el = document.getElementById('dc-roster-list-status');\n
		if (!el) {\n
			return;\n
		}\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		if (cap.mode === 'all') {\n
			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));\n
			return;\n
		}\n
		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')\n
			.replace('{from}', String(cap.from))\n
			.replace('{to}', String(cap.to))\n
			.replace('{total}', String(cap.total));\n
	}\n
\n
	function paintListWindow(options) {\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		if (!tbody) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		if (!assignments.length) {\n
			tbody.replaceChildren();\n
			setAssignmentsEmptyPanelVisible(true);\n
			listState.windowStart = 0;\n
			listState.windowEnd = 0;\n
			listState.paintedCount = 0;\n
			updateListWindowStatus({ start: 0, end: 0 }, 0);\n
			return;\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const scroller = document.getElementById('dc-assignments-table-wrap');\n
		const VW = virtualApi();\n
		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: assignments.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& listState.windowStart === range.start\n
			&& listState.windowEnd === range.end\n
			&& listState.paintedCount === assignments.length) {\n
			updateListWindowStatus(range, assignments.length);\n
			return;\n
		}\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const periodOpen = canAddAssignment();\n
		const frag = document.createDocumentFragment();\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createListSpacer(range.padBefore));\n
		}\n
		const windowRows = assignments.slice(range.start, range.end);\n
		for (const a of windowRows) {\n
			frag.appendChild(buildAssignmentRow(a, periodOpen));\n
		}\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createListSpacer(range.padAfter));\n
		}\n
		tbody.replaceChildren(frag);\n
		listState.windowStart = range.start;\n
		listState.windowEnd = range.end;\n
		listState.paintedCount = assignments.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
		}\n
		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');\n
		const measured = measureHeight(sample);\n
		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {\n
			listState.rowHeight = measured;\n
			listState.remeasuring = true;\n
			paintListWindow({ assignments, force: true });\n
			listState.remeasuring = false;\n
			return;\n
		}\n
		if (measured > 0) {\n
			listState.rowHeight = measured;\n
		}\n
		updateListWindowStatus(range, assignments.length);\n
	}\n
\n
	function selectedPeriodId() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const fromSwitcher = Number(switcher?.value || 0);\n
		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {\n
			return fromSwitcher;\n
		}\n
		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;\n
	}\n
\n
	function periodDateList() {\n
		const periodId = selectedPeriodId();\n
		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));\n
		if (!period?.startDate || !period?.endDate) {\n
			return [];\n
		}\n
		const out = [];\n
		const cursor = new Date(`${period.startDate}T00:00:00Z`);\n
		const end = new Date(`${period.endDate}T00:00:00Z`);\n
		while (cursor <= end) {\n
			out.push(cursor.toISOString().slice(0, 10));\n
			cursor.setUTCDate(cursor.getUTCDate() + 1);\n
			if (out.length > 62) {\n
				break;\n
			}\n
		}\n
		return out;\n
	}\n
\n
	function updateGridWindowStatus(range, total, dayCount) {\n
		const el = document.getElementById('dc-roster-grid-status');\n
		if (!el) {\n
			return;\n
		}\n
		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		let text = '';\n
		if (cap.mode === 'all') {\n
			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));\n
		} else {\n
			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')\n
				.replace('{from}', String(cap.from))\n
				.replace('{to}', String(cap.to))\n
				.replace('{total}', String(cap.total));\n
		}\n
		if (days > 10) {\n
			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');\n
		}\n
		el.textContent = text;\n
	}\n
\n
	function createGridSpacer(px) {\n
		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });\n
		el.setAttribute('aria-hidden', 'true');\n
		el.setAttribute('role', 'presentation');\n
		el.style.height = `${Math.max(0, px)}px`;\n
		return el;\n
	}\n
\n
	function setGridEmptyState(root, message) {\n
		root.removeAttribute('aria-rowcount');\n
		root.removeAttribute('aria-colcount');\n
		root.removeAttribute('tabindex');\n
		root.style.removeProperty('--dc-roster-day-count');\n
		root.style.removeProperty('--dc-roster-day-min');\n
		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');\n
		root.setAttribute('role', 'status');\n
		root.setAttribute('aria-live', 'polite');\n
		root.replaceChildren(create('p', {\n
			class: 'dc-roster-empty-state__text',\n
			text: message,\n
		}));\n
	}\n
\n
	function prepareGridForRows(root, employees, dates) {\n
		root.setAttribute('role', 'grid');\n
		root.removeAttribute('tabindex');\n
		root.removeAttribute('aria-live');\n
		root.setAttribute('aria-rowcount', String(employees.length + 1));\n
		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));\n
		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));\n
		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));\n
		root.style.setProperty('--dc-roster-day-min', dayMin);\n
		root.classList.toggle('dc-roster-grid--long', dates.length > 10);\n
		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);\n
	}\n
\n
	function buildColHead(day, colIdx, dayMeta) {\n
		const parts = dayMeta || D?.formatRosterColumnParts?.(day);\n
		const full = parts?.full || D?.formatDisplayDate?.(day) || day;\n
		let ariaLabel = parts?.ariaLabel || full;\n
		if (parts?.isToday) {\n
			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;\n
		}\n
		const weekdayShort = parts?.weekdayShort || '';\n
		const dayNum = parts?.day || '';\n
		const classNames = ['dc-roster-grid__colhead'];\n
		if (parts?.isWeekend) {\n
			classNames.push('dc-roster-grid__colhead--weekend');\n
		}\n
		if (parts?.isToday) {\n
			classNames.push('dc-roster-grid__colhead--today');\n
		}\n
		const children = [];\n
		if (weekdayShort) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));\n
		}\n
		if (dayNum) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));\n
		}\n
		const attrs = {\n
			'data-col': String(colIdx),\n
			'aria-colindex': String(colIdx + 2),\n
			title: full,\n
			'aria-label': ariaLabel,\n
		};\n
		if (parts?.iso) {\n
			attrs['data-duty-date'] = parts.iso;\n
		}\n
		if (!children.length) {\n
			return create('div', {\n
				class: classNames.join(' '),\n
				role: 'columnheader',\n
				text: full,\n
				attrs,\n
			});\n
		}\n
		return create('div', {\n
			class: classNames.join(' '),\n
			role: 'columnheader',\n
			attrs,\n
		}, children);\n
	}\n
\n
	function buildDutyDayMeta(dates) {\n
		const map = new Map();\n
		(dates || []).forEach((day) => {\n
			map.set(day, D?.formatRosterColumnParts?.(day) || null);\n
		});\n
		return map;\n
	}\n
\n
	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {\n
		const row = create('div', {\n
			class: 'dc-roster-grid__row',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': String(rowIdx + 2) },\n
		});\n
		const prefs = state.preferencesByEmployee[String(emp.id)] || [];\n
		const prefChips = prefs.slice(0, 2).map((p) => {\n
			const band = String(p.band || '');\n
			const label = band === 'early'\n
				? t('dutycheck', 'Früh')\n
				: (band === 'late' ? t('dutycheck', 'Spät') : band);\n
			return create('span', {\n
				class: 'dc-pref-chip dc-pref-chip--static',\n
				text: label,\n
				attrs: { title: label },\n
			});\n
		});\n
		const headKids = [\n
			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),\n
		];\n
		if (prefChips.length) {\n
			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));\n
		}\n
		row.appendChild(create('div', {\n
			class: 'dc-roster-grid__rowhead',\n
			role: 'rowheader',\n
			attrs: { 'aria-colindex': '1' },\n
		}, headKids));\n
		dates.forEach((day, colIdx) => {\n
			const key = cellSelectionKey(emp.id, day);\n
			const cellAssignments = byKey.get(key) || [];\n
			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);\n
			const cell = create('div', {\n
				class: 'dc-roster-grid__cell',\n
				role: 'gridcell',\n
				attrs: {\n
					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',\n
					'data-employee-id': String(emp.id),\n
					'data-duty-date': day,\n
					'data-row': String(rowIdx),\n
					'data-col': String(colIdx),\n
					'aria-colindex': String(colIdx + 2),\n
					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',\n
				},\n
			});\n
			if (dayParts?.isWeekend) {\n
				cell.classList.add('dc-roster-grid__cell--weekend');\n
			}\n
			if (dayParts?.isToday) {\n
				cell.classList.add('dc-roster-grid__cell--today');\n
			}\n
			if (cellAssignments.length) {\n
				cell.classList.add('dc-roster-grid__cell--filled');\n
				const first = cellAssignments[0];\n
				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;\n
				cell.appendChild(create('span', {\n
					class: 'dc-roster-grid__shift',\n
					text: label,\n
				}));\n
				const editLabel = canAddAssignment()\n
					? t('dutycheck', 'Edit assignment')\n
					: t('dutycheck', 'View assignment (read-only)');\n
				cell.setAttribute('aria-label', `${editLabel}: ${label}`);\n
				if (cellAssignments.length > 1) {\n
					cell.appendChild(create('span', {\n
						class: 'dc-roster-grid__more',\n
						text: `+${cellAssignments.length - 1}`,\n
					}));\n
				}\n
			} else {\n
				cell.classList.add('dc-roster-grid__cell--empty');\n
				const blackoutKey = `${emp.id}|${day}`;\n
				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];\n
				if (hasBlackout) {\n
					cell.classList.add('dc-roster-grid__cell--blackout');\n
					cell.appendChild(create('span', {\n
						class: 'dc-blackout-mark',\n
						attrs: { title: t('dutycheck', 'Kann nicht') },\n
					}, [\n
						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),\n
						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),\n
					]));\n
					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));\n
				} else {\n
					cell.setAttribute(\n
						'aria-label',\n
						canAddAssignment()\n
							? t('dutycheck', 'Empty cell — Space to select for bulk fill')\n
							: t('dutycheck', 'Empty cell'),\n
					);\n
				}\n
			}\n
			if (gridState.selected.has(key)) {\n
				cell.classList.add('is-selected');\n
			}\n
			row.appendChild(cell);\n
		});\n
		return row;\n
	}\n
\n
	function syncVisibleGridChrome() {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {\n
			const empId = cell.getAttribute('data-employee-id');\n
			const day = cell.getAttribute('data-duty-date');\n
			const key = cellSelectionKey(empId, day);\n
			const selected = gridState.selected.has(key);\n
			cell.classList.toggle('is-selected', selected);\n
			cell.setAttribute('aria-selected', selected ? 'true' : 'false');\n
			const rowIdx = Number(cell.getAttribute('data-row'));\n
			const colIdx = Number(cell.getAttribute('data-col'));\n
			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;\n
			cell.setAttribute('tabindex', isFocus ? '0' : '-1');\n
		});\n
	}\n
\n
	function focusGridCellIfVisible(root) {\n
		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
		if (node instanceof HTMLElement) {\n
			node.scrollIntoView({\n
				block: 'nearest',\n
				inline: 'nearest',\n
				behavior: 'auto',\n
			});\n
			node.focus();\n
		}\n
	}\n
\n
	function activateGridCell(cell) {\n
		if (!(cell instanceof HTMLElement)) {\n
			return;\n
		}\n
		const rowIdx = Number(cell.getAttribute('data-row'));\n
		const colIdx = Number(cell.getAttribute('data-col'));\n
		if (Number.isInteger(rowIdx) && rowIdx >= 0) {\n
			gridState.focusRow = rowIdx;\n
		}\n
		if (Number.isInteger(colIdx) && colIdx >= 0) {\n
			gridState.focusCol = colIdx;\n
		}\n
		const empId = cell.getAttribute('data-employee-id');\n
		const day = cell.getAttribute('data-duty-date');\n
		const key = cellSelectionKey(empId, day);\n
		const filled = cell.classList.contains('dc-roster-grid__cell--filled');\n
		if (!filled) {\n
			if (canAddAssignment()) {\n
				toggleGridSelection(key);\n
				syncVisibleGridChrome();\n
				updateBulkBar();\n
			}\n
			return;\n
		}\n
		const byKey = assignmentIndex(state.assignments || []);\n
		const cellAssignments = byKey.get(key) || [];\n
		if (canAddAssignment() && cellAssignments[0]) {\n
			openAssignmentEditModal(cellAssignments[0], cell);\n
			return;\n
		}\n
		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');\n
	}\n
\n
	function ensureGridFocusVisible() {\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const employees = state.employees || [];\n
		if (!scroller || !employees.length) {\n
			return;\n
		}\n
		const VW = virtualApi();\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);\n
		const next = VW.scrollTopToRevealIndex({\n
			index: gridState.focusRow,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop: scroller.scrollTop,\n
			total: employees.length,\n
		});\n
		if (Math.abs(next - scroller.scrollTop) > 0.5) {\n
			scroller.scrollTop = next;\n
		}\n
	}\n
\n
	function renderRosterGrid(assignments) {\n
		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });\n
	}\n
\n
	function paintGridWindow(options) {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		const employees = state.employees || [];\n
		const dates = periodDateList();\n
		const byKey = assignmentIndex(assignments);\n
\n
		if (!employees.length || !dates.length) {\n
			setGridEmptyState(\n
				root,\n
				t('dutycheck', 'Add employees and an open period to use the planning grid.'),\n
			);\n
			gridState.windowStart = 0;\n
			gridState.windowEnd = 0;\n
			gridState.paintedEmployeeCount = employees.length;\n
			gridState.paintedDateCount = dates.length;\n
			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		prepareGridForRows(root, employees, dates);\n
		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));\n
		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));\n
\n
		const VW = virtualApi();\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = scroller && gridState.view === 'grid'\n
			? Math.max(0, scroller.clientHeight - headerHeight)\n
			: 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: employees.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& gridState.windowStart === range.start\n
			&& gridState.windowEnd === range.end\n
			&& gridState.paintedEmployeeCount === employees.length\n
			&& gridState.paintedDateCount === dates.length) {\n
			syncVisibleGridChrome();\n
			updateGridWindowStatus(range, employees.length, dates.length);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const savedLeft = scroller ? scroller.scrollLeft : 0;\n
		const dayMetaByDate = buildDutyDayMeta(dates);\n
		const frag = document.createDocumentFragment();\n
		const header = create('div', {\n
			class: 'dc-roster-grid__row dc-roster-grid__row--head',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': '1' },\n
		});\n
		header.appendChild(create('div', {\n
			class: 'dc-roster-grid__corner',\n
			role: 'columnheader',\n
			text: t('dutycheck', 'Person'),\n
			attrs: { 'aria-colindex': '1' },\n
		}));\n
		dates.forEach((day, colIdx) => {\n
			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));\n
		});\n
		frag.appendChild(header);\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createGridSpacer(range.padBefore));\n
		}\n
		const windowEmployees = employees.slice(range.start, range.end);\n
		windowEmployees.forEach((emp, offset) => {\n
			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));\n
		});\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createGridSpacer(range.padAfter));\n
		}\n
		root.replaceChildren(frag);\n
		gridState.windowStart = range.start;\n
		gridState.windowEnd = range.end;\n
		gridState.paintedEmployeeCount = employees.length;\n
		gridState.paintedDateCount = dates.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
			scroller.scrollLeft = savedLeft;\n
		}\n
		const headerEl = root.querySelector('.dc-roster-grid__row--head');\n
		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');\n
		const measuredHeader = measureHeight(headerEl);\n
		const measuredRow = measureHeight(bodyRow);\n
		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)\n
			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))\n
			&& !gridState.remeasuring) {\n
			if (measuredHeader > 0) {\n
				gridState.headerHeight = measuredHeader;\n
			}\n
			if (measuredRow > 0) {\n
				gridState.rowHeight = measuredRow;\n
			}\n
			gridState.remeasuring = true;\n
			paintGridWindow({ assignments, force: true });\n
			gridState.remeasuring = false;\n
			return;\n
		}\n
		if (measuredHeader > 0) {\n
			gridState.headerHeight = measuredHeader;\n
		}\n
		if (measuredRow > 0) {\n
			gridState.rowHeight = measuredRow;\n
		}\n
		bindGridInteractions(root);\n
		updateGridWindowStatus(range, employees.length, dates.length);\n
		updateBulkBar();\n
	}\n
\n
	function bindGridInteractions(root) {\n
		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {\n
			return;\n
		}\n
		root.dataset.dcGridBound = '1';\n
		root.addEventListener('click', (ev) => {\n
			const cell = ev.target.closest('[role="gridcell"]');\n
			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {\n
				return;\n
			}\n
			activateGridCell(cell);\n
		});\n
		root.addEventListener('keydown', (ev) => {\n
			const employees = state.employees || [];\n
			const dates = periodDateList();\n
			const rowCount = employees.length;\n
			const colCount = dates.length;\n
			if (!rowCount || !colCount) {\n
				return;\n
			}\n
			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];\n
			if (!gridKeys.includes(ev.key)) {\n
				return;\n
			}\n
			ev.preventDefault();\n
			if (ev.key === 'ArrowUp') {\n
				gridState.focusRow = Math.max(0, gridState.focusRow - 1);\n
			} else if (ev.key === 'ArrowDown') {\n
				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);\n
			} else if (ev.key === 'ArrowLeft') {\n
				gridState.focusCol = Math.max(0, gridState.focusCol - 1);\n
			} else if (ev.key === 'ArrowRight') {\n
				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);\n
			} else if (ev.key === 'Home') {\n
				gridState.focusCol = 0;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = 0;\n
				}\n
			} else if (ev.key === 'End') {\n
				gridState.focusCol = colCount - 1;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = rowCount - 1;\n
				}\n
			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {\n
				const VW = virtualApi();\n
				const scroller = document.getElementById('dc-roster-grid-scroller');\n
				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
				const viewportHeight = scroller\n
					? Math.max(rowHeight, scroller.clientHeight - headerHeight)\n
					: rowHeight;\n
				const stride = typeof VW.pageStride === 'function'\n
					? VW.pageStride({ rowHeight, viewportHeight })\n
					: 1;\n
				if (ev.key === 'PageDown') {\n
					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);\n
				} else {\n
					gridState.focusRow = Math.max(0, gridState.focusRow - stride);\n
				}\n
			} else if (ev.key === 'Enter') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				activateGridCell(cell);\n
				focusGridCellIfVisible(root);\n
				return;\n
			} else if (ev.key === ' ' || ev.key === 'Spacebar') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				const empId = cell?.getAttribute('data-employee-id');\n
				const day = cell?.getAttribute('data-duty-date');\n
				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');\n
				if (empId && day && isEmpty && canAddAssignment()) {\n
					toggleGridSelection(cellSelectionKey(empId, day));\n
					syncVisibleGridChrome();\n
					updateBulkBar();\n
				}\n
				focusGridCellIfVisible(root);\n
				return;\n
			}\n
			ensureGridFocusVisible();\n
			paintGridWindow({ force: false });\n
			focusGridCellIfVisible(root);\n
		});\n
	}\n
\n
	function toggleGridSelection(key) {\n
		if (gridState.selected.has(key)) {\n
			gridState.selected.delete(key);\n
		} else {\n
			gridState.selected.add(key);\n
		}\n
	}\n
\n
	function updateBulkBar() {\n
		const bar = document.getElementById('dc-roster-bulk-bar');\n
		const countEl = document.getElementById('dc-roster-bulk-count');\n
		if (!bar || !countEl) {\n
			return;\n
		}\n
		const n = gridState.selected.size;\n
		if (n < 1 || !canAddAssignment()) {\n
			bar.hidden = true;\n
			return;\n
		}\n
		bar.hidden = false;\n
		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (sel && !(sel.options && sel.options.length)) {\n
			populateBulkTemplates();\n
		}\n
	}\n
\n
	async function populateBulkTemplates() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (!sel) {\n
			return;\n
		}\n
		await loadTemplates();\n
		sel.replaceChildren();\n
		const templates = state.templates || [];\n
		if (!templates.length) {\n
			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));\n
			return;\n
		}\n
		for (const tpl of templates) {\n
			sel.appendChild(create('option', {\n
				value: String(tpl.id),\n
				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,\n
			}));\n
		}\n
	}\n
\n
	function revealAssignmentRow(id) {\n
		const assignmentId = Number(id);\n
		const assignments = state.assignments || [];\n
		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);\n
		if (idx < 0) {\n
			return;\n
		}\n
		applyRosterViewChrome('list');\n
		const run = () => {\n
			const scroller = document.getElementById('dc-assignments-table-wrap');\n
			const VW = virtualApi();\n
			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
			if (scroller) {\n
				scroller.scrollTop = VW.scrollTopToRevealIndex({\n
					index: idx,\n
					rowHeight,\n
					viewportHeight: Math.max(rowHeight, scroller.clientHeight),\n
					scrollTop: scroller.scrollTop,\n
					total: assignments.length,\n
				});\n
			}\n
			paintListWindow({ assignments, force: true });\n
			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);\n
			if (row instanceof HTMLElement) {\n
				row.scrollIntoView({\n
					block: 'nearest',\n
					behavior: 'auto',\n
				});\n
				row.focus();\n
				row.classList.add('dc-row--flash');\n
				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);\n
			}\n
		};\n
		window.requestAnimationFrame(run);\n
	}\n
\n
	function setRosterPaintAll(paintAll) {\n
		gridState.paintAll = paintAll === true;\n
		paintListWindow({ force: true });\n
		paintGridWindow({ force: true });\n
	}\n
\n
	function wireRosterVirtualization() {\n
		const gridScroller = document.getElementById('dc-roster-grid-scroller');\n
		const listScroller = document.getElementById('dc-assignments-table-wrap');\n
		let gridRaf = 0;\n
		let listRaf = 0;\n
		if (gridScroller) {\n
			gridScroller.addEventListener('scroll', () => {\n
				if (gridRaf) {\n
					return;\n
				}\n
				gridRaf = window.requestAnimationFrame(() => {\n
					gridRaf = 0;\n
					paintGridWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		if (listScroller) {\n
			listScroller.addEventListener('scroll', () => {\n
				if (listRaf) {\n
					return;\n
				}\n
				listRaf = window.requestAnimationFrame(() => {\n
					listRaf = 0;\n
					paintListWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		const resize = () => {\n
			if (gridState.view === 'grid') {\n
				paintGridWindow({ force: true });\n
			} else {\n
				paintListWindow({ force: true });\n
			}\n
		};\n
		if (typeof ResizeObserver === 'function') {\n
			const ro = new ResizeObserver(resize);\n
			if (gridScroller) {\n
				ro.observe(gridScroller);\n
			}\n
			if (listScroller) {\n
				ro.observe(listScroller);\n
			}\n
		} else {\n
			window.addEventListener('resize', resize);\n
		}\n
		window.addEventListener('beforeprint', () => {});\n
		window.addEventListener('afterprint', () => setRosterPaintAll(false));\n
		if (typeof window.matchMedia === 'function') {\n
			try {\n
				const printMq = window.matchMedia('print');\n
				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);\n
				if (typeof printMq.addEventListener === 'function') {\n
					printMq.addEventListener('change', onPrintMq);\n
				} else if (typeof printMq.addListener === 'function') {\n
					printMq.addListener(onPrintMq);\n
				}\n
			} catch (_) {\n
				/* print media query is not available in every test/jsdom shim */\n
			}\n
		}\n
	}\n
\n
	function applyRosterViewChrome(view) {\n
		gridState.view = view === 'list' ? 'list' : 'grid';\n
		const gridWrap = document.getElementById('dc-roster-grid-wrap');\n
		const listPanel = document.getElementById('dc-roster-list-panel');\n
		const gridBtn = document.getElementById('dc-roster-view-grid');\n
		const listBtn = document.getElementById('dc-roster-view-list');\n
		if (gridWrap) {\n
			gridWrap.hidden = gridState.view !== 'grid';\n
		}\n
		if (listPanel) {\n
			listPanel.hidden = gridState.view !== 'list';\n
		}\n
		if (gridBtn) {\n
			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');\n
		}\n
		if (listBtn) {\n
			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');\n
		}\n
	}\n
\n
	function setRosterView(view) {\n
		applyRosterViewChrome(view);\n
		window.requestAnimationFrame(() => {\n
			if (gridState.view === 'list') {\n
				paintListWindow({ force: true });\n
			} else {\n
				paintGridWindow({ force: true });\n
			}\n
		});\n
	}\n
\n
	async function applyBulkFromTemplate() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		const tplId = Number(sel?.value || 0);\n
		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);\n
		if (!tpl || gridState.selected.size < 1) {\n
			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');\n
			return;\n
		}\n
		if (!Number(tpl.locationId || 0)) {\n
			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');\n
			return;\n
		}\n
		const periodId = Number(selectedPeriodId() || 0);\n
		let ok = 0;\n
		let fail = 0;\n
		for (const key of Array.from(gridState.selected)) {\n
			const sep = key.indexOf('|');\n
			if (sep < 1) {\n
				fail += 1;\n
				continue;\n
			}\n
			const employeeId = Number(key.slice(0, sep));\n
			const dutyDate = key.slice(sep + 1);\n
			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {\n
				fail += 1;\n
				continue;\n
			}\n
			const locationId = Number(tpl.locationId || 0);\n
			if (!locationId) {\n
				fail += 1;\n
				continue;\n
			}\n
			try {\n
				await submitAssignment({\n
					periodId,\n
					employeeId: Number(employeeId),\n
					locationId,\n
					dutyDate,\n
					startTime: String(tpl.startTime).slice(0, 5),\n
					endTime: String(tpl.endTime).slice(0, 5),\n
					breakMinutes: Number(tpl.breakMinutes || 0),\n
					note: '',\n
					assignmentId: 0,\n
				}, true);\n
				ok += 1;\n
			} catch {\n
				fail += 1;\n
			}\n
		}\n
		gridState.selected.clear();\n
		await loadRoster(periodId || null);\n
		Msg.announce(\n
			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')\n
				.replace('{ok}', String(ok))\n
				.replace('{fail}', String(fail)),\n
			fail ? 'warning' : 'info',\n
		);\n
	}\n
\n
	function uniqueConflictTypes(conflicts) {\n
		const types = new Set();\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity === 'soft' && conflict?.type) {\n
				types.add(String(conflict.type));\n
			}\n
		}\n
		return Array.from(types);\n
	}\n
\n
	function softConflictSummary(conflicts) {\n
		const lines = [];\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity !== 'soft') {\n
				continue;\n
			}\n
			const raw = String(conflict?.message || '');\n
			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');\n
			if (text && !lines.includes(text)) {\n
				lines.push(text);\n
			}\n
		}\n
		return lines.join('\n');\n
	}\n
\n
	function errorCodeFrom(err) {\n
		return String(err?.code || err?.payload?.error?.code || '');\n
	}\n
\n
	function syncAssignmentPeriodFromSwitcher() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (!switcher || !periodHidden) {\n
			return;\n
		}\n
		const selected = Number(switcher.value);\n
		if (Number.isInteger(selected) && selected > 0) {\n
			periodHidden.value = String(selected);\n
		}\n
	}\n
\n
	function announceAssignmentSaveError(err) {\n
		clearAssignmentFormSuccess();\n
		const friendly = errorMessageFor(err);\n
		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');\n
		updateAssignmentFormFeedback(message);\n
		Msg.announce(message, 'error');\n
	}\n
\n
	async function submitAssignment(payload, retryWithAck = true) {\n
		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		try {\n
			if (editId > 0) {\n
				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);\n
			}\n
			return await Api.post('/apps/dutycheck/api/assignments', payload);\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Save with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return submitAssignment({ ...payload, acknowledgements }, false);\n
			}\n
			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Employee cannot work (blackout)'),\n
					label: t(\n
						'dutycheck',\n
						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',\n
					),\n
					confirmLabel: t('dutycheck', 'Schedule anyway'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
					maxLength: 200,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	function setAssignmentFormBusy(form, busy) {\n
		if (!form) return;\n
		const submit = form.querySelector('button[type="submit"]');\n
		const clear = document.getElementById('dc-assignment-form-clear');\n
		if (submit) {\n
			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';\n
			if (busy) {\n
				submit.setAttribute('aria-busy', 'true');\n
			} else {\n
				submit.removeAttribute('aria-busy');\n
			}\n
		}\n
		if (clear) {\n
			clear.disabled = busy;\n
		}\n
	}\n
\n
	function renderSetupCallout(data) {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		const list = document.getElementById('dc-roster-setup-checklist');\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!callout || !list || !form) return;\n
		const urls = readUrls();\n
		const missing = [];\n
		if (!state.periods.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Create a planning period.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		if (!state.employees.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active employee.'),\n
				url: urls.employees,\n
				cta: t('dutycheck', 'Go to Employees'),\n
			});\n
		}\n
		if (!state.locations.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active location.'),\n
				url: urls.locations,\n
				cta: t('dutycheck', 'Go to Locations'),\n
			});\n
		}\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));\n
		const periodIsOpen = (selectedPeriod?.status || '') === 'open';\n
		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {\n
			missing.push({\n
				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		list.replaceChildren();\n
		if (!missing.length) {\n
			callout.hidden = true;\n
			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {\n
				if (el.dataset.dcLockedBySetup === '1') {\n
					el.disabled = false;\n
					delete el.dataset.dcLockedBySetup;\n
				}\n
			});\n
			updateAddAssignmentControl();\n
			return;\n
		}\n
		for (const item of missing) {\n
			const li = create('li');\n
			li.appendChild(document.createTextNode(item.text + ' '));\n
			if (item.url) {\n
				li.appendChild(create('a', {\n
					class: 'button',\n
					href: item.url,\n
					text: item.cta,\n
				}));\n
			}\n
			list.appendChild(li);\n
		}\n
		callout.hidden = false;\n
		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {\n
			if (!el.disabled) {\n
				el.dataset.dcLockedBySetup = '1';\n
				el.disabled = true;\n
			}\n
		});\n
		updateAddAssignmentControl();\n
	}\n
\n
	function readUrls() {\n
		try {\n
			const raw = document.getElementById('app-content')?.dataset?.dcUrls;\n
			if (!raw) return {};\n
			return JSON.parse(raw) || {};\n
		} catch (_) {\n
			return {};\n
		}\n
	}\n
\n
	function restoreAssignmentFormPanel() {\n
		const host = document.getElementById('dc-assignment-form-host');\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (host && panel && panel.parentElement !== host) {\n
			host.appendChild(panel);\n
		}\n
	}\n
\n
	function selectedPeriodFromState() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;\n
		const periodId = Number(raw);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function isAssignmentSetupBlocking() {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		return !!(callout && !callout.hidden);\n
	}\n
\n
	function canAddAssignment() {\n
		if (!state.canCreateAssignments) {\n
			return false;\n
		}\n
		if (!state.periods.length || !state.employees.length || !state.locations.length) {\n
			return false;\n
		}\n
		const period = selectedPeriodFromState();\n
		return !!(period && String(period.status || '').toLowerCase() === 'open');\n
	}\n
\n
	function addAssignmentDisabledReason() {\n
		if (!state.periods.length) {\n
			return t('dutycheck', 'Create a planning period first.');\n
		}\n
		if (!state.employees.length) {\n
			return t('dutycheck', 'Add at least one active employee before planning shifts.');\n
		}\n
		if (!state.locations.length) {\n
			return t('dutycheck', 'Add at least one active location before planning shifts.');\n
		}\n
		const period = selectedPeriodFromState();\n
		if (!period) {\n
			return t('dutycheck', 'Select a period before adding assignments.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!state.canCreateAssignments) {\n
			return t('dutycheck', 'You cannot add assignments for this period.');\n
		}\n
		return t('dutycheck', 'Adding assignments is not available right now.');\n
	}\n
\n
	function updateAddAssignmentControl() {\n
		const allowed = canAddAssignment();\n
		const reason = addAssignmentDisabledReason();\n
		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {\n
			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.\n
			btn.hidden = !allowed;\n
			btn.disabled = !allowed;\n
			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');\n
			btn.classList.toggle('dc-is-disabled', !allowed);\n
			if (allowed) {\n
				btn.removeAttribute('title');\n
			} else {\n
				btn.setAttribute('title', reason);\n
			}\n
		});\n
		const emptyHint = document.getElementById('dc-roster-empty-add-hint');\n
		if (emptyHint) {\n
			emptyHint.textContent = allowed\n
				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')\n
				: reason;\n
		}\n
	}\n
\n
	function clearAssignmentsSectionSuccess() {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.classList.remove('dc-roster-flash--visible');\n
	}\n
\n
	function showAssignmentsSectionSuccess(message) {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.textContent = String(message);\n
		el.hidden = false;\n
		el.classList.add('dc-roster-flash--visible');\n
	}\n
\n
	function prepareAssignmentFormForModal() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		clearAssignmentFormSuccess();\n
		updateAssignmentFormFeedback(null);\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate && period?.endDate) {\n
			dateInput.min = String(period.startDate);\n
			dateInput.max = String(period.endDate);\n
			if (!dateInput.value) {\n
				dateInput.value = String(period.startDate);\n
			}\n
		}\n
		refreshAssignmentFormEligibility();\n
		applyDefaultBreakToForm();\n
		wireBreakPrefillHint();\n
		fillTemplateSelect();\n
		D?.applyLocaleToTemporalInputs(form);\n
	}\n
\n
	function resetAssignmentFormAfterSave(data) {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		form.reset();\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden && data?.selectedPeriodId) {\n
			periodHidden.value = String(data.selectedPeriodId);\n
		}\n
		applyDefaultBreakToForm();\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate) {\n
			dateInput.value = String(period.startDate);\n
		}\n
		D?.applyLocaleToTemporalInputs(form);\n
		updateAssignmentFormFeedback(null);\n
	}\n
\n
	async function performAssignmentSave() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return false;\n
		}\n
		if (isAssignmentSetupBlocking()) {\n
			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(msg);\n
			Msg.announce(msg, 'error');\n
			return false;\n
		}\n
		const payload = readForm(form);\n
		const validation = validate(payload);\n
		if (validation) {\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(validation);\n
			Msg.announce(validation, 'error');\n
			focusFirstInvalidAssignmentField(payload);\n
			return false;\n
		}\n
		updateAssignmentFormFeedback(null);\n
		setAssignmentFormBusy(form, true);\n
		try {\n
			const response = await submitAssignment(payload, true);\n
			render(response?.data || {});\n
			const savedMsg = buildAssignmentSavedMessage(payload);\n
			clearAssignmentFormSuccess();\n
			showAssignmentsSectionSuccess(savedMsg);\n
			Msg.announce(savedMsg, 'success');\n
			rememberBreakMinutes(payload.breakMinutes);\n
			resetAssignmentFormAfterSave(response?.data || {});\n
			return true;\n
		} catch (err) {\n
			announceAssignmentSaveError(err);\n
			return false;\n
		} finally {\n
			setAssignmentFormBusy(form, false);\n
		}\n
	}\n
\n
	function syncAssignmentModalPrimary(instance) {\n
		if (!instance?.primaryBtn) {\n
			return;\n
		}\n
		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();\n
		instance.primaryBtn.disabled = blocked;\n
		if (blocked) {\n
			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());\n
		} else {\n
			instance.primaryBtn.removeAttribute('title');\n
		}\n
	}\n
\n
	async function openAssignmentCreateModal(triggerEl) {\n
		if (!canAddAssignment()) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
			return;\n
		}\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		renderSetupCallout(state.lastRosterData || {});\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Create assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save assignment'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => {\n
				const ok = await performAssignmentSave();\n
				return ok;\n
			},\n
			onClose: (result) => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				if (!result) {\n
					clearAssignmentFormSuccess();\n
					return;\n
				}\n
				const addBtn = document.getElementById('dc-roster-add-assignment');\n
				if (addBtn && typeof addBtn.focus === 'function') {\n
					addBtn.focus();\n
				}\n
			},\n
			onCancel: () => {\n
				clearAssignmentFormSuccess();\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
		const form = document.getElementById('dc-assignment-form');\n
		if (form && !form.dataset.dcModalSyncWired) {\n
			form.dataset.dcModalSyncWired = '1';\n
			form.addEventListener('change', () => {\n
				if (assignmentModalInstance?._open) {\n
					syncAssignmentModalPrimary(assignmentModalInstance);\n
				}\n
			});\n
		}\n
	}\n
\n
	async function openAssignmentEditModal(assignment, triggerEl) {\n
		if (!canAddAssignment() || !assignment?.id) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			return;\n
		}\n
		state.editingAssignmentId = Number(assignment.id);\n
		state.editingAssignmentVersion = Number(assignment.version ?? 0);\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = String(assignment.id);\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput) {\n
			dateInput.value = String(assignment.dutyDate || '');\n
		}\n
		refreshAssignmentFormEligibility();\n
		const emp = document.getElementById('dc-assignment-employee');\n
		if (emp) {\n
			emp.value = String(assignment.employeeId || '');\n
		}\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (loc) {\n
			loc.value = String(assignment.locationId || '');\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		if (start) {\n
			start.value = String(assignment.startTime || '').slice(0, 5);\n
		}\n
		const end = document.getElementById('dc-assignment-end');\n
		if (end) {\n
			end.value = String(assignment.endTime || '').slice(0, 5);\n
		}\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(assignment.breakMinutes ?? 0);\n
		}\n
		const note = document.getElementById('dc-assignment-note');\n
		if (note) {\n
			note.value = String(assignment.note || '');\n
		}\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Edit assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save changes'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => performAssignmentSave(),\n
			onClose: () => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				clearAssignmentFormSuccess();\n
				if (triggerEl && typeof triggerEl.focus === 'function') {\n
					triggerEl.focus();\n
				}\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
	}\n
\n
	async function cancelAssignmentRow(assignment, triggerEl) {\n
		if (!assignment?.id || !canAddAssignment()) {\n
			return;\n
		}\n
		const ok = window.confirm(\n
			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),\n
		);\n
		if (!ok) {\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});\n
			render(response?.data || {});\n
			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));\n
			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		} finally {\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
		}\n
	}\n
\n
	let templatesInflight = null;\n
\n
	async function ensureAssignmentModalPrereqs() {\n
		const jobs = [loadTemplates()];\n
		if (state.planningDefaultsFresh !== true) {\n
			jobs.push(refreshPlanningDefaultFromServer());\n
		}\n
		await Promise.all(jobs);\n
	}\n
\n
	async function loadTemplates() {\n
		if (state.templatesLoaded) {\n
			fillTemplateSelect();\n
			return;\n
		}\n
		if (templatesInflight) {\n
			await templatesInflight;\n
			fillTemplateSelect();\n
			return;\n
		}\n
		templatesInflight = (async () => {\n
			try {\n
				const response = await Api.get('/apps/dutycheck/api/templates', {});\n
				state.templates = Array.isArray(response?.data) ? response.data : [];\n
				state.templatesLoaded = true;\n
			} catch (_) {\n
				state.templates = [];\n
				state.templatesLoaded = false;\n
			} finally {\n
				templatesInflight = null;\n
			}\n
		})();\n
		await templatesInflight;\n
		fillTemplateSelect();\n
	}\n
\n
	function fillTemplateSelect() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select) {\n
			return;\n
		}\n
		const current = select.value;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));\n
		for (const tpl of state.templates) {\n
			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;\n
			select.appendChild(create('option', { value: String(tpl.id), text: label }));\n
		}\n
		if (current && [...select.options].some((o) => o.value === current)) {\n
			select.value = current;\n
		}\n
	}\n
\n
	function applySelectedTemplate() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select || !select.value) {\n
			return;\n
		}\n
		const tpl = state.templates.find((t) => String(t.id) === String(select.value));\n
		if (!tpl) {\n
			return;\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		const end = document.getElementById('dc-assignment-end');\n
		const br = document.getElementById('dc-assignment-break');\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (start) start.value = String(tpl.startTime || '').slice(0, 5);\n
		if (end) end.value = String(tpl.endTime || '').slice(0, 5);\n
		if (br) br.value = String(tpl.breakMinutes ?? 0);\n
		if (loc && tpl.locationId) loc.value = String(tpl.locationId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		refreshAssignmentFormEligibility();\n
	}\n
\n
	function refreshAcknowledgeStats(periodId) {\n
		const el = document.getElementById('dc-roster-ack-stats');\n
		if (!el) {\n
			return;\n
		}\n
		const id = Number(periodId);\n
		if (!Number.isInteger(id) || id <= 0) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === id);\n
		const status = String(period?.status || '').toLowerCase();\n
		if (status !== 'published' && status !== 'closed') {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const rows = Array.isArray(state.assignments) ? state.assignments : [];\n
		let total = 0;\n
		let acked = 0;\n
		for (const row of rows) {\n
			if (String(row?.status || '').toLowerCase() === 'cancelled') {\n
				continue;\n
			}\n
			total += 1;\n
			if (row?.acknowledged || row?.acknowledgedAt) {\n
				acked += 1;\n
			}\n
		}\n
		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;\n
		el.hidden = false;\n
		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')\n
			.replace('{acked}', String(acked))\n
			.replace('{total}', String(total))\n
			.replace('{pct}', String(pct));\n
	}\n
\n
	function fillCopySourceSelect(selectedPeriodId) {\n
		const select = document.getElementById('dc-roster-copy-source');\n
		const wrap = document.getElementById('dc-roster-copy-period');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!select || !wrap) {\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));\n
		const open = period && String(period.status || '').toLowerCase() === 'open';\n
		wrap.hidden = !open;\n
		state.copyPreviewReady = false;\n
		if (applyBtn) applyBtn.disabled = true;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));\n
		for (const p of state.periods) {\n
			if (Number(p.id) === Number(selectedPeriodId)) {\n
				continue;\n
			}\n
			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;\n
			select.appendChild(create('option', { value: String(p.id), text: label }));\n
		}\n
	}\n
\n
	async function runCopyPeriod(dryRun) {\n
		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);\n
		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);\n
		const status = document.getElementById('dc-roster-copy-status');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!targetId || !sourceId) {\n
			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {\n
				sourcePeriodId: sourceId,\n
				dryRun: !!dryRun,\n
			});\n
			const data = response?.data || {};\n
			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);\n
			const skipped = Number(data.skipped ?? 0);\n
			const msg = dryRun\n
				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')\n
					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))\n
					.replace('{skipped}', String(data.wouldSkip ?? skipped))\n
				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')\n
					.replace('{created}', String(created));\n
			if (status) {\n
				status.hidden = false;\n
				status.textContent = msg;\n
			}\n
			Msg.announce(msg, dryRun ? 'info' : 'success');\n
			if (dryRun) {\n
				state.copyPreviewReady = true;\n
				if (applyBtn) applyBtn.disabled = false;\n
			} else {\n
				state.copyPreviewReady = false;\n
				if (applyBtn) applyBtn.disabled = true;\n
				render(data.roster || data || {});\n
				if (!data.roster && data.assignments) {\n
					render(data);\n
				} else if (!data.roster) {\n
					await loadRoster(targetId);\n
				}\n
			}\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	function updateAdminExportLinks(selectedPeriodId) {\n
		const csv = document.getElementById('dc-roster-export-csv');\n
		const printLink = document.getElementById('dc-roster-export-print');\n
		if (!csv || !printLink) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		const baseCsv = String(urls.rosterExportCsv || '');\n
		const basePrint = String(urls.rosterPrint || '');\n
		const id = Number(selectedPeriodId);\n
		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;\n
		if (!ok) {\n
			csv.href = '#dc-roster-period-section';\n
			printLink.href = '#dc-roster-period-section';\n
			csv.setAttribute('aria-disabled', 'true');\n
			printLink.setAttribute('aria-disabled', 'true');\n
			return;\n
		}\n
		const q = new URLSearchParams({ periodId: String(id) }).toString();\n
		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;\n
		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;\n
		csv.removeAttribute('aria-disabled');\n
		printLink.removeAttribute('aria-disabled');\n
	}\n
\n
	function wireAdminExportGuards() {\n
		const guard = (event) => {\n
			const el = event.currentTarget;\n
			if (el && el.getAttribute('aria-disabled') === 'true') {\n
				event.preventDefault();\n
				Msg.announce(\n
					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),\n
					'warning',\n
				);\n
			}\n
		};\n
		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);\n
		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);\n
	}\n
\n
	let rosterPhpL10nCache;\n
	function getRosterPhpL10n() {\n
		if (rosterPhpL10nCache !== undefined) {\n
			return rosterPhpL10nCache;\n
		}\n
		const el = document.getElementById('dc-roster-php-l10n');\n
		const raw = el && el.dataset ? el.dataset.l10n : '';\n
		if (!raw) {\n
			rosterPhpL10nCache = null;\n
			return null;\n
		}\n
		try {\n
			rosterPhpL10nCache = JSON.parse(raw) || null;\n
		} catch (_) {\n
			rosterPhpL10nCache = null;\n
		}\n
		return rosterPhpL10nCache;\n
	}\n
\n
	function translateConflictMessage(message) {\n
		const raw = String(message || '');\n
		if (!raw) {\n
			return t('dutycheck', 'Unknown conflict');\n
		}\n
		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];\n
		if (fromPhp) {\n
			return fromPhp;\n
		}\n
		return t('dutycheck', raw);\n
	}\n
\n
	function render(data) {\n
		state.lastRosterData = data;\n
		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {\n
			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);\n
			state.planningDefaultsFresh = true;\n
		}\n
		state.periods = data.periods || [];\n
		state.employees = data.employees || [];\n
		state.locations = data.locations || [];\n
		state.assignments = data.assignments || [];\n
		state.absenceBlocks = data.absenceBlocks || [];\n
		state.canCreateAssignments = Boolean(data.canCreateAssignments);\n
		fillPeriodSwitcher(state.periods, data.selectedPeriodId);\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden) {\n
			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));\n
		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {\n
			dateInput.min = String(selectedPeriod.startDate);\n
			dateInput.max = String(selectedPeriod.endDate);\n
			if (!dateInput.value && state.canCreateAssignments) {\n
				dateInput.value = String(selectedPeriod.startDate);\n
			}\n
		} else if (dateInput) {\n
			dateInput.removeAttribute('min');\n
			dateInput.removeAttribute('max');\n
		}\n
		fillSelect('dc-assignment-location', state.locations, {\n
			emptyText: t('dutycheck', 'No active locations yet'),\n
		});\n
		updateLocationTimezoneHint();\n
		refreshAssignmentFormEligibility();\n
		renderAssignments(data.assignments || []);\n
		renderConflicts(data.conflicts || []);\n
		renderSetupCallout(data);\n
		updateAddAssignmentControl();\n
		updateAdminExportLinks(data.selectedPeriodId);\n
		fillCopySourceSelect(data.selectedPeriodId);\n
		refreshAcknowledgeStats(data.selectedPeriodId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
	}\n
\n
	function selectedPeriodIdFromUrl() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const raw = params.get('periodId');\n
		const parsed = Number(raw);\n
		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;\n
	}\n
\n
	function updateUrlPeriodId(periodId) {\n
		const params = new URLSearchParams(window.location.search || '');\n
		if (periodId && Number(periodId) > 0) {\n
			params.set('periodId', String(periodId));\n
		} else {\n
			params.delete('periodId');\n
		}\n
		const query = params.toString();\n
		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);\n
	}\n
\n
	async function loadRoster(periodId) {\n
		if (C.isModalOpen?.()) {\n
			C.dismissOpenModal();\n
		} else {\n
			restoreAssignmentFormPanel();\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		C.setLoadingRow(tbody, 7);\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });\n
			render(response?.data || {});\n
			updateUrlPeriodId(response?.data?.selectedPeriodId || null);\n
			void loadRosterSignals(response?.data || {});\n
			void loadCoverageStrip(response?.data?.selectedPeriodId);\n
			return response?.data || {};\n
		} catch (err) {\n
			const code = String(err?.payload?.error?.code || err?.code || '');\n
			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {\n
				try {\n
					const fallback = await Api.get('/apps/dutycheck/api/roster', {});\n
					render(fallback?.data || {});\n
					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);\n
					void loadRosterSignals(fallback?.data || {});\n
					void loadCoverageStrip(fallback?.data?.selectedPeriodId);\n
					Msg.announce(\n
						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),\n
						'warning',\n
					);\n
					return fallback?.data || {};\n
				} catch (retryErr) {\n
					Msg.handleApiError(retryErr);\n
					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
					return null;\n
				}\n
			}\n
			Msg.handleApiError(err);\n
			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
			return null;\n
		} finally {\n
			C.clearLoadingRow(tbody);\n
		}\n
	}\n
\n
	function dateOverlapsBlackout(day, blackout) {\n
		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);\n
		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);\n
		if (!start || !end) return false;\n
		return day >= start && day <= end;\n
	}\n
\n
	async function loadRosterSignals(data) {\n
		state.preferencesByEmployee = {};\n
		state.blackoutsByEmployeeDate = {};\n
		const employees = data.employees || state.employees || [];\n
		const period = (data.periods || state.periods || []).find(\n
			(p) => Number(p.id) === Number(data.selectedPeriodId),\n
		);\n
		const from = period?.startDate || period?.start_date || '';\n
		const to = period?.endDate || period?.end_date || '';\n
		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);\n
		if (!ids.length) return;\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/roster/signals', {\n
				employeeIds: ids,\n
				from,\n
				to,\n
			});\n
			const prefs = res?.data?.preferences || [];\n
			const blackouts = res?.data?.blackouts || [];\n
			prefs.forEach((p) => {\n
				const eid = String(p.employeeId || p.employee_id || '');\n
				if (!eid) return;\n
				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];\n
				state.preferencesByEmployee[eid].push(p);\n
			});\n
			const days = [];\n
			if (from && to) {\n
				const cur = new Date(from + 'T12:00:00');\n
				const end = new Date(to + 'T12:00:00');\n
				while (cur <= end) {\n
					const pad = (n) => String(n).padStart(2, '0');\n
					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);\n
					cur.setDate(cur.getDate() + 1);\n
				}\n
			}\n
			blackouts.forEach((b) => {\n
				const eid = Number(b.employeeId || b.employee_id || 0);\n
				if (!eid) return;\n
				days.forEach((day) => {\n
					if (dateOverlapsBlackout(day, b)) {\n
						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;\n
					}\n
				});\n
			});\n
			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {\n
				paintGridWindow({ force: true });\n
			}\n
		} catch (_) {\n
			// Optional enrichment — ignore failures.\n
		}\n
	}\n
\n
	async function loadCoverageStrip(periodId) {\n
		const strip = document.getElementById('dc-coverage-strip');\n
		const text = document.getElementById('dc-coverage-strip-text');\n
		if (!strip || !text) return;\n
		const id = Number(periodId || 0);\n
		if (!id) {\n
			strip.hidden = true;\n
			return;\n
		}\n
		try {\n
			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);\n
			const d = res?.data || {};\n
			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);\n
			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);\n
			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);\n
			let message;\n
			if (ready || (hard === 0 && soft === 0)) {\n
				message = t('dutycheck', 'Ready to publish — no blocking issues.');\n
				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--ok');\n
			} else if (hard > 0) {\n
				message = t('dutycheck', '{n} must-fix issues before publish.')\n
					.replace('{n}', String(hard));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');\n
				strip.classList.add('dc-coverage-strip--critical');\n
			} else {\n
				message = t('dutycheck', '{n} items need confirmation.')\n
					.replace('{n}', String(soft));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--warn');\n
			}\n
			text.textContent = message;\n
			strip.hidden = false;\n
		} catch (_) {\n
			strip.hidden = true;\n
		}\n
	}\n
\n
	function currentSuggestLocationId() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const fromUrl = Number(params.get('locationId') || 0);\n
		if (Number.isInteger(fromUrl) && fromUrl > 0) {\n
			return fromUrl;\n
		}\n
		const filterEl = document.getElementById('dc-roster-location')\n
			|| document.getElementById('dc-assignment-location');\n
		const fromUi = Number(filterEl?.value || 0);\n
		if (Number.isInteger(fromUi) && fromUi > 0) {\n
			return fromUi;\n
		}\n
		return null;\n
	}\n
\n
	async function runSuggestFill() {\n
		const periodId = Number(selectedPeriodId() || 0);\n
		if (!periodId) {\n
			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');\n
			return;\n
		}\n
		const btn = document.getElementById('dc-roster-suggest-fill');\n
		if (btn) {\n
			btn.disabled = true;\n
			btn.setAttribute('aria-busy', 'true');\n
		}\n
		const locId = currentSuggestLocationId();\n
		const suggestBody = { locationId: locId || null };\n
		try {\n
			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);\n
			const d = preview?.data || {};\n
			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {\n
				const noLoc = Number(d.skippedNoLocation ?? 0);\n
				Msg.announce(\n
					noLoc > 0\n
						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')\n
						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),\n
					'warning',\n
				);\n
				return;\n
			}\n
			const body = create('div', { class: 'dc-suggest-preview' }, [\n
				create('p', {\n
					text: t('dutycheck', 'Would create {n} shifts from patterns.')\n
						.replace('{n}', String(d.created ?? 0)),\n
				}),\n
				create('ul', { class: 'dc-suggest-preview__counts' }, [\n
					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),\n
				]),\n
			]);\n
			const confirmed = await new Promise((resolve) => {\n
				let settled = false;\n
				const finish = (value) => {\n
					if (settled) return;\n
					settled = true;\n
					resolve(value);\n
				};\n
				C.openModal({\n
					title: t('dutycheck', 'Suggest fill preview'),\n
					primaryLabel: t('dutycheck', 'Apply suggest fill'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					render: () => body,\n
					onSubmit: async () => {\n
						finish(true);\n
						return true;\n
					},\n
					onCancel: () => finish(false),\n
					onClose: () => finish(false),\n
				});\n
			});\n
			if (!confirmed) return;\n
			if ((d.created ?? 0) < 1) {\n
				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');\n
				return;\n
			}\n
			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);\n
			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');\n
			await loadRoster(periodId);\n
		} catch (err) {\n
			const code = String(err?.code || err?.payload?.error?.code || '');\n
			if (code === 'ROTATION_DISABLED') {\n
				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');\n
			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {\n
				Msg.announce(\n
					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),\n
					'warning',\n
				);\n
			} else if (code === 'RATE_LIMITED') {\n
				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');\n
			} else {\n
				Msg.handleApiError(err);\n
			}\n
		} finally {\n
			if (btn) {\n
				btn.disabled = false;\n
				btn.removeAttribute('aria-busy');\n
			}\n
		}\n
	}\n
\n
	function clearAssignmentFormFields() {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) return;\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		const periodVal = periodHidden ? String(periodHidden.value || '') : '';\n
		form.reset();\n
		if (periodHidden && periodVal) {\n
			periodHidden.value = periodVal;\n
		}\n
		applyDefaultBreakToForm();\n
		D?.applyLocaleToTemporalInputs(form);\n
		refreshAssignmentFormEligibility();\n
		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');\n
		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {\n
			showAssignmentFormSuccess(clearedMsg, 'info');\n
		}\n
		Msg.announce(clearedMsg, 'info');\n
	}\n
\n
	function readForm(form) {\n
		const data = new FormData(form);\n
		const rawStart = String(data.get('startTime') || '');\n
		const rawEnd = String(data.get('endTime') || '');\n
		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();\n
		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();\n
		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);\n
		const out = {\n
			periodId: Number(data.get('periodId')),\n
			employeeId: Number(data.get('employeeId')),\n
			locationId: Number(data.get('locationId')),\n
			dutyDate: String(data.get('dutyDate') || ''),\n
			startTime,\n
			endTime,\n
			breakMinutes: Number(data.get('breakMinutes') || 0),\n
			note: String(data.get('note') || '').trim(),\n
			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,\n
		};\n
		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {\n
			out.expectedVersion = Number(state.editingAssignmentVersion);\n
		}\n
		return out;\n
	}\n
\n
	/**\n
	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.\n
	 */\n
	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;\n
		if (start === null || end === null) return null;\n
		let endAdj = end;\n
		if (endAdj <= start) {\n
			endAdj += 24 * 60;\n
		}\n
		return (endAdj - start) - breakMinutes;\n
	}\n
\n
	function validate(payload) {\n
		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {\n
			return t('dutycheck', 'Select a period before saving.');\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		if (!period) {\n
			return t('dutycheck', 'That planning period was not found.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {\n
			return t('dutycheck', 'Select an employee.');\n
		}\n
		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {\n
			return t('dutycheck', 'Select a location.');\n
		}\n
		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {\n
			return t('dutycheck', 'Date, start time, and end time are required.');\n
		}\n
		if (period.startDate && period.endDate\n
			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {\n
			return t('dutycheck', 'Date must fall within the selected period.');\n
		}\n
		if (payload.startTime === payload.endTime) {\n
			return t(\n
				'dutycheck',\n
				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
			);\n
		}\n
		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {\n
			return t('dutycheck', 'Break minutes must be between 0 and 720.');\n
		}\n
		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
		if (effective === null) {\n
			return t('dutycheck', 'Please check the dates and times.');\n
		}\n
		if (effective <= 0) {\n
			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
		}\n
		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			return t('dutycheck', 'This employee has approved leave on the selected date.');\n
		}\n
		if (findOverlapAssignment(payload)) {\n
			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');\n
		}\n
		return null;\n
	}\n
\n
	function errorMessageFor(error) {\n
		const code = errorCodeFrom(error);\n
		switch (code) {\n
			case 'PERIOD_NOT_OPEN':\n
				return t('dutycheck', 'The selected period is not open for planning.');\n
			case 'PERIOD_NOT_FOUND':\n
				return t('dutycheck', 'That planning period was not found.');\n
			case 'DATE_OUTSIDE_PERIOD':\n
				return t('dutycheck', 'Date must fall within the selected period.');\n
			case 'EMPLOYEE_NOT_FOUND':\n
				return t('dutycheck', 'Employee not found or no longer available.');\n
			case 'LOCATION_NOT_FOUND':\n
				return t('dutycheck', 'Location not found or no longer available.');\n
			case 'EQUAL_DUTY_TIMES':\n
				return t(\n
					'dutycheck',\n
					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
				);\n
			case 'INVALID_SHIFT_LENGTH':\n
				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
			case 'INVALID_BREAK_MINUTES':\n
				return t('dutycheck', 'Break minutes are out of the allowed range.');\n
			case 'NOTE_TOO_LONG':\n
				return t('dutycheck', 'Note is too long (maximum 512 characters).');\n
			case 'ASSIGNMENT_OVERLAP':\n
				return t('dutycheck', 'This employee already has an overlapping assignment.');\n
			case 'ASSIGNMENT_DUPLICATE_SLOT':\n
				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');\n
			case 'STALE_VERSION':\n
				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');\n
			case 'CONFLICT_ACK_STALE':\n
				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');\n
			case 'COMPANY_MEMBERSHIP_REQUIRED':\n
				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');\n
			case 'EXPECTED_VERSION_REQUIRED':\n
				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');\n
			case 'ABSENCE_CONFLICT':\n
				return t('dutycheck', 'This employee has an approved absence on that date.');\n
			case 'BLACKOUT_CONFLICT':\n
				return t('dutycheck', 'This employee marked that they cannot work during this time.');\n
			case 'CONFLICT_ACK_REQUIRED':\n
				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');\n
			case 'REASON_TOO_SHORT':\n
				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');\n
			case 'INTERNAL_ERROR':\n
				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');\n
			case 'QUALIFICATION_MISSING':\n
				return t('dutycheck', 'This employee is missing a required qualification for that location.');\n
			case 'ASSIGNMENT_NOT_FOUND':\n
				return t('dutycheck', 'That assignment no longer exists. Reload the page.');\n
			case 'SCHEMA_NOT_READY':\n
				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');\n
			case 'INVALID_DATE':\n
			case 'INVALID_TIME':\n
				return t('dutycheck', 'Please check the dates and times.');\n
			case 'PERIOD_ID_REQUIRED':\n
				return t('dutycheck', 'Select a period before saving.');\n
			case 'EMPLOYEE_ID_REQUIRED':\n
				return t('dutycheck', 'Select an employee.');\n
			case 'LOCATION_ID_REQUIRED':\n
				return t('dutycheck', 'Select a location.');\n
			default:\n
				return null;\n
		}\n
	}\n
\n
\n
	async function loadPendingSwaps() {\n
		const list = document.getElementById('dc-swap-list');\n
		const empty = document.getElementById('dc-swap-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/swaps');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				li.appendChild(create('p', {\n
					text: row.toEmployeeId\n
						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId))\n
							.replace('{to}', String(row.toEmployeeId))\n
						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId)),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });\n
						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');\n
						await loadPendingSwaps();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });\n
						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');\n
						await loadPendingSwaps();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load swap requests.');\n
			}\n
		}\n
	}\n
\n
	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {\n
		try {\n
			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {\n
				acknowledgements,\n
			});\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Approve with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return approveOpenShiftClaim(openShiftId, acks, false);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	async function loadPendingOpenClaims() {\n
		const list = document.getElementById('dc-open-claim-list');\n
		const empty = document.getElementById('dc-open-claim-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;\n
				li.appendChild(create('p', {\n
					text: t('dutycheck', '{date} · employee #{emp}')\n
						.replace('{date}', String(when))\n
						.replace('{emp}', String(row.claimedByEmployeeId || '—')),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await approveOpenShiftClaim(row.id);\n
						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');\n
						await loadPendingOpenClaims();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});\n
						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');\n
						await loadPendingOpenClaims();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load pending claims.');\n
			}\n
		}\n
	}\n
\n
	function wireMarketplace() {\n
		const toggle = document.getElementById('dc-open-shift-create');\n
		const form = document.getElementById('dc-open-shift-form');\n
		toggle?.addEventListener('click', () => {\n
			if (!form) return;\n
			form.hidden = !form.hidden;\n
			const loc = document.getElementById('dc-os-location');\n
			if (loc && state.locations?.length) {\n
				loc.replaceChildren();\n
				for (const l of state.locations) {\n
					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));\n
				}\n
			}\n
			const period = selectedPeriodFromState();\n
			const date = document.getElementById('dc-os-date');\n
			if (date && period?.startDate) date.value = String(period.startDate);\n
		});\n
		document.getElementById('dc-os-save')?.addEventListener('click', async () => {\n
			const period = selectedPeriodFromState();\n
			if (!period?.id) {\n
				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');\n
				return;\n
			}\n
			try {\n
				await Api.post('/apps/dutycheck/api/open-shifts', {\n
					periodId: Number(period.id),\n
					locationId: Number(document.getElementById('dc-os-location')?.value || 0),\n
					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),\n
					startTime: String(document.getElementById('dc-os-start')?.value || ''),\n
					endTime: String(document.getElementById('dc-os-end')?.value || ''),\n
					breakMinutes: 0,\n
				});\n
				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');\n
				if (form) form.hidden = true;\n
			} catch (err) {\n
				Msg.handleApiError(err);\n
			}\n
		});\n
	}\n
\n
	document.addEventListener('DOMContentLoaded', async () => {\n
		D?.applyLocaleToTemporalInputs(document);\n
		wireBreakPrefillHint();\n
		window.addEventListener('dc-planning-defaults-changed', (event) => {\n
			const minutes = Number(event?.detail?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
		});\n
		wireAdminExportGuards();\n
		wireMarketplace();\n
		setRosterView('grid');\n
		wireRosterVirtualization();\n
		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));\n
		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));\n
		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {\n
			gridState.selected.clear();\n
			syncVisibleGridChrome();\n
			updateBulkBar();\n
		});\n
		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {\n
			void applyBulkFromTemplate();\n
		});\n
		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {\n
			void runSuggestFill();\n
		});\n
		await Promise.all([\n
			loadRoster(selectedPeriodIdFromUrl()),\n
			loadPendingSwaps(),\n
			loadPendingOpenClaims(),\n
		]);\n
\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		switcher?.addEventListener('change', async () => {\n
			clearAssignmentsSectionSuccess();\n
			const periodId = Number(switcher.value);\n
			await Promise.all([\n
				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),\n
				loadPendingSwaps(),\n
				loadPendingOpenClaims(),\n
			]);\n
		});\n
\n
		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {\n
			const btn = event.target.closest('.dc-roster-add-assignment-trigger');\n
			if (!btn) {\n
				return;\n
			}\n
			openAssignmentCreateModal(btn);\n
		});\n
\n
		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {\n
			clearAssignmentFormFields();\n
		});\n
\n
		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {\n
			applySelectedTemplate();\n
		});\n
		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {\n
			runCopyPeriod(true);\n
		});\n
		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {\n
			if (!state.copyPreviewReady) {\n
				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');\n
				return;\n
			}\n
			await runCopyPeriod(false);\n
		});\n
		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {\n
			state.copyPreviewReady = false;\n
			const applyBtn = document.getElementById('dc-roster-copy-apply');\n
			if (applyBtn) applyBtn.disabled = true;\n
		});\n
\n
		const form = document.getElementById('dc-assignment-form');\n
		const scheduleEligibilityRefresh = () => {\n
			clearAssignmentFormSuccess();\n
			clearAssignmentsSectionSuccess();\n
			refreshAssignmentFormEligibility();\n
		};\n
		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);\n
		form?.addEventListener('submit', (event) => {\n
			event.preventDefault();\n
			const dialog = form.closest('.dc-modal__dialog--roster-assignment');\n
			if (!dialog) {\n
				return;\n
			}\n
			const primary = dialog.querySelector('.dc-modal__actions .button.primary');\n
			if (primary && !primary.disabled) {\n
				primary.click();\n
			}\n
		});\n
	});\n
})();\n
' contains "window.addEventListener('beforeprint', () => setRosterPaintAll(true));".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:63

FAILURES!
Tests: 6, Assertions: 60, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.999026
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.277364
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.330313
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.241131
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.201204
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.197699
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.1734
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.263616
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.334246
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 5.842293
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 2.82719
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.215271
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.1128
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.095943
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 3.561804
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.178795
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.121131
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.396155
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.239671
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.11116
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.098919
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.569631
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.206163
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 2.570639
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /addEventListener\('beforeprint',\s*\(\) => setRosterPaintAll\(true\)\)/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105636 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => {});
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:253:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.213535
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.32359
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.487662
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 5.200559
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 95.930497
killed skip_print_expand
== mutant skip_print_matchmedia (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

..F...                                                              6 / 6 (100%)

Time: 00:00.013, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testRosterJsWindowsRowsAndKeepsKeyboardReach
Failed asserting that '(function () {\n
	'use strict';\n
\n
	const Api = window.DutyCheckApi;\n
	const Msg = window.DutyCheckMessaging;\n
	const C = window.DutyCheckComponents || window.DutyCheckDom || {};\n
	const D = window.DutyCheckDates;\n
	const ConflictLabels = window.DutyCheckConflictLabels;\n
	const create = C.createElement;\n
	if (typeof create !== 'function') {\n
		throw new Error('DutyCheck components failed to load');\n
	}\n
\n
	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n
\n
	const state = {\n
		periods: [],\n
		employees: [],\n
		locations: [],\n
		assignments: [],\n
		absenceBlocks: [],\n
		canCreateAssignments: false,\n
		lastRosterData: null,\n
		defaultBreakMinutes: 0,\n
		planningDefaultsFresh: false,\n
		templates: [],\n
		templatesLoaded: false,\n
		editingAssignmentId: null,\n
		editingAssignmentVersion: null,\n
		copyPreviewReady: false,\n
		preferencesByEmployee: {},\n
		blackoutsByEmployeeDate: {},\n
	};\n
	let assignmentModalInstance = null;\n
	let suggestedBreakMinutes = 0;\n
\n
	function clampBreakMinutes(value) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n)) {\n
			return 0;\n
		}\n
		return Math.max(0, Math.min(720, Math.round(n)));\n
	}\n
\n
	function readStoredBreakMinutes() {\n
		try {\n
			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n
			if (raw === null || raw === '') {\n
				return null;\n
			}\n
			return clampBreakMinutes(raw);\n
		} catch (_) {\n
			return null;\n
		}\n
	}\n
\n
	function rememberBreakMinutes(minutes) {\n
		try {\n
			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n
		} catch (_) {\n
			/* ignore quota / privacy mode */\n
		}\n
	}\n
\n
	function resolveBreakPrefill() {\n
		const org = clampBreakMinutes(state.defaultBreakMinutes);\n
		// Organisation default from Settings always wins when it is greater than zero.\n
		if (org > 0) {\n
			return { minutes: org, source: 'settings' };\n
		}\n
		const stored = readStoredBreakMinutes();\n
		if (stored !== null) {\n
			return { minutes: stored, source: 'last' };\n
		}\n
		return { minutes: 0, source: 'settings' };\n
	}\n
\n
	async function refreshPlanningDefaultFromServer() {\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n
			const minutes = Number(response?.planning?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
			state.planningDefaultsFresh = true;\n
		} catch (_) {\n
			/* Fall back to defaultBreakMinutes from the last roster API load. */\n
		}\n
	}\n
\n
	function breakPrefillMessage(prefill) {\n
		const n = String(prefill.minutes);\n
		if (prefill.source === 'last') {\n
			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n
		}\n
		if (prefill.minutes === 0) {\n
			return t('dutycheck', 'Pre-filled with 0 min — no break.');\n
		}\n
		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n
	}\n
\n
	function updateBreakPrefillHint(userEdited) {\n
		const el = document.getElementById('dc-assignment-break-prefill');\n
		if (!el) {\n
			return;\n
		}\n
		if (userEdited) {\n
			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n
			return;\n
		}\n
		el.textContent = breakPrefillMessage(resolveBreakPrefill());\n
	}\n
\n
	function applyDefaultBreakToForm() {\n
		const prefill = resolveBreakPrefill();\n
		suggestedBreakMinutes = prefill.minutes;\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(prefill.minutes);\n
		}\n
		updateBreakPrefillHint(false);\n
	}\n
\n
	function wireBreakPrefillHint() {\n
		const br = document.getElementById('dc-assignment-break');\n
		if (!br || br.dataset.dcBreakPrefillWired) {\n
			return;\n
		}\n
		br.dataset.dcBreakPrefillWired = '1';\n
		br.addEventListener('input', () => {\n
			const current = clampBreakMinutes(br.value);\n
			updateBreakPrefillHint(current !== suggestedBreakMinutes);\n
		});\n
		br.addEventListener('change', () => {\n
			const current = clampBreakMinutes(br.value);\n
			if (current === suggestedBreakMinutes) {\n
				updateBreakPrefillHint(false);\n
			}\n
		});\n
	}\n
\n
	function dateToDayIndex(isoDate) {\n
		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n
		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n
			return null;\n
		}\n
		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n
	}\n
\n
	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n
		const dayIndex = dateToDayIndex(dutyDate);\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime);\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime);\n
		if (dayIndex === null || start === null || end === null) {\n
			return null;\n
		}\n
		const absoluteStart = (dayIndex * 1440) + start;\n
		let absoluteEnd = (dayIndex * 1440) + end;\n
		if (absoluteEnd <= absoluteStart) {\n
			absoluteEnd += 1440;\n
		}\n
		return [absoluteStart, absoluteEnd];\n
	}\n
\n
	function absoluteRangesOverlap(a, b) {\n
		return a[0] < b[1] && b[0] < a[1];\n
	}\n
\n
	function isDateInAbsenceBlock(employeeId, dutyDate) {\n
		if (!dutyDate) {\n
			return false;\n
		}\n
		for (const block of state.absenceBlocks) {\n
			if (Number(block?.employeeId) !== Number(employeeId)) {\n
				continue;\n
			}\n
			const start = String(block?.startDate || '');\n
			const end = String(block?.endDate || '');\n
			if (dutyDate >= start && dutyDate <= end) {\n
				return true;\n
			}\n
		}\n
		return false;\n
	}\n
\n
	function employeesAvailableOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return [];\n
		}\n
		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n
	}\n
\n
	function countEmployeesBlockedOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return 0;\n
		}\n
		return state.employees.length - employeesAvailableOnDate(dutyDate).length;\n
	}\n
\n
	function appendHintLink(parent, beforeText, linkText, afterText, href) {\n
		parent.appendChild(document.createTextNode(beforeText));\n
		if (href) {\n
			const link = create('a', {\n
				class: 'dc-hint-link',\n
				href,\n
				text: linkText,\n
			});\n
			parent.appendChild(link);\n
			parent.appendChild(document.createTextNode(afterText));\n
		} else {\n
			parent.appendChild(document.createTextNode(linkText + afterText));\n
		}\n
	}\n
\n
	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n
		const hint = document.getElementById('dc-assignment-employee-hint');\n
		if (!hint) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		hint.replaceChildren();\n
		if (!dutyDate) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n
			));\n
			return;\n
		}\n
		if (!available.length && blocked > 0) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n
					.replace('{n}', String(blocked)),\n
				t('dutycheck', 'review absences'),\n
				'.',\n
				String(urls.absences || ''),\n
			);\n
			return;\n
		}\n
		if (!available.length) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'No employees are available. Add an active employee on the '),\n
				t('dutycheck', 'Employees'),\n
				t('dutycheck', ' page first.'),\n
				String(urls.employees || ''),\n
			);\n
			return;\n
		}\n
		if (blocked > 0) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n
					.replace('{available}', String(available.length))\n
					.replace('{hidden}', String(blocked)),\n
			));\n
			return;\n
		}\n
		hint.appendChild(document.createTextNode(\n
			t('dutycheck', 'All {n} active employees can be scheduled on this date.')\n
				.replace('{n}', String(available.length)),\n
		));\n
	}\n
\n
	function refreshEmployeeSelectForDate() {\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const dutyDate = dateInput ? String(dateInput.value || '') : '';\n
		const available = employeesAvailableOnDate(dutyDate);\n
		const blocked = countEmployeesBlockedOnDate(dutyDate);\n
		setEmployeeAvailabilityHint(dutyDate, available, blocked);\n
		fillSelect('dc-assignment-employee', available, {\n
			emptyText: dutyDate\n
				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n
				: t('dutycheck', 'Pick a date first'),\n
		});\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		if (employeeSelect) {\n
			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n
		}\n
	}\n
\n
	function findOverlapAssignment(payload) {\n
		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n
		if (!candidate) {\n
			return null;\n
		}\n
		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		for (const assignment of state.assignments) {\n
			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n
				continue;\n
			}\n
			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n
				continue;\n
			}\n
			const existing = assignmentAbsoluteRange(\n
				String(assignment?.dutyDate || ''),\n
				String(assignment?.startTime || ''),\n
				String(assignment?.endTime || ''),\n
			);\n
			if (existing && absoluteRangesOverlap(candidate, existing)) {\n
				return assignment;\n
			}\n
		}\n
		return null;\n
	}\n
\n
	function setFormFeedbackA11y(el, isError) {\n
		if (!el) {\n
			return;\n
		}\n
		if (isError) {\n
			el.setAttribute('role', 'alert');\n
			el.setAttribute('aria-live', 'assertive');\n
		} else {\n
			el.setAttribute('role', 'status');\n
			el.setAttribute('aria-live', 'polite');\n
		}\n
	}\n
\n
	function focusFirstInvalidAssignmentField(payload) {\n
		if (!payload) {\n
			return;\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		const order = [\n
			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n
			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n
			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n
			{\n
				test: () => period?.startDate && period?.endDate && payload.dutyDate\n
					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n
				id: 'dc-assignment-date',\n
			},\n
			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },\n
			{ test: () => !payload.locationId, id: 'dc-assignment-location' },\n
			{ test: () => !payload.startTime, id: 'dc-assignment-start' },\n
			{ test: () => !payload.endTime, id: 'dc-assignment-end' },\n
			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },\n
			{\n
				test: () => {\n
					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
					return effective === null || effective <= 0;\n
				},\n
				id: 'dc-assignment-end',\n
			},\n
		];\n
		for (const item of order) {\n
			if (item.test()) {\n
				document.getElementById(item.id)?.focus();\n
				return;\n
			}\n
		}\n
	}\n
\n
	function selectedPeriodForPayload(payload) {\n
		const periodId = Number(payload?.periodId);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function clearAssignmentFormSuccess() {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		if (!el || el.dataset.dcSuccess !== '1') {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.dataset.dcSuccess = '';\n
		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
		setFormFeedbackA11y(el, false);\n
	}\n
\n
	function showAssignmentFormSuccess(message, kind) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		const tone = kind === 'info' ? 'info' : 'success';\n
		el.hidden = false;\n
		el.textContent = String(message);\n
		el.dataset.dcSuccess = '1';\n
		el.classList.remove('dc-roster-form__feedback--error');\n
		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');\n
		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');\n
		setFormFeedbackA11y(el, false);\n
		if (submit) {\n
			submit.disabled = submit.dataset.dcLockedBySetup === '1';\n
		}\n
		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });\n
	}\n
\n
	function labelsForAssignmentPayload(payload) {\n
		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));\n
		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		const locationSelect = document.getElementById('dc-assignment-location');\n
		const employeeName = employee\n
			? String(employee.name || employee.displayName || '')\n
			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		const locationName = location\n
			? String(location.name || '')\n
			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		return { employeeName, locationName };\n
	}\n
\n
	function buildAssignmentSavedMessage(payload) {\n
		const { employeeName, locationName } = labelsForAssignmentPayload(payload);\n
		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;\n
		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)\n
			|| `${payload.startTime} – ${payload.endTime}`;\n
		return t(\n
			'dutycheck',\n
			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',\n
		)\n
			.replace('{employee}', employeeName || '—')\n
			.replace('{date}', String(dateLabel || ''))\n
			.replace('{times}', String(times))\n
			.replace('{location}', locationName || '—');\n
	}\n
\n
	function updateAssignmentFormFeedback(payload) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		if (el.dataset.dcSuccess === '1') {\n
			return;\n
		}\n
		if (typeof payload === 'string') {\n
			el.hidden = false;\n
			el.textContent = payload;\n
			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			el.classList.add('dc-roster-form__feedback--error');\n
			setFormFeedbackA11y(el, true);\n
			if (submit) {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		if (!payload) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			return;\n
		}\n
		let message = '';\n
		let isError = false;\n
		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');\n
			isError = true;\n
		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {\n
			const overlap = findOverlapAssignment(payload);\n
			if (overlap) {\n
				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;\n
				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)\n
					|| `${overlap.startTime} – ${overlap.endTime}`;\n
				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')\n
					.replace('{date}', String(when))\n
					.replace('{times}', String(times));\n
				isError = true;\n
			}\n
		}\n
		if (!message) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			if (submit && submit.dataset.dcLockedBySetup !== '1') {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		el.hidden = false;\n
		el.textContent = message;\n
		el.classList.toggle('dc-roster-form__feedback--error', isError);\n
		setFormFeedbackA11y(el, isError);\n
		if (submit && isError) {\n
			submit.disabled = true;\n
		}\n
	}\n
\n
	function refreshAssignmentFormEligibility() {\n
		refreshEmployeeSelectForDate();\n
		updateLocationTimezoneHint();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		updateAssignmentFormFeedback(readForm(form));\n
	}\n
\n
	function updateLocationTimezoneHint() {\n
		const hint = document.getElementById('dc-assignment-location-tz-hint');\n
		const select = document.getElementById('dc-assignment-location');\n
		if (!hint || !select) {\n
			return;\n
		}\n
		const locationId = Number(select.value);\n
		const location = state.locations.find((row) => Number(row.id) === locationId);\n
		const tz = location && String(location.timezone || '').trim();\n
		if (!tz) {\n
			hint.hidden = true;\n
			hint.replaceChildren();\n
			return;\n
		}\n
		const accountTz = D?.currentTimezone?.() || '';\n
		hint.textContent = t(\n
			'dutycheck',\n
			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',\n
		)\n
			.replace('{locationTimezone}', tz)\n
			.replace('{accountTimezone}', accountTz || 'UTC');\n
		hint.hidden = false;\n
	}\n
\n
	function fillSelect(id, items, options) {\n
		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});\n
		const select = document.getElementById(id);\n
		if (!select) return;\n
		const previous = String(select.value || '');\n
		select.replaceChildren();\n
		if (!items.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = opts.emptyText;\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const item of items) {\n
			const option = document.createElement('option');\n
			option.value = String(item.id);\n
			option.textContent = String(item[opts.labelKey] || item.name || item.id);\n
			select.appendChild(option);\n
		}\n
		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {\n
			select.value = previous;\n
		}\n
	}\n
\n
	function fillPeriodSwitcher(periods, selected) {\n
		const select = document.getElementById('dc-roster-period-switcher');\n
		if (!select) return;\n
		const statusLabelMap = {\n
			open: t('dutycheck', 'Open'),\n
			published: t('dutycheck', 'Published'),\n
			closed: t('dutycheck', 'Closed'),\n
		};\n
		select.replaceChildren();\n
		if (!periods.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = t('dutycheck', 'No periods yet');\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const period of periods) {\n
			const option = document.createElement('option');\n
			option.value = String(period.id);\n
			const start = D?.formatDisplayDate(period.startDate) || period.startDate;\n
			const end = D?.formatDisplayDate(period.endDate) || period.endDate;\n
			const statusKey = String(period.status || '').toLowerCase();\n
			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();\n
			option.textContent = `${start} – ${end} · ${status}`;\n
			select.appendChild(option);\n
		}\n
		if (selected) {\n
			select.value = String(selected);\n
		}\n
	}\n
\n
	function severityBadge(severity) {\n
		const sev = String(severity || 'info');\n
		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;\n
		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });\n
		badge.setAttribute('aria-label', label);\n
		return badge;\n
	}\n
\n
	function renderConflicts(conflicts) {\n
		const list = document.getElementById('dc-conflict-list');\n
		const summary = document.getElementById('dc-conflict-summary');\n
		if (!list) return;\n
		list.setAttribute('role', 'list');\n
		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));\n
		list.replaceChildren();\n
		const hard = conflicts.filter((c) => c?.severity === 'hard').length;\n
		const soft = conflicts.filter((c) => c?.severity === 'soft').length;\n
		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;\n
		if (summary) {\n
			summary.textContent = !conflicts.length\n
				? t('dutycheck', 'No planning issues found.')\n
				: (ConflictLabels\n
					? ConflictLabels.countsSummary(hard, soft, softUnack)\n
					: String(hard));\n
		}\n
		if (!conflicts.length) {\n
			return;\n
		}\n
		for (const conflict of conflicts) {\n
			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });\n
			li.appendChild(severityBadge(conflict?.severity));\n
			const conflictMessage = String(conflict?.message || '');\n
			const titleText = conflictMessage\n
				? translateConflictMessage(conflictMessage)\n
				: t('dutycheck', 'Unknown conflict');\n
			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;\n
			const body = create('div', { class: 'dc-conflict__body' });\n
			const title = create('span', { class: 'dc-conflict__title', text: titleText });\n
			title.id = titleId;\n
			body.appendChild(title);\n
			li.setAttribute('aria-labelledby', titleId);\n
			if (conflict?.acknowledged && conflict?.ackReason) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack',\n
					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),\n
				}));\n
			} else if (conflict?.ackInvalidated) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack-invalid',\n
					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),\n
				}));\n
			}\n
			li.appendChild(body);\n
\n
			const actions = create('div', { class: 'dc-conflict__actions' });\n
			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {\n
				const ackBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Confirm'),\n
				});\n
				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));\n
				actions.appendChild(ackBtn);\n
			}\n
			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();\n
			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {\n
				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()\n
					|| t('dutycheck', 'Open ArbeitszeitCheck');\n
				const recoveryLink = create('a', {\n
					class: 'button primary',\n
					href: recoveryUrl,\n
					text: recoveryLabel,\n
					attrs: {\n
						target: '_blank',\n
						rel: 'noopener noreferrer',\n
						'aria-label': recoveryLabel,\n
					},\n
				});\n
				actions.appendChild(recoveryLink);\n
			}\n
			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];\n
			if (conflict?.type === 'absence_collision' && assignmentIds.length) {\n
				const focusBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Show assignment'),\n
				});\n
				focusBtn.addEventListener('click', () => {\n
					revealAssignmentRow(Number(assignmentIds[0]));\n
				});\n
				actions.appendChild(focusBtn);\n
			}\n
			li.appendChild(actions);\n
			list.appendChild(li);\n
		}\n
	}\n
\n
	async function acknowledgeConflict(conflictId) {\n
		const reason = await C.promptReason({\n
			title: t('dutycheck', 'Confirm this exception'),\n
			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),\n
			confirmLabel: t('dutycheck', 'Confirm'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			minLength: 10,\n
		});\n
		if (reason === null) return;\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });\n
			render(response?.data || {});\n
			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');\n
			Msg.announce(ackMsg, 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	const VIRTUAL_FALLBACK = {\n
		DEFAULT_ROW_HEIGHT: 44,\n
		DEFAULT_OVERSCAN: 6,\n
		UNSIZED_WINDOW_ROWS: 24,\n
		visibleRange(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			const rowHeight = 44;\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: total * rowHeight,\n
				rowHeight,\n
			};\n
		},\n
		scrollTopToRevealIndex() {\n
			return 0;\n
		},\n
		pageStride() {\n
			return 1;\n
		},\n
		windowCaption(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			if (total <= 0) {\n
				return { mode: 'empty', from: 0, to: 0, total: 0 };\n
			}\n
			return { mode: 'all', from: 1, to: total, total };\n
		},\n
	};\n
\n
	function virtualApi() {\n
		const api = window.DutyCheckVirtualWindow;\n
		if (api\n
			&& typeof api.visibleRange === 'function'\n
			&& typeof api.scrollTopToRevealIndex === 'function'\n
			&& typeof api.windowCaption === 'function') {\n
			return api;\n
		}\n
		return VIRTUAL_FALLBACK;\n
	}\n
\n
	function cellSelectionKey(employeeId, dutyDate) {\n
		return `${Number(employeeId)}|${String(dutyDate)}`;\n
	}\n
\n
	function prefersReducedMotion() {\n
		try {\n
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n
		} catch (_) {\n
			return false;\n
		}\n
	}\n
\n
	function measureHeight(el) {\n
		if (!(el instanceof HTMLElement)) {\n
			return 0;\n
		}\n
		const h = el.getBoundingClientRect().height;\n
		return Number.isFinite(h) && h > 0 ? h : 0;\n
	}\n
\n
	function assignmentIndex(assignments) {\n
		const byKey = new Map();\n
		for (const a of assignments || []) {\n
			const key = cellSelectionKey(a.employeeId, a.dutyDate);\n
			if (!byKey.has(key)) {\n
				byKey.set(key, []);\n
			}\n
			byKey.get(key).push(a);\n
		}\n
		return byKey;\n
	}\n
\n
	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {\n
		const emptyPanel = document.getElementById('dc-roster-assignments-empty');\n
		if (emptyPanel) {\n
			emptyPanel.hidden = !showEmptyPanel;\n
		}\n
	}\n
\n
	const gridState = {\n
		view: 'grid',\n
		selected: new Set(), // `${employeeId}|${dutyDate}`\n
		focusRow: 0,\n
		focusCol: 0,\n
		paintAll: false,\n
		rowHeight: 44,\n
		headerHeight: 44,\n
		windowStart: -1,\n
		windowEnd: -1,\n
		paintedEmployeeCount: -1,\n
		paintedDateCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	const listState = {\n
		windowStart: -1,\n
		windowEnd: -1,\n
		rowHeight: 48,\n
		paintedCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	function renderAssignments(assignments) {\n
		paintListWindow({ assignments: assignments || [], force: true });\n
		renderRosterGrid(assignments || []);\n
		updateAddAssignmentControl();\n
	}\n
\n
	function buildAssignmentRow(a, periodOpen) {\n
		const overnightHintText = t('dutycheck', 'Continues into the next day.');\n
		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));\n
		const tr = create('tr');\n
		if (a?.id != null) {\n
			tr.dataset.assignmentId = String(a.id);\n
			tr.setAttribute('tabindex', '-1');\n
		}\n
		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;\n
		const tdDate = create('td', { text: date });\n
		tdDate.dataset.cell = t('dutycheck', 'Date');\n
		tr.appendChild(tdDate);\n
\n
		const tdStart = create('td');\n
		tdStart.dataset.cell = t('dutycheck', 'Start');\n
		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));\n
		tr.appendChild(tdStart);\n
\n
		const tdEnd = create('td');\n
		tdEnd.dataset.cell = t('dutycheck', 'End');\n
		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));\n
		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {\n
			tdEnd.title = overnightHintText;\n
			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));\n
		}\n
		tr.appendChild(tdEnd);\n
\n
		const rest = [\n
			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },\n
			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },\n
			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },\n
			{ label: t('dutycheck', 'Note'), value: a.note || '' },\n
		];\n
		for (const cell of rest) {\n
			const td = create('td', { text: String(cell.value ?? '') });\n
			td.dataset.cell = cell.label;\n
			tr.appendChild(td);\n
		}\n
		const tdActions = create('td');\n
		tdActions.dataset.cell = t('dutycheck', 'Actions');\n
		tdActions.className = 'dc-table__actions';\n
		if (periodOpen && a?.id) {\n
			const editBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Edit'),\n
			});\n
			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));\n
			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));\n
			const cancelBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Cancel shift'),\n
			});\n
			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));\n
			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));\n
			tdActions.appendChild(editBtn);\n
			tdActions.appendChild(cancelBtn);\n
		} else {\n
			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));\n
		}\n
		tr.appendChild(tdActions);\n
		return tr;\n
	}\n
\n
	function createListSpacer(px) {\n
		const tr = create('tr', { class: 'dc-virtual-spacer' });\n
		tr.setAttribute('aria-hidden', 'true');\n
		tr.style.height = `${Math.max(0, px)}px`;\n
		const td = create('td');\n
		td.colSpan = 8;\n
		td.style.height = `${Math.max(0, px)}px`;\n
		td.style.padding = '0';\n
		td.style.border = '0';\n
		td.style.lineHeight = '0';\n
		tr.appendChild(td);\n
		return tr;\n
	}\n
\n
	function updateListWindowStatus(range, total) {\n
		const el = document.getElementById('dc-roster-list-status');\n
		if (!el) {\n
			return;\n
		}\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		if (cap.mode === 'all') {\n
			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));\n
			return;\n
		}\n
		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')\n
			.replace('{from}', String(cap.from))\n
			.replace('{to}', String(cap.to))\n
			.replace('{total}', String(cap.total));\n
	}\n
\n
	function paintListWindow(options) {\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		if (!tbody) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		if (!assignments.length) {\n
			tbody.replaceChildren();\n
			setAssignmentsEmptyPanelVisible(true);\n
			listState.windowStart = 0;\n
			listState.windowEnd = 0;\n
			listState.paintedCount = 0;\n
			updateListWindowStatus({ start: 0, end: 0 }, 0);\n
			return;\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const scroller = document.getElementById('dc-assignments-table-wrap');\n
		const VW = virtualApi();\n
		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: assignments.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& listState.windowStart === range.start\n
			&& listState.windowEnd === range.end\n
			&& listState.paintedCount === assignments.length) {\n
			updateListWindowStatus(range, assignments.length);\n
			return;\n
		}\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const periodOpen = canAddAssignment();\n
		const frag = document.createDocumentFragment();\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createListSpacer(range.padBefore));\n
		}\n
		const windowRows = assignments.slice(range.start, range.end);\n
		for (const a of windowRows) {\n
			frag.appendChild(buildAssignmentRow(a, periodOpen));\n
		}\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createListSpacer(range.padAfter));\n
		}\n
		tbody.replaceChildren(frag);\n
		listState.windowStart = range.start;\n
		listState.windowEnd = range.end;\n
		listState.paintedCount = assignments.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
		}\n
		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');\n
		const measured = measureHeight(sample);\n
		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {\n
			listState.rowHeight = measured;\n
			listState.remeasuring = true;\n
			paintListWindow({ assignments, force: true });\n
			listState.remeasuring = false;\n
			return;\n
		}\n
		if (measured > 0) {\n
			listState.rowHeight = measured;\n
		}\n
		updateListWindowStatus(range, assignments.length);\n
	}\n
\n
	function selectedPeriodId() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const fromSwitcher = Number(switcher?.value || 0);\n
		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {\n
			return fromSwitcher;\n
		}\n
		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;\n
	}\n
\n
	function periodDateList() {\n
		const periodId = selectedPeriodId();\n
		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));\n
		if (!period?.startDate || !period?.endDate) {\n
			return [];\n
		}\n
		const out = [];\n
		const cursor = new Date(`${period.startDate}T00:00:00Z`);\n
		const end = new Date(`${period.endDate}T00:00:00Z`);\n
		while (cursor <= end) {\n
			out.push(cursor.toISOString().slice(0, 10));\n
			cursor.setUTCDate(cursor.getUTCDate() + 1);\n
			if (out.length > 62) {\n
				break;\n
			}\n
		}\n
		return out;\n
	}\n
\n
	function updateGridWindowStatus(range, total, dayCount) {\n
		const el = document.getElementById('dc-roster-grid-status');\n
		if (!el) {\n
			return;\n
		}\n
		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		let text = '';\n
		if (cap.mode === 'all') {\n
			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));\n
		} else {\n
			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')\n
				.replace('{from}', String(cap.from))\n
				.replace('{to}', String(cap.to))\n
				.replace('{total}', String(cap.total));\n
		}\n
		if (days > 10) {\n
			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');\n
		}\n
		el.textContent = text;\n
	}\n
\n
	function createGridSpacer(px) {\n
		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });\n
		el.setAttribute('aria-hidden', 'true');\n
		el.setAttribute('role', 'presentation');\n
		el.style.height = `${Math.max(0, px)}px`;\n
		return el;\n
	}\n
\n
	function setGridEmptyState(root, message) {\n
		root.removeAttribute('aria-rowcount');\n
		root.removeAttribute('aria-colcount');\n
		root.removeAttribute('tabindex');\n
		root.style.removeProperty('--dc-roster-day-count');\n
		root.style.removeProperty('--dc-roster-day-min');\n
		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');\n
		root.setAttribute('role', 'status');\n
		root.setAttribute('aria-live', 'polite');\n
		root.replaceChildren(create('p', {\n
			class: 'dc-roster-empty-state__text',\n
			text: message,\n
		}));\n
	}\n
\n
	function prepareGridForRows(root, employees, dates) {\n
		root.setAttribute('role', 'grid');\n
		root.removeAttribute('tabindex');\n
		root.removeAttribute('aria-live');\n
		root.setAttribute('aria-rowcount', String(employees.length + 1));\n
		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));\n
		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));\n
		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));\n
		root.style.setProperty('--dc-roster-day-min', dayMin);\n
		root.classList.toggle('dc-roster-grid--long', dates.length > 10);\n
		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);\n
	}\n
\n
	function buildColHead(day, colIdx, dayMeta) {\n
		const parts = dayMeta || D?.formatRosterColumnParts?.(day);\n
		const full = parts?.full || D?.formatDisplayDate?.(day) || day;\n
		let ariaLabel = parts?.ariaLabel || full;\n
		if (parts?.isToday) {\n
			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;\n
		}\n
		const weekdayShort = parts?.weekdayShort || '';\n
		const dayNum = parts?.day || '';\n
		const classNames = ['dc-roster-grid__colhead'];\n
		if (parts?.isWeekend) {\n
			classNames.push('dc-roster-grid__colhead--weekend');\n
		}\n
		if (parts?.isToday) {\n
			classNames.push('dc-roster-grid__colhead--today');\n
		}\n
		const children = [];\n
		if (weekdayShort) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));\n
		}\n
		if (dayNum) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));\n
		}\n
		const attrs = {\n
			'data-col': String(colIdx),\n
			'aria-colindex': String(colIdx + 2),\n
			title: full,\n
			'aria-label': ariaLabel,\n
		};\n
		if (parts?.iso) {\n
			attrs['data-duty-date'] = parts.iso;\n
		}\n
		if (!children.length) {\n
			return create('div', {\n
				class: classNames.join(' '),\n
				role: 'columnheader',\n
				text: full,\n
				attrs,\n
			});\n
		}\n
		return create('div', {\n
			class: classNames.join(' '),\n
			role: 'columnheader',\n
			attrs,\n
		}, children);\n
	}\n
\n
	function buildDutyDayMeta(dates) {\n
		const map = new Map();\n
		(dates || []).forEach((day) => {\n
			map.set(day, D?.formatRosterColumnParts?.(day) || null);\n
		});\n
		return map;\n
	}\n
\n
	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {\n
		const row = create('div', {\n
			class: 'dc-roster-grid__row',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': String(rowIdx + 2) },\n
		});\n
		const prefs = state.preferencesByEmployee[String(emp.id)] || [];\n
		const prefChips = prefs.slice(0, 2).map((p) => {\n
			const band = String(p.band || '');\n
			const label = band === 'early'\n
				? t('dutycheck', 'Früh')\n
				: (band === 'late' ? t('dutycheck', 'Spät') : band);\n
			return create('span', {\n
				class: 'dc-pref-chip dc-pref-chip--static',\n
				text: label,\n
				attrs: { title: label },\n
			});\n
		});\n
		const headKids = [\n
			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),\n
		];\n
		if (prefChips.length) {\n
			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));\n
		}\n
		row.appendChild(create('div', {\n
			class: 'dc-roster-grid__rowhead',\n
			role: 'rowheader',\n
			attrs: { 'aria-colindex': '1' },\n
		}, headKids));\n
		dates.forEach((day, colIdx) => {\n
			const key = cellSelectionKey(emp.id, day);\n
			const cellAssignments = byKey.get(key) || [];\n
			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);\n
			const cell = create('div', {\n
				class: 'dc-roster-grid__cell',\n
				role: 'gridcell',\n
				attrs: {\n
					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',\n
					'data-employee-id': String(emp.id),\n
					'data-duty-date': day,\n
					'data-row': String(rowIdx),\n
					'data-col': String(colIdx),\n
					'aria-colindex': String(colIdx + 2),\n
					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',\n
				},\n
			});\n
			if (dayParts?.isWeekend) {\n
				cell.classList.add('dc-roster-grid__cell--weekend');\n
			}\n
			if (dayParts?.isToday) {\n
				cell.classList.add('dc-roster-grid__cell--today');\n
			}\n
			if (cellAssignments.length) {\n
				cell.classList.add('dc-roster-grid__cell--filled');\n
				const first = cellAssignments[0];\n
				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;\n
				cell.appendChild(create('span', {\n
					class: 'dc-roster-grid__shift',\n
					text: label,\n
				}));\n
				const editLabel = canAddAssignment()\n
					? t('dutycheck', 'Edit assignment')\n
					: t('dutycheck', 'View assignment (read-only)');\n
				cell.setAttribute('aria-label', `${editLabel}: ${label}`);\n
				if (cellAssignments.length > 1) {\n
					cell.appendChild(create('span', {\n
						class: 'dc-roster-grid__more',\n
						text: `+${cellAssignments.length - 1}`,\n
					}));\n
				}\n
			} else {\n
				cell.classList.add('dc-roster-grid__cell--empty');\n
				const blackoutKey = `${emp.id}|${day}`;\n
				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];\n
				if (hasBlackout) {\n
					cell.classList.add('dc-roster-grid__cell--blackout');\n
					cell.appendChild(create('span', {\n
						class: 'dc-blackout-mark',\n
						attrs: { title: t('dutycheck', 'Kann nicht') },\n
					}, [\n
						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),\n
						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),\n
					]));\n
					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));\n
				} else {\n
					cell.setAttribute(\n
						'aria-label',\n
						canAddAssignment()\n
							? t('dutycheck', 'Empty cell — Space to select for bulk fill')\n
							: t('dutycheck', 'Empty cell'),\n
					);\n
				}\n
			}\n
			if (gridState.selected.has(key)) {\n
				cell.classList.add('is-selected');\n
			}\n
			row.appendChild(cell);\n
		});\n
		return row;\n
	}\n
\n
	function syncVisibleGridChrome() {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {\n
			const empId = cell.getAttribute('data-employee-id');\n
			const day = cell.getAttribute('data-duty-date');\n
			const key = cellSelectionKey(empId, day);\n
			const selected = gridState.selected.has(key);\n
			cell.classList.toggle('is-selected', selected);\n
			cell.setAttribute('aria-selected', selected ? 'true' : 'false');\n
			const rowIdx = Number(cell.getAttribute('data-row'));\n
			const colIdx = Number(cell.getAttribute('data-col'));\n
			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;\n
			cell.setAttribute('tabindex', isFocus ? '0' : '-1');\n
		});\n
	}\n
\n
	function focusGridCellIfVisible(root) {\n
		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
		if (node instanceof HTMLElement) {\n
			node.scrollIntoView({\n
				block: 'nearest',\n
				inline: 'nearest',\n
				behavior: 'auto',\n
			});\n
			node.focus();\n
		}\n
	}\n
\n
	function activateGridCell(cell) {\n
		if (!(cell instanceof HTMLElement)) {\n
			return;\n
		}\n
		const rowIdx = Number(cell.getAttribute('data-row'));\n
		const colIdx = Number(cell.getAttribute('data-col'));\n
		if (Number.isInteger(rowIdx) && rowIdx >= 0) {\n
			gridState.focusRow = rowIdx;\n
		}\n
		if (Number.isInteger(colIdx) && colIdx >= 0) {\n
			gridState.focusCol = colIdx;\n
		}\n
		const empId = cell.getAttribute('data-employee-id');\n
		const day = cell.getAttribute('data-duty-date');\n
		const key = cellSelectionKey(empId, day);\n
		const filled = cell.classList.contains('dc-roster-grid__cell--filled');\n
		if (!filled) {\n
			if (canAddAssignment()) {\n
				toggleGridSelection(key);\n
				syncVisibleGridChrome();\n
				updateBulkBar();\n
			}\n
			return;\n
		}\n
		const byKey = assignmentIndex(state.assignments || []);\n
		const cellAssignments = byKey.get(key) || [];\n
		if (canAddAssignment() && cellAssignments[0]) {\n
			openAssignmentEditModal(cellAssignments[0], cell);\n
			return;\n
		}\n
		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');\n
	}\n
\n
	function ensureGridFocusVisible() {\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const employees = state.employees || [];\n
		if (!scroller || !employees.length) {\n
			return;\n
		}\n
		const VW = virtualApi();\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);\n
		const next = VW.scrollTopToRevealIndex({\n
			index: gridState.focusRow,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop: scroller.scrollTop,\n
			total: employees.length,\n
		});\n
		if (Math.abs(next - scroller.scrollTop) > 0.5) {\n
			scroller.scrollTop = next;\n
		}\n
	}\n
\n
	function renderRosterGrid(assignments) {\n
		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });\n
	}\n
\n
	function paintGridWindow(options) {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		const employees = state.employees || [];\n
		const dates = periodDateList();\n
		const byKey = assignmentIndex(assignments);\n
\n
		if (!employees.length || !dates.length) {\n
			setGridEmptyState(\n
				root,\n
				t('dutycheck', 'Add employees and an open period to use the planning grid.'),\n
			);\n
			gridState.windowStart = 0;\n
			gridState.windowEnd = 0;\n
			gridState.paintedEmployeeCount = employees.length;\n
			gridState.paintedDateCount = dates.length;\n
			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		prepareGridForRows(root, employees, dates);\n
		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));\n
		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));\n
\n
		const VW = virtualApi();\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = scroller && gridState.view === 'grid'\n
			? Math.max(0, scroller.clientHeight - headerHeight)\n
			: 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: employees.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& gridState.windowStart === range.start\n
			&& gridState.windowEnd === range.end\n
			&& gridState.paintedEmployeeCount === employees.length\n
			&& gridState.paintedDateCount === dates.length) {\n
			syncVisibleGridChrome();\n
			updateGridWindowStatus(range, employees.length, dates.length);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const savedLeft = scroller ? scroller.scrollLeft : 0;\n
		const dayMetaByDate = buildDutyDayMeta(dates);\n
		const frag = document.createDocumentFragment();\n
		const header = create('div', {\n
			class: 'dc-roster-grid__row dc-roster-grid__row--head',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': '1' },\n
		});\n
		header.appendChild(create('div', {\n
			class: 'dc-roster-grid__corner',\n
			role: 'columnheader',\n
			text: t('dutycheck', 'Person'),\n
			attrs: { 'aria-colindex': '1' },\n
		}));\n
		dates.forEach((day, colIdx) => {\n
			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));\n
		});\n
		frag.appendChild(header);\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createGridSpacer(range.padBefore));\n
		}\n
		const windowEmployees = employees.slice(range.start, range.end);\n
		windowEmployees.forEach((emp, offset) => {\n
			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));\n
		});\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createGridSpacer(range.padAfter));\n
		}\n
		root.replaceChildren(frag);\n
		gridState.windowStart = range.start;\n
		gridState.windowEnd = range.end;\n
		gridState.paintedEmployeeCount = employees.length;\n
		gridState.paintedDateCount = dates.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
			scroller.scrollLeft = savedLeft;\n
		}\n
		const headerEl = root.querySelector('.dc-roster-grid__row--head');\n
		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');\n
		const measuredHeader = measureHeight(headerEl);\n
		const measuredRow = measureHeight(bodyRow);\n
		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)\n
			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))\n
			&& !gridState.remeasuring) {\n
			if (measuredHeader > 0) {\n
				gridState.headerHeight = measuredHeader;\n
			}\n
			if (measuredRow > 0) {\n
				gridState.rowHeight = measuredRow;\n
			}\n
			gridState.remeasuring = true;\n
			paintGridWindow({ assignments, force: true });\n
			gridState.remeasuring = false;\n
			return;\n
		}\n
		if (measuredHeader > 0) {\n
			gridState.headerHeight = measuredHeader;\n
		}\n
		if (measuredRow > 0) {\n
			gridState.rowHeight = measuredRow;\n
		}\n
		bindGridInteractions(root);\n
		updateGridWindowStatus(range, employees.length, dates.length);\n
		updateBulkBar();\n
	}\n
\n
	function bindGridInteractions(root) {\n
		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {\n
			return;\n
		}\n
		root.dataset.dcGridBound = '1';\n
		root.addEventListener('click', (ev) => {\n
			const cell = ev.target.closest('[role="gridcell"]');\n
			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {\n
				return;\n
			}\n
			activateGridCell(cell);\n
		});\n
		root.addEventListener('keydown', (ev) => {\n
			const employees = state.employees || [];\n
			const dates = periodDateList();\n
			const rowCount = employees.length;\n
			const colCount = dates.length;\n
			if (!rowCount || !colCount) {\n
				return;\n
			}\n
			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];\n
			if (!gridKeys.includes(ev.key)) {\n
				return;\n
			}\n
			ev.preventDefault();\n
			if (ev.key === 'ArrowUp') {\n
				gridState.focusRow = Math.max(0, gridState.focusRow - 1);\n
			} else if (ev.key === 'ArrowDown') {\n
				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);\n
			} else if (ev.key === 'ArrowLeft') {\n
				gridState.focusCol = Math.max(0, gridState.focusCol - 1);\n
			} else if (ev.key === 'ArrowRight') {\n
				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);\n
			} else if (ev.key === 'Home') {\n
				gridState.focusCol = 0;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = 0;\n
				}\n
			} else if (ev.key === 'End') {\n
				gridState.focusCol = colCount - 1;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = rowCount - 1;\n
				}\n
			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {\n
				const VW = virtualApi();\n
				const scroller = document.getElementById('dc-roster-grid-scroller');\n
				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
				const viewportHeight = scroller\n
					? Math.max(rowHeight, scroller.clientHeight - headerHeight)\n
					: rowHeight;\n
				const stride = typeof VW.pageStride === 'function'\n
					? VW.pageStride({ rowHeight, viewportHeight })\n
					: 1;\n
				if (ev.key === 'PageDown') {\n
					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);\n
				} else {\n
					gridState.focusRow = Math.max(0, gridState.focusRow - stride);\n
				}\n
			} else if (ev.key === 'Enter') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				activateGridCell(cell);\n
				focusGridCellIfVisible(root);\n
				return;\n
			} else if (ev.key === ' ' || ev.key === 'Spacebar') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				const empId = cell?.getAttribute('data-employee-id');\n
				const day = cell?.getAttribute('data-duty-date');\n
				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');\n
				if (empId && day && isEmpty && canAddAssignment()) {\n
					toggleGridSelection(cellSelectionKey(empId, day));\n
					syncVisibleGridChrome();\n
					updateBulkBar();\n
				}\n
				focusGridCellIfVisible(root);\n
				return;\n
			}\n
			ensureGridFocusVisible();\n
			paintGridWindow({ force: false });\n
			focusGridCellIfVisible(root);\n
		});\n
	}\n
\n
	function toggleGridSelection(key) {\n
		if (gridState.selected.has(key)) {\n
			gridState.selected.delete(key);\n
		} else {\n
			gridState.selected.add(key);\n
		}\n
	}\n
\n
	function updateBulkBar() {\n
		const bar = document.getElementById('dc-roster-bulk-bar');\n
		const countEl = document.getElementById('dc-roster-bulk-count');\n
		if (!bar || !countEl) {\n
			return;\n
		}\n
		const n = gridState.selected.size;\n
		if (n < 1 || !canAddAssignment()) {\n
			bar.hidden = true;\n
			return;\n
		}\n
		bar.hidden = false;\n
		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (sel && !(sel.options && sel.options.length)) {\n
			populateBulkTemplates();\n
		}\n
	}\n
\n
	async function populateBulkTemplates() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (!sel) {\n
			return;\n
		}\n
		await loadTemplates();\n
		sel.replaceChildren();\n
		const templates = state.templates || [];\n
		if (!templates.length) {\n
			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));\n
			return;\n
		}\n
		for (const tpl of templates) {\n
			sel.appendChild(create('option', {\n
				value: String(tpl.id),\n
				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,\n
			}));\n
		}\n
	}\n
\n
	function revealAssignmentRow(id) {\n
		const assignmentId = Number(id);\n
		const assignments = state.assignments || [];\n
		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);\n
		if (idx < 0) {\n
			return;\n
		}\n
		applyRosterViewChrome('list');\n
		const run = () => {\n
			const scroller = document.getElementById('dc-assignments-table-wrap');\n
			const VW = virtualApi();\n
			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
			if (scroller) {\n
				scroller.scrollTop = VW.scrollTopToRevealIndex({\n
					index: idx,\n
					rowHeight,\n
					viewportHeight: Math.max(rowHeight, scroller.clientHeight),\n
					scrollTop: scroller.scrollTop,\n
					total: assignments.length,\n
				});\n
			}\n
			paintListWindow({ assignments, force: true });\n
			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);\n
			if (row instanceof HTMLElement) {\n
				row.scrollIntoView({\n
					block: 'nearest',\n
					behavior: 'auto',\n
				});\n
				row.focus();\n
				row.classList.add('dc-row--flash');\n
				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);\n
			}\n
		};\n
		window.requestAnimationFrame(run);\n
	}\n
\n
	function setRosterPaintAll(paintAll) {\n
		gridState.paintAll = paintAll === true;\n
		paintListWindow({ force: true });\n
		paintGridWindow({ force: true });\n
	}\n
\n
	function wireRosterVirtualization() {\n
		const gridScroller = document.getElementById('dc-roster-grid-scroller');\n
		const listScroller = document.getElementById('dc-assignments-table-wrap');\n
		let gridRaf = 0;\n
		let listRaf = 0;\n
		if (gridScroller) {\n
			gridScroller.addEventListener('scroll', () => {\n
				if (gridRaf) {\n
					return;\n
				}\n
				gridRaf = window.requestAnimationFrame(() => {\n
					gridRaf = 0;\n
					paintGridWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		if (listScroller) {\n
			listScroller.addEventListener('scroll', () => {\n
				if (listRaf) {\n
					return;\n
				}\n
				listRaf = window.requestAnimationFrame(() => {\n
					listRaf = 0;\n
					paintListWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		const resize = () => {\n
			if (gridState.view === 'grid') {\n
				paintGridWindow({ force: true });\n
			} else {\n
				paintListWindow({ force: true });\n
			}\n
		};\n
		if (typeof ResizeObserver === 'function') {\n
			const ro = new ResizeObserver(resize);\n
			if (gridScroller) {\n
				ro.observe(gridScroller);\n
			}\n
			if (listScroller) {\n
				ro.observe(listScroller);\n
			}\n
		} else {\n
			window.addEventListener('resize', resize);\n
		}\n
		window.addEventListener('beforeprint', () => setRosterPaintAll(true));\n
		window.addEventListener('afterprint', () => setRosterPaintAll(false));\n
		if (typeof window.matchMedia === 'function') {\n
			try {\n
				const printMq = { addEventListener() {}, addListener() {} };\n
				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);\n
				if (typeof printMq.addEventListener === 'function') {\n
					printMq.addEventListener('change', onPrintMq);\n
				} else if (typeof printMq.addListener === 'function') {\n
					printMq.addListener(onPrintMq);\n
				}\n
			} catch (_) {\n
				/* print media query is not available in every test/jsdom shim */\n
			}\n
		}\n
	}\n
\n
	function applyRosterViewChrome(view) {\n
		gridState.view = view === 'list' ? 'list' : 'grid';\n
		const gridWrap = document.getElementById('dc-roster-grid-wrap');\n
		const listPanel = document.getElementById('dc-roster-list-panel');\n
		const gridBtn = document.getElementById('dc-roster-view-grid');\n
		const listBtn = document.getElementById('dc-roster-view-list');\n
		if (gridWrap) {\n
			gridWrap.hidden = gridState.view !== 'grid';\n
		}\n
		if (listPanel) {\n
			listPanel.hidden = gridState.view !== 'list';\n
		}\n
		if (gridBtn) {\n
			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');\n
		}\n
		if (listBtn) {\n
			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');\n
		}\n
	}\n
\n
	function setRosterView(view) {\n
		applyRosterViewChrome(view);\n
		window.requestAnimationFrame(() => {\n
			if (gridState.view === 'list') {\n
				paintListWindow({ force: true });\n
			} else {\n
				paintGridWindow({ force: true });\n
			}\n
		});\n
	}\n
\n
	async function applyBulkFromTemplate() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		const tplId = Number(sel?.value || 0);\n
		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);\n
		if (!tpl || gridState.selected.size < 1) {\n
			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');\n
			return;\n
		}\n
		if (!Number(tpl.locationId || 0)) {\n
			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');\n
			return;\n
		}\n
		const periodId = Number(selectedPeriodId() || 0);\n
		let ok = 0;\n
		let fail = 0;\n
		for (const key of Array.from(gridState.selected)) {\n
			const sep = key.indexOf('|');\n
			if (sep < 1) {\n
				fail += 1;\n
				continue;\n
			}\n
			const employeeId = Number(key.slice(0, sep));\n
			const dutyDate = key.slice(sep + 1);\n
			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {\n
				fail += 1;\n
				continue;\n
			}\n
			const locationId = Number(tpl.locationId || 0);\n
			if (!locationId) {\n
				fail += 1;\n
				continue;\n
			}\n
			try {\n
				await submitAssignment({\n
					periodId,\n
					employeeId: Number(employeeId),\n
					locationId,\n
					dutyDate,\n
					startTime: String(tpl.startTime).slice(0, 5),\n
					endTime: String(tpl.endTime).slice(0, 5),\n
					breakMinutes: Number(tpl.breakMinutes || 0),\n
					note: '',\n
					assignmentId: 0,\n
				}, true);\n
				ok += 1;\n
			} catch {\n
				fail += 1;\n
			}\n
		}\n
		gridState.selected.clear();\n
		await loadRoster(periodId || null);\n
		Msg.announce(\n
			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')\n
				.replace('{ok}', String(ok))\n
				.replace('{fail}', String(fail)),\n
			fail ? 'warning' : 'info',\n
		);\n
	}\n
\n
	function uniqueConflictTypes(conflicts) {\n
		const types = new Set();\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity === 'soft' && conflict?.type) {\n
				types.add(String(conflict.type));\n
			}\n
		}\n
		return Array.from(types);\n
	}\n
\n
	function softConflictSummary(conflicts) {\n
		const lines = [];\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity !== 'soft') {\n
				continue;\n
			}\n
			const raw = String(conflict?.message || '');\n
			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');\n
			if (text && !lines.includes(text)) {\n
				lines.push(text);\n
			}\n
		}\n
		return lines.join('\n');\n
	}\n
\n
	function errorCodeFrom(err) {\n
		return String(err?.code || err?.payload?.error?.code || '');\n
	}\n
\n
	function syncAssignmentPeriodFromSwitcher() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (!switcher || !periodHidden) {\n
			return;\n
		}\n
		const selected = Number(switcher.value);\n
		if (Number.isInteger(selected) && selected > 0) {\n
			periodHidden.value = String(selected);\n
		}\n
	}\n
\n
	function announceAssignmentSaveError(err) {\n
		clearAssignmentFormSuccess();\n
		const friendly = errorMessageFor(err);\n
		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');\n
		updateAssignmentFormFeedback(message);\n
		Msg.announce(message, 'error');\n
	}\n
\n
	async function submitAssignment(payload, retryWithAck = true) {\n
		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		try {\n
			if (editId > 0) {\n
				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);\n
			}\n
			return await Api.post('/apps/dutycheck/api/assignments', payload);\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Save with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return submitAssignment({ ...payload, acknowledgements }, false);\n
			}\n
			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Employee cannot work (blackout)'),\n
					label: t(\n
						'dutycheck',\n
						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',\n
					),\n
					confirmLabel: t('dutycheck', 'Schedule anyway'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
					maxLength: 200,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	function setAssignmentFormBusy(form, busy) {\n
		if (!form) return;\n
		const submit = form.querySelector('button[type="submit"]');\n
		const clear = document.getElementById('dc-assignment-form-clear');\n
		if (submit) {\n
			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';\n
			if (busy) {\n
				submit.setAttribute('aria-busy', 'true');\n
			} else {\n
				submit.removeAttribute('aria-busy');\n
			}\n
		}\n
		if (clear) {\n
			clear.disabled = busy;\n
		}\n
	}\n
\n
	function renderSetupCallout(data) {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		const list = document.getElementById('dc-roster-setup-checklist');\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!callout || !list || !form) return;\n
		const urls = readUrls();\n
		const missing = [];\n
		if (!state.periods.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Create a planning period.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		if (!state.employees.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active employee.'),\n
				url: urls.employees,\n
				cta: t('dutycheck', 'Go to Employees'),\n
			});\n
		}\n
		if (!state.locations.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active location.'),\n
				url: urls.locations,\n
				cta: t('dutycheck', 'Go to Locations'),\n
			});\n
		}\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));\n
		const periodIsOpen = (selectedPeriod?.status || '') === 'open';\n
		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {\n
			missing.push({\n
				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		list.replaceChildren();\n
		if (!missing.length) {\n
			callout.hidden = true;\n
			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {\n
				if (el.dataset.dcLockedBySetup === '1') {\n
					el.disabled = false;\n
					delete el.dataset.dcLockedBySetup;\n
				}\n
			});\n
			updateAddAssignmentControl();\n
			return;\n
		}\n
		for (const item of missing) {\n
			const li = create('li');\n
			li.appendChild(document.createTextNode(item.text + ' '));\n
			if (item.url) {\n
				li.appendChild(create('a', {\n
					class: 'button',\n
					href: item.url,\n
					text: item.cta,\n
				}));\n
			}\n
			list.appendChild(li);\n
		}\n
		callout.hidden = false;\n
		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {\n
			if (!el.disabled) {\n
				el.dataset.dcLockedBySetup = '1';\n
				el.disabled = true;\n
			}\n
		});\n
		updateAddAssignmentControl();\n
	}\n
\n
	function readUrls() {\n
		try {\n
			const raw = document.getElementById('app-content')?.dataset?.dcUrls;\n
			if (!raw) return {};\n
			return JSON.parse(raw) || {};\n
		} catch (_) {\n
			return {};\n
		}\n
	}\n
\n
	function restoreAssignmentFormPanel() {\n
		const host = document.getElementById('dc-assignment-form-host');\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (host && panel && panel.parentElement !== host) {\n
			host.appendChild(panel);\n
		}\n
	}\n
\n
	function selectedPeriodFromState() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;\n
		const periodId = Number(raw);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function isAssignmentSetupBlocking() {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		return !!(callout && !callout.hidden);\n
	}\n
\n
	function canAddAssignment() {\n
		if (!state.canCreateAssignments) {\n
			return false;\n
		}\n
		if (!state.periods.length || !state.employees.length || !state.locations.length) {\n
			return false;\n
		}\n
		const period = selectedPeriodFromState();\n
		return !!(period && String(period.status || '').toLowerCase() === 'open');\n
	}\n
\n
	function addAssignmentDisabledReason() {\n
		if (!state.periods.length) {\n
			return t('dutycheck', 'Create a planning period first.');\n
		}\n
		if (!state.employees.length) {\n
			return t('dutycheck', 'Add at least one active employee before planning shifts.');\n
		}\n
		if (!state.locations.length) {\n
			return t('dutycheck', 'Add at least one active location before planning shifts.');\n
		}\n
		const period = selectedPeriodFromState();\n
		if (!period) {\n
			return t('dutycheck', 'Select a period before adding assignments.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!state.canCreateAssignments) {\n
			return t('dutycheck', 'You cannot add assignments for this period.');\n
		}\n
		return t('dutycheck', 'Adding assignments is not available right now.');\n
	}\n
\n
	function updateAddAssignmentControl() {\n
		const allowed = canAddAssignment();\n
		const reason = addAssignmentDisabledReason();\n
		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {\n
			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.\n
			btn.hidden = !allowed;\n
			btn.disabled = !allowed;\n
			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');\n
			btn.classList.toggle('dc-is-disabled', !allowed);\n
			if (allowed) {\n
				btn.removeAttribute('title');\n
			} else {\n
				btn.setAttribute('title', reason);\n
			}\n
		});\n
		const emptyHint = document.getElementById('dc-roster-empty-add-hint');\n
		if (emptyHint) {\n
			emptyHint.textContent = allowed\n
				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')\n
				: reason;\n
		}\n
	}\n
\n
	function clearAssignmentsSectionSuccess() {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.classList.remove('dc-roster-flash--visible');\n
	}\n
\n
	function showAssignmentsSectionSuccess(message) {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.textContent = String(message);\n
		el.hidden = false;\n
		el.classList.add('dc-roster-flash--visible');\n
	}\n
\n
	function prepareAssignmentFormForModal() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		clearAssignmentFormSuccess();\n
		updateAssignmentFormFeedback(null);\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate && period?.endDate) {\n
			dateInput.min = String(period.startDate);\n
			dateInput.max = String(period.endDate);\n
			if (!dateInput.value) {\n
				dateInput.value = String(period.startDate);\n
			}\n
		}\n
		refreshAssignmentFormEligibility();\n
		applyDefaultBreakToForm();\n
		wireBreakPrefillHint();\n
		fillTemplateSelect();\n
		D?.applyLocaleToTemporalInputs(form);\n
	}\n
\n
	function resetAssignmentFormAfterSave(data) {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		form.reset();\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden && data?.selectedPeriodId) {\n
			periodHidden.value = String(data.selectedPeriodId);\n
		}\n
		applyDefaultBreakToForm();\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate) {\n
			dateInput.value = String(period.startDate);\n
		}\n
		D?.applyLocaleToTemporalInputs(form);\n
		updateAssignmentFormFeedback(null);\n
	}\n
\n
	async function performAssignmentSave() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return false;\n
		}\n
		if (isAssignmentSetupBlocking()) {\n
			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(msg);\n
			Msg.announce(msg, 'error');\n
			return false;\n
		}\n
		const payload = readForm(form);\n
		const validation = validate(payload);\n
		if (validation) {\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(validation);\n
			Msg.announce(validation, 'error');\n
			focusFirstInvalidAssignmentField(payload);\n
			return false;\n
		}\n
		updateAssignmentFormFeedback(null);\n
		setAssignmentFormBusy(form, true);\n
		try {\n
			const response = await submitAssignment(payload, true);\n
			render(response?.data || {});\n
			const savedMsg = buildAssignmentSavedMessage(payload);\n
			clearAssignmentFormSuccess();\n
			showAssignmentsSectionSuccess(savedMsg);\n
			Msg.announce(savedMsg, 'success');\n
			rememberBreakMinutes(payload.breakMinutes);\n
			resetAssignmentFormAfterSave(response?.data || {});\n
			return true;\n
		} catch (err) {\n
			announceAssignmentSaveError(err);\n
			return false;\n
		} finally {\n
			setAssignmentFormBusy(form, false);\n
		}\n
	}\n
\n
	function syncAssignmentModalPrimary(instance) {\n
		if (!instance?.primaryBtn) {\n
			return;\n
		}\n
		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();\n
		instance.primaryBtn.disabled = blocked;\n
		if (blocked) {\n
			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());\n
		} else {\n
			instance.primaryBtn.removeAttribute('title');\n
		}\n
	}\n
\n
	async function openAssignmentCreateModal(triggerEl) {\n
		if (!canAddAssignment()) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
			return;\n
		}\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		renderSetupCallout(state.lastRosterData || {});\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Create assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save assignment'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => {\n
				const ok = await performAssignmentSave();\n
				return ok;\n
			},\n
			onClose: (result) => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				if (!result) {\n
					clearAssignmentFormSuccess();\n
					return;\n
				}\n
				const addBtn = document.getElementById('dc-roster-add-assignment');\n
				if (addBtn && typeof addBtn.focus === 'function') {\n
					addBtn.focus();\n
				}\n
			},\n
			onCancel: () => {\n
				clearAssignmentFormSuccess();\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
		const form = document.getElementById('dc-assignment-form');\n
		if (form && !form.dataset.dcModalSyncWired) {\n
			form.dataset.dcModalSyncWired = '1';\n
			form.addEventListener('change', () => {\n
				if (assignmentModalInstance?._open) {\n
					syncAssignmentModalPrimary(assignmentModalInstance);\n
				}\n
			});\n
		}\n
	}\n
\n
	async function openAssignmentEditModal(assignment, triggerEl) {\n
		if (!canAddAssignment() || !assignment?.id) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			return;\n
		}\n
		state.editingAssignmentId = Number(assignment.id);\n
		state.editingAssignmentVersion = Number(assignment.version ?? 0);\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = String(assignment.id);\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput) {\n
			dateInput.value = String(assignment.dutyDate || '');\n
		}\n
		refreshAssignmentFormEligibility();\n
		const emp = document.getElementById('dc-assignment-employee');\n
		if (emp) {\n
			emp.value = String(assignment.employeeId || '');\n
		}\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (loc) {\n
			loc.value = String(assignment.locationId || '');\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		if (start) {\n
			start.value = String(assignment.startTime || '').slice(0, 5);\n
		}\n
		const end = document.getElementById('dc-assignment-end');\n
		if (end) {\n
			end.value = String(assignment.endTime || '').slice(0, 5);\n
		}\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(assignment.breakMinutes ?? 0);\n
		}\n
		const note = document.getElementById('dc-assignment-note');\n
		if (note) {\n
			note.value = String(assignment.note || '');\n
		}\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Edit assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save changes'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => performAssignmentSave(),\n
			onClose: () => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				clearAssignmentFormSuccess();\n
				if (triggerEl && typeof triggerEl.focus === 'function') {\n
					triggerEl.focus();\n
				}\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
	}\n
\n
	async function cancelAssignmentRow(assignment, triggerEl) {\n
		if (!assignment?.id || !canAddAssignment()) {\n
			return;\n
		}\n
		const ok = window.confirm(\n
			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),\n
		);\n
		if (!ok) {\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});\n
			render(response?.data || {});\n
			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));\n
			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		} finally {\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
		}\n
	}\n
\n
	let templatesInflight = null;\n
\n
	async function ensureAssignmentModalPrereqs() {\n
		const jobs = [loadTemplates()];\n
		if (state.planningDefaultsFresh !== true) {\n
			jobs.push(refreshPlanningDefaultFromServer());\n
		}\n
		await Promise.all(jobs);\n
	}\n
\n
	async function loadTemplates() {\n
		if (state.templatesLoaded) {\n
			fillTemplateSelect();\n
			return;\n
		}\n
		if (templatesInflight) {\n
			await templatesInflight;\n
			fillTemplateSelect();\n
			return;\n
		}\n
		templatesInflight = (async () => {\n
			try {\n
				const response = await Api.get('/apps/dutycheck/api/templates', {});\n
				state.templates = Array.isArray(response?.data) ? response.data : [];\n
				state.templatesLoaded = true;\n
			} catch (_) {\n
				state.templates = [];\n
				state.templatesLoaded = false;\n
			} finally {\n
				templatesInflight = null;\n
			}\n
		})();\n
		await templatesInflight;\n
		fillTemplateSelect();\n
	}\n
\n
	function fillTemplateSelect() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select) {\n
			return;\n
		}\n
		const current = select.value;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));\n
		for (const tpl of state.templates) {\n
			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;\n
			select.appendChild(create('option', { value: String(tpl.id), text: label }));\n
		}\n
		if (current && [...select.options].some((o) => o.value === current)) {\n
			select.value = current;\n
		}\n
	}\n
\n
	function applySelectedTemplate() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select || !select.value) {\n
			return;\n
		}\n
		const tpl = state.templates.find((t) => String(t.id) === String(select.value));\n
		if (!tpl) {\n
			return;\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		const end = document.getElementById('dc-assignment-end');\n
		const br = document.getElementById('dc-assignment-break');\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (start) start.value = String(tpl.startTime || '').slice(0, 5);\n
		if (end) end.value = String(tpl.endTime || '').slice(0, 5);\n
		if (br) br.value = String(tpl.breakMinutes ?? 0);\n
		if (loc && tpl.locationId) loc.value = String(tpl.locationId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		refreshAssignmentFormEligibility();\n
	}\n
\n
	function refreshAcknowledgeStats(periodId) {\n
		const el = document.getElementById('dc-roster-ack-stats');\n
		if (!el) {\n
			return;\n
		}\n
		const id = Number(periodId);\n
		if (!Number.isInteger(id) || id <= 0) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === id);\n
		const status = String(period?.status || '').toLowerCase();\n
		if (status !== 'published' && status !== 'closed') {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const rows = Array.isArray(state.assignments) ? state.assignments : [];\n
		let total = 0;\n
		let acked = 0;\n
		for (const row of rows) {\n
			if (String(row?.status || '').toLowerCase() === 'cancelled') {\n
				continue;\n
			}\n
			total += 1;\n
			if (row?.acknowledged || row?.acknowledgedAt) {\n
				acked += 1;\n
			}\n
		}\n
		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;\n
		el.hidden = false;\n
		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')\n
			.replace('{acked}', String(acked))\n
			.replace('{total}', String(total))\n
			.replace('{pct}', String(pct));\n
	}\n
\n
	function fillCopySourceSelect(selectedPeriodId) {\n
		const select = document.getElementById('dc-roster-copy-source');\n
		const wrap = document.getElementById('dc-roster-copy-period');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!select || !wrap) {\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));\n
		const open = period && String(period.status || '').toLowerCase() === 'open';\n
		wrap.hidden = !open;\n
		state.copyPreviewReady = false;\n
		if (applyBtn) applyBtn.disabled = true;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));\n
		for (const p of state.periods) {\n
			if (Number(p.id) === Number(selectedPeriodId)) {\n
				continue;\n
			}\n
			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;\n
			select.appendChild(create('option', { value: String(p.id), text: label }));\n
		}\n
	}\n
\n
	async function runCopyPeriod(dryRun) {\n
		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);\n
		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);\n
		const status = document.getElementById('dc-roster-copy-status');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!targetId || !sourceId) {\n
			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {\n
				sourcePeriodId: sourceId,\n
				dryRun: !!dryRun,\n
			});\n
			const data = response?.data || {};\n
			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);\n
			const skipped = Number(data.skipped ?? 0);\n
			const msg = dryRun\n
				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')\n
					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))\n
					.replace('{skipped}', String(data.wouldSkip ?? skipped))\n
				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')\n
					.replace('{created}', String(created));\n
			if (status) {\n
				status.hidden = false;\n
				status.textContent = msg;\n
			}\n
			Msg.announce(msg, dryRun ? 'info' : 'success');\n
			if (dryRun) {\n
				state.copyPreviewReady = true;\n
				if (applyBtn) applyBtn.disabled = false;\n
			} else {\n
				state.copyPreviewReady = false;\n
				if (applyBtn) applyBtn.disabled = true;\n
				render(data.roster || data || {});\n
				if (!data.roster && data.assignments) {\n
					render(data);\n
				} else if (!data.roster) {\n
					await loadRoster(targetId);\n
				}\n
			}\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	function updateAdminExportLinks(selectedPeriodId) {\n
		const csv = document.getElementById('dc-roster-export-csv');\n
		const printLink = document.getElementById('dc-roster-export-print');\n
		if (!csv || !printLink) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		const baseCsv = String(urls.rosterExportCsv || '');\n
		const basePrint = String(urls.rosterPrint || '');\n
		const id = Number(selectedPeriodId);\n
		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;\n
		if (!ok) {\n
			csv.href = '#dc-roster-period-section';\n
			printLink.href = '#dc-roster-period-section';\n
			csv.setAttribute('aria-disabled', 'true');\n
			printLink.setAttribute('aria-disabled', 'true');\n
			return;\n
		}\n
		const q = new URLSearchParams({ periodId: String(id) }).toString();\n
		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;\n
		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;\n
		csv.removeAttribute('aria-disabled');\n
		printLink.removeAttribute('aria-disabled');\n
	}\n
\n
	function wireAdminExportGuards() {\n
		const guard = (event) => {\n
			const el = event.currentTarget;\n
			if (el && el.getAttribute('aria-disabled') === 'true') {\n
				event.preventDefault();\n
				Msg.announce(\n
					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),\n
					'warning',\n
				);\n
			}\n
		};\n
		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);\n
		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);\n
	}\n
\n
	let rosterPhpL10nCache;\n
	function getRosterPhpL10n() {\n
		if (rosterPhpL10nCache !== undefined) {\n
			return rosterPhpL10nCache;\n
		}\n
		const el = document.getElementById('dc-roster-php-l10n');\n
		const raw = el && el.dataset ? el.dataset.l10n : '';\n
		if (!raw) {\n
			rosterPhpL10nCache = null;\n
			return null;\n
		}\n
		try {\n
			rosterPhpL10nCache = JSON.parse(raw) || null;\n
		} catch (_) {\n
			rosterPhpL10nCache = null;\n
		}\n
		return rosterPhpL10nCache;\n
	}\n
\n
	function translateConflictMessage(message) {\n
		const raw = String(message || '');\n
		if (!raw) {\n
			return t('dutycheck', 'Unknown conflict');\n
		}\n
		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];\n
		if (fromPhp) {\n
			return fromPhp;\n
		}\n
		return t('dutycheck', raw);\n
	}\n
\n
	function render(data) {\n
		state.lastRosterData = data;\n
		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {\n
			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);\n
			state.planningDefaultsFresh = true;\n
		}\n
		state.periods = data.periods || [];\n
		state.employees = data.employees || [];\n
		state.locations = data.locations || [];\n
		state.assignments = data.assignments || [];\n
		state.absenceBlocks = data.absenceBlocks || [];\n
		state.canCreateAssignments = Boolean(data.canCreateAssignments);\n
		fillPeriodSwitcher(state.periods, data.selectedPeriodId);\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden) {\n
			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));\n
		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {\n
			dateInput.min = String(selectedPeriod.startDate);\n
			dateInput.max = String(selectedPeriod.endDate);\n
			if (!dateInput.value && state.canCreateAssignments) {\n
				dateInput.value = String(selectedPeriod.startDate);\n
			}\n
		} else if (dateInput) {\n
			dateInput.removeAttribute('min');\n
			dateInput.removeAttribute('max');\n
		}\n
		fillSelect('dc-assignment-location', state.locations, {\n
			emptyText: t('dutycheck', 'No active locations yet'),\n
		});\n
		updateLocationTimezoneHint();\n
		refreshAssignmentFormEligibility();\n
		renderAssignments(data.assignments || []);\n
		renderConflicts(data.conflicts || []);\n
		renderSetupCallout(data);\n
		updateAddAssignmentControl();\n
		updateAdminExportLinks(data.selectedPeriodId);\n
		fillCopySourceSelect(data.selectedPeriodId);\n
		refreshAcknowledgeStats(data.selectedPeriodId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
	}\n
\n
	function selectedPeriodIdFromUrl() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const raw = params.get('periodId');\n
		const parsed = Number(raw);\n
		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;\n
	}\n
\n
	function updateUrlPeriodId(periodId) {\n
		const params = new URLSearchParams(window.location.search || '');\n
		if (periodId && Number(periodId) > 0) {\n
			params.set('periodId', String(periodId));\n
		} else {\n
			params.delete('periodId');\n
		}\n
		const query = params.toString();\n
		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);\n
	}\n
\n
	async function loadRoster(periodId) {\n
		if (C.isModalOpen?.()) {\n
			C.dismissOpenModal();\n
		} else {\n
			restoreAssignmentFormPanel();\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		C.setLoadingRow(tbody, 7);\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });\n
			render(response?.data || {});\n
			updateUrlPeriodId(response?.data?.selectedPeriodId || null);\n
			void loadRosterSignals(response?.data || {});\n
			void loadCoverageStrip(response?.data?.selectedPeriodId);\n
			return response?.data || {};\n
		} catch (err) {\n
			const code = String(err?.payload?.error?.code || err?.code || '');\n
			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {\n
				try {\n
					const fallback = await Api.get('/apps/dutycheck/api/roster', {});\n
					render(fallback?.data || {});\n
					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);\n
					void loadRosterSignals(fallback?.data || {});\n
					void loadCoverageStrip(fallback?.data?.selectedPeriodId);\n
					Msg.announce(\n
						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),\n
						'warning',\n
					);\n
					return fallback?.data || {};\n
				} catch (retryErr) {\n
					Msg.handleApiError(retryErr);\n
					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
					return null;\n
				}\n
			}\n
			Msg.handleApiError(err);\n
			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
			return null;\n
		} finally {\n
			C.clearLoadingRow(tbody);\n
		}\n
	}\n
\n
	function dateOverlapsBlackout(day, blackout) {\n
		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);\n
		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);\n
		if (!start || !end) return false;\n
		return day >= start && day <= end;\n
	}\n
\n
	async function loadRosterSignals(data) {\n
		state.preferencesByEmployee = {};\n
		state.blackoutsByEmployeeDate = {};\n
		const employees = data.employees || state.employees || [];\n
		const period = (data.periods || state.periods || []).find(\n
			(p) => Number(p.id) === Number(data.selectedPeriodId),\n
		);\n
		const from = period?.startDate || period?.start_date || '';\n
		const to = period?.endDate || period?.end_date || '';\n
		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);\n
		if (!ids.length) return;\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/roster/signals', {\n
				employeeIds: ids,\n
				from,\n
				to,\n
			});\n
			const prefs = res?.data?.preferences || [];\n
			const blackouts = res?.data?.blackouts || [];\n
			prefs.forEach((p) => {\n
				const eid = String(p.employeeId || p.employee_id || '');\n
				if (!eid) return;\n
				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];\n
				state.preferencesByEmployee[eid].push(p);\n
			});\n
			const days = [];\n
			if (from && to) {\n
				const cur = new Date(from + 'T12:00:00');\n
				const end = new Date(to + 'T12:00:00');\n
				while (cur <= end) {\n
					const pad = (n) => String(n).padStart(2, '0');\n
					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);\n
					cur.setDate(cur.getDate() + 1);\n
				}\n
			}\n
			blackouts.forEach((b) => {\n
				const eid = Number(b.employeeId || b.employee_id || 0);\n
				if (!eid) return;\n
				days.forEach((day) => {\n
					if (dateOverlapsBlackout(day, b)) {\n
						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;\n
					}\n
				});\n
			});\n
			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {\n
				paintGridWindow({ force: true });\n
			}\n
		} catch (_) {\n
			// Optional enrichment — ignore failures.\n
		}\n
	}\n
\n
	async function loadCoverageStrip(periodId) {\n
		const strip = document.getElementById('dc-coverage-strip');\n
		const text = document.getElementById('dc-coverage-strip-text');\n
		if (!strip || !text) return;\n
		const id = Number(periodId || 0);\n
		if (!id) {\n
			strip.hidden = true;\n
			return;\n
		}\n
		try {\n
			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);\n
			const d = res?.data || {};\n
			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);\n
			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);\n
			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);\n
			let message;\n
			if (ready || (hard === 0 && soft === 0)) {\n
				message = t('dutycheck', 'Ready to publish — no blocking issues.');\n
				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--ok');\n
			} else if (hard > 0) {\n
				message = t('dutycheck', '{n} must-fix issues before publish.')\n
					.replace('{n}', String(hard));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');\n
				strip.classList.add('dc-coverage-strip--critical');\n
			} else {\n
				message = t('dutycheck', '{n} items need confirmation.')\n
					.replace('{n}', String(soft));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--warn');\n
			}\n
			text.textContent = message;\n
			strip.hidden = false;\n
		} catch (_) {\n
			strip.hidden = true;\n
		}\n
	}\n
\n
	function currentSuggestLocationId() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const fromUrl = Number(params.get('locationId') || 0);\n
		if (Number.isInteger(fromUrl) && fromUrl > 0) {\n
			return fromUrl;\n
		}\n
		const filterEl = document.getElementById('dc-roster-location')\n
			|| document.getElementById('dc-assignment-location');\n
		const fromUi = Number(filterEl?.value || 0);\n
		if (Number.isInteger(fromUi) && fromUi > 0) {\n
			return fromUi;\n
		}\n
		return null;\n
	}\n
\n
	async function runSuggestFill() {\n
		const periodId = Number(selectedPeriodId() || 0);\n
		if (!periodId) {\n
			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');\n
			return;\n
		}\n
		const btn = document.getElementById('dc-roster-suggest-fill');\n
		if (btn) {\n
			btn.disabled = true;\n
			btn.setAttribute('aria-busy', 'true');\n
		}\n
		const locId = currentSuggestLocationId();\n
		const suggestBody = { locationId: locId || null };\n
		try {\n
			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);\n
			const d = preview?.data || {};\n
			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {\n
				const noLoc = Number(d.skippedNoLocation ?? 0);\n
				Msg.announce(\n
					noLoc > 0\n
						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')\n
						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),\n
					'warning',\n
				);\n
				return;\n
			}\n
			const body = create('div', { class: 'dc-suggest-preview' }, [\n
				create('p', {\n
					text: t('dutycheck', 'Would create {n} shifts from patterns.')\n
						.replace('{n}', String(d.created ?? 0)),\n
				}),\n
				create('ul', { class: 'dc-suggest-preview__counts' }, [\n
					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),\n
				]),\n
			]);\n
			const confirmed = await new Promise((resolve) => {\n
				let settled = false;\n
				const finish = (value) => {\n
					if (settled) return;\n
					settled = true;\n
					resolve(value);\n
				};\n
				C.openModal({\n
					title: t('dutycheck', 'Suggest fill preview'),\n
					primaryLabel: t('dutycheck', 'Apply suggest fill'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					render: () => body,\n
					onSubmit: async () => {\n
						finish(true);\n
						return true;\n
					},\n
					onCancel: () => finish(false),\n
					onClose: () => finish(false),\n
				});\n
			});\n
			if (!confirmed) return;\n
			if ((d.created ?? 0) < 1) {\n
				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');\n
				return;\n
			}\n
			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);\n
			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');\n
			await loadRoster(periodId);\n
		} catch (err) {\n
			const code = String(err?.code || err?.payload?.error?.code || '');\n
			if (code === 'ROTATION_DISABLED') {\n
				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');\n
			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {\n
				Msg.announce(\n
					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),\n
					'warning',\n
				);\n
			} else if (code === 'RATE_LIMITED') {\n
				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');\n
			} else {\n
				Msg.handleApiError(err);\n
			}\n
		} finally {\n
			if (btn) {\n
				btn.disabled = false;\n
				btn.removeAttribute('aria-busy');\n
			}\n
		}\n
	}\n
\n
	function clearAssignmentFormFields() {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) return;\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		const periodVal = periodHidden ? String(periodHidden.value || '') : '';\n
		form.reset();\n
		if (periodHidden && periodVal) {\n
			periodHidden.value = periodVal;\n
		}\n
		applyDefaultBreakToForm();\n
		D?.applyLocaleToTemporalInputs(form);\n
		refreshAssignmentFormEligibility();\n
		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');\n
		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {\n
			showAssignmentFormSuccess(clearedMsg, 'info');\n
		}\n
		Msg.announce(clearedMsg, 'info');\n
	}\n
\n
	function readForm(form) {\n
		const data = new FormData(form);\n
		const rawStart = String(data.get('startTime') || '');\n
		const rawEnd = String(data.get('endTime') || '');\n
		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();\n
		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();\n
		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);\n
		const out = {\n
			periodId: Number(data.get('periodId')),\n
			employeeId: Number(data.get('employeeId')),\n
			locationId: Number(data.get('locationId')),\n
			dutyDate: String(data.get('dutyDate') || ''),\n
			startTime,\n
			endTime,\n
			breakMinutes: Number(data.get('breakMinutes') || 0),\n
			note: String(data.get('note') || '').trim(),\n
			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,\n
		};\n
		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {\n
			out.expectedVersion = Number(state.editingAssignmentVersion);\n
		}\n
		return out;\n
	}\n
\n
	/**\n
	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.\n
	 */\n
	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;\n
		if (start === null || end === null) return null;\n
		let endAdj = end;\n
		if (endAdj <= start) {\n
			endAdj += 24 * 60;\n
		}\n
		return (endAdj - start) - breakMinutes;\n
	}\n
\n
	function validate(payload) {\n
		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {\n
			return t('dutycheck', 'Select a period before saving.');\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		if (!period) {\n
			return t('dutycheck', 'That planning period was not found.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {\n
			return t('dutycheck', 'Select an employee.');\n
		}\n
		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {\n
			return t('dutycheck', 'Select a location.');\n
		}\n
		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {\n
			return t('dutycheck', 'Date, start time, and end time are required.');\n
		}\n
		if (period.startDate && period.endDate\n
			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {\n
			return t('dutycheck', 'Date must fall within the selected period.');\n
		}\n
		if (payload.startTime === payload.endTime) {\n
			return t(\n
				'dutycheck',\n
				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
			);\n
		}\n
		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {\n
			return t('dutycheck', 'Break minutes must be between 0 and 720.');\n
		}\n
		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
		if (effective === null) {\n
			return t('dutycheck', 'Please check the dates and times.');\n
		}\n
		if (effective <= 0) {\n
			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
		}\n
		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			return t('dutycheck', 'This employee has approved leave on the selected date.');\n
		}\n
		if (findOverlapAssignment(payload)) {\n
			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');\n
		}\n
		return null;\n
	}\n
\n
	function errorMessageFor(error) {\n
		const code = errorCodeFrom(error);\n
		switch (code) {\n
			case 'PERIOD_NOT_OPEN':\n
				return t('dutycheck', 'The selected period is not open for planning.');\n
			case 'PERIOD_NOT_FOUND':\n
				return t('dutycheck', 'That planning period was not found.');\n
			case 'DATE_OUTSIDE_PERIOD':\n
				return t('dutycheck', 'Date must fall within the selected period.');\n
			case 'EMPLOYEE_NOT_FOUND':\n
				return t('dutycheck', 'Employee not found or no longer available.');\n
			case 'LOCATION_NOT_FOUND':\n
				return t('dutycheck', 'Location not found or no longer available.');\n
			case 'EQUAL_DUTY_TIMES':\n
				return t(\n
					'dutycheck',\n
					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
				);\n
			case 'INVALID_SHIFT_LENGTH':\n
				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
			case 'INVALID_BREAK_MINUTES':\n
				return t('dutycheck', 'Break minutes are out of the allowed range.');\n
			case 'NOTE_TOO_LONG':\n
				return t('dutycheck', 'Note is too long (maximum 512 characters).');\n
			case 'ASSIGNMENT_OVERLAP':\n
				return t('dutycheck', 'This employee already has an overlapping assignment.');\n
			case 'ASSIGNMENT_DUPLICATE_SLOT':\n
				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');\n
			case 'STALE_VERSION':\n
				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');\n
			case 'CONFLICT_ACK_STALE':\n
				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');\n
			case 'COMPANY_MEMBERSHIP_REQUIRED':\n
				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');\n
			case 'EXPECTED_VERSION_REQUIRED':\n
				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');\n
			case 'ABSENCE_CONFLICT':\n
				return t('dutycheck', 'This employee has an approved absence on that date.');\n
			case 'BLACKOUT_CONFLICT':\n
				return t('dutycheck', 'This employee marked that they cannot work during this time.');\n
			case 'CONFLICT_ACK_REQUIRED':\n
				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');\n
			case 'REASON_TOO_SHORT':\n
				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');\n
			case 'INTERNAL_ERROR':\n
				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');\n
			case 'QUALIFICATION_MISSING':\n
				return t('dutycheck', 'This employee is missing a required qualification for that location.');\n
			case 'ASSIGNMENT_NOT_FOUND':\n
				return t('dutycheck', 'That assignment no longer exists. Reload the page.');\n
			case 'SCHEMA_NOT_READY':\n
				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');\n
			case 'INVALID_DATE':\n
			case 'INVALID_TIME':\n
				return t('dutycheck', 'Please check the dates and times.');\n
			case 'PERIOD_ID_REQUIRED':\n
				return t('dutycheck', 'Select a period before saving.');\n
			case 'EMPLOYEE_ID_REQUIRED':\n
				return t('dutycheck', 'Select an employee.');\n
			case 'LOCATION_ID_REQUIRED':\n
				return t('dutycheck', 'Select a location.');\n
			default:\n
				return null;\n
		}\n
	}\n
\n
\n
	async function loadPendingSwaps() {\n
		const list = document.getElementById('dc-swap-list');\n
		const empty = document.getElementById('dc-swap-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/swaps');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				li.appendChild(create('p', {\n
					text: row.toEmployeeId\n
						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId))\n
							.replace('{to}', String(row.toEmployeeId))\n
						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId)),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });\n
						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');\n
						await loadPendingSwaps();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });\n
						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');\n
						await loadPendingSwaps();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load swap requests.');\n
			}\n
		}\n
	}\n
\n
	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {\n
		try {\n
			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {\n
				acknowledgements,\n
			});\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Approve with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return approveOpenShiftClaim(openShiftId, acks, false);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	async function loadPendingOpenClaims() {\n
		const list = document.getElementById('dc-open-claim-list');\n
		const empty = document.getElementById('dc-open-claim-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;\n
				li.appendChild(create('p', {\n
					text: t('dutycheck', '{date} · employee #{emp}')\n
						.replace('{date}', String(when))\n
						.replace('{emp}', String(row.claimedByEmployeeId || '—')),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await approveOpenShiftClaim(row.id);\n
						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');\n
						await loadPendingOpenClaims();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});\n
						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');\n
						await loadPendingOpenClaims();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load pending claims.');\n
			}\n
		}\n
	}\n
\n
	function wireMarketplace() {\n
		const toggle = document.getElementById('dc-open-shift-create');\n
		const form = document.getElementById('dc-open-shift-form');\n
		toggle?.addEventListener('click', () => {\n
			if (!form) return;\n
			form.hidden = !form.hidden;\n
			const loc = document.getElementById('dc-os-location');\n
			if (loc && state.locations?.length) {\n
				loc.replaceChildren();\n
				for (const l of state.locations) {\n
					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));\n
				}\n
			}\n
			const period = selectedPeriodFromState();\n
			const date = document.getElementById('dc-os-date');\n
			if (date && period?.startDate) date.value = String(period.startDate);\n
		});\n
		document.getElementById('dc-os-save')?.addEventListener('click', async () => {\n
			const period = selectedPeriodFromState();\n
			if (!period?.id) {\n
				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');\n
				return;\n
			}\n
			try {\n
				await Api.post('/apps/dutycheck/api/open-shifts', {\n
					periodId: Number(period.id),\n
					locationId: Number(document.getElementById('dc-os-location')?.value || 0),\n
					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),\n
					startTime: String(document.getElementById('dc-os-start')?.value || ''),\n
					endTime: String(document.getElementById('dc-os-end')?.value || ''),\n
					breakMinutes: 0,\n
				});\n
				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');\n
				if (form) form.hidden = true;\n
			} catch (err) {\n
				Msg.handleApiError(err);\n
			}\n
		});\n
	}\n
\n
	document.addEventListener('DOMContentLoaded', async () => {\n
		D?.applyLocaleToTemporalInputs(document);\n
		wireBreakPrefillHint();\n
		window.addEventListener('dc-planning-defaults-changed', (event) => {\n
			const minutes = Number(event?.detail?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
		});\n
		wireAdminExportGuards();\n
		wireMarketplace();\n
		setRosterView('grid');\n
		wireRosterVirtualization();\n
		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));\n
		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));\n
		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {\n
			gridState.selected.clear();\n
			syncVisibleGridChrome();\n
			updateBulkBar();\n
		});\n
		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {\n
			void applyBulkFromTemplate();\n
		});\n
		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {\n
			void runSuggestFill();\n
		});\n
		await Promise.all([\n
			loadRoster(selectedPeriodIdFromUrl()),\n
			loadPendingSwaps(),\n
			loadPendingOpenClaims(),\n
		]);\n
\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		switcher?.addEventListener('change', async () => {\n
			clearAssignmentsSectionSuccess();\n
			const periodId = Number(switcher.value);\n
			await Promise.all([\n
				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),\n
				loadPendingSwaps(),\n
				loadPendingOpenClaims(),\n
			]);\n
		});\n
\n
		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {\n
			const btn = event.target.closest('.dc-roster-add-assignment-trigger');\n
			if (!btn) {\n
				return;\n
			}\n
			openAssignmentCreateModal(btn);\n
		});\n
\n
		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {\n
			clearAssignmentFormFields();\n
		});\n
\n
		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {\n
			applySelectedTemplate();\n
		});\n
		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {\n
			runCopyPeriod(true);\n
		});\n
		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {\n
			if (!state.copyPreviewReady) {\n
				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');\n
				return;\n
			}\n
			await runCopyPeriod(false);\n
		});\n
		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {\n
			state.copyPreviewReady = false;\n
			const applyBtn = document.getElementById('dc-roster-copy-apply');\n
			if (applyBtn) applyBtn.disabled = true;\n
		});\n
\n
		const form = document.getElementById('dc-assignment-form');\n
		const scheduleEligibilityRefresh = () => {\n
			clearAssignmentFormSuccess();\n
			clearAssignmentsSectionSuccess();\n
			refreshAssignmentFormEligibility();\n
		};\n
		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);\n
		form?.addEventListener('submit', (event) => {\n
			event.preventDefault();\n
			const dialog = form.closest('.dc-modal__dialog--roster-assignment');\n
			if (!dialog) {\n
				return;\n
			}\n
			const primary = dialog.querySelector('.dc-modal__actions .button.primary');\n
			if (primary && !primary.disabled) {\n
				primary.click();\n
			}\n
		});\n
	});\n
})();\n
' contains "window.matchMedia('print')".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:72

FAILURES!
Tests: 6, Assertions: 69, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.725812
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.232351
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.26182
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.233886
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.180059
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.173074
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.160927
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.23988
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.294348
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 5.213649
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.359539
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.168493
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.107338
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.111332
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 2.038142
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.169305
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.106474
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.370089
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.206611
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.098454
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.105982
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.529726
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.192167
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 2.977699
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /window\.matchMedia\('print'\)/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105674 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = { addEventListener() {}, addListener() {} };
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:264:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.291808
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.354909
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.564519
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 5.806824
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 107.824765
killed skip_print_matchmedia
== mutant reveal_fights_view_switch (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

......                                                              6 / 6 (100%)

Time: 00:00.008, Memory: 40.00 MB

OK (6 tests, 75 assertions)
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.393904
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.143242
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.162188
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.129939
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.095229
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.091773
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.089386
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.14992
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.182029
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 3.616327
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.096629
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.107664
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.053712
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.052159
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.491794
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.090015
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.049873
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.186874
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.119319
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.046177
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.039822
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.257666
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.0997
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 2.054365
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /applyRosterViewChrome\('list'\)/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105649 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		setRosterView('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:263:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.142592
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.22193
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.181697
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 3.957855
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 102.06127
killed reveal_fights_view_switch
== mutant skip_horizontal_reveal (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

..F...                                                              6 / 6 (100%)

Time: 00:00.015, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testRosterJsWindowsRowsAndKeepsKeyboardReach
Failed asserting that '(function () {\n
	'use strict';\n
\n
	const Api = window.DutyCheckApi;\n
	const Msg = window.DutyCheckMessaging;\n
	const C = window.DutyCheckComponents || window.DutyCheckDom || {};\n
	const D = window.DutyCheckDates;\n
	const ConflictLabels = window.DutyCheckConflictLabels;\n
	const create = C.createElement;\n
	if (typeof create !== 'function') {\n
		throw new Error('DutyCheck components failed to load');\n
	}\n
\n
	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n
\n
	const state = {\n
		periods: [],\n
		employees: [],\n
		locations: [],\n
		assignments: [],\n
		absenceBlocks: [],\n
		canCreateAssignments: false,\n
		lastRosterData: null,\n
		defaultBreakMinutes: 0,\n
		planningDefaultsFresh: false,\n
		templates: [],\n
		templatesLoaded: false,\n
		editingAssignmentId: null,\n
		editingAssignmentVersion: null,\n
		copyPreviewReady: false,\n
		preferencesByEmployee: {},\n
		blackoutsByEmployeeDate: {},\n
	};\n
	let assignmentModalInstance = null;\n
	let suggestedBreakMinutes = 0;\n
\n
	function clampBreakMinutes(value) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n)) {\n
			return 0;\n
		}\n
		return Math.max(0, Math.min(720, Math.round(n)));\n
	}\n
\n
	function readStoredBreakMinutes() {\n
		try {\n
			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n
			if (raw === null || raw === '') {\n
				return null;\n
			}\n
			return clampBreakMinutes(raw);\n
		} catch (_) {\n
			return null;\n
		}\n
	}\n
\n
	function rememberBreakMinutes(minutes) {\n
		try {\n
			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n
		} catch (_) {\n
			/* ignore quota / privacy mode */\n
		}\n
	}\n
\n
	function resolveBreakPrefill() {\n
		const org = clampBreakMinutes(state.defaultBreakMinutes);\n
		// Organisation default from Settings always wins when it is greater than zero.\n
		if (org > 0) {\n
			return { minutes: org, source: 'settings' };\n
		}\n
		const stored = readStoredBreakMinutes();\n
		if (stored !== null) {\n
			return { minutes: stored, source: 'last' };\n
		}\n
		return { minutes: 0, source: 'settings' };\n
	}\n
\n
	async function refreshPlanningDefaultFromServer() {\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n
			const minutes = Number(response?.planning?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
			state.planningDefaultsFresh = true;\n
		} catch (_) {\n
			/* Fall back to defaultBreakMinutes from the last roster API load. */\n
		}\n
	}\n
\n
	function breakPrefillMessage(prefill) {\n
		const n = String(prefill.minutes);\n
		if (prefill.source === 'last') {\n
			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n
		}\n
		if (prefill.minutes === 0) {\n
			return t('dutycheck', 'Pre-filled with 0 min — no break.');\n
		}\n
		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n
	}\n
\n
	function updateBreakPrefillHint(userEdited) {\n
		const el = document.getElementById('dc-assignment-break-prefill');\n
		if (!el) {\n
			return;\n
		}\n
		if (userEdited) {\n
			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n
			return;\n
		}\n
		el.textContent = breakPrefillMessage(resolveBreakPrefill());\n
	}\n
\n
	function applyDefaultBreakToForm() {\n
		const prefill = resolveBreakPrefill();\n
		suggestedBreakMinutes = prefill.minutes;\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(prefill.minutes);\n
		}\n
		updateBreakPrefillHint(false);\n
	}\n
\n
	function wireBreakPrefillHint() {\n
		const br = document.getElementById('dc-assignment-break');\n
		if (!br || br.dataset.dcBreakPrefillWired) {\n
			return;\n
		}\n
		br.dataset.dcBreakPrefillWired = '1';\n
		br.addEventListener('input', () => {\n
			const current = clampBreakMinutes(br.value);\n
			updateBreakPrefillHint(current !== suggestedBreakMinutes);\n
		});\n
		br.addEventListener('change', () => {\n
			const current = clampBreakMinutes(br.value);\n
			if (current === suggestedBreakMinutes) {\n
				updateBreakPrefillHint(false);\n
			}\n
		});\n
	}\n
\n
	function dateToDayIndex(isoDate) {\n
		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n
		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n
			return null;\n
		}\n
		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n
	}\n
\n
	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n
		const dayIndex = dateToDayIndex(dutyDate);\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime);\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime);\n
		if (dayIndex === null || start === null || end === null) {\n
			return null;\n
		}\n
		const absoluteStart = (dayIndex * 1440) + start;\n
		let absoluteEnd = (dayIndex * 1440) + end;\n
		if (absoluteEnd <= absoluteStart) {\n
			absoluteEnd += 1440;\n
		}\n
		return [absoluteStart, absoluteEnd];\n
	}\n
\n
	function absoluteRangesOverlap(a, b) {\n
		return a[0] < b[1] && b[0] < a[1];\n
	}\n
\n
	function isDateInAbsenceBlock(employeeId, dutyDate) {\n
		if (!dutyDate) {\n
			return false;\n
		}\n
		for (const block of state.absenceBlocks) {\n
			if (Number(block?.employeeId) !== Number(employeeId)) {\n
				continue;\n
			}\n
			const start = String(block?.startDate || '');\n
			const end = String(block?.endDate || '');\n
			if (dutyDate >= start && dutyDate <= end) {\n
				return true;\n
			}\n
		}\n
		return false;\n
	}\n
\n
	function employeesAvailableOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return [];\n
		}\n
		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n
	}\n
\n
	function countEmployeesBlockedOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return 0;\n
		}\n
		return state.employees.length - employeesAvailableOnDate(dutyDate).length;\n
	}\n
\n
	function appendHintLink(parent, beforeText, linkText, afterText, href) {\n
		parent.appendChild(document.createTextNode(beforeText));\n
		if (href) {\n
			const link = create('a', {\n
				class: 'dc-hint-link',\n
				href,\n
				text: linkText,\n
			});\n
			parent.appendChild(link);\n
			parent.appendChild(document.createTextNode(afterText));\n
		} else {\n
			parent.appendChild(document.createTextNode(linkText + afterText));\n
		}\n
	}\n
\n
	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n
		const hint = document.getElementById('dc-assignment-employee-hint');\n
		if (!hint) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		hint.replaceChildren();\n
		if (!dutyDate) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n
			));\n
			return;\n
		}\n
		if (!available.length && blocked > 0) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n
					.replace('{n}', String(blocked)),\n
				t('dutycheck', 'review absences'),\n
				'.',\n
				String(urls.absences || ''),\n
			);\n
			return;\n
		}\n
		if (!available.length) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'No employees are available. Add an active employee on the '),\n
				t('dutycheck', 'Employees'),\n
				t('dutycheck', ' page first.'),\n
				String(urls.employees || ''),\n
			);\n
			return;\n
		}\n
		if (blocked > 0) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n
					.replace('{available}', String(available.length))\n
					.replace('{hidden}', String(blocked)),\n
			));\n
			return;\n
		}\n
		hint.appendChild(document.createTextNode(\n
			t('dutycheck', 'All {n} active employees can be scheduled on this date.')\n
				.replace('{n}', String(available.length)),\n
		));\n
	}\n
\n
	function refreshEmployeeSelectForDate() {\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const dutyDate = dateInput ? String(dateInput.value || '') : '';\n
		const available = employeesAvailableOnDate(dutyDate);\n
		const blocked = countEmployeesBlockedOnDate(dutyDate);\n
		setEmployeeAvailabilityHint(dutyDate, available, blocked);\n
		fillSelect('dc-assignment-employee', available, {\n
			emptyText: dutyDate\n
				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n
				: t('dutycheck', 'Pick a date first'),\n
		});\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		if (employeeSelect) {\n
			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n
		}\n
	}\n
\n
	function findOverlapAssignment(payload) {\n
		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n
		if (!candidate) {\n
			return null;\n
		}\n
		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		for (const assignment of state.assignments) {\n
			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n
				continue;\n
			}\n
			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n
				continue;\n
			}\n
			const existing = assignmentAbsoluteRange(\n
				String(assignment?.dutyDate || ''),\n
				String(assignment?.startTime || ''),\n
				String(assignment?.endTime || ''),\n
			);\n
			if (existing && absoluteRangesOverlap(candidate, existing)) {\n
				return assignment;\n
			}\n
		}\n
		return null;\n
	}\n
\n
	function setFormFeedbackA11y(el, isError) {\n
		if (!el) {\n
			return;\n
		}\n
		if (isError) {\n
			el.setAttribute('role', 'alert');\n
			el.setAttribute('aria-live', 'assertive');\n
		} else {\n
			el.setAttribute('role', 'status');\n
			el.setAttribute('aria-live', 'polite');\n
		}\n
	}\n
\n
	function focusFirstInvalidAssignmentField(payload) {\n
		if (!payload) {\n
			return;\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		const order = [\n
			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n
			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n
			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n
			{\n
				test: () => period?.startDate && period?.endDate && payload.dutyDate\n
					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n
				id: 'dc-assignment-date',\n
			},\n
			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },\n
			{ test: () => !payload.locationId, id: 'dc-assignment-location' },\n
			{ test: () => !payload.startTime, id: 'dc-assignment-start' },\n
			{ test: () => !payload.endTime, id: 'dc-assignment-end' },\n
			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },\n
			{\n
				test: () => {\n
					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
					return effective === null || effective <= 0;\n
				},\n
				id: 'dc-assignment-end',\n
			},\n
		];\n
		for (const item of order) {\n
			if (item.test()) {\n
				document.getElementById(item.id)?.focus();\n
				return;\n
			}\n
		}\n
	}\n
\n
	function selectedPeriodForPayload(payload) {\n
		const periodId = Number(payload?.periodId);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function clearAssignmentFormSuccess() {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		if (!el || el.dataset.dcSuccess !== '1') {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.dataset.dcSuccess = '';\n
		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
		setFormFeedbackA11y(el, false);\n
	}\n
\n
	function showAssignmentFormSuccess(message, kind) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		const tone = kind === 'info' ? 'info' : 'success';\n
		el.hidden = false;\n
		el.textContent = String(message);\n
		el.dataset.dcSuccess = '1';\n
		el.classList.remove('dc-roster-form__feedback--error');\n
		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');\n
		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');\n
		setFormFeedbackA11y(el, false);\n
		if (submit) {\n
			submit.disabled = submit.dataset.dcLockedBySetup === '1';\n
		}\n
		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });\n
	}\n
\n
	function labelsForAssignmentPayload(payload) {\n
		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));\n
		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		const locationSelect = document.getElementById('dc-assignment-location');\n
		const employeeName = employee\n
			? String(employee.name || employee.displayName || '')\n
			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		const locationName = location\n
			? String(location.name || '')\n
			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		return { employeeName, locationName };\n
	}\n
\n
	function buildAssignmentSavedMessage(payload) {\n
		const { employeeName, locationName } = labelsForAssignmentPayload(payload);\n
		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;\n
		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)\n
			|| `${payload.startTime} – ${payload.endTime}`;\n
		return t(\n
			'dutycheck',\n
			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',\n
		)\n
			.replace('{employee}', employeeName || '—')\n
			.replace('{date}', String(dateLabel || ''))\n
			.replace('{times}', String(times))\n
			.replace('{location}', locationName || '—');\n
	}\n
\n
	function updateAssignmentFormFeedback(payload) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		if (el.dataset.dcSuccess === '1') {\n
			return;\n
		}\n
		if (typeof payload === 'string') {\n
			el.hidden = false;\n
			el.textContent = payload;\n
			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			el.classList.add('dc-roster-form__feedback--error');\n
			setFormFeedbackA11y(el, true);\n
			if (submit) {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		if (!payload) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			return;\n
		}\n
		let message = '';\n
		let isError = false;\n
		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');\n
			isError = true;\n
		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {\n
			const overlap = findOverlapAssignment(payload);\n
			if (overlap) {\n
				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;\n
				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)\n
					|| `${overlap.startTime} – ${overlap.endTime}`;\n
				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')\n
					.replace('{date}', String(when))\n
					.replace('{times}', String(times));\n
				isError = true;\n
			}\n
		}\n
		if (!message) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			if (submit && submit.dataset.dcLockedBySetup !== '1') {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		el.hidden = false;\n
		el.textContent = message;\n
		el.classList.toggle('dc-roster-form__feedback--error', isError);\n
		setFormFeedbackA11y(el, isError);\n
		if (submit && isError) {\n
			submit.disabled = true;\n
		}\n
	}\n
\n
	function refreshAssignmentFormEligibility() {\n
		refreshEmployeeSelectForDate();\n
		updateLocationTimezoneHint();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		updateAssignmentFormFeedback(readForm(form));\n
	}\n
\n
	function updateLocationTimezoneHint() {\n
		const hint = document.getElementById('dc-assignment-location-tz-hint');\n
		const select = document.getElementById('dc-assignment-location');\n
		if (!hint || !select) {\n
			return;\n
		}\n
		const locationId = Number(select.value);\n
		const location = state.locations.find((row) => Number(row.id) === locationId);\n
		const tz = location && String(location.timezone || '').trim();\n
		if (!tz) {\n
			hint.hidden = true;\n
			hint.replaceChildren();\n
			return;\n
		}\n
		const accountTz = D?.currentTimezone?.() || '';\n
		hint.textContent = t(\n
			'dutycheck',\n
			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',\n
		)\n
			.replace('{locationTimezone}', tz)\n
			.replace('{accountTimezone}', accountTz || 'UTC');\n
		hint.hidden = false;\n
	}\n
\n
	function fillSelect(id, items, options) {\n
		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});\n
		const select = document.getElementById(id);\n
		if (!select) return;\n
		const previous = String(select.value || '');\n
		select.replaceChildren();\n
		if (!items.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = opts.emptyText;\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const item of items) {\n
			const option = document.createElement('option');\n
			option.value = String(item.id);\n
			option.textContent = String(item[opts.labelKey] || item.name || item.id);\n
			select.appendChild(option);\n
		}\n
		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {\n
			select.value = previous;\n
		}\n
	}\n
\n
	function fillPeriodSwitcher(periods, selected) {\n
		const select = document.getElementById('dc-roster-period-switcher');\n
		if (!select) return;\n
		const statusLabelMap = {\n
			open: t('dutycheck', 'Open'),\n
			published: t('dutycheck', 'Published'),\n
			closed: t('dutycheck', 'Closed'),\n
		};\n
		select.replaceChildren();\n
		if (!periods.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = t('dutycheck', 'No periods yet');\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const period of periods) {\n
			const option = document.createElement('option');\n
			option.value = String(period.id);\n
			const start = D?.formatDisplayDate(period.startDate) || period.startDate;\n
			const end = D?.formatDisplayDate(period.endDate) || period.endDate;\n
			const statusKey = String(period.status || '').toLowerCase();\n
			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();\n
			option.textContent = `${start} – ${end} · ${status}`;\n
			select.appendChild(option);\n
		}\n
		if (selected) {\n
			select.value = String(selected);\n
		}\n
	}\n
\n
	function severityBadge(severity) {\n
		const sev = String(severity || 'info');\n
		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;\n
		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });\n
		badge.setAttribute('aria-label', label);\n
		return badge;\n
	}\n
\n
	function renderConflicts(conflicts) {\n
		const list = document.getElementById('dc-conflict-list');\n
		const summary = document.getElementById('dc-conflict-summary');\n
		if (!list) return;\n
		list.setAttribute('role', 'list');\n
		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));\n
		list.replaceChildren();\n
		const hard = conflicts.filter((c) => c?.severity === 'hard').length;\n
		const soft = conflicts.filter((c) => c?.severity === 'soft').length;\n
		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;\n
		if (summary) {\n
			summary.textContent = !conflicts.length\n
				? t('dutycheck', 'No planning issues found.')\n
				: (ConflictLabels\n
					? ConflictLabels.countsSummary(hard, soft, softUnack)\n
					: String(hard));\n
		}\n
		if (!conflicts.length) {\n
			return;\n
		}\n
		for (const conflict of conflicts) {\n
			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });\n
			li.appendChild(severityBadge(conflict?.severity));\n
			const conflictMessage = String(conflict?.message || '');\n
			const titleText = conflictMessage\n
				? translateConflictMessage(conflictMessage)\n
				: t('dutycheck', 'Unknown conflict');\n
			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;\n
			const body = create('div', { class: 'dc-conflict__body' });\n
			const title = create('span', { class: 'dc-conflict__title', text: titleText });\n
			title.id = titleId;\n
			body.appendChild(title);\n
			li.setAttribute('aria-labelledby', titleId);\n
			if (conflict?.acknowledged && conflict?.ackReason) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack',\n
					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),\n
				}));\n
			} else if (conflict?.ackInvalidated) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack-invalid',\n
					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),\n
				}));\n
			}\n
			li.appendChild(body);\n
\n
			const actions = create('div', { class: 'dc-conflict__actions' });\n
			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {\n
				const ackBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Confirm'),\n
				});\n
				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));\n
				actions.appendChild(ackBtn);\n
			}\n
			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();\n
			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {\n
				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()\n
					|| t('dutycheck', 'Open ArbeitszeitCheck');\n
				const recoveryLink = create('a', {\n
					class: 'button primary',\n
					href: recoveryUrl,\n
					text: recoveryLabel,\n
					attrs: {\n
						target: '_blank',\n
						rel: 'noopener noreferrer',\n
						'aria-label': recoveryLabel,\n
					},\n
				});\n
				actions.appendChild(recoveryLink);\n
			}\n
			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];\n
			if (conflict?.type === 'absence_collision' && assignmentIds.length) {\n
				const focusBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Show assignment'),\n
				});\n
				focusBtn.addEventListener('click', () => {\n
					revealAssignmentRow(Number(assignmentIds[0]));\n
				});\n
				actions.appendChild(focusBtn);\n
			}\n
			li.appendChild(actions);\n
			list.appendChild(li);\n
		}\n
	}\n
\n
	async function acknowledgeConflict(conflictId) {\n
		const reason = await C.promptReason({\n
			title: t('dutycheck', 'Confirm this exception'),\n
			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),\n
			confirmLabel: t('dutycheck', 'Confirm'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			minLength: 10,\n
		});\n
		if (reason === null) return;\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });\n
			render(response?.data || {});\n
			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');\n
			Msg.announce(ackMsg, 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	const VIRTUAL_FALLBACK = {\n
		DEFAULT_ROW_HEIGHT: 44,\n
		DEFAULT_OVERSCAN: 6,\n
		UNSIZED_WINDOW_ROWS: 24,\n
		visibleRange(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			const rowHeight = 44;\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: total * rowHeight,\n
				rowHeight,\n
			};\n
		},\n
		scrollTopToRevealIndex() {\n
			return 0;\n
		},\n
		pageStride() {\n
			return 1;\n
		},\n
		windowCaption(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			if (total <= 0) {\n
				return { mode: 'empty', from: 0, to: 0, total: 0 };\n
			}\n
			return { mode: 'all', from: 1, to: total, total };\n
		},\n
	};\n
\n
	function virtualApi() {\n
		const api = window.DutyCheckVirtualWindow;\n
		if (api\n
			&& typeof api.visibleRange === 'function'\n
			&& typeof api.scrollTopToRevealIndex === 'function'\n
			&& typeof api.windowCaption === 'function') {\n
			return api;\n
		}\n
		return VIRTUAL_FALLBACK;\n
	}\n
\n
	function cellSelectionKey(employeeId, dutyDate) {\n
		return `${Number(employeeId)}|${String(dutyDate)}`;\n
	}\n
\n
	function prefersReducedMotion() {\n
		try {\n
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n
		} catch (_) {\n
			return false;\n
		}\n
	}\n
\n
	function measureHeight(el) {\n
		if (!(el instanceof HTMLElement)) {\n
			return 0;\n
		}\n
		const h = el.getBoundingClientRect().height;\n
		return Number.isFinite(h) && h > 0 ? h : 0;\n
	}\n
\n
	function assignmentIndex(assignments) {\n
		const byKey = new Map();\n
		for (const a of assignments || []) {\n
			const key = cellSelectionKey(a.employeeId, a.dutyDate);\n
			if (!byKey.has(key)) {\n
				byKey.set(key, []);\n
			}\n
			byKey.get(key).push(a);\n
		}\n
		return byKey;\n
	}\n
\n
	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {\n
		const emptyPanel = document.getElementById('dc-roster-assignments-empty');\n
		if (emptyPanel) {\n
			emptyPanel.hidden = !showEmptyPanel;\n
		}\n
	}\n
\n
	const gridState = {\n
		view: 'grid',\n
		selected: new Set(), // `${employeeId}|${dutyDate}`\n
		focusRow: 0,\n
		focusCol: 0,\n
		paintAll: false,\n
		rowHeight: 44,\n
		headerHeight: 44,\n
		windowStart: -1,\n
		windowEnd: -1,\n
		paintedEmployeeCount: -1,\n
		paintedDateCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	const listState = {\n
		windowStart: -1,\n
		windowEnd: -1,\n
		rowHeight: 48,\n
		paintedCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	function renderAssignments(assignments) {\n
		paintListWindow({ assignments: assignments || [], force: true });\n
		renderRosterGrid(assignments || []);\n
		updateAddAssignmentControl();\n
	}\n
\n
	function buildAssignmentRow(a, periodOpen) {\n
		const overnightHintText = t('dutycheck', 'Continues into the next day.');\n
		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));\n
		const tr = create('tr');\n
		if (a?.id != null) {\n
			tr.dataset.assignmentId = String(a.id);\n
			tr.setAttribute('tabindex', '-1');\n
		}\n
		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;\n
		const tdDate = create('td', { text: date });\n
		tdDate.dataset.cell = t('dutycheck', 'Date');\n
		tr.appendChild(tdDate);\n
\n
		const tdStart = create('td');\n
		tdStart.dataset.cell = t('dutycheck', 'Start');\n
		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));\n
		tr.appendChild(tdStart);\n
\n
		const tdEnd = create('td');\n
		tdEnd.dataset.cell = t('dutycheck', 'End');\n
		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));\n
		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {\n
			tdEnd.title = overnightHintText;\n
			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));\n
		}\n
		tr.appendChild(tdEnd);\n
\n
		const rest = [\n
			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },\n
			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },\n
			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },\n
			{ label: t('dutycheck', 'Note'), value: a.note || '' },\n
		];\n
		for (const cell of rest) {\n
			const td = create('td', { text: String(cell.value ?? '') });\n
			td.dataset.cell = cell.label;\n
			tr.appendChild(td);\n
		}\n
		const tdActions = create('td');\n
		tdActions.dataset.cell = t('dutycheck', 'Actions');\n
		tdActions.className = 'dc-table__actions';\n
		if (periodOpen && a?.id) {\n
			const editBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Edit'),\n
			});\n
			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));\n
			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));\n
			const cancelBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Cancel shift'),\n
			});\n
			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));\n
			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));\n
			tdActions.appendChild(editBtn);\n
			tdActions.appendChild(cancelBtn);\n
		} else {\n
			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));\n
		}\n
		tr.appendChild(tdActions);\n
		return tr;\n
	}\n
\n
	function createListSpacer(px) {\n
		const tr = create('tr', { class: 'dc-virtual-spacer' });\n
		tr.setAttribute('aria-hidden', 'true');\n
		tr.style.height = `${Math.max(0, px)}px`;\n
		const td = create('td');\n
		td.colSpan = 8;\n
		td.style.height = `${Math.max(0, px)}px`;\n
		td.style.padding = '0';\n
		td.style.border = '0';\n
		td.style.lineHeight = '0';\n
		tr.appendChild(td);\n
		return tr;\n
	}\n
\n
	function updateListWindowStatus(range, total) {\n
		const el = document.getElementById('dc-roster-list-status');\n
		if (!el) {\n
			return;\n
		}\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		if (cap.mode === 'all') {\n
			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));\n
			return;\n
		}\n
		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')\n
			.replace('{from}', String(cap.from))\n
			.replace('{to}', String(cap.to))\n
			.replace('{total}', String(cap.total));\n
	}\n
\n
	function paintListWindow(options) {\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		if (!tbody) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		if (!assignments.length) {\n
			tbody.replaceChildren();\n
			setAssignmentsEmptyPanelVisible(true);\n
			listState.windowStart = 0;\n
			listState.windowEnd = 0;\n
			listState.paintedCount = 0;\n
			updateListWindowStatus({ start: 0, end: 0 }, 0);\n
			return;\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const scroller = document.getElementById('dc-assignments-table-wrap');\n
		const VW = virtualApi();\n
		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: assignments.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& listState.windowStart === range.start\n
			&& listState.windowEnd === range.end\n
			&& listState.paintedCount === assignments.length) {\n
			updateListWindowStatus(range, assignments.length);\n
			return;\n
		}\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const periodOpen = canAddAssignment();\n
		const frag = document.createDocumentFragment();\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createListSpacer(range.padBefore));\n
		}\n
		const windowRows = assignments.slice(range.start, range.end);\n
		for (const a of windowRows) {\n
			frag.appendChild(buildAssignmentRow(a, periodOpen));\n
		}\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createListSpacer(range.padAfter));\n
		}\n
		tbody.replaceChildren(frag);\n
		listState.windowStart = range.start;\n
		listState.windowEnd = range.end;\n
		listState.paintedCount = assignments.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
		}\n
		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');\n
		const measured = measureHeight(sample);\n
		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {\n
			listState.rowHeight = measured;\n
			listState.remeasuring = true;\n
			paintListWindow({ assignments, force: true });\n
			listState.remeasuring = false;\n
			return;\n
		}\n
		if (measured > 0) {\n
			listState.rowHeight = measured;\n
		}\n
		updateListWindowStatus(range, assignments.length);\n
	}\n
\n
	function selectedPeriodId() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const fromSwitcher = Number(switcher?.value || 0);\n
		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {\n
			return fromSwitcher;\n
		}\n
		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;\n
	}\n
\n
	function periodDateList() {\n
		const periodId = selectedPeriodId();\n
		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));\n
		if (!period?.startDate || !period?.endDate) {\n
			return [];\n
		}\n
		const out = [];\n
		const cursor = new Date(`${period.startDate}T00:00:00Z`);\n
		const end = new Date(`${period.endDate}T00:00:00Z`);\n
		while (cursor <= end) {\n
			out.push(cursor.toISOString().slice(0, 10));\n
			cursor.setUTCDate(cursor.getUTCDate() + 1);\n
			if (out.length > 62) {\n
				break;\n
			}\n
		}\n
		return out;\n
	}\n
\n
	function updateGridWindowStatus(range, total, dayCount) {\n
		const el = document.getElementById('dc-roster-grid-status');\n
		if (!el) {\n
			return;\n
		}\n
		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		let text = '';\n
		if (cap.mode === 'all') {\n
			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));\n
		} else {\n
			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')\n
				.replace('{from}', String(cap.from))\n
				.replace('{to}', String(cap.to))\n
				.replace('{total}', String(cap.total));\n
		}\n
		if (days > 10) {\n
			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');\n
		}\n
		el.textContent = text;\n
	}\n
\n
	function createGridSpacer(px) {\n
		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });\n
		el.setAttribute('aria-hidden', 'true');\n
		el.setAttribute('role', 'presentation');\n
		el.style.height = `${Math.max(0, px)}px`;\n
		return el;\n
	}\n
\n
	function setGridEmptyState(root, message) {\n
		root.removeAttribute('aria-rowcount');\n
		root.removeAttribute('aria-colcount');\n
		root.removeAttribute('tabindex');\n
		root.style.removeProperty('--dc-roster-day-count');\n
		root.style.removeProperty('--dc-roster-day-min');\n
		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');\n
		root.setAttribute('role', 'status');\n
		root.setAttribute('aria-live', 'polite');\n
		root.replaceChildren(create('p', {\n
			class: 'dc-roster-empty-state__text',\n
			text: message,\n
		}));\n
	}\n
\n
	function prepareGridForRows(root, employees, dates) {\n
		root.setAttribute('role', 'grid');\n
		root.removeAttribute('tabindex');\n
		root.removeAttribute('aria-live');\n
		root.setAttribute('aria-rowcount', String(employees.length + 1));\n
		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));\n
		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));\n
		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));\n
		root.style.setProperty('--dc-roster-day-min', dayMin);\n
		root.classList.toggle('dc-roster-grid--long', dates.length > 10);\n
		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);\n
	}\n
\n
	function buildColHead(day, colIdx, dayMeta) {\n
		const parts = dayMeta || D?.formatRosterColumnParts?.(day);\n
		const full = parts?.full || D?.formatDisplayDate?.(day) || day;\n
		let ariaLabel = parts?.ariaLabel || full;\n
		if (parts?.isToday) {\n
			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;\n
		}\n
		const weekdayShort = parts?.weekdayShort || '';\n
		const dayNum = parts?.day || '';\n
		const classNames = ['dc-roster-grid__colhead'];\n
		if (parts?.isWeekend) {\n
			classNames.push('dc-roster-grid__colhead--weekend');\n
		}\n
		if (parts?.isToday) {\n
			classNames.push('dc-roster-grid__colhead--today');\n
		}\n
		const children = [];\n
		if (weekdayShort) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));\n
		}\n
		if (dayNum) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));\n
		}\n
		const attrs = {\n
			'data-col': String(colIdx),\n
			'aria-colindex': String(colIdx + 2),\n
			title: full,\n
			'aria-label': ariaLabel,\n
		};\n
		if (parts?.iso) {\n
			attrs['data-duty-date'] = parts.iso;\n
		}\n
		if (!children.length) {\n
			return create('div', {\n
				class: classNames.join(' '),\n
				role: 'columnheader',\n
				text: full,\n
				attrs,\n
			});\n
		}\n
		return create('div', {\n
			class: classNames.join(' '),\n
			role: 'columnheader',\n
			attrs,\n
		}, children);\n
	}\n
\n
	function buildDutyDayMeta(dates) {\n
		const map = new Map();\n
		(dates || []).forEach((day) => {\n
			map.set(day, D?.formatRosterColumnParts?.(day) || null);\n
		});\n
		return map;\n
	}\n
\n
	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {\n
		const row = create('div', {\n
			class: 'dc-roster-grid__row',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': String(rowIdx + 2) },\n
		});\n
		const prefs = state.preferencesByEmployee[String(emp.id)] || [];\n
		const prefChips = prefs.slice(0, 2).map((p) => {\n
			const band = String(p.band || '');\n
			const label = band === 'early'\n
				? t('dutycheck', 'Früh')\n
				: (band === 'late' ? t('dutycheck', 'Spät') : band);\n
			return create('span', {\n
				class: 'dc-pref-chip dc-pref-chip--static',\n
				text: label,\n
				attrs: { title: label },\n
			});\n
		});\n
		const headKids = [\n
			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),\n
		];\n
		if (prefChips.length) {\n
			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));\n
		}\n
		row.appendChild(create('div', {\n
			class: 'dc-roster-grid__rowhead',\n
			role: 'rowheader',\n
			attrs: { 'aria-colindex': '1' },\n
		}, headKids));\n
		dates.forEach((day, colIdx) => {\n
			const key = cellSelectionKey(emp.id, day);\n
			const cellAssignments = byKey.get(key) || [];\n
			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);\n
			const cell = create('div', {\n
				class: 'dc-roster-grid__cell',\n
				role: 'gridcell',\n
				attrs: {\n
					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',\n
					'data-employee-id': String(emp.id),\n
					'data-duty-date': day,\n
					'data-row': String(rowIdx),\n
					'data-col': String(colIdx),\n
					'aria-colindex': String(colIdx + 2),\n
					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',\n
				},\n
			});\n
			if (dayParts?.isWeekend) {\n
				cell.classList.add('dc-roster-grid__cell--weekend');\n
			}\n
			if (dayParts?.isToday) {\n
				cell.classList.add('dc-roster-grid__cell--today');\n
			}\n
			if (cellAssignments.length) {\n
				cell.classList.add('dc-roster-grid__cell--filled');\n
				const first = cellAssignments[0];\n
				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;\n
				cell.appendChild(create('span', {\n
					class: 'dc-roster-grid__shift',\n
					text: label,\n
				}));\n
				const editLabel = canAddAssignment()\n
					? t('dutycheck', 'Edit assignment')\n
					: t('dutycheck', 'View assignment (read-only)');\n
				cell.setAttribute('aria-label', `${editLabel}: ${label}`);\n
				if (cellAssignments.length > 1) {\n
					cell.appendChild(create('span', {\n
						class: 'dc-roster-grid__more',\n
						text: `+${cellAssignments.length - 1}`,\n
					}));\n
				}\n
			} else {\n
				cell.classList.add('dc-roster-grid__cell--empty');\n
				const blackoutKey = `${emp.id}|${day}`;\n
				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];\n
				if (hasBlackout) {\n
					cell.classList.add('dc-roster-grid__cell--blackout');\n
					cell.appendChild(create('span', {\n
						class: 'dc-blackout-mark',\n
						attrs: { title: t('dutycheck', 'Kann nicht') },\n
					}, [\n
						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),\n
						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),\n
					]));\n
					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));\n
				} else {\n
					cell.setAttribute(\n
						'aria-label',\n
						canAddAssignment()\n
							? t('dutycheck', 'Empty cell — Space to select for bulk fill')\n
							: t('dutycheck', 'Empty cell'),\n
					);\n
				}\n
			}\n
			if (gridState.selected.has(key)) {\n
				cell.classList.add('is-selected');\n
			}\n
			row.appendChild(cell);\n
		});\n
		return row;\n
	}\n
\n
	function syncVisibleGridChrome() {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {\n
			const empId = cell.getAttribute('data-employee-id');\n
			const day = cell.getAttribute('data-duty-date');\n
			const key = cellSelectionKey(empId, day);\n
			const selected = gridState.selected.has(key);\n
			cell.classList.toggle('is-selected', selected);\n
			cell.setAttribute('aria-selected', selected ? 'true' : 'false');\n
			const rowIdx = Number(cell.getAttribute('data-row'));\n
			const colIdx = Number(cell.getAttribute('data-col'));\n
			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;\n
			cell.setAttribute('tabindex', isFocus ? '0' : '-1');\n
		});\n
	}\n
\n
	function focusGridCellIfVisible(root) {\n
		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
		if (node instanceof HTMLElement) {\n
			node.scrollIntoView({\n
				block: 'nearest',\n
				inline: 'start',\n
				behavior: 'auto',\n
			});\n
			node.focus();\n
		}\n
	}\n
\n
	function activateGridCell(cell) {\n
		if (!(cell instanceof HTMLElement)) {\n
			return;\n
		}\n
		const rowIdx = Number(cell.getAttribute('data-row'));\n
		const colIdx = Number(cell.getAttribute('data-col'));\n
		if (Number.isInteger(rowIdx) && rowIdx >= 0) {\n
			gridState.focusRow = rowIdx;\n
		}\n
		if (Number.isInteger(colIdx) && colIdx >= 0) {\n
			gridState.focusCol = colIdx;\n
		}\n
		const empId = cell.getAttribute('data-employee-id');\n
		const day = cell.getAttribute('data-duty-date');\n
		const key = cellSelectionKey(empId, day);\n
		const filled = cell.classList.contains('dc-roster-grid__cell--filled');\n
		if (!filled) {\n
			if (canAddAssignment()) {\n
				toggleGridSelection(key);\n
				syncVisibleGridChrome();\n
				updateBulkBar();\n
			}\n
			return;\n
		}\n
		const byKey = assignmentIndex(state.assignments || []);\n
		const cellAssignments = byKey.get(key) || [];\n
		if (canAddAssignment() && cellAssignments[0]) {\n
			openAssignmentEditModal(cellAssignments[0], cell);\n
			return;\n
		}\n
		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');\n
	}\n
\n
	function ensureGridFocusVisible() {\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const employees = state.employees || [];\n
		if (!scroller || !employees.length) {\n
			return;\n
		}\n
		const VW = virtualApi();\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);\n
		const next = VW.scrollTopToRevealIndex({\n
			index: gridState.focusRow,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop: scroller.scrollTop,\n
			total: employees.length,\n
		});\n
		if (Math.abs(next - scroller.scrollTop) > 0.5) {\n
			scroller.scrollTop = next;\n
		}\n
	}\n
\n
	function renderRosterGrid(assignments) {\n
		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });\n
	}\n
\n
	function paintGridWindow(options) {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		const employees = state.employees || [];\n
		const dates = periodDateList();\n
		const byKey = assignmentIndex(assignments);\n
\n
		if (!employees.length || !dates.length) {\n
			setGridEmptyState(\n
				root,\n
				t('dutycheck', 'Add employees and an open period to use the planning grid.'),\n
			);\n
			gridState.windowStart = 0;\n
			gridState.windowEnd = 0;\n
			gridState.paintedEmployeeCount = employees.length;\n
			gridState.paintedDateCount = dates.length;\n
			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		prepareGridForRows(root, employees, dates);\n
		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));\n
		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));\n
\n
		const VW = virtualApi();\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = scroller && gridState.view === 'grid'\n
			? Math.max(0, scroller.clientHeight - headerHeight)\n
			: 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: employees.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& gridState.windowStart === range.start\n
			&& gridState.windowEnd === range.end\n
			&& gridState.paintedEmployeeCount === employees.length\n
			&& gridState.paintedDateCount === dates.length) {\n
			syncVisibleGridChrome();\n
			updateGridWindowStatus(range, employees.length, dates.length);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const savedLeft = scroller ? scroller.scrollLeft : 0;\n
		const dayMetaByDate = buildDutyDayMeta(dates);\n
		const frag = document.createDocumentFragment();\n
		const header = create('div', {\n
			class: 'dc-roster-grid__row dc-roster-grid__row--head',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': '1' },\n
		});\n
		header.appendChild(create('div', {\n
			class: 'dc-roster-grid__corner',\n
			role: 'columnheader',\n
			text: t('dutycheck', 'Person'),\n
			attrs: { 'aria-colindex': '1' },\n
		}));\n
		dates.forEach((day, colIdx) => {\n
			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));\n
		});\n
		frag.appendChild(header);\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createGridSpacer(range.padBefore));\n
		}\n
		const windowEmployees = employees.slice(range.start, range.end);\n
		windowEmployees.forEach((emp, offset) => {\n
			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));\n
		});\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createGridSpacer(range.padAfter));\n
		}\n
		root.replaceChildren(frag);\n
		gridState.windowStart = range.start;\n
		gridState.windowEnd = range.end;\n
		gridState.paintedEmployeeCount = employees.length;\n
		gridState.paintedDateCount = dates.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
			scroller.scrollLeft = savedLeft;\n
		}\n
		const headerEl = root.querySelector('.dc-roster-grid__row--head');\n
		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');\n
		const measuredHeader = measureHeight(headerEl);\n
		const measuredRow = measureHeight(bodyRow);\n
		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)\n
			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))\n
			&& !gridState.remeasuring) {\n
			if (measuredHeader > 0) {\n
				gridState.headerHeight = measuredHeader;\n
			}\n
			if (measuredRow > 0) {\n
				gridState.rowHeight = measuredRow;\n
			}\n
			gridState.remeasuring = true;\n
			paintGridWindow({ assignments, force: true });\n
			gridState.remeasuring = false;\n
			return;\n
		}\n
		if (measuredHeader > 0) {\n
			gridState.headerHeight = measuredHeader;\n
		}\n
		if (measuredRow > 0) {\n
			gridState.rowHeight = measuredRow;\n
		}\n
		bindGridInteractions(root);\n
		updateGridWindowStatus(range, employees.length, dates.length);\n
		updateBulkBar();\n
	}\n
\n
	function bindGridInteractions(root) {\n
		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {\n
			return;\n
		}\n
		root.dataset.dcGridBound = '1';\n
		root.addEventListener('click', (ev) => {\n
			const cell = ev.target.closest('[role="gridcell"]');\n
			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {\n
				return;\n
			}\n
			activateGridCell(cell);\n
		});\n
		root.addEventListener('keydown', (ev) => {\n
			const employees = state.employees || [];\n
			const dates = periodDateList();\n
			const rowCount = employees.length;\n
			const colCount = dates.length;\n
			if (!rowCount || !colCount) {\n
				return;\n
			}\n
			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];\n
			if (!gridKeys.includes(ev.key)) {\n
				return;\n
			}\n
			ev.preventDefault();\n
			if (ev.key === 'ArrowUp') {\n
				gridState.focusRow = Math.max(0, gridState.focusRow - 1);\n
			} else if (ev.key === 'ArrowDown') {\n
				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);\n
			} else if (ev.key === 'ArrowLeft') {\n
				gridState.focusCol = Math.max(0, gridState.focusCol - 1);\n
			} else if (ev.key === 'ArrowRight') {\n
				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);\n
			} else if (ev.key === 'Home') {\n
				gridState.focusCol = 0;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = 0;\n
				}\n
			} else if (ev.key === 'End') {\n
				gridState.focusCol = colCount - 1;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = rowCount - 1;\n
				}\n
			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {\n
				const VW = virtualApi();\n
				const scroller = document.getElementById('dc-roster-grid-scroller');\n
				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
				const viewportHeight = scroller\n
					? Math.max(rowHeight, scroller.clientHeight - headerHeight)\n
					: rowHeight;\n
				const stride = typeof VW.pageStride === 'function'\n
					? VW.pageStride({ rowHeight, viewportHeight })\n
					: 1;\n
				if (ev.key === 'PageDown') {\n
					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);\n
				} else {\n
					gridState.focusRow = Math.max(0, gridState.focusRow - stride);\n
				}\n
			} else if (ev.key === 'Enter') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				activateGridCell(cell);\n
				focusGridCellIfVisible(root);\n
				return;\n
			} else if (ev.key === ' ' || ev.key === 'Spacebar') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				const empId = cell?.getAttribute('data-employee-id');\n
				const day = cell?.getAttribute('data-duty-date');\n
				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');\n
				if (empId && day && isEmpty && canAddAssignment()) {\n
					toggleGridSelection(cellSelectionKey(empId, day));\n
					syncVisibleGridChrome();\n
					updateBulkBar();\n
				}\n
				focusGridCellIfVisible(root);\n
				return;\n
			}\n
			ensureGridFocusVisible();\n
			paintGridWindow({ force: false });\n
			focusGridCellIfVisible(root);\n
		});\n
	}\n
\n
	function toggleGridSelection(key) {\n
		if (gridState.selected.has(key)) {\n
			gridState.selected.delete(key);\n
		} else {\n
			gridState.selected.add(key);\n
		}\n
	}\n
\n
	function updateBulkBar() {\n
		const bar = document.getElementById('dc-roster-bulk-bar');\n
		const countEl = document.getElementById('dc-roster-bulk-count');\n
		if (!bar || !countEl) {\n
			return;\n
		}\n
		const n = gridState.selected.size;\n
		if (n < 1 || !canAddAssignment()) {\n
			bar.hidden = true;\n
			return;\n
		}\n
		bar.hidden = false;\n
		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (sel && !(sel.options && sel.options.length)) {\n
			populateBulkTemplates();\n
		}\n
	}\n
\n
	async function populateBulkTemplates() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (!sel) {\n
			return;\n
		}\n
		await loadTemplates();\n
		sel.replaceChildren();\n
		const templates = state.templates || [];\n
		if (!templates.length) {\n
			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));\n
			return;\n
		}\n
		for (const tpl of templates) {\n
			sel.appendChild(create('option', {\n
				value: String(tpl.id),\n
				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,\n
			}));\n
		}\n
	}\n
\n
	function revealAssignmentRow(id) {\n
		const assignmentId = Number(id);\n
		const assignments = state.assignments || [];\n
		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);\n
		if (idx < 0) {\n
			return;\n
		}\n
		applyRosterViewChrome('list');\n
		const run = () => {\n
			const scroller = document.getElementById('dc-assignments-table-wrap');\n
			const VW = virtualApi();\n
			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
			if (scroller) {\n
				scroller.scrollTop = VW.scrollTopToRevealIndex({\n
					index: idx,\n
					rowHeight,\n
					viewportHeight: Math.max(rowHeight, scroller.clientHeight),\n
					scrollTop: scroller.scrollTop,\n
					total: assignments.length,\n
				});\n
			}\n
			paintListWindow({ assignments, force: true });\n
			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);\n
			if (row instanceof HTMLElement) {\n
				row.scrollIntoView({\n
					block: 'nearest',\n
					behavior: 'auto',\n
				});\n
				row.focus();\n
				row.classList.add('dc-row--flash');\n
				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);\n
			}\n
		};\n
		window.requestAnimationFrame(run);\n
	}\n
\n
	function setRosterPaintAll(paintAll) {\n
		gridState.paintAll = paintAll === true;\n
		paintListWindow({ force: true });\n
		paintGridWindow({ force: true });\n
	}\n
\n
	function wireRosterVirtualization() {\n
		const gridScroller = document.getElementById('dc-roster-grid-scroller');\n
		const listScroller = document.getElementById('dc-assignments-table-wrap');\n
		let gridRaf = 0;\n
		let listRaf = 0;\n
		if (gridScroller) {\n
			gridScroller.addEventListener('scroll', () => {\n
				if (gridRaf) {\n
					return;\n
				}\n
				gridRaf = window.requestAnimationFrame(() => {\n
					gridRaf = 0;\n
					paintGridWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		if (listScroller) {\n
			listScroller.addEventListener('scroll', () => {\n
				if (listRaf) {\n
					return;\n
				}\n
				listRaf = window.requestAnimationFrame(() => {\n
					listRaf = 0;\n
					paintListWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		const resize = () => {\n
			if (gridState.view === 'grid') {\n
				paintGridWindow({ force: true });\n
			} else {\n
				paintListWindow({ force: true });\n
			}\n
		};\n
		if (typeof ResizeObserver === 'function') {\n
			const ro = new ResizeObserver(resize);\n
			if (gridScroller) {\n
				ro.observe(gridScroller);\n
			}\n
			if (listScroller) {\n
				ro.observe(listScroller);\n
			}\n
		} else {\n
			window.addEventListener('resize', resize);\n
		}\n
		window.addEventListener('beforeprint', () => setRosterPaintAll(true));\n
		window.addEventListener('afterprint', () => setRosterPaintAll(false));\n
		if (typeof window.matchMedia === 'function') {\n
			try {\n
				const printMq = window.matchMedia('print');\n
				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);\n
				if (typeof printMq.addEventListener === 'function') {\n
					printMq.addEventListener('change', onPrintMq);\n
				} else if (typeof printMq.addListener === 'function') {\n
					printMq.addListener(onPrintMq);\n
				}\n
			} catch (_) {\n
				/* print media query is not available in every test/jsdom shim */\n
			}\n
		}\n
	}\n
\n
	function applyRosterViewChrome(view) {\n
		gridState.view = view === 'list' ? 'list' : 'grid';\n
		const gridWrap = document.getElementById('dc-roster-grid-wrap');\n
		const listPanel = document.getElementById('dc-roster-list-panel');\n
		const gridBtn = document.getElementById('dc-roster-view-grid');\n
		const listBtn = document.getElementById('dc-roster-view-list');\n
		if (gridWrap) {\n
			gridWrap.hidden = gridState.view !== 'grid';\n
		}\n
		if (listPanel) {\n
			listPanel.hidden = gridState.view !== 'list';\n
		}\n
		if (gridBtn) {\n
			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');\n
		}\n
		if (listBtn) {\n
			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');\n
		}\n
	}\n
\n
	function setRosterView(view) {\n
		applyRosterViewChrome(view);\n
		window.requestAnimationFrame(() => {\n
			if (gridState.view === 'list') {\n
				paintListWindow({ force: true });\n
			} else {\n
				paintGridWindow({ force: true });\n
			}\n
		});\n
	}\n
\n
	async function applyBulkFromTemplate() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		const tplId = Number(sel?.value || 0);\n
		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);\n
		if (!tpl || gridState.selected.size < 1) {\n
			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');\n
			return;\n
		}\n
		if (!Number(tpl.locationId || 0)) {\n
			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');\n
			return;\n
		}\n
		const periodId = Number(selectedPeriodId() || 0);\n
		let ok = 0;\n
		let fail = 0;\n
		for (const key of Array.from(gridState.selected)) {\n
			const sep = key.indexOf('|');\n
			if (sep < 1) {\n
				fail += 1;\n
				continue;\n
			}\n
			const employeeId = Number(key.slice(0, sep));\n
			const dutyDate = key.slice(sep + 1);\n
			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {\n
				fail += 1;\n
				continue;\n
			}\n
			const locationId = Number(tpl.locationId || 0);\n
			if (!locationId) {\n
				fail += 1;\n
				continue;\n
			}\n
			try {\n
				await submitAssignment({\n
					periodId,\n
					employeeId: Number(employeeId),\n
					locationId,\n
					dutyDate,\n
					startTime: String(tpl.startTime).slice(0, 5),\n
					endTime: String(tpl.endTime).slice(0, 5),\n
					breakMinutes: Number(tpl.breakMinutes || 0),\n
					note: '',\n
					assignmentId: 0,\n
				}, true);\n
				ok += 1;\n
			} catch {\n
				fail += 1;\n
			}\n
		}\n
		gridState.selected.clear();\n
		await loadRoster(periodId || null);\n
		Msg.announce(\n
			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')\n
				.replace('{ok}', String(ok))\n
				.replace('{fail}', String(fail)),\n
			fail ? 'warning' : 'info',\n
		);\n
	}\n
\n
	function uniqueConflictTypes(conflicts) {\n
		const types = new Set();\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity === 'soft' && conflict?.type) {\n
				types.add(String(conflict.type));\n
			}\n
		}\n
		return Array.from(types);\n
	}\n
\n
	function softConflictSummary(conflicts) {\n
		const lines = [];\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity !== 'soft') {\n
				continue;\n
			}\n
			const raw = String(conflict?.message || '');\n
			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');\n
			if (text && !lines.includes(text)) {\n
				lines.push(text);\n
			}\n
		}\n
		return lines.join('\n');\n
	}\n
\n
	function errorCodeFrom(err) {\n
		return String(err?.code || err?.payload?.error?.code || '');\n
	}\n
\n
	function syncAssignmentPeriodFromSwitcher() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (!switcher || !periodHidden) {\n
			return;\n
		}\n
		const selected = Number(switcher.value);\n
		if (Number.isInteger(selected) && selected > 0) {\n
			periodHidden.value = String(selected);\n
		}\n
	}\n
\n
	function announceAssignmentSaveError(err) {\n
		clearAssignmentFormSuccess();\n
		const friendly = errorMessageFor(err);\n
		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');\n
		updateAssignmentFormFeedback(message);\n
		Msg.announce(message, 'error');\n
	}\n
\n
	async function submitAssignment(payload, retryWithAck = true) {\n
		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		try {\n
			if (editId > 0) {\n
				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);\n
			}\n
			return await Api.post('/apps/dutycheck/api/assignments', payload);\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Save with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return submitAssignment({ ...payload, acknowledgements }, false);\n
			}\n
			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Employee cannot work (blackout)'),\n
					label: t(\n
						'dutycheck',\n
						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',\n
					),\n
					confirmLabel: t('dutycheck', 'Schedule anyway'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
					maxLength: 200,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	function setAssignmentFormBusy(form, busy) {\n
		if (!form) return;\n
		const submit = form.querySelector('button[type="submit"]');\n
		const clear = document.getElementById('dc-assignment-form-clear');\n
		if (submit) {\n
			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';\n
			if (busy) {\n
				submit.setAttribute('aria-busy', 'true');\n
			} else {\n
				submit.removeAttribute('aria-busy');\n
			}\n
		}\n
		if (clear) {\n
			clear.disabled = busy;\n
		}\n
	}\n
\n
	function renderSetupCallout(data) {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		const list = document.getElementById('dc-roster-setup-checklist');\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!callout || !list || !form) return;\n
		const urls = readUrls();\n
		const missing = [];\n
		if (!state.periods.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Create a planning period.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		if (!state.employees.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active employee.'),\n
				url: urls.employees,\n
				cta: t('dutycheck', 'Go to Employees'),\n
			});\n
		}\n
		if (!state.locations.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active location.'),\n
				url: urls.locations,\n
				cta: t('dutycheck', 'Go to Locations'),\n
			});\n
		}\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));\n
		const periodIsOpen = (selectedPeriod?.status || '') === 'open';\n
		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {\n
			missing.push({\n
				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		list.replaceChildren();\n
		if (!missing.length) {\n
			callout.hidden = true;\n
			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {\n
				if (el.dataset.dcLockedBySetup === '1') {\n
					el.disabled = false;\n
					delete el.dataset.dcLockedBySetup;\n
				}\n
			});\n
			updateAddAssignmentControl();\n
			return;\n
		}\n
		for (const item of missing) {\n
			const li = create('li');\n
			li.appendChild(document.createTextNode(item.text + ' '));\n
			if (item.url) {\n
				li.appendChild(create('a', {\n
					class: 'button',\n
					href: item.url,\n
					text: item.cta,\n
				}));\n
			}\n
			list.appendChild(li);\n
		}\n
		callout.hidden = false;\n
		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {\n
			if (!el.disabled) {\n
				el.dataset.dcLockedBySetup = '1';\n
				el.disabled = true;\n
			}\n
		});\n
		updateAddAssignmentControl();\n
	}\n
\n
	function readUrls() {\n
		try {\n
			const raw = document.getElementById('app-content')?.dataset?.dcUrls;\n
			if (!raw) return {};\n
			return JSON.parse(raw) || {};\n
		} catch (_) {\n
			return {};\n
		}\n
	}\n
\n
	function restoreAssignmentFormPanel() {\n
		const host = document.getElementById('dc-assignment-form-host');\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (host && panel && panel.parentElement !== host) {\n
			host.appendChild(panel);\n
		}\n
	}\n
\n
	function selectedPeriodFromState() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;\n
		const periodId = Number(raw);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function isAssignmentSetupBlocking() {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		return !!(callout && !callout.hidden);\n
	}\n
\n
	function canAddAssignment() {\n
		if (!state.canCreateAssignments) {\n
			return false;\n
		}\n
		if (!state.periods.length || !state.employees.length || !state.locations.length) {\n
			return false;\n
		}\n
		const period = selectedPeriodFromState();\n
		return !!(period && String(period.status || '').toLowerCase() === 'open');\n
	}\n
\n
	function addAssignmentDisabledReason() {\n
		if (!state.periods.length) {\n
			return t('dutycheck', 'Create a planning period first.');\n
		}\n
		if (!state.employees.length) {\n
			return t('dutycheck', 'Add at least one active employee before planning shifts.');\n
		}\n
		if (!state.locations.length) {\n
			return t('dutycheck', 'Add at least one active location before planning shifts.');\n
		}\n
		const period = selectedPeriodFromState();\n
		if (!period) {\n
			return t('dutycheck', 'Select a period before adding assignments.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!state.canCreateAssignments) {\n
			return t('dutycheck', 'You cannot add assignments for this period.');\n
		}\n
		return t('dutycheck', 'Adding assignments is not available right now.');\n
	}\n
\n
	function updateAddAssignmentControl() {\n
		const allowed = canAddAssignment();\n
		const reason = addAssignmentDisabledReason();\n
		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {\n
			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.\n
			btn.hidden = !allowed;\n
			btn.disabled = !allowed;\n
			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');\n
			btn.classList.toggle('dc-is-disabled', !allowed);\n
			if (allowed) {\n
				btn.removeAttribute('title');\n
			} else {\n
				btn.setAttribute('title', reason);\n
			}\n
		});\n
		const emptyHint = document.getElementById('dc-roster-empty-add-hint');\n
		if (emptyHint) {\n
			emptyHint.textContent = allowed\n
				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')\n
				: reason;\n
		}\n
	}\n
\n
	function clearAssignmentsSectionSuccess() {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.classList.remove('dc-roster-flash--visible');\n
	}\n
\n
	function showAssignmentsSectionSuccess(message) {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.textContent = String(message);\n
		el.hidden = false;\n
		el.classList.add('dc-roster-flash--visible');\n
	}\n
\n
	function prepareAssignmentFormForModal() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		clearAssignmentFormSuccess();\n
		updateAssignmentFormFeedback(null);\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate && period?.endDate) {\n
			dateInput.min = String(period.startDate);\n
			dateInput.max = String(period.endDate);\n
			if (!dateInput.value) {\n
				dateInput.value = String(period.startDate);\n
			}\n
		}\n
		refreshAssignmentFormEligibility();\n
		applyDefaultBreakToForm();\n
		wireBreakPrefillHint();\n
		fillTemplateSelect();\n
		D?.applyLocaleToTemporalInputs(form);\n
	}\n
\n
	function resetAssignmentFormAfterSave(data) {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		form.reset();\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden && data?.selectedPeriodId) {\n
			periodHidden.value = String(data.selectedPeriodId);\n
		}\n
		applyDefaultBreakToForm();\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate) {\n
			dateInput.value = String(period.startDate);\n
		}\n
		D?.applyLocaleToTemporalInputs(form);\n
		updateAssignmentFormFeedback(null);\n
	}\n
\n
	async function performAssignmentSave() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return false;\n
		}\n
		if (isAssignmentSetupBlocking()) {\n
			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(msg);\n
			Msg.announce(msg, 'error');\n
			return false;\n
		}\n
		const payload = readForm(form);\n
		const validation = validate(payload);\n
		if (validation) {\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(validation);\n
			Msg.announce(validation, 'error');\n
			focusFirstInvalidAssignmentField(payload);\n
			return false;\n
		}\n
		updateAssignmentFormFeedback(null);\n
		setAssignmentFormBusy(form, true);\n
		try {\n
			const response = await submitAssignment(payload, true);\n
			render(response?.data || {});\n
			const savedMsg = buildAssignmentSavedMessage(payload);\n
			clearAssignmentFormSuccess();\n
			showAssignmentsSectionSuccess(savedMsg);\n
			Msg.announce(savedMsg, 'success');\n
			rememberBreakMinutes(payload.breakMinutes);\n
			resetAssignmentFormAfterSave(response?.data || {});\n
			return true;\n
		} catch (err) {\n
			announceAssignmentSaveError(err);\n
			return false;\n
		} finally {\n
			setAssignmentFormBusy(form, false);\n
		}\n
	}\n
\n
	function syncAssignmentModalPrimary(instance) {\n
		if (!instance?.primaryBtn) {\n
			return;\n
		}\n
		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();\n
		instance.primaryBtn.disabled = blocked;\n
		if (blocked) {\n
			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());\n
		} else {\n
			instance.primaryBtn.removeAttribute('title');\n
		}\n
	}\n
\n
	async function openAssignmentCreateModal(triggerEl) {\n
		if (!canAddAssignment()) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
			return;\n
		}\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		renderSetupCallout(state.lastRosterData || {});\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Create assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save assignment'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => {\n
				const ok = await performAssignmentSave();\n
				return ok;\n
			},\n
			onClose: (result) => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				if (!result) {\n
					clearAssignmentFormSuccess();\n
					return;\n
				}\n
				const addBtn = document.getElementById('dc-roster-add-assignment');\n
				if (addBtn && typeof addBtn.focus === 'function') {\n
					addBtn.focus();\n
				}\n
			},\n
			onCancel: () => {\n
				clearAssignmentFormSuccess();\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
		const form = document.getElementById('dc-assignment-form');\n
		if (form && !form.dataset.dcModalSyncWired) {\n
			form.dataset.dcModalSyncWired = '1';\n
			form.addEventListener('change', () => {\n
				if (assignmentModalInstance?._open) {\n
					syncAssignmentModalPrimary(assignmentModalInstance);\n
				}\n
			});\n
		}\n
	}\n
\n
	async function openAssignmentEditModal(assignment, triggerEl) {\n
		if (!canAddAssignment() || !assignment?.id) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			return;\n
		}\n
		state.editingAssignmentId = Number(assignment.id);\n
		state.editingAssignmentVersion = Number(assignment.version ?? 0);\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = String(assignment.id);\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput) {\n
			dateInput.value = String(assignment.dutyDate || '');\n
		}\n
		refreshAssignmentFormEligibility();\n
		const emp = document.getElementById('dc-assignment-employee');\n
		if (emp) {\n
			emp.value = String(assignment.employeeId || '');\n
		}\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (loc) {\n
			loc.value = String(assignment.locationId || '');\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		if (start) {\n
			start.value = String(assignment.startTime || '').slice(0, 5);\n
		}\n
		const end = document.getElementById('dc-assignment-end');\n
		if (end) {\n
			end.value = String(assignment.endTime || '').slice(0, 5);\n
		}\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(assignment.breakMinutes ?? 0);\n
		}\n
		const note = document.getElementById('dc-assignment-note');\n
		if (note) {\n
			note.value = String(assignment.note || '');\n
		}\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Edit assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save changes'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => performAssignmentSave(),\n
			onClose: () => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				clearAssignmentFormSuccess();\n
				if (triggerEl && typeof triggerEl.focus === 'function') {\n
					triggerEl.focus();\n
				}\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
	}\n
\n
	async function cancelAssignmentRow(assignment, triggerEl) {\n
		if (!assignment?.id || !canAddAssignment()) {\n
			return;\n
		}\n
		const ok = window.confirm(\n
			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),\n
		);\n
		if (!ok) {\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});\n
			render(response?.data || {});\n
			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));\n
			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		} finally {\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
		}\n
	}\n
\n
	let templatesInflight = null;\n
\n
	async function ensureAssignmentModalPrereqs() {\n
		const jobs = [loadTemplates()];\n
		if (state.planningDefaultsFresh !== true) {\n
			jobs.push(refreshPlanningDefaultFromServer());\n
		}\n
		await Promise.all(jobs);\n
	}\n
\n
	async function loadTemplates() {\n
		if (state.templatesLoaded) {\n
			fillTemplateSelect();\n
			return;\n
		}\n
		if (templatesInflight) {\n
			await templatesInflight;\n
			fillTemplateSelect();\n
			return;\n
		}\n
		templatesInflight = (async () => {\n
			try {\n
				const response = await Api.get('/apps/dutycheck/api/templates', {});\n
				state.templates = Array.isArray(response?.data) ? response.data : [];\n
				state.templatesLoaded = true;\n
			} catch (_) {\n
				state.templates = [];\n
				state.templatesLoaded = false;\n
			} finally {\n
				templatesInflight = null;\n
			}\n
		})();\n
		await templatesInflight;\n
		fillTemplateSelect();\n
	}\n
\n
	function fillTemplateSelect() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select) {\n
			return;\n
		}\n
		const current = select.value;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));\n
		for (const tpl of state.templates) {\n
			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;\n
			select.appendChild(create('option', { value: String(tpl.id), text: label }));\n
		}\n
		if (current && [...select.options].some((o) => o.value === current)) {\n
			select.value = current;\n
		}\n
	}\n
\n
	function applySelectedTemplate() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select || !select.value) {\n
			return;\n
		}\n
		const tpl = state.templates.find((t) => String(t.id) === String(select.value));\n
		if (!tpl) {\n
			return;\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		const end = document.getElementById('dc-assignment-end');\n
		const br = document.getElementById('dc-assignment-break');\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (start) start.value = String(tpl.startTime || '').slice(0, 5);\n
		if (end) end.value = String(tpl.endTime || '').slice(0, 5);\n
		if (br) br.value = String(tpl.breakMinutes ?? 0);\n
		if (loc && tpl.locationId) loc.value = String(tpl.locationId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		refreshAssignmentFormEligibility();\n
	}\n
\n
	function refreshAcknowledgeStats(periodId) {\n
		const el = document.getElementById('dc-roster-ack-stats');\n
		if (!el) {\n
			return;\n
		}\n
		const id = Number(periodId);\n
		if (!Number.isInteger(id) || id <= 0) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === id);\n
		const status = String(period?.status || '').toLowerCase();\n
		if (status !== 'published' && status !== 'closed') {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const rows = Array.isArray(state.assignments) ? state.assignments : [];\n
		let total = 0;\n
		let acked = 0;\n
		for (const row of rows) {\n
			if (String(row?.status || '').toLowerCase() === 'cancelled') {\n
				continue;\n
			}\n
			total += 1;\n
			if (row?.acknowledged || row?.acknowledgedAt) {\n
				acked += 1;\n
			}\n
		}\n
		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;\n
		el.hidden = false;\n
		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')\n
			.replace('{acked}', String(acked))\n
			.replace('{total}', String(total))\n
			.replace('{pct}', String(pct));\n
	}\n
\n
	function fillCopySourceSelect(selectedPeriodId) {\n
		const select = document.getElementById('dc-roster-copy-source');\n
		const wrap = document.getElementById('dc-roster-copy-period');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!select || !wrap) {\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));\n
		const open = period && String(period.status || '').toLowerCase() === 'open';\n
		wrap.hidden = !open;\n
		state.copyPreviewReady = false;\n
		if (applyBtn) applyBtn.disabled = true;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));\n
		for (const p of state.periods) {\n
			if (Number(p.id) === Number(selectedPeriodId)) {\n
				continue;\n
			}\n
			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;\n
			select.appendChild(create('option', { value: String(p.id), text: label }));\n
		}\n
	}\n
\n
	async function runCopyPeriod(dryRun) {\n
		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);\n
		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);\n
		const status = document.getElementById('dc-roster-copy-status');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!targetId || !sourceId) {\n
			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {\n
				sourcePeriodId: sourceId,\n
				dryRun: !!dryRun,\n
			});\n
			const data = response?.data || {};\n
			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);\n
			const skipped = Number(data.skipped ?? 0);\n
			const msg = dryRun\n
				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')\n
					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))\n
					.replace('{skipped}', String(data.wouldSkip ?? skipped))\n
				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')\n
					.replace('{created}', String(created));\n
			if (status) {\n
				status.hidden = false;\n
				status.textContent = msg;\n
			}\n
			Msg.announce(msg, dryRun ? 'info' : 'success');\n
			if (dryRun) {\n
				state.copyPreviewReady = true;\n
				if (applyBtn) applyBtn.disabled = false;\n
			} else {\n
				state.copyPreviewReady = false;\n
				if (applyBtn) applyBtn.disabled = true;\n
				render(data.roster || data || {});\n
				if (!data.roster && data.assignments) {\n
					render(data);\n
				} else if (!data.roster) {\n
					await loadRoster(targetId);\n
				}\n
			}\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	function updateAdminExportLinks(selectedPeriodId) {\n
		const csv = document.getElementById('dc-roster-export-csv');\n
		const printLink = document.getElementById('dc-roster-export-print');\n
		if (!csv || !printLink) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		const baseCsv = String(urls.rosterExportCsv || '');\n
		const basePrint = String(urls.rosterPrint || '');\n
		const id = Number(selectedPeriodId);\n
		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;\n
		if (!ok) {\n
			csv.href = '#dc-roster-period-section';\n
			printLink.href = '#dc-roster-period-section';\n
			csv.setAttribute('aria-disabled', 'true');\n
			printLink.setAttribute('aria-disabled', 'true');\n
			return;\n
		}\n
		const q = new URLSearchParams({ periodId: String(id) }).toString();\n
		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;\n
		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;\n
		csv.removeAttribute('aria-disabled');\n
		printLink.removeAttribute('aria-disabled');\n
	}\n
\n
	function wireAdminExportGuards() {\n
		const guard = (event) => {\n
			const el = event.currentTarget;\n
			if (el && el.getAttribute('aria-disabled') === 'true') {\n
				event.preventDefault();\n
				Msg.announce(\n
					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),\n
					'warning',\n
				);\n
			}\n
		};\n
		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);\n
		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);\n
	}\n
\n
	let rosterPhpL10nCache;\n
	function getRosterPhpL10n() {\n
		if (rosterPhpL10nCache !== undefined) {\n
			return rosterPhpL10nCache;\n
		}\n
		const el = document.getElementById('dc-roster-php-l10n');\n
		const raw = el && el.dataset ? el.dataset.l10n : '';\n
		if (!raw) {\n
			rosterPhpL10nCache = null;\n
			return null;\n
		}\n
		try {\n
			rosterPhpL10nCache = JSON.parse(raw) || null;\n
		} catch (_) {\n
			rosterPhpL10nCache = null;\n
		}\n
		return rosterPhpL10nCache;\n
	}\n
\n
	function translateConflictMessage(message) {\n
		const raw = String(message || '');\n
		if (!raw) {\n
			return t('dutycheck', 'Unknown conflict');\n
		}\n
		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];\n
		if (fromPhp) {\n
			return fromPhp;\n
		}\n
		return t('dutycheck', raw);\n
	}\n
\n
	function render(data) {\n
		state.lastRosterData = data;\n
		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {\n
			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);\n
			state.planningDefaultsFresh = true;\n
		}\n
		state.periods = data.periods || [];\n
		state.employees = data.employees || [];\n
		state.locations = data.locations || [];\n
		state.assignments = data.assignments || [];\n
		state.absenceBlocks = data.absenceBlocks || [];\n
		state.canCreateAssignments = Boolean(data.canCreateAssignments);\n
		fillPeriodSwitcher(state.periods, data.selectedPeriodId);\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden) {\n
			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));\n
		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {\n
			dateInput.min = String(selectedPeriod.startDate);\n
			dateInput.max = String(selectedPeriod.endDate);\n
			if (!dateInput.value && state.canCreateAssignments) {\n
				dateInput.value = String(selectedPeriod.startDate);\n
			}\n
		} else if (dateInput) {\n
			dateInput.removeAttribute('min');\n
			dateInput.removeAttribute('max');\n
		}\n
		fillSelect('dc-assignment-location', state.locations, {\n
			emptyText: t('dutycheck', 'No active locations yet'),\n
		});\n
		updateLocationTimezoneHint();\n
		refreshAssignmentFormEligibility();\n
		renderAssignments(data.assignments || []);\n
		renderConflicts(data.conflicts || []);\n
		renderSetupCallout(data);\n
		updateAddAssignmentControl();\n
		updateAdminExportLinks(data.selectedPeriodId);\n
		fillCopySourceSelect(data.selectedPeriodId);\n
		refreshAcknowledgeStats(data.selectedPeriodId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
	}\n
\n
	function selectedPeriodIdFromUrl() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const raw = params.get('periodId');\n
		const parsed = Number(raw);\n
		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;\n
	}\n
\n
	function updateUrlPeriodId(periodId) {\n
		const params = new URLSearchParams(window.location.search || '');\n
		if (periodId && Number(periodId) > 0) {\n
			params.set('periodId', String(periodId));\n
		} else {\n
			params.delete('periodId');\n
		}\n
		const query = params.toString();\n
		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);\n
	}\n
\n
	async function loadRoster(periodId) {\n
		if (C.isModalOpen?.()) {\n
			C.dismissOpenModal();\n
		} else {\n
			restoreAssignmentFormPanel();\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		C.setLoadingRow(tbody, 7);\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });\n
			render(response?.data || {});\n
			updateUrlPeriodId(response?.data?.selectedPeriodId || null);\n
			void loadRosterSignals(response?.data || {});\n
			void loadCoverageStrip(response?.data?.selectedPeriodId);\n
			return response?.data || {};\n
		} catch (err) {\n
			const code = String(err?.payload?.error?.code || err?.code || '');\n
			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {\n
				try {\n
					const fallback = await Api.get('/apps/dutycheck/api/roster', {});\n
					render(fallback?.data || {});\n
					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);\n
					void loadRosterSignals(fallback?.data || {});\n
					void loadCoverageStrip(fallback?.data?.selectedPeriodId);\n
					Msg.announce(\n
						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),\n
						'warning',\n
					);\n
					return fallback?.data || {};\n
				} catch (retryErr) {\n
					Msg.handleApiError(retryErr);\n
					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
					return null;\n
				}\n
			}\n
			Msg.handleApiError(err);\n
			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
			return null;\n
		} finally {\n
			C.clearLoadingRow(tbody);\n
		}\n
	}\n
\n
	function dateOverlapsBlackout(day, blackout) {\n
		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);\n
		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);\n
		if (!start || !end) return false;\n
		return day >= start && day <= end;\n
	}\n
\n
	async function loadRosterSignals(data) {\n
		state.preferencesByEmployee = {};\n
		state.blackoutsByEmployeeDate = {};\n
		const employees = data.employees || state.employees || [];\n
		const period = (data.periods || state.periods || []).find(\n
			(p) => Number(p.id) === Number(data.selectedPeriodId),\n
		);\n
		const from = period?.startDate || period?.start_date || '';\n
		const to = period?.endDate || period?.end_date || '';\n
		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);\n
		if (!ids.length) return;\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/roster/signals', {\n
				employeeIds: ids,\n
				from,\n
				to,\n
			});\n
			const prefs = res?.data?.preferences || [];\n
			const blackouts = res?.data?.blackouts || [];\n
			prefs.forEach((p) => {\n
				const eid = String(p.employeeId || p.employee_id || '');\n
				if (!eid) return;\n
				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];\n
				state.preferencesByEmployee[eid].push(p);\n
			});\n
			const days = [];\n
			if (from && to) {\n
				const cur = new Date(from + 'T12:00:00');\n
				const end = new Date(to + 'T12:00:00');\n
				while (cur <= end) {\n
					const pad = (n) => String(n).padStart(2, '0');\n
					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);\n
					cur.setDate(cur.getDate() + 1);\n
				}\n
			}\n
			blackouts.forEach((b) => {\n
				const eid = Number(b.employeeId || b.employee_id || 0);\n
				if (!eid) return;\n
				days.forEach((day) => {\n
					if (dateOverlapsBlackout(day, b)) {\n
						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;\n
					}\n
				});\n
			});\n
			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {\n
				paintGridWindow({ force: true });\n
			}\n
		} catch (_) {\n
			// Optional enrichment — ignore failures.\n
		}\n
	}\n
\n
	async function loadCoverageStrip(periodId) {\n
		const strip = document.getElementById('dc-coverage-strip');\n
		const text = document.getElementById('dc-coverage-strip-text');\n
		if (!strip || !text) return;\n
		const id = Number(periodId || 0);\n
		if (!id) {\n
			strip.hidden = true;\n
			return;\n
		}\n
		try {\n
			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);\n
			const d = res?.data || {};\n
			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);\n
			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);\n
			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);\n
			let message;\n
			if (ready || (hard === 0 && soft === 0)) {\n
				message = t('dutycheck', 'Ready to publish — no blocking issues.');\n
				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--ok');\n
			} else if (hard > 0) {\n
				message = t('dutycheck', '{n} must-fix issues before publish.')\n
					.replace('{n}', String(hard));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');\n
				strip.classList.add('dc-coverage-strip--critical');\n
			} else {\n
				message = t('dutycheck', '{n} items need confirmation.')\n
					.replace('{n}', String(soft));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--warn');\n
			}\n
			text.textContent = message;\n
			strip.hidden = false;\n
		} catch (_) {\n
			strip.hidden = true;\n
		}\n
	}\n
\n
	function currentSuggestLocationId() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const fromUrl = Number(params.get('locationId') || 0);\n
		if (Number.isInteger(fromUrl) && fromUrl > 0) {\n
			return fromUrl;\n
		}\n
		const filterEl = document.getElementById('dc-roster-location')\n
			|| document.getElementById('dc-assignment-location');\n
		const fromUi = Number(filterEl?.value || 0);\n
		if (Number.isInteger(fromUi) && fromUi > 0) {\n
			return fromUi;\n
		}\n
		return null;\n
	}\n
\n
	async function runSuggestFill() {\n
		const periodId = Number(selectedPeriodId() || 0);\n
		if (!periodId) {\n
			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');\n
			return;\n
		}\n
		const btn = document.getElementById('dc-roster-suggest-fill');\n
		if (btn) {\n
			btn.disabled = true;\n
			btn.setAttribute('aria-busy', 'true');\n
		}\n
		const locId = currentSuggestLocationId();\n
		const suggestBody = { locationId: locId || null };\n
		try {\n
			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);\n
			const d = preview?.data || {};\n
			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {\n
				const noLoc = Number(d.skippedNoLocation ?? 0);\n
				Msg.announce(\n
					noLoc > 0\n
						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')\n
						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),\n
					'warning',\n
				);\n
				return;\n
			}\n
			const body = create('div', { class: 'dc-suggest-preview' }, [\n
				create('p', {\n
					text: t('dutycheck', 'Would create {n} shifts from patterns.')\n
						.replace('{n}', String(d.created ?? 0)),\n
				}),\n
				create('ul', { class: 'dc-suggest-preview__counts' }, [\n
					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),\n
				]),\n
			]);\n
			const confirmed = await new Promise((resolve) => {\n
				let settled = false;\n
				const finish = (value) => {\n
					if (settled) return;\n
					settled = true;\n
					resolve(value);\n
				};\n
				C.openModal({\n
					title: t('dutycheck', 'Suggest fill preview'),\n
					primaryLabel: t('dutycheck', 'Apply suggest fill'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					render: () => body,\n
					onSubmit: async () => {\n
						finish(true);\n
						return true;\n
					},\n
					onCancel: () => finish(false),\n
					onClose: () => finish(false),\n
				});\n
			});\n
			if (!confirmed) return;\n
			if ((d.created ?? 0) < 1) {\n
				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');\n
				return;\n
			}\n
			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);\n
			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');\n
			await loadRoster(periodId);\n
		} catch (err) {\n
			const code = String(err?.code || err?.payload?.error?.code || '');\n
			if (code === 'ROTATION_DISABLED') {\n
				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');\n
			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {\n
				Msg.announce(\n
					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),\n
					'warning',\n
				);\n
			} else if (code === 'RATE_LIMITED') {\n
				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');\n
			} else {\n
				Msg.handleApiError(err);\n
			}\n
		} finally {\n
			if (btn) {\n
				btn.disabled = false;\n
				btn.removeAttribute('aria-busy');\n
			}\n
		}\n
	}\n
\n
	function clearAssignmentFormFields() {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) return;\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		const periodVal = periodHidden ? String(periodHidden.value || '') : '';\n
		form.reset();\n
		if (periodHidden && periodVal) {\n
			periodHidden.value = periodVal;\n
		}\n
		applyDefaultBreakToForm();\n
		D?.applyLocaleToTemporalInputs(form);\n
		refreshAssignmentFormEligibility();\n
		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');\n
		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {\n
			showAssignmentFormSuccess(clearedMsg, 'info');\n
		}\n
		Msg.announce(clearedMsg, 'info');\n
	}\n
\n
	function readForm(form) {\n
		const data = new FormData(form);\n
		const rawStart = String(data.get('startTime') || '');\n
		const rawEnd = String(data.get('endTime') || '');\n
		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();\n
		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();\n
		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);\n
		const out = {\n
			periodId: Number(data.get('periodId')),\n
			employeeId: Number(data.get('employeeId')),\n
			locationId: Number(data.get('locationId')),\n
			dutyDate: String(data.get('dutyDate') || ''),\n
			startTime,\n
			endTime,\n
			breakMinutes: Number(data.get('breakMinutes') || 0),\n
			note: String(data.get('note') || '').trim(),\n
			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,\n
		};\n
		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {\n
			out.expectedVersion = Number(state.editingAssignmentVersion);\n
		}\n
		return out;\n
	}\n
\n
	/**\n
	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.\n
	 */\n
	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;\n
		if (start === null || end === null) return null;\n
		let endAdj = end;\n
		if (endAdj <= start) {\n
			endAdj += 24 * 60;\n
		}\n
		return (endAdj - start) - breakMinutes;\n
	}\n
\n
	function validate(payload) {\n
		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {\n
			return t('dutycheck', 'Select a period before saving.');\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		if (!period) {\n
			return t('dutycheck', 'That planning period was not found.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {\n
			return t('dutycheck', 'Select an employee.');\n
		}\n
		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {\n
			return t('dutycheck', 'Select a location.');\n
		}\n
		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {\n
			return t('dutycheck', 'Date, start time, and end time are required.');\n
		}\n
		if (period.startDate && period.endDate\n
			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {\n
			return t('dutycheck', 'Date must fall within the selected period.');\n
		}\n
		if (payload.startTime === payload.endTime) {\n
			return t(\n
				'dutycheck',\n
				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
			);\n
		}\n
		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {\n
			return t('dutycheck', 'Break minutes must be between 0 and 720.');\n
		}\n
		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
		if (effective === null) {\n
			return t('dutycheck', 'Please check the dates and times.');\n
		}\n
		if (effective <= 0) {\n
			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
		}\n
		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			return t('dutycheck', 'This employee has approved leave on the selected date.');\n
		}\n
		if (findOverlapAssignment(payload)) {\n
			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');\n
		}\n
		return null;\n
	}\n
\n
	function errorMessageFor(error) {\n
		const code = errorCodeFrom(error);\n
		switch (code) {\n
			case 'PERIOD_NOT_OPEN':\n
				return t('dutycheck', 'The selected period is not open for planning.');\n
			case 'PERIOD_NOT_FOUND':\n
				return t('dutycheck', 'That planning period was not found.');\n
			case 'DATE_OUTSIDE_PERIOD':\n
				return t('dutycheck', 'Date must fall within the selected period.');\n
			case 'EMPLOYEE_NOT_FOUND':\n
				return t('dutycheck', 'Employee not found or no longer available.');\n
			case 'LOCATION_NOT_FOUND':\n
				return t('dutycheck', 'Location not found or no longer available.');\n
			case 'EQUAL_DUTY_TIMES':\n
				return t(\n
					'dutycheck',\n
					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
				);\n
			case 'INVALID_SHIFT_LENGTH':\n
				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
			case 'INVALID_BREAK_MINUTES':\n
				return t('dutycheck', 'Break minutes are out of the allowed range.');\n
			case 'NOTE_TOO_LONG':\n
				return t('dutycheck', 'Note is too long (maximum 512 characters).');\n
			case 'ASSIGNMENT_OVERLAP':\n
				return t('dutycheck', 'This employee already has an overlapping assignment.');\n
			case 'ASSIGNMENT_DUPLICATE_SLOT':\n
				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');\n
			case 'STALE_VERSION':\n
				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');\n
			case 'CONFLICT_ACK_STALE':\n
				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');\n
			case 'COMPANY_MEMBERSHIP_REQUIRED':\n
				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');\n
			case 'EXPECTED_VERSION_REQUIRED':\n
				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');\n
			case 'ABSENCE_CONFLICT':\n
				return t('dutycheck', 'This employee has an approved absence on that date.');\n
			case 'BLACKOUT_CONFLICT':\n
				return t('dutycheck', 'This employee marked that they cannot work during this time.');\n
			case 'CONFLICT_ACK_REQUIRED':\n
				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');\n
			case 'REASON_TOO_SHORT':\n
				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');\n
			case 'INTERNAL_ERROR':\n
				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');\n
			case 'QUALIFICATION_MISSING':\n
				return t('dutycheck', 'This employee is missing a required qualification for that location.');\n
			case 'ASSIGNMENT_NOT_FOUND':\n
				return t('dutycheck', 'That assignment no longer exists. Reload the page.');\n
			case 'SCHEMA_NOT_READY':\n
				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');\n
			case 'INVALID_DATE':\n
			case 'INVALID_TIME':\n
				return t('dutycheck', 'Please check the dates and times.');\n
			case 'PERIOD_ID_REQUIRED':\n
				return t('dutycheck', 'Select a period before saving.');\n
			case 'EMPLOYEE_ID_REQUIRED':\n
				return t('dutycheck', 'Select an employee.');\n
			case 'LOCATION_ID_REQUIRED':\n
				return t('dutycheck', 'Select a location.');\n
			default:\n
				return null;\n
		}\n
	}\n
\n
\n
	async function loadPendingSwaps() {\n
		const list = document.getElementById('dc-swap-list');\n
		const empty = document.getElementById('dc-swap-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/swaps');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				li.appendChild(create('p', {\n
					text: row.toEmployeeId\n
						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId))\n
							.replace('{to}', String(row.toEmployeeId))\n
						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId)),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });\n
						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');\n
						await loadPendingSwaps();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });\n
						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');\n
						await loadPendingSwaps();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load swap requests.');\n
			}\n
		}\n
	}\n
\n
	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {\n
		try {\n
			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {\n
				acknowledgements,\n
			});\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Approve with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return approveOpenShiftClaim(openShiftId, acks, false);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	async function loadPendingOpenClaims() {\n
		const list = document.getElementById('dc-open-claim-list');\n
		const empty = document.getElementById('dc-open-claim-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;\n
				li.appendChild(create('p', {\n
					text: t('dutycheck', '{date} · employee #{emp}')\n
						.replace('{date}', String(when))\n
						.replace('{emp}', String(row.claimedByEmployeeId || '—')),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await approveOpenShiftClaim(row.id);\n
						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');\n
						await loadPendingOpenClaims();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});\n
						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');\n
						await loadPendingOpenClaims();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load pending claims.');\n
			}\n
		}\n
	}\n
\n
	function wireMarketplace() {\n
		const toggle = document.getElementById('dc-open-shift-create');\n
		const form = document.getElementById('dc-open-shift-form');\n
		toggle?.addEventListener('click', () => {\n
			if (!form) return;\n
			form.hidden = !form.hidden;\n
			const loc = document.getElementById('dc-os-location');\n
			if (loc && state.locations?.length) {\n
				loc.replaceChildren();\n
				for (const l of state.locations) {\n
					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));\n
				}\n
			}\n
			const period = selectedPeriodFromState();\n
			const date = document.getElementById('dc-os-date');\n
			if (date && period?.startDate) date.value = String(period.startDate);\n
		});\n
		document.getElementById('dc-os-save')?.addEventListener('click', async () => {\n
			const period = selectedPeriodFromState();\n
			if (!period?.id) {\n
				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');\n
				return;\n
			}\n
			try {\n
				await Api.post('/apps/dutycheck/api/open-shifts', {\n
					periodId: Number(period.id),\n
					locationId: Number(document.getElementById('dc-os-location')?.value || 0),\n
					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),\n
					startTime: String(document.getElementById('dc-os-start')?.value || ''),\n
					endTime: String(document.getElementById('dc-os-end')?.value || ''),\n
					breakMinutes: 0,\n
				});\n
				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');\n
				if (form) form.hidden = true;\n
			} catch (err) {\n
				Msg.handleApiError(err);\n
			}\n
		});\n
	}\n
\n
	document.addEventListener('DOMContentLoaded', async () => {\n
		D?.applyLocaleToTemporalInputs(document);\n
		wireBreakPrefillHint();\n
		window.addEventListener('dc-planning-defaults-changed', (event) => {\n
			const minutes = Number(event?.detail?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
		});\n
		wireAdminExportGuards();\n
		wireMarketplace();\n
		setRosterView('grid');\n
		wireRosterVirtualization();\n
		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));\n
		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));\n
		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {\n
			gridState.selected.clear();\n
			syncVisibleGridChrome();\n
			updateBulkBar();\n
		});\n
		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {\n
			void applyBulkFromTemplate();\n
		});\n
		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {\n
			void runSuggestFill();\n
		});\n
		await Promise.all([\n
			loadRoster(selectedPeriodIdFromUrl()),\n
			loadPendingSwaps(),\n
			loadPendingOpenClaims(),\n
		]);\n
\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		switcher?.addEventListener('change', async () => {\n
			clearAssignmentsSectionSuccess();\n
			const periodId = Number(switcher.value);\n
			await Promise.all([\n
				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),\n
				loadPendingSwaps(),\n
				loadPendingOpenClaims(),\n
			]);\n
		});\n
\n
		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {\n
			const btn = event.target.closest('.dc-roster-add-assignment-trigger');\n
			if (!btn) {\n
				return;\n
			}\n
			openAssignmentCreateModal(btn);\n
		});\n
\n
		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {\n
			clearAssignmentFormFields();\n
		});\n
\n
		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {\n
			applySelectedTemplate();\n
		});\n
		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {\n
			runCopyPeriod(true);\n
		});\n
		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {\n
			if (!state.copyPreviewReady) {\n
				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');\n
				return;\n
			}\n
			await runCopyPeriod(false);\n
		});\n
		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {\n
			state.copyPreviewReady = false;\n
			const applyBtn = document.getElementById('dc-roster-copy-apply');\n
			if (applyBtn) applyBtn.disabled = true;\n
		});\n
\n
		const form = document.getElementById('dc-assignment-form');\n
		const scheduleEligibilityRefresh = () => {\n
			clearAssignmentFormSuccess();\n
			clearAssignmentsSectionSuccess();\n
			refreshAssignmentFormEligibility();\n
		};\n
		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);\n
		form?.addEventListener('submit', (event) => {\n
			event.preventDefault();\n
			const dialog = form.closest('.dc-modal__dialog--roster-assignment');\n
			if (!dialog) {\n
				return;\n
			}\n
			const primary = dialog.querySelector('.dc-modal__actions .button.primary');\n
			if (primary && !primary.disabled) {\n
				primary.click();\n
			}\n
		});\n
	});\n
})();\n
' contains "inline: 'nearest'".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:68

FAILURES!
Tests: 6, Assertions: 65, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.084685
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.119187
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.151569
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.123495
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.089624
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.082825
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.076109
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.126227
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.174232
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 2.992254
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 0.932011
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.092373
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.047947
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.041929
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.268367
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.080649
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.046687
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.171146
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.126969
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.044863
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.037653
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.260652
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.099635
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 1.810551
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /inline:\s*'nearest'/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105655 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'start',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:260:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.132683
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.215817
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.08793
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 3.545815
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 80.701491
killed skip_horizontal_reveal
== mutant skip_page_keys (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

..F...                                                              6 / 6 (100%)

Time: 00:00.016, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testRosterJsWindowsRowsAndKeepsKeyboardReach
Failed asserting that '(function () {\n
	'use strict';\n
\n
	const Api = window.DutyCheckApi;\n
	const Msg = window.DutyCheckMessaging;\n
	const C = window.DutyCheckComponents || window.DutyCheckDom || {};\n
	const D = window.DutyCheckDates;\n
	const ConflictLabels = window.DutyCheckConflictLabels;\n
	const create = C.createElement;\n
	if (typeof create !== 'function') {\n
		throw new Error('DutyCheck components failed to load');\n
	}\n
\n
	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n
\n
	const state = {\n
		periods: [],\n
		employees: [],\n
		locations: [],\n
		assignments: [],\n
		absenceBlocks: [],\n
		canCreateAssignments: false,\n
		lastRosterData: null,\n
		defaultBreakMinutes: 0,\n
		planningDefaultsFresh: false,\n
		templates: [],\n
		templatesLoaded: false,\n
		editingAssignmentId: null,\n
		editingAssignmentVersion: null,\n
		copyPreviewReady: false,\n
		preferencesByEmployee: {},\n
		blackoutsByEmployeeDate: {},\n
	};\n
	let assignmentModalInstance = null;\n
	let suggestedBreakMinutes = 0;\n
\n
	function clampBreakMinutes(value) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n)) {\n
			return 0;\n
		}\n
		return Math.max(0, Math.min(720, Math.round(n)));\n
	}\n
\n
	function readStoredBreakMinutes() {\n
		try {\n
			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n
			if (raw === null || raw === '') {\n
				return null;\n
			}\n
			return clampBreakMinutes(raw);\n
		} catch (_) {\n
			return null;\n
		}\n
	}\n
\n
	function rememberBreakMinutes(minutes) {\n
		try {\n
			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n
		} catch (_) {\n
			/* ignore quota / privacy mode */\n
		}\n
	}\n
\n
	function resolveBreakPrefill() {\n
		const org = clampBreakMinutes(state.defaultBreakMinutes);\n
		// Organisation default from Settings always wins when it is greater than zero.\n
		if (org > 0) {\n
			return { minutes: org, source: 'settings' };\n
		}\n
		const stored = readStoredBreakMinutes();\n
		if (stored !== null) {\n
			return { minutes: stored, source: 'last' };\n
		}\n
		return { minutes: 0, source: 'settings' };\n
	}\n
\n
	async function refreshPlanningDefaultFromServer() {\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n
			const minutes = Number(response?.planning?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
			state.planningDefaultsFresh = true;\n
		} catch (_) {\n
			/* Fall back to defaultBreakMinutes from the last roster API load. */\n
		}\n
	}\n
\n
	function breakPrefillMessage(prefill) {\n
		const n = String(prefill.minutes);\n
		if (prefill.source === 'last') {\n
			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n
		}\n
		if (prefill.minutes === 0) {\n
			return t('dutycheck', 'Pre-filled with 0 min — no break.');\n
		}\n
		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n
	}\n
\n
	function updateBreakPrefillHint(userEdited) {\n
		const el = document.getElementById('dc-assignment-break-prefill');\n
		if (!el) {\n
			return;\n
		}\n
		if (userEdited) {\n
			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n
			return;\n
		}\n
		el.textContent = breakPrefillMessage(resolveBreakPrefill());\n
	}\n
\n
	function applyDefaultBreakToForm() {\n
		const prefill = resolveBreakPrefill();\n
		suggestedBreakMinutes = prefill.minutes;\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(prefill.minutes);\n
		}\n
		updateBreakPrefillHint(false);\n
	}\n
\n
	function wireBreakPrefillHint() {\n
		const br = document.getElementById('dc-assignment-break');\n
		if (!br || br.dataset.dcBreakPrefillWired) {\n
			return;\n
		}\n
		br.dataset.dcBreakPrefillWired = '1';\n
		br.addEventListener('input', () => {\n
			const current = clampBreakMinutes(br.value);\n
			updateBreakPrefillHint(current !== suggestedBreakMinutes);\n
		});\n
		br.addEventListener('change', () => {\n
			const current = clampBreakMinutes(br.value);\n
			if (current === suggestedBreakMinutes) {\n
				updateBreakPrefillHint(false);\n
			}\n
		});\n
	}\n
\n
	function dateToDayIndex(isoDate) {\n
		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n
		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n
			return null;\n
		}\n
		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n
	}\n
\n
	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n
		const dayIndex = dateToDayIndex(dutyDate);\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime);\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime);\n
		if (dayIndex === null || start === null || end === null) {\n
			return null;\n
		}\n
		const absoluteStart = (dayIndex * 1440) + start;\n
		let absoluteEnd = (dayIndex * 1440) + end;\n
		if (absoluteEnd <= absoluteStart) {\n
			absoluteEnd += 1440;\n
		}\n
		return [absoluteStart, absoluteEnd];\n
	}\n
\n
	function absoluteRangesOverlap(a, b) {\n
		return a[0] < b[1] && b[0] < a[1];\n
	}\n
\n
	function isDateInAbsenceBlock(employeeId, dutyDate) {\n
		if (!dutyDate) {\n
			return false;\n
		}\n
		for (const block of state.absenceBlocks) {\n
			if (Number(block?.employeeId) !== Number(employeeId)) {\n
				continue;\n
			}\n
			const start = String(block?.startDate || '');\n
			const end = String(block?.endDate || '');\n
			if (dutyDate >= start && dutyDate <= end) {\n
				return true;\n
			}\n
		}\n
		return false;\n
	}\n
\n
	function employeesAvailableOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return [];\n
		}\n
		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n
	}\n
\n
	function countEmployeesBlockedOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return 0;\n
		}\n
		return state.employees.length - employeesAvailableOnDate(dutyDate).length;\n
	}\n
\n
	function appendHintLink(parent, beforeText, linkText, afterText, href) {\n
		parent.appendChild(document.createTextNode(beforeText));\n
		if (href) {\n
			const link = create('a', {\n
				class: 'dc-hint-link',\n
				href,\n
				text: linkText,\n
			});\n
			parent.appendChild(link);\n
			parent.appendChild(document.createTextNode(afterText));\n
		} else {\n
			parent.appendChild(document.createTextNode(linkText + afterText));\n
		}\n
	}\n
\n
	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n
		const hint = document.getElementById('dc-assignment-employee-hint');\n
		if (!hint) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		hint.replaceChildren();\n
		if (!dutyDate) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n
			));\n
			return;\n
		}\n
		if (!available.length && blocked > 0) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n
					.replace('{n}', String(blocked)),\n
				t('dutycheck', 'review absences'),\n
				'.',\n
				String(urls.absences || ''),\n
			);\n
			return;\n
		}\n
		if (!available.length) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'No employees are available. Add an active employee on the '),\n
				t('dutycheck', 'Employees'),\n
				t('dutycheck', ' page first.'),\n
				String(urls.employees || ''),\n
			);\n
			return;\n
		}\n
		if (blocked > 0) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n
					.replace('{available}', String(available.length))\n
					.replace('{hidden}', String(blocked)),\n
			));\n
			return;\n
		}\n
		hint.appendChild(document.createTextNode(\n
			t('dutycheck', 'All {n} active employees can be scheduled on this date.')\n
				.replace('{n}', String(available.length)),\n
		));\n
	}\n
\n
	function refreshEmployeeSelectForDate() {\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const dutyDate = dateInput ? String(dateInput.value || '') : '';\n
		const available = employeesAvailableOnDate(dutyDate);\n
		const blocked = countEmployeesBlockedOnDate(dutyDate);\n
		setEmployeeAvailabilityHint(dutyDate, available, blocked);\n
		fillSelect('dc-assignment-employee', available, {\n
			emptyText: dutyDate\n
				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n
				: t('dutycheck', 'Pick a date first'),\n
		});\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		if (employeeSelect) {\n
			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n
		}\n
	}\n
\n
	function findOverlapAssignment(payload) {\n
		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n
		if (!candidate) {\n
			return null;\n
		}\n
		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		for (const assignment of state.assignments) {\n
			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n
				continue;\n
			}\n
			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n
				continue;\n
			}\n
			const existing = assignmentAbsoluteRange(\n
				String(assignment?.dutyDate || ''),\n
				String(assignment?.startTime || ''),\n
				String(assignment?.endTime || ''),\n
			);\n
			if (existing && absoluteRangesOverlap(candidate, existing)) {\n
				return assignment;\n
			}\n
		}\n
		return null;\n
	}\n
\n
	function setFormFeedbackA11y(el, isError) {\n
		if (!el) {\n
			return;\n
		}\n
		if (isError) {\n
			el.setAttribute('role', 'alert');\n
			el.setAttribute('aria-live', 'assertive');\n
		} else {\n
			el.setAttribute('role', 'status');\n
			el.setAttribute('aria-live', 'polite');\n
		}\n
	}\n
\n
	function focusFirstInvalidAssignmentField(payload) {\n
		if (!payload) {\n
			return;\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		const order = [\n
			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n
			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n
			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n
			{\n
				test: () => period?.startDate && period?.endDate && payload.dutyDate\n
					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n
				id: 'dc-assignment-date',\n
			},\n
			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },\n
			{ test: () => !payload.locationId, id: 'dc-assignment-location' },\n
			{ test: () => !payload.startTime, id: 'dc-assignment-start' },\n
			{ test: () => !payload.endTime, id: 'dc-assignment-end' },\n
			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },\n
			{\n
				test: () => {\n
					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
					return effective === null || effective <= 0;\n
				},\n
				id: 'dc-assignment-end',\n
			},\n
		];\n
		for (const item of order) {\n
			if (item.test()) {\n
				document.getElementById(item.id)?.focus();\n
				return;\n
			}\n
		}\n
	}\n
\n
	function selectedPeriodForPayload(payload) {\n
		const periodId = Number(payload?.periodId);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function clearAssignmentFormSuccess() {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		if (!el || el.dataset.dcSuccess !== '1') {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.dataset.dcSuccess = '';\n
		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
		setFormFeedbackA11y(el, false);\n
	}\n
\n
	function showAssignmentFormSuccess(message, kind) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		const tone = kind === 'info' ? 'info' : 'success';\n
		el.hidden = false;\n
		el.textContent = String(message);\n
		el.dataset.dcSuccess = '1';\n
		el.classList.remove('dc-roster-form__feedback--error');\n
		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');\n
		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');\n
		setFormFeedbackA11y(el, false);\n
		if (submit) {\n
			submit.disabled = submit.dataset.dcLockedBySetup === '1';\n
		}\n
		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });\n
	}\n
\n
	function labelsForAssignmentPayload(payload) {\n
		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));\n
		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		const locationSelect = document.getElementById('dc-assignment-location');\n
		const employeeName = employee\n
			? String(employee.name || employee.displayName || '')\n
			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		const locationName = location\n
			? String(location.name || '')\n
			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		return { employeeName, locationName };\n
	}\n
\n
	function buildAssignmentSavedMessage(payload) {\n
		const { employeeName, locationName } = labelsForAssignmentPayload(payload);\n
		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;\n
		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)\n
			|| `${payload.startTime} – ${payload.endTime}`;\n
		return t(\n
			'dutycheck',\n
			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',\n
		)\n
			.replace('{employee}', employeeName || '—')\n
			.replace('{date}', String(dateLabel || ''))\n
			.replace('{times}', String(times))\n
			.replace('{location}', locationName || '—');\n
	}\n
\n
	function updateAssignmentFormFeedback(payload) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		if (el.dataset.dcSuccess === '1') {\n
			return;\n
		}\n
		if (typeof payload === 'string') {\n
			el.hidden = false;\n
			el.textContent = payload;\n
			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			el.classList.add('dc-roster-form__feedback--error');\n
			setFormFeedbackA11y(el, true);\n
			if (submit) {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		if (!payload) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			return;\n
		}\n
		let message = '';\n
		let isError = false;\n
		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');\n
			isError = true;\n
		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {\n
			const overlap = findOverlapAssignment(payload);\n
			if (overlap) {\n
				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;\n
				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)\n
					|| `${overlap.startTime} – ${overlap.endTime}`;\n
				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')\n
					.replace('{date}', String(when))\n
					.replace('{times}', String(times));\n
				isError = true;\n
			}\n
		}\n
		if (!message) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			if (submit && submit.dataset.dcLockedBySetup !== '1') {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		el.hidden = false;\n
		el.textContent = message;\n
		el.classList.toggle('dc-roster-form__feedback--error', isError);\n
		setFormFeedbackA11y(el, isError);\n
		if (submit && isError) {\n
			submit.disabled = true;\n
		}\n
	}\n
\n
	function refreshAssignmentFormEligibility() {\n
		refreshEmployeeSelectForDate();\n
		updateLocationTimezoneHint();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		updateAssignmentFormFeedback(readForm(form));\n
	}\n
\n
	function updateLocationTimezoneHint() {\n
		const hint = document.getElementById('dc-assignment-location-tz-hint');\n
		const select = document.getElementById('dc-assignment-location');\n
		if (!hint || !select) {\n
			return;\n
		}\n
		const locationId = Number(select.value);\n
		const location = state.locations.find((row) => Number(row.id) === locationId);\n
		const tz = location && String(location.timezone || '').trim();\n
		if (!tz) {\n
			hint.hidden = true;\n
			hint.replaceChildren();\n
			return;\n
		}\n
		const accountTz = D?.currentTimezone?.() || '';\n
		hint.textContent = t(\n
			'dutycheck',\n
			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',\n
		)\n
			.replace('{locationTimezone}', tz)\n
			.replace('{accountTimezone}', accountTz || 'UTC');\n
		hint.hidden = false;\n
	}\n
\n
	function fillSelect(id, items, options) {\n
		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});\n
		const select = document.getElementById(id);\n
		if (!select) return;\n
		const previous = String(select.value || '');\n
		select.replaceChildren();\n
		if (!items.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = opts.emptyText;\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const item of items) {\n
			const option = document.createElement('option');\n
			option.value = String(item.id);\n
			option.textContent = String(item[opts.labelKey] || item.name || item.id);\n
			select.appendChild(option);\n
		}\n
		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {\n
			select.value = previous;\n
		}\n
	}\n
\n
	function fillPeriodSwitcher(periods, selected) {\n
		const select = document.getElementById('dc-roster-period-switcher');\n
		if (!select) return;\n
		const statusLabelMap = {\n
			open: t('dutycheck', 'Open'),\n
			published: t('dutycheck', 'Published'),\n
			closed: t('dutycheck', 'Closed'),\n
		};\n
		select.replaceChildren();\n
		if (!periods.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = t('dutycheck', 'No periods yet');\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const period of periods) {\n
			const option = document.createElement('option');\n
			option.value = String(period.id);\n
			const start = D?.formatDisplayDate(period.startDate) || period.startDate;\n
			const end = D?.formatDisplayDate(period.endDate) || period.endDate;\n
			const statusKey = String(period.status || '').toLowerCase();\n
			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();\n
			option.textContent = `${start} – ${end} · ${status}`;\n
			select.appendChild(option);\n
		}\n
		if (selected) {\n
			select.value = String(selected);\n
		}\n
	}\n
\n
	function severityBadge(severity) {\n
		const sev = String(severity || 'info');\n
		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;\n
		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });\n
		badge.setAttribute('aria-label', label);\n
		return badge;\n
	}\n
\n
	function renderConflicts(conflicts) {\n
		const list = document.getElementById('dc-conflict-list');\n
		const summary = document.getElementById('dc-conflict-summary');\n
		if (!list) return;\n
		list.setAttribute('role', 'list');\n
		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));\n
		list.replaceChildren();\n
		const hard = conflicts.filter((c) => c?.severity === 'hard').length;\n
		const soft = conflicts.filter((c) => c?.severity === 'soft').length;\n
		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;\n
		if (summary) {\n
			summary.textContent = !conflicts.length\n
				? t('dutycheck', 'No planning issues found.')\n
				: (ConflictLabels\n
					? ConflictLabels.countsSummary(hard, soft, softUnack)\n
					: String(hard));\n
		}\n
		if (!conflicts.length) {\n
			return;\n
		}\n
		for (const conflict of conflicts) {\n
			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });\n
			li.appendChild(severityBadge(conflict?.severity));\n
			const conflictMessage = String(conflict?.message || '');\n
			const titleText = conflictMessage\n
				? translateConflictMessage(conflictMessage)\n
				: t('dutycheck', 'Unknown conflict');\n
			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;\n
			const body = create('div', { class: 'dc-conflict__body' });\n
			const title = create('span', { class: 'dc-conflict__title', text: titleText });\n
			title.id = titleId;\n
			body.appendChild(title);\n
			li.setAttribute('aria-labelledby', titleId);\n
			if (conflict?.acknowledged && conflict?.ackReason) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack',\n
					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),\n
				}));\n
			} else if (conflict?.ackInvalidated) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack-invalid',\n
					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),\n
				}));\n
			}\n
			li.appendChild(body);\n
\n
			const actions = create('div', { class: 'dc-conflict__actions' });\n
			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {\n
				const ackBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Confirm'),\n
				});\n
				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));\n
				actions.appendChild(ackBtn);\n
			}\n
			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();\n
			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {\n
				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()\n
					|| t('dutycheck', 'Open ArbeitszeitCheck');\n
				const recoveryLink = create('a', {\n
					class: 'button primary',\n
					href: recoveryUrl,\n
					text: recoveryLabel,\n
					attrs: {\n
						target: '_blank',\n
						rel: 'noopener noreferrer',\n
						'aria-label': recoveryLabel,\n
					},\n
				});\n
				actions.appendChild(recoveryLink);\n
			}\n
			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];\n
			if (conflict?.type === 'absence_collision' && assignmentIds.length) {\n
				const focusBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Show assignment'),\n
				});\n
				focusBtn.addEventListener('click', () => {\n
					revealAssignmentRow(Number(assignmentIds[0]));\n
				});\n
				actions.appendChild(focusBtn);\n
			}\n
			li.appendChild(actions);\n
			list.appendChild(li);\n
		}\n
	}\n
\n
	async function acknowledgeConflict(conflictId) {\n
		const reason = await C.promptReason({\n
			title: t('dutycheck', 'Confirm this exception'),\n
			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),\n
			confirmLabel: t('dutycheck', 'Confirm'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			minLength: 10,\n
		});\n
		if (reason === null) return;\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });\n
			render(response?.data || {});\n
			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');\n
			Msg.announce(ackMsg, 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	const VIRTUAL_FALLBACK = {\n
		DEFAULT_ROW_HEIGHT: 44,\n
		DEFAULT_OVERSCAN: 6,\n
		UNSIZED_WINDOW_ROWS: 24,\n
		visibleRange(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			const rowHeight = 44;\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: total * rowHeight,\n
				rowHeight,\n
			};\n
		},\n
		scrollTopToRevealIndex() {\n
			return 0;\n
		},\n
		pageStride() {\n
			return 1;\n
		},\n
		windowCaption(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			if (total <= 0) {\n
				return { mode: 'empty', from: 0, to: 0, total: 0 };\n
			}\n
			return { mode: 'all', from: 1, to: total, total };\n
		},\n
	};\n
\n
	function virtualApi() {\n
		const api = window.DutyCheckVirtualWindow;\n
		if (api\n
			&& typeof api.visibleRange === 'function'\n
			&& typeof api.scrollTopToRevealIndex === 'function'\n
			&& typeof api.windowCaption === 'function') {\n
			return api;\n
		}\n
		return VIRTUAL_FALLBACK;\n
	}\n
\n
	function cellSelectionKey(employeeId, dutyDate) {\n
		return `${Number(employeeId)}|${String(dutyDate)}`;\n
	}\n
\n
	function prefersReducedMotion() {\n
		try {\n
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n
		} catch (_) {\n
			return false;\n
		}\n
	}\n
\n
	function measureHeight(el) {\n
		if (!(el instanceof HTMLElement)) {\n
			return 0;\n
		}\n
		const h = el.getBoundingClientRect().height;\n
		return Number.isFinite(h) && h > 0 ? h : 0;\n
	}\n
\n
	function assignmentIndex(assignments) {\n
		const byKey = new Map();\n
		for (const a of assignments || []) {\n
			const key = cellSelectionKey(a.employeeId, a.dutyDate);\n
			if (!byKey.has(key)) {\n
				byKey.set(key, []);\n
			}\n
			byKey.get(key).push(a);\n
		}\n
		return byKey;\n
	}\n
\n
	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {\n
		const emptyPanel = document.getElementById('dc-roster-assignments-empty');\n
		if (emptyPanel) {\n
			emptyPanel.hidden = !showEmptyPanel;\n
		}\n
	}\n
\n
	const gridState = {\n
		view: 'grid',\n
		selected: new Set(), // `${employeeId}|${dutyDate}`\n
		focusRow: 0,\n
		focusCol: 0,\n
		paintAll: false,\n
		rowHeight: 44,\n
		headerHeight: 44,\n
		windowStart: -1,\n
		windowEnd: -1,\n
		paintedEmployeeCount: -1,\n
		paintedDateCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	const listState = {\n
		windowStart: -1,\n
		windowEnd: -1,\n
		rowHeight: 48,\n
		paintedCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	function renderAssignments(assignments) {\n
		paintListWindow({ assignments: assignments || [], force: true });\n
		renderRosterGrid(assignments || []);\n
		updateAddAssignmentControl();\n
	}\n
\n
	function buildAssignmentRow(a, periodOpen) {\n
		const overnightHintText = t('dutycheck', 'Continues into the next day.');\n
		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));\n
		const tr = create('tr');\n
		if (a?.id != null) {\n
			tr.dataset.assignmentId = String(a.id);\n
			tr.setAttribute('tabindex', '-1');\n
		}\n
		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;\n
		const tdDate = create('td', { text: date });\n
		tdDate.dataset.cell = t('dutycheck', 'Date');\n
		tr.appendChild(tdDate);\n
\n
		const tdStart = create('td');\n
		tdStart.dataset.cell = t('dutycheck', 'Start');\n
		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));\n
		tr.appendChild(tdStart);\n
\n
		const tdEnd = create('td');\n
		tdEnd.dataset.cell = t('dutycheck', 'End');\n
		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));\n
		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {\n
			tdEnd.title = overnightHintText;\n
			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));\n
		}\n
		tr.appendChild(tdEnd);\n
\n
		const rest = [\n
			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },\n
			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },\n
			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },\n
			{ label: t('dutycheck', 'Note'), value: a.note || '' },\n
		];\n
		for (const cell of rest) {\n
			const td = create('td', { text: String(cell.value ?? '') });\n
			td.dataset.cell = cell.label;\n
			tr.appendChild(td);\n
		}\n
		const tdActions = create('td');\n
		tdActions.dataset.cell = t('dutycheck', 'Actions');\n
		tdActions.className = 'dc-table__actions';\n
		if (periodOpen && a?.id) {\n
			const editBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Edit'),\n
			});\n
			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));\n
			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));\n
			const cancelBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Cancel shift'),\n
			});\n
			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));\n
			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));\n
			tdActions.appendChild(editBtn);\n
			tdActions.appendChild(cancelBtn);\n
		} else {\n
			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));\n
		}\n
		tr.appendChild(tdActions);\n
		return tr;\n
	}\n
\n
	function createListSpacer(px) {\n
		const tr = create('tr', { class: 'dc-virtual-spacer' });\n
		tr.setAttribute('aria-hidden', 'true');\n
		tr.style.height = `${Math.max(0, px)}px`;\n
		const td = create('td');\n
		td.colSpan = 8;\n
		td.style.height = `${Math.max(0, px)}px`;\n
		td.style.padding = '0';\n
		td.style.border = '0';\n
		td.style.lineHeight = '0';\n
		tr.appendChild(td);\n
		return tr;\n
	}\n
\n
	function updateListWindowStatus(range, total) {\n
		const el = document.getElementById('dc-roster-list-status');\n
		if (!el) {\n
			return;\n
		}\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		if (cap.mode === 'all') {\n
			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));\n
			return;\n
		}\n
		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')\n
			.replace('{from}', String(cap.from))\n
			.replace('{to}', String(cap.to))\n
			.replace('{total}', String(cap.total));\n
	}\n
\n
	function paintListWindow(options) {\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		if (!tbody) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		if (!assignments.length) {\n
			tbody.replaceChildren();\n
			setAssignmentsEmptyPanelVisible(true);\n
			listState.windowStart = 0;\n
			listState.windowEnd = 0;\n
			listState.paintedCount = 0;\n
			updateListWindowStatus({ start: 0, end: 0 }, 0);\n
			return;\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const scroller = document.getElementById('dc-assignments-table-wrap');\n
		const VW = virtualApi();\n
		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: assignments.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& listState.windowStart === range.start\n
			&& listState.windowEnd === range.end\n
			&& listState.paintedCount === assignments.length) {\n
			updateListWindowStatus(range, assignments.length);\n
			return;\n
		}\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const periodOpen = canAddAssignment();\n
		const frag = document.createDocumentFragment();\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createListSpacer(range.padBefore));\n
		}\n
		const windowRows = assignments.slice(range.start, range.end);\n
		for (const a of windowRows) {\n
			frag.appendChild(buildAssignmentRow(a, periodOpen));\n
		}\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createListSpacer(range.padAfter));\n
		}\n
		tbody.replaceChildren(frag);\n
		listState.windowStart = range.start;\n
		listState.windowEnd = range.end;\n
		listState.paintedCount = assignments.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
		}\n
		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');\n
		const measured = measureHeight(sample);\n
		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {\n
			listState.rowHeight = measured;\n
			listState.remeasuring = true;\n
			paintListWindow({ assignments, force: true });\n
			listState.remeasuring = false;\n
			return;\n
		}\n
		if (measured > 0) {\n
			listState.rowHeight = measured;\n
		}\n
		updateListWindowStatus(range, assignments.length);\n
	}\n
\n
	function selectedPeriodId() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const fromSwitcher = Number(switcher?.value || 0);\n
		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {\n
			return fromSwitcher;\n
		}\n
		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;\n
	}\n
\n
	function periodDateList() {\n
		const periodId = selectedPeriodId();\n
		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));\n
		if (!period?.startDate || !period?.endDate) {\n
			return [];\n
		}\n
		const out = [];\n
		const cursor = new Date(`${period.startDate}T00:00:00Z`);\n
		const end = new Date(`${period.endDate}T00:00:00Z`);\n
		while (cursor <= end) {\n
			out.push(cursor.toISOString().slice(0, 10));\n
			cursor.setUTCDate(cursor.getUTCDate() + 1);\n
			if (out.length > 62) {\n
				break;\n
			}\n
		}\n
		return out;\n
	}\n
\n
	function updateGridWindowStatus(range, total, dayCount) {\n
		const el = document.getElementById('dc-roster-grid-status');\n
		if (!el) {\n
			return;\n
		}\n
		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		let text = '';\n
		if (cap.mode === 'all') {\n
			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));\n
		} else {\n
			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')\n
				.replace('{from}', String(cap.from))\n
				.replace('{to}', String(cap.to))\n
				.replace('{total}', String(cap.total));\n
		}\n
		if (days > 10) {\n
			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');\n
		}\n
		el.textContent = text;\n
	}\n
\n
	function createGridSpacer(px) {\n
		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });\n
		el.setAttribute('aria-hidden', 'true');\n
		el.setAttribute('role', 'presentation');\n
		el.style.height = `${Math.max(0, px)}px`;\n
		return el;\n
	}\n
\n
	function setGridEmptyState(root, message) {\n
		root.removeAttribute('aria-rowcount');\n
		root.removeAttribute('aria-colcount');\n
		root.removeAttribute('tabindex');\n
		root.style.removeProperty('--dc-roster-day-count');\n
		root.style.removeProperty('--dc-roster-day-min');\n
		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');\n
		root.setAttribute('role', 'status');\n
		root.setAttribute('aria-live', 'polite');\n
		root.replaceChildren(create('p', {\n
			class: 'dc-roster-empty-state__text',\n
			text: message,\n
		}));\n
	}\n
\n
	function prepareGridForRows(root, employees, dates) {\n
		root.setAttribute('role', 'grid');\n
		root.removeAttribute('tabindex');\n
		root.removeAttribute('aria-live');\n
		root.setAttribute('aria-rowcount', String(employees.length + 1));\n
		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));\n
		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));\n
		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));\n
		root.style.setProperty('--dc-roster-day-min', dayMin);\n
		root.classList.toggle('dc-roster-grid--long', dates.length > 10);\n
		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);\n
	}\n
\n
	function buildColHead(day, colIdx, dayMeta) {\n
		const parts = dayMeta || D?.formatRosterColumnParts?.(day);\n
		const full = parts?.full || D?.formatDisplayDate?.(day) || day;\n
		let ariaLabel = parts?.ariaLabel || full;\n
		if (parts?.isToday) {\n
			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;\n
		}\n
		const weekdayShort = parts?.weekdayShort || '';\n
		const dayNum = parts?.day || '';\n
		const classNames = ['dc-roster-grid__colhead'];\n
		if (parts?.isWeekend) {\n
			classNames.push('dc-roster-grid__colhead--weekend');\n
		}\n
		if (parts?.isToday) {\n
			classNames.push('dc-roster-grid__colhead--today');\n
		}\n
		const children = [];\n
		if (weekdayShort) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));\n
		}\n
		if (dayNum) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));\n
		}\n
		const attrs = {\n
			'data-col': String(colIdx),\n
			'aria-colindex': String(colIdx + 2),\n
			title: full,\n
			'aria-label': ariaLabel,\n
		};\n
		if (parts?.iso) {\n
			attrs['data-duty-date'] = parts.iso;\n
		}\n
		if (!children.length) {\n
			return create('div', {\n
				class: classNames.join(' '),\n
				role: 'columnheader',\n
				text: full,\n
				attrs,\n
			});\n
		}\n
		return create('div', {\n
			class: classNames.join(' '),\n
			role: 'columnheader',\n
			attrs,\n
		}, children);\n
	}\n
\n
	function buildDutyDayMeta(dates) {\n
		const map = new Map();\n
		(dates || []).forEach((day) => {\n
			map.set(day, D?.formatRosterColumnParts?.(day) || null);\n
		});\n
		return map;\n
	}\n
\n
	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {\n
		const row = create('div', {\n
			class: 'dc-roster-grid__row',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': String(rowIdx + 2) },\n
		});\n
		const prefs = state.preferencesByEmployee[String(emp.id)] || [];\n
		const prefChips = prefs.slice(0, 2).map((p) => {\n
			const band = String(p.band || '');\n
			const label = band === 'early'\n
				? t('dutycheck', 'Früh')\n
				: (band === 'late' ? t('dutycheck', 'Spät') : band);\n
			return create('span', {\n
				class: 'dc-pref-chip dc-pref-chip--static',\n
				text: label,\n
				attrs: { title: label },\n
			});\n
		});\n
		const headKids = [\n
			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),\n
		];\n
		if (prefChips.length) {\n
			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));\n
		}\n
		row.appendChild(create('div', {\n
			class: 'dc-roster-grid__rowhead',\n
			role: 'rowheader',\n
			attrs: { 'aria-colindex': '1' },\n
		}, headKids));\n
		dates.forEach((day, colIdx) => {\n
			const key = cellSelectionKey(emp.id, day);\n
			const cellAssignments = byKey.get(key) || [];\n
			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);\n
			const cell = create('div', {\n
				class: 'dc-roster-grid__cell',\n
				role: 'gridcell',\n
				attrs: {\n
					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',\n
					'data-employee-id': String(emp.id),\n
					'data-duty-date': day,\n
					'data-row': String(rowIdx),\n
					'data-col': String(colIdx),\n
					'aria-colindex': String(colIdx + 2),\n
					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',\n
				},\n
			});\n
			if (dayParts?.isWeekend) {\n
				cell.classList.add('dc-roster-grid__cell--weekend');\n
			}\n
			if (dayParts?.isToday) {\n
				cell.classList.add('dc-roster-grid__cell--today');\n
			}\n
			if (cellAssignments.length) {\n
				cell.classList.add('dc-roster-grid__cell--filled');\n
				const first = cellAssignments[0];\n
				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;\n
				cell.appendChild(create('span', {\n
					class: 'dc-roster-grid__shift',\n
					text: label,\n
				}));\n
				const editLabel = canAddAssignment()\n
					? t('dutycheck', 'Edit assignment')\n
					: t('dutycheck', 'View assignment (read-only)');\n
				cell.setAttribute('aria-label', `${editLabel}: ${label}`);\n
				if (cellAssignments.length > 1) {\n
					cell.appendChild(create('span', {\n
						class: 'dc-roster-grid__more',\n
						text: `+${cellAssignments.length - 1}`,\n
					}));\n
				}\n
			} else {\n
				cell.classList.add('dc-roster-grid__cell--empty');\n
				const blackoutKey = `${emp.id}|${day}`;\n
				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];\n
				if (hasBlackout) {\n
					cell.classList.add('dc-roster-grid__cell--blackout');\n
					cell.appendChild(create('span', {\n
						class: 'dc-blackout-mark',\n
						attrs: { title: t('dutycheck', 'Kann nicht') },\n
					}, [\n
						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),\n
						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),\n
					]));\n
					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));\n
				} else {\n
					cell.setAttribute(\n
						'aria-label',\n
						canAddAssignment()\n
							? t('dutycheck', 'Empty cell — Space to select for bulk fill')\n
							: t('dutycheck', 'Empty cell'),\n
					);\n
				}\n
			}\n
			if (gridState.selected.has(key)) {\n
				cell.classList.add('is-selected');\n
			}\n
			row.appendChild(cell);\n
		});\n
		return row;\n
	}\n
\n
	function syncVisibleGridChrome() {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {\n
			const empId = cell.getAttribute('data-employee-id');\n
			const day = cell.getAttribute('data-duty-date');\n
			const key = cellSelectionKey(empId, day);\n
			const selected = gridState.selected.has(key);\n
			cell.classList.toggle('is-selected', selected);\n
			cell.setAttribute('aria-selected', selected ? 'true' : 'false');\n
			const rowIdx = Number(cell.getAttribute('data-row'));\n
			const colIdx = Number(cell.getAttribute('data-col'));\n
			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;\n
			cell.setAttribute('tabindex', isFocus ? '0' : '-1');\n
		});\n
	}\n
\n
	function focusGridCellIfVisible(root) {\n
		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
		if (node instanceof HTMLElement) {\n
			node.scrollIntoView({\n
				block: 'nearest',\n
				inline: 'nearest',\n
				behavior: 'auto',\n
			});\n
			node.focus();\n
		}\n
	}\n
\n
	function activateGridCell(cell) {\n
		if (!(cell instanceof HTMLElement)) {\n
			return;\n
		}\n
		const rowIdx = Number(cell.getAttribute('data-row'));\n
		const colIdx = Number(cell.getAttribute('data-col'));\n
		if (Number.isInteger(rowIdx) && rowIdx >= 0) {\n
			gridState.focusRow = rowIdx;\n
		}\n
		if (Number.isInteger(colIdx) && colIdx >= 0) {\n
			gridState.focusCol = colIdx;\n
		}\n
		const empId = cell.getAttribute('data-employee-id');\n
		const day = cell.getAttribute('data-duty-date');\n
		const key = cellSelectionKey(empId, day);\n
		const filled = cell.classList.contains('dc-roster-grid__cell--filled');\n
		if (!filled) {\n
			if (canAddAssignment()) {\n
				toggleGridSelection(key);\n
				syncVisibleGridChrome();\n
				updateBulkBar();\n
			}\n
			return;\n
		}\n
		const byKey = assignmentIndex(state.assignments || []);\n
		const cellAssignments = byKey.get(key) || [];\n
		if (canAddAssignment() && cellAssignments[0]) {\n
			openAssignmentEditModal(cellAssignments[0], cell);\n
			return;\n
		}\n
		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');\n
	}\n
\n
	function ensureGridFocusVisible() {\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const employees = state.employees || [];\n
		if (!scroller || !employees.length) {\n
			return;\n
		}\n
		const VW = virtualApi();\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);\n
		const next = VW.scrollTopToRevealIndex({\n
			index: gridState.focusRow,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop: scroller.scrollTop,\n
			total: employees.length,\n
		});\n
		if (Math.abs(next - scroller.scrollTop) > 0.5) {\n
			scroller.scrollTop = next;\n
		}\n
	}\n
\n
	function renderRosterGrid(assignments) {\n
		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });\n
	}\n
\n
	function paintGridWindow(options) {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		const employees = state.employees || [];\n
		const dates = periodDateList();\n
		const byKey = assignmentIndex(assignments);\n
\n
		if (!employees.length || !dates.length) {\n
			setGridEmptyState(\n
				root,\n
				t('dutycheck', 'Add employees and an open period to use the planning grid.'),\n
			);\n
			gridState.windowStart = 0;\n
			gridState.windowEnd = 0;\n
			gridState.paintedEmployeeCount = employees.length;\n
			gridState.paintedDateCount = dates.length;\n
			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		prepareGridForRows(root, employees, dates);\n
		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));\n
		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));\n
\n
		const VW = virtualApi();\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = scroller && gridState.view === 'grid'\n
			? Math.max(0, scroller.clientHeight - headerHeight)\n
			: 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: employees.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& gridState.windowStart === range.start\n
			&& gridState.windowEnd === range.end\n
			&& gridState.paintedEmployeeCount === employees.length\n
			&& gridState.paintedDateCount === dates.length) {\n
			syncVisibleGridChrome();\n
			updateGridWindowStatus(range, employees.length, dates.length);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const savedLeft = scroller ? scroller.scrollLeft : 0;\n
		const dayMetaByDate = buildDutyDayMeta(dates);\n
		const frag = document.createDocumentFragment();\n
		const header = create('div', {\n
			class: 'dc-roster-grid__row dc-roster-grid__row--head',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': '1' },\n
		});\n
		header.appendChild(create('div', {\n
			class: 'dc-roster-grid__corner',\n
			role: 'columnheader',\n
			text: t('dutycheck', 'Person'),\n
			attrs: { 'aria-colindex': '1' },\n
		}));\n
		dates.forEach((day, colIdx) => {\n
			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));\n
		});\n
		frag.appendChild(header);\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createGridSpacer(range.padBefore));\n
		}\n
		const windowEmployees = employees.slice(range.start, range.end);\n
		windowEmployees.forEach((emp, offset) => {\n
			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));\n
		});\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createGridSpacer(range.padAfter));\n
		}\n
		root.replaceChildren(frag);\n
		gridState.windowStart = range.start;\n
		gridState.windowEnd = range.end;\n
		gridState.paintedEmployeeCount = employees.length;\n
		gridState.paintedDateCount = dates.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
			scroller.scrollLeft = savedLeft;\n
		}\n
		const headerEl = root.querySelector('.dc-roster-grid__row--head');\n
		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');\n
		const measuredHeader = measureHeight(headerEl);\n
		const measuredRow = measureHeight(bodyRow);\n
		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)\n
			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))\n
			&& !gridState.remeasuring) {\n
			if (measuredHeader > 0) {\n
				gridState.headerHeight = measuredHeader;\n
			}\n
			if (measuredRow > 0) {\n
				gridState.rowHeight = measuredRow;\n
			}\n
			gridState.remeasuring = true;\n
			paintGridWindow({ assignments, force: true });\n
			gridState.remeasuring = false;\n
			return;\n
		}\n
		if (measuredHeader > 0) {\n
			gridState.headerHeight = measuredHeader;\n
		}\n
		if (measuredRow > 0) {\n
			gridState.rowHeight = measuredRow;\n
		}\n
		bindGridInteractions(root);\n
		updateGridWindowStatus(range, employees.length, dates.length);\n
		updateBulkBar();\n
	}\n
\n
	function bindGridInteractions(root) {\n
		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {\n
			return;\n
		}\n
		root.dataset.dcGridBound = '1';\n
		root.addEventListener('click', (ev) => {\n
			const cell = ev.target.closest('[role="gridcell"]');\n
			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {\n
				return;\n
			}\n
			activateGridCell(cell);\n
		});\n
		root.addEventListener('keydown', (ev) => {\n
			const employees = state.employees || [];\n
			const dates = periodDateList();\n
			const rowCount = employees.length;\n
			const colCount = dates.length;\n
			if (!rowCount || !colCount) {\n
				return;\n
			}\n
			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar'];\n
			if (!gridKeys.includes(ev.key)) {\n
				return;\n
			}\n
			ev.preventDefault();\n
			if (ev.key === 'ArrowUp') {\n
				gridState.focusRow = Math.max(0, gridState.focusRow - 1);\n
			} else if (ev.key === 'ArrowDown') {\n
				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);\n
			} else if (ev.key === 'ArrowLeft') {\n
				gridState.focusCol = Math.max(0, gridState.focusCol - 1);\n
			} else if (ev.key === 'ArrowRight') {\n
				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);\n
			} else if (ev.key === 'Home') {\n
				gridState.focusCol = 0;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = 0;\n
				}\n
			} else if (ev.key === 'End') {\n
				gridState.focusCol = colCount - 1;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = rowCount - 1;\n
				}\n
			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {\n
				const VW = virtualApi();\n
				const scroller = document.getElementById('dc-roster-grid-scroller');\n
				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
				const viewportHeight = scroller\n
					? Math.max(rowHeight, scroller.clientHeight - headerHeight)\n
					: rowHeight;\n
				const stride = typeof VW.pageStride === 'function'\n
					? VW.pageStride({ rowHeight, viewportHeight })\n
					: 1;\n
				if (ev.key === 'PageDown') {\n
					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);\n
				} else {\n
					gridState.focusRow = Math.max(0, gridState.focusRow - stride);\n
				}\n
			} else if (ev.key === 'Enter') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				activateGridCell(cell);\n
				focusGridCellIfVisible(root);\n
				return;\n
			} else if (ev.key === ' ' || ev.key === 'Spacebar') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				const empId = cell?.getAttribute('data-employee-id');\n
				const day = cell?.getAttribute('data-duty-date');\n
				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');\n
				if (empId && day && isEmpty && canAddAssignment()) {\n
					toggleGridSelection(cellSelectionKey(empId, day));\n
					syncVisibleGridChrome();\n
					updateBulkBar();\n
				}\n
				focusGridCellIfVisible(root);\n
				return;\n
			}\n
			ensureGridFocusVisible();\n
			paintGridWindow({ force: false });\n
			focusGridCellIfVisible(root);\n
		});\n
	}\n
\n
	function toggleGridSelection(key) {\n
		if (gridState.selected.has(key)) {\n
			gridState.selected.delete(key);\n
		} else {\n
			gridState.selected.add(key);\n
		}\n
	}\n
\n
	function updateBulkBar() {\n
		const bar = document.getElementById('dc-roster-bulk-bar');\n
		const countEl = document.getElementById('dc-roster-bulk-count');\n
		if (!bar || !countEl) {\n
			return;\n
		}\n
		const n = gridState.selected.size;\n
		if (n < 1 || !canAddAssignment()) {\n
			bar.hidden = true;\n
			return;\n
		}\n
		bar.hidden = false;\n
		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (sel && !(sel.options && sel.options.length)) {\n
			populateBulkTemplates();\n
		}\n
	}\n
\n
	async function populateBulkTemplates() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (!sel) {\n
			return;\n
		}\n
		await loadTemplates();\n
		sel.replaceChildren();\n
		const templates = state.templates || [];\n
		if (!templates.length) {\n
			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));\n
			return;\n
		}\n
		for (const tpl of templates) {\n
			sel.appendChild(create('option', {\n
				value: String(tpl.id),\n
				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,\n
			}));\n
		}\n
	}\n
\n
	function revealAssignmentRow(id) {\n
		const assignmentId = Number(id);\n
		const assignments = state.assignments || [];\n
		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);\n
		if (idx < 0) {\n
			return;\n
		}\n
		applyRosterViewChrome('list');\n
		const run = () => {\n
			const scroller = document.getElementById('dc-assignments-table-wrap');\n
			const VW = virtualApi();\n
			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
			if (scroller) {\n
				scroller.scrollTop = VW.scrollTopToRevealIndex({\n
					index: idx,\n
					rowHeight,\n
					viewportHeight: Math.max(rowHeight, scroller.clientHeight),\n
					scrollTop: scroller.scrollTop,\n
					total: assignments.length,\n
				});\n
			}\n
			paintListWindow({ assignments, force: true });\n
			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);\n
			if (row instanceof HTMLElement) {\n
				row.scrollIntoView({\n
					block: 'nearest',\n
					behavior: 'auto',\n
				});\n
				row.focus();\n
				row.classList.add('dc-row--flash');\n
				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);\n
			}\n
		};\n
		window.requestAnimationFrame(run);\n
	}\n
\n
	function setRosterPaintAll(paintAll) {\n
		gridState.paintAll = paintAll === true;\n
		paintListWindow({ force: true });\n
		paintGridWindow({ force: true });\n
	}\n
\n
	function wireRosterVirtualization() {\n
		const gridScroller = document.getElementById('dc-roster-grid-scroller');\n
		const listScroller = document.getElementById('dc-assignments-table-wrap');\n
		let gridRaf = 0;\n
		let listRaf = 0;\n
		if (gridScroller) {\n
			gridScroller.addEventListener('scroll', () => {\n
				if (gridRaf) {\n
					return;\n
				}\n
				gridRaf = window.requestAnimationFrame(() => {\n
					gridRaf = 0;\n
					paintGridWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		if (listScroller) {\n
			listScroller.addEventListener('scroll', () => {\n
				if (listRaf) {\n
					return;\n
				}\n
				listRaf = window.requestAnimationFrame(() => {\n
					listRaf = 0;\n
					paintListWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		const resize = () => {\n
			if (gridState.view === 'grid') {\n
				paintGridWindow({ force: true });\n
			} else {\n
				paintListWindow({ force: true });\n
			}\n
		};\n
		if (typeof ResizeObserver === 'function') {\n
			const ro = new ResizeObserver(resize);\n
			if (gridScroller) {\n
				ro.observe(gridScroller);\n
			}\n
			if (listScroller) {\n
				ro.observe(listScroller);\n
			}\n
		} else {\n
			window.addEventListener('resize', resize);\n
		}\n
		window.addEventListener('beforeprint', () => setRosterPaintAll(true));\n
		window.addEventListener('afterprint', () => setRosterPaintAll(false));\n
		if (typeof window.matchMedia === 'function') {\n
			try {\n
				const printMq = window.matchMedia('print');\n
				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);\n
				if (typeof printMq.addEventListener === 'function') {\n
					printMq.addEventListener('change', onPrintMq);\n
				} else if (typeof printMq.addListener === 'function') {\n
					printMq.addListener(onPrintMq);\n
				}\n
			} catch (_) {\n
				/* print media query is not available in every test/jsdom shim */\n
			}\n
		}\n
	}\n
\n
	function applyRosterViewChrome(view) {\n
		gridState.view = view === 'list' ? 'list' : 'grid';\n
		const gridWrap = document.getElementById('dc-roster-grid-wrap');\n
		const listPanel = document.getElementById('dc-roster-list-panel');\n
		const gridBtn = document.getElementById('dc-roster-view-grid');\n
		const listBtn = document.getElementById('dc-roster-view-list');\n
		if (gridWrap) {\n
			gridWrap.hidden = gridState.view !== 'grid';\n
		}\n
		if (listPanel) {\n
			listPanel.hidden = gridState.view !== 'list';\n
		}\n
		if (gridBtn) {\n
			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');\n
		}\n
		if (listBtn) {\n
			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');\n
		}\n
	}\n
\n
	function setRosterView(view) {\n
		applyRosterViewChrome(view);\n
		window.requestAnimationFrame(() => {\n
			if (gridState.view === 'list') {\n
				paintListWindow({ force: true });\n
			} else {\n
				paintGridWindow({ force: true });\n
			}\n
		});\n
	}\n
\n
	async function applyBulkFromTemplate() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		const tplId = Number(sel?.value || 0);\n
		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);\n
		if (!tpl || gridState.selected.size < 1) {\n
			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');\n
			return;\n
		}\n
		if (!Number(tpl.locationId || 0)) {\n
			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');\n
			return;\n
		}\n
		const periodId = Number(selectedPeriodId() || 0);\n
		let ok = 0;\n
		let fail = 0;\n
		for (const key of Array.from(gridState.selected)) {\n
			const sep = key.indexOf('|');\n
			if (sep < 1) {\n
				fail += 1;\n
				continue;\n
			}\n
			const employeeId = Number(key.slice(0, sep));\n
			const dutyDate = key.slice(sep + 1);\n
			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {\n
				fail += 1;\n
				continue;\n
			}\n
			const locationId = Number(tpl.locationId || 0);\n
			if (!locationId) {\n
				fail += 1;\n
				continue;\n
			}\n
			try {\n
				await submitAssignment({\n
					periodId,\n
					employeeId: Number(employeeId),\n
					locationId,\n
					dutyDate,\n
					startTime: String(tpl.startTime).slice(0, 5),\n
					endTime: String(tpl.endTime).slice(0, 5),\n
					breakMinutes: Number(tpl.breakMinutes || 0),\n
					note: '',\n
					assignmentId: 0,\n
				}, true);\n
				ok += 1;\n
			} catch {\n
				fail += 1;\n
			}\n
		}\n
		gridState.selected.clear();\n
		await loadRoster(periodId || null);\n
		Msg.announce(\n
			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')\n
				.replace('{ok}', String(ok))\n
				.replace('{fail}', String(fail)),\n
			fail ? 'warning' : 'info',\n
		);\n
	}\n
\n
	function uniqueConflictTypes(conflicts) {\n
		const types = new Set();\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity === 'soft' && conflict?.type) {\n
				types.add(String(conflict.type));\n
			}\n
		}\n
		return Array.from(types);\n
	}\n
\n
	function softConflictSummary(conflicts) {\n
		const lines = [];\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity !== 'soft') {\n
				continue;\n
			}\n
			const raw = String(conflict?.message || '');\n
			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');\n
			if (text && !lines.includes(text)) {\n
				lines.push(text);\n
			}\n
		}\n
		return lines.join('\n');\n
	}\n
\n
	function errorCodeFrom(err) {\n
		return String(err?.code || err?.payload?.error?.code || '');\n
	}\n
\n
	function syncAssignmentPeriodFromSwitcher() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (!switcher || !periodHidden) {\n
			return;\n
		}\n
		const selected = Number(switcher.value);\n
		if (Number.isInteger(selected) && selected > 0) {\n
			periodHidden.value = String(selected);\n
		}\n
	}\n
\n
	function announceAssignmentSaveError(err) {\n
		clearAssignmentFormSuccess();\n
		const friendly = errorMessageFor(err);\n
		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');\n
		updateAssignmentFormFeedback(message);\n
		Msg.announce(message, 'error');\n
	}\n
\n
	async function submitAssignment(payload, retryWithAck = true) {\n
		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		try {\n
			if (editId > 0) {\n
				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);\n
			}\n
			return await Api.post('/apps/dutycheck/api/assignments', payload);\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Save with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return submitAssignment({ ...payload, acknowledgements }, false);\n
			}\n
			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Employee cannot work (blackout)'),\n
					label: t(\n
						'dutycheck',\n
						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',\n
					),\n
					confirmLabel: t('dutycheck', 'Schedule anyway'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
					maxLength: 200,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	function setAssignmentFormBusy(form, busy) {\n
		if (!form) return;\n
		const submit = form.querySelector('button[type="submit"]');\n
		const clear = document.getElementById('dc-assignment-form-clear');\n
		if (submit) {\n
			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';\n
			if (busy) {\n
				submit.setAttribute('aria-busy', 'true');\n
			} else {\n
				submit.removeAttribute('aria-busy');\n
			}\n
		}\n
		if (clear) {\n
			clear.disabled = busy;\n
		}\n
	}\n
\n
	function renderSetupCallout(data) {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		const list = document.getElementById('dc-roster-setup-checklist');\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!callout || !list || !form) return;\n
		const urls = readUrls();\n
		const missing = [];\n
		if (!state.periods.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Create a planning period.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		if (!state.employees.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active employee.'),\n
				url: urls.employees,\n
				cta: t('dutycheck', 'Go to Employees'),\n
			});\n
		}\n
		if (!state.locations.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active location.'),\n
				url: urls.locations,\n
				cta: t('dutycheck', 'Go to Locations'),\n
			});\n
		}\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));\n
		const periodIsOpen = (selectedPeriod?.status || '') === 'open';\n
		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {\n
			missing.push({\n
				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		list.replaceChildren();\n
		if (!missing.length) {\n
			callout.hidden = true;\n
			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {\n
				if (el.dataset.dcLockedBySetup === '1') {\n
					el.disabled = false;\n
					delete el.dataset.dcLockedBySetup;\n
				}\n
			});\n
			updateAddAssignmentControl();\n
			return;\n
		}\n
		for (const item of missing) {\n
			const li = create('li');\n
			li.appendChild(document.createTextNode(item.text + ' '));\n
			if (item.url) {\n
				li.appendChild(create('a', {\n
					class: 'button',\n
					href: item.url,\n
					text: item.cta,\n
				}));\n
			}\n
			list.appendChild(li);\n
		}\n
		callout.hidden = false;\n
		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {\n
			if (!el.disabled) {\n
				el.dataset.dcLockedBySetup = '1';\n
				el.disabled = true;\n
			}\n
		});\n
		updateAddAssignmentControl();\n
	}\n
\n
	function readUrls() {\n
		try {\n
			const raw = document.getElementById('app-content')?.dataset?.dcUrls;\n
			if (!raw) return {};\n
			return JSON.parse(raw) || {};\n
		} catch (_) {\n
			return {};\n
		}\n
	}\n
\n
	function restoreAssignmentFormPanel() {\n
		const host = document.getElementById('dc-assignment-form-host');\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (host && panel && panel.parentElement !== host) {\n
			host.appendChild(panel);\n
		}\n
	}\n
\n
	function selectedPeriodFromState() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;\n
		const periodId = Number(raw);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function isAssignmentSetupBlocking() {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		return !!(callout && !callout.hidden);\n
	}\n
\n
	function canAddAssignment() {\n
		if (!state.canCreateAssignments) {\n
			return false;\n
		}\n
		if (!state.periods.length || !state.employees.length || !state.locations.length) {\n
			return false;\n
		}\n
		const period = selectedPeriodFromState();\n
		return !!(period && String(period.status || '').toLowerCase() === 'open');\n
	}\n
\n
	function addAssignmentDisabledReason() {\n
		if (!state.periods.length) {\n
			return t('dutycheck', 'Create a planning period first.');\n
		}\n
		if (!state.employees.length) {\n
			return t('dutycheck', 'Add at least one active employee before planning shifts.');\n
		}\n
		if (!state.locations.length) {\n
			return t('dutycheck', 'Add at least one active location before planning shifts.');\n
		}\n
		const period = selectedPeriodFromState();\n
		if (!period) {\n
			return t('dutycheck', 'Select a period before adding assignments.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!state.canCreateAssignments) {\n
			return t('dutycheck', 'You cannot add assignments for this period.');\n
		}\n
		return t('dutycheck', 'Adding assignments is not available right now.');\n
	}\n
\n
	function updateAddAssignmentControl() {\n
		const allowed = canAddAssignment();\n
		const reason = addAssignmentDisabledReason();\n
		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {\n
			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.\n
			btn.hidden = !allowed;\n
			btn.disabled = !allowed;\n
			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');\n
			btn.classList.toggle('dc-is-disabled', !allowed);\n
			if (allowed) {\n
				btn.removeAttribute('title');\n
			} else {\n
				btn.setAttribute('title', reason);\n
			}\n
		});\n
		const emptyHint = document.getElementById('dc-roster-empty-add-hint');\n
		if (emptyHint) {\n
			emptyHint.textContent = allowed\n
				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')\n
				: reason;\n
		}\n
	}\n
\n
	function clearAssignmentsSectionSuccess() {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.classList.remove('dc-roster-flash--visible');\n
	}\n
\n
	function showAssignmentsSectionSuccess(message) {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.textContent = String(message);\n
		el.hidden = false;\n
		el.classList.add('dc-roster-flash--visible');\n
	}\n
\n
	function prepareAssignmentFormForModal() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		clearAssignmentFormSuccess();\n
		updateAssignmentFormFeedback(null);\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate && period?.endDate) {\n
			dateInput.min = String(period.startDate);\n
			dateInput.max = String(period.endDate);\n
			if (!dateInput.value) {\n
				dateInput.value = String(period.startDate);\n
			}\n
		}\n
		refreshAssignmentFormEligibility();\n
		applyDefaultBreakToForm();\n
		wireBreakPrefillHint();\n
		fillTemplateSelect();\n
		D?.applyLocaleToTemporalInputs(form);\n
	}\n
\n
	function resetAssignmentFormAfterSave(data) {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		form.reset();\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden && data?.selectedPeriodId) {\n
			periodHidden.value = String(data.selectedPeriodId);\n
		}\n
		applyDefaultBreakToForm();\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate) {\n
			dateInput.value = String(period.startDate);\n
		}\n
		D?.applyLocaleToTemporalInputs(form);\n
		updateAssignmentFormFeedback(null);\n
	}\n
\n
	async function performAssignmentSave() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return false;\n
		}\n
		if (isAssignmentSetupBlocking()) {\n
			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(msg);\n
			Msg.announce(msg, 'error');\n
			return false;\n
		}\n
		const payload = readForm(form);\n
		const validation = validate(payload);\n
		if (validation) {\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(validation);\n
			Msg.announce(validation, 'error');\n
			focusFirstInvalidAssignmentField(payload);\n
			return false;\n
		}\n
		updateAssignmentFormFeedback(null);\n
		setAssignmentFormBusy(form, true);\n
		try {\n
			const response = await submitAssignment(payload, true);\n
			render(response?.data || {});\n
			const savedMsg = buildAssignmentSavedMessage(payload);\n
			clearAssignmentFormSuccess();\n
			showAssignmentsSectionSuccess(savedMsg);\n
			Msg.announce(savedMsg, 'success');\n
			rememberBreakMinutes(payload.breakMinutes);\n
			resetAssignmentFormAfterSave(response?.data || {});\n
			return true;\n
		} catch (err) {\n
			announceAssignmentSaveError(err);\n
			return false;\n
		} finally {\n
			setAssignmentFormBusy(form, false);\n
		}\n
	}\n
\n
	function syncAssignmentModalPrimary(instance) {\n
		if (!instance?.primaryBtn) {\n
			return;\n
		}\n
		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();\n
		instance.primaryBtn.disabled = blocked;\n
		if (blocked) {\n
			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());\n
		} else {\n
			instance.primaryBtn.removeAttribute('title');\n
		}\n
	}\n
\n
	async function openAssignmentCreateModal(triggerEl) {\n
		if (!canAddAssignment()) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
			return;\n
		}\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		renderSetupCallout(state.lastRosterData || {});\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Create assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save assignment'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => {\n
				const ok = await performAssignmentSave();\n
				return ok;\n
			},\n
			onClose: (result) => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				if (!result) {\n
					clearAssignmentFormSuccess();\n
					return;\n
				}\n
				const addBtn = document.getElementById('dc-roster-add-assignment');\n
				if (addBtn && typeof addBtn.focus === 'function') {\n
					addBtn.focus();\n
				}\n
			},\n
			onCancel: () => {\n
				clearAssignmentFormSuccess();\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
		const form = document.getElementById('dc-assignment-form');\n
		if (form && !form.dataset.dcModalSyncWired) {\n
			form.dataset.dcModalSyncWired = '1';\n
			form.addEventListener('change', () => {\n
				if (assignmentModalInstance?._open) {\n
					syncAssignmentModalPrimary(assignmentModalInstance);\n
				}\n
			});\n
		}\n
	}\n
\n
	async function openAssignmentEditModal(assignment, triggerEl) {\n
		if (!canAddAssignment() || !assignment?.id) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			return;\n
		}\n
		state.editingAssignmentId = Number(assignment.id);\n
		state.editingAssignmentVersion = Number(assignment.version ?? 0);\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = String(assignment.id);\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput) {\n
			dateInput.value = String(assignment.dutyDate || '');\n
		}\n
		refreshAssignmentFormEligibility();\n
		const emp = document.getElementById('dc-assignment-employee');\n
		if (emp) {\n
			emp.value = String(assignment.employeeId || '');\n
		}\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (loc) {\n
			loc.value = String(assignment.locationId || '');\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		if (start) {\n
			start.value = String(assignment.startTime || '').slice(0, 5);\n
		}\n
		const end = document.getElementById('dc-assignment-end');\n
		if (end) {\n
			end.value = String(assignment.endTime || '').slice(0, 5);\n
		}\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(assignment.breakMinutes ?? 0);\n
		}\n
		const note = document.getElementById('dc-assignment-note');\n
		if (note) {\n
			note.value = String(assignment.note || '');\n
		}\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Edit assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save changes'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => performAssignmentSave(),\n
			onClose: () => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				clearAssignmentFormSuccess();\n
				if (triggerEl && typeof triggerEl.focus === 'function') {\n
					triggerEl.focus();\n
				}\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
	}\n
\n
	async function cancelAssignmentRow(assignment, triggerEl) {\n
		if (!assignment?.id || !canAddAssignment()) {\n
			return;\n
		}\n
		const ok = window.confirm(\n
			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),\n
		);\n
		if (!ok) {\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});\n
			render(response?.data || {});\n
			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));\n
			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		} finally {\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
		}\n
	}\n
\n
	let templatesInflight = null;\n
\n
	async function ensureAssignmentModalPrereqs() {\n
		const jobs = [loadTemplates()];\n
		if (state.planningDefaultsFresh !== true) {\n
			jobs.push(refreshPlanningDefaultFromServer());\n
		}\n
		await Promise.all(jobs);\n
	}\n
\n
	async function loadTemplates() {\n
		if (state.templatesLoaded) {\n
			fillTemplateSelect();\n
			return;\n
		}\n
		if (templatesInflight) {\n
			await templatesInflight;\n
			fillTemplateSelect();\n
			return;\n
		}\n
		templatesInflight = (async () => {\n
			try {\n
				const response = await Api.get('/apps/dutycheck/api/templates', {});\n
				state.templates = Array.isArray(response?.data) ? response.data : [];\n
				state.templatesLoaded = true;\n
			} catch (_) {\n
				state.templates = [];\n
				state.templatesLoaded = false;\n
			} finally {\n
				templatesInflight = null;\n
			}\n
		})();\n
		await templatesInflight;\n
		fillTemplateSelect();\n
	}\n
\n
	function fillTemplateSelect() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select) {\n
			return;\n
		}\n
		const current = select.value;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));\n
		for (const tpl of state.templates) {\n
			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;\n
			select.appendChild(create('option', { value: String(tpl.id), text: label }));\n
		}\n
		if (current && [...select.options].some((o) => o.value === current)) {\n
			select.value = current;\n
		}\n
	}\n
\n
	function applySelectedTemplate() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select || !select.value) {\n
			return;\n
		}\n
		const tpl = state.templates.find((t) => String(t.id) === String(select.value));\n
		if (!tpl) {\n
			return;\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		const end = document.getElementById('dc-assignment-end');\n
		const br = document.getElementById('dc-assignment-break');\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (start) start.value = String(tpl.startTime || '').slice(0, 5);\n
		if (end) end.value = String(tpl.endTime || '').slice(0, 5);\n
		if (br) br.value = String(tpl.breakMinutes ?? 0);\n
		if (loc && tpl.locationId) loc.value = String(tpl.locationId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		refreshAssignmentFormEligibility();\n
	}\n
\n
	function refreshAcknowledgeStats(periodId) {\n
		const el = document.getElementById('dc-roster-ack-stats');\n
		if (!el) {\n
			return;\n
		}\n
		const id = Number(periodId);\n
		if (!Number.isInteger(id) || id <= 0) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === id);\n
		const status = String(period?.status || '').toLowerCase();\n
		if (status !== 'published' && status !== 'closed') {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const rows = Array.isArray(state.assignments) ? state.assignments : [];\n
		let total = 0;\n
		let acked = 0;\n
		for (const row of rows) {\n
			if (String(row?.status || '').toLowerCase() === 'cancelled') {\n
				continue;\n
			}\n
			total += 1;\n
			if (row?.acknowledged || row?.acknowledgedAt) {\n
				acked += 1;\n
			}\n
		}\n
		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;\n
		el.hidden = false;\n
		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')\n
			.replace('{acked}', String(acked))\n
			.replace('{total}', String(total))\n
			.replace('{pct}', String(pct));\n
	}\n
\n
	function fillCopySourceSelect(selectedPeriodId) {\n
		const select = document.getElementById('dc-roster-copy-source');\n
		const wrap = document.getElementById('dc-roster-copy-period');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!select || !wrap) {\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));\n
		const open = period && String(period.status || '').toLowerCase() === 'open';\n
		wrap.hidden = !open;\n
		state.copyPreviewReady = false;\n
		if (applyBtn) applyBtn.disabled = true;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));\n
		for (const p of state.periods) {\n
			if (Number(p.id) === Number(selectedPeriodId)) {\n
				continue;\n
			}\n
			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;\n
			select.appendChild(create('option', { value: String(p.id), text: label }));\n
		}\n
	}\n
\n
	async function runCopyPeriod(dryRun) {\n
		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);\n
		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);\n
		const status = document.getElementById('dc-roster-copy-status');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!targetId || !sourceId) {\n
			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {\n
				sourcePeriodId: sourceId,\n
				dryRun: !!dryRun,\n
			});\n
			const data = response?.data || {};\n
			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);\n
			const skipped = Number(data.skipped ?? 0);\n
			const msg = dryRun\n
				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')\n
					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))\n
					.replace('{skipped}', String(data.wouldSkip ?? skipped))\n
				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')\n
					.replace('{created}', String(created));\n
			if (status) {\n
				status.hidden = false;\n
				status.textContent = msg;\n
			}\n
			Msg.announce(msg, dryRun ? 'info' : 'success');\n
			if (dryRun) {\n
				state.copyPreviewReady = true;\n
				if (applyBtn) applyBtn.disabled = false;\n
			} else {\n
				state.copyPreviewReady = false;\n
				if (applyBtn) applyBtn.disabled = true;\n
				render(data.roster || data || {});\n
				if (!data.roster && data.assignments) {\n
					render(data);\n
				} else if (!data.roster) {\n
					await loadRoster(targetId);\n
				}\n
			}\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	function updateAdminExportLinks(selectedPeriodId) {\n
		const csv = document.getElementById('dc-roster-export-csv');\n
		const printLink = document.getElementById('dc-roster-export-print');\n
		if (!csv || !printLink) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		const baseCsv = String(urls.rosterExportCsv || '');\n
		const basePrint = String(urls.rosterPrint || '');\n
		const id = Number(selectedPeriodId);\n
		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;\n
		if (!ok) {\n
			csv.href = '#dc-roster-period-section';\n
			printLink.href = '#dc-roster-period-section';\n
			csv.setAttribute('aria-disabled', 'true');\n
			printLink.setAttribute('aria-disabled', 'true');\n
			return;\n
		}\n
		const q = new URLSearchParams({ periodId: String(id) }).toString();\n
		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;\n
		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;\n
		csv.removeAttribute('aria-disabled');\n
		printLink.removeAttribute('aria-disabled');\n
	}\n
\n
	function wireAdminExportGuards() {\n
		const guard = (event) => {\n
			const el = event.currentTarget;\n
			if (el && el.getAttribute('aria-disabled') === 'true') {\n
				event.preventDefault();\n
				Msg.announce(\n
					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),\n
					'warning',\n
				);\n
			}\n
		};\n
		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);\n
		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);\n
	}\n
\n
	let rosterPhpL10nCache;\n
	function getRosterPhpL10n() {\n
		if (rosterPhpL10nCache !== undefined) {\n
			return rosterPhpL10nCache;\n
		}\n
		const el = document.getElementById('dc-roster-php-l10n');\n
		const raw = el && el.dataset ? el.dataset.l10n : '';\n
		if (!raw) {\n
			rosterPhpL10nCache = null;\n
			return null;\n
		}\n
		try {\n
			rosterPhpL10nCache = JSON.parse(raw) || null;\n
		} catch (_) {\n
			rosterPhpL10nCache = null;\n
		}\n
		return rosterPhpL10nCache;\n
	}\n
\n
	function translateConflictMessage(message) {\n
		const raw = String(message || '');\n
		if (!raw) {\n
			return t('dutycheck', 'Unknown conflict');\n
		}\n
		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];\n
		if (fromPhp) {\n
			return fromPhp;\n
		}\n
		return t('dutycheck', raw);\n
	}\n
\n
	function render(data) {\n
		state.lastRosterData = data;\n
		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {\n
			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);\n
			state.planningDefaultsFresh = true;\n
		}\n
		state.periods = data.periods || [];\n
		state.employees = data.employees || [];\n
		state.locations = data.locations || [];\n
		state.assignments = data.assignments || [];\n
		state.absenceBlocks = data.absenceBlocks || [];\n
		state.canCreateAssignments = Boolean(data.canCreateAssignments);\n
		fillPeriodSwitcher(state.periods, data.selectedPeriodId);\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden) {\n
			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));\n
		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {\n
			dateInput.min = String(selectedPeriod.startDate);\n
			dateInput.max = String(selectedPeriod.endDate);\n
			if (!dateInput.value && state.canCreateAssignments) {\n
				dateInput.value = String(selectedPeriod.startDate);\n
			}\n
		} else if (dateInput) {\n
			dateInput.removeAttribute('min');\n
			dateInput.removeAttribute('max');\n
		}\n
		fillSelect('dc-assignment-location', state.locations, {\n
			emptyText: t('dutycheck', 'No active locations yet'),\n
		});\n
		updateLocationTimezoneHint();\n
		refreshAssignmentFormEligibility();\n
		renderAssignments(data.assignments || []);\n
		renderConflicts(data.conflicts || []);\n
		renderSetupCallout(data);\n
		updateAddAssignmentControl();\n
		updateAdminExportLinks(data.selectedPeriodId);\n
		fillCopySourceSelect(data.selectedPeriodId);\n
		refreshAcknowledgeStats(data.selectedPeriodId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
	}\n
\n
	function selectedPeriodIdFromUrl() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const raw = params.get('periodId');\n
		const parsed = Number(raw);\n
		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;\n
	}\n
\n
	function updateUrlPeriodId(periodId) {\n
		const params = new URLSearchParams(window.location.search || '');\n
		if (periodId && Number(periodId) > 0) {\n
			params.set('periodId', String(periodId));\n
		} else {\n
			params.delete('periodId');\n
		}\n
		const query = params.toString();\n
		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);\n
	}\n
\n
	async function loadRoster(periodId) {\n
		if (C.isModalOpen?.()) {\n
			C.dismissOpenModal();\n
		} else {\n
			restoreAssignmentFormPanel();\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		C.setLoadingRow(tbody, 7);\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });\n
			render(response?.data || {});\n
			updateUrlPeriodId(response?.data?.selectedPeriodId || null);\n
			void loadRosterSignals(response?.data || {});\n
			void loadCoverageStrip(response?.data?.selectedPeriodId);\n
			return response?.data || {};\n
		} catch (err) {\n
			const code = String(err?.payload?.error?.code || err?.code || '');\n
			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {\n
				try {\n
					const fallback = await Api.get('/apps/dutycheck/api/roster', {});\n
					render(fallback?.data || {});\n
					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);\n
					void loadRosterSignals(fallback?.data || {});\n
					void loadCoverageStrip(fallback?.data?.selectedPeriodId);\n
					Msg.announce(\n
						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),\n
						'warning',\n
					);\n
					return fallback?.data || {};\n
				} catch (retryErr) {\n
					Msg.handleApiError(retryErr);\n
					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
					return null;\n
				}\n
			}\n
			Msg.handleApiError(err);\n
			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
			return null;\n
		} finally {\n
			C.clearLoadingRow(tbody);\n
		}\n
	}\n
\n
	function dateOverlapsBlackout(day, blackout) {\n
		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);\n
		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);\n
		if (!start || !end) return false;\n
		return day >= start && day <= end;\n
	}\n
\n
	async function loadRosterSignals(data) {\n
		state.preferencesByEmployee = {};\n
		state.blackoutsByEmployeeDate = {};\n
		const employees = data.employees || state.employees || [];\n
		const period = (data.periods || state.periods || []).find(\n
			(p) => Number(p.id) === Number(data.selectedPeriodId),\n
		);\n
		const from = period?.startDate || period?.start_date || '';\n
		const to = period?.endDate || period?.end_date || '';\n
		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);\n
		if (!ids.length) return;\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/roster/signals', {\n
				employeeIds: ids,\n
				from,\n
				to,\n
			});\n
			const prefs = res?.data?.preferences || [];\n
			const blackouts = res?.data?.blackouts || [];\n
			prefs.forEach((p) => {\n
				const eid = String(p.employeeId || p.employee_id || '');\n
				if (!eid) return;\n
				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];\n
				state.preferencesByEmployee[eid].push(p);\n
			});\n
			const days = [];\n
			if (from && to) {\n
				const cur = new Date(from + 'T12:00:00');\n
				const end = new Date(to + 'T12:00:00');\n
				while (cur <= end) {\n
					const pad = (n) => String(n).padStart(2, '0');\n
					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);\n
					cur.setDate(cur.getDate() + 1);\n
				}\n
			}\n
			blackouts.forEach((b) => {\n
				const eid = Number(b.employeeId || b.employee_id || 0);\n
				if (!eid) return;\n
				days.forEach((day) => {\n
					if (dateOverlapsBlackout(day, b)) {\n
						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;\n
					}\n
				});\n
			});\n
			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {\n
				paintGridWindow({ force: true });\n
			}\n
		} catch (_) {\n
			// Optional enrichment — ignore failures.\n
		}\n
	}\n
\n
	async function loadCoverageStrip(periodId) {\n
		const strip = document.getElementById('dc-coverage-strip');\n
		const text = document.getElementById('dc-coverage-strip-text');\n
		if (!strip || !text) return;\n
		const id = Number(periodId || 0);\n
		if (!id) {\n
			strip.hidden = true;\n
			return;\n
		}\n
		try {\n
			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);\n
			const d = res?.data || {};\n
			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);\n
			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);\n
			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);\n
			let message;\n
			if (ready || (hard === 0 && soft === 0)) {\n
				message = t('dutycheck', 'Ready to publish — no blocking issues.');\n
				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--ok');\n
			} else if (hard > 0) {\n
				message = t('dutycheck', '{n} must-fix issues before publish.')\n
					.replace('{n}', String(hard));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');\n
				strip.classList.add('dc-coverage-strip--critical');\n
			} else {\n
				message = t('dutycheck', '{n} items need confirmation.')\n
					.replace('{n}', String(soft));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--warn');\n
			}\n
			text.textContent = message;\n
			strip.hidden = false;\n
		} catch (_) {\n
			strip.hidden = true;\n
		}\n
	}\n
\n
	function currentSuggestLocationId() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const fromUrl = Number(params.get('locationId') || 0);\n
		if (Number.isInteger(fromUrl) && fromUrl > 0) {\n
			return fromUrl;\n
		}\n
		const filterEl = document.getElementById('dc-roster-location')\n
			|| document.getElementById('dc-assignment-location');\n
		const fromUi = Number(filterEl?.value || 0);\n
		if (Number.isInteger(fromUi) && fromUi > 0) {\n
			return fromUi;\n
		}\n
		return null;\n
	}\n
\n
	async function runSuggestFill() {\n
		const periodId = Number(selectedPeriodId() || 0);\n
		if (!periodId) {\n
			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');\n
			return;\n
		}\n
		const btn = document.getElementById('dc-roster-suggest-fill');\n
		if (btn) {\n
			btn.disabled = true;\n
			btn.setAttribute('aria-busy', 'true');\n
		}\n
		const locId = currentSuggestLocationId();\n
		const suggestBody = { locationId: locId || null };\n
		try {\n
			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);\n
			const d = preview?.data || {};\n
			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {\n
				const noLoc = Number(d.skippedNoLocation ?? 0);\n
				Msg.announce(\n
					noLoc > 0\n
						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')\n
						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),\n
					'warning',\n
				);\n
				return;\n
			}\n
			const body = create('div', { class: 'dc-suggest-preview' }, [\n
				create('p', {\n
					text: t('dutycheck', 'Would create {n} shifts from patterns.')\n
						.replace('{n}', String(d.created ?? 0)),\n
				}),\n
				create('ul', { class: 'dc-suggest-preview__counts' }, [\n
					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),\n
				]),\n
			]);\n
			const confirmed = await new Promise((resolve) => {\n
				let settled = false;\n
				const finish = (value) => {\n
					if (settled) return;\n
					settled = true;\n
					resolve(value);\n
				};\n
				C.openModal({\n
					title: t('dutycheck', 'Suggest fill preview'),\n
					primaryLabel: t('dutycheck', 'Apply suggest fill'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					render: () => body,\n
					onSubmit: async () => {\n
						finish(true);\n
						return true;\n
					},\n
					onCancel: () => finish(false),\n
					onClose: () => finish(false),\n
				});\n
			});\n
			if (!confirmed) return;\n
			if ((d.created ?? 0) < 1) {\n
				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');\n
				return;\n
			}\n
			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);\n
			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');\n
			await loadRoster(periodId);\n
		} catch (err) {\n
			const code = String(err?.code || err?.payload?.error?.code || '');\n
			if (code === 'ROTATION_DISABLED') {\n
				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');\n
			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {\n
				Msg.announce(\n
					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),\n
					'warning',\n
				);\n
			} else if (code === 'RATE_LIMITED') {\n
				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');\n
			} else {\n
				Msg.handleApiError(err);\n
			}\n
		} finally {\n
			if (btn) {\n
				btn.disabled = false;\n
				btn.removeAttribute('aria-busy');\n
			}\n
		}\n
	}\n
\n
	function clearAssignmentFormFields() {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) return;\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		const periodVal = periodHidden ? String(periodHidden.value || '') : '';\n
		form.reset();\n
		if (periodHidden && periodVal) {\n
			periodHidden.value = periodVal;\n
		}\n
		applyDefaultBreakToForm();\n
		D?.applyLocaleToTemporalInputs(form);\n
		refreshAssignmentFormEligibility();\n
		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');\n
		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {\n
			showAssignmentFormSuccess(clearedMsg, 'info');\n
		}\n
		Msg.announce(clearedMsg, 'info');\n
	}\n
\n
	function readForm(form) {\n
		const data = new FormData(form);\n
		const rawStart = String(data.get('startTime') || '');\n
		const rawEnd = String(data.get('endTime') || '');\n
		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();\n
		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();\n
		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);\n
		const out = {\n
			periodId: Number(data.get('periodId')),\n
			employeeId: Number(data.get('employeeId')),\n
			locationId: Number(data.get('locationId')),\n
			dutyDate: String(data.get('dutyDate') || ''),\n
			startTime,\n
			endTime,\n
			breakMinutes: Number(data.get('breakMinutes') || 0),\n
			note: String(data.get('note') || '').trim(),\n
			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,\n
		};\n
		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {\n
			out.expectedVersion = Number(state.editingAssignmentVersion);\n
		}\n
		return out;\n
	}\n
\n
	/**\n
	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.\n
	 */\n
	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;\n
		if (start === null || end === null) return null;\n
		let endAdj = end;\n
		if (endAdj <= start) {\n
			endAdj += 24 * 60;\n
		}\n
		return (endAdj - start) - breakMinutes;\n
	}\n
\n
	function validate(payload) {\n
		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {\n
			return t('dutycheck', 'Select a period before saving.');\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		if (!period) {\n
			return t('dutycheck', 'That planning period was not found.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {\n
			return t('dutycheck', 'Select an employee.');\n
		}\n
		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {\n
			return t('dutycheck', 'Select a location.');\n
		}\n
		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {\n
			return t('dutycheck', 'Date, start time, and end time are required.');\n
		}\n
		if (period.startDate && period.endDate\n
			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {\n
			return t('dutycheck', 'Date must fall within the selected period.');\n
		}\n
		if (payload.startTime === payload.endTime) {\n
			return t(\n
				'dutycheck',\n
				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
			);\n
		}\n
		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {\n
			return t('dutycheck', 'Break minutes must be between 0 and 720.');\n
		}\n
		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
		if (effective === null) {\n
			return t('dutycheck', 'Please check the dates and times.');\n
		}\n
		if (effective <= 0) {\n
			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
		}\n
		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			return t('dutycheck', 'This employee has approved leave on the selected date.');\n
		}\n
		if (findOverlapAssignment(payload)) {\n
			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');\n
		}\n
		return null;\n
	}\n
\n
	function errorMessageFor(error) {\n
		const code = errorCodeFrom(error);\n
		switch (code) {\n
			case 'PERIOD_NOT_OPEN':\n
				return t('dutycheck', 'The selected period is not open for planning.');\n
			case 'PERIOD_NOT_FOUND':\n
				return t('dutycheck', 'That planning period was not found.');\n
			case 'DATE_OUTSIDE_PERIOD':\n
				return t('dutycheck', 'Date must fall within the selected period.');\n
			case 'EMPLOYEE_NOT_FOUND':\n
				return t('dutycheck', 'Employee not found or no longer available.');\n
			case 'LOCATION_NOT_FOUND':\n
				return t('dutycheck', 'Location not found or no longer available.');\n
			case 'EQUAL_DUTY_TIMES':\n
				return t(\n
					'dutycheck',\n
					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
				);\n
			case 'INVALID_SHIFT_LENGTH':\n
				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
			case 'INVALID_BREAK_MINUTES':\n
				return t('dutycheck', 'Break minutes are out of the allowed range.');\n
			case 'NOTE_TOO_LONG':\n
				return t('dutycheck', 'Note is too long (maximum 512 characters).');\n
			case 'ASSIGNMENT_OVERLAP':\n
				return t('dutycheck', 'This employee already has an overlapping assignment.');\n
			case 'ASSIGNMENT_DUPLICATE_SLOT':\n
				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');\n
			case 'STALE_VERSION':\n
				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');\n
			case 'CONFLICT_ACK_STALE':\n
				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');\n
			case 'COMPANY_MEMBERSHIP_REQUIRED':\n
				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');\n
			case 'EXPECTED_VERSION_REQUIRED':\n
				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');\n
			case 'ABSENCE_CONFLICT':\n
				return t('dutycheck', 'This employee has an approved absence on that date.');\n
			case 'BLACKOUT_CONFLICT':\n
				return t('dutycheck', 'This employee marked that they cannot work during this time.');\n
			case 'CONFLICT_ACK_REQUIRED':\n
				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');\n
			case 'REASON_TOO_SHORT':\n
				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');\n
			case 'INTERNAL_ERROR':\n
				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');\n
			case 'QUALIFICATION_MISSING':\n
				return t('dutycheck', 'This employee is missing a required qualification for that location.');\n
			case 'ASSIGNMENT_NOT_FOUND':\n
				return t('dutycheck', 'That assignment no longer exists. Reload the page.');\n
			case 'SCHEMA_NOT_READY':\n
				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');\n
			case 'INVALID_DATE':\n
			case 'INVALID_TIME':\n
				return t('dutycheck', 'Please check the dates and times.');\n
			case 'PERIOD_ID_REQUIRED':\n
				return t('dutycheck', 'Select a period before saving.');\n
			case 'EMPLOYEE_ID_REQUIRED':\n
				return t('dutycheck', 'Select an employee.');\n
			case 'LOCATION_ID_REQUIRED':\n
				return t('dutycheck', 'Select a location.');\n
			default:\n
				return null;\n
		}\n
	}\n
\n
\n
	async function loadPendingSwaps() {\n
		const list = document.getElementById('dc-swap-list');\n
		const empty = document.getElementById('dc-swap-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/swaps');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				li.appendChild(create('p', {\n
					text: row.toEmployeeId\n
						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId))\n
							.replace('{to}', String(row.toEmployeeId))\n
						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId)),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });\n
						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');\n
						await loadPendingSwaps();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });\n
						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');\n
						await loadPendingSwaps();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load swap requests.');\n
			}\n
		}\n
	}\n
\n
	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {\n
		try {\n
			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {\n
				acknowledgements,\n
			});\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Approve with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return approveOpenShiftClaim(openShiftId, acks, false);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	async function loadPendingOpenClaims() {\n
		const list = document.getElementById('dc-open-claim-list');\n
		const empty = document.getElementById('dc-open-claim-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;\n
				li.appendChild(create('p', {\n
					text: t('dutycheck', '{date} · employee #{emp}')\n
						.replace('{date}', String(when))\n
						.replace('{emp}', String(row.claimedByEmployeeId || '—')),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await approveOpenShiftClaim(row.id);\n
						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');\n
						await loadPendingOpenClaims();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});\n
						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');\n
						await loadPendingOpenClaims();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load pending claims.');\n
			}\n
		}\n
	}\n
\n
	function wireMarketplace() {\n
		const toggle = document.getElementById('dc-open-shift-create');\n
		const form = document.getElementById('dc-open-shift-form');\n
		toggle?.addEventListener('click', () => {\n
			if (!form) return;\n
			form.hidden = !form.hidden;\n
			const loc = document.getElementById('dc-os-location');\n
			if (loc && state.locations?.length) {\n
				loc.replaceChildren();\n
				for (const l of state.locations) {\n
					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));\n
				}\n
			}\n
			const period = selectedPeriodFromState();\n
			const date = document.getElementById('dc-os-date');\n
			if (date && period?.startDate) date.value = String(period.startDate);\n
		});\n
		document.getElementById('dc-os-save')?.addEventListener('click', async () => {\n
			const period = selectedPeriodFromState();\n
			if (!period?.id) {\n
				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');\n
				return;\n
			}\n
			try {\n
				await Api.post('/apps/dutycheck/api/open-shifts', {\n
					periodId: Number(period.id),\n
					locationId: Number(document.getElementById('dc-os-location')?.value || 0),\n
					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),\n
					startTime: String(document.getElementById('dc-os-start')?.value || ''),\n
					endTime: String(document.getElementById('dc-os-end')?.value || ''),\n
					breakMinutes: 0,\n
				});\n
				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');\n
				if (form) form.hidden = true;\n
			} catch (err) {\n
				Msg.handleApiError(err);\n
			}\n
		});\n
	}\n
\n
	document.addEventListener('DOMContentLoaded', async () => {\n
		D?.applyLocaleToTemporalInputs(document);\n
		wireBreakPrefillHint();\n
		window.addEventListener('dc-planning-defaults-changed', (event) => {\n
			const minutes = Number(event?.detail?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
		});\n
		wireAdminExportGuards();\n
		wireMarketplace();\n
		setRosterView('grid');\n
		wireRosterVirtualization();\n
		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));\n
		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));\n
		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {\n
			gridState.selected.clear();\n
			syncVisibleGridChrome();\n
			updateBulkBar();\n
		});\n
		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {\n
			void applyBulkFromTemplate();\n
		});\n
		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {\n
			void runSuggestFill();\n
		});\n
		await Promise.all([\n
			loadRoster(selectedPeriodIdFromUrl()),\n
			loadPendingSwaps(),\n
			loadPendingOpenClaims(),\n
		]);\n
\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		switcher?.addEventListener('change', async () => {\n
			clearAssignmentsSectionSuccess();\n
			const periodId = Number(switcher.value);\n
			await Promise.all([\n
				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),\n
				loadPendingSwaps(),\n
				loadPendingOpenClaims(),\n
			]);\n
		});\n
\n
		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {\n
			const btn = event.target.closest('.dc-roster-add-assignment-trigger');\n
			if (!btn) {\n
				return;\n
			}\n
			openAssignmentCreateModal(btn);\n
		});\n
\n
		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {\n
			clearAssignmentFormFields();\n
		});\n
\n
		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {\n
			applySelectedTemplate();\n
		});\n
		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {\n
			runCopyPeriod(true);\n
		});\n
		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {\n
			if (!state.copyPreviewReady) {\n
				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');\n
				return;\n
			}\n
			await runCopyPeriod(false);\n
		});\n
		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {\n
			state.copyPreviewReady = false;\n
			const applyBtn = document.getElementById('dc-roster-copy-apply');\n
			if (applyBtn) applyBtn.disabled = true;\n
		});\n
\n
		const form = document.getElementById('dc-assignment-form');\n
		const scheduleEligibilityRefresh = () => {\n
			clearAssignmentFormSuccess();\n
			clearAssignmentsSectionSuccess();\n
			refreshAssignmentFormEligibility();\n
		};\n
		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);\n
		form?.addEventListener('submit', (event) => {\n
			event.preventDefault();\n
			const dialog = form.closest('.dc-modal__dialog--roster-assignment');\n
			if (!dialog) {\n
				return;\n
			}\n
			const primary = dialog.querySelector('.dc-modal__actions .button.primary');\n
			if (primary && !primary.disabled) {\n
				primary.click();\n
			}\n
		});\n
	});\n
})();\n
' contains "'PageUp', 'PageDown'".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:69

FAILURES!
Tests: 6, Assertions: 66, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.221743
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.126726
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.155462
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.135294
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.096959
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.084911
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.0811
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.124675
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.162198
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 3.235487
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.02521
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.119405
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.04715
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.042143
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.399388
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.093107
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.054434
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.193982
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.133685
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.051729
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.048735
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.303688
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.105952
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 2.198919
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /'PageUp',\s*'PageDown'/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105620 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:261:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.182809
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.246604
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.103528
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 4.100915
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 99.639047
killed skip_page_keys
== mutant skip_scroller_tabindex (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

...F..                                                              6 / 6 (100%)

Time: 00:00.015, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testTemplateAndCssKeepGrannyStatusAndPrintAll
Grid scroller must be a keyboard tab stop (WCAG 2.1.1 scrollable region)
Failed asserting that '<?php\n
\n
/**\n
 * Roster planning page.\n
 *\n
 * Layout (top → bottom):\n
 *  - Period switcher\n
 *  - Assignments table (+ “Add assignment” opens the create modal)\n
 *  - Planning checks (conflicts)\n
 *  - Export (app admins only)\n
 *\n
 * @var array $_\n
 * @var \OCP\IL10N $l\n
 */\n
include __DIR__ . '/common/page-start.php';\n
$htmlLang = (string) (($_['clientHints']['htmlLang'] ?? 'en-US'));\n
$urls = (array) ($_['urls'] ?? []);\n
$rosterPhpL10n = [\n
	'conflictMessages' => [],\n
];\n
foreach (\OCA\DutyCheck\Service\RosterService::rosterApiConflictMessageKeys() as $enMsg) {\n
	$rosterPhpL10n['conflictMessages'][$enMsg] = $l->t($enMsg);\n
}\n
?>\n
<div id="dc-roster-php-l10n" hidden data-l10n="<?php p(json_encode($rosterPhpL10n, JSON_UNESCAPED_UNICODE)); ?>"></div>\n
<section class="dc-card dc-empty dc-empty--quickstart" id="dc-roster-quickstart" hidden aria-labelledby="dc-roster-quickstart-title">\n
	<header class="dc-section__header">\n
		<div>\n
			<h2 id="dc-roster-quickstart-title"><?php p($l->t('Quick start')); ?></h2>\n
			<p class="dc-section__sub">\n
				<?php p($l->t('How to add a duty assignment without breaking labour rules or double-booking anyone.')); ?>\n
			</p>\n
		</div>\n
		<button type="button" class="dc-hint-dismiss" data-dc-dismiss-hint="roster_quickstart_v1" aria-describedby="dc-roster-quickstart-title">\n
			<?php p($l->t('Hide tips')); ?>\n
		</button>\n
	</header>\n
	<ol class="dc-quickstart">\n
		<li class="dc-quickstart__item" data-step="select-period">\n
			<strong><?php p($l->t('1. Choose the period to plan in')); ?></strong>\n
			<p>\n
				<?php p($l->t('Use the period selector below. New assignments can only be added to "Open" periods; published and closed ones are read-only.')); ?>\n
			</p>\n
		</li>\n
		<li class="dc-quickstart__item" data-step="add-assignment">\n
			<strong><?php p($l->t('2. Add assignments from the table')); ?></strong>\n
			<p>\n
				<?php p($l->t('Click “Add assignment” next to the assignments list, fill in the three steps in the dialog, and save. Violations of hard rules are blocked with a clear message.')); ?>\n
			</p>\n
		</li>\n
		<li class="dc-quickstart__item" data-step="resolve-conflicts">\n
			<strong><?php p($l->t('3. Resolve every conflict')); ?></strong>\n
			<p>\n
				<?php p($l->t('Issues marked “Must fix” block publishing. “Confirm to continue” (for example less than 11 hours rest between shifts) needs a short written reason — it is stored in the audit log.')); ?>\n
			</p>\n
		</li>\n
		<li class="dc-quickstart__item" data-step="publish-on-periods">\n
			<strong><?php p($l->t('4. Publish the period from Periods')); ?></strong>\n
			<p>\n
				<?php p($l->t('This Roster page stores shifts only. Publishing and closing happen on Periods — employees see My roster only after you publish there. Publish only when every “Must fix” issue is resolved and “Confirm to continue” items are confirmed.')); ?>\n
			</p>\n
			<p class="dc-quickstart__cta">\n
				<a class="button" href="<?php p((string) ($urls['periods'] ?? '#')); ?>"><?php p($l->t('Go to Periods')); ?></a>\n
			</p>\n
		</li>\n
	</ol>\n
</section>\n
\n
<section class="dc-card dc-section dc-roster-panel" id="dc-roster-period-section" aria-labelledby="dc-roster-period-title">\n
	<header class="dc-section__header">\n
		<div>\n
			<h2 id="dc-roster-period-title"><?php p($l->t('Active period')); ?></h2>\n
			<p class="dc-section__sub">\n
				<?php p($l->t('Your choice here controls the assignment list and planning checks below. Only open periods accept new assignments.')); ?>\n
			</p>\n
		</div>\n
	</header>\n
	<div class="dc-form-grid dc-roster-panel__body">\n
		<div class="dc-field dc-field--full">\n
			<label class="dc-field__label" for="dc-roster-period-switcher">\n
				<?php p($l->t('Period')); ?>\n
			</label>\n
			<select id="dc-roster-period-switcher" class="dc-input" aria-describedby="dc-roster-period-hint"></select>\n
			<p id="dc-roster-period-hint" class="dc-field__hint">\n
				<?php p($l->t('Periods are listed newest first. Closed periods are read-only. Changing the period updates the list below — no full page reload.')); ?>\n
			</p>\n
			<p id="dc-roster-ack-stats" class="dc-pill dc-roster-ack-stats" role="status" aria-live="polite" hidden></p>\n
			<div class="dc-roster-period__lifecycle" role="note">\n
				<p class="dc-field__hint">\n
					<?php p($l->t('Publishing and closing happen on the Periods page. Use “Add assignment” on this page to plan shifts.')); ?>\n
				</p>\n
				<a class="button" href="<?php p((string) ($urls['periods'] ?? '#')); ?>"><?php p($l->t('Go to Periods')); ?></a>\n
			</div>\n
			<div class="dc-roster-copy-period" id="dc-roster-copy-period" hidden>\n
				<label class="dc-field__label" for="dc-roster-copy-source"><?php p($l->t('Copy assignments from another period')); ?></label>\n
				<div class="dc-roster-copy-period__row">\n
					<select id="dc-roster-copy-source" class="dc-input" aria-describedby="dc-roster-copy-hint"></select>\n
					<button type="button" class="button" id="dc-roster-copy-preview"><?php p($l->t('Preview copy')); ?></button>\n
					<button type="button" class="button primary" id="dc-roster-copy-apply" disabled><?php p($l->t('Apply copy')); ?></button>\n
				</div>\n
				<p id="dc-roster-copy-hint" class="dc-field__hint">\n
					<?php p($l->t('Preview never writes. Apply only after you confirm the dry-run counts. Conflicts are recomputed after apply.')); ?>\n
				</p>\n
				<p id="dc-roster-copy-status" class="dc-roster-flash" role="status" aria-live="polite" aria-atomic="true" hidden></p>\n
			</div>\n
		</div>\n
	</div>\n
</section>\n
\n
<section class="dc-card dc-section dc-roster-panel" id="dc-roster-assignments-section" aria-labelledby="dc-assignments-title">\n
	<header class="dc-section__header">\n
		<div>\n
			<h2 id="dc-assignments-title"><?php p($l->t('Assignments')); ?></h2>\n
			<p class="dc-section__sub">\n
				<?php p($l->t('Shifts for the period selected above. Use the grid for a week overview, or the list for details. Large teams stay fast: only the rows on screen are drawn — scroll to see everyone.')); ?>\n
			</p>\n
		</div>\n
		<div class="dc-section__controls">\n
			<div class="dc-roster-view-toggle" role="group" aria-label="<?php p($l->t('Roster view')); ?>">\n
				<button type="button" class="button" id="dc-roster-view-grid" aria-pressed="true"><?php p($l->t('Grid')); ?></button>\n
				<button type="button" class="button" id="dc-roster-view-list" aria-pressed="false"><?php p($l->t('List')); ?></button>\n
			</div>\n
			<button type="button" class="button" id="dc-roster-suggest-fill"\n
				aria-describedby="dc-roster-suggest-fill-desc">\n
				<?php p($l->t('Suggest fill')); ?>\n
			</button>\n
			<span id="dc-roster-suggest-fill-desc" class="dc-sr-only">\n
				<?php p($l->t('Preview filling empty cells from each person’s rotation pattern, then confirm.')); ?>\n
			</span>\n
			<button type="button" class="button primary dc-roster-add-assignment-trigger" id="dc-roster-add-assignment"\n
				aria-describedby="dc-roster-add-assignment-desc" aria-disabled="true">\n
				<?php p($l->t('Add assignment')); ?>\n
			</button>\n
			<span id="dc-roster-add-assignment-desc" class="dc-sr-only">\n
				<?php p($l->t('Opens a dialog to create a new shift for the selected open period.')); ?>\n
			</span>\n
		</div>\n
	</header>\n
	<p id="dc-roster-assignments-success" class="dc-roster-flash" role="status" aria-live="polite" aria-atomic="true" hidden></p>\n
	<div id="dc-coverage-strip" class="dc-coverage-strip" role="status" aria-live="polite" hidden>\n
		<span class="dc-coverage-strip__label"><?php p($l->t('Publish readiness')); ?></span>\n
		<span id="dc-coverage-strip-text" class="dc-coverage-strip__text"></span>\n
	</div>\n
	<div id="dc-roster-grid-wrap" class="dc-roster-grid-wrap">\n
		<p id="dc-roster-grid-status" class="dc-roster-virtual-status" aria-hidden="true"></p>\n
		<div id="dc-roster-grid-scroller" class="dc-roster-grid-scroller" role="region" aria-labelledby="dc-assignments-title">\n
			<div\n
				id="dc-roster-grid"\n
				class="dc-roster-grid"\n
				aria-labelledby="dc-assignments-title"\n
				aria-describedby="dc-roster-grid-hint"\n
			></div>\n
		</div>\n
		<p id="dc-roster-grid-hint" class="dc-field__hint">\n
			<?php p($l->t('Rows are people, columns are days. For a full month, scroll sideways to see every date. Arrow keys move between cells — including people who are scrolled out of view. Page Up and Page Down jump a screen. Home and End jump to the first or last day. Enter opens the shift. Space selects empty cells for bulk fill.')); ?>\n
		</p>\n
		<div id="dc-roster-bulk-bar" class="dc-roster-bulk-bar" hidden>\n
			<p class="dc-roster-bulk-bar__count" id="dc-roster-bulk-count" role="status" aria-live="polite"></p>\n
			<label class="dc-field__label" for="dc-roster-bulk-template"><?php p($l->t('Fill selected cells from template')); ?></label>\n
			<div class="dc-roster-bulk-bar__row">\n
				<select id="dc-roster-bulk-template" class="dc-input"></select>\n
				<button type="button" class="button primary" id="dc-roster-bulk-apply"><?php p($l->t('Apply to selected')); ?></button>\n
				<button type="button" class="button" id="dc-roster-bulk-clear"><?php p($l->t('Clear selection')); ?></button>\n
			</div>\n
		</div>\n
	</div>\n
	<div id="dc-roster-list-panel" hidden>\n
		<p id="dc-roster-list-status" class="dc-roster-virtual-status" aria-hidden="true"></p>\n
		<div class="dc-table-wrap dc-roster-list-scroller" id="dc-assignments-table-wrap" tabindex="0" role="region" aria-labelledby="dc-assignments-title">\n
		<table class="dc-table" id="dc-assignments-table">\n
			<caption class="dc-sr-only"><?php p($l->t('Assignments for the selected period')); ?></caption>\n
			<thead>\n
				<tr>\n
					<th scope="col"><?php p($l->t('Date')); ?></th>\n
					<th scope="col"><?php p($l->t('Start')); ?></th>\n
					<th scope="col"><?php p($l->t('End')); ?></th>\n
					<th scope="col"><?php p($l->t('Employee')); ?></th>\n
					<th scope="col"><?php p($l->t('Location')); ?></th>\n
					<th scope="col"><?php p($l->t('Break')); ?></th>\n
					<th scope="col"><?php p($l->t('Note')); ?></th>\n
					<th scope="col"><?php p($l->t('Actions')); ?></th>\n
				</tr>\n
			</thead>\n
			<tbody id="dc-assignments-table-body"></tbody>\n
		</table>\n
		<div id="dc-roster-assignments-empty" class="dc-roster-empty-panel" hidden>\n
			<p class="dc-roster-empty-state__text">\n
				<?php p($l->t('No assignments in this period yet.')); ?>\n
			</p>\n
			<p id="dc-roster-empty-add-hint" class="dc-roster-empty-state__hint"></p>\n
		</div>\n
		</div>\n
	</div>\n
</section>\n
\n
<section class="dc-card dc-section dc-roster-panel" id="dc-roster-conflicts-section" aria-labelledby="dc-conflict-title">\n
	<header class="dc-section__header">\n
		<div>\n
			<h2 id="dc-conflict-title"><?php p($l->t('Planning checks')); ?></h2>\n
			<p class="dc-section__sub">\n
				<?php p($l->t('Automatic checks for the same period as the assignment list. “Must fix” blocks publishing until resolved. “Confirm to continue” lets you proceed after you confirm with a short reason.')); ?>\n
			</p>\n
		</div>\n
		<div class="dc-section__controls">\n
			<span id="dc-conflict-summary" class="dc-pill" aria-live="polite">\n
				<?php p($l->t('Loading conflicts...')); ?>\n
			</span>\n
		</div>\n
	</header>\n
	<ul id="dc-conflict-list" class="dc-conflicts" role="list" aria-label="<?php p($l->t('Planning checks')); ?>" aria-live="polite"></ul>\n
</section>\n
\n
<section class="dc-card dc-section dc-roster-panel" id="dc-roster-marketplace-section" aria-labelledby="dc-marketplace-title">\n
	<header class="dc-section__header">\n
		<div>\n
			<h2 id="dc-marketplace-title"><?php p($l->t('Swaps & open shifts')); ?></h2>\n
			<p class="dc-section__sub">\n
				<?php p($l->t('Review staff swap requests and post unassigned shifts for the selected period. Approving a pool swap cancels the old shift and opens it for claiming.')); ?>\n
			</p>\n
		</div>\n
		<div class="dc-section__controls">\n
			<button type="button" class="button" id="dc-open-shift-create"><?php p($l->t('Post open shift')); ?></button>\n
		</div>\n
	</header>\n
	<div class="dc-form-grid" id="dc-open-shift-form" hidden>\n
		<div class="dc-field">\n
			<label class="dc-field__label" for="dc-os-date"><?php p($l->t('Date')); ?></label>\n
			<input id="dc-os-date" type="date" class="dc-input">\n
		</div>\n
		<div class="dc-field">\n
			<label class="dc-field__label" for="dc-os-location"><?php p($l->t('Location')); ?></label>\n
			<select id="dc-os-location" class="dc-input"></select>\n
		</div>\n
		<div class="dc-field">\n
			<label class="dc-field__label" for="dc-os-start"><?php p($l->t('Start')); ?></label>\n
			<input id="dc-os-start" type="time" class="dc-input dc-input--time-24h" step="60" lang="en-GB">\n
		</div>\n
		<div class="dc-field">\n
			<label class="dc-field__label" for="dc-os-end"><?php p($l->t('End')); ?></label>\n
			<input id="dc-os-end" type="time" class="dc-input dc-input--time-24h" step="60" lang="en-GB">\n
		</div>\n
		<div class="dc-form-actions">\n
			<button type="button" class="button primary" id="dc-os-save"><?php p($l->t('Save open shift')); ?></button>\n
		</div>\n
	</div>\n
	<h3 class="dc-subsection-heading"><?php p($l->t('Pending swap requests')); ?></h3>\n
	<ul id="dc-swap-list" class="dc-conflicts" role="list" aria-live="polite"></ul>\n
	<p id="dc-swap-empty" class="dc-field__hint" hidden><?php p($l->t('No pending swaps.')); ?></p>\n
	<h3 class="dc-subsection-heading"><?php p($l->t('Pending open-shift claims')); ?></h3>\n
	<ul id="dc-open-claim-list" class="dc-conflicts" role="list" aria-live="polite"></ul>\n
	<p id="dc-open-claim-empty" class="dc-field__hint" hidden><?php p($l->t('No pending claims.')); ?></p>\n
</section>\n
\n
<?php if (!empty($_['isAppAdmin'])): ?>\n
<section class="dc-card dc-section dc-roster-panel dc-roster-export" id="dc-roster-admin-export" aria-labelledby="dc-roster-export-title">\n
	<header class="dc-section__header">\n
		<div>\n
			<h2 id="dc-roster-export-title"><?php p($l->t('Export & print')); ?></h2>\n
			<p class="dc-section__sub">\n
				<?php p($l->t('Download a spreadsheet file or open a clean print layout for the period selected above. Only DutyCheck and server administrators see this section.')); ?>\n
			</p>\n
			<p class="dc-roster-export__hint" id="dc-roster-export-hint">\n
				<?php p($l->t('Files include every assignment row for the active period. Use them for archives, handovers, or regulatory evidence — treat exports like personal data and store them securely.')); ?>\n
			</p>\n
		</div>\n
	</header>\n
	<div class="dc-roster-export__actions" role="group" aria-labelledby="dc-roster-export-title">\n
		<a class="button primary" id="dc-roster-export-csv" href="#" aria-describedby="dc-roster-export-hint">\n
			<?php p($l->t('Download CSV')); ?>\n
		</a>\n
		<a class="button" id="dc-roster-export-print" href="#" target="_blank" rel="noopener noreferrer" aria-describedby="dc-roster-export-hint">\n
			<?php p($l->t('Open printable view')); ?>\n
		</a>\n
	</div>\n
</section>\n
<?php endif; ?>\n
\n
<div id="dc-assignment-form-host" class="dc-assignment-form-host" hidden>\n
	<div id="dc-assignment-form-panel" class="dc-assignment-form-panel">\n
		<div id="dc-roster-setup-callout" class="dc-callout dc-callout--warning" role="status" aria-live="polite" hidden>\n
			<p><strong><?php p($l->t('Setup is required before you can plan duties.')); ?></strong></p>\n
			<ul class="dc-callout__list" id="dc-roster-setup-checklist"></ul>\n
		</div>\n
		<form id="dc-assignment-form" class="dc-form-grid dc-form-grid--roster-assignment" novalidate>\n
			<input type="hidden" name="periodId" id="dc-assignment-period" value="">\n
			<input type="hidden" name="assignmentId" id="dc-assignment-id" value="">\n
			<p id="dc-assignment-form-intro" class="dc-roster-form__intro">\n
				<?php p($l->t('Three steps: pick the day, choose who works and where, then enter shift times. People on approved leave are not listed for that day.')); ?>\n
			</p>\n
			<div class="dc-field dc-field--full" id="dc-assignment-template-field">\n
				<label class="dc-field__label" for="dc-assignment-template"><?php p($l->t('Shift template (optional)')); ?></label>\n
				<select id="dc-assignment-template" class="dc-input" aria-describedby="dc-assignment-template-hint">\n
					<option value=""><?php p($l->t('No template — enter times manually')); ?></option>\n
				</select>\n
				<p id="dc-assignment-template-hint" class="dc-field__hint">\n
					<?php p($l->t('Templates fill start, end, and break. You can still change any field before saving.')); ?>\n
				</p>\n
			</div>\n
			<fieldset class="dc-roster-form__step-group" aria-labelledby="dc-roster-step-day">\n
				<h3 id="dc-roster-step-day" class="dc-roster-form__section-heading dc-roster-form__section-heading--step">\n
					<span class="dc-roster-form__step-badge" aria-hidden="true">1</span>\n
					<?php p($l->t('Pick the duty day')); ?>\n
				</h3>\n
				<div class="dc-field dc-field--roster-date">\n
					<label class="dc-field__label" for="dc-assignment-date"><?php p($l->t('Date')); ?></label>\n
					<input id="dc-assignment-date" type="date" name="dutyDate" class="dc-input" lang="<?php p($htmlLang); ?>"\n
						aria-describedby="dc-assignment-date-hint" required>\n
					<p id="dc-assignment-date-hint" class="dc-field__hint">\n
						<?php p($l->t('Only dates inside the open planning period selected on this page.')); ?>\n
					</p>\n
				</div>\n
			</fieldset>\n
			<fieldset class="dc-roster-form__step-group" aria-labelledby="dc-roster-step-who">\n
				<h3 id="dc-roster-step-who" class="dc-roster-form__section-heading dc-roster-form__section-heading--step">\n
					<span class="dc-roster-form__step-badge" aria-hidden="true">2</span>\n
					<?php p($l->t('Who and where')); ?>\n
				</h3>\n
				<div class="dc-field dc-field--roster-employee">\n
					<label class="dc-field__label" for="dc-assignment-employee"><?php p($l->t('Employee')); ?></label>\n
					<select id="dc-assignment-employee" name="employeeId" class="dc-input" required\n
						aria-describedby="dc-assignment-employee-hint dc-assignment-form-feedback"></select>\n
					<p id="dc-assignment-employee-hint" class="dc-field__hint" aria-live="polite" aria-atomic="true">\n
						<?php p($l->t('Pick a date in step 1 first. People on approved leave that day are not listed here.')); ?>\n
					</p>\n
				</div>\n
				<div class="dc-field dc-field--roster-location">\n
					<label class="dc-field__label" for="dc-assignment-location"><?php p($l->t('Location')); ?></label>\n
					<select id="dc-assignment-location" name="locationId" class="dc-input" required\n
						aria-describedby="dc-assignment-location-tz-hint"></select>\n
					<p id="dc-assignment-location-tz-hint" class="dc-field__hint" aria-live="polite" aria-atomic="true" hidden></p>\n
				</div>\n
			</fieldset>\n
			<fieldset class="dc-roster-form__step-group" aria-labelledby="dc-roster-shift-schedule-label">\n
				<h3 id="dc-roster-shift-schedule-label" class="dc-roster-form__section-heading dc-roster-form__section-heading--step">\n
					<span class="dc-roster-form__step-badge" aria-hidden="true">3</span>\n
					<?php p($l->t('Shift times')); ?>\n
				</h3>\n
				<div class="dc-field dc-field--full dc-field--roster-time-hint">\n
					<p id="dc-roster-24h-hint" class="dc-field__hint dc-roster-24h-hint">\n
						<?php p($l->t('Times use the 24-hour clock (HH:mm). For shifts past midnight, set an end time earlier than the start time (e.g. 22:00–06:00).')); ?>\n
					</p>\n
				</div>\n
				<div class="dc-field dc-field--roster-start">\n
					<label class="dc-field__label" for="dc-assignment-start"><?php p($l->t('Start')); ?></label>\n
					<input id="dc-assignment-start" type="time" name="startTime" class="dc-input dc-input--time-24h" step="60"\n
						lang="en-GB"\n
						aria-label="<?php p($l->t('Start time (24-hour HH:mm)')); ?>"\n
						aria-describedby="dc-roster-24h-hint dc-assignment-location-tz-hint" required>\n
				</div>\n
				<div class="dc-field dc-field--roster-end">\n
					<label class="dc-field__label" for="dc-assignment-end"><?php p($l->t('End')); ?></label>\n
					<input id="dc-assignment-end" type="time" name="endTime" class="dc-input dc-input--time-24h" step="60"\n
						lang="en-GB"\n
						aria-label="<?php p($l->t('End time (24-hour HH:mm)')); ?>"\n
						aria-describedby="dc-roster-24h-hint dc-assignment-location-tz-hint dc-assignment-rest-hint" required>\n
				</div>\n
				<p id="dc-assignment-rest-hint" class="dc-field__hint dc-field--full dc-field--roster-rest-hint">\n
					<?php p($l->t('If Save asks you to confirm, a planning rule was triggered (for example less than 11 hours rest between shifts). You can review issues in')); ?>\n
					<a class="dc-hint-link" href="#dc-roster-conflicts-section"><?php p($l->t('Planning checks')); ?></a><?php p(' '); ?><?php p($l->t('on this page.')); ?>\n
				</p>\n
			</fieldset>\n
			<hr class="dc-form-grid__divider" aria-hidden="true">\n
			<div class="dc-roster-form__extras" role="group" aria-labelledby="dc-roster-form-extras-label">\n
				<h3 id="dc-roster-form-extras-label" class="dc-roster-form__section-heading">\n
					<?php p($l->t('Break and note')); ?>\n
				</h3>\n
			<div class="dc-field dc-field--roster-break">\n
				<label class="dc-field__label" for="dc-assignment-break"><?php p($l->t('Break (minutes)')); ?></label>\n
				<input id="dc-assignment-break" type="number" name="breakMinutes" class="dc-input dc-input--num"\n
					min="0" max="720" step="5" value="0" required\n
					aria-describedby="dc-assignment-break-prefill dc-assignment-break-hint">\n
				<p id="dc-assignment-break-prefill" class="dc-roster-break-prefill" aria-live="polite" aria-atomic="true"></p>\n
				<p id="dc-assignment-break-hint" class="dc-field__hint">\n
					<?php p($l->t('Unpaid break during the shift. Use 0 when there is no break.')); ?>\n
				</p>\n
			</div>\n
			<div class="dc-field dc-field--roster-note">\n
				<label class="dc-field__label" for="dc-assignment-note"><?php p($l->t('Note (optional)')); ?></label>\n
				<input id="dc-assignment-note" type="text" name="note" class="dc-input" maxlength="512" autocomplete="off">\n
			</div>\n
			</div>\n
			<div id="dc-assignment-form-feedback" class="dc-roster-form__feedback" role="status" aria-live="polite" aria-atomic="true" hidden></div>\n
			<div class="dc-form-actions dc-form-actions--roster">\n
				<button type="submit" class="button primary" tabindex="-1" aria-hidden="true" hidden><?php p($l->t('Save assignment')); ?></button>\n
				<button type="button" id="dc-assignment-form-clear" class="button"><?php p($l->t('Clear entered fields')); ?></button>\n
			</div>\n
		</form>\n
	</div>\n
</div>\n
<?php include __DIR__ . '/common/page-end.php'; ?>\n
' matches PCRE pattern "/id="dc-roster-grid-scroller"[^>]*tabindex="0"/".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:88

FAILURES!
Tests: 6, Assertions: 58, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.16532
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.127005
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.154681
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.128055
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.093016
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.082496
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.080061
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.132354
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.159064
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 3.138042
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 0.940583
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.086929
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.046879
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.04631
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.276544
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.081709
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.061014
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.18762
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.116866
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.045999
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.037438
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.252203
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.094434
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 0.668738
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.111339
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.189488
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    not ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.332263
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:287:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /id="dc-roster-grid-scroller"[^>]*tabindex="0"/. Input:
        
        '<?php\n' +
          '\n' +
          '/**\n' +
          ' * Roster planning page.\n' +
          ' *\n' +
          ' * Layout (top → bottom):\n' +
          ' *  - Period switcher\n' +
          ' *  - Assignments table (+ “Add assignment” opens the create modal)\n' +
          ' *  - Planning checks (conflicts)\n' +
          ' *  - Export (app admins only)\n' +
          ' *\n' +
          ' * @var array $_\n' +
          ' * @var \\OCP\\IL10N $l\n' +
          ' */\n' +
          "include __DIR__ . '/common/page-start.php';\n" +
          "$htmlLang = (string) (($_['clientHints']['htmlLang'] ?? 'en-US'));\n" +
          "$urls = (array) ($_['urls'] ?? []);\n" +
          '$rosterPhpL10n = [\n' +
          "\t'conflictMessages' => [],\n" +
          '];\n' +
          'foreach (\\OCA\\DutyCheck\\Service\\RosterService::rosterApiConflictMessageKeys() as $enMsg) {\n' +
          "\t$rosterPhpL10n['conflictMessages'][$enMsg] = $l->t($enMsg);\n" +
          '}\n' +
          '?>\n' +
          '<div id="dc-roster-php-l10n" hidden data-l10n="<?php p(json_encode($rosterPhpL10n, JSON_UNESCAPED_UNICODE)); ?>"></div>\n' +
          '<section class="dc-card dc-empty dc-empty--quickstart" id="dc-roster-quickstart" hidden aria-labelledby="dc-roster-quickstart-title">\n' +
          '\t<header class="dc-section__header">\n' +
          '\t\t<div>\n' +
          `\t\t\t<h2 id="dc-roster-quickstart-title"><?php p($l->t('Quick start')); ?></h2>\n` +
          '\t\t\t<p class="dc-section__sub">\n' +
          "\t\t\t\t<?php p($l->t('How to add a duty assignment without breaking labour rules or double-booking anyone.')); ?>\n" +
          '\t\t\t</p>\n' +
          '\t\t</div>\n' +
          '\t\t<button type="button" class="dc-hint-dismiss" data-dc-dismiss-hint="roster_quickstart_v1" aria-describedby="dc-roster-quickstart-title">\n' +
          "\t\t\t<?php p($l->t('Hide tips')); ?>\n" +
          '\t\t</button>\n' +
          '\t</header>\n' +
          '\t<ol class="dc-quickstart">\n' +
          '\t\t<li class="dc-quickstart__item" data-step="select-period">\n' +
          "\t\t\t<strong><?php p($l->t('1. Choose the period to plan in')); ?></strong>\n" +
          '\t\t\t<p>\n' +
          `\t\t\t\t<?php p($l->t('Use the period selector below. New assignments can only be added to "Open" periods; published and closed ones are read-only.')); ?>\n` +
          '\t\t\t</p>\n' +
          '\t\t</li>\n' +
          '\t\t<li class="dc-quickstart__item" data-step="add-assignment">\n' +
          "\t\t\t<strong><?php p($l->t('2. Add assignments from the table')); ?></strong>\n" +
          '\t\t\t<p>\n' +
          "\t\t\t\t<?php p($l->t('Click “Add assignment” next to the assignments list, fill in the three steps in the dialog, and save. Violations of hard rules are blocked with a clear message.')); ?>\n" +
          '\t\t\t</p>\n' +
          '\t\t</li>\n' +
          '\t\t<li class="dc-quickstart__item" data-step="resolve-conflicts">\n' +
          "\t\t\t<strong><?php p($l->t('3. Resolve every conflict')); ?></strong>\n" +
          '\t\t\t<p>\n' +
          "\t\t\t\t<?php p($l->t('Issues marked “Must fix” block publishing. “Confirm to continue” (for example less than 11 hours rest between shifts) needs a short written reason — it is stored in the audit log.')); ?>\n" +
          '\t\t\t</p>\n' +
          '\t\t</li>\n' +
          '\t\t<li class="dc-quickstart__item" data-step="publish-on-periods">\n' +
          "\t\t\t<strong><?php p($l->t('4. Publish the period from Periods')); ?></strong>\n" +
          '\t\t\t<p>\n' +
          "\t\t\t\t<?php p($l->t('This Roster page stores shifts only. Publishing and closing happen on Periods — employees see My roster only after you publish there. Publish only when every “Must fix” issue is resolved and “Confirm to continue” items are confirmed.')); ?>\n" +
          '\t\t\t</p>\n' +
          '\t\t\t<p class="dc-quickstart__cta">\n' +
          `\t\t\t\t<a class="button" href="<?php p((string) ($urls['periods'] ?? '#')); ?>"><?php p($l->t('Go to Periods')); ?></a>\n` +
          '\t\t\t</p>\n' +
          '\t\t</li>\n' +
          '\t</ol>\n' +
          '</section>\n' +
          '\n' +
          '<section class="dc-card dc-section dc-roster-panel" id="dc-roster-period-section" aria-labelledby="dc-roster-period-title">\n' +
          '\t<header class="dc-section__header">\n' +
          '\t\t<div>\n' +
          `\t\t\t<h2 id="dc-roster-period-title"><?php p($l->t('Active period')); ?></h2>\n` +
          '\t\t\t<p class="dc-section__sub">\n' +
          "\t\t\t\t<?php p($l->t('Your choice here controls the assignment list and planning checks below. Only open periods accept new assignments.')); ?>\n" +
          '\t\t\t</p>\n' +
          '\t\t</div>\n' +
          '\t</header>\n' +
          '\t<div class="dc-form-grid dc-roster-panel__body">\n' +
          '\t\t<div class="dc-field dc-field--full">\n' +
          '\t\t\t<label class="dc-field__label" for="dc-roster-period-switcher">\n' +
          "\t\t\t\t<?php p($l->t('Period')); ?>\n" +
          '\t\t\t</label>\n' +
          '\t\t\t<select id="dc-roster-period-switcher" class="dc-input" aria-describedby="dc-roster-period-hint"></select>\n' +
          '\t\t\t<p id="dc-roster-period-hint" class="dc-field__hint">\n' +
          "\t\t\t\t<?php p($l->t('Periods are listed newest first. Closed periods are read-only. Changing the period updates the list below — no full page reload.')); ?>\n" +
          '\t\t\t</p>\n' +
          '\t\t\t<p id="dc-roster-ack-stats" class="dc-pill dc-roster-ack-stats" role="status" aria-live="polite" hidden></p>\n' +
          '\t\t\t<div class="dc-roster-period__lifecycle" role="note">\n' +
          '\t\t\t\t<p class="dc-field__hint">\n' +
          "\t\t\t\t\t<?php p($l->t('Publishing and closing happen on the Periods page. Use “Add assignment” on this page to plan shifts.')); ?>\n" +
          '\t\t\t\t</p>\n' +
          `\t\t\t\t<a class="button" href="<?php p((string) ($urls['periods'] ?? '#')); ?>"><?php p($l->t('Go to Periods')); ?></a>\n` +
          '\t\t\t</div>\n' +
          '\t\t\t<div class="dc-roster-copy-period" id="dc-roster-copy-period" hidden>\n' +
          `\t\t\t\t<label class="dc-field__label" for="dc-roster-copy-source"><?php p($l->t('Copy assignments from another period')); ?></label>\n` +
          '\t\t\t\t<div class="dc-roster-copy-period__row">\n' +
          '\t\t\t\t\t<select id="dc-roster-copy-source" class="dc-input" aria-describedby="dc-roster-copy-hint"></select>\n' +
          `\t\t\t\t\t<button type="button" class="button" id="dc-roster-copy-preview"><?php p($l->t('Preview copy')); ?></button>\n` +
          `\t\t\t\t\t<button type="button" class="button primary" id="dc-roster-copy-apply" disabled><?php p($l->t('Apply copy')); ?></button>\n` +
          '\t\t\t\t</div>\n' +
          '\t\t\t\t<p id="dc-roster-copy-hint" class="dc-field__hint">\n' +
          "\t\t\t\t\t<?php p($l->t('Preview never writes. Apply only after you confirm the dry-run counts. Conflicts are recomputed after apply.')); ?>\n" +
          '\t\t\t\t</p>\n' +
          '\t\t\t\t<p id="dc-roster-copy-status" class="dc-roster-flash" role="status" aria-live="polite" aria-atomic="true" hidden></p>\n' +
          '\t\t\t</div>\n' +
          '\t\t</div>\n' +
          '\t</div>\n' +
          '</section>\n' +
          '\n' +
          '<section class="dc-card dc-section dc-roster-panel" id="dc-roster-assignments-section" aria-labelledby="dc-assignments-title">\n' +
          '\t<header class="dc-section__header">\n' +
          '\t\t<div>\n' +
          `\t\t\t<h2 id="dc-assignments-title"><?php p($l->t('Assignments')); ?></h2>\n` +
          '\t\t\t<p class="dc-section__sub">\n' +
          "\t\t\t\t<?php p($l->t('Shifts for the period selected above. Use the grid for a week overview, or the list for details. Large teams stay fast: only the rows on screen are drawn — scroll to see everyone.')); ?>\n" +
          '\t\t\t</p>\n' +
          '\t\t</div>\n' +
          '\t\t<div class="dc-section__controls">\n' +
          `\t\t\t<div class="dc-roster-view-toggle" role="group" aria-label="<?php p($l->t('Roster view')); ?>">\n` +
          `\t\t\t\t<button type="button" class="button" id="dc-roster-view-grid" aria-pressed="true"><?php p($l->t('Grid')); ?></button>\n` +
          `\t\t\t\t<button type="button" class="button" id="dc-roster-view-list" aria-pressed="false"><?php p($l->t('List')); ?></button>\n` +
          '\t\t\t</div>\n' +
          '\t\t\t<button type="button" class="button" id="dc-roster-suggest-fill"\n' +
          '\t\t\t\taria-describedby="dc-roster-suggest-fill-desc">\n' +
          "\t\t\t\t<?php p($l->t('Suggest fill')); ?>\n" +
          '\t\t\t</button>\n' +
          '\t\t\t<span id="dc-roster-suggest-fill-desc" class="dc-sr-only">\n' +
          "\t\t\t\t<?php p($l->t('Preview filling empty cells from each person’s rotation pattern, then confirm.')); ?>\n" +
          '\t\t\t</span>\n' +
          '\t\t\t<button type="button" class="button primary dc-roster-add-assignment-trigger" id="dc-roster-add-assignment"\n' +
          '\t\t\t\taria-describedby="dc-roster-add-assignment-desc" aria-disabled="true">\n' +
          "\t\t\t\t<?php p($l->t('Add assignment')); ?>\n" +
          '\t\t\t</button>\n' +
          '\t\t\t<span id="dc-roster-add-assignment-desc" class="dc-sr-only">\n' +
          "\t\t\t\t<?php p($l->t('Opens a dialog to create a new shift for the selected open period.')); ?>\n" +
          '\t\t\t</span>\n' +
          '\t\t</div>\n' +
          '\t</header>\n' +
          '\t<p id="dc-roster-assignments-success" class="dc-roster-flash" role="status" aria-live="polite" aria-atomic="true" hidden></p>\n' +
          '\t<div id="dc-coverage-strip" class="dc-coverage-strip" role="status" aria-live="polite" hidden>\n' +
          `\t\t<span class="dc-coverage-strip__label"><?php p($l->t('Publish readiness')); ?></span>\n` +
          '\t\t<span id="dc-coverage-strip-text" class="dc-coverage-strip__text"></span>\n' +
          '\t</div>\n' +
          '\t<div id="dc-roster-grid-wrap" class="dc-roster-grid-wrap">\n' +
          '\t\t<p id="dc-roster-grid-status" class="dc-roster-virtual-status" aria-hidden="true"></p>\n' +
          '\t\t<div id="dc-roster-grid-scroller" class="dc-roster-grid-scroller" role="region" aria-labelledby="dc-assignments-title">\n' +
          '\t\t\t<div\n' +
          '\t\t\t\tid="dc-roster-grid"\n' +
          '\t\t\t\tclass="dc-roster-grid"\n' +
          '\t\t\t\taria-labelledby="dc-assignments-title"\n' +
          '\t\t\t\taria-describedby="dc-roster-grid-hint"\n' +
          '\t\t\t></div>\n' +
          '\t\t</div>\n' +
          '\t\t<p id="dc-roster-grid-hint" class="dc-field__hint">\n' +
          "\t\t\t<?php p($l->t('Rows are people, columns are days. For a full month, scroll sideways to see every date. Arrow keys move between cells — including people who are scrolled out of view. Page Up and Page Down jump a screen. Home and End jump to the first or last day. Enter opens the shift. Space selects empty cells for bulk fill.')); ?>\n" +
          '\t\t</p>\n' +
          '\t\t<div id="dc-roster-bulk-bar" class="dc-roster-bulk-bar" hidden>\n' +
          '\t\t\t<p class="dc-roster-bulk-bar__count" id="dc-roster-bulk-count" role="status" aria-live="polite"></p>\n' +
          `\t\t\t<label class="dc-field__label" for="dc-roster-bulk-template"><?php p($l->t('Fill selected cells from template')); ?></label>\n` +
          '\t\t\t<div class="dc-roster-bulk-bar__row">\n' +
          '\t\t\t\t<select id="dc-roster-bulk-template" class="dc-input"></select>\n' +
          `\t\t\t\t<button type="button" class="button primary" id="dc-roster-bulk-apply"><?php p($l->t('Apply to selected')); ?></button>\n` +
          `\t\t\t\t<button type="button" class="button" id="dc-roster-bulk-clear"><?php p($l->t('Clear selection')); ?></button>\n` +
          '\t\t\t</div>\n' +
          '\t\t</div>\n' +
          '\t</div>\n' +
          '\t<div id="dc-roster-list-panel" hidden>\n' +
          '\t\t<p id="dc-roster-list-status" class="dc-roster-virtual-status" aria-hidden="true"></p>\n' +
          '\t\t<div class="dc-table-wrap dc-roster-list-scroller" id="dc-assignments-table-wrap" tabindex="0" role="region" aria-labelledby="dc-assignments-title">\n' +
          '\t\t<table class="dc-table" id="dc-assignments-table">\n' +
          `\t\t\t<caption class="dc-sr-only"><?php p($l->t('Assignments for the selected period')); ?></caption>\n` +
          '\t\t\t<thead>\n' +
          '\t\t\t\t<tr>\n' +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('Date')); ?></th>\n` +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('Start')); ?></th>\n` +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('End')); ?></th>\n` +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('Employee')); ?></th>\n` +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('Location')); ?></th>\n` +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('Break')); ?></th>\n` +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('Note')); ?></th>\n` +
          `\t\t\t\t\t<th scope="col"><?php p($l->t('Actions')); ?></th>\n` +
          '\t\t\t\t</tr>\n' +
          '\t\t\t</thead>\n' +
          '\t\t\t<tbody id="dc-assignments-table-body"></tbody>\n' +
          '\t\t</table>\n' +
          '\t\t<div id="dc-roster-assignments-empty" class="dc'... 12068 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        <?php
        
        /**
         * Roster planning page.
         *
         * Layout (top → bottom):
         *  - Period switcher
         *  - Assignments table (+ “Add assignment” opens the create modal)
         *  - Planning checks (conflicts)
         *  - Export (app admins only)
         *
         * @var array $_
         * @var \OCP\IL10N $l
         */
        include __DIR__ . '/common/page-start.php';
        $htmlLang = (string) (($_['clientHints']['htmlLang'] ?? 'en-US'));
        $urls = (array) ($_['urls'] ?? []);
        $rosterPhpL10n = [
        	'conflictMessages' => [],
        ];
        foreach (\OCA\DutyCheck\Service\RosterService::rosterApiConflictMessageKeys() as $enMsg) {
        	$rosterPhpL10n['conflictMessages'][$enMsg] = $l->t($enMsg);
        }
        ?>
        <div id="dc-roster-php-l10n" hidden data-l10n="<?php p(json_encode($rosterPhpL10n, JSON_UNESCAPED_UNICODE)); ?>"></div>
        <section class="dc-card dc-empty dc-empty--quickstart" id="dc-roster-quickstart" hidden aria-labelledby="dc-roster-quickstart-title">
        	<header class="dc-section__header">
        		<div>
        			<h2 id="dc-roster-quickstart-title"><?php p($l->t('Quick start')); ?></h2>
        			<p class="dc-section__sub">
        				<?php p($l->t('How to add a duty assignment without breaking labour rules or double-booking anyone.')); ?>
        			</p>
        		</div>
        		<button type="button" class="dc-hint-dismiss" data-dc-dismiss-hint="roster_quickstart_v1" aria-describedby="dc-roster-quickstart-title">
        			<?php p($l->t('Hide tips')); ?>
        		</button>
        	</header>
        	<ol class="dc-quickstart">
        		<li class="dc-quickstart__item" data-step="select-period">
        			<strong><?php p($l->t('1. Choose the period to plan in')); ?></strong>
        			<p>
        				<?php p($l->t('Use the period selector below. New assignments can only be added to "Open" periods; published and closed ones are read-only.')); ?>
        			</p>
        		</li>
        		<li class="dc-quickstart__item" data-step="add-assignment">
        			<strong><?php p($l->t('2. Add assignments from the table')); ?></strong>
        			<p>
        				<?php p($l->t('Click “Add assignment” next to the assignments list, fill in the three steps in the dialog, and save. Violations of hard rules are blocked with a clear message.')); ?>
        			</p>
        		</li>
        		<li class="dc-quickstart__item" data-step="resolve-conflicts">
        			<strong><?php p($l->t('3. Resolve every conflict')); ?></strong>
        			<p>
        				<?php p($l->t('Issues marked “Must fix” block publishing. “Confirm to continue” (for example less than 11 hours rest between shifts) needs a short written reason — it is stored in the audit log.')); ?>
        			</p>
        		</li>
        		<li class="dc-quickstart__item" data-step="publish-on-periods">
        			<strong><?php p($l->t('4. Publish the period from Periods')); ?></strong>
        			<p>
        				<?php p($l->t('This Roster page stores shifts only. Publishing and closing happen on Periods — employees see My roster only after you publish there. Publish only when every “Must fix” issue is resolved and “Confirm to continue” items are confirmed.')); ?>
        			</p>
        			<p class="dc-quickstart__cta">
        				<a class="button" href="<?php p((string) ($urls['periods'] ?? '#')); ?>"><?php p($l->t('Go to Periods')); ?></a>
        			</p>
        		</li>
        	</ol>
        </section>
        
        <section class="dc-card dc-section dc-roster-panel" id="dc-roster-period-section" aria-labelledby="dc-roster-period-title">
        	<header class="dc-section__header">
        		<div>
        			<h2 id="dc-roster-period-title"><?php p($l->t('Active period')); ?></h2>
        			<p class="dc-section__sub">
        				<?php p($l->t('Your choice here controls the assignment list and planning checks below. Only open periods accept new assignments.')); ?>
        			</p>
        		</div>
        	</header>
        	<div class="dc-form-grid dc-roster-panel__body">
        		<div class="dc-field dc-field--full">
        			<label class="dc-field__label" for="dc-roster-period-switcher">
        				<?php p($l->t('Period')); ?>
        			</label>
        			<select id="dc-roster-period-switcher" class="dc-input" aria-describedby="dc-roster-period-hint"></select>
        			<p id="dc-roster-period-hint" class="dc-field__hint">
        				<?php p($l->t('Periods are listed newest first. Closed periods are read-only. Changing the period updates the list below — no full page reload.')); ?>
        			</p>
        			<p id="dc-roster-ack-stats" class="dc-pill dc-roster-ack-stats" role="status" aria-live="polite" hidden></p>
        			<div class="dc-roster-period__lifecycle" role="note">
        				<p class="dc-field__hint">
        					<?php p($l->t('Publishing and closing happen on the Periods page. Use “Add assignment” on this page to plan shifts.')); ?>
        				</p>
        				<a class="button" href="<?php p((string) ($urls['periods'] ?? '#')); ?>"><?php p($l->t('Go to Periods')); ?></a>
        			</div>
        			<div class="dc-roster-copy-period" id="dc-roster-copy-period" hidden>
        				<label class="dc-field__label" for="dc-roster-copy-source"><?php p($l->t('Copy assignments from another period')); ?></label>
        				<div class="dc-roster-copy-period__row">
        					<select id="dc-roster-copy-source" class="dc-input" aria-describedby="dc-roster-copy-hint"></select>
        					<button type="button" class="button" id="dc-roster-copy-preview"><?php p($l->t('Preview copy')); ?></button>
        					<button type="button" class="button primary" id="dc-roster-copy-apply" disabled><?php p($l->t('Apply copy')); ?></button>
        				</div>
        				<p id="dc-roster-copy-hint" class="dc-field__hint">
        					<?php p($l->t('Preview never writes. Apply only after you confirm the dry-run counts. Conflicts are recomputed after apply.')); ?>
        				</p>
        				<p id="dc-roster-copy-status" class="dc-roster-flash" role="status" aria-live="polite" aria-atomic="true" hidden></p>
        			</div>
        		</div>
        	</div>
        </section>
        
        <section class="dc-card dc-section dc-roster-panel" id="dc-roster-assignments-section" aria-labelledby="dc-assignments-title">
        	<header class="dc-section__header">
        		<div>
        			<h2 id="dc-assignments-title"><?php p($l->t('Assignments')); ?></h2>
        			<p class="dc-section__sub">
        				<?php p($l->t('Shifts for the period selected above. Use the grid for a week overview, or the list for details. Large teams stay fast: only the rows on screen are drawn — scroll to see everyone.')); ?>
        			</p>
        		</div>
        		<div class="dc-section__controls">
        			<div class="dc-roster-view-toggle" role="group" aria-label="<?php p($l->t('Roster view')); ?>">
        				<button type="button" class="button" id="dc-roster-view-grid" aria-pressed="true"><?php p($l->t('Grid')); ?></button>
        				<button type="button" class="button" id="dc-roster-view-list" aria-pressed="false"><?php p($l->t('List')); ?></button>
        			</div>
        			<button type="button" class="button" id="dc-roster-suggest-fill"
        				aria-describedby="dc-roster-suggest-fill-desc">
        				<?php p($l->t('Suggest fill')); ?>
        			</button>
        			<span id="dc-roster-suggest-fill-desc" class="dc-sr-only">
        				<?php p($l->t('Preview filling empty cells from each person’s rotation pattern, then confirm.')); ?>
        			</span>
        			<button type="button" class="button primary dc-roster-add-assignment-trigger" id="dc-roster-add-assignment"
        				aria-describedby="dc-roster-add-assignment-desc" aria-disabled="true">
        				<?php p($l->t('Add assignment')); ?>
        			</button>
        			<span id="dc-roster-add-assignment-desc" class="dc-sr-only">
        				<?php p($l->t('Opens a dialog to create a new shift for the selected open period.')); ?>
        			</span>
        		</div>
        	</header>
        	<p id="dc-roster-assignments-success" class="dc-roster-flash" role="status" aria-live="polite" aria-atomic="true" hidden></p>
        	<div id="dc-coverage-strip" class="dc-coverage-strip" role="status" aria-live="polite" hidden>
        		<span class="dc-coverage-strip__label"><?php p($l->t('Publish readiness')); ?></span>
        		<span id="dc-coverage-strip-text" class="dc-coverage-strip__text"></span>
        	</div>
        	<div id="dc-roster-grid-wrap" class="dc-roster-grid-wrap">
        		<p id="dc-roster-grid-status" class="dc-roster-virtual-status" aria-hidden="true"></p>
        		<div id="dc-roster-grid-scroller" class="dc-roster-grid-scroller" role="region" aria-labelledby="dc-assignments-title">
        			<div
        				id="dc-roster-grid"
        				class="dc-roster-grid"
        				aria-labelledby="dc-assignments-title"
        				aria-describedby="dc-roster-grid-hint"
        			></div>
        		</div>
        		<p id="dc-roster-grid-hint" class="dc-field__hint">
        			<?php p($l->t('Rows are people, columns are days. For a full month, scroll sideways to see every date. Arrow keys move between cells — including people who are scrolled out of view. Page Up and Page Down jump a screen. Home and End jump to the first or last day. Enter opens the shift. Space selects empty cells for bulk fill.')); ?>
        		</p>
        		<div id="dc-roster-bulk-bar" class="dc-roster-bulk-bar" hidden>
        			<p class="dc-roster-bulk-bar__count" id="dc-roster-bulk-count" role="status" aria-live="polite"></p>
        			<label class="dc-field__label" for="dc-roster-bulk-template"><?php p($l->t('Fill selected cells from template')); ?></label>
        			<div class="dc-roster-bulk-bar__row">
        				<select id="dc-roster-bulk-template" class="dc-input"></select>
        				<button type="button" class="button primary" id="dc-roster-bulk-apply"><?php p($l->t('Apply to selected')); ?></button>
        				<button type="button" class="button" id="dc-roster-bulk-clear"><?php p($l->t('Clear selection')); ?></button>
        			</div>
        		</div>
        	</div>
        	<div id="dc-roster-list-panel" hidden>
        		<p id="dc-roster-list-status" class="dc-roster-virtual-status" aria-hidden="true"></p>
        		<div class="dc-table-wrap dc-roster-list-scroller" id="dc-assignments-table-wrap" tabindex="0" role="region" aria-labelledby="dc-assignments-title">
        		<table class="dc-table" id="dc-assignments-table">
        			<caption class="dc-sr-only"><?php p($l->t('Assignments for the selected period')); ?></caption>
        			<thead>
        				<tr>
        					<th scope="col"><?php p($l->t('Date')); ?></th>
        					<th scope="col"><?php p($l->t('Start')); ?></th>
        					<th scope="col"><?php p($l->t('End')); ?></th>
        					<th scope="col"><?php p($l->t('Employee')); ?></th>
        					<th scope="col"><?php p($l->t('Location')); ?></th>
        					<th scope="col"><?php p($l->t('Break')); ?></th>
        					<th scope="col"><?php p($l->t('Note')); ?></th>
        					<th scope="col"><?php p($l->t('Actions')); ?></th>
        				</tr>
        			</thead>
        			<tbody id="dc-assignments-table-body"></tbody>
        		</table>
        		<div id="dc-roster-assignments-empty" class="dc-roster-empty-panel" hidden>
        			<p class="dc-roster-empty-state__text">
        				<?php p($l->t('No assignments in this period yet.')); ?>
        			</p>
        			<p id="dc-roster-empty-add-hint" class="dc-roster-empty-state__hint"></p>
        		</div>
        		</div>
        	</div>
        </section>
        
        <section class="dc-card dc-section dc-roster-panel" id="dc-roster-conflicts-section" aria-labelledby="dc-conflict-title">
        	<header class="dc-section__header">
        		<div>
        			<h2 id="dc-conflict-title"><?php p($l->t('Planning checks')); ?></h2>
        			<p class="dc-section__sub">
        				<?php p($l->t('Automatic checks for the same period as the assignment list. “Must fix” blocks publishing until resolved. “Confirm to continue” lets you proceed after you confirm with a short reason.')); ?>
        			</p>
        		</div>
        		<div class="dc-section__controls">
        			<span id="dc-conflict-summary" class="dc-pill" aria-live="polite">
        				<?php p($l->t('Loading conflicts...')); ?>
        			</span>
        		</div>
        	</header>
        	<ul id="dc-conflict-list" class="dc-conflicts" role="list" aria-label="<?php p($l->t('Planning checks')); ?>" aria-live="polite"></ul>
        </section>
        
        <section class="dc-card dc-section dc-roster-panel" id="dc-roster-marketplace-section" aria-labelledby="dc-marketplace-title">
        	<header class="dc-section__header">
        		<div>
        			<h2 id="dc-marketplace-title"><?php p($l->t('Swaps & open shifts')); ?></h2>
        			<p class="dc-section__sub">
        				<?php p($l->t('Review staff swap requests and post unassigned shifts for the selected period. Approving a pool swap cancels the old shift and opens it for claiming.')); ?>
        			</p>
        		</div>
        		<div class="dc-section__controls">
        			<button type="button" class="button" id="dc-open-shift-create"><?php p($l->t('Post open shift')); ?></button>
        		</div>
        	</header>
        	<div class="dc-form-grid" id="dc-open-shift-form" hidden>
        		<div class="dc-field">
        			<label class="dc-field__label" for="dc-os-date"><?php p($l->t('Date')); ?></label>
        			<input id="dc-os-date" type="date" class="dc-input">
        		</div>
        		<div class="dc-field">
        			<label class="dc-field__label" for="dc-os-location"><?php p($l->t('Location')); ?></label>
        			<select id="dc-os-location" class="dc-input"></select>
        		</div>
        		<div class="dc-field">
        			<label class="dc-field__label" for="dc-os-start"><?php p($l->t('Start')); ?></label>
        			<input id="dc-os-start" type="time" class="dc-input dc-input--time-24h" step="60" lang="en-GB">
        		</div>
        		<div class="dc-field">
        			<label class="dc-field__label" for="dc-os-end"><?php p($l->t('End')); ?></label>
        			<input id="dc-os-end" type="time" class="dc-input dc-input--time-24h" step="60" lang="en-GB">
        		</div>
        		<div class="dc-form-actions">
        			<button type="button" class="button primary" id="dc-os-save"><?php p($l->t('Save open shift')); ?></button>
        		</div>
        	</div>
        	<h3 class="dc-subsection-heading"><?php p($l->t('Pending swap requests')); ?></h3>
        	<ul id="dc-swap-list" class="dc-conflicts" role="list" aria-live="polite"></ul>
        	<p id="dc-swap-empty" class="dc-field__hint" hidden><?php p($l->t('No pending swaps.')); ?></p>
        	<h3 class="dc-subsection-heading"><?php p($l->t('Pending open-shift claims')); ?></h3>
        	<ul id="dc-open-claim-list" class="dc-conflicts" role="list" aria-live="polite"></ul>
        	<p id="dc-open-claim-empty" class="dc-field__hint" hidden><?php p($l->t('No pending claims.')); ?></p>
        </section>
        
        <?php if (!empty($_['isAppAdmin'])): ?>
        <section class="dc-card dc-section dc-roster-panel dc-roster-export" id="dc-roster-admin-export" aria-labelledby="dc-roster-export-title">
        	<header class="dc-section__header">
        		<div>
        			<h2 id="dc-roster-export-title"><?php p($l->t('Export & print')); ?></h2>
        			<p class="dc-section__sub">
        				<?php p($l->t('Download a spreadsheet file or open a clean print layout for the period selected above. Only DutyCheck and server administrators see this section.')); ?>
        			</p>
        			<p class="dc-roster-export__hint" id="dc-roster-export-hint">
        				<?php p($l->t('Files include every assignment row for the active period. Use them for archives, handovers, or regulatory evidence — treat exports like personal data and store them securely.')); ?>
        			</p>
        		</div>
        	</header>
        	<div class="dc-roster-export__actions" role="group" aria-labelledby="dc-roster-export-title">
        		<a class="button primary" id="dc-roster-export-csv" href="#" aria-describedby="dc-roster-export-hint">
        			<?php p($l->t('Download CSV')); ?>
        		</a>
        		<a class="button" id="dc-roster-export-print" href="#" target="_blank" rel="noopener noreferrer" aria-describedby="dc-roster-export-hint">
        			<?php p($l->t('Open printable view')); ?>
        		</a>
        	</div>
        </section>
        <?php endif; ?>
        
        <div id="dc-assignment-form-host" class="dc-assignment-form-host" hidden>
        	<div id="dc-assignment-form-panel" class="dc-assignment-form-panel">
        		<div id="dc-roster-setup-callout" class="dc-callout dc-callout--warning" role="status" aria-live="polite" hidden>
        			<p><strong><?php p($l->t('Setup is required before you can plan duties.')); ?></strong></p>
        			<ul class="dc-callout__list" id="dc-roster-setup-checklist"></ul>
        		</div>
        		<form id="dc-assignment-form" class="dc-form-grid dc-form-grid--roster-assignment" novalidate>
        			<input type="hidden" name="periodId" id="dc-assignment-period" value="">
        			<input type="hidden" name="assignmentId" id="dc-assignment-id" value="">
        			<p id="dc-assignment-form-intro" class="dc-roster-form__intro">
        				<?php p($l->t('Three steps: pick the day, choose who works and where, then enter shift times. People on approved leave are not listed for that day.')); ?>
        			</p>
        			<div class="dc-field dc-field--full" id="dc-assignment-template-field">
        				<label class="dc-field__label" for="dc-assignment-template"><?php p($l->t('Shift template (optional)')); ?></label>
        				<select id="dc-assignment-template" class="dc-input" aria-describedby="dc-assignment-template-hint">
        					<option value=""><?php p($l->t('No template — enter times manually')); ?></option>
        				</select>
        				<p id="dc-assignment-template-hint" class="dc-field__hint">
        					<?php p($l->t('Templates fill start, end, and break. You can still change any field before saving.')); ?>
        				</p>
        			</div>
        			<fieldset class="dc-roster-form__step-group" aria-labelledby="dc-roster-step-day">
        				<h3 id="dc-roster-step-day" class="dc-roster-form__section-heading dc-roster-form__section-heading--step">
        					<span class="dc-roster-form__step-badge" aria-hidden="true">1</span>
        					<?php p($l->t('Pick the duty day')); ?>
        				</h3>
        				<div class="dc-field dc-field--roster-date">
        					<label class="dc-field__label" for="dc-assignment-date"><?php p($l->t('Date')); ?></label>
        					<input id="dc-assignment-date" type="date" name="dutyDate" class="dc-input" lang="<?php p($htmlLang); ?>"
        						aria-describedby="dc-assignment-date-hint" required>
        					<p id="dc-assignment-date-hint" class="dc-field__hint">
        						<?php p($l->t('Only dates inside the open planning period selected on this page.')); ?>
        					</p>
        				</div>
        			</fieldset>
        			<fieldset class="dc-roster-form__step-group" aria-labelledby="dc-roster-step-who">
        				<h3 id="dc-roster-step-who" class="dc-roster-form__section-heading dc-roster-form__section-heading--step">
        					<span class="dc-roster-form__step-badge" aria-hidden="true">2</span>
        					<?php p($l->t('Who and where')); ?>
        				</h3>
        				<div class="dc-field dc-field--roster-employee">
        					<label class="dc-field__label" for="dc-assignment-employee"><?php p($l->t('Employee')); ?></label>
        					<select id="dc-assignment-employee" name="employeeId" class="dc-input" required
        						aria-describedby="dc-assignment-employee-hint dc-assignment-form-feedback"></select>
        					<p id="dc-assignment-employee-hint" class="dc-field__hint" aria-live="polite" aria-atomic="true">
        						<?php p($l->t('Pick a date in step 1 first. People on approved leave that day are not listed here.')); ?>
        					</p>
        				</div>
        				<div class="dc-field dc-field--roster-location">
        					<label class="dc-field__label" for="dc-assignment-location"><?php p($l->t('Location')); ?></label>
        					<select id="dc-assignment-location" name="locationId" class="dc-input" required
        						aria-describedby="dc-assignment-location-tz-hint"></select>
        					<p id="dc-assignment-location-tz-hint" class="dc-field__hint" aria-live="polite" aria-atomic="true" hidden></p>
        				</div>
        			</fieldset>
        			<fieldset class="dc-roster-form__step-group" aria-labelledby="dc-roster-shift-schedule-label">
        				<h3 id="dc-roster-shift-schedule-label" class="dc-roster-form__section-heading dc-roster-form__section-heading--step">
        					<span class="dc-roster-form__step-badge" aria-hidden="true">3</span>
        					<?php p($l->t('Shift times')); ?>
        				</h3>
        				<div class="dc-field dc-field--full dc-field--roster-time-hint">
        					<p id="dc-roster-24h-hint" class="dc-field__hint dc-roster-24h-hint">
        						<?php p($l->t('Times use the 24-hour clock (HH:mm). For shifts past midnight, set an end time earlier than the start time (e.g. 22:00–06:00).')); ?>
        					</p>
        				</div>
        				<div class="dc-field dc-field--roster-start">
        					<label class="dc-field__label" for="dc-assignment-start"><?php p($l->t('Start')); ?></label>
        					<input id="dc-assignment-start" type="time" name="startTime" class="dc-input dc-input--time-24h" step="60"
        						lang="en-GB"
        						aria-label="<?php p($l->t('Start time (24-hour HH:mm)')); ?>"
        						aria-describedby="dc-roster-24h-hint dc-assignment-location-tz-hint" required>
        				</div>
        				<div class="dc-field dc-field--roster-end">
        					<label class="dc-field__label" for="dc-assignment-end"><?php p($l->t('End')); ?></label>
        					<input id="dc-assignment-end" type="time" name="endTime" class="dc-input dc-input--time-24h" step="60"
        						lang="en-GB"
        						aria-label="<?php p($l->t('End time (24-hour HH:mm)')); ?>"
        						aria-describedby="dc-roster-24h-hint dc-assignment-location-tz-hint dc-assignment-rest-hint" required>
        				</div>
        				<p id="dc-assignment-rest-hint" class="dc-field__hint dc-field--full dc-field--roster-rest-hint">
        					<?php p($l->t('If Save asks you to confirm, a planning rule was triggered (for example less than 11 hours rest between shifts). You can review issues in')); ?>
        					<a class="dc-hint-link" href="#dc-roster-conflicts-section"><?php p($l->t('Planning checks')); ?></a><?php p(' '); ?><?php p($l->t('on this page.')); ?>
        				</p>
        			</fieldset>
        			<hr class="dc-form-grid__divider" aria-hidden="true">
        			<div class="dc-roster-form__extras" role="group" aria-labelledby="dc-roster-form-extras-label">
        				<h3 id="dc-roster-form-extras-label" class="dc-roster-form__section-heading">
        					<?php p($l->t('Break and note')); ?>
        				</h3>
        			<div class="dc-field dc-field--roster-break">
        				<label class="dc-field__label" for="dc-assignment-break"><?php p($l->t('Break (minutes)')); ?></label>
        				<input id="dc-assignment-break" type="number" name="breakMinutes" class="dc-input dc-input--num"
        					min="0" max="720" step="5" value="0" required
        					aria-describedby="dc-assignment-break-prefill dc-assignment-break-hint">
        				<p id="dc-assignment-break-prefill" class="dc-roster-break-prefill" aria-live="polite" aria-atomic="true"></p>
        				<p id="dc-assignment-break-hint" class="dc-field__hint">
        					<?php p($l->t('Unpaid break during the shift. Use 0 when there is no break.')); ?>
        				</p>
        			</div>
        			<div class="dc-field dc-field--roster-note">
        				<label class="dc-field__label" for="dc-assignment-note"><?php p($l->t('Note (optional)')); ?></label>
        				<input id="dc-assignment-note" type="text" name="note" class="dc-input" maxlength="512" autocomplete="off">
        			</div>
        			</div>
        			<div id="dc-assignment-form-feedback" class="dc-roster-form__feedback" role="status" aria-live="polite" aria-atomic="true" hidden></div>
        			<div class="dc-form-actions dc-form-actions--roster">
        				<button type="submit" class="button primary" tabindex="-1" aria-hidden="true" hidden><?php p($l->t('Save assignment')); ?></button>
        				<button type="button" id="dc-assignment-form-clear" class="button"><?php p($l->t('Clear entered fields')); ?></button>
        			</div>
        		</form>
        	</div>
        </div>
        <?php include __DIR__ . '/common/page-end.php'; ?>
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:292:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 2.586659
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 71.247194
killed skip_scroller_tabindex
== mutant month_grid_auto_fit_regression (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

...F..                                                              6 / 6 (100%)

Time: 00:00.013, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testTemplateAndCssKeepGrannyStatusAndPrintAll
Failed asserting that '/*\n
 * DutyCheck design system.\n
 *\n
 * Authored to mirror the BudgetCheck visual language while keeping a\n
 * distinct `dc-*` namespace. Colors come from Nextcloud variables\n
 * (--color-*, --color-main-*, --color-primary-element, --color-success,\n
 * --color-warning, --color-error). App-specific tokens are prefixed\n
 * `--dc-*` and only define spacing, radii, typography scale, shadows, and\n
 * tinted surfaces derived via `color-mix()` so the app matches every NC\n
 * theme without hard-coded hex values.\n
 *\n
 * Accessibility (WCAG 2.1 AA):\n
 *  - Visible focus rings on every focusable element.\n
 *  - 4.5:1 text contrast on text, surfaces tinted not coloured-out.\n
 *  - 44 × 44 px minimum touch targets on interactive controls.\n
 *  - Reflowing grids; no information conveyed by colour alone (status\n
 *    badges always carry a label and shape cue).\n
 *  - Mobile-first layout: base styles target <768px; min-width 480px,\n
 *    768px (Nextcloud tablet), 520px/900px (quickstart) build upward.\n
 *  - prefers-reduced-motion honoured.\n
 *\n
 * Buttons and button-like controls (`<button>`, `.button`,\n
 * `input[type=submit]`) use inline-flex with `min-height: 44px` so labels\n
 * and icons stay vertically and horizontally centred on every browser.\n
 */\n
\n
/* ── Tokens ──────────────────────────────────────────────────── */\n
:root {\n
	/* Spacing scale (4 px grid — never invent 10/18/20 px) */\n
	--dc-space-1: 4px;\n
	--dc-space-2: 8px;\n
	--dc-space-3: 12px;\n
	--dc-space-4: 16px;\n
	--dc-space-5: 24px;\n
	--dc-space-6: 32px;\n
	--dc-space-7: 48px;\n
	--dc-space-8: 64px;\n
\n
	/* Radii */\n
	--dc-radius-sm: 6px;\n
	--dc-radius-md: 12px;\n
	--dc-radius-lg: 16px;\n
\n
	/* Type scale */\n
	--dc-fs-xs: 0.75rem;\n
	--dc-fs-sm: 0.875rem;\n
	--dc-fs-md: 1rem;\n
	--dc-fs-lg: 1.125rem;\n
	--dc-fs-xl: 1.5rem;\n
	--dc-fs-2xl: 1.875rem;\n
	--dc-line-tight: 1.2;\n
	--dc-line-base: 1.45;\n
\n
	--dc-breakpoint-sm: 480px;\n
	--dc-breakpoint-md: 768px;\n
	--dc-breakpoint-lg: 1024px;\n
\n
	--dc-icon-sm: 1rem;\n
	--dc-icon-md: 1.125rem;\n
	--dc-icon-lg: 1.375rem;\n
	--dc-icon-well: 2.25rem;\n
	--dc-touch: 44px;\n
\n
	--dc-tab-num: tabular-nums;\n
}\n
\n
/*\n
 * Theme-dependent tokens MUST live on themed surfaces (body / .dc-app / .dc-modal),\n
 * not :root. Nextcloud sets --color-main-* on body; resolving color-mix on :root\n
 * falls back to light #fff while text inherits dark-theme --color-main-text\n
 * → WCAG contrast ~1.1:1. Modals mount on document.body outside #app-content.\n
 *\n
 * Tints mix INTO --color-main-background (AZC contract) — never into transparent —\n
 * so callouts stay visible on every NC theme including high-contrast.\n
 */\n
body,\n
#app-content.dc-app,\n
.dc-app,\n
.dc-modal,\n
.dc-toasts {\n
	--dc-overlay-top: var(--header-height, 50px);\n
	--dc-overlay-height: calc(100dvh - var(--header-height, 50px));\n
\n
	/* Shadows */\n
	--dc-shadow-sm: 0 1px 2px color-mix(in srgb, var(--color-main-text, #1d1d1d) 8%, transparent);\n
	--dc-shadow-md: 0 8px 24px color-mix(in srgb, var(--color-main-text, #1d1d1d) 12%, transparent);\n
\n
	/* Surfaces */\n
	--dc-bg-soft: color-mix(in srgb, var(--color-main-background, #fff) 96%, var(--color-main-text, #1d1d1d) 4%);\n
	--dc-bg-card: var(--color-main-background, #fff);\n
	--dc-bg-stripe: color-mix(in srgb, var(--color-main-background, #fff) 92%, var(--color-main-text, #1d1d1d) 8%);\n
	--dc-border: color-mix(in srgb, var(--color-main-text, #1d1d1d) 12%, transparent);\n
	--dc-border-strong: color-mix(in srgb, var(--color-main-text, #1d1d1d) 24%, transparent);\n
	/* Prefer Nextcloud's AA-tested muted token (same approach as BudgetCheck).\n
	   Fallback mixes against the background (not transparent) so secondary text\n
	   still clears 4.5:1 on tinted surfaces across custom themes. */\n
	--dc-muted: var(\n
		--color-text-maxcontrast,\n
		color-mix(in srgb, var(--color-main-text, #1d1d1d) 67%, var(--color-main-background, #fff))\n
	);\n
	--dc-text: var(--color-main-text, #1d1d1d);\n
\n
	/* Severity tints — mix into main-background (AZC / design-system §1.5) */\n
	--dc-tint-info: color-mix(in srgb, var(--color-primary-element, #0082c9) 16%, var(--color-main-background, #fff));\n
	--dc-tint-success: color-mix(in srgb, var(--color-success-text, var(--color-success, #46ba61)) 18%, var(--color-main-background, #fff));\n
	/* NC34+ --color-success is a pale *surface*; borders/icons need the ink / element token. */\n
	--dc-tint-success-border: color-mix(in srgb, var(--color-element-success, var(--color-success-text, #005416)) 55%, var(--color-main-background, #fff));\n
	--dc-tint-warning: color-mix(in srgb, var(--color-warning-text, var(--color-warning, #c98600)) 22%, var(--color-main-background, #fff));\n
	--dc-tint-critical: color-mix(in srgb, var(--color-error-text, var(--color-error, #cc4646)) 18%, var(--color-main-background, #fff));\n
	--dc-scrim: color-mix(in srgb, var(--color-main-text, #000) 60%, transparent);\n
\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
\n
/* ── Skip link / live regions ────────────────────────────────── */\n
.dc-skip-link {\n
	position: absolute;\n
	left: -9999px;\n
	top: var(--dc-space-2);\n
	padding: var(--dc-space-2) var(--dc-space-4);\n
	background: var(--color-primary-element, #0082c9);\n
	color: var(--color-primary-element-text, #fff);\n
	border-radius: var(--dc-radius-sm);\n
	z-index: 1000;\n
	font-weight: 600;\n
	text-decoration: none;\n
}\n
.dc-skip-link:focus,\n
.dc-skip-link:focus-visible {\n
	left: var(--dc-space-3);\n
	outline: 3px solid var(--color-primary-element-text, #fff);\n
	outline-offset: 2px;\n
}\n
.dc-sr-only {\n
	position: absolute !important;\n
	width: 1px;\n
	height: 1px;\n
	margin: -1px;\n
	padding: 0;\n
	border: 0;\n
	overflow: hidden;\n
	clip: rect(0 0 0 0);\n
	white-space: nowrap;\n
}\n
\n
/* ── App shell ───────────────────────────────────────────────── */\n
#app-content.dc-app,\n
.dc-app {\n
	display: flex;\n
	flex-direction: column;\n
	min-height: 100%;\n
	background: var(--dc-bg-soft);\n
	overflow-x: clip;\n
}\n
\n
/*\n
 * Nextcloud core sets `#app-content #app-content-wrapper` to a flex row\n
 * stretching its children. DutyCheck stacks header + main, so we must\n
 * force column direction here, otherwise the breadcrumb sits next to the\n
 * page body.\n
 */\n
#app-content.dc-app #app-content-wrapper.dc-shell,\n
.dc-shell {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: stretch;\n
	width: 100%;\n
	max-width: none;\n
	margin: 0;\n
	padding: max(var(--dc-space-4), env(safe-area-inset-top, 0px))\n
		max(var(--dc-space-4), env(safe-area-inset-right, 0px))\n
		max(var(--dc-space-5), env(safe-area-inset-bottom, 0px))\n
		max(var(--dc-space-4), env(safe-area-inset-left, 0px));\n
	box-sizing: border-box;\n
	overflow-x: clip;\n
}\n
/* Shell width modifiers (AZC parity — reading measure stays on prose, not the page) */\n
#app-content.dc-app #app-content-wrapper.dc-shell--wide,\n
.dc-shell--wide {\n
	max-width: none;\n
	margin-inline: 0;\n
}\n
#app-content.dc-app #app-content-wrapper.dc-shell--constrained,\n
.dc-shell--constrained {\n
	max-width: 72rem;\n
	margin-inline: auto;\n
}\n
#app-content.dc-app #app-content-wrapper.dc-shell--minimal,\n
.dc-shell--minimal {\n
	max-width: 40rem;\n
	margin-inline: auto;\n
}\n
#app-content.dc-app #dc-main-content.dc-main,\n
.dc-main {\n
	flex: 1 1 auto;\n
	min-width: 0;\n
	max-width: 100%;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-6);\n
	outline: none;\n
	overflow-x: clip;\n
}\n
.dc-page-stack {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: stretch;\n
	gap: var(--dc-space-5);\n
	width: 100%;\n
	max-width: none;\n
	min-width: 0;\n
}\n
/* Anchor jumps clear the Nextcloud header + page chrome. */\n
#app-content.dc-app .dc-card[id],\n
.dc-card[id] {\n
	scroll-margin-top: 5.5rem;\n
}\n
#dc-main-content.dc-main:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 2px;\n
}\n
\n
/* Hide-when-hidden override (avoids form fields inside .dc-field stomping `[hidden]`) */\n
.dc-app [hidden],\n
.dc-modal [hidden] {\n
	display: none !important;\n
}\n
\n
/* ── Header / breadcrumb ─────────────────────────────────────── */\n
#app-content.dc-app .dc-page-header,\n
.dc-page-header {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	margin-bottom: var(--dc-space-5);\n
}\n
.dc-breadcrumb {\n
	font-size: var(--dc-fs-sm);\n
	color: var(--dc-muted);\n
}\n
.dc-breadcrumb ol {\n
	display: flex;\n
	flex-wrap: wrap;\n
	align-items: center;\n
	gap: var(--dc-space-2);\n
	padding: 0;\n
	margin: 0;\n
	list-style: none;\n
}\n
.dc-breadcrumb__brand {\n
	color: inherit;\n
	font-weight: 600;\n
	text-decoration: none;\n
}\n
.dc-breadcrumb__brand:hover { text-decoration: underline; }\n
.dc-breadcrumb__parent {\n
	color: inherit;\n
	text-decoration: none;\n
	overflow-wrap: anywhere;\n
	min-width: 0;\n
}\n
.dc-breadcrumb__parent:hover { text-decoration: underline; }\n
.dc-breadcrumb__current {\n
	font-weight: 500;\n
	color: var(--color-main-text, #1d1d1d);\n
	/* Long translated titles must wrap, not push the shell into overflow clip. */\n
	overflow-wrap: anywhere;\n
	min-width: 0;\n
}\n
.dc-breadcrumb__sep { color: var(--dc-muted); }\n
\n
.dc-page-header__main {\n
	display: grid;\n
	grid-template-columns: 48px 1fr;\n
	gap: var(--dc-space-4);\n
	align-items: start;\n
}\n
.dc-page-header__icon {\n
	display: grid;\n
	place-items: center;\n
	width: 48px;\n
	height: 48px;\n
	border-radius: var(--dc-radius-md);\n
	background: var(--dc-tint-info);\n
	color: var(--color-primary-element, #0082c9);\n
}\n
.dc-page-header__icon-svg {\n
	width: 24px;\n
	height: 24px;\n
	min-width: 24px;\n
	min-height: 24px;\n
}\n
.dc-page-header__actions {\n
	grid-column: 1 / -1;\n
}\n
/*\n
 * The title column is a `1fr` grid track; without `min-width: 0` its\n
 * min-content size wins and the shell's `overflow-x: clip` cuts the h1\n
 * mid-glyph on very narrow viewports (split panes, <340px). `anywhere`\n
 * also lets long single-word titles participate in min-content shrinking.\n
 */\n
.dc-page-header__text {\n
	min-width: 0;\n
}\n
.dc-page-header__text h1 {\n
	margin: 0;\n
	font-size: var(--dc-fs-2xl);\n
	line-height: var(--dc-line-tight);\n
	overflow-wrap: anywhere;\n
}\n
.dc-page-header__lead {\n
	margin: var(--dc-space-1) 0 0 0;\n
	color: var(--dc-muted);\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
	max-width: 60ch;\n
	overflow-wrap: anywhere;\n
}\n
.dc-page-header__actions {\n
	display: flex;\n
	gap: var(--dc-space-2);\n
	flex-wrap: wrap;\n
	min-height: 36px;\n
	align-self: center;\n
}\n
\n
/* ── Role / scope strip ──────────────────────────────────────── */\n
/*\n
 * Neutralise Nextcloud core apps.css definition-list chrome:\n
 *   dl { padding: 12px 0 }\n
 *   dt, dd { display: inline-block; padding: 12px; padding-inline-start: 0 }\n
 *   dt { width: 130px; white-space: nowrap; text-align: end }\n
 * Without these resets, "Role" sits end-aligned in a 130px box and looks like\n
 * a missing icon / left gap. Use logical start alignment for RTL.\n
 */\n
#app-content.dc-app .dc-scope-strip,\n
.dc-app .dc-scope-strip {\n
	display: grid;\n
	grid-template-columns: 1fr;\n
	gap: var(--dc-space-3);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	background: var(--dc-bg-card);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	font-size: var(--dc-fs-sm);\n
	margin: 0;\n
}\n
#app-content.dc-app .dc-scope-strip__item,\n
.dc-app .dc-scope-strip__item {\n
	display: grid;\n
	grid-template-columns: minmax(5.5rem, auto) minmax(0, 1fr);\n
	gap: var(--dc-space-2) var(--dc-space-3);\n
	align-items: center;\n
	justify-items: stretch;\n
	min-width: 0;\n
}\n
#app-content.dc-app .dc-scope-strip dt,\n
#app-content.dc-app .dc-scope-strip dd,\n
#app-content.dc-app .dc-scope-strip__label,\n
#app-content.dc-app .dc-scope-strip__value,\n
.dc-app .dc-scope-strip dt,\n
.dc-app .dc-scope-strip dd,\n
.dc-app .dc-scope-strip__label,\n
.dc-app .dc-scope-strip__value {\n
	display: block;\n
	float: none;\n
	width: auto;\n
	max-width: 100%;\n
	padding: 0;\n
	margin: 0;\n
	text-align: start;\n
	white-space: normal;\n
}\n
#app-content.dc-app .dc-scope-strip__label,\n
.dc-app .dc-scope-strip__label {\n
	color: var(--dc-muted);\n
	font-weight: 600;\n
	text-transform: uppercase;\n
	letter-spacing: 0.05em;\n
	font-size: var(--dc-fs-xs);\n
	line-height: var(--dc-line-tight);\n
}\n
#app-content.dc-app .dc-scope-strip__value,\n
.dc-app .dc-scope-strip__value {\n
	font-weight: 600;\n
	color: var(--color-main-text, #1d1d1d);\n
	min-width: 0;\n
	overflow-wrap: anywhere;\n
}\n
@media (min-width: 640px) {\n
	#app-content.dc-app .dc-scope-strip,\n
	.dc-app .dc-scope-strip {\n
		grid-template-columns: repeat(3, minmax(0, 1fr));\n
		gap: var(--dc-space-4);\n
	}\n
	#app-content.dc-app .dc-scope-strip__item,\n
	.dc-app .dc-scope-strip__item {\n
		grid-template-columns: 1fr;\n
		gap: var(--dc-space-1);\n
		padding-inline-start: var(--dc-space-4);\n
		border-inline-start: 1px solid var(--dc-border);\n
	}\n
	#app-content.dc-app .dc-scope-strip__item:first-child,\n
	.dc-app .dc-scope-strip__item:first-child {\n
		padding-inline-start: 0;\n
		border-inline-start: 0;\n
	}\n
}\n
\n
/* ── Cards and sections ──────────────────────────────────────── */\n
.dc-card {\n
	background: var(--dc-bg-card);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	box-shadow: var(--dc-shadow-sm);\n
	padding: var(--dc-space-4);\n
	min-width: 0;\n
	max-width: 100%;\n
	box-sizing: border-box;\n
}\n
.dc-section {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-4);\n
	min-width: 0;\n
	max-width: 100%;\n
}\n
.dc-section__header {\n
	display: grid;\n
	grid-template-columns: 1fr;\n
	align-items: start;\n
	gap: var(--dc-space-3) var(--dc-space-4);\n
}\n
/* Same min-content escape hatch as the page header (see .dc-page-header__text). */\n
.dc-section__header > div {\n
	min-width: 0;\n
}\n
.dc-section__controls {\n
	justify-self: stretch;\n
}\n
.dc-section__header > .dc-hint-dismiss {\n
	justify-self: stretch;\n
	text-align: center;\n
}\n
.dc-section__header h2 {\n
	margin: 0;\n
	font-size: var(--dc-fs-xl);\n
	line-height: var(--dc-line-tight);\n
	overflow-wrap: anywhere;\n
}\n
.dc-section__sub {\n
	margin: var(--dc-space-1) 0 0;\n
	color: var(--dc-muted);\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-section__controls {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
	min-width: 0;\n
}\n
\n
/* ── Badges / pills / chips ──────────────────────────────────── */\n
.dc-badge {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-1);\n
	padding: var(--dc-space-1) var(--dc-space-3);\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 600;\n
	border-radius: 999px;\n
	letter-spacing: 0.04em;\n
	text-transform: uppercase;\n
	background: var(--dc-bg-stripe);\n
	color: var(--color-main-text, #1d1d1d);\n
	border: 1px solid var(--dc-border);\n
	white-space: normal;\n
	overflow-wrap: anywhere;\n
	max-width: 100%;\n
}\n
.dc-badge--info { background: var(--dc-tint-info); border-color: transparent; }\n
.dc-badge--success { background: var(--dc-tint-success); border-color: transparent; }\n
.dc-badge--neutral {\n
	background: color-mix(in srgb, var(--color-main-text, #1d1d1d) 8%, var(--color-main-background, #fff) 92%);\n
	border-color: var(--dc-border-strong);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-badge--warning { background: var(--dc-tint-warning); border-color: transparent; }\n
.dc-badge--critical { background: var(--dc-tint-critical); border-color: transparent; }\n
\n
/* Status badges with icon hint (open/published/closed, conflict severity, absence) */\n
.dc-status-badge {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-1);\n
	min-height: 1.75rem;\n
	padding: 2px 10px;\n
	border-radius: 999px;\n
	background: var(--dc-bg-stripe);\n
	color: var(--color-main-text, #1d1d1d);\n
	border: 1px solid var(--dc-border);\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 700;\n
	letter-spacing: 0.03em;\n
	text-transform: uppercase;\n
	white-space: nowrap;\n
}\n
.dc-status-badge::before {\n
	content: '';\n
	display: inline-block;\n
	width: 8px;\n
	height: 8px;\n
	border-radius: 50%;\n
	background: currentColor;\n
	opacity: 0.7;\n
}\n
/* Semantic text colors: modern Nextcloud ships --color-{success,error,…} as\n
   pale *background* tints; the AA-contrast text companions are the\n
   --color-*-text variables. Fall back to a darkened mix for legacy themes. */\n
.dc-status-badge--open,\n
.dc-status-badge--pending {\n
	background: var(--dc-tint-info);\n
	border-color: transparent;\n
	color: var(--color-info-text, color-mix(in srgb, var(--color-primary-element, #0082c9) 80%, var(--color-main-text, #1d1d1d) 20%));\n
}\n
.dc-status-badge--published,\n
.dc-status-badge--approved {\n
	background: var(--dc-tint-success);\n
	border-color: transparent;\n
	color: var(--color-success-text, color-mix(in srgb, var(--color-success, #46ba61) 70%, var(--color-main-text, #1d1d1d) 30%));\n
}\n
.dc-status-badge--closed,\n
.dc-status-badge--cancelled {\n
	background: var(--dc-bg-stripe);\n
	border-color: var(--dc-border);\n
	color: var(--dc-muted);\n
}\n
.dc-status-badge--rejected {\n
	background: var(--dc-tint-critical);\n
	border-color: transparent;\n
	color: var(--color-error-text, color-mix(in srgb, var(--color-error, #cc4646) 75%, var(--color-main-text, #1d1d1d) 25%));\n
}\n
\n
/* Planning issue severity (must fix / confirm to continue) */\n
.dc-severity {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-1);\n
	padding: 2px 10px;\n
	border-radius: 999px;\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 700;\n
	letter-spacing: 0.01em;\n
	text-transform: none;\n
}\n
.dc-hint-link {\n
	color: var(--color-primary-element, #00679e);\n
	font-weight: 600;\n
	text-decoration: underline;\n
	text-underline-offset: 2px;\n
}\n
.dc-hint-link:hover,\n
.dc-hint-link:focus-visible {\n
	/* NOT --color-primary-element-text: that is the on-primary (white) color. */\n
	color: var(--color-primary-element-hover, #00507a);\n
	text-decoration-thickness: 2px;\n
}\n
.dc-hint-link:focus-visible {\n
	outline: 2px solid var(--color-primary-element, #00679e);\n
	outline-offset: 2px;\n
	border-radius: 2px;\n
}\n
.dc-severity--hard {\n
	background: var(--dc-tint-critical);\n
	color: var(--color-error-text, color-mix(in srgb, var(--color-error, #cc4646) 80%, var(--color-main-text, #1d1d1d) 20%));\n
}\n
.dc-severity--soft {\n
	background: var(--dc-tint-warning);\n
	color: var(--color-warning-text, color-mix(in srgb, var(--color-warning, #c98600) 70%, var(--color-main-text, #1d1d1d) 30%));\n
}\n
.dc-severity--info {\n
	background: var(--dc-tint-info);\n
	color: var(--color-info-text, color-mix(in srgb, var(--color-primary-element, #0082c9) 80%, var(--color-main-text, #1d1d1d) 20%));\n
}\n
\n
.dc-pill {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-1);\n
	padding: 2px 10px;\n
	border-radius: 999px;\n
	background: var(--dc-bg-stripe);\n
	color: var(--dc-muted);\n
	font-size: var(--dc-fs-xs);\n
	font-variant-numeric: var(--dc-tab-num);\n
	min-height: 1.75rem;\n
}\n
.dc-pill[aria-busy='true'] {\n
	font-style: italic;\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-chip-list {\n
	list-style: none;\n
	margin: var(--dc-space-2) 0 0;\n
	padding: 0;\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
	min-height: 2.75rem;\n
}\n
.dc-chip {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-2);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-stripe);\n
	border: 1px solid var(--dc-border);\n
	font-size: var(--dc-fs-sm);\n
	max-width: 100%;\n
}\n
.dc-chip__text {\n
	font-variant-numeric: var(--dc-tab-num);\n
	overflow-wrap: anywhere;\n
}\n
.dc-chip__remove {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	border: 0;\n
	background: transparent;\n
	cursor: pointer;\n
	color: var(--dc-muted);\n
	padding: var(--dc-space-1);\n
	min-width: 44px;\n
	min-height: 44px;\n
	border-radius: var(--dc-radius-sm);\n
	line-height: 1;\n
	font-size: 1.25rem;\n
	box-sizing: border-box;\n
}\n
.dc-chip__remove:hover {\n
	color: var(--color-main-text, #1d1d1d);\n
	background: color-mix(in srgb, var(--color-main-text, #1d1d1d) 8%, transparent);\n
}\n
\n
/* ── Empty / hint cards / callouts ───────────────────────────── */\n
.dc-empty {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
}\n
.dc-empty p, .dc-empty h2 { margin: 0; }\n
.dc-callout {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-tint-info);\n
}\n
.dc-callout--warning { background: var(--dc-tint-warning); }\n
.dc-callout--critical { background: var(--dc-tint-critical); }\n
.dc-callout--success { background: var(--dc-tint-success); }\n
.dc-callout p { margin: 0; }\n
.dc-callout strong { font-weight: 700; }\n
.dc-callout__title {\n
	margin: 0;\n
	font-size: var(--dc-fs-lg, 1.125rem);\n
	font-weight: 700;\n
	line-height: var(--dc-line-tight, 1.25);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-callout__actions {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	margin-top: var(--dc-space-1);\n
}\n
.dc-company-access-banner {\n
	margin-bottom: var(--dc-space-5);\n
}\n
.dc-company-access-banner .dc-callout__title {\n
	font-size: var(--dc-fs-lg, 1.125rem);\n
}\n
.dc-callout__actions .button {\n
	min-height: 44px;\n
}\n
.dc-callout__hint { color: var(--color-main-text, #1d1d1d); font-size: var(--dc-fs-sm); }\n
/* Tinted callout backgrounds cost ~1 contrast step: muted ink tuned for the\n
   main background drops below 4.5:1 (WCAG 1.4.3) on them, so any muted copy\n
   nested in a callout is promoted to full-contrast ink. */\n
.dc-callout .dc-field__hint,\n
.dc-callout .dc-section__sub,\n
.dc-callout .dc-loading {\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-loading {\n
	color: var(--dc-muted);\n
	font-style: italic;\n
}\n
\n
/* Quickstart cards */\n
.dc-quickstart {\n
	list-style: decimal inside;\n
	padding: 0;\n
	margin: 0;\n
	display: grid;\n
	gap: var(--dc-space-3);\n
	grid-template-columns: 1fr;\n
	min-width: 0;\n
	max-width: 100%;\n
}\n
@media (min-width: 520px) {\n
	.dc-quickstart {\n
		grid-template-columns: repeat(2, minmax(0, 1fr));\n
	}\n
}\n
@media (min-width: 900px) {\n
	.dc-quickstart {\n
		grid-template-columns: repeat(3, minmax(0, 1fr));\n
	}\n
	/* Keep the “publish on Periods” step from squeezing into a 4th skinny column */\n
	.dc-quickstart__item[data-step="publish-on-periods"] {\n
		grid-column: 1 / -1;\n
	}\n
}\n
.dc-quickstart__item {\n
	padding: var(--dc-space-4);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-card);\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	min-width: 0;\n
	max-width: 100%;\n
	overflow-wrap: anywhere;\n
	word-break: break-word;\n
}\n
.dc-quickstart__item p { margin: 0; color: var(--dc-muted); font-size: var(--dc-fs-sm); overflow-wrap: anywhere; }\n
.dc-quickstart__cta {\n
	margin-top: var(--dc-space-3);\n
	display: flex;\n
	justify-content: center;\n
	width: 100%;\n
	max-width: 100%;\n
	text-align: center;\n
	min-width: 0;\n
}\n
.dc-quickstart__item strong { font-size: var(--dc-fs-md); line-height: var(--dc-line-tight); color: var(--color-main-text, #1d1d1d); overflow-wrap: anywhere; }\n
.dc-quickstart__item .button,\n
.dc-quickstart__item .dc-btn,\n
.dc-quickstart__cta .button,\n
.dc-quickstart__cta .dc-btn {\n
	align-self: center;\n
	min-height: 44px;\n
	margin-top: auto;\n
	text-align: center;\n
}\n
\n
/* Setup progress (dashboard — not dismissable until complete)\n
 *\n
 * NC34+ ships --color-success / --color-error / --color-warning as pale\n
 * *surface* tints. Never put --color-primary-element-text (white) on them —\n
 * measured white-on-#D8F3DA ≈ 1.18:1 (fails WCAG 1.4.11). Use the\n
 * --color-*-text companions (same contract as .dc-status-badge--*).\n
 */\n
.dc-setup-checklist {\n
	list-style: none;\n
	padding: 0;\n
	margin: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
}\n
.dc-setup-checklist__item {\n
	display: grid;\n
	grid-template-columns: var(--dc-touch) 1fr;\n
	gap: var(--dc-space-3);\n
	align-items: start;\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-card);\n
}\n
/* Done rows collapse — one glance, no competing CTAs. */\n
.dc-setup-checklist__item.is-done {\n
	border-color: var(--dc-tint-success-border, var(--dc-border));\n
	background: var(--dc-tint-success);\n
	align-items: center;\n
	padding-block: var(--dc-space-2);\n
}\n
.dc-setup-checklist__item.is-done .dc-setup-checklist__hint {\n
	display: none;\n
}\n
.dc-setup-checklist__item.is-current {\n
	border-color: var(--color-primary-element, #00679e);\n
	border-width: 2px;\n
	padding: var(--dc-space-4);\n
	background: var(--dc-bg-card);\n
	box-shadow: var(--dc-shadow-sm);\n
}\n
.dc-setup-checklist__status {\n
	display: flex;\n
	align-items: center;\n
	justify-content: center;\n
	width: var(--dc-touch);\n
	height: var(--dc-touch);\n
	border-radius: 50%;\n
	font-size: 1.25rem;\n
	font-weight: 700;\n
	line-height: 1;\n
	flex-shrink: 0;\n
	background: var(--color-background-dark, #e8e8e8);\n
	color: var(--color-main-text, #1d1d1d);\n
	border: 2px solid transparent;\n
	box-sizing: border-box;\n
}\n
.dc-setup-checklist__item.is-done .dc-setup-checklist__status {\n
	/* Pale success surface + dark success ink (≥ 4.5:1). Ring uses the\n
	   saturated element token so the mark stays visible without colour alone. */\n
	background: var(--color-success, var(--dc-tint-success));\n
	color: var(--color-success-text, color-mix(in srgb, var(--color-success, #46ba61) 70%, var(--color-main-text, #1d1d1d) 30%));\n
	border-color: var(--color-element-success, var(--color-success-text, #005416));\n
}\n
.dc-setup-checklist__item.is-current .dc-setup-checklist__status {\n
	background: var(--color-primary-element, #00679e);\n
	color: var(--color-primary-element-text, #fff);\n
	border-color: var(--color-primary-element, #00679e);\n
}\n
.dc-setup-checklist__body {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	min-width: 0;\n
}\n
.dc-setup-checklist__body strong {\n
	font-size: var(--dc-fs-md);\n
	line-height: var(--dc-line-tight);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-setup-checklist__item.is-done .dc-setup-checklist__body strong {\n
	font-weight: 600;\n
	color: var(--color-success-text, var(--color-main-text, #1d1d1d));\n
}\n
.dc-setup-checklist__hint {\n
	margin: 0;\n
	color: var(--dc-muted);\n
	font-size: var(--dc-fs-sm);\n
}\n
.dc-setup-checklist__item.is-current .dc-setup-checklist__hint {\n
	/* Hint sits on card white/dark bg — muted is fine; keep readable size. */\n
	font-size: var(--dc-fs-md);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-setup-checklist__body .button {\n
	align-self: flex-start;\n
	min-height: var(--dc-touch);\n
	min-width: var(--dc-touch);\n
}\n
.dc-setup-checklist__body .button.primary {\n
	font-size: var(--dc-fs-md);\n
	font-weight: 700;\n
	padding-inline: var(--dc-space-5);\n
}\n
@media (max-width: 480px) {\n
	.dc-setup-checklist__body .button.primary {\n
		width: 100%;\n
		justify-content: center;\n
	}\n
}\n
@media (forced-colors: active) {\n
	.dc-setup-checklist__item.is-done .dc-setup-checklist__status,\n
	.dc-setup-checklist__item.is-current .dc-setup-checklist__status {\n
		border: 2px solid CanvasText;\n
		forced-color-adjust: none;\n
	}\n
}\n
\n
/* ── Quickstart card variant (dismissable) ───────────────────── */\n
.dc-empty--quickstart {\n
	position: relative;\n
	padding-inline-start: calc(var(--dc-space-4) + 4px);\n
}\n
.dc-empty--quickstart::before {\n
	content: "";\n
	position: absolute;\n
	inset-block: var(--dc-space-3);\n
	inset-inline-start: 0;\n
	width: 4px;\n
	border-radius: 4px;\n
	background: var(--color-primary-element, #1976d2);\n
	opacity: 0.85;\n
}\n
.dc-empty--quickstart .dc-section__header h2 {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-2);\n
}\n
.dc-empty--quickstart .dc-section__header h2::before {\n
	content: "★";\n
	color: var(--color-primary-element, #1976d2);\n
	font-size: 0.85em;\n
	line-height: 1;\n
}\n
\n
/* ── Hint dismiss button ─────────────────────────────────────── */\n
.dc-hint-dismiss {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	background: transparent;\n
	border: 1px solid var(--dc-border);\n
	color: var(--color-main-text, #1d1d1d);\n
	border-radius: var(--dc-radius-sm);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	cursor: pointer;\n
	min-height: 44px;\n
	min-width: 44px;\n
	box-sizing: border-box;\n
	line-height: 1.2;\n
	font: inherit;\n
	text-align: center;\n
}\n
.dc-hint-dismiss:hover { background: var(--dc-bg-soft); }\n
.dc-hint-dismiss:focus-visible {\n
	outline: 2px solid var(--color-primary-element, #1976d2);\n
	outline-offset: 2px;\n
}\n
\n
/* Settings — ArbeitszeitCheck integration (clear hierarchy, touch targets, wrap) */\n
.dc-subsection-heading {\n
	margin: 0 0 var(--dc-space-2);\n
	font-size: var(--dc-fs-sm);\n
	font-weight: 700;\n
	letter-spacing: 0.05em;\n
	text-transform: uppercase;\n
	color: var(--dc-muted);\n
	line-height: var(--dc-line-tight);\n
}\n
.dc-at-integration__body {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-4);\n
}\n
.dc-at-integration__status {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	min-width: 0;\n
}\n
.dc-at-integration__banner-row {\n
	display: flex;\n
	flex-wrap: wrap;\n
	align-items: flex-start;\n
	gap: var(--dc-space-2);\n
}\n
.dc-at-integration__banner {\n
	flex: 1 1 220px;\n
	min-width: 0;\n
	margin: 0;\n
}\n
.dc-at-integration__retry {\n
	flex: 0 0 auto;\n
	align-self: flex-start;\n
	min-height: 44px;\n
}\n
.dc-at-integration__retry:focus-visible {\n
	outline: 2px solid var(--color-primary-element, #0082c9);\n
	outline-offset: 2px;\n
}\n
.dc-at-integration__meta {\n
	margin: 0;\n
	max-width: 65ch;\n
}\n
.dc-at-banner .dc-callout__actions {\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
}\n
.dc-at-banner .dc-callout__actions .button {\n
	min-height: 44px;\n
	min-width: 44px;\n
}\n
.dc-at-integration__controls .dc-field {\n
	max-width: 48rem;\n
}\n
@media (max-width: 720px) {\n
	.dc-at-integration__banner-row {\n
		flex-direction: column;\n
	}\n
	.dc-at-integration__actions {\n
		flex-direction: column;\n
		align-items: stretch;\n
	}\n
	.dc-at-integration__actions .button,\n
	.dc-at-integration__actions a.button {\n
		width: 100%;\n
		justify-content: center;\n
	}\n
}\n
@media (prefers-reduced-motion: reduce) {\n
	.dc-at-integration__actions .button[disabled] {\n
		transition: none;\n
	}\n
}\n
@media (forced-colors: active) {\n
	.dc-at-banner,\n
	.dc-at-integration__banner {\n
		border: 2px solid CanvasText;\n
	}\n
	.dc-at-banner :focus-visible,\n
	.dc-at-integration__actions .button:focus-visible,\n
	.dc-at-integration__retry:focus-visible {\n
		outline: 2px solid CanvasText;\n
		outline-offset: 2px;\n
	}\n
}\n
@media print {\n
	/* REQ-UX-13: hide dismissible banner region; keep table source column. */\n
	.dc-at-banner,\n
	.dc-at-integration__banner {\n
		display: none !important;\n
	}\n
	.dc-at-integration {\n
		break-inside: avoid;\n
	}\n
}\n
.dc-at-integration__divider {\n
	margin: var(--dc-space-1) 0;\n
}\n
.dc-at-integration__controls {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	min-width: 0;\n
}\n
.dc-at-integration__actions {\n
	flex-wrap: wrap;\n
	justify-content: flex-start;\n
	align-self: stretch;\n
	width: 100%;\n
	margin-top: var(--dc-space-2);\n
	padding-top: var(--dc-space-3);\n
	border-top: 1px solid var(--dc-border);\n
}\n
/* .dc-field + .dc-form-actions on one node breaks layout (column + centered children) */\n
.dc-field.dc-form-actions {\n
	flex-direction: row;\n
	flex-wrap: wrap;\n
	justify-content: flex-start;\n
	align-items: center;\n
	align-self: stretch;\n
	width: 100%;\n
}\n
.dc-at-integration__actions .button,\n
.dc-at-integration__actions a.button {\n
	min-height: 44px;\n
	box-sizing: border-box;\n
}\n
\n
/* ── Forms ───────────────────────────────────────────────────── */\n
.dc-form-grid,\n
.dc-filter-grid {\n
	display: grid;\n
	grid-template-columns: minmax(0, 1fr);\n
	gap: var(--dc-space-3);\n
	align-items: start;\n
	min-width: 0;\n
	max-width: 100%;\n
	width: 100%;\n
	box-sizing: border-box;\n
}\n
.dc-form-grid--nested {\n
	margin-block-start: var(--dc-space-4);\n
}\n
.dc-form-grid > .dc-field--full,\n
.dc-filter-grid > .dc-field--full {\n
	grid-column: 1 / -1;\n
}\n
.dc-form-grid > hr.dc-form-grid__divider {\n
	grid-column: 1 / -1;\n
	width: 100%;\n
	height: 0;\n
	margin: var(--dc-space-1) 0;\n
	border: 0;\n
	border-top: 1px solid var(--dc-border);\n
}\n
\n
.dc-field {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-1);\n
	min-width: 0;\n
}\n
.dc-field__label {\n
	font-weight: 600;\n
	font-size: var(--dc-fs-sm);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-field__hint {\n
	margin: var(--dc-space-1) 0 0;\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-roster-break-prefill {\n
	margin: var(--dc-space-1) 0 0;\n
	font-size: var(--dc-fs-sm);\n
	font-weight: 600;\n
	color: var(--color-main-text, #1d1d1d);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-roster-break-prefill:empty {\n
	display: none;\n
}\n
.dc-field__error {\n
	margin: var(--dc-space-1) 0 0;\n
	font-size: var(--dc-fs-xs);\n
	color: var(--color-error-text, var(--color-error, #cc4646));\n
	font-weight: 600;\n
}\n
\n
/*\n
 * On multi-column rows, stacked fields (label + control [+ hint]) must share\n
 * the same vertical rhythm. Without a hint row, `align-items: start` lets\n
 * sibling fields with hints push their label lower. Reserve hint space with\n
 * ::after so all inputs share one baseline.\n
 */\n
.dc-form-grid > .dc-field:not(.dc-field--inline):not(.dc-field--checkbox):not(:has(.dc-field__hint))::after,\n
.dc-filter-grid > .dc-field:not(.dc-field--inline):not(.dc-field--checkbox):not(:has(.dc-field__hint))::after {\n
	content: '';\n
	display: block;\n
	margin-top: var(--dc-space-1);\n
	min-height: calc(2 * var(--dc-line-base) * var(--dc-fs-xs));\n
	pointer-events: none;\n
}\n
\n
.dc-input,\n
#app-content.dc-app input[type='text'],\n
#app-content.dc-app input[type='number'],\n
#app-content.dc-app input[type='date'],\n
#app-content.dc-app input[type='time'],\n
#app-content.dc-app input[type='search'],\n
#app-content.dc-app input[type='email'],\n
#app-content.dc-app input[type='url'],\n
#app-content.dc-app select,\n
#app-content.dc-app textarea {\n
	width: 100%;\n
	max-width: 100%;\n
	min-width: 0;\n
	min-height: 44px;\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	border: 1px solid var(--dc-border-strong);\n
	background: var(--dc-bg-card);\n
	color: var(--color-main-text, #1d1d1d);\n
	font: inherit;\n
	box-sizing: border-box;\n
}\n
#app-content.dc-app textarea {\n
	min-height: 6rem;\n
	resize: vertical;\n
}\n
.dc-input:focus-visible,\n
#app-content.dc-app input:not([type='checkbox']):not([type='radio']):focus-visible,\n
#app-content.dc-app select:focus-visible,\n
#app-content.dc-app textarea:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 1px;\n
	border-color: var(--color-primary-element, #0082c9);\n
}\n
\n
input[type=number], input[type=month], input[type=date], input[type=time],\n
.dc-input--num, .dc-input--date {\n
	font-variant-numeric: var(--dc-tab-num);\n
}\n
\n
/*\n
 * Checkbox + copy in an input-sized box (same border/padding rhythm as .dc-input).\n
 *\n
 * Nextcloud core applies `min-height: var(--default-clickable-area)` to ALL\n
 * `input` elements. Without an explicit min-width/min-height reset the native\n
 * control stretches to ~44px and washes out or clips under dark / high-contrast\n
 * themes. Match AZC settings + DeskCheck form-controls: fixed 1.25rem box,\n
 * theme tokens for fill/border, accent-color for the checkmark.\n
 */\n
.dc-checkbox,\n
label.dc-checkbox {\n
	display: inline-flex !important;\n
	align-items: center;\n
	gap: var(--dc-space-3);\n
	width: 100%;\n
	max-width: 100%;\n
	min-width: 0;\n
	box-sizing: border-box;\n
	min-height: var(--dc-touch, 44px);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border: 1px solid var(--dc-border-strong);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-card);\n
	color: var(--dc-text, var(--color-main-text, #1d1d1d));\n
	cursor: pointer;\n
	margin: 0;\n
	-webkit-tap-highlight-color: transparent;\n
}\n
.dc-checkbox input[type='checkbox'],\n
.dc-check input[type='checkbox'],\n
.dc-field--radio input[type='radio'] {\n
	flex-shrink: 0;\n
	width: 1.25rem;\n
	height: 1.25rem;\n
	/* Beat NC core `input { min-height: var(--default-clickable-area) }` */\n
	min-width: 1.25rem;\n
	min-height: 1.25rem;\n
	max-width: 1.25rem;\n
	max-height: 1.25rem;\n
	margin: 0;\n
	padding: 0;\n
	cursor: pointer;\n
	box-sizing: border-box;\n
	accent-color: var(--color-primary-element, #0082c9);\n
	/* Theme-aware native chrome (light / dark / high-contrast / custom primary) */\n
	background-color: var(--dc-bg-card, var(--color-main-background, #fff));\n
	border: 1px solid var(--dc-border-strong, var(--color-border-dark, #999));\n
	border-radius: 3px;\n
	color: var(--dc-text, var(--color-main-text, #1d1d1d));\n
	/* Follow body[data-theme-*] so the UA paints the glyph for the active scheme */\n
	color-scheme: inherit;\n
	appearance: auto;\n
	-webkit-appearance: auto;\n
}\n
.dc-field--radio input[type='radio'] {\n
	border-radius: 50%;\n
	margin: 0.2em 0 0;\n
}\n
.dc-checkbox input[type='checkbox']:focus-visible,\n
.dc-check input[type='checkbox']:focus-visible,\n
.dc-field--radio input[type='radio']:focus-visible {\n
	outline: 3px solid var(--color-primary-element, #0082c9);\n
	outline-offset: 2px;\n
	border-color: var(--color-primary-element, #0082c9);\n
}\n
.dc-checkbox:has(input:disabled) {\n
	cursor: not-allowed;\n
	opacity: 0.72;\n
}\n
.dc-checkbox:has(input:disabled) input {\n
	cursor: not-allowed;\n
}\n
.dc-checkbox:focus-within {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 1px;\n
}\n
.dc-checkbox > span,\n
.dc-checkbox__text {\n
	flex: 1;\n
	min-width: 0;\n
	line-height: var(--dc-line-base);\n
	font-size: var(--dc-fs-sm);\n
	color: inherit;\n
}\n
\n
/* Radio inside .dc-field--radio */\n
.dc-field--radio {\n
	flex-direction: row;\n
	align-items: flex-start;\n
	gap: var(--dc-space-3);\n
	padding: var(--dc-space-1) 0;\n
	color: var(--dc-text, var(--color-main-text, #1d1d1d));\n
}\n
\n
.dc-form-actions,\n
.dc-filter-actions,\n
.dc-action-row {\n
	display: flex;\n
	gap: var(--dc-space-2);\n
	flex-wrap: wrap;\n
	align-items: center;\n
	justify-content: flex-start;\n
	grid-column: 1 / -1;\n
}\n
\n
/* Employee form: explicit two-column rhythm + section separators */\n
.dc-form-grid--employees {\n
	grid-template-columns: 1fr;\n
	column-gap: var(--dc-space-4);\n
	row-gap: var(--dc-space-3);\n
}\n
.dc-form-grid--employees > .dc-field--employee-link {\n
	grid-column: 1 / -1;\n
	padding-bottom: var(--dc-space-3);\n
	border-bottom: 1px solid var(--dc-border);\n
}\n
.dc-form-grid--employees > .dc-field--employee-name {\n
	grid-column: 1 / -1;\n
}\n
.dc-form-grid--employees > .dc-field--employee-active {\n
	grid-column: 1 / -1;\n
}\n
.dc-form-grid--employees > .dc-form-actions {\n
	justify-content: flex-end;\n
	padding-top: var(--dc-space-3);\n
	border-top: 1px solid var(--dc-border);\n
}\n
\n
/*\n
 * Location form: stacked segments with full-width rules so each block reads as a\n
 * clear row (identity + status, timezone, actions).\n
 */\n
.dc-form-stack {\n
	display: flex;\n
	flex-direction: column;\n
	gap: 0;\n
	min-width: 0;\n
}\n
.dc-form-stack--locations > .dc-form-stack__segment {\n
	padding-block: var(--dc-space-4);\n
	border-bottom: 1px solid var(--dc-border);\n
}\n
.dc-form-stack--locations > .dc-form-stack__segment:first-child {\n
	padding-top: 0;\n
}\n
.dc-form-stack--locations > .dc-form-stack__segment:last-child {\n
	border-bottom: 0;\n
	padding-bottom: 0;\n
}\n
.dc-form-stack__grid--2 {\n
	display: grid;\n
	grid-template-columns: 1fr;\n
	column-gap: var(--dc-space-4);\n
	row-gap: var(--dc-space-3);\n
	align-items: start;\n
}\n
.dc-form-stack__grid--2 > .dc-field--location-active {\n
	align-self: stretch;\n
}\n
.dc-form-stack__segment--actions .dc-form-actions {\n
	margin: 0;\n
	justify-content: flex-end;\n
	width: 100%;\n
}\n
/* Searchable IANA timezone picker (locations form) */\n
.dc-timezone-picker {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	min-width: 0;\n
	max-width: 100%;\n
}\n
.dc-timezone-picker__control {\n
	display: flex;\n
	align-items: stretch;\n
	gap: var(--dc-space-2);\n
	min-width: 0;\n
}\n
.dc-timezone-picker__input {\n
	flex: 1 1 auto;\n
	min-width: 0;\n
	margin: 0;\n
}\n
.dc-timezone-picker__native {\n
	position: absolute;\n
	width: 1px;\n
	height: 1px;\n
	padding: 0;\n
	margin: -1px;\n
	overflow: hidden;\n
	clip: rect(0, 0, 0, 0);\n
	white-space: nowrap;\n
	border: 0;\n
}\n
.dc-timezone-picker__clear {\n
	flex: 0 0 auto;\n
	align-self: stretch;\n
	min-width: 44px;\n
	min-height: 44px;\n
	padding-inline: var(--dc-space-2);\n
	font-size: 1.25rem;\n
	line-height: 1;\n
	box-sizing: border-box;\n
}\n
.dc-timezone-picker__results {\n
	position: relative;\n
	z-index: 20;\n
	margin: 0;\n
	padding: var(--dc-space-1) 0;\n
	list-style: none;\n
	max-height: min(50vh, 16rem);\n
	overflow-y: auto;\n
	border: 1px solid var(--color-border, #ededed);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--color-main-background, #fff);\n
	box-shadow: var(--dc-shadow-sm);\n
}\n
.dc-timezone-picker__results[hidden] {\n
	display: none;\n
}\n
.dc-timezone-picker__group {\n
	padding: var(--dc-space-2) var(--dc-space-3) var(--dc-space-1);\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 700;\n
	letter-spacing: 0.02em;\n
	text-transform: uppercase;\n
	color: var(--dc-muted);\n
}\n
.dc-timezone-picker__option {\n
	display: flex;\n
	align-items: baseline;\n
	justify-content: space-between;\n
	gap: var(--dc-space-3);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	cursor: pointer;\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-timezone-picker__option:hover,\n
.dc-timezone-picker__option[aria-selected="true"] {\n
	background: var(--color-background-hover, rgba(0, 0, 0, 0.05));\n
}\n
.dc-timezone-picker__option:focus-visible {\n
	outline: 2px solid var(--color-primary-element, #0082c9);\n
	outline-offset: -2px;\n
}\n
.dc-timezone-picker__option-id {\n
	font-weight: 600;\n
	word-break: break-word;\n
}\n
.dc-timezone-picker__option-offset {\n
	flex-shrink: 0;\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
}\n
.dc-timezone-picker__empty {\n
	padding: var(--dc-space-3);\n
	font-size: var(--dc-fs-sm);\n
	color: var(--dc-muted);\n
}\n
.dc-timezone-picker__status {\n
	margin: 0;\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-timezone-picker__status--error {\n
	color: var(--color-error, #e9322d);\n
	font-weight: 600;\n
}\n
.dc-timezone-picker__status[hidden] {\n
	display: none;\n
}\n
\n
/* Roster planning */\n
.dc-roster-flash {\n
	margin: calc(-1 * var(--dc-space-2)) 0 var(--dc-space-3);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-tint-success);\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-roster-flash[hidden] {\n
	display: none !important;\n
}\n
.dc-roster-empty-panel {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: center;\n
	justify-content: center;\n
	gap: var(--dc-space-2);\n
	box-sizing: border-box;\n
	width: 100%;\n
	padding: var(--dc-space-7) var(--dc-space-4);\n
	text-align: center;\n
	border-top: 1px solid var(--dc-border);\n
	background: var(--dc-bg-card);\n
}\n
.dc-roster-empty-panel[hidden] {\n
	display: none !important;\n
}\n
.dc-roster-empty-state__text {\n
	margin: 0;\n
	max-width: 32rem;\n
	font-size: var(--dc-fs-md);\n
	line-height: var(--dc-line-base);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-roster-empty-state__hint {\n
	margin: 0;\n
	max-width: 32rem;\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
	color: var(--dc-muted);\n
}\n
.button.dc-is-disabled,\n
.button.primary[aria-disabled='true']:not(:disabled) {\n
	opacity: 0.55;\n
	cursor: not-allowed;\n
	filter: grayscale(0.15);\n
}\n
.dc-assignment-form-host {\n
	display: none !important;\n
}\n
.dc-modal__dialog--roster-assignment {\n
	max-width: min(720px, calc(100vw - 2 * var(--dc-space-4)));\n
}\n
.dc-modal__dialog--roster-assignment .dc-modal__body {\n
	padding-top: 0;\n
}\n
.dc-modal__dialog--roster-assignment .dc-form-actions--roster {\n
	justify-content: flex-start;\n
	margin-top: var(--dc-space-2);\n
	padding-top: var(--dc-space-2);\n
	border-top: 1px solid var(--dc-border);\n
}\n
.dc-modal__dialog--roster-assignment .dc-form-actions--roster .button[type="submit"] {\n
	display: none;\n
}\n
.dc-roster-panel,\n
.dc-settings-panel {\n
	scroll-margin-top: var(--dc-space-3);\n
	border-inline-start: 4px solid var(--color-primary-element, #0082c9);\n
	padding-inline-start: var(--dc-space-4);\n
}\n
.dc-settings-planning-note {\n
	margin-top: var(--dc-space-2);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-soft);\n
	border: 1px solid var(--dc-border);\n
}\n
.dc-roster-panel__body {\n
	max-width: 100%;\n
}\n
.dc-app--my-roster .dc-main {\n
	gap: var(--dc-space-5);\n
}\n
.dc-app--my-roster .dc-section.dc-roster-panel {\n
	box-shadow: var(--dc-shadow-sm);\n
}\n
.dc-app--my-roster .dc-section:not(.dc-roster-panel) {\n
	box-shadow: var(--dc-shadow-sm);\n
}\n
.dc-roster-period__lifecycle {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: flex-start;\n
	gap: var(--dc-space-3);\n
	margin-top: var(--dc-space-4);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border-radius: var(--dc-radius-sm);\n
	border: 1px solid var(--dc-border);\n
	background: var(--dc-bg-soft);\n
}\n
.dc-roster-period__lifecycle .dc-field__hint {\n
	margin: 0;\n
}\n
.dc-roster-form__step-group {\n
	grid-column: 1 / -1;\n
	margin: 0;\n
	padding: var(--dc-space-4);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	background: var(--color-main-background, #fff);\n
}\n
.dc-roster-form__step-group + .dc-roster-form__step-group {\n
	margin-top: var(--dc-space-2);\n
}\n
.dc-roster-form__step-group .dc-roster-form__section-heading {\n
	margin: 0 0 var(--dc-space-3);\n
}\n
.dc-roster-form__step-group .dc-field--roster-time-hint,\n
.dc-roster-form__step-group .dc-field--roster-rest-hint {\n
	margin-bottom: var(--dc-space-2);\n
}\n
.dc-form-grid--roster-assignment fieldset {\n
	border: none;\n
	min-inline-size: 0;\n
}\n
.dc-form-grid--roster-assignment {\n
	grid-template-columns: 1fr;\n
	column-gap: var(--dc-space-4);\n
	row-gap: var(--dc-space-3);\n
	align-items: start;\n
}\n
.dc-roster-form__intro {\n
	grid-column: 1 / -1;\n
	margin: 0 0 var(--dc-space-2);\n
	padding: var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-soft);\n
	border: 1px solid var(--dc-border);\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-normal);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-roster-form__section-heading--step {\n
	display: flex;\n
	align-items: center;\n
	gap: var(--dc-space-2);\n
}\n
.dc-roster-form__step-badge {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	min-width: 1.75rem;\n
	height: 1.75rem;\n
	padding: 0 var(--dc-space-1);\n
	border-radius: 999px;\n
	background: var(--color-primary-element, #00679e);\n
	color: var(--color-primary-element-text, #fff);\n
	font-size: var(--dc-fs-sm);\n
	font-weight: 700;\n
	flex-shrink: 0;\n
}\n
.dc-roster-form__feedback {\n
	grid-column: 1 / -1;\n
	margin: 0;\n
	padding: var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	border: 1px solid var(--dc-border);\n
	background: var(--color-warning, #fcf4d4);\n
	color: var(--color-main-text, #1d1d1d);\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-normal);\n
}\n
.dc-roster-form__feedback[hidden] {\n
	display: none !important;\n
}\n
.dc-roster-form__feedback--error {\n
	border-color: var(--color-error, #c00);\n
	background: var(--color-error, #c00);\n
	background: color-mix(in srgb, var(--color-error, #c00) 12%, var(--color-main-background, #fff));\n
}\n
.dc-roster-form__feedback--success {\n
	border-color: var(--color-element-success, var(--color-success-text, var(--color-success, #46ba61)));\n
	border-inline-start-width: 4px;\n
	background: color-mix(in srgb, var(--color-success-text, var(--color-success, #46ba61)) 14%, var(--color-main-background, #fff));\n
	font-weight: 600;\n
}\n
.dc-roster-form__feedback--info {\n
	border-color: var(--color-primary-element, #0082c9);\n
	border-inline-start-width: 4px;\n
	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 10%, var(--color-main-background, #fff));\n
}\n
.dc-form-grid--roster-assignment > .dc-roster-form__intro,\n
.dc-form-grid--roster-assignment > .dc-roster-form__feedback,\n
.dc-form-grid--roster-assignment > .dc-roster-form__step-group,\n
.dc-form-grid--roster-assignment > .dc-roster-form__extras {\n
	grid-column: 1 / -1;\n
}\n
.dc-roster-form__extras {\n
	display: grid;\n
	grid-template-columns: 1fr;\n
	row-gap: var(--dc-space-3);\n
	padding: var(--dc-space-4);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	background: var(--color-main-background, #fff);\n
}\n
.dc-roster-form__extras > .dc-roster-form__section-heading {\n
	margin: 0 0 var(--dc-space-1);\n
	font-size: var(--dc-fs-md);\n
	font-weight: 700;\n
}\n
.dc-roster-form__step-group > .dc-roster-form__section-heading {\n
	grid-column: 1 / -1;\n
	margin: var(--dc-space-4) 0 var(--dc-space-1);\n
	padding: 0;\n
	border: 0;\n
	font-size: var(--dc-fs-md);\n
	font-weight: 700;\n
	line-height: var(--dc-line-tight);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-form-grid--roster-assignment > .dc-field--roster-time-hint {\n
	grid-column: 1 / -1;\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	margin: 0;\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-soft);\n
	border: 1px solid var(--dc-border);\n
}\n
.dc-form-grid--roster-assignment .dc-roster-24h-hint {\n
	margin: 0;\n
}\n
.dc-form-grid--roster-assignment > .dc-field--roster-rest-hint {\n
	grid-column: 1 / -1;\n
	margin: 0;\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-soft);\n
	border: 1px solid var(--dc-border);\n
}\n
.dc-form-grid--roster-assignment > .dc-form-actions--roster {\n
	grid-column: 1 / -1;\n
	margin-top: var(--dc-space-2);\n
	padding-top: var(--dc-space-4);\n
	border-top: 1px solid var(--dc-border);\n
	justify-content: flex-end;\n
}\n
.dc-input--time-24h {\n
	font-variant-numeric: tabular-nums;\n
}\n
\n
/* Entity picker (search + suggestion list) */\n
.dc-entity-picker {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	max-width: 100%;\n
}\n
.dc-entity-picker__field {\n
	position: relative;\n
}\n
.dc-entity-picker__field .dc-icon--lead {\n
	position: absolute;\n
	inset-inline-start: var(--dc-space-3);\n
	top: 50%;\n
	transform: translateY(-50%);\n
	color: var(--dc-muted);\n
	pointer-events: none;\n
}\n
.dc-entity-picker__field input {\n
	padding-inline-start: calc(var(--dc-space-4) + 18px + var(--dc-space-2));\n
}\n
.dc-entity-results {\n
	list-style: none;\n
	margin: 0;\n
	padding: var(--dc-space-1) 0;\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-card);\n
	max-height: 14rem;\n
	overflow: auto;\n
	box-shadow: var(--dc-shadow-sm);\n
}\n
.dc-entity-results:empty {\n
	display: none;\n
}\n
.dc-entity-results:not(:empty) {\n
	margin-top: var(--dc-space-2);\n
}\n
.dc-entity-results li {\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	cursor: pointer;\n
	min-height: 44px;\n
	display: flex;\n
	align-items: center;\n
	gap: var(--dc-space-2);\n
}\n
.dc-entity-results li:hover,\n
.dc-entity-results li[aria-selected='true'] {\n
	background: var(--dc-tint-info);\n
}\n
.dc-entity-results li:focus-visible {\n
	background: var(--dc-tint-info);\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: -3px;\n
}\n
.dc-entity-results__title {\n
	font-weight: 600;\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-entity-results__sub {\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
	margin-top: 2px;\n
}\n
.dc-entity-results__empty {\n
	padding: var(--dc-space-3) var(--dc-space-3);\n
	color: var(--dc-muted);\n
	font-size: var(--dc-fs-sm);\n
}\n
\n
/* ── Buttons ─────────────────────────────────────────────────── */\n
.dc-app .button,\n
.dc-app a.button,\n
.dc-app button.button,\n
.dc-app input[type='submit'].button,\n
.dc-app input[type='button'].button,\n
.dc-app input[type='reset'].button,\n
#app-content.dc-app button,\n
#app-content.dc-app .button,\n
#app-content.dc-app a.button {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	gap: var(--dc-space-2);\n
	min-height: 44px;\n
	max-width: 100%;\n
	padding: var(--dc-space-2) var(--dc-space-4);\n
	box-sizing: border-box;\n
	line-height: 1.25;\n
	text-align: center;\n
	white-space: nowrap;\n
	font: inherit;\n
	border-radius: var(--dc-radius-sm);\n
	cursor: pointer;\n
}\n
\n
#app-content.dc-app button.primary,\n
.dc-app .button.primary,\n
.dc-app a.button.primary {\n
	min-height: 44px;\n
}\n
.dc-app .button.button--text,\n
#app-content.dc-app button.button--text {\n
	/* WCAG 2.5.8: text-style actions still need a 44×44 hit target. */\n
	min-height: 44px;\n
	min-width: 44px;\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border: 0;\n
	background: transparent;\n
	color: var(--color-primary-element, var(--color-main-text));\n
	text-decoration: underline;\n
	text-underline-offset: 2px;\n
	font-weight: 500;\n
	white-space: normal;\n
}\n
.dc-app .button.button--text:focus-visible,\n
#app-content.dc-app button.button--text:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 2px;\n
}\n
\n
#app-content.dc-app button.dc-btn-icon,\n
.dc-app .dc-btn-icon {\n
	min-width: 44px;\n
	width: 44px;\n
	padding: var(--dc-space-2);\n
}\n
\n
#app-content.dc-app button.danger,\n
.dc-app .button.danger {\n
	color: var(--color-primary-element-text, #fff);\n
	background: var(--color-error, #cc4646);\n
	border-color: var(--color-error, #cc4646);\n
}\n
#app-content.dc-app button.danger:hover,\n
.dc-app .button.danger:hover {\n
	filter: brightness(0.9);\n
}\n
\n
/* ── Tables ──────────────────────────────────────────────────── */\n
.dc-table-wrap {\n
	overflow-x: auto;\n
	-webkit-overflow-scrolling: touch;\n
	border-radius: var(--dc-radius-sm);\n
	border: 0;\n
	background: transparent;\n
}\n
/* Overflow regions must be a keyboard tab stop (WCAG 2.1.1 / axe scrollable-region-focusable). */\n
.dc-table-wrap:focus-visible,\n
.dc-roster-grid-scroller:focus-visible,\n
.dc-roster-list-scroller:focus-visible,\n
.dc-windowed-table:focus-visible,\n
.dc-print-table-wrap:focus-visible {\n
	outline: 3px solid var(--color-primary-element, var(--color-primary, #0082c9));\n
	outline-offset: 2px;\n
}\n
.dc-table {\n
	width: 100%;\n
	border-collapse: collapse;\n
	font-variant-numeric: var(--dc-tab-num);\n
	background: var(--dc-bg-card);\n
}\n
.dc-table,\n
.dc-table tbody,\n
.dc-table tr,\n
.dc-table td,\n
.dc-table th {\n
	display: block;\n
	width: 100%;\n
}\n
.dc-table thead {\n
	position: absolute;\n
	width: 1px;\n
	height: 1px;\n
	margin: -1px;\n
	padding: 0;\n
	overflow: hidden;\n
	clip: rect(0 0 0 0);\n
	white-space: nowrap;\n
	border: 0;\n
}\n
.dc-table tbody tr {\n
	display: grid;\n
	gap: var(--dc-space-1);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	margin-bottom: var(--dc-space-3);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	background: var(--dc-bg-card);\n
}\n
.dc-table tbody tr:nth-child(even) td,\n
.dc-table tbody tr:nth-child(even) th {\n
	background: transparent;\n
}\n
.dc-table td,\n
.dc-table tbody th.dc-table__rowhead {\n
	border: 0;\n
	padding: 0;\n
	background: transparent;\n
}\n
.dc-table td[data-cell]::before,\n
.dc-table tbody th.dc-table__rowhead[data-cell]::before {\n
	content: attr(data-cell);\n
	display: block;\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 700;\n
	letter-spacing: 0.04em;\n
	color: var(--dc-muted);\n
	text-transform: uppercase;\n
	margin-bottom: 2px;\n
}\n
.dc-table tbody tr:has(td[colspan]):not(.dc-virtual-spacer),\n
.dc-table tbody tr.dc-table__empty-row {\n
	display: block;\n
	text-align: center;\n
}\n
.dc-table tbody tr:has(td[colspan]):not(.dc-virtual-spacer) td::before,\n
.dc-table tbody tr.dc-table__empty-row td::before {\n
	content: none;\n
	display: none;\n
}\n
.dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell,\n
.dc-table tbody tr:has(td[colspan]):not(.dc-virtual-spacer) td[colspan] {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: center;\n
	justify-content: center;\n
	width: 100%;\n
	padding: var(--dc-space-6) var(--dc-space-4);\n
	text-align: center;\n
	border-bottom: 0;\n
}\n
.dc-table .dc-row-actions {\n
	padding-top: var(--dc-space-2);\n
}\n
.dc-table caption {\n
	text-align: left;\n
}\n
.dc-table th, .dc-table td {\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	text-align: left;\n
	vertical-align: middle;\n
	border-bottom: 1px solid var(--dc-border);\n
}\n
.dc-table thead th {\n
	background: var(--dc-bg-stripe);\n
	font-size: var(--dc-fs-sm);\n
	font-weight: 600;\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-table tbody tr:nth-child(even) td {\n
	background: color-mix(in srgb, var(--dc-bg-card) 96%, var(--color-main-text, #1d1d1d) 4%);\n
}\n
.dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell,\n
.dc-table tbody tr:has(td[colspan]) td[colspan] {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: center;\n
	justify-content: center;\n
	text-align: center;\n
	padding: var(--dc-space-6) var(--dc-space-4);\n
	border-bottom: 0;\n
}\n
.dc-table tbody tr.is-selected td,\n
.dc-table tbody tr[aria-selected='true'] td {\n
	background: var(--dc-tint-info);\n
}\n
.dc-table tbody tr.dc-row--selectable {\n
	cursor: pointer;\n
}\n
.dc-table tbody tr.dc-row--selectable:hover td {\n
	background: color-mix(in srgb, var(--dc-tint-info) 70%, var(--dc-bg-card) 30%);\n
}\n
/* Keyboard selection happens via the real button inside the row. */\n
.dc-table .dc-period-select {\n
	font-variant-numeric: tabular-nums;\n
}\n
.dc-table .dc-period-select[aria-current='true'] {\n
	font-weight: 700;\n
}\n
.dc-table .dc-row-actions {\n
	display: flex;\n
	gap: var(--dc-space-2);\n
	flex-wrap: wrap;\n
}\n
.dc-table .dc-time-cell__value {\n
	white-space: nowrap;\n
	font-variant-numeric: tabular-nums;\n
}\n
.dc-table__col--actions { white-space: nowrap; }\n
\n
/* Notes / muted detail under a primary cell */\n
.dc-row-meta {\n
	display: block;\n
	margin-top: 2px;\n
	color: var(--dc-muted);\n
	font-size: var(--dc-fs-xs);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-row--flash {\n
	outline: 2px solid var(--color-primary-element);\n
	outline-offset: 2px;\n
	background-color: var(--color-background-hover);\n
}\n
\n
/* Duty times: fixed-width numerals for scanability (24-hour display) */\n
.dc-time-cell__value {\n
	font-variant-numeric: tabular-nums;\n
	font-weight: 600;\n
	letter-spacing: 0.02em;\n
}\n
\n
/* ── Summary tiles ───────────────────────────────────────────── */\n
.dc-summary-grid {\n
	display: grid;\n
	grid-template-columns: 1fr;\n
	gap: var(--dc-space-3);\n
}\n
.dc-summary-tile {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-1);\n
	padding: var(--dc-space-4);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	background: var(--dc-bg-card);\n
}\n
.dc-summary-tile--primary {\n
	background: var(--dc-tint-info);\n
	border-color: transparent;\n
}\n
.dc-summary-tile--success { background: var(--dc-tint-success); border-color: transparent; }\n
.dc-summary-tile--warning { background: var(--dc-tint-warning); border-color: transparent; }\n
.dc-summary-tile--critical { background: var(--dc-tint-critical); border-color: transparent; }\n
.dc-summary-tile__label {\n
	font-size: var(--dc-fs-xs);\n
	text-transform: uppercase;\n
	letter-spacing: 0.05em;\n
	color: var(--dc-muted);\n
	font-weight: 600;\n
}\n
.dc-summary-tile--primary .dc-summary-tile__label,\n
.dc-summary-tile--primary .dc-summary-tile__hint,\n
.dc-summary-tile--success .dc-summary-tile__label,\n
.dc-summary-tile--success .dc-summary-tile__hint,\n
.dc-summary-tile--warning .dc-summary-tile__label,\n
.dc-summary-tile--warning .dc-summary-tile__hint,\n
.dc-summary-tile--critical .dc-summary-tile__label,\n
.dc-summary-tile--critical .dc-summary-tile__hint {\n
	/* Secondary copy on tinted tiles must clear 4.5:1 — muted does not. */\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-summary-tile__value {\n
	font-size: var(--dc-fs-2xl);\n
	font-weight: 700;\n
	font-variant-numeric: var(--dc-tab-num);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-summary-tile__hint {\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
	line-height: var(--dc-line-base);\n
}\n
\n
/* ── Sidebar / navigation ────────────────────────────────────── */\n
#app-navigation.dc-nav {\n
	display: flex;\n
	flex-direction: column;\n
}\n
.dc-nav__brand {\n
	display: flex;\n
	align-items: center;\n
	gap: var(--dc-space-3);\n
	padding: var(--dc-space-4);\n
	border-bottom: 1px solid var(--dc-border);\n
}\n
.dc-nav__brand-icon {\n
	display: grid;\n
	place-items: center;\n
	width: 36px;\n
	height: 36px;\n
	border-radius: var(--dc-radius-md);\n
	background: var(--dc-tint-info);\n
	color: var(--color-primary-element, #0082c9);\n
	flex-shrink: 0;\n
}\n
.dc-nav__brand-icon svg { width: 22px; height: 22px; }\n
.dc-nav__brand-text { display: flex; flex-direction: column; min-width: 0; }\n
.dc-nav__title {\n
	margin: 0;\n
	font-size: var(--dc-fs-md);\n
	font-weight: 700;\n
	line-height: var(--dc-line-tight);\n
}\n
.dc-nav__subtitle {\n
	margin: 0;\n
	color: var(--dc-muted);\n
	font-size: var(--dc-fs-xs);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-nav__role {\n
	margin-top: var(--dc-space-1);\n
	align-self: flex-start;\n
}\n
\n
.dc-nav__group {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-1);\n
	padding: var(--dc-space-2) var(--dc-space-2) 0;\n
}\n
.dc-nav__group-title {\n
	margin: var(--dc-space-2) var(--dc-space-3) var(--dc-space-1);\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
	text-transform: uppercase;\n
	letter-spacing: 0.05em;\n
}\n
.dc-nav__list {\n
	list-style: none;\n
	padding: 0;\n
	margin: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: 2px;\n
}\n
.dc-nav__item { margin: 0; }\n
.dc-nav__link {\n
	display: grid;\n
	grid-template-columns: 24px minmax(0, 1fr);\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	color: var(--color-main-text, #1d1d1d);\n
	text-decoration: none;\n
	min-height: 44px;\n
}\n
.dc-nav__link:hover {\n
	background: var(--dc-bg-stripe);\n
}\n
.dc-nav__item.is-active .dc-nav__link {\n
	background: var(--dc-tint-info);\n
	font-weight: 600;\n
}\n
.dc-nav__item.is-active .dc-nav__icon {\n
	color: var(--color-primary-element, #0082c9);\n
}\n
/* Muted hints fail AA on the primary tint pill — use main text on active. */\n
.dc-nav__item.is-active .dc-nav__hint {\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-nav__icon {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	color: var(--dc-muted);\n
}\n
.dc-nav__label { display: grid; gap: 2px; min-width: 0; }\n
.dc-nav__name {\n
	display: block;\n
	font-size: var(--dc-fs-sm);\n
}\n
.dc-nav__hint {\n
	display: block;\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
	line-height: var(--dc-line-base);\n
	overflow: hidden;\n
	text-overflow: ellipsis;\n
	white-space: nowrap;\n
}\n
\n
/* ── Conflicts & warnings ────────────────────────────────────── */\n
.dc-conflicts {\n
	list-style: none;\n
	padding: 0;\n
	margin: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
}\n
.dc-conflict {\n
	display: grid;\n
	grid-template-columns: auto minmax(0, 1fr);\n
	gap: var(--dc-space-3);\n
	align-items: start;\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border-radius: var(--dc-radius-sm);\n
	border-inline-start: 4px solid var(--color-primary-element, #0082c9);\n
	background: var(--dc-tint-info);\n
}\n
.dc-conflict__actions {\n
	grid-column: 1 / -1;\n
}\n
.dc-conflict--hard {\n
	border-inline-start-color: var(--color-error, #cc4646);\n
	background: var(--dc-tint-critical);\n
}\n
.dc-conflict--soft {\n
	border-inline-start-color: var(--color-warning, #c98600);\n
	background: var(--dc-tint-warning);\n
}\n
.dc-conflict__body { min-width: 0; }\n
.dc-conflict__title { font-weight: 600; color: var(--color-main-text, #1d1d1d); }\n
.dc-conflict__message {\n
	font-size: var(--dc-fs-sm);\n
	color: var(--color-main-text, #1d1d1d);\n
	margin-top: var(--dc-space-1);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-conflict__actions {\n
	display: flex;\n
	gap: var(--dc-space-2);\n
	flex-wrap: wrap;\n
	align-items: center;\n
}\n
.dc-conflict__ack {\n
	display: block;\n
	margin-top: var(--dc-space-1);\n
	color: var(--color-main-text, #1d1d1d);\n
	font-size: var(--dc-fs-xs);\n
}\n
.dc-conflict__ack-invalid {\n
	display: block;\n
	margin-top: var(--dc-space-1);\n
	color: var(--color-warning-text, color-mix(in srgb, var(--color-warning, #c98600) 80%, var(--color-main-text, #1d1d1d) 20%));\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 600;\n
}\n
\n
/* ── Modal ───────────────────────────────────────────────────── */\n
.dc-modal {\n
	position: fixed;\n
	inset: 0;\n
	display: grid;\n
	place-items: center;\n
	background: var(--dc-scrim);\n
	z-index: 10000;\n
	padding: max(var(--dc-space-4), env(safe-area-inset-top, 0px)) max(var(--dc-space-4), env(safe-area-inset-right, 0px)) max(var(--dc-space-4), env(safe-area-inset-bottom, 0px)) max(var(--dc-space-4), env(safe-area-inset-left, 0px));\n
	box-sizing: border-box;\n
}\n
.dc-modal__dialog {\n
	background: var(--dc-bg-card);\n
	border-radius: var(--dc-radius-md);\n
	box-shadow: var(--dc-shadow-md);\n
	max-width: min(640px, calc(100vw - 2 * var(--dc-space-4)));\n
	width: 100%;\n
	max-height: min(90vh, calc(100dvh - 2 * var(--dc-space-4)));\n
	overflow: auto;\n
	padding: var(--dc-space-4);\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-modal__header {\n
	display: flex;\n
	justify-content: space-between;\n
	align-items: center;\n
	gap: var(--dc-space-3);\n
}\n
.dc-modal__title {\n
	margin: 0;\n
	font-size: var(--dc-fs-xl);\n
	line-height: var(--dc-line-tight);\n
}\n
.dc-modal__close {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	background: transparent;\n
	border: 0;\n
	color: inherit;\n
	cursor: pointer;\n
	padding: var(--dc-space-2);\n
	border-radius: var(--dc-radius-sm);\n
	min-width: 44px;\n
	min-height: 44px;\n
	box-sizing: border-box;\n
	line-height: 1;\n
	font: inherit;\n
}\n
.dc-modal__close:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
}\n
.dc-modal__body {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
}\n
.dc-modal__body p { margin: 0; line-height: var(--dc-line-base); }\n
.dc-modal__textarea {\n
	min-height: 7rem;\n
	resize: vertical;\n
}\n
.dc-modal__actions {\n
	display: flex;\n
	justify-content: flex-end;\n
	gap: var(--dc-space-2);\n
	flex-wrap: wrap;\n
	margin-top: var(--dc-space-2);\n
}\n
\n
/* ── Toasts ──────────────────────────────────────────────────── */\n
.dc-toasts {\n
	position: fixed;\n
	bottom: var(--dc-space-4);\n
	inset-inline: var(--dc-space-4);\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	z-index: 12000;\n
	max-width: none;\n
	pointer-events: none;\n
}\n
.dc-toasts .dc-toast {\n
	pointer-events: auto;\n
	max-width: min(400px, 100%);\n
	margin-inline-start: auto;\n
}\n
.dc-toast {\n
	background: var(--dc-bg-card);\n
	border: 1px solid var(--dc-border);\n
	border-inline-start: 4px solid var(--color-primary-element, #0082c9);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border-radius: var(--dc-radius-sm);\n
	box-shadow: var(--dc-shadow-sm);\n
	display: flex;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-toast--success { border-inline-start-color: var(--color-element-success, var(--color-success-text, var(--color-success, #46ba61))); }\n
.dc-toast--info { border-inline-start-color: var(--color-primary-element, #0082c9); }\n
.dc-toast--error { border-inline-start-color: var(--color-error, #cc4646); }\n
.dc-toast--warning { border-inline-start-color: var(--color-warning, #c98600); }\n
.dc-toast__close {\n
	margin-inline-start: auto;\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	background: transparent;\n
	border: 0;\n
	cursor: pointer;\n
	padding: var(--dc-space-2);\n
	min-height: 44px;\n
	min-width: 44px;\n
	box-sizing: border-box;\n
	line-height: 1;\n
	font: inherit;\n
	border-radius: var(--dc-radius-sm);\n
	color: inherit;\n
}\n
.dc-toast__close:focus-visible {\n
	outline: 2px solid var(--color-main-text, #1d1d1d);\n
	outline-offset: 2px;\n
}\n
\n
/* ── Calendar subscription (employee self-service) ───────────── */\n
.dc-ical-panel {\n
	border-inline-start: 4px solid color-mix(in srgb, var(--color-warning, #c98600) 55%, var(--color-main-background, #fff));\n
	padding-inline-start: var(--dc-space-4);\n
}\n
.dc-ical-panel__body {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-5);\n
	max-width: 100%;\n
}\n
.dc-ical-subtitle {\n
	margin: 0 0 var(--dc-space-3);\n
	font-size: var(--dc-fs-lg);\n
	font-weight: 700;\n
	line-height: var(--dc-line-tight);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-ical-steps__list {\n
	margin: 0;\n
	padding-inline-start: 1.35rem;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	line-height: var(--dc-line-base);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-ical-steps__list li::marker {\n
	font-weight: 700;\n
	color: var(--color-primary-element, #0082c9);\n
}\n
.dc-callout__hint--tight {\n
	margin: 0;\n
	line-height: var(--dc-line-base);\n
}\n
.dc-ical-link-card {\n
	padding: var(--dc-space-4) var(--dc-space-5);\n
	border-radius: var(--dc-radius-md);\n
	border: 1px solid var(--dc-border);\n
	background: var(--dc-bg-soft);\n
	box-shadow: var(--dc-shadow-sm);\n
}\n
.dc-ical__actions {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
	margin-bottom: var(--dc-space-3);\n
}\n
.dc-ical__actions .button {\n
	flex: 1 1 auto;\n
	min-width: min(100%, 200px);\n
	min-height: 44px;\n
}\n
.dc-field--ical-url {\n
	margin-bottom: 0;\n
}\n
.dc-ical__url {\n
	width: 100%;\n
	font-family: var(--font-monospace, ui-monospace, "SFMono-Regular", "Menlo", monospace);\n
	font-size: var(--dc-fs-sm);\n
}\n
\n
/* ── Snapshot integrity banner ───────────────────────────────── */\n
.dc-integrity-banner {\n
	display: flex;\n
	gap: var(--dc-space-2);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-tint-critical);\n
	color: var(--color-main-text, #1d1d1d);\n
	font-weight: 600;\n
}\n
.dc-integrity-banner[hidden] { display: none; }\n
\n
/* ── Hash cell with copy ─────────────────────────────────────── */\n
.dc-hash-cell {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-2);\n
	max-width: 100%;\n
	min-width: 0;\n
}\n
.dc-hash-cell__code {\n
	font-family: var(--font-monospace, ui-monospace, "SFMono-Regular", "Menlo", monospace);\n
	font-size: var(--dc-fs-xs);\n
	overflow-wrap: anywhere;\n
	min-width: 0;\n
}\n
.dc-hash-cell__copy {\n
	flex-shrink: 0;\n
}\n
\n
/* ── Callouts with bulleted list ─────────────────────────────── */\n
.dc-callout__list {\n
	margin: 0;\n
	padding: 0 0 0 1.25rem;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-1);\n
}\n
.dc-callout__list li {\n
	margin: 0;\n
	line-height: var(--dc-line-base);\n
}\n
.dc-callout__list li .button {\n
	margin-inline-start: var(--dc-space-2);\n
	vertical-align: middle;\n
}\n
\n
/* ── Loading row inside data tables ──────────────────────────── */\n
.dc-table tbody tr.dc-table__loading-row td {\n
	font-style: italic;\n
	color: var(--dc-muted);\n
	background: var(--dc-bg-card);\n
}\n
\n
.dc-table tbody tr.dc-table__fetch-error-row td.dc-table__fetch-error-cell {\n
	color: var(--color-error-text, var(--color-error, #c0392b));\n
	background: var(--dc-tint-critical);\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
}\n
\n
/* ── Common icon sizing ──────────────────────────────────────── */\n
#app-content.dc-app svg.dc-icon,\n
#app-navigation.dc-nav svg.dc-icon,\n
.dc-icon {\n
	width: 20px;\n
	height: 20px;\n
	min-width: 20px;\n
	min-height: 20px;\n
	max-width: 20px;\n
	max-height: 20px;\n
	vertical-align: middle;\n
}\n
.dc-icon--sm,\n
#app-content.dc-app svg.dc-icon.dc-icon--sm,\n
#app-navigation.dc-nav svg.dc-icon.dc-icon--sm {\n
	width: 16px;\n
	height: 16px;\n
	min-width: 16px;\n
	min-height: 16px;\n
	max-width: 16px;\n
	max-height: 16px;\n
}\n
.dc-icon--lg,\n
#app-content.dc-app svg.dc-icon.dc-icon--lg {\n
	width: 28px;\n
	height: 28px;\n
	min-width: 28px;\n
	min-height: 28px;\n
	max-width: 28px;\n
	max-height: 28px;\n
}\n
/* Page header icon scales up at 768px (see responsive block). */\n
\n
/* ── Access denied ───────────────────────────────────────────── */\n
.dc-app--access-denied {\n
	display: grid;\n
	place-items: center;\n
	min-height: 60vh;\n
	padding: var(--dc-space-5);\n
	background: var(--dc-bg-soft);\n
}\n
.dc-denied {\n
	max-width: 540px;\n
}\n
.dc-denied .dc-card {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: center;\n
	gap: var(--dc-space-3);\n
	text-align: center;\n
	padding: var(--dc-space-6);\n
}\n
.dc-denied .dc-page-header__icon {\n
	background: var(--dc-tint-critical);\n
	color: var(--color-error, #cc4646);\n
}\n
\n
/* ── Focus ring (keyboard) — solid primary for ≥3:1 against surfaces ── */\n
a:focus-visible,\n
button:focus-visible,\n
[role='button']:focus-visible,\n
.dc-app a:focus-visible,\n
.dc-app button:focus-visible,\n
.dc-modal a:focus-visible,\n
.dc-modal button:focus-visible {\n
	outline: 3px solid var(--color-primary-element, #0082c9);\n
	outline-offset: 2px;\n
}\n
\n
@media (prefers-contrast: more) {\n
	a:focus-visible,\n
	button:focus-visible,\n
	[role='button']:focus-visible,\n
	.dc-app a:focus-visible,\n
	.dc-app button:focus-visible,\n
	.dc-modal a:focus-visible,\n
	.dc-modal button:focus-visible,\n
	.dc-input:focus-visible,\n
	#app-content.dc-app input:not([type='checkbox']):not([type='radio']):focus-visible,\n
	#app-content.dc-app select:focus-visible,\n
	#app-content.dc-app textarea:focus-visible,\n
	.dc-checkbox input[type='checkbox']:focus-visible,\n
	.dc-check input[type='checkbox']:focus-visible,\n
	.dc-field--radio input[type='radio']:focus-visible {\n
		outline-width: 3px;\n
		outline-color: var(--color-main-text, #1d1d1d);\n
	}\n
}\n
\n
@media (forced-colors: active) {\n
	.dc-card,\n
	.dc-callout,\n
	.dc-badge,\n
	.dc-status-badge,\n
	.dc-conflict,\n
	.dc-modal__dialog,\n
	.dc-toast,\n
	.dc-checkbox,\n
	label.dc-checkbox {\n
		border: 1px solid CanvasText;\n
	}\n
	.dc-checkbox input[type='checkbox'],\n
	.dc-check input[type='checkbox'],\n
	.dc-field--radio input[type='radio'] {\n
		/* Keep the 1.25rem box; let the UA map fill/border to system colours */\n
		border: 1px solid CanvasText;\n
		background-color: Canvas;\n
		accent-color: Highlight;\n
	}\n
	a:focus-visible,\n
	button:focus-visible,\n
	[role='button']:focus-visible,\n
	.dc-checkbox:focus-within,\n
	.dc-checkbox input[type='checkbox']:focus-visible,\n
	.dc-check input[type='checkbox']:focus-visible,\n
	.dc-field--radio input[type='radio']:focus-visible {\n
		outline: 3px solid CanvasText;\n
		outline-offset: 2px;\n
	}\n
}\n
\n
/* ── Reduced motion ──────────────────────────────────────────── */\n
@media (prefers-reduced-motion: reduce) {\n
	*, *::before, *::after {\n
		animation-duration: 0.001ms !important;\n
		animation-iteration-count: 1 !important;\n
		transition-duration: 0.001ms !important;\n
		scroll-behavior: auto !important;\n
	}\n
}\n
\n
/*\n
 * ── Responsive (mobile-first, Nextcloud breakpoints) ───────────\n
 * Base (<768px): stacked forms, card-style tables, full-width inputs.\n
 * 768px+: tablet/desktop chrome (legacy layout was default above 720px).\n
 * 1024px+: same as 768px+ — desktop is pixel-identical to pre-refactor\n
 *   styles; core #app-navigation uses its own toggle below 1024px.\n
 */\n
\n
/* 480px — two summary tiles per row on narrow phones in landscape */\n
@media (min-width: 480px) {\n
	.dc-summary-grid {\n
		grid-template-columns: 1fr 1fr;\n
	}\n
}\n
\n
/*\n
 * 768px — Nextcloud tablet: restore multi-column forms, data tables, and\n
 * page chrome (matches pre-mobile-first layout from 720px upward).\n
 */\n
@media (min-width: 768px) {\n
	#app-content.dc-app #app-content-wrapper.dc-shell,\n
	.dc-shell {\n
		padding: var(--dc-space-5) var(--dc-space-5) var(--dc-space-7);\n
	}\n
	.dc-card {\n
		padding: var(--dc-space-5);\n
	}\n
	.dc-page-header__main {\n
		grid-template-columns: 64px 1fr auto;\n
	}\n
	.dc-page-header__actions {\n
		grid-column: auto;\n
	}\n
	.dc-page-header__icon {\n
		width: 56px;\n
		height: 56px;\n
	}\n
	.dc-page-header__icon-svg {\n
		width: 32px;\n
		height: 32px;\n
		min-width: 32px;\n
		min-height: 32px;\n
	}\n
	.dc-form-grid,\n
	.dc-filter-grid {\n
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));\n
	}\n
	.dc-form-grid--employees {\n
		grid-template-columns: repeat(2, minmax(0, 1fr));\n
	}\n
	.dc-form-grid--employees > .dc-field--employee-name {\n
		grid-column: 1 / 2;\n
	}\n
	.dc-form-grid--employees > .dc-field--employee-active {\n
		grid-column: 2 / 3;\n
	}\n
	.dc-form-stack__grid--2 {\n
		grid-template-columns: repeat(2, minmax(0, 1fr));\n
	}\n
	.dc-form-stack__grid--2 > .dc-field--location-active {\n
		align-self: end;\n
	}\n
	.dc-roster-form__extras {\n
		grid-template-columns: repeat(2, minmax(0, 1fr));\n
		column-gap: var(--dc-space-4);\n
	}\n
	.dc-roster-form__extras > .dc-roster-form__section-heading {\n
		grid-column: 1 / -1;\n
	}\n
	.dc-roster-form__extras > .dc-field--roster-break {\n
		grid-column: 1 / 2;\n
	}\n
	.dc-roster-form__extras > .dc-field--roster-note {\n
		grid-column: 2 / -1;\n
	}\n
	.dc-roster-form__step-group:has(.dc-field--roster-employee) {\n
		display: grid;\n
		grid-template-columns: repeat(2, minmax(0, 1fr));\n
		column-gap: var(--dc-space-4);\n
		row-gap: var(--dc-space-3);\n
	}\n
	.dc-roster-form__step-group:has(.dc-field--roster-employee) > .dc-roster-form__section-heading,\n
	.dc-roster-form__step-group:has(.dc-field--roster-employee) > .dc-field--roster-employee,\n
	.dc-roster-form__step-group:has(.dc-field--roster-employee) > .dc-field--roster-location {\n
		grid-column: 1 / -1;\n
	}\n
	.dc-roster-form__step-group:has(.dc-field--roster-start) {\n
		display: grid;\n
		grid-template-columns: repeat(2, minmax(0, 1fr));\n
		column-gap: var(--dc-space-4);\n
		row-gap: var(--dc-space-3);\n
	}\n
	.dc-roster-form__step-group:has(.dc-field--roster-start) > .dc-roster-form__section-heading,\n
	.dc-roster-form__step-group:has(.dc-field--roster-start) > .dc-field--roster-time-hint,\n
	.dc-roster-form__step-group:has(.dc-field--roster-start) > .dc-field--roster-rest-hint {\n
		grid-column: 1 / -1;\n
	}\n
	.dc-form-grid--my-roster-filter {\n
		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) auto;\n
	}\n
	.dc-timezone-picker {\n
		max-width: 36rem;\n
	}\n
	.dc-summary-grid {\n
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));\n
	}\n
	.dc-section__header {\n
		grid-template-columns: minmax(0, 1fr) auto;\n
	}\n
	.dc-section__controls {\n
		justify-self: end;\n
	}\n
	.dc-section__header > .dc-hint-dismiss {\n
		justify-self: end;\n
		text-align: inherit;\n
	}\n
	.dc-conflict {\n
		grid-template-columns: auto minmax(0, 1fr) auto;\n
	}\n
	.dc-conflict__actions {\n
		grid-column: auto;\n
	}\n
	.dc-ical__actions .button {\n
		flex: 0 1 auto;\n
		min-width: 0;\n
	}\n
	.dc-table-wrap {\n
		border: 1px solid var(--dc-border);\n
		background: var(--dc-bg-card);\n
	}\n
	.dc-table {\n
		display: table;\n
		width: 100%;\n
		border-collapse: collapse;\n
	}\n
	.dc-table thead {\n
		position: static;\n
		width: auto;\n
		height: auto;\n
		margin: 0;\n
		padding: 0;\n
		overflow: visible;\n
		clip: auto;\n
		white-space: normal;\n
		border: 0;\n
		display: table-header-group;\n
	}\n
	.dc-table tbody {\n
		display: table-row-group;\n
	}\n
	.dc-table tr {\n
		display: table-row;\n
		width: auto;\n
	}\n
	.dc-table td,\n
	.dc-table th {\n
		display: table-cell;\n
		width: auto;\n
		padding: var(--dc-space-3) var(--dc-space-4);\n
		border-bottom: 1px solid var(--dc-border);\n
	}\n
	.dc-table tbody tr {\n
		display: table-row;\n
		gap: 0;\n
		padding: 0;\n
		margin-bottom: 0;\n
		border: 0;\n
		border-radius: 0;\n
		background: transparent;\n
	}\n
	.dc-table tbody tr:nth-child(even) td {\n
		background: color-mix(in srgb, var(--dc-bg-card) 96%, var(--color-main-text, #1d1d1d) 4%);\n
	}\n
	.dc-table td[data-cell]::before,\n
	.dc-table tbody th.dc-table__rowhead[data-cell]::before {\n
		content: none;\n
		display: none;\n
	}\n
	.dc-table .dc-row-actions {\n
		padding-top: 0;\n
	}\n
	.dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell,\n
	.dc-table tbody tr:has(td[colspan]) td[colspan] {\n
		display: table-cell;\n
		text-align: center;\n
		vertical-align: middle;\n
		padding: var(--dc-space-7) var(--dc-space-4);\n
	}\n
	.dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell .dc-roster-empty-state {\n
		margin-inline: auto;\n
	}\n
	.dc-modal__dialog {\n
		padding: var(--dc-space-5);\n
	}\n
}\n
\n
/* Employee self-service (my-roster / my-absences) */\n
.dc-employee-crossnav {\n
	margin-top: var(--dc-space-4);\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
}\n
.dc-employee-crossnav .button {\n
	white-space: normal;\n
	text-align: center;\n
	min-height: 44px;\n
	line-height: var(--dc-line-base);\n
}\n
.dc-employee-alert {\n
	margin-bottom: 0;\n
}\n
\n
.dc-summary-tile--action {\n
	text-decoration: none;\n
	color: inherit;\n
	border: 1px solid var(--dc-border);\n
	box-sizing: border-box;\n
	justify-content: center;\n
	text-align: center;\n
	min-height: 100%;\n
	transition: background 0.15s ease, border-color 0.15s ease;\n
}\n
a.dc-summary-tile--action:hover {\n
	background: var(--dc-tint-info);\n
	border-color: transparent;\n
}\n
a.dc-summary-tile--action:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 2px;\n
}\n
.dc-summary-tile--action .dc-summary-tile__value {\n
	font-size: var(--dc-fs-lg);\n
}\n
\n
.dc-table th.dc-table__rowhead {\n
	text-align: left;\n
	font-weight: 600;\n
	color: var(--color-main-text, #1d1d1d);\n
	vertical-align: middle;\n
	border-bottom: 1px solid var(--dc-border);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	background: var(--dc-bg-card);\n
}\n
\n
.dc-fieldset-reset {\n
	border: 0;\n
	margin: 0;\n
	padding: 0;\n
	min-width: 0;\n
}\n
.dc-form-grid--my-absence fieldset.dc-fieldset-reset {\n
	display: contents;\n
}\n
\n
form.dc-form-grid[aria-busy='true'] {\n
	opacity: 0.92;\n
	pointer-events: none;\n
}\n
form.dc-form-grid[aria-busy='true'] .button {\n
	cursor: wait;\n
}\n
\n
/* My-roster quick-range chips + custom range form */\n
.dc-quickfilters {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	margin-bottom: var(--dc-space-3);\n
}\n
.dc-quickfilters__btn {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	min-height: 44px;\n
	padding: 0 var(--dc-space-4);\n
	border-radius: 999px;\n
	border: 1px solid var(--dc-border-strong);\n
	background: var(--color-main-background, #fff);\n
	color: var(--color-main-text, #1d1d1d);\n
	cursor: pointer;\n
	font-weight: 600;\n
	font-size: var(--dc-fs-sm);\n
	line-height: 1;\n
	transition: background 0.15s ease, border-color 0.15s ease, color 0.15s ease;\n
}\n
.dc-quickfilters__btn:hover {\n
	border-color: var(--color-primary-element, #0082c9);\n
}\n
.dc-quickfilters__btn:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 55%, transparent);\n
	outline-offset: 2px;\n
}\n
.dc-quickfilters__btn[aria-pressed='true'] {\n
	background: var(--color-primary-element, #0082c9);\n
	color: var(--color-primary-element-text, #fff);\n
	border-color: var(--color-primary-element, #0082c9);\n
	box-shadow: var(--dc-shadow-sm);\n
}\n
.dc-quickfilters__btn[disabled] {\n
	opacity: 0.55;\n
	cursor: not-allowed;\n
}\n
\n
.dc-form-grid--my-roster-filter {\n
	display: grid;\n
	grid-template-columns: 1fr;\n
	gap: var(--dc-space-3);\n
	align-items: end;\n
}\n
.dc-form-actions--my-roster-filter {\n
	display: flex;\n
	align-items: end;\n
	justify-content: flex-start;\n
}\n
.dc-form-actions--my-roster-filter .button {\n
	min-height: 44px;\n
}\n
\n
/* ── Administrator: roster export (screen) ─────────────────── */\n
.dc-roster-export {\n
	border-inline-start: 4px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 45%, var(--color-main-background, #fff));\n
	background: var(--dc-tint-info);\n
}\n
.dc-roster-export__actions {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-3);\n
	align-items: center;\n
	margin-top: var(--dc-space-3);\n
}\n
.dc-roster-export__actions .button {\n
	min-height: 44px;\n
}\n
.dc-roster-export__hint {\n
	margin: 0;\n
	color: var(--color-main-text, #1d1d1d);\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
	max-width: 52rem;\n
}\n
.dc-roster-export a.button[aria-disabled='true'] {\n
	opacity: 0.6;\n
	cursor: not-allowed;\n
}\n
\n
/* ── Printable roster (blank layout) ───────────────────────── */\n
.dc-html-print,\n
.dc-body-print {\n
	margin: 0;\n
	background: var(--color-main-background, #fff);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-app--roster-print {\n
	max-width: 72rem;\n
	margin: 0 auto;\n
	padding: var(--dc-space-5) var(--dc-space-4) var(--dc-space-7);\n
	box-sizing: border-box;\n
}\n
.dc-print-header {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-4);\n
	align-items: flex-start;\n
	justify-content: space-between;\n
	margin-bottom: var(--dc-space-5);\n
	padding-bottom: var(--dc-space-4);\n
	border-bottom: 2px solid var(--dc-border-strong);\n
}\n
.dc-print-header__brand {\n
	flex: 0 0 auto;\n
	display: flex;\n
	align-items: center;\n
	justify-content: center;\n
	width: 48px;\n
	height: 48px;\n
	border-radius: var(--dc-radius-md);\n
	background: var(--dc-tint-info);\n
	color: var(--color-primary-element, #0082c9);\n
}\n
.dc-print-header__icon {\n
	width: 28px;\n
	height: 28px;\n
}\n
.dc-print-header__text {\n
	flex: 1 1 240px;\n
	min-width: 0;\n
}\n
.dc-print-title {\n
	margin: 0 0 var(--dc-space-2);\n
	font-size: var(--dc-fs-2xl);\n
	line-height: var(--dc-line-tight);\n
	font-weight: 700;\n
}\n
.dc-print-meta {\n
	margin: 0;\n
	font-size: var(--dc-fs-md);\n
	line-height: var(--dc-line-base);\n
	color: var(--color-main-text, #1d1d1d);\n
}\n
.dc-print-meta--sub {\n
	margin-top: var(--dc-space-2);\n
	font-size: var(--dc-fs-sm);\n
	color: var(--dc-muted);\n
}\n
.dc-print-toolbar {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
}\n
.dc-print-footnote {\n
	font-size: var(--dc-fs-sm);\n
	color: var(--dc-muted);\n
	margin: 0 0 var(--dc-space-3);\n
	padding: var(--dc-space-3);\n
	background: var(--dc-bg-stripe);\n
	border-radius: var(--dc-radius-sm);\n
	border: 1px solid var(--dc-border);\n
}\n
.dc-print-table-wrap {\n
	overflow-x: auto;\n
	-webkit-overflow-scrolling: touch;\n
}\n
.dc-print-table {\n
	width: 100%;\n
	border-collapse: collapse;\n
	font-size: var(--dc-fs-sm);\n
}\n
.dc-print-table th,\n
.dc-print-table td {\n
	text-align: left;\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border: 1px solid var(--dc-border-strong);\n
	vertical-align: top;\n
}\n
.dc-print-table th {\n
	background: var(--dc-bg-stripe);\n
	font-weight: 700;\n
}\n
.dc-print-table__empty {\n
	text-align: center;\n
	color: var(--dc-muted);\n
	padding: var(--dc-space-5);\n
}\n
.dc-print-overnight {\n
	font-weight: 700;\n
	color: var(--color-main-text, #1d1d1d);\n
	margin-inline-start: var(--dc-space-1);\n
}\n
.dc-print-legend {\n
	font-size: var(--dc-fs-xs);\n
	color: var(--dc-muted);\n
	margin-top: var(--dc-space-2);\n
}\n
.print-only {\n
	display: none;\n
}\n
\n
@media (min-width: 768px) {\n
	#dc-assignments-table-wrap {\n
		border-radius: var(--dc-radius-sm);\n
		background: var(--dc-bg-card);\n
	}\n
	#dc-assignments-table-wrap .dc-table {\n
		margin-bottom: 0;\n
	}\n
	#dc-assignments-table-wrap .dc-roster-empty-panel {\n
		border-top: 1px solid var(--dc-border);\n
		border-radius: 0 0 var(--dc-radius-sm) var(--dc-radius-sm);\n
	}\n
}\n
.dc-app--roster-print-error .dc-print-error {\n
	max-width: 36rem;\n
	margin: var(--dc-space-7) auto;\n
	padding: var(--dc-space-6);\n
	background: var(--dc-bg-card);\n
	border-radius: var(--dc-radius-lg);\n
	box-shadow: var(--dc-shadow-md);\n
	border: 1px solid var(--dc-border);\n
	text-align: center;\n
}\n
.dc-print-error__icon {\n
	display: flex;\n
	justify-content: center;\n
	margin-bottom: var(--dc-space-3);\n
	color: var(--color-primary-element, #0082c9);\n
}\n
.dc-print-error__message {\n
	font-size: var(--dc-fs-md);\n
	line-height: var(--dc-line-base);\n
}\n
@media print {\n
	.no-print {\n
		display: none !important;\n
	}\n
	.print-only {\n
		display: block;\n
	}\n
	.dc-app--roster-print {\n
		max-width: none;\n
		padding: 0;\n
	}\n
	.dc-print-table th {\n
		background: var(--dc-bg-stripe) !important;\n
		-webkit-print-color-adjust: exact;\n
		print-color-adjust: exact;\n
	}\n
	a[href]::after {\n
		content: none !important;\n
	}\n
	/* REQ-UX-13: must win over the global wipe — expand AT recovery / source outbound URLs. */\n
	.dc-table a[href]::after,\n
	.dc-conflict__actions a[href]::after,\n
	.dc-row-actions a[href]::after {\n
		content: " (" attr(href) ")" !important;\n
		font-size: 0.85em;\n
		word-break: break-all;\n
	}\n
}\n
@media (prefers-reduced-motion: reduce) {\n
	.dc-roster-export,\n
	.dc-print-header {\n
		transition: none;\n
	}\n
}\n
\n
/* === support-us:dc start === */\n
/**\n
 * Support & Us — shared family styles (WCAG 2.1 AA, responsive).\n
 * Prefix: dc\n
 * Maps to Nextcloud --color-* so light / dark / high-contrast / custom accents adapt.\n
 */\n
\n
.dc-support-us {\n
	margin-block-start: var(--dc-space-5, 1.5rem);\n
	min-width: 0;\n
	width: 100%;\n
	box-sizing: border-box;\n
}\n
\n
.dc-support-us--page {\n
	margin-block-start: 0;\n
	border: 0;\n
	background: transparent;\n
	max-width: none;\n
}\n
\n
.dc-support-us__header {\n
	margin-block-end: var(--dc-space-4, 1rem);\n
}\n
\n
.dc-support-us__title {\n
	margin: 0 0 var(--dc-space-2, 0.5rem);\n
	font-size: var(--dc-fs-xl, 1.5rem);\n
	font-weight: 700;\n
	line-height: var(--dc-line-tight, 1.2);\n
	color: var(--color-main-text);\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-support-us__intro {\n
	margin: 0;\n
	max-width: 65ch;\n
	font-size: var(--dc-fs-md, 1rem);\n
	line-height: var(--dc-line-base, 1.45);\n
	color: var(--color-text-maxcontrast);\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-support-us__body {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-5, 1.5rem);\n
	min-width: 0;\n
	width: 100%;\n
}\n
\n
.dc-support-us__primary {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: flex-start;\n
	gap: var(--dc-space-3, 0.75rem);\n
	min-width: 0;\n
	width: 100%;\n
	padding: var(--dc-space-4, 1rem);\n
	border: 1px solid var(--color-border-maxcontrast, var(--color-border));\n
	border-inline-start: 4px solid var(--color-primary-element);\n
	border-radius: var(--dc-radius-md, var(--border-radius-large, 12px));\n
	background: color-mix(in srgb, var(--color-primary-element) 10%, var(--color-main-background));\n
	box-sizing: border-box;\n
}\n
\n
.dc-support-us__primary-copy,\n
.dc-support-us__primary-actions {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: flex-start;\n
	gap: var(--dc-space-3, 0.75rem);\n
	min-width: 0;\n
	width: 100%;\n
}\n
\n
.dc-support-us__offer-title {\n
	margin: 0;\n
	font-size: var(--dc-fs-lg, 1.125rem);\n
	font-weight: 700;\n
	line-height: var(--dc-line-tight, 1.25);\n
	color: var(--color-main-text);\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-support-us__benefit,\n
.dc-support-us__coverage,\n
.dc-support-us__hint {\n
	margin: 0;\n
	max-width: 65ch;\n
	font-size: var(--dc-fs-sm, 0.875rem);\n
	line-height: var(--dc-line-base, 1.45);\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-support-us__benefit {\n
	font-size: var(--dc-fs-md, 1rem);\n
	color: var(--color-main-text);\n
}\n
\n
.dc-support-us__coverage,\n
.dc-support-us__hint {\n
	color: var(--color-text-maxcontrast);\n
}\n
\n
.dc-support-us__hint a {\n
	color: var(--color-main-text);\n
	text-decoration: underline;\n
	text-underline-offset: 0.15em;\n
}\n
\n
.dc-support-us__secondary {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3, 0.75rem);\n
	min-width: 0;\n
	width: 100%;\n
}\n
\n
.dc-support-us__secondary-title {\n
	margin: 0;\n
	font-size: var(--dc-fs-md, 1rem);\n
	font-weight: 600;\n
	line-height: var(--dc-line-tight, 1.25);\n
	color: var(--color-main-text);\n
}\n
\n
.dc-support-us__options {\n
	display: grid;\n
	grid-template-columns: repeat(auto-fit, minmax(min(100%, 16rem), 1fr));\n
	gap: var(--dc-space-4, 1rem);\n
	width: 100%;\n
	min-width: 0;\n
}\n
\n
.dc-support-us__option {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: stretch;\n
	gap: var(--dc-space-3, 0.75rem);\n
	min-width: 0;\n
	padding: var(--dc-space-4, 1rem);\n
	border: 1px solid var(--color-border);\n
	border-radius: var(--dc-radius-md, var(--border-radius-large, 12px));\n
	background: var(--color-main-background);\n
	box-sizing: border-box;\n
}\n
\n
.dc-support-us__option-title {\n
	margin: 0;\n
	font-size: var(--dc-fs-lg, 1.125rem);\n
	font-weight: 700;\n
	line-height: var(--dc-line-tight, 1.25);\n
	color: var(--color-main-text);\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-support-us__option-hint {\n
	margin: 0;\n
	flex: 1 1 auto;\n
	font-size: var(--dc-fs-sm, 0.875rem);\n
	line-height: var(--dc-line-base, 1.45);\n
	color: var(--color-text-maxcontrast);\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-support-us__cta {\n
	/* WCAG 2.5.5: px + !important — NC root is 15px and .azc-btn uses 2.75rem (41.25px). */\n
	min-height: 44px !important;\n
	min-width: 44px !important;\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	padding-inline: var(--dc-space-4, 1rem);\n
	text-align: center;\n
	text-decoration: none;\n
	box-sizing: border-box;\n
	white-space: normal;\n
	overflow-wrap: anywhere;\n
	max-width: 100%;\n
}\n
\n
.dc-support-us__cta--primary {\n
	min-height: 48px !important;\n
}\n
\n
.dc-support-us__option .dc-support-us__cta,\n
.dc-support-us--page .dc-support-us__primary-actions .dc-support-us__cta {\n
	align-self: stretch;\n
	width: 100%;\n
}\n
\n
.dc-support-us__cta:focus-visible,\n
.dc-support-us__hint a:focus-visible,\n
.dc-support-us__more a:focus-visible,\n
.dc-support-us__contact a:focus-visible {\n
	outline: 2px solid var(--color-primary-element);\n
	outline-offset: 2px;\n
}\n
\n
.dc-support-us__cta:focus:not(:focus-visible),\n
.dc-support-us__hint a:focus:not(:focus-visible),\n
.dc-support-us__more a:focus:not(:focus-visible),\n
.dc-support-us__contact a:focus:not(:focus-visible) {\n
	outline: none;\n
}\n
\n
.dc-support-us__tertiary {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2, 0.5rem);\n
	padding-block-start: var(--dc-space-4, 1rem);\n
	border-block-start: 1px solid var(--color-border);\n
}\n
\n
.dc-support-us__more,\n
.dc-support-us__contact {\n
	margin: 0;\n
	font-size: var(--dc-fs-sm, 0.875rem);\n
	line-height: var(--dc-line-base, 1.45);\n
	color: var(--color-text-maxcontrast);\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-support-us__more a,\n
.dc-support-us__contact a {\n
	color: var(--color-main-text);\n
	text-decoration: underline;\n
	text-underline-offset: 0.15em;\n
}\n
\n
.dc-support-us__new-tab {\n
	position: absolute;\n
	width: 1px;\n
	height: 1px;\n
	padding: 0;\n
	margin: -1px;\n
	overflow: hidden;\n
	clip: rect(0, 0, 0, 0);\n
	clip-path: inset(50%);\n
	white-space: nowrap;\n
	border: 0;\n
}\n
\n
@media (max-width: 768px) {\n
	.dc-support-us__options {\n
		grid-template-columns: 1fr;\n
	}\n
\n
	.dc-support-us__cta,\n
	.dc-support-us__primary,\n
	.dc-support-us__option {\n
		width: 100%;\n
		max-width: none;\n
	}\n
}\n
\n
@media (prefers-contrast: more) {\n
	.dc-support-us__primary {\n
		background: var(--color-main-background);\n
		border-color: var(--color-main-text);\n
		border-inline-start-width: 4px;\n
	}\n
\n
	.dc-support-us__option {\n
		border-width: 2px;\n
		border-color: var(--color-main-text);\n
	}\n
\n
	.dc-support-us__cta:focus-visible,\n
	.dc-support-us__hint a:focus-visible,\n
	.dc-support-us__more a:focus-visible,\n
	.dc-support-us__contact a:focus-visible {\n
		outline-width: 3px;\n
	}\n
}\n
\n
@media (forced-colors: active) {\n
	.dc-support-us__primary,\n
	.dc-support-us__option {\n
		border: 2px solid CanvasText;\n
		background: Canvas;\n
	}\n
\n
	.dc-support-us__cta:focus-visible,\n
	.dc-support-us__hint a:focus-visible,\n
	.dc-support-us__more a:focus-visible,\n
	.dc-support-us__contact a:focus-visible {\n
		outline: 3px solid Highlight;\n
	}\n
}\n
\n
@media (prefers-reduced-motion: reduce) {\n
	.dc-support-us__cta,\n
	.dc-support-us__option {\n
		transition: none;\n
	}\n
}\n
/* === support-us:dc end === */\n
\n
\n
/* My roster: accessible swap dialog (replaces window.prompt) */\n
/* Nextcloud core server.css applies a legacy HTML5 reset (`dialog { display: block }`)\n
   that defeats the UA's `dialog:not([open]) { display: none }` — re-hide closed dialogs. */\n
.dc-dialog:not([open]) {\n
	display: none;\n
}\n
.dc-dialog {\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	padding: 0;\n
	max-width: min(28rem, calc(100vw - 2rem));\n
	width: 100%;\n
	background: var(--dc-bg-card);\n
	color: var(--color-main-text, #1d1d1d);\n
	box-shadow: var(--dc-shadow-md);\n
}\n
.dc-dialog::backdrop {\n
	/* Theme-safe overlay: never a hardcoded blue cast. */\n
	background: color-mix(in srgb, var(--color-main-text, #000) 45%, transparent);\n
}\n
.dc-dialog__panel {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	padding: var(--dc-space-5) var(--dc-space-5) var(--dc-space-4);\n
	margin: 0;\n
}\n
.dc-dialog__title {\n
	margin: 0;\n
	font-size: var(--dc-fs-lg);\n
	line-height: var(--dc-line-tight);\n
}\n
.dc-dialog__intro {\n
	margin: 0;\n
	color: var(--dc-muted);\n
	line-height: var(--dc-line-base);\n
}\n
.dc-dialog__actions {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	justify-content: flex-end;\n
	margin-top: var(--dc-space-1);\n
}\n
.dc-dialog__actions .button {\n
	min-height: 44px;\n
}\n
\n
/* Settings sub-pages: in-page chip bar (DeskCheck parity). Critical when\n
   Nextcloud collapses #app-navigation on narrow viewports — without it,\n
   sibling settings pages would be unreachable on phones/tablets. */\n
.dc-settings-nav {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	margin: 0 0 var(--dc-space-5);\n
	padding: 0 0 var(--dc-space-3);\n
	border-bottom: 1px solid var(--dc-border);\n
}\n
.dc-app .dc-settings-nav__link {\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	min-height: 44px;\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--color-main-background, #fff);\n
	color: var(--color-main-text, #222);\n
	text-decoration: none;\n
	font-weight: 600;\n
	font-size: var(--dc-fs-sm);\n
	line-height: 1.2;\n
}\n
.dc-app .dc-settings-nav__link:hover {\n
	border-color: var(--color-primary-element, #0082c9);\n
	background: var(--dc-bg-stripe);\n
	color: var(--color-main-text, #222);\n
}\n
.dc-app .dc-settings-nav__link:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 2px;\n
}\n
.dc-app .dc-settings-nav__link.is-active,\n
.dc-app .dc-settings-nav__link[aria-current="page"] {\n
	/* Keep main-text ink (not primary) — primary-on-dark-tint failed axe at 4.28:1. */\n
	border-color: var(--color-primary-element, #0082c9);\n
	background: var(--dc-tint-info);\n
	color: var(--color-main-text, #222);\n
	font-weight: 700;\n
}\n
.dc-inline-link {\n
	color: var(--color-primary-element, #0082c9);\n
	text-decoration: underline;\n
	text-underline-offset: 2px;\n
}\n
.dc-inline-link:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 2px;\n
}\n
@media (max-width: 640px) {\n
	.dc-settings-nav {\n
		flex-direction: column;\n
	}\n
	.dc-app .dc-settings-nav__link {\n
		width: 100%;\n
		justify-content: flex-start;\n
	}\n
}\n
\n
/* Settings sub-pages: sidebar sub-navigation expanded while on a settings page.\n
   One entry per sub-page; the active child carries aria-current="page". */\n
.dc-nav__sublist {\n
	list-style: none;\n
	margin: 2px 0 var(--dc-space-2) var(--dc-space-3);\n
	/* Indent past the 24px icon column so children read as nested; the\n
	   vertical guide line makes the hierarchy obvious at a glance. */\n
	padding: 0 0 0 calc(24px + var(--dc-space-2) - 2px);\n
	display: flex;\n
	flex-direction: column;\n
	gap: 2px;\n
	border-left: 2px solid var(--dc-border);\n
}\n
.dc-nav__subitem { margin: 0; }\n
.dc-nav__sublink {\n
	display: flex;\n
	align-items: center;\n
	min-height: 44px;\n
	padding: var(--dc-space-1) var(--dc-space-3);\n
	border-radius: var(--dc-radius-sm);\n
	color: var(--color-main-text, #1d1d1d);\n
	text-decoration: none;\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-tight);\n
	overflow-wrap: anywhere;\n
}\n
.dc-nav__sublink:hover {\n
	background: var(--dc-bg-stripe);\n
}\n
.dc-nav__sublink:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n
	outline-offset: 2px;\n
}\n
.dc-nav__subitem.is-active .dc-nav__sublink {\n
	background: var(--dc-tint-info);\n
	font-weight: 600;\n
}\n
\n
/* Wave A planner: copy-period + ack stats — use design tokens, never raw hex */\n
.dc-roster-copy-period {\n
	margin-top: var(--dc-space-4);\n
	padding-top: var(--dc-space-3);\n
	border-top: 1px solid var(--dc-border);\n
}\n
.dc-roster-copy-period__row {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
}\n
.dc-roster-copy-period__row .dc-input {\n
	min-width: min(100%, 16rem);\n
	flex: 1 1 12rem;\n
}\n
.dc-roster-ack-stats {\n
	margin-top: var(--dc-space-2);\n
	display: inline-block;\n
}\n
.dc-table__actions {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-1);\n
	align-items: center;\n
}\n
@media (max-width: 719px) {\n
	.dc-roster-copy-period__row {\n
		flex-direction: column;\n
		align-items: stretch;\n
	}\n
}\n
\n
/* Settings: named location checklists (planner scope) */\n
.dc-fieldset {\n
	margin: 0;\n
	padding: 0;\n
	border: 0;\n
	min-width: 0;\n
}\n
.dc-check-grid {\n
	display: grid;\n
	grid-template-columns: repeat(auto-fill, minmax(12rem, 1fr));\n
	gap: var(--dc-space-2) var(--dc-space-4);\n
	margin-top: var(--dc-space-2);\n
}\n
.dc-check {\n
	display: flex;\n
	align-items: flex-start;\n
	gap: var(--dc-space-2);\n
	min-height: var(--dc-touch);\n
	padding: var(--dc-space-1) var(--dc-space-1);\n
	line-height: var(--dc-line-base);\n
	cursor: pointer;\n
}\n
.dc-check input[type='checkbox'] {\n
	/* size / theme chrome shared with .dc-checkbox above */\n
	margin-top: 0.15rem;\n
}\n
.dc-check:focus-within {\n
	outline: 2px solid var(--color-primary-element, #0082c9);\n
	outline-offset: 2px;\n
	border-radius: var(--dc-radius-sm);\n
}\n
\n
/* Roster planning grid (employee × day) */\n
.dc-roster-view-toggle {\n
	display: inline-flex;\n
	gap: var(--dc-space-1);\n
	margin-inline-end: var(--dc-space-2);\n
}\n
.dc-roster-virtual-status {\n
	margin: var(--dc-space-2) 0 0;\n
	padding: 0;\n
	color: var(--color-main-text);\n
	font-size: var(--dc-fs-md, 1rem);\n
	line-height: var(--dc-line-base, 1.5);\n
	overflow-wrap: anywhere;\n
	min-height: 1.5em;\n
}\n
.dc-roster-grid-wrap {\n
	margin-top: var(--dc-space-3);\n
}\n
.dc-roster-grid-scroller,\n
.dc-roster-list-scroller {\n
	overflow: auto;\n
	overscroll-behavior: contain;\n
	-webkit-overflow-scrolling: touch;\n
	max-height: min(70vh, 40rem);\n
	max-height: min(70dvh, 40rem);\n
	border: 1px solid var(--color-border, var(--dc-border));\n
	border-radius: var(--dc-radius-md);\n
	background: var(--color-main-background);\n
	scrollbar-gutter: stable;\n
}\n
.dc-windowed-table {\n
	overflow: auto;\n
	overscroll-behavior: contain;\n
	-webkit-overflow-scrolling: touch;\n
	max-height: min(70vh, 40rem);\n
	max-height: min(70dvh, 40rem);\n
	border: 1px solid var(--color-border, var(--dc-border));\n
	border-radius: var(--dc-radius-md);\n
	background: var(--color-main-background);\n
}\n
.dc-roster-list-scroller {\n
	margin-top: var(--dc-space-2);\n
}\n
.dc-virtual-spacer {\n
	display: block;\n
	margin: 0;\n
	padding: 0;\n
	border: 0;\n
	background: transparent;\n
	pointer-events: none;\n
	line-height: 0;\n
	font-size: 0;\n
}\n
.dc-roster-grid__spacer {\n
	width: 100%;\n
}\n
/* Roster list stays a real table at every width (WCAG 1.4.10 data-table exception).\n
   Card-stacking would make row heights variable and desync the virtual window. */\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table {\n
	display: table;\n
	width: max(100%, 52rem);\n
	border-collapse: collapse;\n
	table-layout: fixed;\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table thead {\n
	display: table-header-group;\n
	position: sticky;\n
	top: 0;\n
	z-index: 2;\n
	width: auto;\n
	height: auto;\n
	margin: 0;\n
	padding: 0;\n
	overflow: visible;\n
	clip: auto;\n
	white-space: normal;\n
	border: 0;\n
	background: var(--color-main-background);\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody {\n
	display: table-row-group;\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tr {\n
	display: table-row;\n
	width: auto;\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table th,\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table td {\n
	display: table-cell;\n
	width: auto;\n
	min-height: var(--dc-touch);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-bottom: 1px solid var(--dc-border, var(--color-border));\n
	white-space: nowrap;\n
	overflow: hidden;\n
	text-overflow: ellipsis;\n
	vertical-align: middle;\n
	text-align: start;\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table td[data-cell]::before {\n
	content: none;\n
	display: none;\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody tr:not(.dc-virtual-spacer) {\n
	display: table-row;\n
	gap: 0;\n
	margin: 0;\n
	padding: 0;\n
	border: 0;\n
	border-radius: 0;\n
	background: transparent;\n
	min-height: var(--dc-touch);\n
	height: var(--dc-touch);\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody tr.dc-virtual-spacer {\n
	display: table-row;\n
	margin: 0;\n
	padding: 0;\n
	border: 0;\n
	background: transparent;\n
	min-height: 0;\n
	height: auto;\n
	pointer-events: none;\n
}\n
#dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody tr.dc-virtual-spacer td {\n
	display: table-cell;\n
	padding: 0 !important;\n
	border: 0 !important;\n
	background: transparent !important;\n
	line-height: 0;\n
	font-size: 0;\n
	overflow: hidden;\n
}\n
/* Catalog lists (employees / locations / absences): real table at every width so\n
   virtual row heights stay uniform (same exception as the roster list, FM-19). */\n
.dc-windowed-table .dc-table {\n
	display: table;\n
	width: max(100%, 40rem);\n
	border-collapse: collapse;\n
	table-layout: fixed;\n
}\n
.dc-windowed-table .dc-table thead {\n
	display: table-header-group;\n
	position: sticky;\n
	top: 0;\n
	z-index: 2;\n
	width: auto;\n
	height: auto;\n
	margin: 0;\n
	padding: 0;\n
	overflow: visible;\n
	clip: auto;\n
	white-space: normal;\n
	border: 0;\n
	background: var(--color-main-background);\n
}\n
.dc-windowed-table .dc-table tbody {\n
	display: table-row-group;\n
}\n
.dc-windowed-table .dc-table tr {\n
	display: table-row;\n
	width: auto;\n
}\n
.dc-windowed-table .dc-table th,\n
.dc-windowed-table .dc-table td {\n
	display: table-cell;\n
	width: auto;\n
	min-height: var(--dc-touch);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border-bottom: 1px solid var(--dc-border, var(--color-border));\n
	white-space: nowrap;\n
	overflow: hidden;\n
	text-overflow: ellipsis;\n
	vertical-align: middle;\n
	text-align: start;\n
}\n
.dc-windowed-table .dc-table td[data-cell]::before {\n
	content: none;\n
	display: none;\n
}\n
.dc-windowed-table .dc-table tbody tr:not(.dc-virtual-spacer) {\n
	display: table-row;\n
	gap: 0;\n
	margin: 0;\n
	padding: 0;\n
	border: 0;\n
	border-radius: 0;\n
	background: transparent;\n
	min-height: var(--dc-touch);\n
	height: var(--dc-touch);\n
}\n
.dc-windowed-table .dc-table tbody tr.dc-virtual-spacer {\n
	display: table-row;\n
	margin: 0;\n
	padding: 0;\n
	border: 0;\n
	background: transparent;\n
	min-height: 0;\n
	height: auto;\n
	pointer-events: none;\n
}\n
.dc-windowed-table .dc-table tbody tr.dc-virtual-spacer td {\n
	display: table-cell;\n
	padding: 0 !important;\n
	border: 0 !important;\n
	background: transparent !important;\n
	line-height: 0;\n
	font-size: 0;\n
	overflow: hidden;\n
}\n
.dc-roster-grid {\n
	/* Grow with day columns so month periods scroll horizontally instead of wrapping. */\n
	--dc-roster-day-min: 4.5rem;\n
	min-width: 100%;\n
	width: max-content;\n
	outline: none;\n
}\n
.dc-roster-grid:focus-visible {\n
	outline: 2px solid var(--color-primary-element, #0082c9);\n
	outline-offset: 2px;\n
}\n
.dc-roster-grid__table {\n
	display: grid;\n
	gap: 0;\n
	background: var(--color-main-background);\n
}\n
.dc-roster-grid__row {\n
	/* Explicit day count (set by JS). auto-fit collapsed month headers into overlapping piles. */\n
	display: grid;\n
	grid-template-columns: minmax(7rem, 10rem) repeat(auto-fit, minmax(4.5rem, 1fr));\n
	min-width: max(100%, calc(10rem + (var(--dc-roster-day-count, 7) * var(--dc-roster-day-min))));\n
	border-bottom: 1px solid var(--color-border, var(--dc-border));\n
}\n
.dc-roster-grid__row:last-child {\n
	border-bottom: 0;\n
}\n
.dc-roster-grid__row--head {\n
	position: sticky;\n
	top: 0;\n
	z-index: 3;\n
	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 8%, var(--color-main-background));\n
	font-weight: 600;\n
	box-shadow: 0 1px 0 color-mix(in srgb, var(--color-border, var(--dc-border)) 80%, transparent);\n
}\n
.dc-roster-grid__corner {\n
	position: sticky;\n
	left: 0;\n
	z-index: 4;\n
	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 8%, var(--color-main-background));\n
}\n
.dc-roster-grid__rowhead {\n
	position: sticky;\n
	left: 0;\n
	z-index: 1;\n
	background: var(--color-main-background);\n
	font-weight: 600;\n
}\n
.dc-roster-grid__corner,\n
.dc-roster-grid__colhead,\n
.dc-roster-grid__rowhead,\n
.dc-roster-grid__cell {\n
	min-height: var(--dc-touch);\n
	padding: var(--dc-space-1) var(--dc-space-2);\n
	border-inline-end: 1px solid var(--color-border, var(--dc-border));\n
}\n
.dc-roster-grid__colhead {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: center;\n
	justify-content: center;\n
	gap: 0.1rem;\n
	text-align: center;\n
	line-height: 1.15;\n
	overflow: hidden;\n
	white-space: nowrap;\n
	font-variant-numeric: tabular-nums;\n
	letter-spacing: 0.01em;\n
}\n
.dc-roster-grid__colhead-wd {\n
	display: block;\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 600;\n
	color: var(--color-text-maxcontrast);\n
}\n
.dc-roster-grid__colhead-day {\n
	display: block;\n
	font-size: var(--dc-fs-sm);\n
	font-weight: 700;\n
	color: var(--color-main-text);\n
}\n
.dc-roster-grid__colhead--weekend,\n
.dc-roster-grid__cell--weekend {\n
	background: color-mix(in srgb, var(--color-main-text) 5%, var(--color-main-background));\n
}\n
.dc-roster-grid__row--head .dc-roster-grid__colhead--weekend {\n
	background: color-mix(\n
		in srgb,\n
		var(--color-main-text) 6%,\n
		color-mix(in srgb, var(--color-primary-element, #0082c9) 8%, var(--color-main-background))\n
	);\n
}\n
.dc-roster-grid__colhead--today {\n
	box-shadow: inset 0 -3px 0 var(--color-primary-element, #0082c9);\n
}\n
.dc-roster-grid__colhead--today .dc-roster-grid__colhead-day {\n
	color: var(--color-primary-element-text, #ffffff);\n
	background: var(--color-primary-element, #0082c9);\n
	padding: 0.15rem 0.4rem;\n
	border-radius: var(--dc-radius-sm);\n
	min-width: 1.5rem;\n
}\n
.dc-roster-grid__cell--today {\n
	box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-primary-element, #0082c9) 45%, transparent);\n
}\n
.dc-roster-grid--month .dc-roster-grid__colhead {\n
	padding-inline: var(--dc-space-1);\n
}\n
.dc-roster-grid__cell:last-child,\n
.dc-roster-grid__colhead:last-child {\n
	border-inline-end: 0;\n
}\n
.dc-roster-grid__cell {\n
	display: flex;\n
	flex-direction: column;\n
	align-items: stretch;\n
	justify-content: center;\n
	gap: var(--dc-space-1);\n
	cursor: pointer;\n
}\n
.dc-roster-grid__cell:focus-visible,\n
.dc-roster-grid__cell.is-selected {\n
	outline: 2px solid var(--color-primary-element, #0082c9);\n
	outline-offset: -2px;\n
	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 12%, var(--color-main-background));\n
}\n
.dc-roster-grid__cell--empty {\n
	background: color-mix(in srgb, var(--color-main-background) 92%, var(--color-border, var(--dc-border)));\n
}\n
.dc-roster-grid__shift {\n
	display: block;\n
	min-height: calc(var(--dc-touch) - var(--dc-space-2));\n
	border: 0;\n
	border-radius: var(--dc-radius-sm);\n
	background: color-mix(in srgb, var(--color-success, #2e7d32) 18%, var(--color-main-background));\n
	color: var(--color-main-text);\n
	font: inherit;\n
	cursor: inherit;\n
	text-align: center;\n
	padding: var(--dc-space-1) var(--dc-space-1);\n
	pointer-events: none;\n
}\n
.dc-roster-grid__more {\n
	font-size: 0.75rem;\n
	color: var(--color-text-maxcontrast);\n
	text-align: center;\n
}\n
.dc-roster-bulk-bar {\n
	margin-top: var(--dc-space-3);\n
	padding: var(--dc-space-3);\n
	border: 1px solid var(--color-border, var(--dc-border));\n
	border-radius: var(--dc-radius-md);\n
	background: var(--color-main-background);\n
}\n
.dc-roster-bulk-bar__row {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	align-items: center;\n
	margin-top: var(--dc-space-2);\n
}\n
.dc-settings-privacy__list {\n
	margin: 0 0 var(--dc-space-4);\n
	padding-inline-start: 1.25rem;\n
	line-height: 1.5;\n
}\n
.dc-settings-privacy__glossary {\n
	display: grid;\n
	gap: var(--dc-space-3);\n
	margin: 0;\n
	padding: 0;\n
	min-width: 0;\n
	max-width: 100%;\n
}\n
/* Same Nextcloud core dt/dd reset as .dc-scope-strip (130px end-aligned labels). */\n
#app-content.dc-app .dc-settings-privacy__glossary dt,\n
#app-content.dc-app .dc-settings-privacy__glossary dd,\n
.dc-app .dc-settings-privacy__glossary dt,\n
.dc-app .dc-settings-privacy__glossary dd {\n
	display: block;\n
	float: none;\n
	width: auto;\n
	max-width: 100%;\n
	padding: 0;\n
	text-align: start;\n
	white-space: normal;\n
}\n
.dc-settings-privacy__glossary dt {\n
	font-weight: 600;\n
	overflow-wrap: anywhere;\n
	margin: 0;\n
	min-width: 0;\n
}\n
.dc-settings-privacy__glossary dd {\n
	margin: 0.15rem 0 0;\n
	color: var(--color-text-maxcontrast);\n
	overflow-wrap: anywhere;\n
	min-width: 0;\n
}\n
.dc-print-integrity {\n
	margin-top: var(--dc-space-4);\n
	padding-top: var(--dc-space-3);\n
	border-top: 1px solid var(--color-border, var(--dc-border));\n
	font-size: var(--dc-fs-sm);\n
}\n
.dc-print-integrity__value {\n
	word-break: break-all;\n
	font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;\n
}\n
@media (max-width: 719px) {\n
	.dc-roster-grid__row {\n
		grid-template-columns: minmax(5.5rem, 7rem) repeat(var(--dc-roster-day-count, 7), minmax(var(--dc-roster-day-min, 3.75rem), 1fr));\n
		min-width: max(100%, calc(7rem + (var(--dc-roster-day-count, 7) * var(--dc-roster-day-min, 3.75rem))));\n
	}\n
	.dc-roster-view-toggle {\n
		width: 100%;\n
		margin: 0 0 var(--dc-space-2);\n
	}\n
	.dc-roster-grid-scroller,\n
	.dc-roster-list-scroller {\n
		max-height: min(65vh, 28rem);\n
		max-height: min(65dvh, 28rem);\n
	}\n
	.dc-windowed-table {\n
		max-height: min(65vh, 28rem);\n
		max-height: min(65dvh, 28rem);\n
	}\n
}\n
@media (min-width: 768px) {\n
	.dc-roster-list-scroller .dc-table thead {\n
		position: sticky;\n
		top: 0;\n
		z-index: 2;\n
		background: var(--color-main-background);\n
	}\n
}\n
@media print {\n
	.dc-roster-grid-scroller,\n
	.dc-roster-list-scroller {\n
		overflow: visible !important;\n
		max-height: none !important;\n
		border: 0;\n
	}\n
	.dc-windowed-table {\n
		overflow: visible !important;\n
		max-height: none !important;\n
		border: 0;\n
	}\n
	.dc-roster-grid__row--head,\n
	.dc-roster-grid__corner,\n
	.dc-roster-grid__rowhead,\n
	.dc-table thead {\n
		position: static;\n
	}\n
}\n
@media (prefers-reduced-motion: reduce) {\n
	.dc-roster-grid__cell,\n
	.dc-roster-grid__shift {\n
		transition: none;\n
	}\n
}\n
\n
/* Soft keyboard: leave room when visualViewport / scrollIntoView adjusts. */\n
.form-textarea,\n
textarea.form-input,\n
textarea.bc-input,\n
textarea.crm-input,\n
textarea.iv-input,\n
textarea.ic-input,\n
textarea.mc-input,\n
textarea.mn-input,\n
textarea.tc-input,\n
textarea.dk-input,\n
textarea {\n
	scroll-margin-bottom: min(45vh, 20rem);\n
	scroll-margin-top: 5rem;\n
}\n
\n
/* === app-feedback:dc start === */\n
#app-navigation .dc-nav-footer,\n
.dc-nav .dc-nav-footer,\n
.dc-app-nav .dc-nav-footer {\n
	margin-top: auto;\n
	flex-shrink: 0;\n
	min-width: 0;\n
	width: 100%;\n
	box-sizing: border-box;\n
	padding: var(--dc-space-2, 0.5rem) var(--dc-space-3, 0.75rem)\n
		max(var(--dc-space-3, 0.75rem), env(safe-area-inset-bottom, 0px));\n
	border-top: 1px solid var(--dc-border, color-mix(in srgb, var(--color-main-text) 12%, var(--color-main-background)));\n
	background: transparent;\n
	z-index: 1;\n
}\n
\n
.dc-nav-footer__popover {\n
	position: relative;\n
	display: flex;\n
	flex-direction: column;\n
	align-items: center;\n
	gap: var(--dc-space-2, 0.5rem);\n
}\n
\n
.dc-nav-footer__trigger {\n
	display: inline-flex;\n
	gap: var(--dc-space-3, 0.75rem);\n
	min-height: 44px;\n
	min-width: 44px;\n
	padding: var(--dc-space-2, 0.5rem) var(--dc-space-3, 0.75rem);\n
	border-radius: var(--dc-radius-sm, 6px);\n
	box-sizing: border-box;\n
	border: none;\n
	background: none;\n
	color: var(--color-main-text);\n
	font: inherit;\n
	cursor: pointer;\n
	align-items: center;\n
	justify-content: center;\n
	white-space: nowrap;\n
}\n
\n
.dc-nav-footer__trigger:hover {\n
	background: color-mix(in srgb, var(--color-primary-element) 8%, var(--color-main-background));\n
}\n
\n
.dc-nav-footer__trigger:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element) 60%, transparent);\n
	outline-offset: 2px;\n
}\n
\n
.dc-nav-footer__trigger:focus:not(:focus-visible) {\n
	outline: none;\n
}\n
\n
.dc-nav-footer__trigger-icon .dc-icon {\n
	width: 20px;\n
	height: 20px;\n
}\n
\n
.dc-nav-footer__menu {\n
	position: absolute;\n
	bottom: calc(100% + var(--dc-space-2, 0.5rem));\n
	left: 50%;\n
	transform: translateX(-50%);\n
	min-width: max-content;\n
	max-width: calc(100vw - 2rem);\n
	list-style: none;\n
	margin: 0;\n
	padding: var(--dc-space-2, 0.5rem);\n
	background: var(--color-main-background);\n
	border: 1px solid var(--dc-border, var(--color-border));\n
	border-radius: var(--dc-radius-md, 12px);\n
	box-shadow:\n
		0 2px 6px color-mix(in srgb, var(--color-main-text) 6%, transparent),\n
		0 8px 24px color-mix(in srgb, var(--color-main-text) 10%, transparent);\n
	display: flex;\n
	flex-direction: column;\n
	gap: 2px;\n
	z-index: 100;\n
}\n
\n
.dc-nav-footer__menu[hidden] {\n
	display: none;\n
}\n
\n
.dc-nav-footer__menu-item {\n
	display: flex;\n
	align-items: center;\n
	gap: var(--dc-space-3, 0.75rem);\n
	min-height: 44px;\n
	min-width: 44px;\n
	padding: var(--dc-space-2, 0.5rem) var(--dc-space-3, 0.75rem);\n
	border-radius: var(--dc-radius-sm, 6px);\n
	color: var(--color-main-text);\n
	text-decoration: none;\n
	font-size: var(--dc-fs-sm, 0.875rem);\n
	line-height: var(--dc-line-base, 1.45);\n
	white-space: normal;\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-nav-footer__menu-item:hover {\n
	background: color-mix(in srgb, var(--color-primary-element) 8%, var(--color-main-background));\n
}\n
\n
.dc-nav-footer__menu-item:focus-visible {\n
	outline: 3px solid color-mix(in srgb, var(--color-primary-element) 60%, transparent);\n
	outline-offset: -2px;\n
}\n
\n
.dc-nav-footer__menu-item:focus:not(:focus-visible) {\n
	outline: none;\n
}\n
\n
.dc-nav-footer__menu-icon .dc-icon {\n
	width: 18px;\n
	height: 18px;\n
}\n
\n
.dc-nav-footer__note {\n
	margin: 0;\n
	font-size: var(--dc-fs-xs, 0.75rem);\n
	line-height: 1.45;\n
	color: var(--color-text-maxcontrast);\n
	text-align: center;\n
	overflow-wrap: anywhere;\n
}\n
\n
@media (width < 480px) {\n
	.dc-nav-footer__menu {\n
		left: var(--dc-space-2, 0.5rem);\n
		right: var(--dc-space-2, 0.5rem);\n
		transform: none;\n
		min-width: 0;\n
	}\n
}\n
\n
.dc-nav-footer__new-tab {\n
	position: absolute;\n
	width: 1px;\n
	height: 1px;\n
	padding: 0;\n
	margin: -1px;\n
	overflow: hidden;\n
	clip: rect(0, 0, 0, 0);\n
	clip-path: inset(50%);\n
	white-space: nowrap;\n
	border: 0;\n
}\n
\n
.dc-nav-footer__toast-link {\n
	display: inline-flex;\n
	align-items: center;\n
	min-height: 44px;\n
	min-width: 44px;\n
	margin-top: var(--dc-space-2, 0.5rem);\n
	padding: 0;\n
	font-size: var(--dc-fs-sm, 0.875rem);\n
	line-height: 1.45;\n
	color: var(--color-error-text, var(--color-main-text));\n
	text-decoration: underline;\n
	text-underline-offset: 0.15em;\n
	overflow-wrap: anywhere;\n
}\n
\n
.dc-nav-footer__toast-link:focus-visible {\n
	outline: 2px solid var(--color-primary-element);\n
	outline-offset: 2px;\n
}\n
\n
.dc-nav-footer__toast-link:focus:not(:focus-visible) {\n
	outline: none;\n
}\n
\n
@media (prefers-contrast: more) {\n
	#app-navigation .dc-nav-footer,\n
	.dc-nav .dc-nav-footer,\n
	.dc-app-nav .dc-nav-footer {\n
		border-top-width: 2px;\n
		border-top-color: var(--color-main-text);\n
	}\n
\n
	.dc-nav-footer__menu {\n
		border-width: 2px;\n
	}\n
\n
	.dc-nav-footer__trigger:focus-visible,\n
	.dc-nav-footer__menu-item:focus-visible,\n
	.dc-nav-footer__toast-link:focus-visible {\n
		outline-width: 3px;\n
	}\n
}\n
\n
@media (forced-colors: active) {\n
	.dc-nav-footer__menu-item,\n
	.dc-nav-footer__toast-link {\n
		forced-color-adjust: none;\n
		color: LinkText;\n
	}\n
\n
	.dc-nav-footer__trigger:focus-visible,\n
	.dc-nav-footer__menu-item:focus-visible,\n
	.dc-nav-footer__toast-link:focus-visible {\n
		outline: 3px solid Highlight;\n
	}\n
}\n
\n
@media (prefers-reduced-motion: reduce) {\n
	.dc-nav-footer__trigger,\n
	.dc-nav-footer__menu-item,\n
	.dc-nav-footer__toast-link {\n
		transition: none;\n
	}\n
}\n
/* === app-feedback:dc end === */\n
\n
/* === roster-self-service-ga:dc begin === */\n
.dc-dienst-team__form {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-6);\n
}\n
\n
.dc-dienst-team__group {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	padding-bottom: var(--dc-space-5);\n
	border-bottom: 1px solid var(--dc-border);\n
}\n
\n
.dc-dienst-team__group:last-of-type {\n
	border-bottom: 0;\n
	padding-bottom: 0;\n
}\n
\n
.dc-dienst-team__group-title {\n
	margin: 0;\n
	font-size: var(--dc-fs-lg);\n
	font-weight: 600;\n
	line-height: var(--dc-line-tight);\n
	color: var(--dc-text);\n
}\n
\n
.dc-dienst-team__group-lead,\n
.dc-dienst-team__hint {\n
	display: block;\n
	margin: 0;\n
	font-size: var(--dc-fs-sm);\n
	line-height: var(--dc-line-base);\n
	color: var(--dc-muted);\n
	font-weight: 400;\n
}\n
\n
.dc-dienst-team__primary {\n
	min-height: var(--dc-touch);\n
	padding: var(--dc-space-2) 0;\n
}\n
\n
.dc-dienst-team__primary + .dc-dienst-team__primary {\n
	margin-top: var(--dc-space-1);\n
}\n
\n
.dc-checkbox.dc-dienst-team__primary .dc-checkbox__text {\n
	display: flex;\n
	flex-direction: column;\n
	gap: 0.15rem;\n
}\n
\n
@media (max-width: 640px) {\n
	.dc-today__filters {\n
		grid-template-columns: 1fr;\n
	}\n
\n
	.dc-dienst-team__quiet-times {\n
		grid-template-columns: 1fr;\n
	}\n
\n
	.dc-patterns__item-actions {\n
		flex-wrap: wrap;\n
	}\n
\n
	.dc-patterns__item-actions .button {\n
		min-height: var(--dc-touch);\n
	}\n
}\n
\n
.dc-dienst-team__badge {\n
	margin-left: var(--dc-space-2);\n
	vertical-align: middle;\n
}\n
\n
.dc-dienst-team__inline-link {\n
	margin: 0;\n
}\n
\n
.dc-dienst-team__details {\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	background: var(--dc-bg-soft);\n
}\n
\n
.dc-dienst-team__summary {\n
	cursor: pointer;\n
	min-height: var(--dc-touch);\n
	display: flex;\n
	align-items: center;\n
	font-weight: 600;\n
	list-style: none;\n
}\n
\n
.dc-dienst-team__summary::-webkit-details-marker {\n
	display: none;\n
}\n
\n
.dc-dienst-team__summary:focus-visible {\n
	outline: 2px solid var(--color-primary-element);\n
	outline-offset: 2px;\n
}\n
\n
.dc-dienst-team__quiet-times {\n
	margin-top: var(--dc-space-2);\n
}\n
\n
.dc-dienst-team__more-block {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	margin-top: var(--dc-space-2);\n
}\n
\n
.dc-today__filters {\n
	margin-bottom: var(--dc-space-4);\n
	align-items: end;\n
}\n
\n
.dc-today__date-shortcuts {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
	margin-top: var(--dc-space-2);\n
}\n
\n
.dc-today__date-shortcuts .button {\n
	min-height: var(--dc-touch);\n
	min-width: 44px;\n
}\n
\n
@media (max-width: 640px) {\n
	.dc-today__date-shortcuts {\n
		flex-direction: column;\n
	}\n
\n
	.dc-today__date-shortcuts .button {\n
		width: 100%;\n
	}\n
}\n
\n
.dc-today__skeleton {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
	margin: var(--dc-space-4) 0;\n
}\n
\n
.dc-today__skeleton-row {\n
	height: 3.25rem;\n
	border-radius: var(--dc-radius-md);\n
	background: linear-gradient(\n
		90deg,\n
		var(--dc-bg-soft) 0%,\n
		var(--dc-bg-stripe) 50%,\n
		var(--dc-bg-soft) 100%\n
	);\n
	background-size: 200% 100%;\n
	animation: dc-today-shimmer 1.4s ease-in-out infinite;\n
}\n
\n
@keyframes dc-today-shimmer {\n
	0% { background-position: 100% 0; }\n
	100% { background-position: -100% 0; }\n
}\n
\n
@media (prefers-reduced-motion: reduce) {\n
	.dc-today__skeleton-row {\n
		animation: none;\n
		background: var(--dc-bg-stripe);\n
	}\n
}\n
\n
.dc-today__gaps {\n
	list-style: none;\n
	margin: 0 0 var(--dc-space-4);\n
	padding: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
}\n
\n
.dc-today__gap {\n
	display: flex;\n
	align-items: flex-start;\n
	gap: var(--dc-space-3);\n
	min-height: var(--dc-touch);\n
	padding: var(--dc-space-3);\n
	border-radius: var(--dc-radius-md);\n
	background: var(--dc-tint-warning);\n
	border: 1px solid var(--dc-border);\n
	color: var(--dc-text);\n
}\n
\n
.dc-today__gap-icon {\n
	flex-shrink: 0;\n
	width: 1.75rem;\n
	height: 1.75rem;\n
	border-radius: 50%;\n
	display: inline-flex;\n
	align-items: center;\n
	justify-content: center;\n
	font-weight: 700;\n
	background: var(--color-main-background);\n
	border: 2px solid var(--color-warning-text, var(--color-warning, #c98600));\n
	color: var(--color-warning-text, var(--color-warning, #c98600));\n
}\n
\n
.dc-today__timeline {\n
	list-style: none;\n
	margin: 0;\n
	padding: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
}\n
\n
.dc-today__shift {\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border-radius: var(--dc-radius-md);\n
	border: 1px solid var(--dc-border);\n
	background: var(--dc-bg-card);\n
	min-height: var(--dc-touch);\n
}\n
\n
.dc-today__shift--draft {\n
	border-style: dashed;\n
	background: var(--dc-bg-soft);\n
}\n
\n
.dc-today__shift-main {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-1);\n
}\n
\n
.dc-today__shift-name {\n
	font-size: var(--dc-fs-md);\n
	font-weight: 600;\n
}\n
\n
.dc-today__shift-meta {\n
	font-size: var(--dc-fs-sm);\n
	color: var(--dc-muted);\n
}\n
\n
.dc-today__empty {\n
	padding: var(--dc-space-5);\n
	text-align: center;\n
	color: var(--dc-muted);\n
}\n
\n
.dc-my-team__filters {\n
	margin-top: var(--dc-space-2, 0.5rem);\n
}\n
\n
.dc-my-team__filters .dc-today__date-shortcuts .button,\n
.dc-my-team__filters .dc-form-actions .button {\n
	min-height: 44px;\n
	min-width: 44px;\n
}\n
\n
.dc-my-team__list {\n
	list-style: none;\n
	margin: var(--dc-space-3, 0.75rem) 0 0;\n
	padding: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2, 0.65rem);\n
}\n
\n
.dc-my-team__row {\n
	display: grid;\n
	grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr) minmax(0, 0.8fr);\n
	gap: 0.35rem 0.75rem;\n
	align-items: baseline;\n
	padding: 0.75rem 0.9rem;\n
	border: 1px solid var(--color-border, var(--dc-border, #ddd));\n
	border-radius: var(--border-radius-large, 8px);\n
	background: var(--color-main-background);\n
}\n
\n
.dc-my-team__name {\n
	font-size: 1.05rem;\n
}\n
\n
.dc-my-team__when,\n
.dc-my-team__loc {\n
	color: var(--dc-muted, var(--color-text-maxcontrast));\n
	font-size: 0.95rem;\n
}\n
\n
@media (max-width: 640px) {\n
	.dc-my-team__row {\n
		grid-template-columns: 1fr;\n
	}\n
}\n
\n
.dc-dienst-team .dc-section__sub {\n
	margin: 0 0 0.75rem;\n
	max-width: 42rem;\n
	color: var(--dc-muted, var(--color-text-maxcontrast));\n
}\n
\n
.dc-patterns__list {\n
	list-style: none;\n
	margin: 0;\n
	padding: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-3);\n
}\n
\n
.dc-patterns__item {\n
	display: flex;\n
	flex-wrap: wrap;\n
	align-items: center;\n
	justify-content: space-between;\n
	gap: var(--dc-space-3);\n
	padding: var(--dc-space-4);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
	background: var(--dc-bg-card);\n
	min-height: var(--dc-touch);\n
}\n
\n
.dc-patterns__item-main {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-1);\n
	min-width: 0;\n
}\n
\n
.dc-patterns__item-meta {\n
	font-size: var(--dc-fs-sm);\n
	color: var(--dc-muted);\n
}\n
\n
.dc-patterns__item-actions {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: var(--dc-space-2);\n
}\n
\n
.dc-patterns__empty {\n
	padding: var(--dc-space-5);\n
	text-align: center;\n
	color: var(--dc-muted);\n
}\n
\n
.dc-patterns__editor {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-4);\n
}\n
\n
.dc-patterns__week {\n
	margin: 0;\n
	padding: var(--dc-space-3);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-md);\n
}\n
\n
.dc-patterns__grid {\n
	display: grid;\n
	grid-template-columns: repeat(auto-fit, minmax(11rem, 1fr));\n
	gap: var(--dc-space-3);\n
}\n
\n
.dc-patterns__day {\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
	padding: var(--dc-space-2);\n
	border-radius: var(--dc-radius-sm);\n
	background: var(--dc-bg-soft);\n
}\n
\n
.dc-patterns__day-head {\n
	font-weight: 600;\n
	font-size: var(--dc-fs-sm);\n
}\n
\n
.dc-patterns__work-toggle {\n
	width: 100%;\n
}\n
\n
.dc-coverage-strip {\n
	display: flex;\n
	flex-wrap: wrap;\n
	align-items: center;\n
	gap: var(--dc-space-2) var(--dc-space-3);\n
	margin: 0 0 var(--dc-space-3);\n
	padding: var(--dc-space-3) var(--dc-space-4);\n
	border-radius: var(--dc-radius-md);\n
	border: 1px solid var(--dc-border);\n
	background: var(--dc-bg-soft);\n
	min-height: var(--dc-touch);\n
}\n
\n
.dc-coverage-strip__label {\n
	font-weight: 600;\n
	font-size: var(--dc-fs-sm);\n
}\n
\n
.dc-coverage-strip__text {\n
	font-size: var(--dc-fs-sm);\n
	color: var(--dc-text);\n
}\n
\n
.dc-coverage-strip--ok {\n
	background: var(--dc-tint-success);\n
}\n
\n
.dc-coverage-strip--warn {\n
	background: var(--dc-tint-warning);\n
}\n
\n
.dc-coverage-strip--critical {\n
	background: var(--dc-tint-critical);\n
}\n
\n
.dc-pref-chip,\n
.dc-pref-chip-row {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-2);\n
}\n
\n
.dc-pref-chip-row {\n
	flex-wrap: wrap;\n
	margin-bottom: var(--dc-space-3);\n
}\n
\n
.dc-pref-chip {\n
	min-height: var(--dc-touch);\n
	min-width: var(--dc-touch);\n
	padding: 0 var(--dc-space-4);\n
	border-radius: var(--dc-radius-sm);\n
	border: 1px solid var(--dc-border-strong);\n
	background: var(--dc-bg-card);\n
	color: var(--dc-text);\n
	font-size: var(--dc-fs-sm);\n
	font-weight: 600;\n
	cursor: pointer;\n
}\n
\n
.dc-pref-chip:focus-visible {\n
	outline: 2px solid var(--color-primary-element);\n
	outline-offset: 2px;\n
}\n
\n
.dc-pref-chip--static {\n
	cursor: default;\n
	background: var(--dc-tint-info);\n
	pointer-events: none;\n
}\n
\n
.dc-blackout-mark {\n
	display: inline-flex;\n
	align-items: center;\n
	gap: var(--dc-space-1);\n
	font-size: var(--dc-fs-xs);\n
	font-weight: 600;\n
	color: var(--color-error-text, var(--color-error, #cc4646));\n
	line-height: 1.2;\n
}\n
\n
.dc-blackout-mark__icon {\n
	flex-shrink: 0;\n
}\n
\n
.dc-roster-grid__cell--blackout {\n
	background: var(--dc-tint-critical);\n
}\n
\n
.dc-roster-grid__pref-chips {\n
	display: flex;\n
	flex-wrap: wrap;\n
	gap: 2px;\n
	margin-top: 2px;\n
}\n
\n
.dc-roster-grid__rowhead-name {\n
	display: block;\n
	overflow: hidden;\n
	text-overflow: ellipsis;\n
	white-space: nowrap;\n
}\n
\n
.dc-my-availability__block {\n
	margin-bottom: var(--dc-space-5);\n
}\n
\n
.dc-my-availability__list {\n
	list-style: none;\n
	margin: var(--dc-space-3) 0 0;\n
	padding: 0;\n
	display: flex;\n
	flex-direction: column;\n
	gap: var(--dc-space-2);\n
}\n
\n
.dc-my-availability__row {\n
	display: flex;\n
	flex-wrap: wrap;\n
	align-items: center;\n
	justify-content: space-between;\n
	gap: var(--dc-space-2);\n
	min-height: var(--dc-touch);\n
	padding: var(--dc-space-2) var(--dc-space-3);\n
	border: 1px solid var(--dc-border);\n
	border-radius: var(--dc-radius-sm);\n
}\n
\n
.dc-suggest-preview__counts {\n
	margin: var(--dc-space-3) 0 0;\n
	padding-left: 1.25rem;\n
}\n
\n
.dc-roster-flash--error {\n
	color: var(--color-error-text, var(--color-error, #cc4646));\n
}\n
\n
@media (max-width: 768px) {\n
	.dc-patterns__item {\n
		flex-direction: column;\n
		align-items: stretch;\n
	}\n
\n
	.dc-patterns__item-actions .button {\n
		width: 100%;\n
		justify-content: center;\n
	}\n
}\n
/* === roster-self-service-ga:dc end === */\n
' does not match PCRE pattern "/\.dc-roster-grid__row \{[\s\S]{0,200}repeat\(auto-fit/s".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:119

FAILURES!
Tests: 6, Assertions: 71, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.837285
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.21909
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.279207
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.230157
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.188641
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.170881
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.167365
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.259246
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.310376
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 5.411598
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.256623
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.137375
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.098424
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.103733
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 1.87725
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.175048
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.118216
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.39269
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.227039
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.103806
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.096749
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.551529
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.194222
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 1.040943
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.239826
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.303814
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    not ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 3.296256
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:287:2'
      failureType: 'testCodeFailure'
      error: |-
        The input was expected to not match the regular expression /\.dc-roster-grid__row \{[\s\S]{0,200}repeat\(auto-fit/. Input:
        
        '/*\n' +
          ' * DutyCheck design system.\n' +
          ' *\n' +
          ' * Authored to mirror the BudgetCheck visual language while keeping a\n' +
          ' * distinct `dc-*` namespace. Colors come from Nextcloud variables\n' +
          ' * (--color-*, --color-main-*, --color-primary-element, --color-success,\n' +
          ' * --color-warning, --color-error). App-specific tokens are prefixed\n' +
          ' * `--dc-*` and only define spacing, radii, typography scale, shadows, and\n' +
          ' * tinted surfaces derived via `color-mix()` so the app matches every NC\n' +
          ' * theme without hard-coded hex values.\n' +
          ' *\n' +
          ' * Accessibility (WCAG 2.1 AA):\n' +
          ' *  - Visible focus rings on every focusable element.\n' +
          ' *  - 4.5:1 text contrast on text, surfaces tinted not coloured-out.\n' +
          ' *  - 44 × 44 px minimum touch targets on interactive controls.\n' +
          ' *  - Reflowing grids; no information conveyed by colour alone (status\n' +
          ' *    badges always carry a label and shape cue).\n' +
          ' *  - Mobile-first layout: base styles target <768px; min-width 480px,\n' +
          ' *    768px (Nextcloud tablet), 520px/900px (quickstart) build upward.\n' +
          ' *  - prefers-reduced-motion honoured.\n' +
          ' *\n' +
          ' * Buttons and button-like controls (`<button>`, `.button`,\n' +
          ' * `input[type=submit]`) use inline-flex with `min-height: 44px` so labels\n' +
          ' * and icons stay vertically and horizontally centred on every browser.\n' +
          ' */\n' +
          '\n' +
          '/* ── Tokens ──────────────────────────────────────────────────── */\n' +
          ':root {\n' +
          '\t/* Spacing scale (4 px grid — never invent 10/18/20 px) */\n' +
          '\t--dc-space-1: 4px;\n' +
          '\t--dc-space-2: 8px;\n' +
          '\t--dc-space-3: 12px;\n' +
          '\t--dc-space-4: 16px;\n' +
          '\t--dc-space-5: 24px;\n' +
          '\t--dc-space-6: 32px;\n' +
          '\t--dc-space-7: 48px;\n' +
          '\t--dc-space-8: 64px;\n' +
          '\n' +
          '\t/* Radii */\n' +
          '\t--dc-radius-sm: 6px;\n' +
          '\t--dc-radius-md: 12px;\n' +
          '\t--dc-radius-lg: 16px;\n' +
          '\n' +
          '\t/* Type scale */\n' +
          '\t--dc-fs-xs: 0.75rem;\n' +
          '\t--dc-fs-sm: 0.875rem;\n' +
          '\t--dc-fs-md: 1rem;\n' +
          '\t--dc-fs-lg: 1.125rem;\n' +
          '\t--dc-fs-xl: 1.5rem;\n' +
          '\t--dc-fs-2xl: 1.875rem;\n' +
          '\t--dc-line-tight: 1.2;\n' +
          '\t--dc-line-base: 1.45;\n' +
          '\n' +
          '\t--dc-breakpoint-sm: 480px;\n' +
          '\t--dc-breakpoint-md: 768px;\n' +
          '\t--dc-breakpoint-lg: 1024px;\n' +
          '\n' +
          '\t--dc-icon-sm: 1rem;\n' +
          '\t--dc-icon-md: 1.125rem;\n' +
          '\t--dc-icon-lg: 1.375rem;\n' +
          '\t--dc-icon-well: 2.25rem;\n' +
          '\t--dc-touch: 44px;\n' +
          '\n' +
          '\t--dc-tab-num: tabular-nums;\n' +
          '}\n' +
          '\n' +
          '/*\n' +
          ' * Theme-dependent tokens MUST live on themed surfaces (body / .dc-app / .dc-modal),\n' +
          ' * not :root. Nextcloud sets --color-main-* on body; resolving color-mix on :root\n' +
          ' * falls back to light #fff while text inherits dark-theme --color-main-text\n' +
          ' * → WCAG contrast ~1.1:1. Modals mount on document.body outside #app-content.\n' +
          ' *\n' +
          ' * Tints mix INTO --color-main-background (AZC contract) — never into transparent —\n' +
          ' * so callouts stay visible on every NC theme including high-contrast.\n' +
          ' */\n' +
          'body,\n' +
          '#app-content.dc-app,\n' +
          '.dc-app,\n' +
          '.dc-modal,\n' +
          '.dc-toasts {\n' +
          '\t--dc-overlay-top: var(--header-height, 50px);\n' +
          '\t--dc-overlay-height: calc(100dvh - var(--header-height, 50px));\n' +
          '\n' +
          '\t/* Shadows */\n' +
          '\t--dc-shadow-sm: 0 1px 2px color-mix(in srgb, var(--color-main-text, #1d1d1d) 8%, transparent);\n' +
          '\t--dc-shadow-md: 0 8px 24px color-mix(in srgb, var(--color-main-text, #1d1d1d) 12%, transparent);\n' +
          '\n' +
          '\t/* Surfaces */\n' +
          '\t--dc-bg-soft: color-mix(in srgb, var(--color-main-background, #fff) 96%, var(--color-main-text, #1d1d1d) 4%);\n' +
          '\t--dc-bg-card: var(--color-main-background, #fff);\n' +
          '\t--dc-bg-stripe: color-mix(in srgb, var(--color-main-background, #fff) 92%, var(--color-main-text, #1d1d1d) 8%);\n' +
          '\t--dc-border: color-mix(in srgb, var(--color-main-text, #1d1d1d) 12%, transparent);\n' +
          '\t--dc-border-strong: color-mix(in srgb, var(--color-main-text, #1d1d1d) 24%, transparent);\n' +
          "\t/* Prefer Nextcloud's AA-tested muted token (same approach as BudgetCheck).\n" +
          '\t   Fallback mixes against the background (not transparent) so secondary text\n' +
          '\t   still clears 4.5:1 on tinted surfaces across custom themes. */\n' +
          '\t--dc-muted: var(\n' +
          '\t\t--color-text-maxcontrast,\n' +
          '\t\tcolor-mix(in srgb, var(--color-main-text, #1d1d1d) 67%, var(--color-main-background, #fff))\n' +
          '\t);\n' +
          '\t--dc-text: var(--color-main-text, #1d1d1d);\n' +
          '\n' +
          '\t/* Severity tints — mix into main-background (AZC / design-system §1.5) */\n' +
          '\t--dc-tint-info: color-mix(in srgb, var(--color-primary-element, #0082c9) 16%, var(--color-main-background, #fff));\n' +
          '\t--dc-tint-success: color-mix(in srgb, var(--color-success-text, var(--color-success, #46ba61)) 18%, var(--color-main-background, #fff));\n' +
          '\t/* NC34+ --color-success is a pale *surface*; borders/icons need the ink / element token. */\n' +
          '\t--dc-tint-success-border: color-mix(in srgb, var(--color-element-success, var(--color-success-text, #005416)) 55%, var(--color-main-background, #fff));\n' +
          '\t--dc-tint-warning: color-mix(in srgb, var(--color-warning-text, var(--color-warning, #c98600)) 22%, var(--color-main-background, #fff));\n' +
          '\t--dc-tint-critical: color-mix(in srgb, var(--color-error-text, var(--color-error, #cc4646)) 18%, var(--color-main-background, #fff));\n' +
          '\t--dc-scrim: color-mix(in srgb, var(--color-main-text, #000) 60%, transparent);\n' +
          '\n' +
          '\tcolor: var(--color-main-text, #1d1d1d);\n' +
          '}\n' +
          '\n' +
          '/* ── Skip link / live regions ────────────────────────────────── */\n' +
          '.dc-skip-link {\n' +
          '\tposition: absolute;\n' +
          '\tleft: -9999px;\n' +
          '\ttop: var(--dc-space-2);\n' +
          '\tpadding: var(--dc-space-2) var(--dc-space-4);\n' +
          '\tbackground: var(--color-primary-element, #0082c9);\n' +
          '\tcolor: var(--color-primary-element-text, #fff);\n' +
          '\tborder-radius: var(--dc-radius-sm);\n' +
          '\tz-index: 1000;\n' +
          '\tfont-weight: 600;\n' +
          '\ttext-decoration: none;\n' +
          '}\n' +
          '.dc-skip-link:focus,\n' +
          '.dc-skip-link:focus-visible {\n' +
          '\tleft: var(--dc-space-3);\n' +
          '\toutline: 3px solid var(--color-primary-element-text, #fff);\n' +
          '\toutline-offset: 2px;\n' +
          '}\n' +
          '.dc-sr-only {\n' +
          '\tposition: absolute !important;\n' +
          '\twidth: 1px;\n' +
          '\theight: 1px;\n' +
          '\tmargin: -1px;\n' +
          '\tpadding: 0;\n' +
          '\tborder: 0;\n' +
          '\toverflow: hidden;\n' +
          '\tclip: rect(0 0 0 0);\n' +
          '\twhite-space: nowrap;\n' +
          '}\n' +
          '\n' +
          '/* ── App shell ───────────────────────────────────────────────── */\n' +
          '#app-content.dc-app,\n' +
          '.dc-app {\n' +
          '\tdisplay: flex;\n' +
          '\tflex-direction: column;\n' +
          '\tmin-height: 100%;\n' +
          '\tbackground: var(--dc-bg-soft);\n' +
          '\toverflow-x: clip;\n' +
          '}\n' +
          '\n' +
          '/*\n' +
          ' * Nextcloud core sets `#app-content #app-content-wrapper` to a flex row\n' +
          ' * stretching its children. DutyCheck stacks header + main, so we must\n' +
          ' * force column direction here, otherwise the breadcrumb sits next to the\n' +
          ' * page body.\n' +
          ' */\n' +
          '#app-content.dc-app #app-content-wrapper.dc-shell,\n' +
          '.dc-shell {\n' +
          '\tdisplay: flex;\n' +
          '\tflex-direction: column;\n' +
          '\talign-items: stretch;\n' +
          '\twidth: 100%;\n' +
          '\tmax-width: none;\n' +
          '\tmargin: 0;\n' +
          '\tpadding: max(var(--dc-space-4), env(safe-area-inset-top, 0px))\n' +
          '\t\tmax(var(--dc-space-4), env(safe-area-inset-right, 0px))\n' +
          '\t\tmax(var(--dc-space-5), env(safe-area-inset-bottom, 0px))\n' +
          '\t\tmax(var(--dc-space-4), env(safe-area-inset-left, 0px));\n' +
          '\tbox-sizing: border-box;\n' +
          '\toverflow-x: clip;\n' +
          '}\n' +
          '/* Shell width modifiers (AZC parity — reading measure stays on prose, not the page) */\n' +
          '#app-content.dc-app #app-content-wrapper.dc-shell--wide,\n' +
          '.dc-shell--wide {\n' +
          '\tmax-width: none;\n' +
          '\tmargin-inline: 0;\n' +
          '}\n' +
          '#app-content.dc-app #app-content-wrapper.dc-shell--constrained,\n' +
          '.dc-shell--constrained {\n' +
          '\tmax-width: 72rem;\n' +
          '\tmargin-inline: auto;\n' +
          '}\n' +
          '#app-content.dc-app #app-content-wrapper.dc-shell--minimal,\n' +
          '.dc-shell--minimal {\n' +
          '\tmax-width: 40rem;\n' +
          '\tmargin-inline: auto;\n' +
          '}\n' +
          '#app-content.dc-app #dc-main-content.dc-main,\n' +
          '.dc-main {\n' +
          '\tflex: 1 1 auto;\n' +
          '\tmin-width: 0;\n' +
          '\tmax-width: 100%;\n' +
          '\tdisplay: flex;\n' +
          '\tflex-direction: column;\n' +
          '\tgap: var(--dc-space-6);\n' +
          '\toutline: none;\n' +
          '\toverflow-x: clip;\n' +
          '}\n' +
          '.dc-page-stack {\n' +
          '\tdisplay: flex;\n' +
          '\tflex-direction: column;\n' +
          '\talign-items: stretch;\n' +
          '\tgap: var(--dc-space-5);\n' +
          '\twidth: 100%;\n' +
          '\tmax-width: none;\n' +
          '\tmin-width: 0;\n' +
          '}\n' +
          '/* Anchor jumps clear the Nextcloud header + page chrome. */\n' +
          '#app-content.dc-app .dc-card[id],\n' +
          '.dc-card[id] {\n' +
          '\tscroll-margin-top: 5.5rem;\n' +
          '}\n' +
          '#dc-main-content.dc-main:focus-visible {\n' +
          '\toutline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);\n' +
          '\toutline-offset: 2px;\n' +
          '}\n' +
          '\n' +
          '/* Hide-when-hidden override (avoids form fields inside .dc-field stomping `[hidden]`) */\n' +
          '.dc-app [hidden],\n' +
          '.dc-modal [hidden] {\n' +
          '\tdisplay: none !important;\n' +
          '}\n' +
          '\n' +
          '/* ── Header / breadcrumb ─────────────────────────────────────── */\n' +
          '#app-content.dc-app .dc-page-header,\n' +
          '.dc-page-header {\n' +
          '\tdisplay: flex;\n' +
          '\tflex-direction: column;\n' +
          '\tgap: var(--dc-space-3);\n' +
          '\tmargin-bottom: var(--dc-space-5);\n' +
          '}\n' +
          '.dc-breadcrumb {\n' +
          '\tfont-size: var(--dc-fs-sm);\n' +
          '\tcolor: var(--dc-muted);\n' +
          '}\n' +
          '.dc-breadcrumb ol {\n' +
          '\tdisplay: flex;\n' +
          '\tflex-wrap: wrap;\n' +
          '\talign-items: center;\n' +
          '\tgap: var(--dc-space-2);\n' +
          '\tpadding: 0;\n' +
          '\tmargin: 0;\n' +
          '\tlist-style: none;\n' +
          '}\n' +
          '.dc-breadcrumb__brand {\n' +
          '\tcolor: inherit;\n' +
          '\tfont-weight: 600;\n' +
          '\ttext-decoration: none;\n' +
          '}\n' +
          '.dc-breadcrumb__brand:hover { text-decoration: underline; }\n' +
          '.dc-breadcrumb__parent {\n' +
          '\tcolor: inherit;\n' +
          '\ttext-decoration: none;\n' +
          '\toverflow-wrap: anywhere;\n' +
          '\tmin-width: 0;\n' +
          '}\n' +
          '.dc-breadcrumb__parent:hover { text-decoration: underline; }\n' +
          '.dc-breadcrumb__current {\n' +
          '\tfont-weight: 500;\n' +
          '\tcolor: var(--color-main-text, #1d1d1d);\n' +
          '\t/* Long translated titles must wrap, not push the shell into overflow clip. */\n' +
          '\toverflow-wrap: anywhere;\n' +
          '\tmin-width: 0;\n' +
          '}\n' +
          '.dc-breadcrumb__sep { color: var(--dc-muted); }\n' +
          '\n' +
          '.dc-page-header__main {\n' +
          '\tdisplay: grid;\n' +
          '\tgrid-template-columns: 48px 1fr;\n' +
          '\tgap: var(--dc-space-4);\n' +
          '\talign-items: start;\n' +
          '}\n' +
          '.dc-page-header__icon {\n' +
          '\tdisplay: grid;\n' +
          '\tplace-items: center;\n' +
          '\twidth: 48px;\n' +
          '\theight: 48px;\n' +
          '\tborder-radius: var(--dc-radius-md);\n' +
          '\tbackground: var(--dc-tint-info);\n' +
          '\tcolor: var(--color-primary-element, #0082c9);\n' +
          '}\n' +
          '.dc-page-header__icon-svg {\n' +
          '\twidth: 24px;\n' +
          '\theight: 24px;\n' +
          '\tmin-width: 24px;\n' +
          '\tmin-height: 24px;\n' +
          '}\n' +
          '.dc-page-header__actions {\n' +
          '\tgrid-column: 1 / -1;\n' +
          '}\n' +
          '/*\n' +
          ' * The title column is a `1fr` grid track; without `min-width: 0` its\n' +
          " * min-content size wins and the shell's `overflow-x: clip` cuts the h1\n" +
          ' * mid-glyph on very narrow viewports (split panes, <340px). `anywhere`\n' +
          ' * also lets long single-word titles participate in min-content shrinking.\n' +
          ' */\n' +
          '.dc-page-header__text {\n' +
          '\tmin-width: 0;\n' +
          '}\n' +
          '.dc-page-header__text h1 {\n' +
          '\tmargin: 0;\n' +
          '\tfont-size: var(--dc-fs-2xl);\n' +
          '\tline-height: var(--dc-line-tight);\n' +
          '\toverflow-wrap: anywhere;\n' +
          '}\n' +
          '.dc-page-header__lead {\n' +
          '\tmargin: var(--dc-space-1) 0 0 0;\n' +
          '\tcolor: var(--dc-muted);\n' +
          '\tfont-size: var(--dc-fs-sm);\n' +
          '\tline-height: var(--dc-line-base);\n' +
          '\tmax-width: 60ch;\n' +
          '\toverflow-wrap: anywhere;\n' +
          '}\n' +
          '.dc-page-header__actions {\n' +
          '\tdisplay: flex;\n' +
          '\tgap: var(--dc-space-2);\n' +
          '\tflex-wrap: wrap;\n' +
          '\tmin-height: 36px;\n' +
          '\talign-self: center;\n' +
          '}'... 116031 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        /*
         * DutyCheck design system.
         *
         * Authored to mirror the BudgetCheck visual language while keeping a
         * distinct `dc-*` namespace. Colors come from Nextcloud variables
         * (--color-*, --color-main-*, --color-primary-element, --color-success,
         * --color-warning, --color-error). App-specific tokens are prefixed
         * `--dc-*` and only define spacing, radii, typography scale, shadows, and
         * tinted surfaces derived via `color-mix()` so the app matches every NC
         * theme without hard-coded hex values.
         *
         * Accessibility (WCAG 2.1 AA):
         *  - Visible focus rings on every focusable element.
         *  - 4.5:1 text contrast on text, surfaces tinted not coloured-out.
         *  - 44 × 44 px minimum touch targets on interactive controls.
         *  - Reflowing grids; no information conveyed by colour alone (status
         *    badges always carry a label and shape cue).
         *  - Mobile-first layout: base styles target <768px; min-width 480px,
         *    768px (Nextcloud tablet), 520px/900px (quickstart) build upward.
         *  - prefers-reduced-motion honoured.
         *
         * Buttons and button-like controls (`<button>`, `.button`,
         * `input[type=submit]`) use inline-flex with `min-height: 44px` so labels
         * and icons stay vertically and horizontally centred on every browser.
         */
        
        /* ── Tokens ──────────────────────────────────────────────────── */
        :root {
        	/* Spacing scale (4 px grid — never invent 10/18/20 px) */
        	--dc-space-1: 4px;
        	--dc-space-2: 8px;
        	--dc-space-3: 12px;
        	--dc-space-4: 16px;
        	--dc-space-5: 24px;
        	--dc-space-6: 32px;
        	--dc-space-7: 48px;
        	--dc-space-8: 64px;
        
        	/* Radii */
        	--dc-radius-sm: 6px;
        	--dc-radius-md: 12px;
        	--dc-radius-lg: 16px;
        
        	/* Type scale */
        	--dc-fs-xs: 0.75rem;
        	--dc-fs-sm: 0.875rem;
        	--dc-fs-md: 1rem;
        	--dc-fs-lg: 1.125rem;
        	--dc-fs-xl: 1.5rem;
        	--dc-fs-2xl: 1.875rem;
        	--dc-line-tight: 1.2;
        	--dc-line-base: 1.45;
        
        	--dc-breakpoint-sm: 480px;
        	--dc-breakpoint-md: 768px;
        	--dc-breakpoint-lg: 1024px;
        
        	--dc-icon-sm: 1rem;
        	--dc-icon-md: 1.125rem;
        	--dc-icon-lg: 1.375rem;
        	--dc-icon-well: 2.25rem;
        	--dc-touch: 44px;
        
        	--dc-tab-num: tabular-nums;
        }
        
        /*
         * Theme-dependent tokens MUST live on themed surfaces (body / .dc-app / .dc-modal),
         * not :root. Nextcloud sets --color-main-* on body; resolving color-mix on :root
         * falls back to light #fff while text inherits dark-theme --color-main-text
         * → WCAG contrast ~1.1:1. Modals mount on document.body outside #app-content.
         *
         * Tints mix INTO --color-main-background (AZC contract) — never into transparent —
         * so callouts stay visible on every NC theme including high-contrast.
         */
        body,
        #app-content.dc-app,
        .dc-app,
        .dc-modal,
        .dc-toasts {
        	--dc-overlay-top: var(--header-height, 50px);
        	--dc-overlay-height: calc(100dvh - var(--header-height, 50px));
        
        	/* Shadows */
        	--dc-shadow-sm: 0 1px 2px color-mix(in srgb, var(--color-main-text, #1d1d1d) 8%, transparent);
        	--dc-shadow-md: 0 8px 24px color-mix(in srgb, var(--color-main-text, #1d1d1d) 12%, transparent);
        
        	/* Surfaces */
        	--dc-bg-soft: color-mix(in srgb, var(--color-main-background, #fff) 96%, var(--color-main-text, #1d1d1d) 4%);
        	--dc-bg-card: var(--color-main-background, #fff);
        	--dc-bg-stripe: color-mix(in srgb, var(--color-main-background, #fff) 92%, var(--color-main-text, #1d1d1d) 8%);
        	--dc-border: color-mix(in srgb, var(--color-main-text, #1d1d1d) 12%, transparent);
        	--dc-border-strong: color-mix(in srgb, var(--color-main-text, #1d1d1d) 24%, transparent);
        	/* Prefer Nextcloud's AA-tested muted token (same approach as BudgetCheck).
        	   Fallback mixes against the background (not transparent) so secondary text
        	   still clears 4.5:1 on tinted surfaces across custom themes. */
        	--dc-muted: var(
        		--color-text-maxcontrast,
        		color-mix(in srgb, var(--color-main-text, #1d1d1d) 67%, var(--color-main-background, #fff))
        	);
        	--dc-text: var(--color-main-text, #1d1d1d);
        
        	/* Severity tints — mix into main-background (AZC / design-system §1.5) */
        	--dc-tint-info: color-mix(in srgb, var(--color-primary-element, #0082c9) 16%, var(--color-main-background, #fff));
        	--dc-tint-success: color-mix(in srgb, var(--color-success-text, var(--color-success, #46ba61)) 18%, var(--color-main-background, #fff));
        	/* NC34+ --color-success is a pale *surface*; borders/icons need the ink / element token. */
        	--dc-tint-success-border: color-mix(in srgb, var(--color-element-success, var(--color-success-text, #005416)) 55%, var(--color-main-background, #fff));
        	--dc-tint-warning: color-mix(in srgb, var(--color-warning-text, var(--color-warning, #c98600)) 22%, var(--color-main-background, #fff));
        	--dc-tint-critical: color-mix(in srgb, var(--color-error-text, var(--color-error, #cc4646)) 18%, var(--color-main-background, #fff));
        	--dc-scrim: color-mix(in srgb, var(--color-main-text, #000) 60%, transparent);
        
        	color: var(--color-main-text, #1d1d1d);
        }
        
        /* ── Skip link / live regions ────────────────────────────────── */
        .dc-skip-link {
        	position: absolute;
        	left: -9999px;
        	top: var(--dc-space-2);
        	padding: var(--dc-space-2) var(--dc-space-4);
        	background: var(--color-primary-element, #0082c9);
        	color: var(--color-primary-element-text, #fff);
        	border-radius: var(--dc-radius-sm);
        	z-index: 1000;
        	font-weight: 600;
        	text-decoration: none;
        }
        .dc-skip-link:focus,
        .dc-skip-link:focus-visible {
        	left: var(--dc-space-3);
        	outline: 3px solid var(--color-primary-element-text, #fff);
        	outline-offset: 2px;
        }
        .dc-sr-only {
        	position: absolute !important;
        	width: 1px;
        	height: 1px;
        	margin: -1px;
        	padding: 0;
        	border: 0;
        	overflow: hidden;
        	clip: rect(0 0 0 0);
        	white-space: nowrap;
        }
        
        /* ── App shell ───────────────────────────────────────────────── */
        #app-content.dc-app,
        .dc-app {
        	display: flex;
        	flex-direction: column;
        	min-height: 100%;
        	background: var(--dc-bg-soft);
        	overflow-x: clip;
        }
        
        /*
         * Nextcloud core sets `#app-content #app-content-wrapper` to a flex row
         * stretching its children. DutyCheck stacks header + main, so we must
         * force column direction here, otherwise the breadcrumb sits next to the
         * page body.
         */
        #app-content.dc-app #app-content-wrapper.dc-shell,
        .dc-shell {
        	display: flex;
        	flex-direction: column;
        	align-items: stretch;
        	width: 100%;
        	max-width: none;
        	margin: 0;
        	padding: max(var(--dc-space-4), env(safe-area-inset-top, 0px))
        		max(var(--dc-space-4), env(safe-area-inset-right, 0px))
        		max(var(--dc-space-5), env(safe-area-inset-bottom, 0px))
        		max(var(--dc-space-4), env(safe-area-inset-left, 0px));
        	box-sizing: border-box;
        	overflow-x: clip;
        }
        /* Shell width modifiers (AZC parity — reading measure stays on prose, not the page) */
        #app-content.dc-app #app-content-wrapper.dc-shell--wide,
        .dc-shell--wide {
        	max-width: none;
        	margin-inline: 0;
        }
        #app-content.dc-app #app-content-wrapper.dc-shell--constrained,
        .dc-shell--constrained {
        	max-width: 72rem;
        	margin-inline: auto;
        }
        #app-content.dc-app #app-content-wrapper.dc-shell--minimal,
        .dc-shell--minimal {
        	max-width: 40rem;
        	margin-inline: auto;
        }
        #app-content.dc-app #dc-main-content.dc-main,
        .dc-main {
        	flex: 1 1 auto;
        	min-width: 0;
        	max-width: 100%;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-6);
        	outline: none;
        	overflow-x: clip;
        }
        .dc-page-stack {
        	display: flex;
        	flex-direction: column;
        	align-items: stretch;
        	gap: var(--dc-space-5);
        	width: 100%;
        	max-width: none;
        	min-width: 0;
        }
        /* Anchor jumps clear the Nextcloud header + page chrome. */
        #app-content.dc-app .dc-card[id],
        .dc-card[id] {
        	scroll-margin-top: 5.5rem;
        }
        #dc-main-content.dc-main:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 2px;
        }
        
        /* Hide-when-hidden override (avoids form fields inside .dc-field stomping `[hidden]`) */
        .dc-app [hidden],
        .dc-modal [hidden] {
        	display: none !important;
        }
        
        /* ── Header / breadcrumb ─────────────────────────────────────── */
        #app-content.dc-app .dc-page-header,
        .dc-page-header {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	margin-bottom: var(--dc-space-5);
        }
        .dc-breadcrumb {
        	font-size: var(--dc-fs-sm);
        	color: var(--dc-muted);
        }
        .dc-breadcrumb ol {
        	display: flex;
        	flex-wrap: wrap;
        	align-items: center;
        	gap: var(--dc-space-2);
        	padding: 0;
        	margin: 0;
        	list-style: none;
        }
        .dc-breadcrumb__brand {
        	color: inherit;
        	font-weight: 600;
        	text-decoration: none;
        }
        .dc-breadcrumb__brand:hover { text-decoration: underline; }
        .dc-breadcrumb__parent {
        	color: inherit;
        	text-decoration: none;
        	overflow-wrap: anywhere;
        	min-width: 0;
        }
        .dc-breadcrumb__parent:hover { text-decoration: underline; }
        .dc-breadcrumb__current {
        	font-weight: 500;
        	color: var(--color-main-text, #1d1d1d);
        	/* Long translated titles must wrap, not push the shell into overflow clip. */
        	overflow-wrap: anywhere;
        	min-width: 0;
        }
        .dc-breadcrumb__sep { color: var(--dc-muted); }
        
        .dc-page-header__main {
        	display: grid;
        	grid-template-columns: 48px 1fr;
        	gap: var(--dc-space-4);
        	align-items: start;
        }
        .dc-page-header__icon {
        	display: grid;
        	place-items: center;
        	width: 48px;
        	height: 48px;
        	border-radius: var(--dc-radius-md);
        	background: var(--dc-tint-info);
        	color: var(--color-primary-element, #0082c9);
        }
        .dc-page-header__icon-svg {
        	width: 24px;
        	height: 24px;
        	min-width: 24px;
        	min-height: 24px;
        }
        .dc-page-header__actions {
        	grid-column: 1 / -1;
        }
        /*
         * The title column is a `1fr` grid track; without `min-width: 0` its
         * min-content size wins and the shell's `overflow-x: clip` cuts the h1
         * mid-glyph on very narrow viewports (split panes, <340px). `anywhere`
         * also lets long single-word titles participate in min-content shrinking.
         */
        .dc-page-header__text {
        	min-width: 0;
        }
        .dc-page-header__text h1 {
        	margin: 0;
        	font-size: var(--dc-fs-2xl);
        	line-height: var(--dc-line-tight);
        	overflow-wrap: anywhere;
        }
        .dc-page-header__lead {
        	margin: var(--dc-space-1) 0 0 0;
        	color: var(--dc-muted);
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        	max-width: 60ch;
        	overflow-wrap: anywhere;
        }
        .dc-page-header__actions {
        	display: flex;
        	gap: var(--dc-space-2);
        	flex-wrap: wrap;
        	min-height: 36px;
        	align-self: center;
        }
        
        /* ── Role / scope strip ──────────────────────────────────────── */
        /*
         * Neutralise Nextcloud core apps.css definition-list chrome:
         *   dl { padding: 12px 0 }
         *   dt, dd { display: inline-block; padding: 12px; padding-inline-start: 0 }
         *   dt { width: 130px; white-space: nowrap; text-align: end }
         * Without these resets, "Role" sits end-aligned in a 130px box and looks like
         * a missing icon / left gap. Use logical start alignment for RTL.
         */
        #app-content.dc-app .dc-scope-strip,
        .dc-app .dc-scope-strip {
        	display: grid;
        	grid-template-columns: 1fr;
        	gap: var(--dc-space-3);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	background: var(--dc-bg-card);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	font-size: var(--dc-fs-sm);
        	margin: 0;
        }
        #app-content.dc-app .dc-scope-strip__item,
        .dc-app .dc-scope-strip__item {
        	display: grid;
        	grid-template-columns: minmax(5.5rem, auto) minmax(0, 1fr);
        	gap: var(--dc-space-2) var(--dc-space-3);
        	align-items: center;
        	justify-items: stretch;
        	min-width: 0;
        }
        #app-content.dc-app .dc-scope-strip dt,
        #app-content.dc-app .dc-scope-strip dd,
        #app-content.dc-app .dc-scope-strip__label,
        #app-content.dc-app .dc-scope-strip__value,
        .dc-app .dc-scope-strip dt,
        .dc-app .dc-scope-strip dd,
        .dc-app .dc-scope-strip__label,
        .dc-app .dc-scope-strip__value {
        	display: block;
        	float: none;
        	width: auto;
        	max-width: 100%;
        	padding: 0;
        	margin: 0;
        	text-align: start;
        	white-space: normal;
        }
        #app-content.dc-app .dc-scope-strip__label,
        .dc-app .dc-scope-strip__label {
        	color: var(--dc-muted);
        	font-weight: 600;
        	text-transform: uppercase;
        	letter-spacing: 0.05em;
        	font-size: var(--dc-fs-xs);
        	line-height: var(--dc-line-tight);
        }
        #app-content.dc-app .dc-scope-strip__value,
        .dc-app .dc-scope-strip__value {
        	font-weight: 600;
        	color: var(--color-main-text, #1d1d1d);
        	min-width: 0;
        	overflow-wrap: anywhere;
        }
        @media (min-width: 640px) {
        	#app-content.dc-app .dc-scope-strip,
        	.dc-app .dc-scope-strip {
        		grid-template-columns: repeat(3, minmax(0, 1fr));
        		gap: var(--dc-space-4);
        	}
        	#app-content.dc-app .dc-scope-strip__item,
        	.dc-app .dc-scope-strip__item {
        		grid-template-columns: 1fr;
        		gap: var(--dc-space-1);
        		padding-inline-start: var(--dc-space-4);
        		border-inline-start: 1px solid var(--dc-border);
        	}
        	#app-content.dc-app .dc-scope-strip__item:first-child,
        	.dc-app .dc-scope-strip__item:first-child {
        		padding-inline-start: 0;
        		border-inline-start: 0;
        	}
        }
        
        /* ── Cards and sections ──────────────────────────────────────── */
        .dc-card {
        	background: var(--dc-bg-card);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	box-shadow: var(--dc-shadow-sm);
        	padding: var(--dc-space-4);
        	min-width: 0;
        	max-width: 100%;
        	box-sizing: border-box;
        }
        .dc-section {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-4);
        	min-width: 0;
        	max-width: 100%;
        }
        .dc-section__header {
        	display: grid;
        	grid-template-columns: 1fr;
        	align-items: start;
        	gap: var(--dc-space-3) var(--dc-space-4);
        }
        /* Same min-content escape hatch as the page header (see .dc-page-header__text). */
        .dc-section__header > div {
        	min-width: 0;
        }
        .dc-section__controls {
        	justify-self: stretch;
        }
        .dc-section__header > .dc-hint-dismiss {
        	justify-self: stretch;
        	text-align: center;
        }
        .dc-section__header h2 {
        	margin: 0;
        	font-size: var(--dc-fs-xl);
        	line-height: var(--dc-line-tight);
        	overflow-wrap: anywhere;
        }
        .dc-section__sub {
        	margin: var(--dc-space-1) 0 0;
        	color: var(--dc-muted);
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        }
        .dc-section__controls {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	align-items: center;
        	min-width: 0;
        }
        
        /* ── Badges / pills / chips ──────────────────────────────────── */
        .dc-badge {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-1);
        	padding: var(--dc-space-1) var(--dc-space-3);
        	font-size: var(--dc-fs-xs);
        	font-weight: 600;
        	border-radius: 999px;
        	letter-spacing: 0.04em;
        	text-transform: uppercase;
        	background: var(--dc-bg-stripe);
        	color: var(--color-main-text, #1d1d1d);
        	border: 1px solid var(--dc-border);
        	white-space: normal;
        	overflow-wrap: anywhere;
        	max-width: 100%;
        }
        .dc-badge--info { background: var(--dc-tint-info); border-color: transparent; }
        .dc-badge--success { background: var(--dc-tint-success); border-color: transparent; }
        .dc-badge--neutral {
        	background: color-mix(in srgb, var(--color-main-text, #1d1d1d) 8%, var(--color-main-background, #fff) 92%);
        	border-color: var(--dc-border-strong);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-badge--warning { background: var(--dc-tint-warning); border-color: transparent; }
        .dc-badge--critical { background: var(--dc-tint-critical); border-color: transparent; }
        
        /* Status badges with icon hint (open/published/closed, conflict severity, absence) */
        .dc-status-badge {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-1);
        	min-height: 1.75rem;
        	padding: 2px 10px;
        	border-radius: 999px;
        	background: var(--dc-bg-stripe);
        	color: var(--color-main-text, #1d1d1d);
        	border: 1px solid var(--dc-border);
        	font-size: var(--dc-fs-xs);
        	font-weight: 700;
        	letter-spacing: 0.03em;
        	text-transform: uppercase;
        	white-space: nowrap;
        }
        .dc-status-badge::before {
        	content: '';
        	display: inline-block;
        	width: 8px;
        	height: 8px;
        	border-radius: 50%;
        	background: currentColor;
        	opacity: 0.7;
        }
        /* Semantic text colors: modern Nextcloud ships --color-{success,error,…} as
           pale *background* tints; the AA-contrast text companions are the
           --color-*-text variables. Fall back to a darkened mix for legacy themes. */
        .dc-status-badge--open,
        .dc-status-badge--pending {
        	background: var(--dc-tint-info);
        	border-color: transparent;
        	color: var(--color-info-text, color-mix(in srgb, var(--color-primary-element, #0082c9) 80%, var(--color-main-text, #1d1d1d) 20%));
        }
        .dc-status-badge--published,
        .dc-status-badge--approved {
        	background: var(--dc-tint-success);
        	border-color: transparent;
        	color: var(--color-success-text, color-mix(in srgb, var(--color-success, #46ba61) 70%, var(--color-main-text, #1d1d1d) 30%));
        }
        .dc-status-badge--closed,
        .dc-status-badge--cancelled {
        	background: var(--dc-bg-stripe);
        	border-color: var(--dc-border);
        	color: var(--dc-muted);
        }
        .dc-status-badge--rejected {
        	background: var(--dc-tint-critical);
        	border-color: transparent;
        	color: var(--color-error-text, color-mix(in srgb, var(--color-error, #cc4646) 75%, var(--color-main-text, #1d1d1d) 25%));
        }
        
        /* Planning issue severity (must fix / confirm to continue) */
        .dc-severity {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-1);
        	padding: 2px 10px;
        	border-radius: 999px;
        	font-size: var(--dc-fs-xs);
        	font-weight: 700;
        	letter-spacing: 0.01em;
        	text-transform: none;
        }
        .dc-hint-link {
        	color: var(--color-primary-element, #00679e);
        	font-weight: 600;
        	text-decoration: underline;
        	text-underline-offset: 2px;
        }
        .dc-hint-link:hover,
        .dc-hint-link:focus-visible {
        	/* NOT --color-primary-element-text: that is the on-primary (white) color. */
        	color: var(--color-primary-element-hover, #00507a);
        	text-decoration-thickness: 2px;
        }
        .dc-hint-link:focus-visible {
        	outline: 2px solid var(--color-primary-element, #00679e);
        	outline-offset: 2px;
        	border-radius: 2px;
        }
        .dc-severity--hard {
        	background: var(--dc-tint-critical);
        	color: var(--color-error-text, color-mix(in srgb, var(--color-error, #cc4646) 80%, var(--color-main-text, #1d1d1d) 20%));
        }
        .dc-severity--soft {
        	background: var(--dc-tint-warning);
        	color: var(--color-warning-text, color-mix(in srgb, var(--color-warning, #c98600) 70%, var(--color-main-text, #1d1d1d) 30%));
        }
        .dc-severity--info {
        	background: var(--dc-tint-info);
        	color: var(--color-info-text, color-mix(in srgb, var(--color-primary-element, #0082c9) 80%, var(--color-main-text, #1d1d1d) 20%));
        }
        
        .dc-pill {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-1);
        	padding: 2px 10px;
        	border-radius: 999px;
        	background: var(--dc-bg-stripe);
        	color: var(--dc-muted);
        	font-size: var(--dc-fs-xs);
        	font-variant-numeric: var(--dc-tab-num);
        	min-height: 1.75rem;
        }
        .dc-pill[aria-busy='true'] {
        	font-style: italic;
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-chip-list {
        	list-style: none;
        	margin: var(--dc-space-2) 0 0;
        	padding: 0;
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	align-items: center;
        	min-height: 2.75rem;
        }
        .dc-chip {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-2);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-stripe);
        	border: 1px solid var(--dc-border);
        	font-size: var(--dc-fs-sm);
        	max-width: 100%;
        }
        .dc-chip__text {
        	font-variant-numeric: var(--dc-tab-num);
        	overflow-wrap: anywhere;
        }
        .dc-chip__remove {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	border: 0;
        	background: transparent;
        	cursor: pointer;
        	color: var(--dc-muted);
        	padding: var(--dc-space-1);
        	min-width: 44px;
        	min-height: 44px;
        	border-radius: var(--dc-radius-sm);
        	line-height: 1;
        	font-size: 1.25rem;
        	box-sizing: border-box;
        }
        .dc-chip__remove:hover {
        	color: var(--color-main-text, #1d1d1d);
        	background: color-mix(in srgb, var(--color-main-text, #1d1d1d) 8%, transparent);
        }
        
        /* ── Empty / hint cards / callouts ───────────────────────────── */
        .dc-empty {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        }
        .dc-empty p, .dc-empty h2 { margin: 0; }
        .dc-callout {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-tint-info);
        }
        .dc-callout--warning { background: var(--dc-tint-warning); }
        .dc-callout--critical { background: var(--dc-tint-critical); }
        .dc-callout--success { background: var(--dc-tint-success); }
        .dc-callout p { margin: 0; }
        .dc-callout strong { font-weight: 700; }
        .dc-callout__title {
        	margin: 0;
        	font-size: var(--dc-fs-lg, 1.125rem);
        	font-weight: 700;
        	line-height: var(--dc-line-tight, 1.25);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-callout__actions {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	margin-top: var(--dc-space-1);
        }
        .dc-company-access-banner {
        	margin-bottom: var(--dc-space-5);
        }
        .dc-company-access-banner .dc-callout__title {
        	font-size: var(--dc-fs-lg, 1.125rem);
        }
        .dc-callout__actions .button {
        	min-height: 44px;
        }
        .dc-callout__hint { color: var(--color-main-text, #1d1d1d); font-size: var(--dc-fs-sm); }
        /* Tinted callout backgrounds cost ~1 contrast step: muted ink tuned for the
           main background drops below 4.5:1 (WCAG 1.4.3) on them, so any muted copy
           nested in a callout is promoted to full-contrast ink. */
        .dc-callout .dc-field__hint,
        .dc-callout .dc-section__sub,
        .dc-callout .dc-loading {
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-loading {
        	color: var(--dc-muted);
        	font-style: italic;
        }
        
        /* Quickstart cards */
        .dc-quickstart {
        	list-style: decimal inside;
        	padding: 0;
        	margin: 0;
        	display: grid;
        	gap: var(--dc-space-3);
        	grid-template-columns: 1fr;
        	min-width: 0;
        	max-width: 100%;
        }
        @media (min-width: 520px) {
        	.dc-quickstart {
        		grid-template-columns: repeat(2, minmax(0, 1fr));
        	}
        }
        @media (min-width: 900px) {
        	.dc-quickstart {
        		grid-template-columns: repeat(3, minmax(0, 1fr));
        	}
        	/* Keep the “publish on Periods” step from squeezing into a 4th skinny column */
        	.dc-quickstart__item[data-step="publish-on-periods"] {
        		grid-column: 1 / -1;
        	}
        }
        .dc-quickstart__item {
        	padding: var(--dc-space-4);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-card);
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	min-width: 0;
        	max-width: 100%;
        	overflow-wrap: anywhere;
        	word-break: break-word;
        }
        .dc-quickstart__item p { margin: 0; color: var(--dc-muted); font-size: var(--dc-fs-sm); overflow-wrap: anywhere; }
        .dc-quickstart__cta {
        	margin-top: var(--dc-space-3);
        	display: flex;
        	justify-content: center;
        	width: 100%;
        	max-width: 100%;
        	text-align: center;
        	min-width: 0;
        }
        .dc-quickstart__item strong { font-size: var(--dc-fs-md); line-height: var(--dc-line-tight); color: var(--color-main-text, #1d1d1d); overflow-wrap: anywhere; }
        .dc-quickstart__item .button,
        .dc-quickstart__item .dc-btn,
        .dc-quickstart__cta .button,
        .dc-quickstart__cta .dc-btn {
        	align-self: center;
        	min-height: 44px;
        	margin-top: auto;
        	text-align: center;
        }
        
        /* Setup progress (dashboard — not dismissable until complete)
         *
         * NC34+ ships --color-success / --color-error / --color-warning as pale
         * *surface* tints. Never put --color-primary-element-text (white) on them —
         * measured white-on-#D8F3DA ≈ 1.18:1 (fails WCAG 1.4.11). Use the
         * --color-*-text companions (same contract as .dc-status-badge--*).
         */
        .dc-setup-checklist {
        	list-style: none;
        	padding: 0;
        	margin: 0;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        }
        .dc-setup-checklist__item {
        	display: grid;
        	grid-template-columns: var(--dc-touch) 1fr;
        	gap: var(--dc-space-3);
        	align-items: start;
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-card);
        }
        /* Done rows collapse — one glance, no competing CTAs. */
        .dc-setup-checklist__item.is-done {
        	border-color: var(--dc-tint-success-border, var(--dc-border));
        	background: var(--dc-tint-success);
        	align-items: center;
        	padding-block: var(--dc-space-2);
        }
        .dc-setup-checklist__item.is-done .dc-setup-checklist__hint {
        	display: none;
        }
        .dc-setup-checklist__item.is-current {
        	border-color: var(--color-primary-element, #00679e);
        	border-width: 2px;
        	padding: var(--dc-space-4);
        	background: var(--dc-bg-card);
        	box-shadow: var(--dc-shadow-sm);
        }
        .dc-setup-checklist__status {
        	display: flex;
        	align-items: center;
        	justify-content: center;
        	width: var(--dc-touch);
        	height: var(--dc-touch);
        	border-radius: 50%;
        	font-size: 1.25rem;
        	font-weight: 700;
        	line-height: 1;
        	flex-shrink: 0;
        	background: var(--color-background-dark, #e8e8e8);
        	color: var(--color-main-text, #1d1d1d);
        	border: 2px solid transparent;
        	box-sizing: border-box;
        }
        .dc-setup-checklist__item.is-done .dc-setup-checklist__status {
        	/* Pale success surface + dark success ink (≥ 4.5:1). Ring uses the
        	   saturated element token so the mark stays visible without colour alone. */
        	background: var(--color-success, var(--dc-tint-success));
        	color: var(--color-success-text, color-mix(in srgb, var(--color-success, #46ba61) 70%, var(--color-main-text, #1d1d1d) 30%));
        	border-color: var(--color-element-success, var(--color-success-text, #005416));
        }
        .dc-setup-checklist__item.is-current .dc-setup-checklist__status {
        	background: var(--color-primary-element, #00679e);
        	color: var(--color-primary-element-text, #fff);
        	border-color: var(--color-primary-element, #00679e);
        }
        .dc-setup-checklist__body {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	min-width: 0;
        }
        .dc-setup-checklist__body strong {
        	font-size: var(--dc-fs-md);
        	line-height: var(--dc-line-tight);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-setup-checklist__item.is-done .dc-setup-checklist__body strong {
        	font-weight: 600;
        	color: var(--color-success-text, var(--color-main-text, #1d1d1d));
        }
        .dc-setup-checklist__hint {
        	margin: 0;
        	color: var(--dc-muted);
        	font-size: var(--dc-fs-sm);
        }
        .dc-setup-checklist__item.is-current .dc-setup-checklist__hint {
        	/* Hint sits on card white/dark bg — muted is fine; keep readable size. */
        	font-size: var(--dc-fs-md);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-setup-checklist__body .button {
        	align-self: flex-start;
        	min-height: var(--dc-touch);
        	min-width: var(--dc-touch);
        }
        .dc-setup-checklist__body .button.primary {
        	font-size: var(--dc-fs-md);
        	font-weight: 700;
        	padding-inline: var(--dc-space-5);
        }
        @media (max-width: 480px) {
        	.dc-setup-checklist__body .button.primary {
        		width: 100%;
        		justify-content: center;
        	}
        }
        @media (forced-colors: active) {
        	.dc-setup-checklist__item.is-done .dc-setup-checklist__status,
        	.dc-setup-checklist__item.is-current .dc-setup-checklist__status {
        		border: 2px solid CanvasText;
        		forced-color-adjust: none;
        	}
        }
        
        /* ── Quickstart card variant (dismissable) ───────────────────── */
        .dc-empty--quickstart {
        	position: relative;
        	padding-inline-start: calc(var(--dc-space-4) + 4px);
        }
        .dc-empty--quickstart::before {
        	content: "";
        	position: absolute;
        	inset-block: var(--dc-space-3);
        	inset-inline-start: 0;
        	width: 4px;
        	border-radius: 4px;
        	background: var(--color-primary-element, #1976d2);
        	opacity: 0.85;
        }
        .dc-empty--quickstart .dc-section__header h2 {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-2);
        }
        .dc-empty--quickstart .dc-section__header h2::before {
        	content: "★";
        	color: var(--color-primary-element, #1976d2);
        	font-size: 0.85em;
        	line-height: 1;
        }
        
        /* ── Hint dismiss button ─────────────────────────────────────── */
        .dc-hint-dismiss {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	background: transparent;
        	border: 1px solid var(--dc-border);
        	color: var(--color-main-text, #1d1d1d);
        	border-radius: var(--dc-radius-sm);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	cursor: pointer;
        	min-height: 44px;
        	min-width: 44px;
        	box-sizing: border-box;
        	line-height: 1.2;
        	font: inherit;
        	text-align: center;
        }
        .dc-hint-dismiss:hover { background: var(--dc-bg-soft); }
        .dc-hint-dismiss:focus-visible {
        	outline: 2px solid var(--color-primary-element, #1976d2);
        	outline-offset: 2px;
        }
        
        /* Settings — ArbeitszeitCheck integration (clear hierarchy, touch targets, wrap) */
        .dc-subsection-heading {
        	margin: 0 0 var(--dc-space-2);
        	font-size: var(--dc-fs-sm);
        	font-weight: 700;
        	letter-spacing: 0.05em;
        	text-transform: uppercase;
        	color: var(--dc-muted);
        	line-height: var(--dc-line-tight);
        }
        .dc-at-integration__body {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-4);
        }
        .dc-at-integration__status {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	min-width: 0;
        }
        .dc-at-integration__banner-row {
        	display: flex;
        	flex-wrap: wrap;
        	align-items: flex-start;
        	gap: var(--dc-space-2);
        }
        .dc-at-integration__banner {
        	flex: 1 1 220px;
        	min-width: 0;
        	margin: 0;
        }
        .dc-at-integration__retry {
        	flex: 0 0 auto;
        	align-self: flex-start;
        	min-height: 44px;
        }
        .dc-at-integration__retry:focus-visible {
        	outline: 2px solid var(--color-primary-element, #0082c9);
        	outline-offset: 2px;
        }
        .dc-at-integration__meta {
        	margin: 0;
        	max-width: 65ch;
        }
        .dc-at-banner .dc-callout__actions {
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        }
        .dc-at-banner .dc-callout__actions .button {
        	min-height: 44px;
        	min-width: 44px;
        }
        .dc-at-integration__controls .dc-field {
        	max-width: 48rem;
        }
        @media (max-width: 720px) {
        	.dc-at-integration__banner-row {
        		flex-direction: column;
        	}
        	.dc-at-integration__actions {
        		flex-direction: column;
        		align-items: stretch;
        	}
        	.dc-at-integration__actions .button,
        	.dc-at-integration__actions a.button {
        		width: 100%;
        		justify-content: center;
        	}
        }
        @media (prefers-reduced-motion: reduce) {
        	.dc-at-integration__actions .button[disabled] {
        		transition: none;
        	}
        }
        @media (forced-colors: active) {
        	.dc-at-banner,
        	.dc-at-integration__banner {
        		border: 2px solid CanvasText;
        	}
        	.dc-at-banner :focus-visible,
        	.dc-at-integration__actions .button:focus-visible,
        	.dc-at-integration__retry:focus-visible {
        		outline: 2px solid CanvasText;
        		outline-offset: 2px;
        	}
        }
        @media print {
        	/* REQ-UX-13: hide dismissible banner region; keep table source column. */
        	.dc-at-banner,
        	.dc-at-integration__banner {
        		display: none !important;
        	}
        	.dc-at-integration {
        		break-inside: avoid;
        	}
        }
        .dc-at-integration__divider {
        	margin: var(--dc-space-1) 0;
        }
        .dc-at-integration__controls {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	min-width: 0;
        }
        .dc-at-integration__actions {
        	flex-wrap: wrap;
        	justify-content: flex-start;
        	align-self: stretch;
        	width: 100%;
        	margin-top: var(--dc-space-2);
        	padding-top: var(--dc-space-3);
        	border-top: 1px solid var(--dc-border);
        }
        /* .dc-field + .dc-form-actions on one node breaks layout (column + centered children) */
        .dc-field.dc-form-actions {
        	flex-direction: row;
        	flex-wrap: wrap;
        	justify-content: flex-start;
        	align-items: center;
        	align-self: stretch;
        	width: 100%;
        }
        .dc-at-integration__actions .button,
        .dc-at-integration__actions a.button {
        	min-height: 44px;
        	box-sizing: border-box;
        }
        
        /* ── Forms ───────────────────────────────────────────────────── */
        .dc-form-grid,
        .dc-filter-grid {
        	display: grid;
        	grid-template-columns: minmax(0, 1fr);
        	gap: var(--dc-space-3);
        	align-items: start;
        	min-width: 0;
        	max-width: 100%;
        	width: 100%;
        	box-sizing: border-box;
        }
        .dc-form-grid--nested {
        	margin-block-start: var(--dc-space-4);
        }
        .dc-form-grid > .dc-field--full,
        .dc-filter-grid > .dc-field--full {
        	grid-column: 1 / -1;
        }
        .dc-form-grid > hr.dc-form-grid__divider {
        	grid-column: 1 / -1;
        	width: 100%;
        	height: 0;
        	margin: var(--dc-space-1) 0;
        	border: 0;
        	border-top: 1px solid var(--dc-border);
        }
        
        .dc-field {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-1);
        	min-width: 0;
        }
        .dc-field__label {
        	font-weight: 600;
        	font-size: var(--dc-fs-sm);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-field__hint {
        	margin: var(--dc-space-1) 0 0;
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        	line-height: var(--dc-line-base);
        }
        .dc-roster-break-prefill {
        	margin: var(--dc-space-1) 0 0;
        	font-size: var(--dc-fs-sm);
        	font-weight: 600;
        	color: var(--color-main-text, #1d1d1d);
        	line-height: var(--dc-line-base);
        }
        .dc-roster-break-prefill:empty {
        	display: none;
        }
        .dc-field__error {
        	margin: var(--dc-space-1) 0 0;
        	font-size: var(--dc-fs-xs);
        	color: var(--color-error-text, var(--color-error, #cc4646));
        	font-weight: 600;
        }
        
        /*
         * On multi-column rows, stacked fields (label + control [+ hint]) must share
         * the same vertical rhythm. Without a hint row, `align-items: start` lets
         * sibling fields with hints push their label lower. Reserve hint space with
         * ::after so all inputs share one baseline.
         */
        .dc-form-grid > .dc-field:not(.dc-field--inline):not(.dc-field--checkbox):not(:has(.dc-field__hint))::after,
        .dc-filter-grid > .dc-field:not(.dc-field--inline):not(.dc-field--checkbox):not(:has(.dc-field__hint))::after {
        	content: '';
        	display: block;
        	margin-top: var(--dc-space-1);
        	min-height: calc(2 * var(--dc-line-base) * var(--dc-fs-xs));
        	pointer-events: none;
        }
        
        .dc-input,
        #app-content.dc-app input[type='text'],
        #app-content.dc-app input[type='number'],
        #app-content.dc-app input[type='date'],
        #app-content.dc-app input[type='time'],
        #app-content.dc-app input[type='search'],
        #app-content.dc-app input[type='email'],
        #app-content.dc-app input[type='url'],
        #app-content.dc-app select,
        #app-content.dc-app textarea {
        	width: 100%;
        	max-width: 100%;
        	min-width: 0;
        	min-height: 44px;
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	border: 1px solid var(--dc-border-strong);
        	background: var(--dc-bg-card);
        	color: var(--color-main-text, #1d1d1d);
        	font: inherit;
        	box-sizing: border-box;
        }
        #app-content.dc-app textarea {
        	min-height: 6rem;
        	resize: vertical;
        }
        .dc-input:focus-visible,
        #app-content.dc-app input:not([type='checkbox']):not([type='radio']):focus-visible,
        #app-content.dc-app select:focus-visible,
        #app-content.dc-app textarea:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 1px;
        	border-color: var(--color-primary-element, #0082c9);
        }
        
        input[type=number], input[type=month], input[type=date], input[type=time],
        .dc-input--num, .dc-input--date {
        	font-variant-numeric: var(--dc-tab-num);
        }
        
        /*
         * Checkbox + copy in an input-sized box (same border/padding rhythm as .dc-input).
         *
         * Nextcloud core applies `min-height: var(--default-clickable-area)` to ALL
         * `input` elements. Without an explicit min-width/min-height reset the native
         * control stretches to ~44px and washes out or clips under dark / high-contrast
         * themes. Match AZC settings + DeskCheck form-controls: fixed 1.25rem box,
         * theme tokens for fill/border, accent-color for the checkmark.
         */
        .dc-checkbox,
        label.dc-checkbox {
        	display: inline-flex !important;
        	align-items: center;
        	gap: var(--dc-space-3);
        	width: 100%;
        	max-width: 100%;
        	min-width: 0;
        	box-sizing: border-box;
        	min-height: var(--dc-touch, 44px);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border: 1px solid var(--dc-border-strong);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-card);
        	color: var(--dc-text, var(--color-main-text, #1d1d1d));
        	cursor: pointer;
        	margin: 0;
        	-webkit-tap-highlight-color: transparent;
        }
        .dc-checkbox input[type='checkbox'],
        .dc-check input[type='checkbox'],
        .dc-field--radio input[type='radio'] {
        	flex-shrink: 0;
        	width: 1.25rem;
        	height: 1.25rem;
        	/* Beat NC core `input { min-height: var(--default-clickable-area) }` */
        	min-width: 1.25rem;
        	min-height: 1.25rem;
        	max-width: 1.25rem;
        	max-height: 1.25rem;
        	margin: 0;
        	padding: 0;
        	cursor: pointer;
        	box-sizing: border-box;
        	accent-color: var(--color-primary-element, #0082c9);
        	/* Theme-aware native chrome (light / dark / high-contrast / custom primary) */
        	background-color: var(--dc-bg-card, var(--color-main-background, #fff));
        	border: 1px solid var(--dc-border-strong, var(--color-border-dark, #999));
        	border-radius: 3px;
        	color: var(--dc-text, var(--color-main-text, #1d1d1d));
        	/* Follow body[data-theme-*] so the UA paints the glyph for the active scheme */
        	color-scheme: inherit;
        	appearance: auto;
        	-webkit-appearance: auto;
        }
        .dc-field--radio input[type='radio'] {
        	border-radius: 50%;
        	margin: 0.2em 0 0;
        }
        .dc-checkbox input[type='checkbox']:focus-visible,
        .dc-check input[type='checkbox']:focus-visible,
        .dc-field--radio input[type='radio']:focus-visible {
        	outline: 3px solid var(--color-primary-element, #0082c9);
        	outline-offset: 2px;
        	border-color: var(--color-primary-element, #0082c9);
        }
        .dc-checkbox:has(input:disabled) {
        	cursor: not-allowed;
        	opacity: 0.72;
        }
        .dc-checkbox:has(input:disabled) input {
        	cursor: not-allowed;
        }
        .dc-checkbox:focus-within {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 1px;
        }
        .dc-checkbox > span,
        .dc-checkbox__text {
        	flex: 1;
        	min-width: 0;
        	line-height: var(--dc-line-base);
        	font-size: var(--dc-fs-sm);
        	color: inherit;
        }
        
        /* Radio inside .dc-field--radio */
        .dc-field--radio {
        	flex-direction: row;
        	align-items: flex-start;
        	gap: var(--dc-space-3);
        	padding: var(--dc-space-1) 0;
        	color: var(--dc-text, var(--color-main-text, #1d1d1d));
        }
        
        .dc-form-actions,
        .dc-filter-actions,
        .dc-action-row {
        	display: flex;
        	gap: var(--dc-space-2);
        	flex-wrap: wrap;
        	align-items: center;
        	justify-content: flex-start;
        	grid-column: 1 / -1;
        }
        
        /* Employee form: explicit two-column rhythm + section separators */
        .dc-form-grid--employees {
        	grid-template-columns: 1fr;
        	column-gap: var(--dc-space-4);
        	row-gap: var(--dc-space-3);
        }
        .dc-form-grid--employees > .dc-field--employee-link {
        	grid-column: 1 / -1;
        	padding-bottom: var(--dc-space-3);
        	border-bottom: 1px solid var(--dc-border);
        }
        .dc-form-grid--employees > .dc-field--employee-name {
        	grid-column: 1 / -1;
        }
        .dc-form-grid--employees > .dc-field--employee-active {
        	grid-column: 1 / -1;
        }
        .dc-form-grid--employees > .dc-form-actions {
        	justify-content: flex-end;
        	padding-top: var(--dc-space-3);
        	border-top: 1px solid var(--dc-border);
        }
        
        /*
         * Location form: stacked segments with full-width rules so each block reads as a
         * clear row (identity + status, timezone, actions).
         */
        .dc-form-stack {
        	display: flex;
        	flex-direction: column;
        	gap: 0;
        	min-width: 0;
        }
        .dc-form-stack--locations > .dc-form-stack__segment {
        	padding-block: var(--dc-space-4);
        	border-bottom: 1px solid var(--dc-border);
        }
        .dc-form-stack--locations > .dc-form-stack__segment:first-child {
        	padding-top: 0;
        }
        .dc-form-stack--locations > .dc-form-stack__segment:last-child {
        	border-bottom: 0;
        	padding-bottom: 0;
        }
        .dc-form-stack__grid--2 {
        	display: grid;
        	grid-template-columns: 1fr;
        	column-gap: var(--dc-space-4);
        	row-gap: var(--dc-space-3);
        	align-items: start;
        }
        .dc-form-stack__grid--2 > .dc-field--location-active {
        	align-self: stretch;
        }
        .dc-form-stack__segment--actions .dc-form-actions {
        	margin: 0;
        	justify-content: flex-end;
        	width: 100%;
        }
        /* Searchable IANA timezone picker (locations form) */
        .dc-timezone-picker {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	min-width: 0;
        	max-width: 100%;
        }
        .dc-timezone-picker__control {
        	display: flex;
        	align-items: stretch;
        	gap: var(--dc-space-2);
        	min-width: 0;
        }
        .dc-timezone-picker__input {
        	flex: 1 1 auto;
        	min-width: 0;
        	margin: 0;
        }
        .dc-timezone-picker__native {
        	position: absolute;
        	width: 1px;
        	height: 1px;
        	padding: 0;
        	margin: -1px;
        	overflow: hidden;
        	clip: rect(0, 0, 0, 0);
        	white-space: nowrap;
        	border: 0;
        }
        .dc-timezone-picker__clear {
        	flex: 0 0 auto;
        	align-self: stretch;
        	min-width: 44px;
        	min-height: 44px;
        	padding-inline: var(--dc-space-2);
        	font-size: 1.25rem;
        	line-height: 1;
        	box-sizing: border-box;
        }
        .dc-timezone-picker__results {
        	position: relative;
        	z-index: 20;
        	margin: 0;
        	padding: var(--dc-space-1) 0;
        	list-style: none;
        	max-height: min(50vh, 16rem);
        	overflow-y: auto;
        	border: 1px solid var(--color-border, #ededed);
        	border-radius: var(--dc-radius-sm);
        	background: var(--color-main-background, #fff);
        	box-shadow: var(--dc-shadow-sm);
        }
        .dc-timezone-picker__results[hidden] {
        	display: none;
        }
        .dc-timezone-picker__group {
        	padding: var(--dc-space-2) var(--dc-space-3) var(--dc-space-1);
        	font-size: var(--dc-fs-xs);
        	font-weight: 700;
        	letter-spacing: 0.02em;
        	text-transform: uppercase;
        	color: var(--dc-muted);
        }
        .dc-timezone-picker__option {
        	display: flex;
        	align-items: baseline;
        	justify-content: space-between;
        	gap: var(--dc-space-3);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	cursor: pointer;
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-timezone-picker__option:hover,
        .dc-timezone-picker__option[aria-selected="true"] {
        	background: var(--color-background-hover, rgba(0, 0, 0, 0.05));
        }
        .dc-timezone-picker__option:focus-visible {
        	outline: 2px solid var(--color-primary-element, #0082c9);
        	outline-offset: -2px;
        }
        .dc-timezone-picker__option-id {
        	font-weight: 600;
        	word-break: break-word;
        }
        .dc-timezone-picker__option-offset {
        	flex-shrink: 0;
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        }
        .dc-timezone-picker__empty {
        	padding: var(--dc-space-3);
        	font-size: var(--dc-fs-sm);
        	color: var(--dc-muted);
        }
        .dc-timezone-picker__status {
        	margin: 0;
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        	line-height: var(--dc-line-base);
        }
        .dc-timezone-picker__status--error {
        	color: var(--color-error, #e9322d);
        	font-weight: 600;
        }
        .dc-timezone-picker__status[hidden] {
        	display: none;
        }
        
        /* Roster planning */
        .dc-roster-flash {
        	margin: calc(-1 * var(--dc-space-2)) 0 var(--dc-space-3);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-tint-success);
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-roster-flash[hidden] {
        	display: none !important;
        }
        .dc-roster-empty-panel {
        	display: flex;
        	flex-direction: column;
        	align-items: center;
        	justify-content: center;
        	gap: var(--dc-space-2);
        	box-sizing: border-box;
        	width: 100%;
        	padding: var(--dc-space-7) var(--dc-space-4);
        	text-align: center;
        	border-top: 1px solid var(--dc-border);
        	background: var(--dc-bg-card);
        }
        .dc-roster-empty-panel[hidden] {
        	display: none !important;
        }
        .dc-roster-empty-state__text {
        	margin: 0;
        	max-width: 32rem;
        	font-size: var(--dc-fs-md);
        	line-height: var(--dc-line-base);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-roster-empty-state__hint {
        	margin: 0;
        	max-width: 32rem;
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        	color: var(--dc-muted);
        }
        .button.dc-is-disabled,
        .button.primary[aria-disabled='true']:not(:disabled) {
        	opacity: 0.55;
        	cursor: not-allowed;
        	filter: grayscale(0.15);
        }
        .dc-assignment-form-host {
        	display: none !important;
        }
        .dc-modal__dialog--roster-assignment {
        	max-width: min(720px, calc(100vw - 2 * var(--dc-space-4)));
        }
        .dc-modal__dialog--roster-assignment .dc-modal__body {
        	padding-top: 0;
        }
        .dc-modal__dialog--roster-assignment .dc-form-actions--roster {
        	justify-content: flex-start;
        	margin-top: var(--dc-space-2);
        	padding-top: var(--dc-space-2);
        	border-top: 1px solid var(--dc-border);
        }
        .dc-modal__dialog--roster-assignment .dc-form-actions--roster .button[type="submit"] {
        	display: none;
        }
        .dc-roster-panel,
        .dc-settings-panel {
        	scroll-margin-top: var(--dc-space-3);
        	border-inline-start: 4px solid var(--color-primary-element, #0082c9);
        	padding-inline-start: var(--dc-space-4);
        }
        .dc-settings-planning-note {
        	margin-top: var(--dc-space-2);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-soft);
        	border: 1px solid var(--dc-border);
        }
        .dc-roster-panel__body {
        	max-width: 100%;
        }
        .dc-app--my-roster .dc-main {
        	gap: var(--dc-space-5);
        }
        .dc-app--my-roster .dc-section.dc-roster-panel {
        	box-shadow: var(--dc-shadow-sm);
        }
        .dc-app--my-roster .dc-section:not(.dc-roster-panel) {
        	box-shadow: var(--dc-shadow-sm);
        }
        .dc-roster-period__lifecycle {
        	display: flex;
        	flex-direction: column;
        	align-items: flex-start;
        	gap: var(--dc-space-3);
        	margin-top: var(--dc-space-4);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border-radius: var(--dc-radius-sm);
        	border: 1px solid var(--dc-border);
        	background: var(--dc-bg-soft);
        }
        .dc-roster-period__lifecycle .dc-field__hint {
        	margin: 0;
        }
        .dc-roster-form__step-group {
        	grid-column: 1 / -1;
        	margin: 0;
        	padding: var(--dc-space-4);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	background: var(--color-main-background, #fff);
        }
        .dc-roster-form__step-group + .dc-roster-form__step-group {
        	margin-top: var(--dc-space-2);
        }
        .dc-roster-form__step-group .dc-roster-form__section-heading {
        	margin: 0 0 var(--dc-space-3);
        }
        .dc-roster-form__step-group .dc-field--roster-time-hint,
        .dc-roster-form__step-group .dc-field--roster-rest-hint {
        	margin-bottom: var(--dc-space-2);
        }
        .dc-form-grid--roster-assignment fieldset {
        	border: none;
        	min-inline-size: 0;
        }
        .dc-form-grid--roster-assignment {
        	grid-template-columns: 1fr;
        	column-gap: var(--dc-space-4);
        	row-gap: var(--dc-space-3);
        	align-items: start;
        }
        .dc-roster-form__intro {
        	grid-column: 1 / -1;
        	margin: 0 0 var(--dc-space-2);
        	padding: var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-soft);
        	border: 1px solid var(--dc-border);
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-normal);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-roster-form__section-heading--step {
        	display: flex;
        	align-items: center;
        	gap: var(--dc-space-2);
        }
        .dc-roster-form__step-badge {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	min-width: 1.75rem;
        	height: 1.75rem;
        	padding: 0 var(--dc-space-1);
        	border-radius: 999px;
        	background: var(--color-primary-element, #00679e);
        	color: var(--color-primary-element-text, #fff);
        	font-size: var(--dc-fs-sm);
        	font-weight: 700;
        	flex-shrink: 0;
        }
        .dc-roster-form__feedback {
        	grid-column: 1 / -1;
        	margin: 0;
        	padding: var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	border: 1px solid var(--dc-border);
        	background: var(--color-warning, #fcf4d4);
        	color: var(--color-main-text, #1d1d1d);
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-normal);
        }
        .dc-roster-form__feedback[hidden] {
        	display: none !important;
        }
        .dc-roster-form__feedback--error {
        	border-color: var(--color-error, #c00);
        	background: var(--color-error, #c00);
        	background: color-mix(in srgb, var(--color-error, #c00) 12%, var(--color-main-background, #fff));
        }
        .dc-roster-form__feedback--success {
        	border-color: var(--color-element-success, var(--color-success-text, var(--color-success, #46ba61)));
        	border-inline-start-width: 4px;
        	background: color-mix(in srgb, var(--color-success-text, var(--color-success, #46ba61)) 14%, var(--color-main-background, #fff));
        	font-weight: 600;
        }
        .dc-roster-form__feedback--info {
        	border-color: var(--color-primary-element, #0082c9);
        	border-inline-start-width: 4px;
        	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 10%, var(--color-main-background, #fff));
        }
        .dc-form-grid--roster-assignment > .dc-roster-form__intro,
        .dc-form-grid--roster-assignment > .dc-roster-form__feedback,
        .dc-form-grid--roster-assignment > .dc-roster-form__step-group,
        .dc-form-grid--roster-assignment > .dc-roster-form__extras {
        	grid-column: 1 / -1;
        }
        .dc-roster-form__extras {
        	display: grid;
        	grid-template-columns: 1fr;
        	row-gap: var(--dc-space-3);
        	padding: var(--dc-space-4);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	background: var(--color-main-background, #fff);
        }
        .dc-roster-form__extras > .dc-roster-form__section-heading {
        	margin: 0 0 var(--dc-space-1);
        	font-size: var(--dc-fs-md);
        	font-weight: 700;
        }
        .dc-roster-form__step-group > .dc-roster-form__section-heading {
        	grid-column: 1 / -1;
        	margin: var(--dc-space-4) 0 var(--dc-space-1);
        	padding: 0;
        	border: 0;
        	font-size: var(--dc-fs-md);
        	font-weight: 700;
        	line-height: var(--dc-line-tight);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-form-grid--roster-assignment > .dc-field--roster-time-hint {
        	grid-column: 1 / -1;
        	padding: var(--dc-space-2) var(--dc-space-3);
        	margin: 0;
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-soft);
        	border: 1px solid var(--dc-border);
        }
        .dc-form-grid--roster-assignment .dc-roster-24h-hint {
        	margin: 0;
        }
        .dc-form-grid--roster-assignment > .dc-field--roster-rest-hint {
        	grid-column: 1 / -1;
        	margin: 0;
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-soft);
        	border: 1px solid var(--dc-border);
        }
        .dc-form-grid--roster-assignment > .dc-form-actions--roster {
        	grid-column: 1 / -1;
        	margin-top: var(--dc-space-2);
        	padding-top: var(--dc-space-4);
        	border-top: 1px solid var(--dc-border);
        	justify-content: flex-end;
        }
        .dc-input--time-24h {
        	font-variant-numeric: tabular-nums;
        }
        
        /* Entity picker (search + suggestion list) */
        .dc-entity-picker {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	max-width: 100%;
        }
        .dc-entity-picker__field {
        	position: relative;
        }
        .dc-entity-picker__field .dc-icon--lead {
        	position: absolute;
        	inset-inline-start: var(--dc-space-3);
        	top: 50%;
        	transform: translateY(-50%);
        	color: var(--dc-muted);
        	pointer-events: none;
        }
        .dc-entity-picker__field input {
        	padding-inline-start: calc(var(--dc-space-4) + 18px + var(--dc-space-2));
        }
        .dc-entity-results {
        	list-style: none;
        	margin: 0;
        	padding: var(--dc-space-1) 0;
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-card);
        	max-height: 14rem;
        	overflow: auto;
        	box-shadow: var(--dc-shadow-sm);
        }
        .dc-entity-results:empty {
        	display: none;
        }
        .dc-entity-results:not(:empty) {
        	margin-top: var(--dc-space-2);
        }
        .dc-entity-results li {
        	padding: var(--dc-space-2) var(--dc-space-3);
        	cursor: pointer;
        	min-height: 44px;
        	display: flex;
        	align-items: center;
        	gap: var(--dc-space-2);
        }
        .dc-entity-results li:hover,
        .dc-entity-results li[aria-selected='true'] {
        	background: var(--dc-tint-info);
        }
        .dc-entity-results li:focus-visible {
        	background: var(--dc-tint-info);
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: -3px;
        }
        .dc-entity-results__title {
        	font-weight: 600;
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-entity-results__sub {
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        	margin-top: 2px;
        }
        .dc-entity-results__empty {
        	padding: var(--dc-space-3) var(--dc-space-3);
        	color: var(--dc-muted);
        	font-size: var(--dc-fs-sm);
        }
        
        /* ── Buttons ─────────────────────────────────────────────────── */
        .dc-app .button,
        .dc-app a.button,
        .dc-app button.button,
        .dc-app input[type='submit'].button,
        .dc-app input[type='button'].button,
        .dc-app input[type='reset'].button,
        #app-content.dc-app button,
        #app-content.dc-app .button,
        #app-content.dc-app a.button {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	gap: var(--dc-space-2);
        	min-height: 44px;
        	max-width: 100%;
        	padding: var(--dc-space-2) var(--dc-space-4);
        	box-sizing: border-box;
        	line-height: 1.25;
        	text-align: center;
        	white-space: nowrap;
        	font: inherit;
        	border-radius: var(--dc-radius-sm);
        	cursor: pointer;
        }
        
        #app-content.dc-app button.primary,
        .dc-app .button.primary,
        .dc-app a.button.primary {
        	min-height: 44px;
        }
        .dc-app .button.button--text,
        #app-content.dc-app button.button--text {
        	/* WCAG 2.5.8: text-style actions still need a 44×44 hit target. */
        	min-height: 44px;
        	min-width: 44px;
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border: 0;
        	background: transparent;
        	color: var(--color-primary-element, var(--color-main-text));
        	text-decoration: underline;
        	text-underline-offset: 2px;
        	font-weight: 500;
        	white-space: normal;
        }
        .dc-app .button.button--text:focus-visible,
        #app-content.dc-app button.button--text:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 2px;
        }
        
        #app-content.dc-app button.dc-btn-icon,
        .dc-app .dc-btn-icon {
        	min-width: 44px;
        	width: 44px;
        	padding: var(--dc-space-2);
        }
        
        #app-content.dc-app button.danger,
        .dc-app .button.danger {
        	color: var(--color-primary-element-text, #fff);
        	background: var(--color-error, #cc4646);
        	border-color: var(--color-error, #cc4646);
        }
        #app-content.dc-app button.danger:hover,
        .dc-app .button.danger:hover {
        	filter: brightness(0.9);
        }
        
        /* ── Tables ──────────────────────────────────────────────────── */
        .dc-table-wrap {
        	overflow-x: auto;
        	-webkit-overflow-scrolling: touch;
        	border-radius: var(--dc-radius-sm);
        	border: 0;
        	background: transparent;
        }
        /* Overflow regions must be a keyboard tab stop (WCAG 2.1.1 / axe scrollable-region-focusable). */
        .dc-table-wrap:focus-visible,
        .dc-roster-grid-scroller:focus-visible,
        .dc-roster-list-scroller:focus-visible,
        .dc-windowed-table:focus-visible,
        .dc-print-table-wrap:focus-visible {
        	outline: 3px solid var(--color-primary-element, var(--color-primary, #0082c9));
        	outline-offset: 2px;
        }
        .dc-table {
        	width: 100%;
        	border-collapse: collapse;
        	font-variant-numeric: var(--dc-tab-num);
        	background: var(--dc-bg-card);
        }
        .dc-table,
        .dc-table tbody,
        .dc-table tr,
        .dc-table td,
        .dc-table th {
        	display: block;
        	width: 100%;
        }
        .dc-table thead {
        	position: absolute;
        	width: 1px;
        	height: 1px;
        	margin: -1px;
        	padding: 0;
        	overflow: hidden;
        	clip: rect(0 0 0 0);
        	white-space: nowrap;
        	border: 0;
        }
        .dc-table tbody tr {
        	display: grid;
        	gap: var(--dc-space-1);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	margin-bottom: var(--dc-space-3);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	background: var(--dc-bg-card);
        }
        .dc-table tbody tr:nth-child(even) td,
        .dc-table tbody tr:nth-child(even) th {
        	background: transparent;
        }
        .dc-table td,
        .dc-table tbody th.dc-table__rowhead {
        	border: 0;
        	padding: 0;
        	background: transparent;
        }
        .dc-table td[data-cell]::before,
        .dc-table tbody th.dc-table__rowhead[data-cell]::before {
        	content: attr(data-cell);
        	display: block;
        	font-size: var(--dc-fs-xs);
        	font-weight: 700;
        	letter-spacing: 0.04em;
        	color: var(--dc-muted);
        	text-transform: uppercase;
        	margin-bottom: 2px;
        }
        .dc-table tbody tr:has(td[colspan]):not(.dc-virtual-spacer),
        .dc-table tbody tr.dc-table__empty-row {
        	display: block;
        	text-align: center;
        }
        .dc-table tbody tr:has(td[colspan]):not(.dc-virtual-spacer) td::before,
        .dc-table tbody tr.dc-table__empty-row td::before {
        	content: none;
        	display: none;
        }
        .dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell,
        .dc-table tbody tr:has(td[colspan]):not(.dc-virtual-spacer) td[colspan] {
        	display: flex;
        	flex-direction: column;
        	align-items: center;
        	justify-content: center;
        	width: 100%;
        	padding: var(--dc-space-6) var(--dc-space-4);
        	text-align: center;
        	border-bottom: 0;
        }
        .dc-table .dc-row-actions {
        	padding-top: var(--dc-space-2);
        }
        .dc-table caption {
        	text-align: left;
        }
        .dc-table th, .dc-table td {
        	padding: var(--dc-space-3) var(--dc-space-4);
        	text-align: left;
        	vertical-align: middle;
        	border-bottom: 1px solid var(--dc-border);
        }
        .dc-table thead th {
        	background: var(--dc-bg-stripe);
        	font-size: var(--dc-fs-sm);
        	font-weight: 600;
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-table tbody tr:nth-child(even) td {
        	background: color-mix(in srgb, var(--dc-bg-card) 96%, var(--color-main-text, #1d1d1d) 4%);
        }
        .dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell,
        .dc-table tbody tr:has(td[colspan]) td[colspan] {
        	display: flex;
        	flex-direction: column;
        	align-items: center;
        	justify-content: center;
        	text-align: center;
        	padding: var(--dc-space-6) var(--dc-space-4);
        	border-bottom: 0;
        }
        .dc-table tbody tr.is-selected td,
        .dc-table tbody tr[aria-selected='true'] td {
        	background: var(--dc-tint-info);
        }
        .dc-table tbody tr.dc-row--selectable {
        	cursor: pointer;
        }
        .dc-table tbody tr.dc-row--selectable:hover td {
        	background: color-mix(in srgb, var(--dc-tint-info) 70%, var(--dc-bg-card) 30%);
        }
        /* Keyboard selection happens via the real button inside the row. */
        .dc-table .dc-period-select {
        	font-variant-numeric: tabular-nums;
        }
        .dc-table .dc-period-select[aria-current='true'] {
        	font-weight: 700;
        }
        .dc-table .dc-row-actions {
        	display: flex;
        	gap: var(--dc-space-2);
        	flex-wrap: wrap;
        }
        .dc-table .dc-time-cell__value {
        	white-space: nowrap;
        	font-variant-numeric: tabular-nums;
        }
        .dc-table__col--actions { white-space: nowrap; }
        
        /* Notes / muted detail under a primary cell */
        .dc-row-meta {
        	display: block;
        	margin-top: 2px;
        	color: var(--dc-muted);
        	font-size: var(--dc-fs-xs);
        	line-height: var(--dc-line-base);
        }
        .dc-row--flash {
        	outline: 2px solid var(--color-primary-element);
        	outline-offset: 2px;
        	background-color: var(--color-background-hover);
        }
        
        /* Duty times: fixed-width numerals for scanability (24-hour display) */
        .dc-time-cell__value {
        	font-variant-numeric: tabular-nums;
        	font-weight: 600;
        	letter-spacing: 0.02em;
        }
        
        /* ── Summary tiles ───────────────────────────────────────────── */
        .dc-summary-grid {
        	display: grid;
        	grid-template-columns: 1fr;
        	gap: var(--dc-space-3);
        }
        .dc-summary-tile {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-1);
        	padding: var(--dc-space-4);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	background: var(--dc-bg-card);
        }
        .dc-summary-tile--primary {
        	background: var(--dc-tint-info);
        	border-color: transparent;
        }
        .dc-summary-tile--success { background: var(--dc-tint-success); border-color: transparent; }
        .dc-summary-tile--warning { background: var(--dc-tint-warning); border-color: transparent; }
        .dc-summary-tile--critical { background: var(--dc-tint-critical); border-color: transparent; }
        .dc-summary-tile__label {
        	font-size: var(--dc-fs-xs);
        	text-transform: uppercase;
        	letter-spacing: 0.05em;
        	color: var(--dc-muted);
        	font-weight: 600;
        }
        .dc-summary-tile--primary .dc-summary-tile__label,
        .dc-summary-tile--primary .dc-summary-tile__hint,
        .dc-summary-tile--success .dc-summary-tile__label,
        .dc-summary-tile--success .dc-summary-tile__hint,
        .dc-summary-tile--warning .dc-summary-tile__label,
        .dc-summary-tile--warning .dc-summary-tile__hint,
        .dc-summary-tile--critical .dc-summary-tile__label,
        .dc-summary-tile--critical .dc-summary-tile__hint {
        	/* Secondary copy on tinted tiles must clear 4.5:1 — muted does not. */
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-summary-tile__value {
        	font-size: var(--dc-fs-2xl);
        	font-weight: 700;
        	font-variant-numeric: var(--dc-tab-num);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-summary-tile__hint {
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        	line-height: var(--dc-line-base);
        }
        
        /* ── Sidebar / navigation ────────────────────────────────────── */
        #app-navigation.dc-nav {
        	display: flex;
        	flex-direction: column;
        }
        .dc-nav__brand {
        	display: flex;
        	align-items: center;
        	gap: var(--dc-space-3);
        	padding: var(--dc-space-4);
        	border-bottom: 1px solid var(--dc-border);
        }
        .dc-nav__brand-icon {
        	display: grid;
        	place-items: center;
        	width: 36px;
        	height: 36px;
        	border-radius: var(--dc-radius-md);
        	background: var(--dc-tint-info);
        	color: var(--color-primary-element, #0082c9);
        	flex-shrink: 0;
        }
        .dc-nav__brand-icon svg { width: 22px; height: 22px; }
        .dc-nav__brand-text { display: flex; flex-direction: column; min-width: 0; }
        .dc-nav__title {
        	margin: 0;
        	font-size: var(--dc-fs-md);
        	font-weight: 700;
        	line-height: var(--dc-line-tight);
        }
        .dc-nav__subtitle {
        	margin: 0;
        	color: var(--dc-muted);
        	font-size: var(--dc-fs-xs);
        	line-height: var(--dc-line-base);
        }
        .dc-nav__role {
        	margin-top: var(--dc-space-1);
        	align-self: flex-start;
        }
        
        .dc-nav__group {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-1);
        	padding: var(--dc-space-2) var(--dc-space-2) 0;
        }
        .dc-nav__group-title {
        	margin: var(--dc-space-2) var(--dc-space-3) var(--dc-space-1);
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        	text-transform: uppercase;
        	letter-spacing: 0.05em;
        }
        .dc-nav__list {
        	list-style: none;
        	padding: 0;
        	margin: 0;
        	display: flex;
        	flex-direction: column;
        	gap: 2px;
        }
        .dc-nav__item { margin: 0; }
        .dc-nav__link {
        	display: grid;
        	grid-template-columns: 24px minmax(0, 1fr);
        	gap: var(--dc-space-2);
        	align-items: center;
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	color: var(--color-main-text, #1d1d1d);
        	text-decoration: none;
        	min-height: 44px;
        }
        .dc-nav__link:hover {
        	background: var(--dc-bg-stripe);
        }
        .dc-nav__item.is-active .dc-nav__link {
        	background: var(--dc-tint-info);
        	font-weight: 600;
        }
        .dc-nav__item.is-active .dc-nav__icon {
        	color: var(--color-primary-element, #0082c9);
        }
        /* Muted hints fail AA on the primary tint pill — use main text on active. */
        .dc-nav__item.is-active .dc-nav__hint {
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-nav__icon {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	color: var(--dc-muted);
        }
        .dc-nav__label { display: grid; gap: 2px; min-width: 0; }
        .dc-nav__name {
        	display: block;
        	font-size: var(--dc-fs-sm);
        }
        .dc-nav__hint {
        	display: block;
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        	line-height: var(--dc-line-base);
        	overflow: hidden;
        	text-overflow: ellipsis;
        	white-space: nowrap;
        }
        
        /* ── Conflicts & warnings ────────────────────────────────────── */
        .dc-conflicts {
        	list-style: none;
        	padding: 0;
        	margin: 0;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        }
        .dc-conflict {
        	display: grid;
        	grid-template-columns: auto minmax(0, 1fr);
        	gap: var(--dc-space-3);
        	align-items: start;
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border-radius: var(--dc-radius-sm);
        	border-inline-start: 4px solid var(--color-primary-element, #0082c9);
        	background: var(--dc-tint-info);
        }
        .dc-conflict__actions {
        	grid-column: 1 / -1;
        }
        .dc-conflict--hard {
        	border-inline-start-color: var(--color-error, #cc4646);
        	background: var(--dc-tint-critical);
        }
        .dc-conflict--soft {
        	border-inline-start-color: var(--color-warning, #c98600);
        	background: var(--dc-tint-warning);
        }
        .dc-conflict__body { min-width: 0; }
        .dc-conflict__title { font-weight: 600; color: var(--color-main-text, #1d1d1d); }
        .dc-conflict__message {
        	font-size: var(--dc-fs-sm);
        	color: var(--color-main-text, #1d1d1d);
        	margin-top: var(--dc-space-1);
        	line-height: var(--dc-line-base);
        }
        .dc-conflict__actions {
        	display: flex;
        	gap: var(--dc-space-2);
        	flex-wrap: wrap;
        	align-items: center;
        }
        .dc-conflict__ack {
        	display: block;
        	margin-top: var(--dc-space-1);
        	color: var(--color-main-text, #1d1d1d);
        	font-size: var(--dc-fs-xs);
        }
        .dc-conflict__ack-invalid {
        	display: block;
        	margin-top: var(--dc-space-1);
        	color: var(--color-warning-text, color-mix(in srgb, var(--color-warning, #c98600) 80%, var(--color-main-text, #1d1d1d) 20%));
        	font-size: var(--dc-fs-xs);
        	font-weight: 600;
        }
        
        /* ── Modal ───────────────────────────────────────────────────── */
        .dc-modal {
        	position: fixed;
        	inset: 0;
        	display: grid;
        	place-items: center;
        	background: var(--dc-scrim);
        	z-index: 10000;
        	padding: max(var(--dc-space-4), env(safe-area-inset-top, 0px)) max(var(--dc-space-4), env(safe-area-inset-right, 0px)) max(var(--dc-space-4), env(safe-area-inset-bottom, 0px)) max(var(--dc-space-4), env(safe-area-inset-left, 0px));
        	box-sizing: border-box;
        }
        .dc-modal__dialog {
        	background: var(--dc-bg-card);
        	border-radius: var(--dc-radius-md);
        	box-shadow: var(--dc-shadow-md);
        	max-width: min(640px, calc(100vw - 2 * var(--dc-space-4)));
        	width: 100%;
        	max-height: min(90vh, calc(100dvh - 2 * var(--dc-space-4)));
        	overflow: auto;
        	padding: var(--dc-space-4);
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-modal__header {
        	display: flex;
        	justify-content: space-between;
        	align-items: center;
        	gap: var(--dc-space-3);
        }
        .dc-modal__title {
        	margin: 0;
        	font-size: var(--dc-fs-xl);
        	line-height: var(--dc-line-tight);
        }
        .dc-modal__close {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	background: transparent;
        	border: 0;
        	color: inherit;
        	cursor: pointer;
        	padding: var(--dc-space-2);
        	border-radius: var(--dc-radius-sm);
        	min-width: 44px;
        	min-height: 44px;
        	box-sizing: border-box;
        	line-height: 1;
        	font: inherit;
        }
        .dc-modal__close:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        }
        .dc-modal__body {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        }
        .dc-modal__body p { margin: 0; line-height: var(--dc-line-base); }
        .dc-modal__textarea {
        	min-height: 7rem;
        	resize: vertical;
        }
        .dc-modal__actions {
        	display: flex;
        	justify-content: flex-end;
        	gap: var(--dc-space-2);
        	flex-wrap: wrap;
        	margin-top: var(--dc-space-2);
        }
        
        /* ── Toasts ──────────────────────────────────────────────────── */
        .dc-toasts {
        	position: fixed;
        	bottom: var(--dc-space-4);
        	inset-inline: var(--dc-space-4);
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	z-index: 12000;
        	max-width: none;
        	pointer-events: none;
        }
        .dc-toasts .dc-toast {
        	pointer-events: auto;
        	max-width: min(400px, 100%);
        	margin-inline-start: auto;
        }
        .dc-toast {
        	background: var(--dc-bg-card);
        	border: 1px solid var(--dc-border);
        	border-inline-start: 4px solid var(--color-primary-element, #0082c9);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border-radius: var(--dc-radius-sm);
        	box-shadow: var(--dc-shadow-sm);
        	display: flex;
        	gap: var(--dc-space-2);
        	align-items: center;
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-toast--success { border-inline-start-color: var(--color-element-success, var(--color-success-text, var(--color-success, #46ba61))); }
        .dc-toast--info { border-inline-start-color: var(--color-primary-element, #0082c9); }
        .dc-toast--error { border-inline-start-color: var(--color-error, #cc4646); }
        .dc-toast--warning { border-inline-start-color: var(--color-warning, #c98600); }
        .dc-toast__close {
        	margin-inline-start: auto;
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	background: transparent;
        	border: 0;
        	cursor: pointer;
        	padding: var(--dc-space-2);
        	min-height: 44px;
        	min-width: 44px;
        	box-sizing: border-box;
        	line-height: 1;
        	font: inherit;
        	border-radius: var(--dc-radius-sm);
        	color: inherit;
        }
        .dc-toast__close:focus-visible {
        	outline: 2px solid var(--color-main-text, #1d1d1d);
        	outline-offset: 2px;
        }
        
        /* ── Calendar subscription (employee self-service) ───────────── */
        .dc-ical-panel {
        	border-inline-start: 4px solid color-mix(in srgb, var(--color-warning, #c98600) 55%, var(--color-main-background, #fff));
        	padding-inline-start: var(--dc-space-4);
        }
        .dc-ical-panel__body {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-5);
        	max-width: 100%;
        }
        .dc-ical-subtitle {
        	margin: 0 0 var(--dc-space-3);
        	font-size: var(--dc-fs-lg);
        	font-weight: 700;
        	line-height: var(--dc-line-tight);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-ical-steps__list {
        	margin: 0;
        	padding-inline-start: 1.35rem;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	line-height: var(--dc-line-base);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-ical-steps__list li::marker {
        	font-weight: 700;
        	color: var(--color-primary-element, #0082c9);
        }
        .dc-callout__hint--tight {
        	margin: 0;
        	line-height: var(--dc-line-base);
        }
        .dc-ical-link-card {
        	padding: var(--dc-space-4) var(--dc-space-5);
        	border-radius: var(--dc-radius-md);
        	border: 1px solid var(--dc-border);
        	background: var(--dc-bg-soft);
        	box-shadow: var(--dc-shadow-sm);
        }
        .dc-ical__actions {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	align-items: center;
        	margin-bottom: var(--dc-space-3);
        }
        .dc-ical__actions .button {
        	flex: 1 1 auto;
        	min-width: min(100%, 200px);
        	min-height: 44px;
        }
        .dc-field--ical-url {
        	margin-bottom: 0;
        }
        .dc-ical__url {
        	width: 100%;
        	font-family: var(--font-monospace, ui-monospace, "SFMono-Regular", "Menlo", monospace);
        	font-size: var(--dc-fs-sm);
        }
        
        /* ── Snapshot integrity banner ───────────────────────────────── */
        .dc-integrity-banner {
        	display: flex;
        	gap: var(--dc-space-2);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-tint-critical);
        	color: var(--color-main-text, #1d1d1d);
        	font-weight: 600;
        }
        .dc-integrity-banner[hidden] { display: none; }
        
        /* ── Hash cell with copy ─────────────────────────────────────── */
        .dc-hash-cell {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-2);
        	max-width: 100%;
        	min-width: 0;
        }
        .dc-hash-cell__code {
        	font-family: var(--font-monospace, ui-monospace, "SFMono-Regular", "Menlo", monospace);
        	font-size: var(--dc-fs-xs);
        	overflow-wrap: anywhere;
        	min-width: 0;
        }
        .dc-hash-cell__copy {
        	flex-shrink: 0;
        }
        
        /* ── Callouts with bulleted list ─────────────────────────────── */
        .dc-callout__list {
        	margin: 0;
        	padding: 0 0 0 1.25rem;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-1);
        }
        .dc-callout__list li {
        	margin: 0;
        	line-height: var(--dc-line-base);
        }
        .dc-callout__list li .button {
        	margin-inline-start: var(--dc-space-2);
        	vertical-align: middle;
        }
        
        /* ── Loading row inside data tables ──────────────────────────── */
        .dc-table tbody tr.dc-table__loading-row td {
        	font-style: italic;
        	color: var(--dc-muted);
        	background: var(--dc-bg-card);
        }
        
        .dc-table tbody tr.dc-table__fetch-error-row td.dc-table__fetch-error-cell {
        	color: var(--color-error-text, var(--color-error, #c0392b));
        	background: var(--dc-tint-critical);
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        }
        
        /* ── Common icon sizing ──────────────────────────────────────── */
        #app-content.dc-app svg.dc-icon,
        #app-navigation.dc-nav svg.dc-icon,
        .dc-icon {
        	width: 20px;
        	height: 20px;
        	min-width: 20px;
        	min-height: 20px;
        	max-width: 20px;
        	max-height: 20px;
        	vertical-align: middle;
        }
        .dc-icon--sm,
        #app-content.dc-app svg.dc-icon.dc-icon--sm,
        #app-navigation.dc-nav svg.dc-icon.dc-icon--sm {
        	width: 16px;
        	height: 16px;
        	min-width: 16px;
        	min-height: 16px;
        	max-width: 16px;
        	max-height: 16px;
        }
        .dc-icon--lg,
        #app-content.dc-app svg.dc-icon.dc-icon--lg {
        	width: 28px;
        	height: 28px;
        	min-width: 28px;
        	min-height: 28px;
        	max-width: 28px;
        	max-height: 28px;
        }
        /* Page header icon scales up at 768px (see responsive block). */
        
        /* ── Access denied ───────────────────────────────────────────── */
        .dc-app--access-denied {
        	display: grid;
        	place-items: center;
        	min-height: 60vh;
        	padding: var(--dc-space-5);
        	background: var(--dc-bg-soft);
        }
        .dc-denied {
        	max-width: 540px;
        }
        .dc-denied .dc-card {
        	display: flex;
        	flex-direction: column;
        	align-items: center;
        	gap: var(--dc-space-3);
        	text-align: center;
        	padding: var(--dc-space-6);
        }
        .dc-denied .dc-page-header__icon {
        	background: var(--dc-tint-critical);
        	color: var(--color-error, #cc4646);
        }
        
        /* ── Focus ring (keyboard) — solid primary for ≥3:1 against surfaces ── */
        a:focus-visible,
        button:focus-visible,
        [role='button']:focus-visible,
        .dc-app a:focus-visible,
        .dc-app button:focus-visible,
        .dc-modal a:focus-visible,
        .dc-modal button:focus-visible {
        	outline: 3px solid var(--color-primary-element, #0082c9);
        	outline-offset: 2px;
        }
        
        @media (prefers-contrast: more) {
        	a:focus-visible,
        	button:focus-visible,
        	[role='button']:focus-visible,
        	.dc-app a:focus-visible,
        	.dc-app button:focus-visible,
        	.dc-modal a:focus-visible,
        	.dc-modal button:focus-visible,
        	.dc-input:focus-visible,
        	#app-content.dc-app input:not([type='checkbox']):not([type='radio']):focus-visible,
        	#app-content.dc-app select:focus-visible,
        	#app-content.dc-app textarea:focus-visible,
        	.dc-checkbox input[type='checkbox']:focus-visible,
        	.dc-check input[type='checkbox']:focus-visible,
        	.dc-field--radio input[type='radio']:focus-visible {
        		outline-width: 3px;
        		outline-color: var(--color-main-text, #1d1d1d);
        	}
        }
        
        @media (forced-colors: active) {
        	.dc-card,
        	.dc-callout,
        	.dc-badge,
        	.dc-status-badge,
        	.dc-conflict,
        	.dc-modal__dialog,
        	.dc-toast,
        	.dc-checkbox,
        	label.dc-checkbox {
        		border: 1px solid CanvasText;
        	}
        	.dc-checkbox input[type='checkbox'],
        	.dc-check input[type='checkbox'],
        	.dc-field--radio input[type='radio'] {
        		/* Keep the 1.25rem box; let the UA map fill/border to system colours */
        		border: 1px solid CanvasText;
        		background-color: Canvas;
        		accent-color: Highlight;
        	}
        	a:focus-visible,
        	button:focus-visible,
        	[role='button']:focus-visible,
        	.dc-checkbox:focus-within,
        	.dc-checkbox input[type='checkbox']:focus-visible,
        	.dc-check input[type='checkbox']:focus-visible,
        	.dc-field--radio input[type='radio']:focus-visible {
        		outline: 3px solid CanvasText;
        		outline-offset: 2px;
        	}
        }
        
        /* ── Reduced motion ──────────────────────────────────────────── */
        @media (prefers-reduced-motion: reduce) {
        	*, *::before, *::after {
        		animation-duration: 0.001ms !important;
        		animation-iteration-count: 1 !important;
        		transition-duration: 0.001ms !important;
        		scroll-behavior: auto !important;
        	}
        }
        
        /*
         * ── Responsive (mobile-first, Nextcloud breakpoints) ───────────
         * Base (<768px): stacked forms, card-style tables, full-width inputs.
         * 768px+: tablet/desktop chrome (legacy layout was default above 720px).
         * 1024px+: same as 768px+ — desktop is pixel-identical to pre-refactor
         *   styles; core #app-navigation uses its own toggle below 1024px.
         */
        
        /* 480px — two summary tiles per row on narrow phones in landscape */
        @media (min-width: 480px) {
        	.dc-summary-grid {
        		grid-template-columns: 1fr 1fr;
        	}
        }
        
        /*
         * 768px — Nextcloud tablet: restore multi-column forms, data tables, and
         * page chrome (matches pre-mobile-first layout from 720px upward).
         */
        @media (min-width: 768px) {
        	#app-content.dc-app #app-content-wrapper.dc-shell,
        	.dc-shell {
        		padding: var(--dc-space-5) var(--dc-space-5) var(--dc-space-7);
        	}
        	.dc-card {
        		padding: var(--dc-space-5);
        	}
        	.dc-page-header__main {
        		grid-template-columns: 64px 1fr auto;
        	}
        	.dc-page-header__actions {
        		grid-column: auto;
        	}
        	.dc-page-header__icon {
        		width: 56px;
        		height: 56px;
        	}
        	.dc-page-header__icon-svg {
        		width: 32px;
        		height: 32px;
        		min-width: 32px;
        		min-height: 32px;
        	}
        	.dc-form-grid,
        	.dc-filter-grid {
        		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        	}
        	.dc-form-grid--employees {
        		grid-template-columns: repeat(2, minmax(0, 1fr));
        	}
        	.dc-form-grid--employees > .dc-field--employee-name {
        		grid-column: 1 / 2;
        	}
        	.dc-form-grid--employees > .dc-field--employee-active {
        		grid-column: 2 / 3;
        	}
        	.dc-form-stack__grid--2 {
        		grid-template-columns: repeat(2, minmax(0, 1fr));
        	}
        	.dc-form-stack__grid--2 > .dc-field--location-active {
        		align-self: end;
        	}
        	.dc-roster-form__extras {
        		grid-template-columns: repeat(2, minmax(0, 1fr));
        		column-gap: var(--dc-space-4);
        	}
        	.dc-roster-form__extras > .dc-roster-form__section-heading {
        		grid-column: 1 / -1;
        	}
        	.dc-roster-form__extras > .dc-field--roster-break {
        		grid-column: 1 / 2;
        	}
        	.dc-roster-form__extras > .dc-field--roster-note {
        		grid-column: 2 / -1;
        	}
        	.dc-roster-form__step-group:has(.dc-field--roster-employee) {
        		display: grid;
        		grid-template-columns: repeat(2, minmax(0, 1fr));
        		column-gap: var(--dc-space-4);
        		row-gap: var(--dc-space-3);
        	}
        	.dc-roster-form__step-group:has(.dc-field--roster-employee) > .dc-roster-form__section-heading,
        	.dc-roster-form__step-group:has(.dc-field--roster-employee) > .dc-field--roster-employee,
        	.dc-roster-form__step-group:has(.dc-field--roster-employee) > .dc-field--roster-location {
        		grid-column: 1 / -1;
        	}
        	.dc-roster-form__step-group:has(.dc-field--roster-start) {
        		display: grid;
        		grid-template-columns: repeat(2, minmax(0, 1fr));
        		column-gap: var(--dc-space-4);
        		row-gap: var(--dc-space-3);
        	}
        	.dc-roster-form__step-group:has(.dc-field--roster-start) > .dc-roster-form__section-heading,
        	.dc-roster-form__step-group:has(.dc-field--roster-start) > .dc-field--roster-time-hint,
        	.dc-roster-form__step-group:has(.dc-field--roster-start) > .dc-field--roster-rest-hint {
        		grid-column: 1 / -1;
        	}
        	.dc-form-grid--my-roster-filter {
        		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) auto;
        	}
        	.dc-timezone-picker {
        		max-width: 36rem;
        	}
        	.dc-summary-grid {
        		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        	}
        	.dc-section__header {
        		grid-template-columns: minmax(0, 1fr) auto;
        	}
        	.dc-section__controls {
        		justify-self: end;
        	}
        	.dc-section__header > .dc-hint-dismiss {
        		justify-self: end;
        		text-align: inherit;
        	}
        	.dc-conflict {
        		grid-template-columns: auto minmax(0, 1fr) auto;
        	}
        	.dc-conflict__actions {
        		grid-column: auto;
        	}
        	.dc-ical__actions .button {
        		flex: 0 1 auto;
        		min-width: 0;
        	}
        	.dc-table-wrap {
        		border: 1px solid var(--dc-border);
        		background: var(--dc-bg-card);
        	}
        	.dc-table {
        		display: table;
        		width: 100%;
        		border-collapse: collapse;
        	}
        	.dc-table thead {
        		position: static;
        		width: auto;
        		height: auto;
        		margin: 0;
        		padding: 0;
        		overflow: visible;
        		clip: auto;
        		white-space: normal;
        		border: 0;
        		display: table-header-group;
        	}
        	.dc-table tbody {
        		display: table-row-group;
        	}
        	.dc-table tr {
        		display: table-row;
        		width: auto;
        	}
        	.dc-table td,
        	.dc-table th {
        		display: table-cell;
        		width: auto;
        		padding: var(--dc-space-3) var(--dc-space-4);
        		border-bottom: 1px solid var(--dc-border);
        	}
        	.dc-table tbody tr {
        		display: table-row;
        		gap: 0;
        		padding: 0;
        		margin-bottom: 0;
        		border: 0;
        		border-radius: 0;
        		background: transparent;
        	}
        	.dc-table tbody tr:nth-child(even) td {
        		background: color-mix(in srgb, var(--dc-bg-card) 96%, var(--color-main-text, #1d1d1d) 4%);
        	}
        	.dc-table td[data-cell]::before,
        	.dc-table tbody th.dc-table__rowhead[data-cell]::before {
        		content: none;
        		display: none;
        	}
        	.dc-table .dc-row-actions {
        		padding-top: 0;
        	}
        	.dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell,
        	.dc-table tbody tr:has(td[colspan]) td[colspan] {
        		display: table-cell;
        		text-align: center;
        		vertical-align: middle;
        		padding: var(--dc-space-7) var(--dc-space-4);
        	}
        	.dc-table tbody tr.dc-table__empty-row td.dc-table__empty-cell .dc-roster-empty-state {
        		margin-inline: auto;
        	}
        	.dc-modal__dialog {
        		padding: var(--dc-space-5);
        	}
        }
        
        /* Employee self-service (my-roster / my-absences) */
        .dc-employee-crossnav {
        	margin-top: var(--dc-space-4);
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	align-items: center;
        }
        .dc-employee-crossnav .button {
        	white-space: normal;
        	text-align: center;
        	min-height: 44px;
        	line-height: var(--dc-line-base);
        }
        .dc-employee-alert {
        	margin-bottom: 0;
        }
        
        .dc-summary-tile--action {
        	text-decoration: none;
        	color: inherit;
        	border: 1px solid var(--dc-border);
        	box-sizing: border-box;
        	justify-content: center;
        	text-align: center;
        	min-height: 100%;
        	transition: background 0.15s ease, border-color 0.15s ease;
        }
        a.dc-summary-tile--action:hover {
        	background: var(--dc-tint-info);
        	border-color: transparent;
        }
        a.dc-summary-tile--action:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 2px;
        }
        .dc-summary-tile--action .dc-summary-tile__value {
        	font-size: var(--dc-fs-lg);
        }
        
        .dc-table th.dc-table__rowhead {
        	text-align: left;
        	font-weight: 600;
        	color: var(--color-main-text, #1d1d1d);
        	vertical-align: middle;
        	border-bottom: 1px solid var(--dc-border);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	background: var(--dc-bg-card);
        }
        
        .dc-fieldset-reset {
        	border: 0;
        	margin: 0;
        	padding: 0;
        	min-width: 0;
        }
        .dc-form-grid--my-absence fieldset.dc-fieldset-reset {
        	display: contents;
        }
        
        form.dc-form-grid[aria-busy='true'] {
        	opacity: 0.92;
        	pointer-events: none;
        }
        form.dc-form-grid[aria-busy='true'] .button {
        	cursor: wait;
        }
        
        /* My-roster quick-range chips + custom range form */
        .dc-quickfilters {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	margin-bottom: var(--dc-space-3);
        }
        .dc-quickfilters__btn {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	min-height: 44px;
        	padding: 0 var(--dc-space-4);
        	border-radius: 999px;
        	border: 1px solid var(--dc-border-strong);
        	background: var(--color-main-background, #fff);
        	color: var(--color-main-text, #1d1d1d);
        	cursor: pointer;
        	font-weight: 600;
        	font-size: var(--dc-fs-sm);
        	line-height: 1;
        	transition: background 0.15s ease, border-color 0.15s ease, color 0.15s ease;
        }
        .dc-quickfilters__btn:hover {
        	border-color: var(--color-primary-element, #0082c9);
        }
        .dc-quickfilters__btn:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 55%, transparent);
        	outline-offset: 2px;
        }
        .dc-quickfilters__btn[aria-pressed='true'] {
        	background: var(--color-primary-element, #0082c9);
        	color: var(--color-primary-element-text, #fff);
        	border-color: var(--color-primary-element, #0082c9);
        	box-shadow: var(--dc-shadow-sm);
        }
        .dc-quickfilters__btn[disabled] {
        	opacity: 0.55;
        	cursor: not-allowed;
        }
        
        .dc-form-grid--my-roster-filter {
        	display: grid;
        	grid-template-columns: 1fr;
        	gap: var(--dc-space-3);
        	align-items: end;
        }
        .dc-form-actions--my-roster-filter {
        	display: flex;
        	align-items: end;
        	justify-content: flex-start;
        }
        .dc-form-actions--my-roster-filter .button {
        	min-height: 44px;
        }
        
        /* ── Administrator: roster export (screen) ─────────────────── */
        .dc-roster-export {
        	border-inline-start: 4px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 45%, var(--color-main-background, #fff));
        	background: var(--dc-tint-info);
        }
        .dc-roster-export__actions {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-3);
        	align-items: center;
        	margin-top: var(--dc-space-3);
        }
        .dc-roster-export__actions .button {
        	min-height: 44px;
        }
        .dc-roster-export__hint {
        	margin: 0;
        	color: var(--color-main-text, #1d1d1d);
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        	max-width: 52rem;
        }
        .dc-roster-export a.button[aria-disabled='true'] {
        	opacity: 0.6;
        	cursor: not-allowed;
        }
        
        /* ── Printable roster (blank layout) ───────────────────────── */
        .dc-html-print,
        .dc-body-print {
        	margin: 0;
        	background: var(--color-main-background, #fff);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-app--roster-print {
        	max-width: 72rem;
        	margin: 0 auto;
        	padding: var(--dc-space-5) var(--dc-space-4) var(--dc-space-7);
        	box-sizing: border-box;
        }
        .dc-print-header {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-4);
        	align-items: flex-start;
        	justify-content: space-between;
        	margin-bottom: var(--dc-space-5);
        	padding-bottom: var(--dc-space-4);
        	border-bottom: 2px solid var(--dc-border-strong);
        }
        .dc-print-header__brand {
        	flex: 0 0 auto;
        	display: flex;
        	align-items: center;
        	justify-content: center;
        	width: 48px;
        	height: 48px;
        	border-radius: var(--dc-radius-md);
        	background: var(--dc-tint-info);
        	color: var(--color-primary-element, #0082c9);
        }
        .dc-print-header__icon {
        	width: 28px;
        	height: 28px;
        }
        .dc-print-header__text {
        	flex: 1 1 240px;
        	min-width: 0;
        }
        .dc-print-title {
        	margin: 0 0 var(--dc-space-2);
        	font-size: var(--dc-fs-2xl);
        	line-height: var(--dc-line-tight);
        	font-weight: 700;
        }
        .dc-print-meta {
        	margin: 0;
        	font-size: var(--dc-fs-md);
        	line-height: var(--dc-line-base);
        	color: var(--color-main-text, #1d1d1d);
        }
        .dc-print-meta--sub {
        	margin-top: var(--dc-space-2);
        	font-size: var(--dc-fs-sm);
        	color: var(--dc-muted);
        }
        .dc-print-toolbar {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	align-items: center;
        }
        .dc-print-footnote {
        	font-size: var(--dc-fs-sm);
        	color: var(--dc-muted);
        	margin: 0 0 var(--dc-space-3);
        	padding: var(--dc-space-3);
        	background: var(--dc-bg-stripe);
        	border-radius: var(--dc-radius-sm);
        	border: 1px solid var(--dc-border);
        }
        .dc-print-table-wrap {
        	overflow-x: auto;
        	-webkit-overflow-scrolling: touch;
        }
        .dc-print-table {
        	width: 100%;
        	border-collapse: collapse;
        	font-size: var(--dc-fs-sm);
        }
        .dc-print-table th,
        .dc-print-table td {
        	text-align: left;
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border: 1px solid var(--dc-border-strong);
        	vertical-align: top;
        }
        .dc-print-table th {
        	background: var(--dc-bg-stripe);
        	font-weight: 700;
        }
        .dc-print-table__empty {
        	text-align: center;
        	color: var(--dc-muted);
        	padding: var(--dc-space-5);
        }
        .dc-print-overnight {
        	font-weight: 700;
        	color: var(--color-main-text, #1d1d1d);
        	margin-inline-start: var(--dc-space-1);
        }
        .dc-print-legend {
        	font-size: var(--dc-fs-xs);
        	color: var(--dc-muted);
        	margin-top: var(--dc-space-2);
        }
        .print-only {
        	display: none;
        }
        
        @media (min-width: 768px) {
        	#dc-assignments-table-wrap {
        		border-radius: var(--dc-radius-sm);
        		background: var(--dc-bg-card);
        	}
        	#dc-assignments-table-wrap .dc-table {
        		margin-bottom: 0;
        	}
        	#dc-assignments-table-wrap .dc-roster-empty-panel {
        		border-top: 1px solid var(--dc-border);
        		border-radius: 0 0 var(--dc-radius-sm) var(--dc-radius-sm);
        	}
        }
        .dc-app--roster-print-error .dc-print-error {
        	max-width: 36rem;
        	margin: var(--dc-space-7) auto;
        	padding: var(--dc-space-6);
        	background: var(--dc-bg-card);
        	border-radius: var(--dc-radius-lg);
        	box-shadow: var(--dc-shadow-md);
        	border: 1px solid var(--dc-border);
        	text-align: center;
        }
        .dc-print-error__icon {
        	display: flex;
        	justify-content: center;
        	margin-bottom: var(--dc-space-3);
        	color: var(--color-primary-element, #0082c9);
        }
        .dc-print-error__message {
        	font-size: var(--dc-fs-md);
        	line-height: var(--dc-line-base);
        }
        @media print {
        	.no-print {
        		display: none !important;
        	}
        	.print-only {
        		display: block;
        	}
        	.dc-app--roster-print {
        		max-width: none;
        		padding: 0;
        	}
        	.dc-print-table th {
        		background: var(--dc-bg-stripe) !important;
        		-webkit-print-color-adjust: exact;
        		print-color-adjust: exact;
        	}
        	a[href]::after {
        		content: none !important;
        	}
        	/* REQ-UX-13: must win over the global wipe — expand AT recovery / source outbound URLs. */
        	.dc-table a[href]::after,
        	.dc-conflict__actions a[href]::after,
        	.dc-row-actions a[href]::after {
        		content: " (" attr(href) ")" !important;
        		font-size: 0.85em;
        		word-break: break-all;
        	}
        }
        @media (prefers-reduced-motion: reduce) {
        	.dc-roster-export,
        	.dc-print-header {
        		transition: none;
        	}
        }
        
        /* === support-us:dc start === */
        /**
         * Support & Us — shared family styles (WCAG 2.1 AA, responsive).
         * Prefix: dc
         * Maps to Nextcloud --color-* so light / dark / high-contrast / custom accents adapt.
         */
        
        .dc-support-us {
        	margin-block-start: var(--dc-space-5, 1.5rem);
        	min-width: 0;
        	width: 100%;
        	box-sizing: border-box;
        }
        
        .dc-support-us--page {
        	margin-block-start: 0;
        	border: 0;
        	background: transparent;
        	max-width: none;
        }
        
        .dc-support-us__header {
        	margin-block-end: var(--dc-space-4, 1rem);
        }
        
        .dc-support-us__title {
        	margin: 0 0 var(--dc-space-2, 0.5rem);
        	font-size: var(--dc-fs-xl, 1.5rem);
        	font-weight: 700;
        	line-height: var(--dc-line-tight, 1.2);
        	color: var(--color-main-text);
        	overflow-wrap: anywhere;
        }
        
        .dc-support-us__intro {
        	margin: 0;
        	max-width: 65ch;
        	font-size: var(--dc-fs-md, 1rem);
        	line-height: var(--dc-line-base, 1.45);
        	color: var(--color-text-maxcontrast);
        	overflow-wrap: anywhere;
        }
        
        .dc-support-us__body {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-5, 1.5rem);
        	min-width: 0;
        	width: 100%;
        }
        
        .dc-support-us__primary {
        	display: flex;
        	flex-direction: column;
        	align-items: flex-start;
        	gap: var(--dc-space-3, 0.75rem);
        	min-width: 0;
        	width: 100%;
        	padding: var(--dc-space-4, 1rem);
        	border: 1px solid var(--color-border-maxcontrast, var(--color-border));
        	border-inline-start: 4px solid var(--color-primary-element);
        	border-radius: var(--dc-radius-md, var(--border-radius-large, 12px));
        	background: color-mix(in srgb, var(--color-primary-element) 10%, var(--color-main-background));
        	box-sizing: border-box;
        }
        
        .dc-support-us__primary-copy,
        .dc-support-us__primary-actions {
        	display: flex;
        	flex-direction: column;
        	align-items: flex-start;
        	gap: var(--dc-space-3, 0.75rem);
        	min-width: 0;
        	width: 100%;
        }
        
        .dc-support-us__offer-title {
        	margin: 0;
        	font-size: var(--dc-fs-lg, 1.125rem);
        	font-weight: 700;
        	line-height: var(--dc-line-tight, 1.25);
        	color: var(--color-main-text);
        	overflow-wrap: anywhere;
        }
        
        .dc-support-us__benefit,
        .dc-support-us__coverage,
        .dc-support-us__hint {
        	margin: 0;
        	max-width: 65ch;
        	font-size: var(--dc-fs-sm, 0.875rem);
        	line-height: var(--dc-line-base, 1.45);
        	overflow-wrap: anywhere;
        }
        
        .dc-support-us__benefit {
        	font-size: var(--dc-fs-md, 1rem);
        	color: var(--color-main-text);
        }
        
        .dc-support-us__coverage,
        .dc-support-us__hint {
        	color: var(--color-text-maxcontrast);
        }
        
        .dc-support-us__hint a {
        	color: var(--color-main-text);
        	text-decoration: underline;
        	text-underline-offset: 0.15em;
        }
        
        .dc-support-us__secondary {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3, 0.75rem);
        	min-width: 0;
        	width: 100%;
        }
        
        .dc-support-us__secondary-title {
        	margin: 0;
        	font-size: var(--dc-fs-md, 1rem);
        	font-weight: 600;
        	line-height: var(--dc-line-tight, 1.25);
        	color: var(--color-main-text);
        }
        
        .dc-support-us__options {
        	display: grid;
        	grid-template-columns: repeat(auto-fit, minmax(min(100%, 16rem), 1fr));
        	gap: var(--dc-space-4, 1rem);
        	width: 100%;
        	min-width: 0;
        }
        
        .dc-support-us__option {
        	display: flex;
        	flex-direction: column;
        	align-items: stretch;
        	gap: var(--dc-space-3, 0.75rem);
        	min-width: 0;
        	padding: var(--dc-space-4, 1rem);
        	border: 1px solid var(--color-border);
        	border-radius: var(--dc-radius-md, var(--border-radius-large, 12px));
        	background: var(--color-main-background);
        	box-sizing: border-box;
        }
        
        .dc-support-us__option-title {
        	margin: 0;
        	font-size: var(--dc-fs-lg, 1.125rem);
        	font-weight: 700;
        	line-height: var(--dc-line-tight, 1.25);
        	color: var(--color-main-text);
        	overflow-wrap: anywhere;
        }
        
        .dc-support-us__option-hint {
        	margin: 0;
        	flex: 1 1 auto;
        	font-size: var(--dc-fs-sm, 0.875rem);
        	line-height: var(--dc-line-base, 1.45);
        	color: var(--color-text-maxcontrast);
        	overflow-wrap: anywhere;
        }
        
        .dc-support-us__cta {
        	/* WCAG 2.5.5: px + !important — NC root is 15px and .azc-btn uses 2.75rem (41.25px). */
        	min-height: 44px !important;
        	min-width: 44px !important;
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	padding-inline: var(--dc-space-4, 1rem);
        	text-align: center;
        	text-decoration: none;
        	box-sizing: border-box;
        	white-space: normal;
        	overflow-wrap: anywhere;
        	max-width: 100%;
        }
        
        .dc-support-us__cta--primary {
        	min-height: 48px !important;
        }
        
        .dc-support-us__option .dc-support-us__cta,
        .dc-support-us--page .dc-support-us__primary-actions .dc-support-us__cta {
        	align-self: stretch;
        	width: 100%;
        }
        
        .dc-support-us__cta:focus-visible,
        .dc-support-us__hint a:focus-visible,
        .dc-support-us__more a:focus-visible,
        .dc-support-us__contact a:focus-visible {
        	outline: 2px solid var(--color-primary-element);
        	outline-offset: 2px;
        }
        
        .dc-support-us__cta:focus:not(:focus-visible),
        .dc-support-us__hint a:focus:not(:focus-visible),
        .dc-support-us__more a:focus:not(:focus-visible),
        .dc-support-us__contact a:focus:not(:focus-visible) {
        	outline: none;
        }
        
        .dc-support-us__tertiary {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2, 0.5rem);
        	padding-block-start: var(--dc-space-4, 1rem);
        	border-block-start: 1px solid var(--color-border);
        }
        
        .dc-support-us__more,
        .dc-support-us__contact {
        	margin: 0;
        	font-size: var(--dc-fs-sm, 0.875rem);
        	line-height: var(--dc-line-base, 1.45);
        	color: var(--color-text-maxcontrast);
        	overflow-wrap: anywhere;
        }
        
        .dc-support-us__more a,
        .dc-support-us__contact a {
        	color: var(--color-main-text);
        	text-decoration: underline;
        	text-underline-offset: 0.15em;
        }
        
        .dc-support-us__new-tab {
        	position: absolute;
        	width: 1px;
        	height: 1px;
        	padding: 0;
        	margin: -1px;
        	overflow: hidden;
        	clip: rect(0, 0, 0, 0);
        	clip-path: inset(50%);
        	white-space: nowrap;
        	border: 0;
        }
        
        @media (max-width: 768px) {
        	.dc-support-us__options {
        		grid-template-columns: 1fr;
        	}
        
        	.dc-support-us__cta,
        	.dc-support-us__primary,
        	.dc-support-us__option {
        		width: 100%;
        		max-width: none;
        	}
        }
        
        @media (prefers-contrast: more) {
        	.dc-support-us__primary {
        		background: var(--color-main-background);
        		border-color: var(--color-main-text);
        		border-inline-start-width: 4px;
        	}
        
        	.dc-support-us__option {
        		border-width: 2px;
        		border-color: var(--color-main-text);
        	}
        
        	.dc-support-us__cta:focus-visible,
        	.dc-support-us__hint a:focus-visible,
        	.dc-support-us__more a:focus-visible,
        	.dc-support-us__contact a:focus-visible {
        		outline-width: 3px;
        	}
        }
        
        @media (forced-colors: active) {
        	.dc-support-us__primary,
        	.dc-support-us__option {
        		border: 2px solid CanvasText;
        		background: Canvas;
        	}
        
        	.dc-support-us__cta:focus-visible,
        	.dc-support-us__hint a:focus-visible,
        	.dc-support-us__more a:focus-visible,
        	.dc-support-us__contact a:focus-visible {
        		outline: 3px solid Highlight;
        	}
        }
        
        @media (prefers-reduced-motion: reduce) {
        	.dc-support-us__cta,
        	.dc-support-us__option {
        		transition: none;
        	}
        }
        /* === support-us:dc end === */
        
        
        /* My roster: accessible swap dialog (replaces window.prompt) */
        /* Nextcloud core server.css applies a legacy HTML5 reset (`dialog { display: block }`)
           that defeats the UA's `dialog:not([open]) { display: none }` — re-hide closed dialogs. */
        .dc-dialog:not([open]) {
        	display: none;
        }
        .dc-dialog {
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	padding: 0;
        	max-width: min(28rem, calc(100vw - 2rem));
        	width: 100%;
        	background: var(--dc-bg-card);
        	color: var(--color-main-text, #1d1d1d);
        	box-shadow: var(--dc-shadow-md);
        }
        .dc-dialog::backdrop {
        	/* Theme-safe overlay: never a hardcoded blue cast. */
        	background: color-mix(in srgb, var(--color-main-text, #000) 45%, transparent);
        }
        .dc-dialog__panel {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	padding: var(--dc-space-5) var(--dc-space-5) var(--dc-space-4);
        	margin: 0;
        }
        .dc-dialog__title {
        	margin: 0;
        	font-size: var(--dc-fs-lg);
        	line-height: var(--dc-line-tight);
        }
        .dc-dialog__intro {
        	margin: 0;
        	color: var(--dc-muted);
        	line-height: var(--dc-line-base);
        }
        .dc-dialog__actions {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	justify-content: flex-end;
        	margin-top: var(--dc-space-1);
        }
        .dc-dialog__actions .button {
        	min-height: 44px;
        }
        
        /* Settings sub-pages: in-page chip bar (DeskCheck parity). Critical when
           Nextcloud collapses #app-navigation on narrow viewports — without it,
           sibling settings pages would be unreachable on phones/tablets. */
        .dc-settings-nav {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	margin: 0 0 var(--dc-space-5);
        	padding: 0 0 var(--dc-space-3);
        	border-bottom: 1px solid var(--dc-border);
        }
        .dc-app .dc-settings-nav__link {
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	min-height: 44px;
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-sm);
        	background: var(--color-main-background, #fff);
        	color: var(--color-main-text, #222);
        	text-decoration: none;
        	font-weight: 600;
        	font-size: var(--dc-fs-sm);
        	line-height: 1.2;
        }
        .dc-app .dc-settings-nav__link:hover {
        	border-color: var(--color-primary-element, #0082c9);
        	background: var(--dc-bg-stripe);
        	color: var(--color-main-text, #222);
        }
        .dc-app .dc-settings-nav__link:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 2px;
        }
        .dc-app .dc-settings-nav__link.is-active,
        .dc-app .dc-settings-nav__link[aria-current="page"] {
        	/* Keep main-text ink (not primary) — primary-on-dark-tint failed axe at 4.28:1. */
        	border-color: var(--color-primary-element, #0082c9);
        	background: var(--dc-tint-info);
        	color: var(--color-main-text, #222);
        	font-weight: 700;
        }
        .dc-inline-link {
        	color: var(--color-primary-element, #0082c9);
        	text-decoration: underline;
        	text-underline-offset: 2px;
        }
        .dc-inline-link:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 2px;
        }
        @media (max-width: 640px) {
        	.dc-settings-nav {
        		flex-direction: column;
        	}
        	.dc-app .dc-settings-nav__link {
        		width: 100%;
        		justify-content: flex-start;
        	}
        }
        
        /* Settings sub-pages: sidebar sub-navigation expanded while on a settings page.
           One entry per sub-page; the active child carries aria-current="page". */
        .dc-nav__sublist {
        	list-style: none;
        	margin: 2px 0 var(--dc-space-2) var(--dc-space-3);
        	/* Indent past the 24px icon column so children read as nested; the
        	   vertical guide line makes the hierarchy obvious at a glance. */
        	padding: 0 0 0 calc(24px + var(--dc-space-2) - 2px);
        	display: flex;
        	flex-direction: column;
        	gap: 2px;
        	border-left: 2px solid var(--dc-border);
        }
        .dc-nav__subitem { margin: 0; }
        .dc-nav__sublink {
        	display: flex;
        	align-items: center;
        	min-height: 44px;
        	padding: var(--dc-space-1) var(--dc-space-3);
        	border-radius: var(--dc-radius-sm);
        	color: var(--color-main-text, #1d1d1d);
        	text-decoration: none;
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-tight);
        	overflow-wrap: anywhere;
        }
        .dc-nav__sublink:hover {
        	background: var(--dc-bg-stripe);
        }
        .dc-nav__sublink:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element, #0082c9) 60%, transparent);
        	outline-offset: 2px;
        }
        .dc-nav__subitem.is-active .dc-nav__sublink {
        	background: var(--dc-tint-info);
        	font-weight: 600;
        }
        
        /* Wave A planner: copy-period + ack stats — use design tokens, never raw hex */
        .dc-roster-copy-period {
        	margin-top: var(--dc-space-4);
        	padding-top: var(--dc-space-3);
        	border-top: 1px solid var(--dc-border);
        }
        .dc-roster-copy-period__row {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	align-items: center;
        }
        .dc-roster-copy-period__row .dc-input {
        	min-width: min(100%, 16rem);
        	flex: 1 1 12rem;
        }
        .dc-roster-ack-stats {
        	margin-top: var(--dc-space-2);
        	display: inline-block;
        }
        .dc-table__actions {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-1);
        	align-items: center;
        }
        @media (max-width: 719px) {
        	.dc-roster-copy-period__row {
        		flex-direction: column;
        		align-items: stretch;
        	}
        }
        
        /* Settings: named location checklists (planner scope) */
        .dc-fieldset {
        	margin: 0;
        	padding: 0;
        	border: 0;
        	min-width: 0;
        }
        .dc-check-grid {
        	display: grid;
        	grid-template-columns: repeat(auto-fill, minmax(12rem, 1fr));
        	gap: var(--dc-space-2) var(--dc-space-4);
        	margin-top: var(--dc-space-2);
        }
        .dc-check {
        	display: flex;
        	align-items: flex-start;
        	gap: var(--dc-space-2);
        	min-height: var(--dc-touch);
        	padding: var(--dc-space-1) var(--dc-space-1);
        	line-height: var(--dc-line-base);
        	cursor: pointer;
        }
        .dc-check input[type='checkbox'] {
        	/* size / theme chrome shared with .dc-checkbox above */
        	margin-top: 0.15rem;
        }
        .dc-check:focus-within {
        	outline: 2px solid var(--color-primary-element, #0082c9);
        	outline-offset: 2px;
        	border-radius: var(--dc-radius-sm);
        }
        
        /* Roster planning grid (employee × day) */
        .dc-roster-view-toggle {
        	display: inline-flex;
        	gap: var(--dc-space-1);
        	margin-inline-end: var(--dc-space-2);
        }
        .dc-roster-virtual-status {
        	margin: var(--dc-space-2) 0 0;
        	padding: 0;
        	color: var(--color-main-text);
        	font-size: var(--dc-fs-md, 1rem);
        	line-height: var(--dc-line-base, 1.5);
        	overflow-wrap: anywhere;
        	min-height: 1.5em;
        }
        .dc-roster-grid-wrap {
        	margin-top: var(--dc-space-3);
        }
        .dc-roster-grid-scroller,
        .dc-roster-list-scroller {
        	overflow: auto;
        	overscroll-behavior: contain;
        	-webkit-overflow-scrolling: touch;
        	max-height: min(70vh, 40rem);
        	max-height: min(70dvh, 40rem);
        	border: 1px solid var(--color-border, var(--dc-border));
        	border-radius: var(--dc-radius-md);
        	background: var(--color-main-background);
        	scrollbar-gutter: stable;
        }
        .dc-windowed-table {
        	overflow: auto;
        	overscroll-behavior: contain;
        	-webkit-overflow-scrolling: touch;
        	max-height: min(70vh, 40rem);
        	max-height: min(70dvh, 40rem);
        	border: 1px solid var(--color-border, var(--dc-border));
        	border-radius: var(--dc-radius-md);
        	background: var(--color-main-background);
        }
        .dc-roster-list-scroller {
        	margin-top: var(--dc-space-2);
        }
        .dc-virtual-spacer {
        	display: block;
        	margin: 0;
        	padding: 0;
        	border: 0;
        	background: transparent;
        	pointer-events: none;
        	line-height: 0;
        	font-size: 0;
        }
        .dc-roster-grid__spacer {
        	width: 100%;
        }
        /* Roster list stays a real table at every width (WCAG 1.4.10 data-table exception).
           Card-stacking would make row heights variable and desync the virtual window. */
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table {
        	display: table;
        	width: max(100%, 52rem);
        	border-collapse: collapse;
        	table-layout: fixed;
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table thead {
        	display: table-header-group;
        	position: sticky;
        	top: 0;
        	z-index: 2;
        	width: auto;
        	height: auto;
        	margin: 0;
        	padding: 0;
        	overflow: visible;
        	clip: auto;
        	white-space: normal;
        	border: 0;
        	background: var(--color-main-background);
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody {
        	display: table-row-group;
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tr {
        	display: table-row;
        	width: auto;
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table th,
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table td {
        	display: table-cell;
        	width: auto;
        	min-height: var(--dc-touch);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-bottom: 1px solid var(--dc-border, var(--color-border));
        	white-space: nowrap;
        	overflow: hidden;
        	text-overflow: ellipsis;
        	vertical-align: middle;
        	text-align: start;
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table td[data-cell]::before {
        	content: none;
        	display: none;
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody tr:not(.dc-virtual-spacer) {
        	display: table-row;
        	gap: 0;
        	margin: 0;
        	padding: 0;
        	border: 0;
        	border-radius: 0;
        	background: transparent;
        	min-height: var(--dc-touch);
        	height: var(--dc-touch);
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody tr.dc-virtual-spacer {
        	display: table-row;
        	margin: 0;
        	padding: 0;
        	border: 0;
        	background: transparent;
        	min-height: 0;
        	height: auto;
        	pointer-events: none;
        }
        #dc-assignments-table-wrap.dc-roster-list-scroller .dc-table tbody tr.dc-virtual-spacer td {
        	display: table-cell;
        	padding: 0 !important;
        	border: 0 !important;
        	background: transparent !important;
        	line-height: 0;
        	font-size: 0;
        	overflow: hidden;
        }
        /* Catalog lists (employees / locations / absences): real table at every width so
           virtual row heights stay uniform (same exception as the roster list, FM-19). */
        .dc-windowed-table .dc-table {
        	display: table;
        	width: max(100%, 40rem);
        	border-collapse: collapse;
        	table-layout: fixed;
        }
        .dc-windowed-table .dc-table thead {
        	display: table-header-group;
        	position: sticky;
        	top: 0;
        	z-index: 2;
        	width: auto;
        	height: auto;
        	margin: 0;
        	padding: 0;
        	overflow: visible;
        	clip: auto;
        	white-space: normal;
        	border: 0;
        	background: var(--color-main-background);
        }
        .dc-windowed-table .dc-table tbody {
        	display: table-row-group;
        }
        .dc-windowed-table .dc-table tr {
        	display: table-row;
        	width: auto;
        }
        .dc-windowed-table .dc-table th,
        .dc-windowed-table .dc-table td {
        	display: table-cell;
        	width: auto;
        	min-height: var(--dc-touch);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border-bottom: 1px solid var(--dc-border, var(--color-border));
        	white-space: nowrap;
        	overflow: hidden;
        	text-overflow: ellipsis;
        	vertical-align: middle;
        	text-align: start;
        }
        .dc-windowed-table .dc-table td[data-cell]::before {
        	content: none;
        	display: none;
        }
        .dc-windowed-table .dc-table tbody tr:not(.dc-virtual-spacer) {
        	display: table-row;
        	gap: 0;
        	margin: 0;
        	padding: 0;
        	border: 0;
        	border-radius: 0;
        	background: transparent;
        	min-height: var(--dc-touch);
        	height: var(--dc-touch);
        }
        .dc-windowed-table .dc-table tbody tr.dc-virtual-spacer {
        	display: table-row;
        	margin: 0;
        	padding: 0;
        	border: 0;
        	background: transparent;
        	min-height: 0;
        	height: auto;
        	pointer-events: none;
        }
        .dc-windowed-table .dc-table tbody tr.dc-virtual-spacer td {
        	display: table-cell;
        	padding: 0 !important;
        	border: 0 !important;
        	background: transparent !important;
        	line-height: 0;
        	font-size: 0;
        	overflow: hidden;
        }
        .dc-roster-grid {
        	/* Grow with day columns so month periods scroll horizontally instead of wrapping. */
        	--dc-roster-day-min: 4.5rem;
        	min-width: 100%;
        	width: max-content;
        	outline: none;
        }
        .dc-roster-grid:focus-visible {
        	outline: 2px solid var(--color-primary-element, #0082c9);
        	outline-offset: 2px;
        }
        .dc-roster-grid__table {
        	display: grid;
        	gap: 0;
        	background: var(--color-main-background);
        }
        .dc-roster-grid__row {
        	/* Explicit day count (set by JS). auto-fit collapsed month headers into overlapping piles. */
        	display: grid;
        	grid-template-columns: minmax(7rem, 10rem) repeat(auto-fit, minmax(4.5rem, 1fr));
        	min-width: max(100%, calc(10rem + (var(--dc-roster-day-count, 7) * var(--dc-roster-day-min))));
        	border-bottom: 1px solid var(--color-border, var(--dc-border));
        }
        .dc-roster-grid__row:last-child {
        	border-bottom: 0;
        }
        .dc-roster-grid__row--head {
        	position: sticky;
        	top: 0;
        	z-index: 3;
        	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 8%, var(--color-main-background));
        	font-weight: 600;
        	box-shadow: 0 1px 0 color-mix(in srgb, var(--color-border, var(--dc-border)) 80%, transparent);
        }
        .dc-roster-grid__corner {
        	position: sticky;
        	left: 0;
        	z-index: 4;
        	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 8%, var(--color-main-background));
        }
        .dc-roster-grid__rowhead {
        	position: sticky;
        	left: 0;
        	z-index: 1;
        	background: var(--color-main-background);
        	font-weight: 600;
        }
        .dc-roster-grid__corner,
        .dc-roster-grid__colhead,
        .dc-roster-grid__rowhead,
        .dc-roster-grid__cell {
        	min-height: var(--dc-touch);
        	padding: var(--dc-space-1) var(--dc-space-2);
        	border-inline-end: 1px solid var(--color-border, var(--dc-border));
        }
        .dc-roster-grid__colhead {
        	display: flex;
        	flex-direction: column;
        	align-items: center;
        	justify-content: center;
        	gap: 0.1rem;
        	text-align: center;
        	line-height: 1.15;
        	overflow: hidden;
        	white-space: nowrap;
        	font-variant-numeric: tabular-nums;
        	letter-spacing: 0.01em;
        }
        .dc-roster-grid__colhead-wd {
        	display: block;
        	font-size: var(--dc-fs-xs);
        	font-weight: 600;
        	color: var(--color-text-maxcontrast);
        }
        .dc-roster-grid__colhead-day {
        	display: block;
        	font-size: var(--dc-fs-sm);
        	font-weight: 700;
        	color: var(--color-main-text);
        }
        .dc-roster-grid__colhead--weekend,
        .dc-roster-grid__cell--weekend {
        	background: color-mix(in srgb, var(--color-main-text) 5%, var(--color-main-background));
        }
        .dc-roster-grid__row--head .dc-roster-grid__colhead--weekend {
        	background: color-mix(
        		in srgb,
        		var(--color-main-text) 6%,
        		color-mix(in srgb, var(--color-primary-element, #0082c9) 8%, var(--color-main-background))
        	);
        }
        .dc-roster-grid__colhead--today {
        	box-shadow: inset 0 -3px 0 var(--color-primary-element, #0082c9);
        }
        .dc-roster-grid__colhead--today .dc-roster-grid__colhead-day {
        	color: var(--color-primary-element-text, #ffffff);
        	background: var(--color-primary-element, #0082c9);
        	padding: 0.15rem 0.4rem;
        	border-radius: var(--dc-radius-sm);
        	min-width: 1.5rem;
        }
        .dc-roster-grid__cell--today {
        	box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-primary-element, #0082c9) 45%, transparent);
        }
        .dc-roster-grid--month .dc-roster-grid__colhead {
        	padding-inline: var(--dc-space-1);
        }
        .dc-roster-grid__cell:last-child,
        .dc-roster-grid__colhead:last-child {
        	border-inline-end: 0;
        }
        .dc-roster-grid__cell {
        	display: flex;
        	flex-direction: column;
        	align-items: stretch;
        	justify-content: center;
        	gap: var(--dc-space-1);
        	cursor: pointer;
        }
        .dc-roster-grid__cell:focus-visible,
        .dc-roster-grid__cell.is-selected {
        	outline: 2px solid var(--color-primary-element, #0082c9);
        	outline-offset: -2px;
        	background: color-mix(in srgb, var(--color-primary-element, #0082c9) 12%, var(--color-main-background));
        }
        .dc-roster-grid__cell--empty {
        	background: color-mix(in srgb, var(--color-main-background) 92%, var(--color-border, var(--dc-border)));
        }
        .dc-roster-grid__shift {
        	display: block;
        	min-height: calc(var(--dc-touch) - var(--dc-space-2));
        	border: 0;
        	border-radius: var(--dc-radius-sm);
        	background: color-mix(in srgb, var(--color-success, #2e7d32) 18%, var(--color-main-background));
        	color: var(--color-main-text);
        	font: inherit;
        	cursor: inherit;
        	text-align: center;
        	padding: var(--dc-space-1) var(--dc-space-1);
        	pointer-events: none;
        }
        .dc-roster-grid__more {
        	font-size: 0.75rem;
        	color: var(--color-text-maxcontrast);
        	text-align: center;
        }
        .dc-roster-bulk-bar {
        	margin-top: var(--dc-space-3);
        	padding: var(--dc-space-3);
        	border: 1px solid var(--color-border, var(--dc-border));
        	border-radius: var(--dc-radius-md);
        	background: var(--color-main-background);
        }
        .dc-roster-bulk-bar__row {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	align-items: center;
        	margin-top: var(--dc-space-2);
        }
        .dc-settings-privacy__list {
        	margin: 0 0 var(--dc-space-4);
        	padding-inline-start: 1.25rem;
        	line-height: 1.5;
        }
        .dc-settings-privacy__glossary {
        	display: grid;
        	gap: var(--dc-space-3);
        	margin: 0;
        	padding: 0;
        	min-width: 0;
        	max-width: 100%;
        }
        /* Same Nextcloud core dt/dd reset as .dc-scope-strip (130px end-aligned labels). */
        #app-content.dc-app .dc-settings-privacy__glossary dt,
        #app-content.dc-app .dc-settings-privacy__glossary dd,
        .dc-app .dc-settings-privacy__glossary dt,
        .dc-app .dc-settings-privacy__glossary dd {
        	display: block;
        	float: none;
        	width: auto;
        	max-width: 100%;
        	padding: 0;
        	text-align: start;
        	white-space: normal;
        }
        .dc-settings-privacy__glossary dt {
        	font-weight: 600;
        	overflow-wrap: anywhere;
        	margin: 0;
        	min-width: 0;
        }
        .dc-settings-privacy__glossary dd {
        	margin: 0.15rem 0 0;
        	color: var(--color-text-maxcontrast);
        	overflow-wrap: anywhere;
        	min-width: 0;
        }
        .dc-print-integrity {
        	margin-top: var(--dc-space-4);
        	padding-top: var(--dc-space-3);
        	border-top: 1px solid var(--color-border, var(--dc-border));
        	font-size: var(--dc-fs-sm);
        }
        .dc-print-integrity__value {
        	word-break: break-all;
        	font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
        }
        @media (max-width: 719px) {
        	.dc-roster-grid__row {
        		grid-template-columns: minmax(5.5rem, 7rem) repeat(var(--dc-roster-day-count, 7), minmax(var(--dc-roster-day-min, 3.75rem), 1fr));
        		min-width: max(100%, calc(7rem + (var(--dc-roster-day-count, 7) * var(--dc-roster-day-min, 3.75rem))));
        	}
        	.dc-roster-view-toggle {
        		width: 100%;
        		margin: 0 0 var(--dc-space-2);
        	}
        	.dc-roster-grid-scroller,
        	.dc-roster-list-scroller {
        		max-height: min(65vh, 28rem);
        		max-height: min(65dvh, 28rem);
        	}
        	.dc-windowed-table {
        		max-height: min(65vh, 28rem);
        		max-height: min(65dvh, 28rem);
        	}
        }
        @media (min-width: 768px) {
        	.dc-roster-list-scroller .dc-table thead {
        		position: sticky;
        		top: 0;
        		z-index: 2;
        		background: var(--color-main-background);
        	}
        }
        @media print {
        	.dc-roster-grid-scroller,
        	.dc-roster-list-scroller {
        		overflow: visible !important;
        		max-height: none !important;
        		border: 0;
        	}
        	.dc-windowed-table {
        		overflow: visible !important;
        		max-height: none !important;
        		border: 0;
        	}
        	.dc-roster-grid__row--head,
        	.dc-roster-grid__corner,
        	.dc-roster-grid__rowhead,
        	.dc-table thead {
        		position: static;
        	}
        }
        @media (prefers-reduced-motion: reduce) {
        	.dc-roster-grid__cell,
        	.dc-roster-grid__shift {
        		transition: none;
        	}
        }
        
        /* Soft keyboard: leave room when visualViewport / scrollIntoView adjusts. */
        .form-textarea,
        textarea.form-input,
        textarea.bc-input,
        textarea.crm-input,
        textarea.iv-input,
        textarea.ic-input,
        textarea.mc-input,
        textarea.mn-input,
        textarea.tc-input,
        textarea.dk-input,
        textarea {
        	scroll-margin-bottom: min(45vh, 20rem);
        	scroll-margin-top: 5rem;
        }
        
        /* === app-feedback:dc start === */
        #app-navigation .dc-nav-footer,
        .dc-nav .dc-nav-footer,
        .dc-app-nav .dc-nav-footer {
        	margin-top: auto;
        	flex-shrink: 0;
        	min-width: 0;
        	width: 100%;
        	box-sizing: border-box;
        	padding: var(--dc-space-2, 0.5rem) var(--dc-space-3, 0.75rem)
        		max(var(--dc-space-3, 0.75rem), env(safe-area-inset-bottom, 0px));
        	border-top: 1px solid var(--dc-border, color-mix(in srgb, var(--color-main-text) 12%, var(--color-main-background)));
        	background: transparent;
        	z-index: 1;
        }
        
        .dc-nav-footer__popover {
        	position: relative;
        	display: flex;
        	flex-direction: column;
        	align-items: center;
        	gap: var(--dc-space-2, 0.5rem);
        }
        
        .dc-nav-footer__trigger {
        	display: inline-flex;
        	gap: var(--dc-space-3, 0.75rem);
        	min-height: 44px;
        	min-width: 44px;
        	padding: var(--dc-space-2, 0.5rem) var(--dc-space-3, 0.75rem);
        	border-radius: var(--dc-radius-sm, 6px);
        	box-sizing: border-box;
        	border: none;
        	background: none;
        	color: var(--color-main-text);
        	font: inherit;
        	cursor: pointer;
        	align-items: center;
        	justify-content: center;
        	white-space: nowrap;
        }
        
        .dc-nav-footer__trigger:hover {
        	background: color-mix(in srgb, var(--color-primary-element) 8%, var(--color-main-background));
        }
        
        .dc-nav-footer__trigger:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element) 60%, transparent);
        	outline-offset: 2px;
        }
        
        .dc-nav-footer__trigger:focus:not(:focus-visible) {
        	outline: none;
        }
        
        .dc-nav-footer__trigger-icon .dc-icon {
        	width: 20px;
        	height: 20px;
        }
        
        .dc-nav-footer__menu {
        	position: absolute;
        	bottom: calc(100% + var(--dc-space-2, 0.5rem));
        	left: 50%;
        	transform: translateX(-50%);
        	min-width: max-content;
        	max-width: calc(100vw - 2rem);
        	list-style: none;
        	margin: 0;
        	padding: var(--dc-space-2, 0.5rem);
        	background: var(--color-main-background);
        	border: 1px solid var(--dc-border, var(--color-border));
        	border-radius: var(--dc-radius-md, 12px);
        	box-shadow:
        		0 2px 6px color-mix(in srgb, var(--color-main-text) 6%, transparent),
        		0 8px 24px color-mix(in srgb, var(--color-main-text) 10%, transparent);
        	display: flex;
        	flex-direction: column;
        	gap: 2px;
        	z-index: 100;
        }
        
        .dc-nav-footer__menu[hidden] {
        	display: none;
        }
        
        .dc-nav-footer__menu-item {
        	display: flex;
        	align-items: center;
        	gap: var(--dc-space-3, 0.75rem);
        	min-height: 44px;
        	min-width: 44px;
        	padding: var(--dc-space-2, 0.5rem) var(--dc-space-3, 0.75rem);
        	border-radius: var(--dc-radius-sm, 6px);
        	color: var(--color-main-text);
        	text-decoration: none;
        	font-size: var(--dc-fs-sm, 0.875rem);
        	line-height: var(--dc-line-base, 1.45);
        	white-space: normal;
        	overflow-wrap: anywhere;
        }
        
        .dc-nav-footer__menu-item:hover {
        	background: color-mix(in srgb, var(--color-primary-element) 8%, var(--color-main-background));
        }
        
        .dc-nav-footer__menu-item:focus-visible {
        	outline: 3px solid color-mix(in srgb, var(--color-primary-element) 60%, transparent);
        	outline-offset: -2px;
        }
        
        .dc-nav-footer__menu-item:focus:not(:focus-visible) {
        	outline: none;
        }
        
        .dc-nav-footer__menu-icon .dc-icon {
        	width: 18px;
        	height: 18px;
        }
        
        .dc-nav-footer__note {
        	margin: 0;
        	font-size: var(--dc-fs-xs, 0.75rem);
        	line-height: 1.45;
        	color: var(--color-text-maxcontrast);
        	text-align: center;
        	overflow-wrap: anywhere;
        }
        
        @media (width < 480px) {
        	.dc-nav-footer__menu {
        		left: var(--dc-space-2, 0.5rem);
        		right: var(--dc-space-2, 0.5rem);
        		transform: none;
        		min-width: 0;
        	}
        }
        
        .dc-nav-footer__new-tab {
        	position: absolute;
        	width: 1px;
        	height: 1px;
        	padding: 0;
        	margin: -1px;
        	overflow: hidden;
        	clip: rect(0, 0, 0, 0);
        	clip-path: inset(50%);
        	white-space: nowrap;
        	border: 0;
        }
        
        .dc-nav-footer__toast-link {
        	display: inline-flex;
        	align-items: center;
        	min-height: 44px;
        	min-width: 44px;
        	margin-top: var(--dc-space-2, 0.5rem);
        	padding: 0;
        	font-size: var(--dc-fs-sm, 0.875rem);
        	line-height: 1.45;
        	color: var(--color-error-text, var(--color-main-text));
        	text-decoration: underline;
        	text-underline-offset: 0.15em;
        	overflow-wrap: anywhere;
        }
        
        .dc-nav-footer__toast-link:focus-visible {
        	outline: 2px solid var(--color-primary-element);
        	outline-offset: 2px;
        }
        
        .dc-nav-footer__toast-link:focus:not(:focus-visible) {
        	outline: none;
        }
        
        @media (prefers-contrast: more) {
        	#app-navigation .dc-nav-footer,
        	.dc-nav .dc-nav-footer,
        	.dc-app-nav .dc-nav-footer {
        		border-top-width: 2px;
        		border-top-color: var(--color-main-text);
        	}
        
        	.dc-nav-footer__menu {
        		border-width: 2px;
        	}
        
        	.dc-nav-footer__trigger:focus-visible,
        	.dc-nav-footer__menu-item:focus-visible,
        	.dc-nav-footer__toast-link:focus-visible {
        		outline-width: 3px;
        	}
        }
        
        @media (forced-colors: active) {
        	.dc-nav-footer__menu-item,
        	.dc-nav-footer__toast-link {
        		forced-color-adjust: none;
        		color: LinkText;
        	}
        
        	.dc-nav-footer__trigger:focus-visible,
        	.dc-nav-footer__menu-item:focus-visible,
        	.dc-nav-footer__toast-link:focus-visible {
        		outline: 3px solid Highlight;
        	}
        }
        
        @media (prefers-reduced-motion: reduce) {
        	.dc-nav-footer__trigger,
        	.dc-nav-footer__menu-item,
        	.dc-nav-footer__toast-link {
        		transition: none;
        	}
        }
        /* === app-feedback:dc end === */
        
        /* === roster-self-service-ga:dc begin === */
        .dc-dienst-team__form {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-6);
        }
        
        .dc-dienst-team__group {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	padding-bottom: var(--dc-space-5);
        	border-bottom: 1px solid var(--dc-border);
        }
        
        .dc-dienst-team__group:last-of-type {
        	border-bottom: 0;
        	padding-bottom: 0;
        }
        
        .dc-dienst-team__group-title {
        	margin: 0;
        	font-size: var(--dc-fs-lg);
        	font-weight: 600;
        	line-height: var(--dc-line-tight);
        	color: var(--dc-text);
        }
        
        .dc-dienst-team__group-lead,
        .dc-dienst-team__hint {
        	display: block;
        	margin: 0;
        	font-size: var(--dc-fs-sm);
        	line-height: var(--dc-line-base);
        	color: var(--dc-muted);
        	font-weight: 400;
        }
        
        .dc-dienst-team__primary {
        	min-height: var(--dc-touch);
        	padding: var(--dc-space-2) 0;
        }
        
        .dc-dienst-team__primary + .dc-dienst-team__primary {
        	margin-top: var(--dc-space-1);
        }
        
        .dc-checkbox.dc-dienst-team__primary .dc-checkbox__text {
        	display: flex;
        	flex-direction: column;
        	gap: 0.15rem;
        }
        
        @media (max-width: 640px) {
        	.dc-today__filters {
        		grid-template-columns: 1fr;
        	}
        
        	.dc-dienst-team__quiet-times {
        		grid-template-columns: 1fr;
        	}
        
        	.dc-patterns__item-actions {
        		flex-wrap: wrap;
        	}
        
        	.dc-patterns__item-actions .button {
        		min-height: var(--dc-touch);
        	}
        }
        
        .dc-dienst-team__badge {
        	margin-left: var(--dc-space-2);
        	vertical-align: middle;
        }
        
        .dc-dienst-team__inline-link {
        	margin: 0;
        }
        
        .dc-dienst-team__details {
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	background: var(--dc-bg-soft);
        }
        
        .dc-dienst-team__summary {
        	cursor: pointer;
        	min-height: var(--dc-touch);
        	display: flex;
        	align-items: center;
        	font-weight: 600;
        	list-style: none;
        }
        
        .dc-dienst-team__summary::-webkit-details-marker {
        	display: none;
        }
        
        .dc-dienst-team__summary:focus-visible {
        	outline: 2px solid var(--color-primary-element);
        	outline-offset: 2px;
        }
        
        .dc-dienst-team__quiet-times {
        	margin-top: var(--dc-space-2);
        }
        
        .dc-dienst-team__more-block {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	margin-top: var(--dc-space-2);
        }
        
        .dc-today__filters {
        	margin-bottom: var(--dc-space-4);
        	align-items: end;
        }
        
        .dc-today__date-shortcuts {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        	margin-top: var(--dc-space-2);
        }
        
        .dc-today__date-shortcuts .button {
        	min-height: var(--dc-touch);
        	min-width: 44px;
        }
        
        @media (max-width: 640px) {
        	.dc-today__date-shortcuts {
        		flex-direction: column;
        	}
        
        	.dc-today__date-shortcuts .button {
        		width: 100%;
        	}
        }
        
        .dc-today__skeleton {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        	margin: var(--dc-space-4) 0;
        }
        
        .dc-today__skeleton-row {
        	height: 3.25rem;
        	border-radius: var(--dc-radius-md);
        	background: linear-gradient(
        		90deg,
        		var(--dc-bg-soft) 0%,
        		var(--dc-bg-stripe) 50%,
        		var(--dc-bg-soft) 100%
        	);
        	background-size: 200% 100%;
        	animation: dc-today-shimmer 1.4s ease-in-out infinite;
        }
        
        @keyframes dc-today-shimmer {
        	0% { background-position: 100% 0; }
        	100% { background-position: -100% 0; }
        }
        
        @media (prefers-reduced-motion: reduce) {
        	.dc-today__skeleton-row {
        		animation: none;
        		background: var(--dc-bg-stripe);
        	}
        }
        
        .dc-today__gaps {
        	list-style: none;
        	margin: 0 0 var(--dc-space-4);
        	padding: 0;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        }
        
        .dc-today__gap {
        	display: flex;
        	align-items: flex-start;
        	gap: var(--dc-space-3);
        	min-height: var(--dc-touch);
        	padding: var(--dc-space-3);
        	border-radius: var(--dc-radius-md);
        	background: var(--dc-tint-warning);
        	border: 1px solid var(--dc-border);
        	color: var(--dc-text);
        }
        
        .dc-today__gap-icon {
        	flex-shrink: 0;
        	width: 1.75rem;
        	height: 1.75rem;
        	border-radius: 50%;
        	display: inline-flex;
        	align-items: center;
        	justify-content: center;
        	font-weight: 700;
        	background: var(--color-main-background);
        	border: 2px solid var(--color-warning-text, var(--color-warning, #c98600));
        	color: var(--color-warning-text, var(--color-warning, #c98600));
        }
        
        .dc-today__timeline {
        	list-style: none;
        	margin: 0;
        	padding: 0;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        }
        
        .dc-today__shift {
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border-radius: var(--dc-radius-md);
        	border: 1px solid var(--dc-border);
        	background: var(--dc-bg-card);
        	min-height: var(--dc-touch);
        }
        
        .dc-today__shift--draft {
        	border-style: dashed;
        	background: var(--dc-bg-soft);
        }
        
        .dc-today__shift-main {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-1);
        }
        
        .dc-today__shift-name {
        	font-size: var(--dc-fs-md);
        	font-weight: 600;
        }
        
        .dc-today__shift-meta {
        	font-size: var(--dc-fs-sm);
        	color: var(--dc-muted);
        }
        
        .dc-today__empty {
        	padding: var(--dc-space-5);
        	text-align: center;
        	color: var(--dc-muted);
        }
        
        .dc-my-team__filters {
        	margin-top: var(--dc-space-2, 0.5rem);
        }
        
        .dc-my-team__filters .dc-today__date-shortcuts .button,
        .dc-my-team__filters .dc-form-actions .button {
        	min-height: 44px;
        	min-width: 44px;
        }
        
        .dc-my-team__list {
        	list-style: none;
        	margin: var(--dc-space-3, 0.75rem) 0 0;
        	padding: 0;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2, 0.65rem);
        }
        
        .dc-my-team__row {
        	display: grid;
        	grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr) minmax(0, 0.8fr);
        	gap: 0.35rem 0.75rem;
        	align-items: baseline;
        	padding: 0.75rem 0.9rem;
        	border: 1px solid var(--color-border, var(--dc-border, #ddd));
        	border-radius: var(--border-radius-large, 8px);
        	background: var(--color-main-background);
        }
        
        .dc-my-team__name {
        	font-size: 1.05rem;
        }
        
        .dc-my-team__when,
        .dc-my-team__loc {
        	color: var(--dc-muted, var(--color-text-maxcontrast));
        	font-size: 0.95rem;
        }
        
        @media (max-width: 640px) {
        	.dc-my-team__row {
        		grid-template-columns: 1fr;
        	}
        }
        
        .dc-dienst-team .dc-section__sub {
        	margin: 0 0 0.75rem;
        	max-width: 42rem;
        	color: var(--dc-muted, var(--color-text-maxcontrast));
        }
        
        .dc-patterns__list {
        	list-style: none;
        	margin: 0;
        	padding: 0;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-3);
        }
        
        .dc-patterns__item {
        	display: flex;
        	flex-wrap: wrap;
        	align-items: center;
        	justify-content: space-between;
        	gap: var(--dc-space-3);
        	padding: var(--dc-space-4);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        	background: var(--dc-bg-card);
        	min-height: var(--dc-touch);
        }
        
        .dc-patterns__item-main {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-1);
        	min-width: 0;
        }
        
        .dc-patterns__item-meta {
        	font-size: var(--dc-fs-sm);
        	color: var(--dc-muted);
        }
        
        .dc-patterns__item-actions {
        	display: flex;
        	flex-wrap: wrap;
        	gap: var(--dc-space-2);
        }
        
        .dc-patterns__empty {
        	padding: var(--dc-space-5);
        	text-align: center;
        	color: var(--dc-muted);
        }
        
        .dc-patterns__editor {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-4);
        }
        
        .dc-patterns__week {
        	margin: 0;
        	padding: var(--dc-space-3);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-md);
        }
        
        .dc-patterns__grid {
        	display: grid;
        	grid-template-columns: repeat(auto-fit, minmax(11rem, 1fr));
        	gap: var(--dc-space-3);
        }
        
        .dc-patterns__day {
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        	padding: var(--dc-space-2);
        	border-radius: var(--dc-radius-sm);
        	background: var(--dc-bg-soft);
        }
        
        .dc-patterns__day-head {
        	font-weight: 600;
        	font-size: var(--dc-fs-sm);
        }
        
        .dc-patterns__work-toggle {
        	width: 100%;
        }
        
        .dc-coverage-strip {
        	display: flex;
        	flex-wrap: wrap;
        	align-items: center;
        	gap: var(--dc-space-2) var(--dc-space-3);
        	margin: 0 0 var(--dc-space-3);
        	padding: var(--dc-space-3) var(--dc-space-4);
        	border-radius: var(--dc-radius-md);
        	border: 1px solid var(--dc-border);
        	background: var(--dc-bg-soft);
        	min-height: var(--dc-touch);
        }
        
        .dc-coverage-strip__label {
        	font-weight: 600;
        	font-size: var(--dc-fs-sm);
        }
        
        .dc-coverage-strip__text {
        	font-size: var(--dc-fs-sm);
        	color: var(--dc-text);
        }
        
        .dc-coverage-strip--ok {
        	background: var(--dc-tint-success);
        }
        
        .dc-coverage-strip--warn {
        	background: var(--dc-tint-warning);
        }
        
        .dc-coverage-strip--critical {
        	background: var(--dc-tint-critical);
        }
        
        .dc-pref-chip,
        .dc-pref-chip-row {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-2);
        }
        
        .dc-pref-chip-row {
        	flex-wrap: wrap;
        	margin-bottom: var(--dc-space-3);
        }
        
        .dc-pref-chip {
        	min-height: var(--dc-touch);
        	min-width: var(--dc-touch);
        	padding: 0 var(--dc-space-4);
        	border-radius: var(--dc-radius-sm);
        	border: 1px solid var(--dc-border-strong);
        	background: var(--dc-bg-card);
        	color: var(--dc-text);
        	font-size: var(--dc-fs-sm);
        	font-weight: 600;
        	cursor: pointer;
        }
        
        .dc-pref-chip:focus-visible {
        	outline: 2px solid var(--color-primary-element);
        	outline-offset: 2px;
        }
        
        .dc-pref-chip--static {
        	cursor: default;
        	background: var(--dc-tint-info);
        	pointer-events: none;
        }
        
        .dc-blackout-mark {
        	display: inline-flex;
        	align-items: center;
        	gap: var(--dc-space-1);
        	font-size: var(--dc-fs-xs);
        	font-weight: 600;
        	color: var(--color-error-text, var(--color-error, #cc4646));
        	line-height: 1.2;
        }
        
        .dc-blackout-mark__icon {
        	flex-shrink: 0;
        }
        
        .dc-roster-grid__cell--blackout {
        	background: var(--dc-tint-critical);
        }
        
        .dc-roster-grid__pref-chips {
        	display: flex;
        	flex-wrap: wrap;
        	gap: 2px;
        	margin-top: 2px;
        }
        
        .dc-roster-grid__rowhead-name {
        	display: block;
        	overflow: hidden;
        	text-overflow: ellipsis;
        	white-space: nowrap;
        }
        
        .dc-my-availability__block {
        	margin-bottom: var(--dc-space-5);
        }
        
        .dc-my-availability__list {
        	list-style: none;
        	margin: var(--dc-space-3) 0 0;
        	padding: 0;
        	display: flex;
        	flex-direction: column;
        	gap: var(--dc-space-2);
        }
        
        .dc-my-availability__row {
        	display: flex;
        	flex-wrap: wrap;
        	align-items: center;
        	justify-content: space-between;
        	gap: var(--dc-space-2);
        	min-height: var(--dc-touch);
        	padding: var(--dc-space-2) var(--dc-space-3);
        	border: 1px solid var(--dc-border);
        	border-radius: var(--dc-radius-sm);
        }
        
        .dc-suggest-preview__counts {
        	margin: var(--dc-space-3) 0 0;
        	padding-left: 1.25rem;
        }
        
        .dc-roster-flash--error {
        	color: var(--color-error-text, var(--color-error, #cc4646));
        }
        
        @media (max-width: 768px) {
        	.dc-patterns__item {
        		flex-direction: column;
        		align-items: stretch;
        	}
        
        	.dc-patterns__item-actions .button {
        		width: 100%;
        		justify-content: center;
        	}
        }
        /* === roster-self-service-ga:dc end === */
        
      operator: 'doesNotMatch'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:307:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 5.472904
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 105.31156
killed month_grid_auto_fit_regression
== mutant drop_day_count_css_var (must FAIL) ==
PHPUnit 9.6.34 by Sebastian Bergmann and contributors.

....F.                                                              6 / 6 (100%)

Time: 00:00.010, Memory: 40.00 MB

There was 1 failure:

1) OCA\DutyCheck\Tests\Unit\Support\RosterVirtualizationContractTest::testRosterJsSetsExplicitDayTracksAndCompactHeaders
Failed asserting that '(function () {\n
	'use strict';\n
\n
	const Api = window.DutyCheckApi;\n
	const Msg = window.DutyCheckMessaging;\n
	const C = window.DutyCheckComponents || window.DutyCheckDom || {};\n
	const D = window.DutyCheckDates;\n
	const ConflictLabels = window.DutyCheckConflictLabels;\n
	const create = C.createElement;\n
	if (typeof create !== 'function') {\n
		throw new Error('DutyCheck components failed to load');\n
	}\n
\n
	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n
\n
	const state = {\n
		periods: [],\n
		employees: [],\n
		locations: [],\n
		assignments: [],\n
		absenceBlocks: [],\n
		canCreateAssignments: false,\n
		lastRosterData: null,\n
		defaultBreakMinutes: 0,\n
		planningDefaultsFresh: false,\n
		templates: [],\n
		templatesLoaded: false,\n
		editingAssignmentId: null,\n
		editingAssignmentVersion: null,\n
		copyPreviewReady: false,\n
		preferencesByEmployee: {},\n
		blackoutsByEmployeeDate: {},\n
	};\n
	let assignmentModalInstance = null;\n
	let suggestedBreakMinutes = 0;\n
\n
	function clampBreakMinutes(value) {\n
		const n = Number(value);\n
		if (!Number.isFinite(n)) {\n
			return 0;\n
		}\n
		return Math.max(0, Math.min(720, Math.round(n)));\n
	}\n
\n
	function readStoredBreakMinutes() {\n
		try {\n
			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n
			if (raw === null || raw === '') {\n
				return null;\n
			}\n
			return clampBreakMinutes(raw);\n
		} catch (_) {\n
			return null;\n
		}\n
	}\n
\n
	function rememberBreakMinutes(minutes) {\n
		try {\n
			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n
		} catch (_) {\n
			/* ignore quota / privacy mode */\n
		}\n
	}\n
\n
	function resolveBreakPrefill() {\n
		const org = clampBreakMinutes(state.defaultBreakMinutes);\n
		// Organisation default from Settings always wins when it is greater than zero.\n
		if (org > 0) {\n
			return { minutes: org, source: 'settings' };\n
		}\n
		const stored = readStoredBreakMinutes();\n
		if (stored !== null) {\n
			return { minutes: stored, source: 'last' };\n
		}\n
		return { minutes: 0, source: 'settings' };\n
	}\n
\n
	async function refreshPlanningDefaultFromServer() {\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n
			const minutes = Number(response?.planning?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
			state.planningDefaultsFresh = true;\n
		} catch (_) {\n
			/* Fall back to defaultBreakMinutes from the last roster API load. */\n
		}\n
	}\n
\n
	function breakPrefillMessage(prefill) {\n
		const n = String(prefill.minutes);\n
		if (prefill.source === 'last') {\n
			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n
		}\n
		if (prefill.minutes === 0) {\n
			return t('dutycheck', 'Pre-filled with 0 min — no break.');\n
		}\n
		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n
	}\n
\n
	function updateBreakPrefillHint(userEdited) {\n
		const el = document.getElementById('dc-assignment-break-prefill');\n
		if (!el) {\n
			return;\n
		}\n
		if (userEdited) {\n
			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n
			return;\n
		}\n
		el.textContent = breakPrefillMessage(resolveBreakPrefill());\n
	}\n
\n
	function applyDefaultBreakToForm() {\n
		const prefill = resolveBreakPrefill();\n
		suggestedBreakMinutes = prefill.minutes;\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(prefill.minutes);\n
		}\n
		updateBreakPrefillHint(false);\n
	}\n
\n
	function wireBreakPrefillHint() {\n
		const br = document.getElementById('dc-assignment-break');\n
		if (!br || br.dataset.dcBreakPrefillWired) {\n
			return;\n
		}\n
		br.dataset.dcBreakPrefillWired = '1';\n
		br.addEventListener('input', () => {\n
			const current = clampBreakMinutes(br.value);\n
			updateBreakPrefillHint(current !== suggestedBreakMinutes);\n
		});\n
		br.addEventListener('change', () => {\n
			const current = clampBreakMinutes(br.value);\n
			if (current === suggestedBreakMinutes) {\n
				updateBreakPrefillHint(false);\n
			}\n
		});\n
	}\n
\n
	function dateToDayIndex(isoDate) {\n
		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n
		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n
			return null;\n
		}\n
		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n
	}\n
\n
	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n
		const dayIndex = dateToDayIndex(dutyDate);\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime);\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime);\n
		if (dayIndex === null || start === null || end === null) {\n
			return null;\n
		}\n
		const absoluteStart = (dayIndex * 1440) + start;\n
		let absoluteEnd = (dayIndex * 1440) + end;\n
		if (absoluteEnd <= absoluteStart) {\n
			absoluteEnd += 1440;\n
		}\n
		return [absoluteStart, absoluteEnd];\n
	}\n
\n
	function absoluteRangesOverlap(a, b) {\n
		return a[0] < b[1] && b[0] < a[1];\n
	}\n
\n
	function isDateInAbsenceBlock(employeeId, dutyDate) {\n
		if (!dutyDate) {\n
			return false;\n
		}\n
		for (const block of state.absenceBlocks) {\n
			if (Number(block?.employeeId) !== Number(employeeId)) {\n
				continue;\n
			}\n
			const start = String(block?.startDate || '');\n
			const end = String(block?.endDate || '');\n
			if (dutyDate >= start && dutyDate <= end) {\n
				return true;\n
			}\n
		}\n
		return false;\n
	}\n
\n
	function employeesAvailableOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return [];\n
		}\n
		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n
	}\n
\n
	function countEmployeesBlockedOnDate(dutyDate) {\n
		if (!dutyDate) {\n
			return 0;\n
		}\n
		return state.employees.length - employeesAvailableOnDate(dutyDate).length;\n
	}\n
\n
	function appendHintLink(parent, beforeText, linkText, afterText, href) {\n
		parent.appendChild(document.createTextNode(beforeText));\n
		if (href) {\n
			const link = create('a', {\n
				class: 'dc-hint-link',\n
				href,\n
				text: linkText,\n
			});\n
			parent.appendChild(link);\n
			parent.appendChild(document.createTextNode(afterText));\n
		} else {\n
			parent.appendChild(document.createTextNode(linkText + afterText));\n
		}\n
	}\n
\n
	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n
		const hint = document.getElementById('dc-assignment-employee-hint');\n
		if (!hint) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		hint.replaceChildren();\n
		if (!dutyDate) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n
			));\n
			return;\n
		}\n
		if (!available.length && blocked > 0) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n
					.replace('{n}', String(blocked)),\n
				t('dutycheck', 'review absences'),\n
				'.',\n
				String(urls.absences || ''),\n
			);\n
			return;\n
		}\n
		if (!available.length) {\n
			appendHintLink(\n
				hint,\n
				t('dutycheck', 'No employees are available. Add an active employee on the '),\n
				t('dutycheck', 'Employees'),\n
				t('dutycheck', ' page first.'),\n
				String(urls.employees || ''),\n
			);\n
			return;\n
		}\n
		if (blocked > 0) {\n
			hint.appendChild(document.createTextNode(\n
				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n
					.replace('{available}', String(available.length))\n
					.replace('{hidden}', String(blocked)),\n
			));\n
			return;\n
		}\n
		hint.appendChild(document.createTextNode(\n
			t('dutycheck', 'All {n} active employees can be scheduled on this date.')\n
				.replace('{n}', String(available.length)),\n
		));\n
	}\n
\n
	function refreshEmployeeSelectForDate() {\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const dutyDate = dateInput ? String(dateInput.value || '') : '';\n
		const available = employeesAvailableOnDate(dutyDate);\n
		const blocked = countEmployeesBlockedOnDate(dutyDate);\n
		setEmployeeAvailabilityHint(dutyDate, available, blocked);\n
		fillSelect('dc-assignment-employee', available, {\n
			emptyText: dutyDate\n
				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n
				: t('dutycheck', 'Pick a date first'),\n
		});\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		if (employeeSelect) {\n
			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n
		}\n
	}\n
\n
	function findOverlapAssignment(payload) {\n
		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n
		if (!candidate) {\n
			return null;\n
		}\n
		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		for (const assignment of state.assignments) {\n
			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n
				continue;\n
			}\n
			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n
				continue;\n
			}\n
			const existing = assignmentAbsoluteRange(\n
				String(assignment?.dutyDate || ''),\n
				String(assignment?.startTime || ''),\n
				String(assignment?.endTime || ''),\n
			);\n
			if (existing && absoluteRangesOverlap(candidate, existing)) {\n
				return assignment;\n
			}\n
		}\n
		return null;\n
	}\n
\n
	function setFormFeedbackA11y(el, isError) {\n
		if (!el) {\n
			return;\n
		}\n
		if (isError) {\n
			el.setAttribute('role', 'alert');\n
			el.setAttribute('aria-live', 'assertive');\n
		} else {\n
			el.setAttribute('role', 'status');\n
			el.setAttribute('aria-live', 'polite');\n
		}\n
	}\n
\n
	function focusFirstInvalidAssignmentField(payload) {\n
		if (!payload) {\n
			return;\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		const order = [\n
			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n
			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n
			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n
			{\n
				test: () => period?.startDate && period?.endDate && payload.dutyDate\n
					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n
				id: 'dc-assignment-date',\n
			},\n
			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },\n
			{ test: () => !payload.locationId, id: 'dc-assignment-location' },\n
			{ test: () => !payload.startTime, id: 'dc-assignment-start' },\n
			{ test: () => !payload.endTime, id: 'dc-assignment-end' },\n
			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },\n
			{\n
				test: () => {\n
					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
					return effective === null || effective <= 0;\n
				},\n
				id: 'dc-assignment-end',\n
			},\n
		];\n
		for (const item of order) {\n
			if (item.test()) {\n
				document.getElementById(item.id)?.focus();\n
				return;\n
			}\n
		}\n
	}\n
\n
	function selectedPeriodForPayload(payload) {\n
		const periodId = Number(payload?.periodId);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function clearAssignmentFormSuccess() {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		if (!el || el.dataset.dcSuccess !== '1') {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.dataset.dcSuccess = '';\n
		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
		setFormFeedbackA11y(el, false);\n
	}\n
\n
	function showAssignmentFormSuccess(message, kind) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		const tone = kind === 'info' ? 'info' : 'success';\n
		el.hidden = false;\n
		el.textContent = String(message);\n
		el.dataset.dcSuccess = '1';\n
		el.classList.remove('dc-roster-form__feedback--error');\n
		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');\n
		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');\n
		setFormFeedbackA11y(el, false);\n
		if (submit) {\n
			submit.disabled = submit.dataset.dcLockedBySetup === '1';\n
		}\n
		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });\n
	}\n
\n
	function labelsForAssignmentPayload(payload) {\n
		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));\n
		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));\n
		const employeeSelect = document.getElementById('dc-assignment-employee');\n
		const locationSelect = document.getElementById('dc-assignment-location');\n
		const employeeName = employee\n
			? String(employee.name || employee.displayName || '')\n
			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		const locationName = location\n
			? String(location.name || '')\n
			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();\n
		return { employeeName, locationName };\n
	}\n
\n
	function buildAssignmentSavedMessage(payload) {\n
		const { employeeName, locationName } = labelsForAssignmentPayload(payload);\n
		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;\n
		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)\n
			|| `${payload.startTime} – ${payload.endTime}`;\n
		return t(\n
			'dutycheck',\n
			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',\n
		)\n
			.replace('{employee}', employeeName || '—')\n
			.replace('{date}', String(dateLabel || ''))\n
			.replace('{times}', String(times))\n
			.replace('{location}', locationName || '—');\n
	}\n
\n
	function updateAssignmentFormFeedback(payload) {\n
		const el = document.getElementById('dc-assignment-form-feedback');\n
		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');\n
		if (!el) {\n
			return;\n
		}\n
		if (el.dataset.dcSuccess === '1') {\n
			return;\n
		}\n
		if (typeof payload === 'string') {\n
			el.hidden = false;\n
			el.textContent = payload;\n
			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			el.classList.add('dc-roster-form__feedback--error');\n
			setFormFeedbackA11y(el, true);\n
			if (submit) {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		if (!payload) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			return;\n
		}\n
		let message = '';\n
		let isError = false;\n
		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');\n
			isError = true;\n
		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {\n
			const overlap = findOverlapAssignment(payload);\n
			if (overlap) {\n
				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;\n
				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)\n
					|| `${overlap.startTime} – ${overlap.endTime}`;\n
				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')\n
					.replace('{date}', String(when))\n
					.replace('{times}', String(times));\n
				isError = true;\n
			}\n
		}\n
		if (!message) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');\n
			setFormFeedbackA11y(el, false);\n
			if (submit && submit.dataset.dcLockedBySetup !== '1') {\n
				submit.disabled = false;\n
			}\n
			return;\n
		}\n
		el.hidden = false;\n
		el.textContent = message;\n
		el.classList.toggle('dc-roster-form__feedback--error', isError);\n
		setFormFeedbackA11y(el, isError);\n
		if (submit && isError) {\n
			submit.disabled = true;\n
		}\n
	}\n
\n
	function refreshAssignmentFormEligibility() {\n
		refreshEmployeeSelectForDate();\n
		updateLocationTimezoneHint();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		updateAssignmentFormFeedback(readForm(form));\n
	}\n
\n
	function updateLocationTimezoneHint() {\n
		const hint = document.getElementById('dc-assignment-location-tz-hint');\n
		const select = document.getElementById('dc-assignment-location');\n
		if (!hint || !select) {\n
			return;\n
		}\n
		const locationId = Number(select.value);\n
		const location = state.locations.find((row) => Number(row.id) === locationId);\n
		const tz = location && String(location.timezone || '').trim();\n
		if (!tz) {\n
			hint.hidden = true;\n
			hint.replaceChildren();\n
			return;\n
		}\n
		const accountTz = D?.currentTimezone?.() || '';\n
		hint.textContent = t(\n
			'dutycheck',\n
			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',\n
		)\n
			.replace('{locationTimezone}', tz)\n
			.replace('{accountTimezone}', accountTz || 'UTC');\n
		hint.hidden = false;\n
	}\n
\n
	function fillSelect(id, items, options) {\n
		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});\n
		const select = document.getElementById(id);\n
		if (!select) return;\n
		const previous = String(select.value || '');\n
		select.replaceChildren();\n
		if (!items.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = opts.emptyText;\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const item of items) {\n
			const option = document.createElement('option');\n
			option.value = String(item.id);\n
			option.textContent = String(item[opts.labelKey] || item.name || item.id);\n
			select.appendChild(option);\n
		}\n
		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {\n
			select.value = previous;\n
		}\n
	}\n
\n
	function fillPeriodSwitcher(periods, selected) {\n
		const select = document.getElementById('dc-roster-period-switcher');\n
		if (!select) return;\n
		const statusLabelMap = {\n
			open: t('dutycheck', 'Open'),\n
			published: t('dutycheck', 'Published'),\n
			closed: t('dutycheck', 'Closed'),\n
		};\n
		select.replaceChildren();\n
		if (!periods.length) {\n
			const option = document.createElement('option');\n
			option.value = '';\n
			option.textContent = t('dutycheck', 'No periods yet');\n
			select.appendChild(option);\n
			select.disabled = true;\n
			return;\n
		}\n
		select.disabled = false;\n
		for (const period of periods) {\n
			const option = document.createElement('option');\n
			option.value = String(period.id);\n
			const start = D?.formatDisplayDate(period.startDate) || period.startDate;\n
			const end = D?.formatDisplayDate(period.endDate) || period.endDate;\n
			const statusKey = String(period.status || '').toLowerCase();\n
			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();\n
			option.textContent = `${start} – ${end} · ${status}`;\n
			select.appendChild(option);\n
		}\n
		if (selected) {\n
			select.value = String(selected);\n
		}\n
	}\n
\n
	function severityBadge(severity) {\n
		const sev = String(severity || 'info');\n
		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;\n
		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });\n
		badge.setAttribute('aria-label', label);\n
		return badge;\n
	}\n
\n
	function renderConflicts(conflicts) {\n
		const list = document.getElementById('dc-conflict-list');\n
		const summary = document.getElementById('dc-conflict-summary');\n
		if (!list) return;\n
		list.setAttribute('role', 'list');\n
		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));\n
		list.replaceChildren();\n
		const hard = conflicts.filter((c) => c?.severity === 'hard').length;\n
		const soft = conflicts.filter((c) => c?.severity === 'soft').length;\n
		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;\n
		if (summary) {\n
			summary.textContent = !conflicts.length\n
				? t('dutycheck', 'No planning issues found.')\n
				: (ConflictLabels\n
					? ConflictLabels.countsSummary(hard, soft, softUnack)\n
					: String(hard));\n
		}\n
		if (!conflicts.length) {\n
			return;\n
		}\n
		for (const conflict of conflicts) {\n
			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });\n
			li.appendChild(severityBadge(conflict?.severity));\n
			const conflictMessage = String(conflict?.message || '');\n
			const titleText = conflictMessage\n
				? translateConflictMessage(conflictMessage)\n
				: t('dutycheck', 'Unknown conflict');\n
			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;\n
			const body = create('div', { class: 'dc-conflict__body' });\n
			const title = create('span', { class: 'dc-conflict__title', text: titleText });\n
			title.id = titleId;\n
			body.appendChild(title);\n
			li.setAttribute('aria-labelledby', titleId);\n
			if (conflict?.acknowledged && conflict?.ackReason) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack',\n
					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),\n
				}));\n
			} else if (conflict?.ackInvalidated) {\n
				body.appendChild(create('span', {\n
					class: 'dc-conflict__ack-invalid',\n
					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),\n
				}));\n
			}\n
			li.appendChild(body);\n
\n
			const actions = create('div', { class: 'dc-conflict__actions' });\n
			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {\n
				const ackBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Confirm'),\n
				});\n
				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));\n
				actions.appendChild(ackBtn);\n
			}\n
			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();\n
			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {\n
				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()\n
					|| t('dutycheck', 'Open ArbeitszeitCheck');\n
				const recoveryLink = create('a', {\n
					class: 'button primary',\n
					href: recoveryUrl,\n
					text: recoveryLabel,\n
					attrs: {\n
						target: '_blank',\n
						rel: 'noopener noreferrer',\n
						'aria-label': recoveryLabel,\n
					},\n
				});\n
				actions.appendChild(recoveryLink);\n
			}\n
			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];\n
			if (conflict?.type === 'absence_collision' && assignmentIds.length) {\n
				const focusBtn = create('button', {\n
					type: 'button',\n
					class: 'button',\n
					text: t('dutycheck', 'Show assignment'),\n
				});\n
				focusBtn.addEventListener('click', () => {\n
					revealAssignmentRow(Number(assignmentIds[0]));\n
				});\n
				actions.appendChild(focusBtn);\n
			}\n
			li.appendChild(actions);\n
			list.appendChild(li);\n
		}\n
	}\n
\n
	async function acknowledgeConflict(conflictId) {\n
		const reason = await C.promptReason({\n
			title: t('dutycheck', 'Confirm this exception'),\n
			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),\n
			confirmLabel: t('dutycheck', 'Confirm'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			minLength: 10,\n
		});\n
		if (reason === null) return;\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });\n
			render(response?.data || {});\n
			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');\n
			Msg.announce(ackMsg, 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	const VIRTUAL_FALLBACK = {\n
		DEFAULT_ROW_HEIGHT: 44,\n
		DEFAULT_OVERSCAN: 6,\n
		UNSIZED_WINDOW_ROWS: 24,\n
		visibleRange(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			const rowHeight = 44;\n
			return {\n
				start: 0,\n
				end: total,\n
				padBefore: 0,\n
				padAfter: 0,\n
				totalHeight: total * rowHeight,\n
				rowHeight,\n
			};\n
		},\n
		scrollTopToRevealIndex() {\n
			return 0;\n
		},\n
		pageStride() {\n
			return 1;\n
		},\n
		windowCaption(opts) {\n
			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));\n
			if (total <= 0) {\n
				return { mode: 'empty', from: 0, to: 0, total: 0 };\n
			}\n
			return { mode: 'all', from: 1, to: total, total };\n
		},\n
	};\n
\n
	function virtualApi() {\n
		const api = window.DutyCheckVirtualWindow;\n
		if (api\n
			&& typeof api.visibleRange === 'function'\n
			&& typeof api.scrollTopToRevealIndex === 'function'\n
			&& typeof api.windowCaption === 'function') {\n
			return api;\n
		}\n
		return VIRTUAL_FALLBACK;\n
	}\n
\n
	function cellSelectionKey(employeeId, dutyDate) {\n
		return `${Number(employeeId)}|${String(dutyDate)}`;\n
	}\n
\n
	function prefersReducedMotion() {\n
		try {\n
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n
		} catch (_) {\n
			return false;\n
		}\n
	}\n
\n
	function measureHeight(el) {\n
		if (!(el instanceof HTMLElement)) {\n
			return 0;\n
		}\n
		const h = el.getBoundingClientRect().height;\n
		return Number.isFinite(h) && h > 0 ? h : 0;\n
	}\n
\n
	function assignmentIndex(assignments) {\n
		const byKey = new Map();\n
		for (const a of assignments || []) {\n
			const key = cellSelectionKey(a.employeeId, a.dutyDate);\n
			if (!byKey.has(key)) {\n
				byKey.set(key, []);\n
			}\n
			byKey.get(key).push(a);\n
		}\n
		return byKey;\n
	}\n
\n
	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {\n
		const emptyPanel = document.getElementById('dc-roster-assignments-empty');\n
		if (emptyPanel) {\n
			emptyPanel.hidden = !showEmptyPanel;\n
		}\n
	}\n
\n
	const gridState = {\n
		view: 'grid',\n
		selected: new Set(), // `${employeeId}|${dutyDate}`\n
		focusRow: 0,\n
		focusCol: 0,\n
		paintAll: false,\n
		rowHeight: 44,\n
		headerHeight: 44,\n
		windowStart: -1,\n
		windowEnd: -1,\n
		paintedEmployeeCount: -1,\n
		paintedDateCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	const listState = {\n
		windowStart: -1,\n
		windowEnd: -1,\n
		rowHeight: 48,\n
		paintedCount: -1,\n
		remeasuring: false,\n
	};\n
\n
	function renderAssignments(assignments) {\n
		paintListWindow({ assignments: assignments || [], force: true });\n
		renderRosterGrid(assignments || []);\n
		updateAddAssignmentControl();\n
	}\n
\n
	function buildAssignmentRow(a, periodOpen) {\n
		const overnightHintText = t('dutycheck', 'Continues into the next day.');\n
		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));\n
		const tr = create('tr');\n
		if (a?.id != null) {\n
			tr.dataset.assignmentId = String(a.id);\n
			tr.setAttribute('tabindex', '-1');\n
		}\n
		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;\n
		const tdDate = create('td', { text: date });\n
		tdDate.dataset.cell = t('dutycheck', 'Date');\n
		tr.appendChild(tdDate);\n
\n
		const tdStart = create('td');\n
		tdStart.dataset.cell = t('dutycheck', 'Start');\n
		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));\n
		tr.appendChild(tdStart);\n
\n
		const tdEnd = create('td');\n
		tdEnd.dataset.cell = t('dutycheck', 'End');\n
		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));\n
		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {\n
			tdEnd.title = overnightHintText;\n
			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));\n
		}\n
		tr.appendChild(tdEnd);\n
\n
		const rest = [\n
			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },\n
			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },\n
			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },\n
			{ label: t('dutycheck', 'Note'), value: a.note || '' },\n
		];\n
		for (const cell of rest) {\n
			const td = create('td', { text: String(cell.value ?? '') });\n
			td.dataset.cell = cell.label;\n
			tr.appendChild(td);\n
		}\n
		const tdActions = create('td');\n
		tdActions.dataset.cell = t('dutycheck', 'Actions');\n
		tdActions.className = 'dc-table__actions';\n
		if (periodOpen && a?.id) {\n
			const editBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Edit'),\n
			});\n
			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));\n
			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));\n
			const cancelBtn = create('button', {\n
				type: 'button',\n
				class: 'button button--text',\n
				text: t('dutycheck', 'Cancel shift'),\n
			});\n
			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));\n
			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));\n
			tdActions.appendChild(editBtn);\n
			tdActions.appendChild(cancelBtn);\n
		} else {\n
			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));\n
		}\n
		tr.appendChild(tdActions);\n
		return tr;\n
	}\n
\n
	function createListSpacer(px) {\n
		const tr = create('tr', { class: 'dc-virtual-spacer' });\n
		tr.setAttribute('aria-hidden', 'true');\n
		tr.style.height = `${Math.max(0, px)}px`;\n
		const td = create('td');\n
		td.colSpan = 8;\n
		td.style.height = `${Math.max(0, px)}px`;\n
		td.style.padding = '0';\n
		td.style.border = '0';\n
		td.style.lineHeight = '0';\n
		tr.appendChild(td);\n
		return tr;\n
	}\n
\n
	function updateListWindowStatus(range, total) {\n
		const el = document.getElementById('dc-roster-list-status');\n
		if (!el) {\n
			return;\n
		}\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		if (cap.mode === 'all') {\n
			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));\n
			return;\n
		}\n
		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')\n
			.replace('{from}', String(cap.from))\n
			.replace('{to}', String(cap.to))\n
			.replace('{total}', String(cap.total));\n
	}\n
\n
	function paintListWindow(options) {\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		if (!tbody) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		if (!assignments.length) {\n
			tbody.replaceChildren();\n
			setAssignmentsEmptyPanelVisible(true);\n
			listState.windowStart = 0;\n
			listState.windowEnd = 0;\n
			listState.paintedCount = 0;\n
			updateListWindowStatus({ start: 0, end: 0 }, 0);\n
			return;\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const scroller = document.getElementById('dc-assignments-table-wrap');\n
		const VW = virtualApi();\n
		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: assignments.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& listState.windowStart === range.start\n
			&& listState.windowEnd === range.end\n
			&& listState.paintedCount === assignments.length) {\n
			updateListWindowStatus(range, assignments.length);\n
			return;\n
		}\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const periodOpen = canAddAssignment();\n
		const frag = document.createDocumentFragment();\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createListSpacer(range.padBefore));\n
		}\n
		const windowRows = assignments.slice(range.start, range.end);\n
		for (const a of windowRows) {\n
			frag.appendChild(buildAssignmentRow(a, periodOpen));\n
		}\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createListSpacer(range.padAfter));\n
		}\n
		tbody.replaceChildren(frag);\n
		listState.windowStart = range.start;\n
		listState.windowEnd = range.end;\n
		listState.paintedCount = assignments.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
		}\n
		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');\n
		const measured = measureHeight(sample);\n
		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {\n
			listState.rowHeight = measured;\n
			listState.remeasuring = true;\n
			paintListWindow({ assignments, force: true });\n
			listState.remeasuring = false;\n
			return;\n
		}\n
		if (measured > 0) {\n
			listState.rowHeight = measured;\n
		}\n
		updateListWindowStatus(range, assignments.length);\n
	}\n
\n
	function selectedPeriodId() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const fromSwitcher = Number(switcher?.value || 0);\n
		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {\n
			return fromSwitcher;\n
		}\n
		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;\n
	}\n
\n
	function periodDateList() {\n
		const periodId = selectedPeriodId();\n
		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));\n
		if (!period?.startDate || !period?.endDate) {\n
			return [];\n
		}\n
		const out = [];\n
		const cursor = new Date(`${period.startDate}T00:00:00Z`);\n
		const end = new Date(`${period.endDate}T00:00:00Z`);\n
		while (cursor <= end) {\n
			out.push(cursor.toISOString().slice(0, 10));\n
			cursor.setUTCDate(cursor.getUTCDate() + 1);\n
			if (out.length > 62) {\n
				break;\n
			}\n
		}\n
		return out;\n
	}\n
\n
	function updateGridWindowStatus(range, total, dayCount) {\n
		const el = document.getElementById('dc-roster-grid-status');\n
		if (!el) {\n
			return;\n
		}\n
		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;\n
		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });\n
		if (cap.mode === 'empty') {\n
			el.textContent = '';\n
			return;\n
		}\n
		let text = '';\n
		if (cap.mode === 'all') {\n
			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));\n
		} else {\n
			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')\n
				.replace('{from}', String(cap.from))\n
				.replace('{to}', String(cap.to))\n
				.replace('{total}', String(cap.total));\n
		}\n
		if (days > 10) {\n
			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');\n
		}\n
		el.textContent = text;\n
	}\n
\n
	function createGridSpacer(px) {\n
		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });\n
		el.setAttribute('aria-hidden', 'true');\n
		el.setAttribute('role', 'presentation');\n
		el.style.height = `${Math.max(0, px)}px`;\n
		return el;\n
	}\n
\n
	function setGridEmptyState(root, message) {\n
		root.removeAttribute('aria-rowcount');\n
		root.removeAttribute('aria-colcount');\n
		root.removeAttribute('tabindex');\n
		root.style.removeProperty('--dc-roster-day-count');\n
		root.style.removeProperty('--dc-roster-day-min');\n
		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');\n
		root.setAttribute('role', 'status');\n
		root.setAttribute('aria-live', 'polite');\n
		root.replaceChildren(create('p', {\n
			class: 'dc-roster-empty-state__text',\n
			text: message,\n
		}));\n
	}\n
\n
	function prepareGridForRows(root, employees, dates) {\n
		root.setAttribute('role', 'grid');\n
		root.removeAttribute('tabindex');\n
		root.removeAttribute('aria-live');\n
		root.setAttribute('aria-rowcount', String(employees.length + 1));\n
		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));\n
		root.style.removeProperty('--dc-roster-day-count');\n
		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));\n
		root.style.setProperty('--dc-roster-day-min', dayMin);\n
		root.classList.toggle('dc-roster-grid--long', dates.length > 10);\n
		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);\n
	}\n
\n
	function buildColHead(day, colIdx, dayMeta) {\n
		const parts = dayMeta || D?.formatRosterColumnParts?.(day);\n
		const full = parts?.full || D?.formatDisplayDate?.(day) || day;\n
		let ariaLabel = parts?.ariaLabel || full;\n
		if (parts?.isToday) {\n
			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;\n
		}\n
		const weekdayShort = parts?.weekdayShort || '';\n
		const dayNum = parts?.day || '';\n
		const classNames = ['dc-roster-grid__colhead'];\n
		if (parts?.isWeekend) {\n
			classNames.push('dc-roster-grid__colhead--weekend');\n
		}\n
		if (parts?.isToday) {\n
			classNames.push('dc-roster-grid__colhead--today');\n
		}\n
		const children = [];\n
		if (weekdayShort) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));\n
		}\n
		if (dayNum) {\n
			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));\n
		}\n
		const attrs = {\n
			'data-col': String(colIdx),\n
			'aria-colindex': String(colIdx + 2),\n
			title: full,\n
			'aria-label': ariaLabel,\n
		};\n
		if (parts?.iso) {\n
			attrs['data-duty-date'] = parts.iso;\n
		}\n
		if (!children.length) {\n
			return create('div', {\n
				class: classNames.join(' '),\n
				role: 'columnheader',\n
				text: full,\n
				attrs,\n
			});\n
		}\n
		return create('div', {\n
			class: classNames.join(' '),\n
			role: 'columnheader',\n
			attrs,\n
		}, children);\n
	}\n
\n
	function buildDutyDayMeta(dates) {\n
		const map = new Map();\n
		(dates || []).forEach((day) => {\n
			map.set(day, D?.formatRosterColumnParts?.(day) || null);\n
		});\n
		return map;\n
	}\n
\n
	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {\n
		const row = create('div', {\n
			class: 'dc-roster-grid__row',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': String(rowIdx + 2) },\n
		});\n
		const prefs = state.preferencesByEmployee[String(emp.id)] || [];\n
		const prefChips = prefs.slice(0, 2).map((p) => {\n
			const band = String(p.band || '');\n
			const label = band === 'early'\n
				? t('dutycheck', 'Früh')\n
				: (band === 'late' ? t('dutycheck', 'Spät') : band);\n
			return create('span', {\n
				class: 'dc-pref-chip dc-pref-chip--static',\n
				text: label,\n
				attrs: { title: label },\n
			});\n
		});\n
		const headKids = [\n
			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),\n
		];\n
		if (prefChips.length) {\n
			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));\n
		}\n
		row.appendChild(create('div', {\n
			class: 'dc-roster-grid__rowhead',\n
			role: 'rowheader',\n
			attrs: { 'aria-colindex': '1' },\n
		}, headKids));\n
		dates.forEach((day, colIdx) => {\n
			const key = cellSelectionKey(emp.id, day);\n
			const cellAssignments = byKey.get(key) || [];\n
			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);\n
			const cell = create('div', {\n
				class: 'dc-roster-grid__cell',\n
				role: 'gridcell',\n
				attrs: {\n
					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',\n
					'data-employee-id': String(emp.id),\n
					'data-duty-date': day,\n
					'data-row': String(rowIdx),\n
					'data-col': String(colIdx),\n
					'aria-colindex': String(colIdx + 2),\n
					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',\n
				},\n
			});\n
			if (dayParts?.isWeekend) {\n
				cell.classList.add('dc-roster-grid__cell--weekend');\n
			}\n
			if (dayParts?.isToday) {\n
				cell.classList.add('dc-roster-grid__cell--today');\n
			}\n
			if (cellAssignments.length) {\n
				cell.classList.add('dc-roster-grid__cell--filled');\n
				const first = cellAssignments[0];\n
				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;\n
				cell.appendChild(create('span', {\n
					class: 'dc-roster-grid__shift',\n
					text: label,\n
				}));\n
				const editLabel = canAddAssignment()\n
					? t('dutycheck', 'Edit assignment')\n
					: t('dutycheck', 'View assignment (read-only)');\n
				cell.setAttribute('aria-label', `${editLabel}: ${label}`);\n
				if (cellAssignments.length > 1) {\n
					cell.appendChild(create('span', {\n
						class: 'dc-roster-grid__more',\n
						text: `+${cellAssignments.length - 1}`,\n
					}));\n
				}\n
			} else {\n
				cell.classList.add('dc-roster-grid__cell--empty');\n
				const blackoutKey = `${emp.id}|${day}`;\n
				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];\n
				if (hasBlackout) {\n
					cell.classList.add('dc-roster-grid__cell--blackout');\n
					cell.appendChild(create('span', {\n
						class: 'dc-blackout-mark',\n
						attrs: { title: t('dutycheck', 'Kann nicht') },\n
					}, [\n
						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),\n
						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),\n
					]));\n
					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));\n
				} else {\n
					cell.setAttribute(\n
						'aria-label',\n
						canAddAssignment()\n
							? t('dutycheck', 'Empty cell — Space to select for bulk fill')\n
							: t('dutycheck', 'Empty cell'),\n
					);\n
				}\n
			}\n
			if (gridState.selected.has(key)) {\n
				cell.classList.add('is-selected');\n
			}\n
			row.appendChild(cell);\n
		});\n
		return row;\n
	}\n
\n
	function syncVisibleGridChrome() {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {\n
			const empId = cell.getAttribute('data-employee-id');\n
			const day = cell.getAttribute('data-duty-date');\n
			const key = cellSelectionKey(empId, day);\n
			const selected = gridState.selected.has(key);\n
			cell.classList.toggle('is-selected', selected);\n
			cell.setAttribute('aria-selected', selected ? 'true' : 'false');\n
			const rowIdx = Number(cell.getAttribute('data-row'));\n
			const colIdx = Number(cell.getAttribute('data-col'));\n
			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;\n
			cell.setAttribute('tabindex', isFocus ? '0' : '-1');\n
		});\n
	}\n
\n
	function focusGridCellIfVisible(root) {\n
		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
		if (node instanceof HTMLElement) {\n
			node.scrollIntoView({\n
				block: 'nearest',\n
				inline: 'nearest',\n
				behavior: 'auto',\n
			});\n
			node.focus();\n
		}\n
	}\n
\n
	function activateGridCell(cell) {\n
		if (!(cell instanceof HTMLElement)) {\n
			return;\n
		}\n
		const rowIdx = Number(cell.getAttribute('data-row'));\n
		const colIdx = Number(cell.getAttribute('data-col'));\n
		if (Number.isInteger(rowIdx) && rowIdx >= 0) {\n
			gridState.focusRow = rowIdx;\n
		}\n
		if (Number.isInteger(colIdx) && colIdx >= 0) {\n
			gridState.focusCol = colIdx;\n
		}\n
		const empId = cell.getAttribute('data-employee-id');\n
		const day = cell.getAttribute('data-duty-date');\n
		const key = cellSelectionKey(empId, day);\n
		const filled = cell.classList.contains('dc-roster-grid__cell--filled');\n
		if (!filled) {\n
			if (canAddAssignment()) {\n
				toggleGridSelection(key);\n
				syncVisibleGridChrome();\n
				updateBulkBar();\n
			}\n
			return;\n
		}\n
		const byKey = assignmentIndex(state.assignments || []);\n
		const cellAssignments = byKey.get(key) || [];\n
		if (canAddAssignment() && cellAssignments[0]) {\n
			openAssignmentEditModal(cellAssignments[0], cell);\n
			return;\n
		}\n
		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');\n
	}\n
\n
	function ensureGridFocusVisible() {\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const employees = state.employees || [];\n
		if (!scroller || !employees.length) {\n
			return;\n
		}\n
		const VW = virtualApi();\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);\n
		const next = VW.scrollTopToRevealIndex({\n
			index: gridState.focusRow,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop: scroller.scrollTop,\n
			total: employees.length,\n
		});\n
		if (Math.abs(next - scroller.scrollTop) > 0.5) {\n
			scroller.scrollTop = next;\n
		}\n
	}\n
\n
	function renderRosterGrid(assignments) {\n
		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });\n
	}\n
\n
	function paintGridWindow(options) {\n
		const root = document.getElementById('dc-roster-grid');\n
		if (!root) {\n
			return;\n
		}\n
		const assignments = options && Array.isArray(options.assignments)\n
			? options.assignments\n
			: (state.assignments || []);\n
		const force = !!(options && options.force);\n
		const employees = state.employees || [];\n
		const dates = periodDateList();\n
		const byKey = assignmentIndex(assignments);\n
\n
		if (!employees.length || !dates.length) {\n
			setGridEmptyState(\n
				root,\n
				t('dutycheck', 'Add employees and an open period to use the planning grid.'),\n
			);\n
			gridState.windowStart = 0;\n
			gridState.windowEnd = 0;\n
			gridState.paintedEmployeeCount = employees.length;\n
			gridState.paintedDateCount = dates.length;\n
			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		prepareGridForRows(root, employees, dates);\n
		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));\n
		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));\n
\n
		const VW = virtualApi();\n
		const scroller = document.getElementById('dc-roster-grid-scroller');\n
		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
		const viewportHeight = scroller && gridState.view === 'grid'\n
			? Math.max(0, scroller.clientHeight - headerHeight)\n
			: 0;\n
		const scrollTop = scroller ? scroller.scrollTop : 0;\n
		const range = VW.visibleRange({\n
			total: employees.length,\n
			rowHeight,\n
			viewportHeight,\n
			scrollTop,\n
			overscan: VW.DEFAULT_OVERSCAN,\n
			paintAll: gridState.paintAll === true,\n
		});\n
		if (!force\n
			&& gridState.windowStart === range.start\n
			&& gridState.windowEnd === range.end\n
			&& gridState.paintedEmployeeCount === employees.length\n
			&& gridState.paintedDateCount === dates.length) {\n
			syncVisibleGridChrome();\n
			updateGridWindowStatus(range, employees.length, dates.length);\n
			updateBulkBar();\n
			return;\n
		}\n
\n
		const savedTop = scroller ? scroller.scrollTop : 0;\n
		const savedLeft = scroller ? scroller.scrollLeft : 0;\n
		const dayMetaByDate = buildDutyDayMeta(dates);\n
		const frag = document.createDocumentFragment();\n
		const header = create('div', {\n
			class: 'dc-roster-grid__row dc-roster-grid__row--head',\n
			role: 'row',\n
			attrs: { 'aria-rowindex': '1' },\n
		});\n
		header.appendChild(create('div', {\n
			class: 'dc-roster-grid__corner',\n
			role: 'columnheader',\n
			text: t('dutycheck', 'Person'),\n
			attrs: { 'aria-colindex': '1' },\n
		}));\n
		dates.forEach((day, colIdx) => {\n
			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));\n
		});\n
		frag.appendChild(header);\n
		if (range.padBefore > 0) {\n
			frag.appendChild(createGridSpacer(range.padBefore));\n
		}\n
		const windowEmployees = employees.slice(range.start, range.end);\n
		windowEmployees.forEach((emp, offset) => {\n
			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));\n
		});\n
		if (range.padAfter > 0) {\n
			frag.appendChild(createGridSpacer(range.padAfter));\n
		}\n
		root.replaceChildren(frag);\n
		gridState.windowStart = range.start;\n
		gridState.windowEnd = range.end;\n
		gridState.paintedEmployeeCount = employees.length;\n
		gridState.paintedDateCount = dates.length;\n
		if (scroller) {\n
			scroller.scrollTop = savedTop;\n
			scroller.scrollLeft = savedLeft;\n
		}\n
		const headerEl = root.querySelector('.dc-roster-grid__row--head');\n
		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');\n
		const measuredHeader = measureHeight(headerEl);\n
		const measuredRow = measureHeight(bodyRow);\n
		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)\n
			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))\n
			&& !gridState.remeasuring) {\n
			if (measuredHeader > 0) {\n
				gridState.headerHeight = measuredHeader;\n
			}\n
			if (measuredRow > 0) {\n
				gridState.rowHeight = measuredRow;\n
			}\n
			gridState.remeasuring = true;\n
			paintGridWindow({ assignments, force: true });\n
			gridState.remeasuring = false;\n
			return;\n
		}\n
		if (measuredHeader > 0) {\n
			gridState.headerHeight = measuredHeader;\n
		}\n
		if (measuredRow > 0) {\n
			gridState.rowHeight = measuredRow;\n
		}\n
		bindGridInteractions(root);\n
		updateGridWindowStatus(range, employees.length, dates.length);\n
		updateBulkBar();\n
	}\n
\n
	function bindGridInteractions(root) {\n
		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {\n
			return;\n
		}\n
		root.dataset.dcGridBound = '1';\n
		root.addEventListener('click', (ev) => {\n
			const cell = ev.target.closest('[role="gridcell"]');\n
			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {\n
				return;\n
			}\n
			activateGridCell(cell);\n
		});\n
		root.addEventListener('keydown', (ev) => {\n
			const employees = state.employees || [];\n
			const dates = periodDateList();\n
			const rowCount = employees.length;\n
			const colCount = dates.length;\n
			if (!rowCount || !colCount) {\n
				return;\n
			}\n
			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];\n
			if (!gridKeys.includes(ev.key)) {\n
				return;\n
			}\n
			ev.preventDefault();\n
			if (ev.key === 'ArrowUp') {\n
				gridState.focusRow = Math.max(0, gridState.focusRow - 1);\n
			} else if (ev.key === 'ArrowDown') {\n
				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);\n
			} else if (ev.key === 'ArrowLeft') {\n
				gridState.focusCol = Math.max(0, gridState.focusCol - 1);\n
			} else if (ev.key === 'ArrowRight') {\n
				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);\n
			} else if (ev.key === 'Home') {\n
				gridState.focusCol = 0;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = 0;\n
				}\n
			} else if (ev.key === 'End') {\n
				gridState.focusCol = colCount - 1;\n
				if (ev.ctrlKey || ev.metaKey) {\n
					gridState.focusRow = rowCount - 1;\n
				}\n
			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {\n
				const VW = virtualApi();\n
				const scroller = document.getElementById('dc-roster-grid-scroller');\n
				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;\n
				const viewportHeight = scroller\n
					? Math.max(rowHeight, scroller.clientHeight - headerHeight)\n
					: rowHeight;\n
				const stride = typeof VW.pageStride === 'function'\n
					? VW.pageStride({ rowHeight, viewportHeight })\n
					: 1;\n
				if (ev.key === 'PageDown') {\n
					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);\n
				} else {\n
					gridState.focusRow = Math.max(0, gridState.focusRow - stride);\n
				}\n
			} else if (ev.key === 'Enter') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				activateGridCell(cell);\n
				focusGridCellIfVisible(root);\n
				return;\n
			} else if (ev.key === ' ' || ev.key === 'Spacebar') {\n
				ensureGridFocusVisible();\n
				paintGridWindow({ force: false });\n
				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);\n
				const empId = cell?.getAttribute('data-employee-id');\n
				const day = cell?.getAttribute('data-duty-date');\n
				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');\n
				if (empId && day && isEmpty && canAddAssignment()) {\n
					toggleGridSelection(cellSelectionKey(empId, day));\n
					syncVisibleGridChrome();\n
					updateBulkBar();\n
				}\n
				focusGridCellIfVisible(root);\n
				return;\n
			}\n
			ensureGridFocusVisible();\n
			paintGridWindow({ force: false });\n
			focusGridCellIfVisible(root);\n
		});\n
	}\n
\n
	function toggleGridSelection(key) {\n
		if (gridState.selected.has(key)) {\n
			gridState.selected.delete(key);\n
		} else {\n
			gridState.selected.add(key);\n
		}\n
	}\n
\n
	function updateBulkBar() {\n
		const bar = document.getElementById('dc-roster-bulk-bar');\n
		const countEl = document.getElementById('dc-roster-bulk-count');\n
		if (!bar || !countEl) {\n
			return;\n
		}\n
		const n = gridState.selected.size;\n
		if (n < 1 || !canAddAssignment()) {\n
			bar.hidden = true;\n
			return;\n
		}\n
		bar.hidden = false;\n
		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (sel && !(sel.options && sel.options.length)) {\n
			populateBulkTemplates();\n
		}\n
	}\n
\n
	async function populateBulkTemplates() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		if (!sel) {\n
			return;\n
		}\n
		await loadTemplates();\n
		sel.replaceChildren();\n
		const templates = state.templates || [];\n
		if (!templates.length) {\n
			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));\n
			return;\n
		}\n
		for (const tpl of templates) {\n
			sel.appendChild(create('option', {\n
				value: String(tpl.id),\n
				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,\n
			}));\n
		}\n
	}\n
\n
	function revealAssignmentRow(id) {\n
		const assignmentId = Number(id);\n
		const assignments = state.assignments || [];\n
		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);\n
		if (idx < 0) {\n
			return;\n
		}\n
		applyRosterViewChrome('list');\n
		const run = () => {\n
			const scroller = document.getElementById('dc-assignments-table-wrap');\n
			const VW = virtualApi();\n
			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;\n
			if (scroller) {\n
				scroller.scrollTop = VW.scrollTopToRevealIndex({\n
					index: idx,\n
					rowHeight,\n
					viewportHeight: Math.max(rowHeight, scroller.clientHeight),\n
					scrollTop: scroller.scrollTop,\n
					total: assignments.length,\n
				});\n
			}\n
			paintListWindow({ assignments, force: true });\n
			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);\n
			if (row instanceof HTMLElement) {\n
				row.scrollIntoView({\n
					block: 'nearest',\n
					behavior: 'auto',\n
				});\n
				row.focus();\n
				row.classList.add('dc-row--flash');\n
				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);\n
			}\n
		};\n
		window.requestAnimationFrame(run);\n
	}\n
\n
	function setRosterPaintAll(paintAll) {\n
		gridState.paintAll = paintAll === true;\n
		paintListWindow({ force: true });\n
		paintGridWindow({ force: true });\n
	}\n
\n
	function wireRosterVirtualization() {\n
		const gridScroller = document.getElementById('dc-roster-grid-scroller');\n
		const listScroller = document.getElementById('dc-assignments-table-wrap');\n
		let gridRaf = 0;\n
		let listRaf = 0;\n
		if (gridScroller) {\n
			gridScroller.addEventListener('scroll', () => {\n
				if (gridRaf) {\n
					return;\n
				}\n
				gridRaf = window.requestAnimationFrame(() => {\n
					gridRaf = 0;\n
					paintGridWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		if (listScroller) {\n
			listScroller.addEventListener('scroll', () => {\n
				if (listRaf) {\n
					return;\n
				}\n
				listRaf = window.requestAnimationFrame(() => {\n
					listRaf = 0;\n
					paintListWindow({ force: false });\n
				});\n
			}, { passive: true });\n
		}\n
		const resize = () => {\n
			if (gridState.view === 'grid') {\n
				paintGridWindow({ force: true });\n
			} else {\n
				paintListWindow({ force: true });\n
			}\n
		};\n
		if (typeof ResizeObserver === 'function') {\n
			const ro = new ResizeObserver(resize);\n
			if (gridScroller) {\n
				ro.observe(gridScroller);\n
			}\n
			if (listScroller) {\n
				ro.observe(listScroller);\n
			}\n
		} else {\n
			window.addEventListener('resize', resize);\n
		}\n
		window.addEventListener('beforeprint', () => setRosterPaintAll(true));\n
		window.addEventListener('afterprint', () => setRosterPaintAll(false));\n
		if (typeof window.matchMedia === 'function') {\n
			try {\n
				const printMq = window.matchMedia('print');\n
				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);\n
				if (typeof printMq.addEventListener === 'function') {\n
					printMq.addEventListener('change', onPrintMq);\n
				} else if (typeof printMq.addListener === 'function') {\n
					printMq.addListener(onPrintMq);\n
				}\n
			} catch (_) {\n
				/* print media query is not available in every test/jsdom shim */\n
			}\n
		}\n
	}\n
\n
	function applyRosterViewChrome(view) {\n
		gridState.view = view === 'list' ? 'list' : 'grid';\n
		const gridWrap = document.getElementById('dc-roster-grid-wrap');\n
		const listPanel = document.getElementById('dc-roster-list-panel');\n
		const gridBtn = document.getElementById('dc-roster-view-grid');\n
		const listBtn = document.getElementById('dc-roster-view-list');\n
		if (gridWrap) {\n
			gridWrap.hidden = gridState.view !== 'grid';\n
		}\n
		if (listPanel) {\n
			listPanel.hidden = gridState.view !== 'list';\n
		}\n
		if (gridBtn) {\n
			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');\n
		}\n
		if (listBtn) {\n
			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');\n
		}\n
	}\n
\n
	function setRosterView(view) {\n
		applyRosterViewChrome(view);\n
		window.requestAnimationFrame(() => {\n
			if (gridState.view === 'list') {\n
				paintListWindow({ force: true });\n
			} else {\n
				paintGridWindow({ force: true });\n
			}\n
		});\n
	}\n
\n
	async function applyBulkFromTemplate() {\n
		const sel = document.getElementById('dc-roster-bulk-template');\n
		const tplId = Number(sel?.value || 0);\n
		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);\n
		if (!tpl || gridState.selected.size < 1) {\n
			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');\n
			return;\n
		}\n
		if (!Number(tpl.locationId || 0)) {\n
			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');\n
			return;\n
		}\n
		const periodId = Number(selectedPeriodId() || 0);\n
		let ok = 0;\n
		let fail = 0;\n
		for (const key of Array.from(gridState.selected)) {\n
			const sep = key.indexOf('|');\n
			if (sep < 1) {\n
				fail += 1;\n
				continue;\n
			}\n
			const employeeId = Number(key.slice(0, sep));\n
			const dutyDate = key.slice(sep + 1);\n
			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {\n
				fail += 1;\n
				continue;\n
			}\n
			const locationId = Number(tpl.locationId || 0);\n
			if (!locationId) {\n
				fail += 1;\n
				continue;\n
			}\n
			try {\n
				await submitAssignment({\n
					periodId,\n
					employeeId: Number(employeeId),\n
					locationId,\n
					dutyDate,\n
					startTime: String(tpl.startTime).slice(0, 5),\n
					endTime: String(tpl.endTime).slice(0, 5),\n
					breakMinutes: Number(tpl.breakMinutes || 0),\n
					note: '',\n
					assignmentId: 0,\n
				}, true);\n
				ok += 1;\n
			} catch {\n
				fail += 1;\n
			}\n
		}\n
		gridState.selected.clear();\n
		await loadRoster(periodId || null);\n
		Msg.announce(\n
			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')\n
				.replace('{ok}', String(ok))\n
				.replace('{fail}', String(fail)),\n
			fail ? 'warning' : 'info',\n
		);\n
	}\n
\n
	function uniqueConflictTypes(conflicts) {\n
		const types = new Set();\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity === 'soft' && conflict?.type) {\n
				types.add(String(conflict.type));\n
			}\n
		}\n
		return Array.from(types);\n
	}\n
\n
	function softConflictSummary(conflicts) {\n
		const lines = [];\n
		for (const conflict of conflicts || []) {\n
			if (conflict?.severity !== 'soft') {\n
				continue;\n
			}\n
			const raw = String(conflict?.message || '');\n
			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');\n
			if (text && !lines.includes(text)) {\n
				lines.push(text);\n
			}\n
		}\n
		return lines.join('\n');\n
	}\n
\n
	function errorCodeFrom(err) {\n
		return String(err?.code || err?.payload?.error?.code || '');\n
	}\n
\n
	function syncAssignmentPeriodFromSwitcher() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (!switcher || !periodHidden) {\n
			return;\n
		}\n
		const selected = Number(switcher.value);\n
		if (Number.isInteger(selected) && selected > 0) {\n
			periodHidden.value = String(selected);\n
		}\n
	}\n
\n
	function announceAssignmentSaveError(err) {\n
		clearAssignmentFormSuccess();\n
		const friendly = errorMessageFor(err);\n
		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');\n
		updateAssignmentFormFeedback(message);\n
		Msg.announce(message, 'error');\n
	}\n
\n
	async function submitAssignment(payload, retryWithAck = true) {\n
		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n
		try {\n
			if (editId > 0) {\n
				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);\n
			}\n
			return await Api.post('/apps/dutycheck/api/assignments', payload);\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Save with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return submitAssignment({ ...payload, acknowledgements }, false);\n
			}\n
			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Employee cannot work (blackout)'),\n
					label: t(\n
						'dutycheck',\n
						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',\n
					),\n
					confirmLabel: t('dutycheck', 'Schedule anyway'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
					maxLength: 200,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	function setAssignmentFormBusy(form, busy) {\n
		if (!form) return;\n
		const submit = form.querySelector('button[type="submit"]');\n
		const clear = document.getElementById('dc-assignment-form-clear');\n
		if (submit) {\n
			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';\n
			if (busy) {\n
				submit.setAttribute('aria-busy', 'true');\n
			} else {\n
				submit.removeAttribute('aria-busy');\n
			}\n
		}\n
		if (clear) {\n
			clear.disabled = busy;\n
		}\n
	}\n
\n
	function renderSetupCallout(data) {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		const list = document.getElementById('dc-roster-setup-checklist');\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!callout || !list || !form) return;\n
		const urls = readUrls();\n
		const missing = [];\n
		if (!state.periods.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Create a planning period.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		if (!state.employees.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active employee.'),\n
				url: urls.employees,\n
				cta: t('dutycheck', 'Go to Employees'),\n
			});\n
		}\n
		if (!state.locations.length) {\n
			missing.push({\n
				text: t('dutycheck', 'Add at least one active location.'),\n
				url: urls.locations,\n
				cta: t('dutycheck', 'Go to Locations'),\n
			});\n
		}\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));\n
		const periodIsOpen = (selectedPeriod?.status || '') === 'open';\n
		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {\n
			missing.push({\n
				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),\n
				url: urls.periods,\n
				cta: t('dutycheck', 'Go to Periods'),\n
			});\n
		}\n
		list.replaceChildren();\n
		if (!missing.length) {\n
			callout.hidden = true;\n
			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {\n
				if (el.dataset.dcLockedBySetup === '1') {\n
					el.disabled = false;\n
					delete el.dataset.dcLockedBySetup;\n
				}\n
			});\n
			updateAddAssignmentControl();\n
			return;\n
		}\n
		for (const item of missing) {\n
			const li = create('li');\n
			li.appendChild(document.createTextNode(item.text + ' '));\n
			if (item.url) {\n
				li.appendChild(create('a', {\n
					class: 'button',\n
					href: item.url,\n
					text: item.cta,\n
				}));\n
			}\n
			list.appendChild(li);\n
		}\n
		callout.hidden = false;\n
		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {\n
			if (!el.disabled) {\n
				el.dataset.dcLockedBySetup = '1';\n
				el.disabled = true;\n
			}\n
		});\n
		updateAddAssignmentControl();\n
	}\n
\n
	function readUrls() {\n
		try {\n
			const raw = document.getElementById('app-content')?.dataset?.dcUrls;\n
			if (!raw) return {};\n
			return JSON.parse(raw) || {};\n
		} catch (_) {\n
			return {};\n
		}\n
	}\n
\n
	function restoreAssignmentFormPanel() {\n
		const host = document.getElementById('dc-assignment-form-host');\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (host && panel && panel.parentElement !== host) {\n
			host.appendChild(panel);\n
		}\n
	}\n
\n
	function selectedPeriodFromState() {\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;\n
		const periodId = Number(raw);\n
		if (!Number.isInteger(periodId) || periodId <= 0) {\n
			return null;\n
		}\n
		return state.periods.find((p) => Number(p.id) === periodId) || null;\n
	}\n
\n
	function isAssignmentSetupBlocking() {\n
		const callout = document.getElementById('dc-roster-setup-callout');\n
		return !!(callout && !callout.hidden);\n
	}\n
\n
	function canAddAssignment() {\n
		if (!state.canCreateAssignments) {\n
			return false;\n
		}\n
		if (!state.periods.length || !state.employees.length || !state.locations.length) {\n
			return false;\n
		}\n
		const period = selectedPeriodFromState();\n
		return !!(period && String(period.status || '').toLowerCase() === 'open');\n
	}\n
\n
	function addAssignmentDisabledReason() {\n
		if (!state.periods.length) {\n
			return t('dutycheck', 'Create a planning period first.');\n
		}\n
		if (!state.employees.length) {\n
			return t('dutycheck', 'Add at least one active employee before planning shifts.');\n
		}\n
		if (!state.locations.length) {\n
			return t('dutycheck', 'Add at least one active location before planning shifts.');\n
		}\n
		const period = selectedPeriodFromState();\n
		if (!period) {\n
			return t('dutycheck', 'Select a period before adding assignments.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!state.canCreateAssignments) {\n
			return t('dutycheck', 'You cannot add assignments for this period.');\n
		}\n
		return t('dutycheck', 'Adding assignments is not available right now.');\n
	}\n
\n
	function updateAddAssignmentControl() {\n
		const allowed = canAddAssignment();\n
		const reason = addAssignmentDisabledReason();\n
		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {\n
			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.\n
			btn.hidden = !allowed;\n
			btn.disabled = !allowed;\n
			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');\n
			btn.classList.toggle('dc-is-disabled', !allowed);\n
			if (allowed) {\n
				btn.removeAttribute('title');\n
			} else {\n
				btn.setAttribute('title', reason);\n
			}\n
		});\n
		const emptyHint = document.getElementById('dc-roster-empty-add-hint');\n
		if (emptyHint) {\n
			emptyHint.textContent = allowed\n
				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')\n
				: reason;\n
		}\n
	}\n
\n
	function clearAssignmentsSectionSuccess() {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.hidden = true;\n
		el.textContent = '';\n
		el.classList.remove('dc-roster-flash--visible');\n
	}\n
\n
	function showAssignmentsSectionSuccess(message) {\n
		const el = document.getElementById('dc-roster-assignments-success');\n
		if (!el) {\n
			return;\n
		}\n
		el.textContent = String(message);\n
		el.hidden = false;\n
		el.classList.add('dc-roster-flash--visible');\n
	}\n
\n
	function prepareAssignmentFormForModal() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		clearAssignmentFormSuccess();\n
		updateAssignmentFormFeedback(null);\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate && period?.endDate) {\n
			dateInput.min = String(period.startDate);\n
			dateInput.max = String(period.endDate);\n
			if (!dateInput.value) {\n
				dateInput.value = String(period.startDate);\n
			}\n
		}\n
		refreshAssignmentFormEligibility();\n
		applyDefaultBreakToForm();\n
		wireBreakPrefillHint();\n
		fillTemplateSelect();\n
		D?.applyLocaleToTemporalInputs(form);\n
	}\n
\n
	function resetAssignmentFormAfterSave(data) {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return;\n
		}\n
		form.reset();\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden && data?.selectedPeriodId) {\n
			periodHidden.value = String(data.selectedPeriodId);\n
		}\n
		applyDefaultBreakToForm();\n
		const period = selectedPeriodFromState();\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput && period?.startDate) {\n
			dateInput.value = String(period.startDate);\n
		}\n
		D?.applyLocaleToTemporalInputs(form);\n
		updateAssignmentFormFeedback(null);\n
	}\n
\n
	async function performAssignmentSave() {\n
		syncAssignmentPeriodFromSwitcher();\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) {\n
			return false;\n
		}\n
		if (isAssignmentSetupBlocking()) {\n
			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(msg);\n
			Msg.announce(msg, 'error');\n
			return false;\n
		}\n
		const payload = readForm(form);\n
		const validation = validate(payload);\n
		if (validation) {\n
			clearAssignmentFormSuccess();\n
			updateAssignmentFormFeedback(validation);\n
			Msg.announce(validation, 'error');\n
			focusFirstInvalidAssignmentField(payload);\n
			return false;\n
		}\n
		updateAssignmentFormFeedback(null);\n
		setAssignmentFormBusy(form, true);\n
		try {\n
			const response = await submitAssignment(payload, true);\n
			render(response?.data || {});\n
			const savedMsg = buildAssignmentSavedMessage(payload);\n
			clearAssignmentFormSuccess();\n
			showAssignmentsSectionSuccess(savedMsg);\n
			Msg.announce(savedMsg, 'success');\n
			rememberBreakMinutes(payload.breakMinutes);\n
			resetAssignmentFormAfterSave(response?.data || {});\n
			return true;\n
		} catch (err) {\n
			announceAssignmentSaveError(err);\n
			return false;\n
		} finally {\n
			setAssignmentFormBusy(form, false);\n
		}\n
	}\n
\n
	function syncAssignmentModalPrimary(instance) {\n
		if (!instance?.primaryBtn) {\n
			return;\n
		}\n
		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();\n
		instance.primaryBtn.disabled = blocked;\n
		if (blocked) {\n
			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());\n
		} else {\n
			instance.primaryBtn.removeAttribute('title');\n
		}\n
	}\n
\n
	async function openAssignmentCreateModal(triggerEl) {\n
		if (!canAddAssignment()) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
			return;\n
		}\n
		state.editingAssignmentId = null;\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = '';\n
		}\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		renderSetupCallout(state.lastRosterData || {});\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Create assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save assignment'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => {\n
				const ok = await performAssignmentSave();\n
				return ok;\n
			},\n
			onClose: (result) => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				if (!result) {\n
					clearAssignmentFormSuccess();\n
					return;\n
				}\n
				const addBtn = document.getElementById('dc-roster-add-assignment');\n
				if (addBtn && typeof addBtn.focus === 'function') {\n
					addBtn.focus();\n
				}\n
			},\n
			onCancel: () => {\n
				clearAssignmentFormSuccess();\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
		const form = document.getElementById('dc-assignment-form');\n
		if (form && !form.dataset.dcModalSyncWired) {\n
			form.dataset.dcModalSyncWired = '1';\n
			form.addEventListener('change', () => {\n
				if (assignmentModalInstance?._open) {\n
					syncAssignmentModalPrimary(assignmentModalInstance);\n
				}\n
			});\n
		}\n
	}\n
\n
	async function openAssignmentEditModal(assignment, triggerEl) {\n
		if (!canAddAssignment() || !assignment?.id) {\n
			Msg.announce(addAssignmentDisabledReason(), 'warning');\n
			return;\n
		}\n
		state.editingAssignmentId = Number(assignment.id);\n
		state.editingAssignmentVersion = Number(assignment.version ?? 0);\n
		await ensureAssignmentModalPrereqs();\n
		prepareAssignmentFormForModal();\n
		const idHidden = document.getElementById('dc-assignment-id');\n
		if (idHidden) {\n
			idHidden.value = String(assignment.id);\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		if (dateInput) {\n
			dateInput.value = String(assignment.dutyDate || '');\n
		}\n
		refreshAssignmentFormEligibility();\n
		const emp = document.getElementById('dc-assignment-employee');\n
		if (emp) {\n
			emp.value = String(assignment.employeeId || '');\n
		}\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (loc) {\n
			loc.value = String(assignment.locationId || '');\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		if (start) {\n
			start.value = String(assignment.startTime || '').slice(0, 5);\n
		}\n
		const end = document.getElementById('dc-assignment-end');\n
		if (end) {\n
			end.value = String(assignment.endTime || '').slice(0, 5);\n
		}\n
		const br = document.getElementById('dc-assignment-break');\n
		if (br) {\n
			br.value = String(assignment.breakMinutes ?? 0);\n
		}\n
		const note = document.getElementById('dc-assignment-note');\n
		if (note) {\n
			note.value = String(assignment.note || '');\n
		}\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		clearAssignmentsSectionSuccess();\n
		const panel = document.getElementById('dc-assignment-form-panel');\n
		if (!panel) {\n
			return;\n
		}\n
		const instance = C.openModal({\n
			title: t('dutycheck', 'Edit assignment'),\n
			dialogClass: 'dc-modal__dialog--roster-assignment',\n
			primaryLabel: t('dutycheck', 'Save changes'),\n
			cancelLabel: t('dutycheck', 'Cancel'),\n
			render: () => panel,\n
			onSubmit: async () => performAssignmentSave(),\n
			onClose: () => {\n
				assignmentModalInstance = null;\n
				state.editingAssignmentId = null;\n
				restoreAssignmentFormHost();\n
				clearAssignmentFormSuccess();\n
				if (triggerEl && typeof triggerEl.focus === 'function') {\n
					triggerEl.focus();\n
				}\n
			},\n
		});\n
		assignmentModalInstance = instance;\n
		syncAssignmentModalPrimary(instance);\n
	}\n
\n
	async function cancelAssignmentRow(assignment, triggerEl) {\n
		if (!assignment?.id || !canAddAssignment()) {\n
			return;\n
		}\n
		const ok = window.confirm(\n
			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),\n
		);\n
		if (!ok) {\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});\n
			render(response?.data || {});\n
			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));\n
			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		} finally {\n
			if (triggerEl && typeof triggerEl.focus === 'function') {\n
				triggerEl.focus();\n
			}\n
		}\n
	}\n
\n
	let templatesInflight = null;\n
\n
	async function ensureAssignmentModalPrereqs() {\n
		const jobs = [loadTemplates()];\n
		if (state.planningDefaultsFresh !== true) {\n
			jobs.push(refreshPlanningDefaultFromServer());\n
		}\n
		await Promise.all(jobs);\n
	}\n
\n
	async function loadTemplates() {\n
		if (state.templatesLoaded) {\n
			fillTemplateSelect();\n
			return;\n
		}\n
		if (templatesInflight) {\n
			await templatesInflight;\n
			fillTemplateSelect();\n
			return;\n
		}\n
		templatesInflight = (async () => {\n
			try {\n
				const response = await Api.get('/apps/dutycheck/api/templates', {});\n
				state.templates = Array.isArray(response?.data) ? response.data : [];\n
				state.templatesLoaded = true;\n
			} catch (_) {\n
				state.templates = [];\n
				state.templatesLoaded = false;\n
			} finally {\n
				templatesInflight = null;\n
			}\n
		})();\n
		await templatesInflight;\n
		fillTemplateSelect();\n
	}\n
\n
	function fillTemplateSelect() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select) {\n
			return;\n
		}\n
		const current = select.value;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));\n
		for (const tpl of state.templates) {\n
			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;\n
			select.appendChild(create('option', { value: String(tpl.id), text: label }));\n
		}\n
		if (current && [...select.options].some((o) => o.value === current)) {\n
			select.value = current;\n
		}\n
	}\n
\n
	function applySelectedTemplate() {\n
		const select = document.getElementById('dc-assignment-template');\n
		if (!select || !select.value) {\n
			return;\n
		}\n
		const tpl = state.templates.find((t) => String(t.id) === String(select.value));\n
		if (!tpl) {\n
			return;\n
		}\n
		const start = document.getElementById('dc-assignment-start');\n
		const end = document.getElementById('dc-assignment-end');\n
		const br = document.getElementById('dc-assignment-break');\n
		const loc = document.getElementById('dc-assignment-location');\n
		if (start) start.value = String(tpl.startTime || '').slice(0, 5);\n
		if (end) end.value = String(tpl.endTime || '').slice(0, 5);\n
		if (br) br.value = String(tpl.breakMinutes ?? 0);\n
		if (loc && tpl.locationId) loc.value = String(tpl.locationId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
		refreshAssignmentFormEligibility();\n
	}\n
\n
	function refreshAcknowledgeStats(periodId) {\n
		const el = document.getElementById('dc-roster-ack-stats');\n
		if (!el) {\n
			return;\n
		}\n
		const id = Number(periodId);\n
		if (!Number.isInteger(id) || id <= 0) {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === id);\n
		const status = String(period?.status || '').toLowerCase();\n
		if (status !== 'published' && status !== 'closed') {\n
			el.hidden = true;\n
			el.textContent = '';\n
			return;\n
		}\n
		const rows = Array.isArray(state.assignments) ? state.assignments : [];\n
		let total = 0;\n
		let acked = 0;\n
		for (const row of rows) {\n
			if (String(row?.status || '').toLowerCase() === 'cancelled') {\n
				continue;\n
			}\n
			total += 1;\n
			if (row?.acknowledged || row?.acknowledgedAt) {\n
				acked += 1;\n
			}\n
		}\n
		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;\n
		el.hidden = false;\n
		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')\n
			.replace('{acked}', String(acked))\n
			.replace('{total}', String(total))\n
			.replace('{pct}', String(pct));\n
	}\n
\n
	function fillCopySourceSelect(selectedPeriodId) {\n
		const select = document.getElementById('dc-roster-copy-source');\n
		const wrap = document.getElementById('dc-roster-copy-period');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!select || !wrap) {\n
			return;\n
		}\n
		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));\n
		const open = period && String(period.status || '').toLowerCase() === 'open';\n
		wrap.hidden = !open;\n
		state.copyPreviewReady = false;\n
		if (applyBtn) applyBtn.disabled = true;\n
		select.replaceChildren();\n
		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));\n
		for (const p of state.periods) {\n
			if (Number(p.id) === Number(selectedPeriodId)) {\n
				continue;\n
			}\n
			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;\n
			select.appendChild(create('option', { value: String(p.id), text: label }));\n
		}\n
	}\n
\n
	async function runCopyPeriod(dryRun) {\n
		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);\n
		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);\n
		const status = document.getElementById('dc-roster-copy-status');\n
		const applyBtn = document.getElementById('dc-roster-copy-apply');\n
		if (!targetId || !sourceId) {\n
			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');\n
			return;\n
		}\n
		try {\n
			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {\n
				sourcePeriodId: sourceId,\n
				dryRun: !!dryRun,\n
			});\n
			const data = response?.data || {};\n
			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);\n
			const skipped = Number(data.skipped ?? 0);\n
			const msg = dryRun\n
				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')\n
					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))\n
					.replace('{skipped}', String(data.wouldSkip ?? skipped))\n
				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')\n
					.replace('{created}', String(created));\n
			if (status) {\n
				status.hidden = false;\n
				status.textContent = msg;\n
			}\n
			Msg.announce(msg, dryRun ? 'info' : 'success');\n
			if (dryRun) {\n
				state.copyPreviewReady = true;\n
				if (applyBtn) applyBtn.disabled = false;\n
			} else {\n
				state.copyPreviewReady = false;\n
				if (applyBtn) applyBtn.disabled = true;\n
				render(data.roster || data || {});\n
				if (!data.roster && data.assignments) {\n
					render(data);\n
				} else if (!data.roster) {\n
					await loadRoster(targetId);\n
				}\n
			}\n
		} catch (err) {\n
			Msg.handleApiError(err);\n
		}\n
	}\n
\n
	function updateAdminExportLinks(selectedPeriodId) {\n
		const csv = document.getElementById('dc-roster-export-csv');\n
		const printLink = document.getElementById('dc-roster-export-print');\n
		if (!csv || !printLink) {\n
			return;\n
		}\n
		const urls = readUrls();\n
		const baseCsv = String(urls.rosterExportCsv || '');\n
		const basePrint = String(urls.rosterPrint || '');\n
		const id = Number(selectedPeriodId);\n
		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;\n
		if (!ok) {\n
			csv.href = '#dc-roster-period-section';\n
			printLink.href = '#dc-roster-period-section';\n
			csv.setAttribute('aria-disabled', 'true');\n
			printLink.setAttribute('aria-disabled', 'true');\n
			return;\n
		}\n
		const q = new URLSearchParams({ periodId: String(id) }).toString();\n
		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;\n
		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;\n
		csv.removeAttribute('aria-disabled');\n
		printLink.removeAttribute('aria-disabled');\n
	}\n
\n
	function wireAdminExportGuards() {\n
		const guard = (event) => {\n
			const el = event.currentTarget;\n
			if (el && el.getAttribute('aria-disabled') === 'true') {\n
				event.preventDefault();\n
				Msg.announce(\n
					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),\n
					'warning',\n
				);\n
			}\n
		};\n
		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);\n
		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);\n
	}\n
\n
	let rosterPhpL10nCache;\n
	function getRosterPhpL10n() {\n
		if (rosterPhpL10nCache !== undefined) {\n
			return rosterPhpL10nCache;\n
		}\n
		const el = document.getElementById('dc-roster-php-l10n');\n
		const raw = el && el.dataset ? el.dataset.l10n : '';\n
		if (!raw) {\n
			rosterPhpL10nCache = null;\n
			return null;\n
		}\n
		try {\n
			rosterPhpL10nCache = JSON.parse(raw) || null;\n
		} catch (_) {\n
			rosterPhpL10nCache = null;\n
		}\n
		return rosterPhpL10nCache;\n
	}\n
\n
	function translateConflictMessage(message) {\n
		const raw = String(message || '');\n
		if (!raw) {\n
			return t('dutycheck', 'Unknown conflict');\n
		}\n
		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];\n
		if (fromPhp) {\n
			return fromPhp;\n
		}\n
		return t('dutycheck', raw);\n
	}\n
\n
	function render(data) {\n
		state.lastRosterData = data;\n
		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {\n
			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);\n
			state.planningDefaultsFresh = true;\n
		}\n
		state.periods = data.periods || [];\n
		state.employees = data.employees || [];\n
		state.locations = data.locations || [];\n
		state.assignments = data.assignments || [];\n
		state.absenceBlocks = data.absenceBlocks || [];\n
		state.canCreateAssignments = Boolean(data.canCreateAssignments);\n
		fillPeriodSwitcher(state.periods, data.selectedPeriodId);\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		if (periodHidden) {\n
			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';\n
		}\n
		const dateInput = document.getElementById('dc-assignment-date');\n
		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));\n
		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {\n
			dateInput.min = String(selectedPeriod.startDate);\n
			dateInput.max = String(selectedPeriod.endDate);\n
			if (!dateInput.value && state.canCreateAssignments) {\n
				dateInput.value = String(selectedPeriod.startDate);\n
			}\n
		} else if (dateInput) {\n
			dateInput.removeAttribute('min');\n
			dateInput.removeAttribute('max');\n
		}\n
		fillSelect('dc-assignment-location', state.locations, {\n
			emptyText: t('dutycheck', 'No active locations yet'),\n
		});\n
		updateLocationTimezoneHint();\n
		refreshAssignmentFormEligibility();\n
		renderAssignments(data.assignments || []);\n
		renderConflicts(data.conflicts || []);\n
		renderSetupCallout(data);\n
		updateAddAssignmentControl();\n
		updateAdminExportLinks(data.selectedPeriodId);\n
		fillCopySourceSelect(data.selectedPeriodId);\n
		refreshAcknowledgeStats(data.selectedPeriodId);\n
		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);\n
	}\n
\n
	function selectedPeriodIdFromUrl() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const raw = params.get('periodId');\n
		const parsed = Number(raw);\n
		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;\n
	}\n
\n
	function updateUrlPeriodId(periodId) {\n
		const params = new URLSearchParams(window.location.search || '');\n
		if (periodId && Number(periodId) > 0) {\n
			params.set('periodId', String(periodId));\n
		} else {\n
			params.delete('periodId');\n
		}\n
		const query = params.toString();\n
		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);\n
	}\n
\n
	async function loadRoster(periodId) {\n
		if (C.isModalOpen?.()) {\n
			C.dismissOpenModal();\n
		} else {\n
			restoreAssignmentFormPanel();\n
		}\n
		setAssignmentsEmptyPanelVisible(false);\n
		const tbody = document.getElementById('dc-assignments-table-body');\n
		C.setLoadingRow(tbody, 7);\n
		try {\n
			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });\n
			render(response?.data || {});\n
			updateUrlPeriodId(response?.data?.selectedPeriodId || null);\n
			void loadRosterSignals(response?.data || {});\n
			void loadCoverageStrip(response?.data?.selectedPeriodId);\n
			return response?.data || {};\n
		} catch (err) {\n
			const code = String(err?.payload?.error?.code || err?.code || '');\n
			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {\n
				try {\n
					const fallback = await Api.get('/apps/dutycheck/api/roster', {});\n
					render(fallback?.data || {});\n
					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);\n
					void loadRosterSignals(fallback?.data || {});\n
					void loadCoverageStrip(fallback?.data?.selectedPeriodId);\n
					Msg.announce(\n
						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),\n
						'warning',\n
					);\n
					return fallback?.data || {};\n
				} catch (retryErr) {\n
					Msg.handleApiError(retryErr);\n
					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
					return null;\n
				}\n
			}\n
			Msg.handleApiError(err);\n
			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));\n
			return null;\n
		} finally {\n
			C.clearLoadingRow(tbody);\n
		}\n
	}\n
\n
	function dateOverlapsBlackout(day, blackout) {\n
		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);\n
		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);\n
		if (!start || !end) return false;\n
		return day >= start && day <= end;\n
	}\n
\n
	async function loadRosterSignals(data) {\n
		state.preferencesByEmployee = {};\n
		state.blackoutsByEmployeeDate = {};\n
		const employees = data.employees || state.employees || [];\n
		const period = (data.periods || state.periods || []).find(\n
			(p) => Number(p.id) === Number(data.selectedPeriodId),\n
		);\n
		const from = period?.startDate || period?.start_date || '';\n
		const to = period?.endDate || period?.end_date || '';\n
		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);\n
		if (!ids.length) return;\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/roster/signals', {\n
				employeeIds: ids,\n
				from,\n
				to,\n
			});\n
			const prefs = res?.data?.preferences || [];\n
			const blackouts = res?.data?.blackouts || [];\n
			prefs.forEach((p) => {\n
				const eid = String(p.employeeId || p.employee_id || '');\n
				if (!eid) return;\n
				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];\n
				state.preferencesByEmployee[eid].push(p);\n
			});\n
			const days = [];\n
			if (from && to) {\n
				const cur = new Date(from + 'T12:00:00');\n
				const end = new Date(to + 'T12:00:00');\n
				while (cur <= end) {\n
					const pad = (n) => String(n).padStart(2, '0');\n
					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);\n
					cur.setDate(cur.getDate() + 1);\n
				}\n
			}\n
			blackouts.forEach((b) => {\n
				const eid = Number(b.employeeId || b.employee_id || 0);\n
				if (!eid) return;\n
				days.forEach((day) => {\n
					if (dateOverlapsBlackout(day, b)) {\n
						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;\n
					}\n
				});\n
			});\n
			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {\n
				paintGridWindow({ force: true });\n
			}\n
		} catch (_) {\n
			// Optional enrichment — ignore failures.\n
		}\n
	}\n
\n
	async function loadCoverageStrip(periodId) {\n
		const strip = document.getElementById('dc-coverage-strip');\n
		const text = document.getElementById('dc-coverage-strip-text');\n
		if (!strip || !text) return;\n
		const id = Number(periodId || 0);\n
		if (!id) {\n
			strip.hidden = true;\n
			return;\n
		}\n
		try {\n
			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);\n
			const d = res?.data || {};\n
			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);\n
			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);\n
			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);\n
			let message;\n
			if (ready || (hard === 0 && soft === 0)) {\n
				message = t('dutycheck', 'Ready to publish — no blocking issues.');\n
				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--ok');\n
			} else if (hard > 0) {\n
				message = t('dutycheck', '{n} must-fix issues before publish.')\n
					.replace('{n}', String(hard));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');\n
				strip.classList.add('dc-coverage-strip--critical');\n
			} else {\n
				message = t('dutycheck', '{n} items need confirmation.')\n
					.replace('{n}', String(soft));\n
				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');\n
				strip.classList.add('dc-coverage-strip--warn');\n
			}\n
			text.textContent = message;\n
			strip.hidden = false;\n
		} catch (_) {\n
			strip.hidden = true;\n
		}\n
	}\n
\n
	function currentSuggestLocationId() {\n
		const params = new URLSearchParams(window.location.search || '');\n
		const fromUrl = Number(params.get('locationId') || 0);\n
		if (Number.isInteger(fromUrl) && fromUrl > 0) {\n
			return fromUrl;\n
		}\n
		const filterEl = document.getElementById('dc-roster-location')\n
			|| document.getElementById('dc-assignment-location');\n
		const fromUi = Number(filterEl?.value || 0);\n
		if (Number.isInteger(fromUi) && fromUi > 0) {\n
			return fromUi;\n
		}\n
		return null;\n
	}\n
\n
	async function runSuggestFill() {\n
		const periodId = Number(selectedPeriodId() || 0);\n
		if (!periodId) {\n
			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');\n
			return;\n
		}\n
		const btn = document.getElementById('dc-roster-suggest-fill');\n
		if (btn) {\n
			btn.disabled = true;\n
			btn.setAttribute('aria-busy', 'true');\n
		}\n
		const locId = currentSuggestLocationId();\n
		const suggestBody = { locationId: locId || null };\n
		try {\n
			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);\n
			const d = preview?.data || {};\n
			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {\n
				const noLoc = Number(d.skippedNoLocation ?? 0);\n
				Msg.announce(\n
					noLoc > 0\n
						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')\n
						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),\n
					'warning',\n
				);\n
				return;\n
			}\n
			const body = create('div', { class: 'dc-suggest-preview' }, [\n
				create('p', {\n
					text: t('dutycheck', 'Would create {n} shifts from patterns.')\n
						.replace('{n}', String(d.created ?? 0)),\n
				}),\n
				create('ul', { class: 'dc-suggest-preview__counts' }, [\n
					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),\n
					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),\n
				]),\n
			]);\n
			const confirmed = await new Promise((resolve) => {\n
				let settled = false;\n
				const finish = (value) => {\n
					if (settled) return;\n
					settled = true;\n
					resolve(value);\n
				};\n
				C.openModal({\n
					title: t('dutycheck', 'Suggest fill preview'),\n
					primaryLabel: t('dutycheck', 'Apply suggest fill'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					render: () => body,\n
					onSubmit: async () => {\n
						finish(true);\n
						return true;\n
					},\n
					onCancel: () => finish(false),\n
					onClose: () => finish(false),\n
				});\n
			});\n
			if (!confirmed) return;\n
			if ((d.created ?? 0) < 1) {\n
				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');\n
				return;\n
			}\n
			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);\n
			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');\n
			await loadRoster(periodId);\n
		} catch (err) {\n
			const code = String(err?.code || err?.payload?.error?.code || '');\n
			if (code === 'ROTATION_DISABLED') {\n
				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');\n
			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {\n
				Msg.announce(\n
					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),\n
					'warning',\n
				);\n
			} else if (code === 'RATE_LIMITED') {\n
				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');\n
			} else {\n
				Msg.handleApiError(err);\n
			}\n
		} finally {\n
			if (btn) {\n
				btn.disabled = false;\n
				btn.removeAttribute('aria-busy');\n
			}\n
		}\n
	}\n
\n
	function clearAssignmentFormFields() {\n
		const form = document.getElementById('dc-assignment-form');\n
		if (!form) return;\n
		const periodHidden = document.getElementById('dc-assignment-period');\n
		const periodVal = periodHidden ? String(periodHidden.value || '') : '';\n
		form.reset();\n
		if (periodHidden && periodVal) {\n
			periodHidden.value = periodVal;\n
		}\n
		applyDefaultBreakToForm();\n
		D?.applyLocaleToTemporalInputs(form);\n
		refreshAssignmentFormEligibility();\n
		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');\n
		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {\n
			showAssignmentFormSuccess(clearedMsg, 'info');\n
		}\n
		Msg.announce(clearedMsg, 'info');\n
	}\n
\n
	function readForm(form) {\n
		const data = new FormData(form);\n
		const rawStart = String(data.get('startTime') || '');\n
		const rawEnd = String(data.get('endTime') || '');\n
		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();\n
		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();\n
		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);\n
		const out = {\n
			periodId: Number(data.get('periodId')),\n
			employeeId: Number(data.get('employeeId')),\n
			locationId: Number(data.get('locationId')),\n
			dutyDate: String(data.get('dutyDate') || ''),\n
			startTime,\n
			endTime,\n
			breakMinutes: Number(data.get('breakMinutes') || 0),\n
			note: String(data.get('note') || '').trim(),\n
			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,\n
		};\n
		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {\n
			out.expectedVersion = Number(state.editingAssignmentVersion);\n
		}\n
		return out;\n
	}\n
\n
	/**\n
	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.\n
	 */\n
	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {\n
		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;\n
		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;\n
		if (start === null || end === null) return null;\n
		let endAdj = end;\n
		if (endAdj <= start) {\n
			endAdj += 24 * 60;\n
		}\n
		return (endAdj - start) - breakMinutes;\n
	}\n
\n
	function validate(payload) {\n
		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {\n
			return t('dutycheck', 'Select a period before saving.');\n
		}\n
		const period = selectedPeriodForPayload(payload);\n
		if (!period) {\n
			return t('dutycheck', 'That planning period was not found.');\n
		}\n
		if (String(period.status || '').toLowerCase() !== 'open') {\n
			return t('dutycheck', 'The selected period is not open for planning.');\n
		}\n
		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {\n
			return t('dutycheck', 'Select an employee.');\n
		}\n
		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {\n
			return t('dutycheck', 'Select a location.');\n
		}\n
		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {\n
			return t('dutycheck', 'Date, start time, and end time are required.');\n
		}\n
		if (period.startDate && period.endDate\n
			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {\n
			return t('dutycheck', 'Date must fall within the selected period.');\n
		}\n
		if (payload.startTime === payload.endTime) {\n
			return t(\n
				'dutycheck',\n
				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
			);\n
		}\n
		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {\n
			return t('dutycheck', 'Break minutes must be between 0 and 720.');\n
		}\n
		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);\n
		if (effective === null) {\n
			return t('dutycheck', 'Please check the dates and times.');\n
		}\n
		if (effective <= 0) {\n
			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
		}\n
		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {\n
			return t('dutycheck', 'This employee has approved leave on the selected date.');\n
		}\n
		if (findOverlapAssignment(payload)) {\n
			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');\n
		}\n
		return null;\n
	}\n
\n
	function errorMessageFor(error) {\n
		const code = errorCodeFrom(error);\n
		switch (code) {\n
			case 'PERIOD_NOT_OPEN':\n
				return t('dutycheck', 'The selected period is not open for planning.');\n
			case 'PERIOD_NOT_FOUND':\n
				return t('dutycheck', 'That planning period was not found.');\n
			case 'DATE_OUTSIDE_PERIOD':\n
				return t('dutycheck', 'Date must fall within the selected period.');\n
			case 'EMPLOYEE_NOT_FOUND':\n
				return t('dutycheck', 'Employee not found or no longer available.');\n
			case 'LOCATION_NOT_FOUND':\n
				return t('dutycheck', 'Location not found or no longer available.');\n
			case 'EQUAL_DUTY_TIMES':\n
				return t(\n
					'dutycheck',\n
					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',\n
				);\n
			case 'INVALID_SHIFT_LENGTH':\n
				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');\n
			case 'INVALID_BREAK_MINUTES':\n
				return t('dutycheck', 'Break minutes are out of the allowed range.');\n
			case 'NOTE_TOO_LONG':\n
				return t('dutycheck', 'Note is too long (maximum 512 characters).');\n
			case 'ASSIGNMENT_OVERLAP':\n
				return t('dutycheck', 'This employee already has an overlapping assignment.');\n
			case 'ASSIGNMENT_DUPLICATE_SLOT':\n
				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');\n
			case 'STALE_VERSION':\n
				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');\n
			case 'CONFLICT_ACK_STALE':\n
				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');\n
			case 'COMPANY_MEMBERSHIP_REQUIRED':\n
				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');\n
			case 'EXPECTED_VERSION_REQUIRED':\n
				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');\n
			case 'ABSENCE_CONFLICT':\n
				return t('dutycheck', 'This employee has an approved absence on that date.');\n
			case 'BLACKOUT_CONFLICT':\n
				return t('dutycheck', 'This employee marked that they cannot work during this time.');\n
			case 'CONFLICT_ACK_REQUIRED':\n
				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');\n
			case 'REASON_TOO_SHORT':\n
				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');\n
			case 'INTERNAL_ERROR':\n
				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');\n
			case 'QUALIFICATION_MISSING':\n
				return t('dutycheck', 'This employee is missing a required qualification for that location.');\n
			case 'ASSIGNMENT_NOT_FOUND':\n
				return t('dutycheck', 'That assignment no longer exists. Reload the page.');\n
			case 'SCHEMA_NOT_READY':\n
				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');\n
			case 'INVALID_DATE':\n
			case 'INVALID_TIME':\n
				return t('dutycheck', 'Please check the dates and times.');\n
			case 'PERIOD_ID_REQUIRED':\n
				return t('dutycheck', 'Select a period before saving.');\n
			case 'EMPLOYEE_ID_REQUIRED':\n
				return t('dutycheck', 'Select an employee.');\n
			case 'LOCATION_ID_REQUIRED':\n
				return t('dutycheck', 'Select a location.');\n
			default:\n
				return null;\n
		}\n
	}\n
\n
\n
	async function loadPendingSwaps() {\n
		const list = document.getElementById('dc-swap-list');\n
		const empty = document.getElementById('dc-swap-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/swaps');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				li.appendChild(create('p', {\n
					text: row.toEmployeeId\n
						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId))\n
							.replace('{to}', String(row.toEmployeeId))\n
						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')\n
							.replace('{id}', String(row.assignmentId))\n
							.replace('{from}', String(row.fromEmployeeId)),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });\n
						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');\n
						await loadPendingSwaps();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });\n
						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');\n
						await loadPendingSwaps();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load swap requests.');\n
			}\n
		}\n
	}\n
\n
	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {\n
		try {\n
			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {\n
				acknowledgements,\n
			});\n
		} catch (error) {\n
			const code = errorCodeFrom(error);\n
			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {\n
				const conflicts = error?.payload?.error?.conflicts || [];\n
				let conflictTypes = uniqueConflictTypes(conflicts);\n
				if (!conflictTypes.length) {\n
					conflictTypes = ['rest_time_violation'];\n
				}\n
				const summary = softConflictSummary(conflicts);\n
				const reason = await C.promptReason({\n
					title: t('dutycheck', 'Planning rule needs your confirmation'),\n
					label: summary\n
						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')\n
							.replace('{details}', summary)\n
						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),\n
					confirmLabel: t('dutycheck', 'Approve with confirmation'),\n
					cancelLabel: t('dutycheck', 'Cancel'),\n
					minLength: 10,\n
				});\n
				if (reason === null || reason.length < 10) {\n
					throw error;\n
				}\n
				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));\n
				return approveOpenShiftClaim(openShiftId, acks, false);\n
			}\n
			throw error;\n
		}\n
	}\n
\n
	async function loadPendingOpenClaims() {\n
		const list = document.getElementById('dc-open-claim-list');\n
		const empty = document.getElementById('dc-open-claim-empty');\n
		if (!list) return;\n
		list.replaceChildren();\n
		try {\n
			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');\n
			const rows = Array.isArray(res?.data) ? res.data : [];\n
			if (empty) empty.hidden = rows.length > 0;\n
			for (const row of rows) {\n
				const li = create('li', { class: 'dc-conflicts__item' });\n
				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;\n
				li.appendChild(create('p', {\n
					text: t('dutycheck', '{date} · employee #{emp}')\n
						.replace('{date}', String(when))\n
						.replace('{emp}', String(row.claimedByEmployeeId || '—')),\n
				}));\n
				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });\n
				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });\n
				approve.style.minHeight = '44px';\n
				reject.style.minHeight = '44px';\n
				approve.addEventListener('click', async () => {\n
					try {\n
						await approveOpenShiftClaim(row.id);\n
						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');\n
						await loadPendingOpenClaims();\n
						await loadRoster(selectedPeriodFromState()?.id || null);\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				reject.addEventListener('click', async () => {\n
					try {\n
						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});\n
						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');\n
						await loadPendingOpenClaims();\n
					} catch (err) {\n
						Msg.handleApiError(err);\n
					}\n
				});\n
				li.appendChild(approve);\n
				li.appendChild(reject);\n
				list.appendChild(li);\n
			}\n
		} catch (_) {\n
			if (empty) {\n
				empty.hidden = false;\n
				empty.textContent = t('dutycheck', 'Could not load pending claims.');\n
			}\n
		}\n
	}\n
\n
	function wireMarketplace() {\n
		const toggle = document.getElementById('dc-open-shift-create');\n
		const form = document.getElementById('dc-open-shift-form');\n
		toggle?.addEventListener('click', () => {\n
			if (!form) return;\n
			form.hidden = !form.hidden;\n
			const loc = document.getElementById('dc-os-location');\n
			if (loc && state.locations?.length) {\n
				loc.replaceChildren();\n
				for (const l of state.locations) {\n
					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));\n
				}\n
			}\n
			const period = selectedPeriodFromState();\n
			const date = document.getElementById('dc-os-date');\n
			if (date && period?.startDate) date.value = String(period.startDate);\n
		});\n
		document.getElementById('dc-os-save')?.addEventListener('click', async () => {\n
			const period = selectedPeriodFromState();\n
			if (!period?.id) {\n
				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');\n
				return;\n
			}\n
			try {\n
				await Api.post('/apps/dutycheck/api/open-shifts', {\n
					periodId: Number(period.id),\n
					locationId: Number(document.getElementById('dc-os-location')?.value || 0),\n
					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),\n
					startTime: String(document.getElementById('dc-os-start')?.value || ''),\n
					endTime: String(document.getElementById('dc-os-end')?.value || ''),\n
					breakMinutes: 0,\n
				});\n
				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');\n
				if (form) form.hidden = true;\n
			} catch (err) {\n
				Msg.handleApiError(err);\n
			}\n
		});\n
	}\n
\n
	document.addEventListener('DOMContentLoaded', async () => {\n
		D?.applyLocaleToTemporalInputs(document);\n
		wireBreakPrefillHint();\n
		window.addEventListener('dc-planning-defaults-changed', (event) => {\n
			const minutes = Number(event?.detail?.defaultBreakMinutes);\n
			if (Number.isFinite(minutes)) {\n
				state.defaultBreakMinutes = clampBreakMinutes(minutes);\n
			}\n
		});\n
		wireAdminExportGuards();\n
		wireMarketplace();\n
		setRosterView('grid');\n
		wireRosterVirtualization();\n
		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));\n
		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));\n
		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {\n
			gridState.selected.clear();\n
			syncVisibleGridChrome();\n
			updateBulkBar();\n
		});\n
		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {\n
			void applyBulkFromTemplate();\n
		});\n
		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {\n
			void runSuggestFill();\n
		});\n
		await Promise.all([\n
			loadRoster(selectedPeriodIdFromUrl()),\n
			loadPendingSwaps(),\n
			loadPendingOpenClaims(),\n
		]);\n
\n
		const switcher = document.getElementById('dc-roster-period-switcher');\n
		switcher?.addEventListener('change', async () => {\n
			clearAssignmentsSectionSuccess();\n
			const periodId = Number(switcher.value);\n
			await Promise.all([\n
				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),\n
				loadPendingSwaps(),\n
				loadPendingOpenClaims(),\n
			]);\n
		});\n
\n
		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {\n
			const btn = event.target.closest('.dc-roster-add-assignment-trigger');\n
			if (!btn) {\n
				return;\n
			}\n
			openAssignmentCreateModal(btn);\n
		});\n
\n
		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {\n
			clearAssignmentFormFields();\n
		});\n
\n
		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {\n
			applySelectedTemplate();\n
		});\n
		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {\n
			runCopyPeriod(true);\n
		});\n
		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {\n
			if (!state.copyPreviewReady) {\n
				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');\n
				return;\n
			}\n
			await runCopyPeriod(false);\n
		});\n
		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {\n
			state.copyPreviewReady = false;\n
			const applyBtn = document.getElementById('dc-roster-copy-apply');\n
			if (applyBtn) applyBtn.disabled = true;\n
		});\n
\n
		const form = document.getElementById('dc-assignment-form');\n
		const scheduleEligibilityRefresh = () => {\n
			clearAssignmentFormSuccess();\n
			clearAssignmentsSectionSuccess();\n
			refreshAssignmentFormEligibility();\n
		};\n
		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);\n
		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);\n
		form?.addEventListener('submit', (event) => {\n
			event.preventDefault();\n
			const dialog = form.closest('.dc-modal__dialog--roster-assignment');\n
			if (!dialog) {\n
				return;\n
			}\n
			const primary = dialog.querySelector('.dc-modal__actions .button.primary');\n
			if (primary && !primary.disabled) {\n
				primary.click();\n
			}\n
		});\n
	});\n
})();\n
' contains "setProperty('--dc-roster-day-count'".

/var/www/html/custom_apps/dutycheck/tests/Unit/Support/RosterVirtualizationContractTest.php:132

FAILURES!
Tests: 6, Assertions: 69, Failures: 1.
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.227438
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.129496
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.158852
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.133544
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.100729
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.089699
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.082836
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.129219
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.176579
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 3.874614
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 0.237212
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.062996
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.045605
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.046387
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 0.567664
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.077291
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.048264
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.171413
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.12128
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.065718
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.040046
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.28128
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.101247
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 0.73888
      type: 'test'
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.11881
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.197155
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    not ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 2.379828
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:287:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /setProperty\('--dc-roster-day-count'/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105625 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: total,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.removeProperty('--dc-roster-day-count');
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:315:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 3.750697
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 98.266337
killed drop_day_count_css_var
== mutant fallback_paints_nothing (must FAIL) ==
TAP version 13
# Subtest: DutyCheckVirtualWindow.visibleRange
    # Subtest: returns an empty window for total 0
    ok 1 - returns an empty window for total 0
      ---
      duration_ms: 1.219488
      type: 'test'
      ...
    # Subtest: fails closed on NaN / negative inputs
    ok 2 - fails closed on NaN / negative inputs
      ---
      duration_ms: 0.126635
      type: 'test'
      ...
    # Subtest: applies overscan around the visible slice
    ok 3 - applies overscan around the visible slice
      ---
      duration_ms: 0.165377
      type: 'test'
      ...
    # Subtest: clamps the last row when scrolled past the end
    ok 4 - clamps the last row when scrolled past the end
      ---
      duration_ms: 0.134525
      type: 'test'
      ...
    # Subtest: paints a fallback window when the viewport is not laid out yet
    ok 5 - paints a fallback window when the viewport is not laid out yet
      ---
      duration_ms: 0.101247
      type: 'test'
      ...
    # Subtest: honors scrollTop while the viewport is still unsized (conflict reveal)
    ok 6 - honors scrollTop while the viewport is still unsized (conflict reveal)
      ---
      duration_ms: 0.129213
      type: 'test'
      ...
    # Subtest: paintAll returns the full range regardless of scroll
    ok 7 - paintAll returns the full range regardless of scroll
      ---
      duration_ms: 0.354311
      type: 'test'
      ...
    # Subtest: never paints more rows than total even with huge overscan
    ok 8 - never paints more rows than total even with huge overscan
      ---
      duration_ms: 0.286947
      type: 'test'
      ...
    # Subtest: keeps the painted window bounded as the roster grows
    ok 9 - keeps the painted window bounded as the roster grows
      ---
      duration_ms: 0.336774
      type: 'test'
      ...
    1..9
ok 1 - DutyCheckVirtualWindow.visibleRange
  ---
  duration_ms: 4.290455
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.scrollTopToRevealIndex
    # Subtest: scrolls up when the row sits above the viewport
    ok 1 - scrolls up when the row sits above the viewport
      ---
      duration_ms: 1.553605
      type: 'test'
      ...
    # Subtest: scrolls down when the row sits below the viewport
    ok 2 - scrolls down when the row sits below the viewport
      ---
      duration_ms: 0.12913
      type: 'test'
      ...
    # Subtest: leaves scrollTop unchanged when the row is already visible
    ok 3 - leaves scrollTop unchanged when the row is already visible
      ---
      duration_ms: 0.10963
      type: 'test'
      ...
    # Subtest: exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
    ok 4 - exposes the same helper as scrollOffsetToRevealIndex for horizontal reveal
      ---
      duration_ms: 0.10869
      type: 'test'
      ...
    1..4
ok 2 - DutyCheckVirtualWindow.scrollTopToRevealIndex
  ---
  duration_ms: 2.216446
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.pageStride
    # Subtest: pages by almost a full viewport so the previous row stays as context
    ok 1 - pages by almost a full viewport so the previous row stays as context
      ---
      duration_ms: 0.205631
      type: 'test'
      ...
    # Subtest: never returns a zero stride
    ok 2 - never returns a zero stride
      ---
      duration_ms: 0.12967
      type: 'test'
      ...
    1..2
ok 3 - DutyCheckVirtualWindow.pageStride
  ---
  duration_ms: 0.44924
  type: 'suite'
  ...
# Subtest: DutyCheckVirtualWindow.windowCaption
    # Subtest: uses 1-based inclusive bounds for a partial window
    ok 1 - uses 1-based inclusive bounds for a partial window
      ---
      duration_ms: 0.259815
      type: 'test'
      ...
    # Subtest: reports all when the window covers the list
    ok 2 - reports all when the window covers the list
      ---
      duration_ms: 0.136453
      type: 'test'
      ...
    # Subtest: reports empty for an empty list
    ok 3 - reports empty for an empty list
      ---
      duration_ms: 0.111867
      type: 'test'
      ...
    1..3
ok 4 - DutyCheckVirtualWindow.windowCaption
  ---
  duration_ms: 0.637529
  type: 'suite'
  ...
# Subtest: roster.js virtualizes grid and list without paginating the API
    # Subtest: loads the virtual-window helper before roster.js
    ok 1 - loads the virtual-window helper before roster.js
      ---
      duration_ms: 0.190519
      type: 'test'
      ...
    # Subtest: windows employee rows and assignment rows
    not ok 2 - windows employee rows and assignment rows
      ---
      duration_ms: 2.459604
      type: 'test'
      location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:245:2'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /end:\s*total/. Input:
        
        '(function () {\n' +
          "\t'use strict';\n" +
          '\n' +
          '\tconst Api = window.DutyCheckApi;\n' +
          '\tconst Msg = window.DutyCheckMessaging;\n' +
          '\tconst C = window.DutyCheckComponents || window.DutyCheckDom || {};\n' +
          '\tconst D = window.DutyCheckDates;\n' +
          '\tconst ConflictLabels = window.DutyCheckConflictLabels;\n' +
          '\tconst create = C.createElement;\n' +
          "\tif (typeof create !== 'function') {\n" +
          "\t\tthrow new Error('DutyCheck components failed to load');\n" +
          '\t}\n' +
          '\n' +
          "\tconst BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';\n" +
          '\n' +
          '\tconst state = {\n' +
          '\t\tperiods: [],\n' +
          '\t\temployees: [],\n' +
          '\t\tlocations: [],\n' +
          '\t\tassignments: [],\n' +
          '\t\tabsenceBlocks: [],\n' +
          '\t\tcanCreateAssignments: false,\n' +
          '\t\tlastRosterData: null,\n' +
          '\t\tdefaultBreakMinutes: 0,\n' +
          '\t\tplanningDefaultsFresh: false,\n' +
          '\t\ttemplates: [],\n' +
          '\t\ttemplatesLoaded: false,\n' +
          '\t\teditingAssignmentId: null,\n' +
          '\t\teditingAssignmentVersion: null,\n' +
          '\t\tcopyPreviewReady: false,\n' +
          '\t\tpreferencesByEmployee: {},\n' +
          '\t\tblackoutsByEmployeeDate: {},\n' +
          '\t};\n' +
          '\tlet assignmentModalInstance = null;\n' +
          '\tlet suggestedBreakMinutes = 0;\n' +
          '\n' +
          '\tfunction clampBreakMinutes(value) {\n' +
          '\t\tconst n = Number(value);\n' +
          '\t\tif (!Number.isFinite(n)) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn Math.max(0, Math.min(720, Math.round(n)));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction readStoredBreakMinutes() {\n' +
          '\t\ttry {\n' +
          '\t\t\tconst raw = sessionStorage.getItem(BREAK_STORAGE_KEY);\n' +
          "\t\t\tif (raw === null || raw === '') {\n" +
          '\t\t\t\treturn null;\n' +
          '\t\t\t}\n' +
          '\t\t\treturn clampBreakMinutes(raw);\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction rememberBreakMinutes(minutes) {\n' +
          '\t\ttry {\n' +
          '\t\t\tsessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* ignore quota / privacy mode */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction resolveBreakPrefill() {\n' +
          '\t\tconst org = clampBreakMinutes(state.defaultBreakMinutes);\n' +
          '\t\t// Organisation default from Settings always wins when it is greater than zero.\n' +
          '\t\tif (org > 0) {\n' +
          "\t\t\treturn { minutes: org, source: 'settings' };\n" +
          '\t\t}\n' +
          '\t\tconst stored = readStoredBreakMinutes();\n' +
          '\t\tif (stored !== null) {\n' +
          "\t\t\treturn { minutes: stored, source: 'last' };\n" +
          '\t\t}\n' +
          "\t\treturn { minutes: 0, source: 'settings' };\n" +
          '\t}\n' +
          '\n' +
          '\tasync function refreshPlanningDefaultFromServer() {\n' +
          '\t\ttry {\n' +
          "\t\t\tconst response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');\n" +
          '\t\t\tconst minutes = Number(response?.planning?.defaultBreakMinutes);\n' +
          '\t\t\tif (Number.isFinite(minutes)) {\n' +
          '\t\t\t\tstate.defaultBreakMinutes = clampBreakMinutes(minutes);\n' +
          '\t\t\t}\n' +
          '\t\t\tstate.planningDefaultsFresh = true;\n' +
          '\t\t} catch (_) {\n' +
          '\t\t\t/* Fall back to defaultBreakMinutes from the last roster API load. */\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction breakPrefillMessage(prefill) {\n' +
          '\t\tconst n = String(prefill.minutes);\n' +
          "\t\tif (prefill.source === 'last') {\n" +
          "\t\t\treturn t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);\n" +
          '\t\t}\n' +
          '\t\tif (prefill.minutes === 0) {\n' +
          "\t\t\treturn t('dutycheck', 'Pre-filled with 0 min — no break.');\n" +
          '\t\t}\n' +
          "\t\treturn t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);\n" +
          '\t}\n' +
          '\n' +
          '\tfunction updateBreakPrefillHint(userEdited) {\n' +
          "\t\tconst el = document.getElementById('dc-assignment-break-prefill');\n" +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (userEdited) {\n' +
          "\t\t\tel.textContent = t('dutycheck', 'You changed the suggested break for this shift.');\n" +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tel.textContent = breakPrefillMessage(resolveBreakPrefill());\n' +
          '\t}\n' +
          '\n' +
          '\tfunction applyDefaultBreakToForm() {\n' +
          '\t\tconst prefill = resolveBreakPrefill();\n' +
          '\t\tsuggestedBreakMinutes = prefill.minutes;\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (br) {\n' +
          '\t\t\tbr.value = String(prefill.minutes);\n' +
          '\t\t}\n' +
          '\t\tupdateBreakPrefillHint(false);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction wireBreakPrefillHint() {\n' +
          "\t\tconst br = document.getElementById('dc-assignment-break');\n" +
          '\t\tif (!br || br.dataset.dcBreakPrefillWired) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          "\t\tbr.dataset.dcBreakPrefillWired = '1';\n" +
          "\t\tbr.addEventListener('input', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tupdateBreakPrefillHint(current !== suggestedBreakMinutes);\n' +
          '\t\t});\n' +
          "\t\tbr.addEventListener('change', () => {\n" +
          '\t\t\tconst current = clampBreakMinutes(br.value);\n' +
          '\t\t\tif (current === suggestedBreakMinutes) {\n' +
          '\t\t\t\tupdateBreakPrefillHint(false);\n' +
          '\t\t\t}\n' +
          '\t\t});\n' +
          '\t}\n' +
          '\n' +
          '\tfunction dateToDayIndex(isoDate) {\n' +
          "\t\tconst parts = String(isoDate || '').split('-').map((segment) => Number(segment));\n" +
          '\t\tif (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\treturn Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);\n' +
          '\t}\n' +
          '\n' +
          '\tfunction assignmentAbsoluteRange(dutyDate, startTime, endTime) {\n' +
          '\t\tconst dayIndex = dateToDayIndex(dutyDate);\n' +
          '\t\tconst start = D?.wallClockMinutesFromTimeString?.(startTime);\n' +
          '\t\tconst end = D?.wallClockMinutesFromTimeString?.(endTime);\n' +
          '\t\tif (dayIndex === null || start === null || end === null) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst absoluteStart = (dayIndex * 1440) + start;\n' +
          '\t\tlet absoluteEnd = (dayIndex * 1440) + end;\n' +
          '\t\tif (absoluteEnd <= absoluteStart) {\n' +
          '\t\t\tabsoluteEnd += 1440;\n' +
          '\t\t}\n' +
          '\t\treturn [absoluteStart, absoluteEnd];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction absoluteRangesOverlap(a, b) {\n' +
          '\t\treturn a[0] < b[1] && b[0] < a[1];\n' +
          '\t}\n' +
          '\n' +
          '\tfunction isDateInAbsenceBlock(employeeId, dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn false;\n' +
          '\t\t}\n' +
          '\t\tfor (const block of state.absenceBlocks) {\n' +
          '\t\t\tif (Number(block?.employeeId) !== Number(employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          "\t\t\tconst start = String(block?.startDate || '');\n" +
          "\t\t\tconst end = String(block?.endDate || '');\n" +
          '\t\t\tif (dutyDate >= start && dutyDate <= end) {\n' +
          '\t\t\t\treturn true;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn false;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction employeesAvailableOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn [];\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction countEmployeesBlockedOnDate(dutyDate) {\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\treturn 0;\n' +
          '\t\t}\n' +
          '\t\treturn state.employees.length - employeesAvailableOnDate(dutyDate).length;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction appendHintLink(parent, beforeText, linkText, afterText, href) {\n' +
          '\t\tparent.appendChild(document.createTextNode(beforeText));\n' +
          '\t\tif (href) {\n' +
          "\t\t\tconst link = create('a', {\n" +
          "\t\t\t\tclass: 'dc-hint-link',\n" +
          '\t\t\t\thref,\n' +
          '\t\t\t\ttext: linkText,\n' +
          '\t\t\t});\n' +
          '\t\t\tparent.appendChild(link);\n' +
          '\t\t\tparent.appendChild(document.createTextNode(afterText));\n' +
          '\t\t} else {\n' +
          '\t\t\tparent.appendChild(document.createTextNode(linkText + afterText));\n' +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setEmployeeAvailabilityHint(dutyDate, available, blocked) {\n' +
          "\t\tconst hint = document.getElementById('dc-assignment-employee-hint');\n" +
          '\t\tif (!hint) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst urls = readUrls();\n' +
          '\t\thint.replaceChildren();\n' +
          '\t\tif (!dutyDate) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length && blocked > 0) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')\n" +
          "\t\t\t\t\t.replace('{n}', String(blocked)),\n" +
          "\t\t\t\tt('dutycheck', 'review absences'),\n" +
          "\t\t\t\t'.',\n" +
          "\t\t\t\tString(urls.absences || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (!available.length) {\n' +
          '\t\t\tappendHintLink(\n' +
          '\t\t\t\thint,\n' +
          "\t\t\t\tt('dutycheck', 'No employees are available. Add an active employee on the '),\n" +
          "\t\t\t\tt('dutycheck', 'Employees'),\n" +
          "\t\t\t\tt('dutycheck', ' page first.'),\n" +
          "\t\t\t\tString(urls.employees || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (blocked > 0) {\n' +
          '\t\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\t\tt('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')\n" +
          "\t\t\t\t\t.replace('{available}', String(available.length))\n" +
          "\t\t\t\t\t.replace('{hidden}', String(blocked)),\n" +
          '\t\t\t));\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\thint.appendChild(document.createTextNode(\n' +
          "\t\t\tt('dutycheck', 'All {n} active employees can be scheduled on this date.')\n" +
          "\t\t\t\t.replace('{n}', String(available.length)),\n" +
          '\t\t));\n' +
          '\t}\n' +
          '\n' +
          '\tfunction refreshEmployeeSelectForDate() {\n' +
          "\t\tconst dateInput = document.getElementById('dc-assignment-date');\n" +
          "\t\tconst dutyDate = dateInput ? String(dateInput.value || '') : '';\n" +
          '\t\tconst available = employeesAvailableOnDate(dutyDate);\n' +
          '\t\tconst blocked = countEmployeesBlockedOnDate(dutyDate);\n' +
          '\t\tsetEmployeeAvailabilityHint(dutyDate, available, blocked);\n' +
          "\t\tfillSelect('dc-assignment-employee', available, {\n" +
          '\t\t\temptyText: dutyDate\n' +
          "\t\t\t\t? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')\n" +
          "\t\t\t\t: t('dutycheck', 'Pick a date first'),\n" +
          '\t\t});\n' +
          "\t\tconst employeeSelect = document.getElementById('dc-assignment-employee');\n" +
          '\t\tif (employeeSelect) {\n' +
          "\t\t\temployeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction findOverlapAssignment(payload) {\n' +
          '\t\tconst candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);\n' +
          '\t\tif (!candidate) {\n' +
          '\t\t\treturn null;\n' +
          '\t\t}\n' +
          '\t\tconst ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);\n' +
          '\t\tfor (const assignment of state.assignments) {\n' +
          '\t\t\tif (ignoreId > 0 && Number(assignment?.id) === ignoreId) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tif (Number(assignment?.employeeId) !== Number(payload.employeeId)) {\n' +
          '\t\t\t\tcontinue;\n' +
          '\t\t\t}\n' +
          '\t\t\tconst existing = assignmentAbsoluteRange(\n' +
          "\t\t\t\tString(assignment?.dutyDate || ''),\n" +
          "\t\t\t\tString(assignment?.startTime || ''),\n" +
          "\t\t\t\tString(assignment?.endTime || ''),\n" +
          '\t\t\t);\n' +
          '\t\t\tif (existing && absoluteRangesOverlap(candidate, existing)) {\n' +
          '\t\t\t\treturn assignment;\n' +
          '\t\t\t}\n' +
          '\t\t}\n' +
          '\t\treturn null;\n' +
          '\t}\n' +
          '\n' +
          '\tfunction setFormFeedbackA11y(el, isError) {\n' +
          '\t\tif (!el) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tif (isError) {\n' +
          "\t\t\tel.setAttribute('role', 'alert');\n" +
          "\t\t\tel.setAttribute('aria-live', 'assertive');\n" +
          '\t\t} else {\n' +
          "\t\t\tel.setAttribute('role', 'status');\n" +
          "\t\t\tel.setAttribute('aria-live', 'polite');\n" +
          '\t\t}\n' +
          '\t}\n' +
          '\n' +
          '\tfunction focusFirstInvalidAssignmentField(payload) {\n' +
          '\t\tif (!payload) {\n' +
          '\t\t\treturn;\n' +
          '\t\t}\n' +
          '\t\tconst period = selectedPeriodForPayload(payload);\n' +
          '\t\tconst order = [\n' +
          "\t\t\t{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },\n" +
          "\t\t\t{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },\n" +
          '\t\t\t{\n' +
          '\t\t\t\ttest: () => period?.startDate && period?.endDate && payload.dutyDate\n' +
          '\t\t\t\t\t&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),\n' +
          "\t\t\t\tid: 'dc-assignment-date',\n" +
          '\t\t\t},\n' +
          "\t\t\t{ test: () => !payload.employeeId, id: 'dc-ass"... 105653 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        (function () {
        	'use strict';
        
        	const Api = window.DutyCheckApi;
        	const Msg = window.DutyCheckMessaging;
        	const C = window.DutyCheckComponents || window.DutyCheckDom || {};
        	const D = window.DutyCheckDates;
        	const ConflictLabels = window.DutyCheckConflictLabels;
        	const create = C.createElement;
        	if (typeof create !== 'function') {
        		throw new Error('DutyCheck components failed to load');
        	}
        
        	const BREAK_STORAGE_KEY = 'dutycheck.roster.lastBreakMinutes';
        
        	const state = {
        		periods: [],
        		employees: [],
        		locations: [],
        		assignments: [],
        		absenceBlocks: [],
        		canCreateAssignments: false,
        		lastRosterData: null,
        		defaultBreakMinutes: 0,
        		planningDefaultsFresh: false,
        		templates: [],
        		templatesLoaded: false,
        		editingAssignmentId: null,
        		editingAssignmentVersion: null,
        		copyPreviewReady: false,
        		preferencesByEmployee: {},
        		blackoutsByEmployeeDate: {},
        	};
        	let assignmentModalInstance = null;
        	let suggestedBreakMinutes = 0;
        
        	function clampBreakMinutes(value) {
        		const n = Number(value);
        		if (!Number.isFinite(n)) {
        			return 0;
        		}
        		return Math.max(0, Math.min(720, Math.round(n)));
        	}
        
        	function readStoredBreakMinutes() {
        		try {
        			const raw = sessionStorage.getItem(BREAK_STORAGE_KEY);
        			if (raw === null || raw === '') {
        				return null;
        			}
        			return clampBreakMinutes(raw);
        		} catch (_) {
        			return null;
        		}
        	}
        
        	function rememberBreakMinutes(minutes) {
        		try {
        			sessionStorage.setItem(BREAK_STORAGE_KEY, String(clampBreakMinutes(minutes)));
        		} catch (_) {
        			/* ignore quota / privacy mode */
        		}
        	}
        
        	function resolveBreakPrefill() {
        		const org = clampBreakMinutes(state.defaultBreakMinutes);
        		// Organisation default from Settings always wins when it is greater than zero.
        		if (org > 0) {
        			return { minutes: org, source: 'settings' };
        		}
        		const stored = readStoredBreakMinutes();
        		if (stored !== null) {
        			return { minutes: stored, source: 'last' };
        		}
        		return { minutes: 0, source: 'settings' };
        	}
        
        	async function refreshPlanningDefaultFromServer() {
        		try {
        			const response = await Api.get('/apps/dutycheck/api/admin/planning-defaults');
        			const minutes = Number(response?.planning?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        			state.planningDefaultsFresh = true;
        		} catch (_) {
        			/* Fall back to defaultBreakMinutes from the last roster API load. */
        		}
        	}
        
        	function breakPrefillMessage(prefill) {
        		const n = String(prefill.minutes);
        		if (prefill.source === 'last') {
        			return t('dutycheck', 'Pre-filled with {n} min — same as your last shift.').replace('{n}', n);
        		}
        		if (prefill.minutes === 0) {
        			return t('dutycheck', 'Pre-filled with 0 min — no break.');
        		}
        		return t('dutycheck', 'Pre-filled with {n} min — organisation default in Settings.').replace('{n}', n);
        	}
        
        	function updateBreakPrefillHint(userEdited) {
        		const el = document.getElementById('dc-assignment-break-prefill');
        		if (!el) {
        			return;
        		}
        		if (userEdited) {
        			el.textContent = t('dutycheck', 'You changed the suggested break for this shift.');
        			return;
        		}
        		el.textContent = breakPrefillMessage(resolveBreakPrefill());
        	}
        
        	function applyDefaultBreakToForm() {
        		const prefill = resolveBreakPrefill();
        		suggestedBreakMinutes = prefill.minutes;
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(prefill.minutes);
        		}
        		updateBreakPrefillHint(false);
        	}
        
        	function wireBreakPrefillHint() {
        		const br = document.getElementById('dc-assignment-break');
        		if (!br || br.dataset.dcBreakPrefillWired) {
        			return;
        		}
        		br.dataset.dcBreakPrefillWired = '1';
        		br.addEventListener('input', () => {
        			const current = clampBreakMinutes(br.value);
        			updateBreakPrefillHint(current !== suggestedBreakMinutes);
        		});
        		br.addEventListener('change', () => {
        			const current = clampBreakMinutes(br.value);
        			if (current === suggestedBreakMinutes) {
        				updateBreakPrefillHint(false);
        			}
        		});
        	}
        
        	function dateToDayIndex(isoDate) {
        		const parts = String(isoDate || '').split('-').map((segment) => Number(segment));
        		if (parts.length !== 3 || parts.some((n) => !Number.isFinite(n))) {
        			return null;
        		}
        		return Math.floor(Date.UTC(parts[0], parts[1] - 1, parts[2]) / 86400000);
        	}
        
        	function assignmentAbsoluteRange(dutyDate, startTime, endTime) {
        		const dayIndex = dateToDayIndex(dutyDate);
        		const start = D?.wallClockMinutesFromTimeString?.(startTime);
        		const end = D?.wallClockMinutesFromTimeString?.(endTime);
        		if (dayIndex === null || start === null || end === null) {
        			return null;
        		}
        		const absoluteStart = (dayIndex * 1440) + start;
        		let absoluteEnd = (dayIndex * 1440) + end;
        		if (absoluteEnd <= absoluteStart) {
        			absoluteEnd += 1440;
        		}
        		return [absoluteStart, absoluteEnd];
        	}
        
        	function absoluteRangesOverlap(a, b) {
        		return a[0] < b[1] && b[0] < a[1];
        	}
        
        	function isDateInAbsenceBlock(employeeId, dutyDate) {
        		if (!dutyDate) {
        			return false;
        		}
        		for (const block of state.absenceBlocks) {
        			if (Number(block?.employeeId) !== Number(employeeId)) {
        				continue;
        			}
        			const start = String(block?.startDate || '');
        			const end = String(block?.endDate || '');
        			if (dutyDate >= start && dutyDate <= end) {
        				return true;
        			}
        		}
        		return false;
        	}
        
        	function employeesAvailableOnDate(dutyDate) {
        		if (!dutyDate) {
        			return [];
        		}
        		return state.employees.filter((employee) => !isDateInAbsenceBlock(employee.id, dutyDate));
        	}
        
        	function countEmployeesBlockedOnDate(dutyDate) {
        		if (!dutyDate) {
        			return 0;
        		}
        		return state.employees.length - employeesAvailableOnDate(dutyDate).length;
        	}
        
        	function appendHintLink(parent, beforeText, linkText, afterText, href) {
        		parent.appendChild(document.createTextNode(beforeText));
        		if (href) {
        			const link = create('a', {
        				class: 'dc-hint-link',
        				href,
        				text: linkText,
        			});
        			parent.appendChild(link);
        			parent.appendChild(document.createTextNode(afterText));
        		} else {
        			parent.appendChild(document.createTextNode(linkText + afterText));
        		}
        	}
        
        	function setEmployeeAvailabilityHint(dutyDate, available, blocked) {
        		const hint = document.getElementById('dc-assignment-employee-hint');
        		if (!hint) {
        			return;
        		}
        		const urls = readUrls();
        		hint.replaceChildren();
        		if (!dutyDate) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', 'First pick a date above. Anyone on approved leave that day is left out of the list.'),
        			));
        			return;
        		}
        		if (!available.length && blocked > 0) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'Nobody can work this day — all {n} employees are on approved leave. Pick another date or ')
        					.replace('{n}', String(blocked)),
        				t('dutycheck', 'review absences'),
        				'.',
        				String(urls.absences || ''),
        			);
        			return;
        		}
        		if (!available.length) {
        			appendHintLink(
        				hint,
        				t('dutycheck', 'No employees are available. Add an active employee on the '),
        				t('dutycheck', 'Employees'),
        				t('dutycheck', ' page first.'),
        				String(urls.employees || ''),
        			);
        			return;
        		}
        		if (blocked > 0) {
        			hint.appendChild(document.createTextNode(
        				t('dutycheck', '{available} can be scheduled · {hidden} on approved leave (not in the list)')
        					.replace('{available}', String(available.length))
        					.replace('{hidden}', String(blocked)),
        			));
        			return;
        		}
        		hint.appendChild(document.createTextNode(
        			t('dutycheck', 'All {n} active employees can be scheduled on this date.')
        				.replace('{n}', String(available.length)),
        		));
        	}
        
        	function refreshEmployeeSelectForDate() {
        		const dateInput = document.getElementById('dc-assignment-date');
        		const dutyDate = dateInput ? String(dateInput.value || '') : '';
        		const available = employeesAvailableOnDate(dutyDate);
        		const blocked = countEmployeesBlockedOnDate(dutyDate);
        		setEmployeeAvailabilityHint(dutyDate, available, blocked);
        		fillSelect('dc-assignment-employee', available, {
        			emptyText: dutyDate
        				? t('dutycheck', 'No one is available on this date (approved leave). Pick another date.')
        				: t('dutycheck', 'Pick a date first'),
        		});
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		if (employeeSelect) {
        			employeeSelect.disabled = !dutyDate || !available.length || employeeSelect.dataset.dcLockedBySetup === '1';
        		}
        	}
        
        	function findOverlapAssignment(payload) {
        		const candidate = assignmentAbsoluteRange(payload.dutyDate, payload.startTime, payload.endTime);
        		if (!candidate) {
        			return null;
        		}
        		const ignoreId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		for (const assignment of state.assignments) {
        			if (ignoreId > 0 && Number(assignment?.id) === ignoreId) {
        				continue;
        			}
        			if (Number(assignment?.employeeId) !== Number(payload.employeeId)) {
        				continue;
        			}
        			const existing = assignmentAbsoluteRange(
        				String(assignment?.dutyDate || ''),
        				String(assignment?.startTime || ''),
        				String(assignment?.endTime || ''),
        			);
        			if (existing && absoluteRangesOverlap(candidate, existing)) {
        				return assignment;
        			}
        		}
        		return null;
        	}
        
        	function setFormFeedbackA11y(el, isError) {
        		if (!el) {
        			return;
        		}
        		if (isError) {
        			el.setAttribute('role', 'alert');
        			el.setAttribute('aria-live', 'assertive');
        		} else {
        			el.setAttribute('role', 'status');
        			el.setAttribute('aria-live', 'polite');
        		}
        	}
        
        	function focusFirstInvalidAssignmentField(payload) {
        		if (!payload) {
        			return;
        		}
        		const period = selectedPeriodForPayload(payload);
        		const order = [
        			{ test: () => !payload.periodId, id: 'dc-roster-period-switcher' },
        			{ test: () => period && String(period.status || '').toLowerCase() !== 'open', id: 'dc-roster-period-switcher' },
        			{ test: () => !payload.dutyDate, id: 'dc-assignment-date' },
        			{
        				test: () => period?.startDate && period?.endDate && payload.dutyDate
        					&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate)),
        				id: 'dc-assignment-date',
        			},
        			{ test: () => !payload.employeeId, id: 'dc-assignment-employee' },
        			{ test: () => !payload.locationId, id: 'dc-assignment-location' },
        			{ test: () => !payload.startTime, id: 'dc-assignment-start' },
        			{ test: () => !payload.endTime, id: 'dc-assignment-end' },
        			{ test: () => payload.startTime && payload.endTime && payload.startTime === payload.endTime, id: 'dc-assignment-start' },
        			{
        				test: () => {
        					const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        					return effective === null || effective <= 0;
        				},
        				id: 'dc-assignment-end',
        			},
        		];
        		for (const item of order) {
        			if (item.test()) {
        				document.getElementById(item.id)?.focus();
        				return;
        			}
        		}
        	}
        
        	function selectedPeriodForPayload(payload) {
        		const periodId = Number(payload?.periodId);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function clearAssignmentFormSuccess() {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		if (!el || el.dataset.dcSuccess !== '1') {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.dataset.dcSuccess = '';
        		el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        		setFormFeedbackA11y(el, false);
        	}
        
        	function showAssignmentFormSuccess(message, kind) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		const tone = kind === 'info' ? 'info' : 'success';
        		el.hidden = false;
        		el.textContent = String(message);
        		el.dataset.dcSuccess = '1';
        		el.classList.remove('dc-roster-form__feedback--error');
        		el.classList.toggle('dc-roster-form__feedback--success', tone === 'success');
        		el.classList.toggle('dc-roster-form__feedback--info', tone === 'info');
        		setFormFeedbackA11y(el, false);
        		if (submit) {
        			submit.disabled = submit.dataset.dcLockedBySetup === '1';
        		}
        		el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        	}
        
        	function labelsForAssignmentPayload(payload) {
        		const employee = state.employees.find((row) => Number(row.id) === Number(payload?.employeeId));
        		const location = state.locations.find((row) => Number(row.id) === Number(payload?.locationId));
        		const employeeSelect = document.getElementById('dc-assignment-employee');
        		const locationSelect = document.getElementById('dc-assignment-location');
        		const employeeName = employee
        			? String(employee.name || employee.displayName || '')
        			: (employeeSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		const locationName = location
        			? String(location.name || '')
        			: (locationSelect?.selectedOptions?.[0]?.textContent || '').trim();
        		return { employeeName, locationName };
        	}
        
        	function buildAssignmentSavedMessage(payload) {
        		const { employeeName, locationName } = labelsForAssignmentPayload(payload);
        		const dateLabel = D?.formatDisplayDate?.(payload.dutyDate) || payload.dutyDate;
        		const times = D?.formatClock24Range?.(payload.startTime, payload.endTime)
        			|| `${payload.startTime} – ${payload.endTime}`;
        		return t(
        			'dutycheck',
        			'Assignment saved — {employee} on {date}, {times}, at {location}. The list above is updated.',
        		)
        			.replace('{employee}', employeeName || '—')
        			.replace('{date}', String(dateLabel || ''))
        			.replace('{times}', String(times))
        			.replace('{location}', locationName || '—');
        	}
        
        	function updateAssignmentFormFeedback(payload) {
        		const el = document.getElementById('dc-assignment-form-feedback');
        		const submit = document.querySelector('#dc-assignment-form button[type="submit"]');
        		if (!el) {
        			return;
        		}
        		if (el.dataset.dcSuccess === '1') {
        			return;
        		}
        		if (typeof payload === 'string') {
        			el.hidden = false;
        			el.textContent = payload;
        			el.classList.remove('dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			el.classList.add('dc-roster-form__feedback--error');
        			setFormFeedbackA11y(el, true);
        			if (submit) {
        				submit.disabled = false;
        			}
        			return;
        		}
        		if (!payload) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			return;
        		}
        		let message = '';
        		let isError = false;
        		if (payload && payload.employeeId > 0 && payload.dutyDate && isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			message = t('dutycheck', 'This person is on approved leave on the selected date and cannot be scheduled.');
        			isError = true;
        		} else if (payload && payload.employeeId > 0 && payload.dutyDate && payload.startTime && payload.endTime) {
        			const overlap = findOverlapAssignment(payload);
        			if (overlap) {
        				const when = D?.formatDisplayDate?.(overlap.dutyDate) || overlap.dutyDate;
        				const times = D?.formatClock24Range?.(overlap.startTime, overlap.endTime)
        					|| `${overlap.startTime} – ${overlap.endTime}`;
        				message = t('dutycheck', 'These times overlap an existing shift on {date} ({times}). Change the times or choose another employee.')
        					.replace('{date}', String(when))
        					.replace('{times}', String(times));
        				isError = true;
        			}
        		}
        		if (!message) {
        			el.hidden = true;
        			el.textContent = '';
        			el.classList.remove('dc-roster-form__feedback--error', 'dc-roster-form__feedback--success', 'dc-roster-form__feedback--info');
        			setFormFeedbackA11y(el, false);
        			if (submit && submit.dataset.dcLockedBySetup !== '1') {
        				submit.disabled = false;
        			}
        			return;
        		}
        		el.hidden = false;
        		el.textContent = message;
        		el.classList.toggle('dc-roster-form__feedback--error', isError);
        		setFormFeedbackA11y(el, isError);
        		if (submit && isError) {
        			submit.disabled = true;
        		}
        	}
        
        	function refreshAssignmentFormEligibility() {
        		refreshEmployeeSelectForDate();
        		updateLocationTimezoneHint();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		updateAssignmentFormFeedback(readForm(form));
        	}
        
        	function updateLocationTimezoneHint() {
        		const hint = document.getElementById('dc-assignment-location-tz-hint');
        		const select = document.getElementById('dc-assignment-location');
        		if (!hint || !select) {
        			return;
        		}
        		const locationId = Number(select.value);
        		const location = state.locations.find((row) => Number(row.id) === locationId);
        		const tz = location && String(location.timezone || '').trim();
        		if (!tz) {
        			hint.hidden = true;
        			hint.replaceChildren();
        			return;
        		}
        		const accountTz = D?.currentTimezone?.() || '';
        		hint.textContent = t(
        			'dutycheck',
        			'Shift times use the location timezone ({locationTimezone}). Change it on the Locations page. Your account timezone ({accountTimezone}) is shown in the header.',
        		)
        			.replace('{locationTimezone}', tz)
        			.replace('{accountTimezone}', accountTz || 'UTC');
        		hint.hidden = false;
        	}
        
        	function fillSelect(id, items, options) {
        		const opts = Object.assign({ labelKey: 'name', emptyText: t('dutycheck', 'No options available') }, options || {});
        		const select = document.getElementById(id);
        		if (!select) return;
        		const previous = String(select.value || '');
        		select.replaceChildren();
        		if (!items.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = opts.emptyText;
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const item of items) {
        			const option = document.createElement('option');
        			option.value = String(item.id);
        			option.textContent = String(item[opts.labelKey] || item.name || item.id);
        			select.appendChild(option);
        		}
        		if (previous && Array.from(select.options).some((opt) => opt.value === previous)) {
        			select.value = previous;
        		}
        	}
        
        	function fillPeriodSwitcher(periods, selected) {
        		const select = document.getElementById('dc-roster-period-switcher');
        		if (!select) return;
        		const statusLabelMap = {
        			open: t('dutycheck', 'Open'),
        			published: t('dutycheck', 'Published'),
        			closed: t('dutycheck', 'Closed'),
        		};
        		select.replaceChildren();
        		if (!periods.length) {
        			const option = document.createElement('option');
        			option.value = '';
        			option.textContent = t('dutycheck', 'No periods yet');
        			select.appendChild(option);
        			select.disabled = true;
        			return;
        		}
        		select.disabled = false;
        		for (const period of periods) {
        			const option = document.createElement('option');
        			option.value = String(period.id);
        			const start = D?.formatDisplayDate(period.startDate) || period.startDate;
        			const end = D?.formatDisplayDate(period.endDate) || period.endDate;
        			const statusKey = String(period.status || '').toLowerCase();
        			const status = statusLabelMap[statusKey] || String(period.status || '').toUpperCase();
        			option.textContent = `${start} – ${end} · ${status}`;
        			select.appendChild(option);
        		}
        		if (selected) {
        			select.value = String(selected);
        		}
        	}
        
        	function severityBadge(severity) {
        		const sev = String(severity || 'info');
        		const label = ConflictLabels ? ConflictLabels.severityLabel(severity) : sev;
        		const badge = create('span', { class: 'dc-severity dc-severity--' + sev, text: label });
        		badge.setAttribute('aria-label', label);
        		return badge;
        	}
        
        	function renderConflicts(conflicts) {
        		const list = document.getElementById('dc-conflict-list');
        		const summary = document.getElementById('dc-conflict-summary');
        		if (!list) return;
        		list.setAttribute('role', 'list');
        		list.setAttribute('aria-label', t('dutycheck', 'Planning checks'));
        		list.replaceChildren();
        		const hard = conflicts.filter((c) => c?.severity === 'hard').length;
        		const soft = conflicts.filter((c) => c?.severity === 'soft').length;
        		const softUnack = conflicts.filter((c) => c?.severity === 'soft' && !c?.acknowledged).length;
        		if (summary) {
        			summary.textContent = !conflicts.length
        				? t('dutycheck', 'No planning issues found.')
        				: (ConflictLabels
        					? ConflictLabels.countsSummary(hard, soft, softUnack)
        					: String(hard));
        		}
        		if (!conflicts.length) {
        			return;
        		}
        		for (const conflict of conflicts) {
        			const li = create('li', { class: 'dc-conflict dc-conflict--' + (conflict?.severity || 'info') });
        			li.appendChild(severityBadge(conflict?.severity));
        			const conflictMessage = String(conflict?.message || '');
        			const titleText = conflictMessage
        				? translateConflictMessage(conflictMessage)
        				: t('dutycheck', 'Unknown conflict');
        			const titleId = `dc-conflict-title-${String(conflict?.id || conflicts.indexOf(conflict))}`;
        			const body = create('div', { class: 'dc-conflict__body' });
        			const title = create('span', { class: 'dc-conflict__title', text: titleText });
        			title.id = titleId;
        			body.appendChild(title);
        			li.setAttribute('aria-labelledby', titleId);
        			if (conflict?.acknowledged && conflict?.ackReason) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack',
        					text: t('dutycheck', 'Confirmed: {reason}').replace('{reason}', String(conflict.ackReason)),
        				}));
        			} else if (conflict?.ackInvalidated) {
        				body.appendChild(create('span', {
        					class: 'dc-conflict__ack-invalid',
        					text: t('dutycheck', 'The roster changed — please confirm again with a new reason.'),
        				}));
        			}
        			li.appendChild(body);
        
        			const actions = create('div', { class: 'dc-conflict__actions' });
        			if (conflict?.severity === 'soft' && conflict?.id && !conflict?.acknowledged) {
        				const ackBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Confirm'),
        				});
        				ackBtn.addEventListener('click', () => acknowledgeConflict(conflict.id));
        				actions.appendChild(ackBtn);
        			}
        			const recoveryUrl = String(conflict?.recoveryUrl || '').trim();
        			if (recoveryUrl && recoveryUrl !== '#' && !/^javascript:/i.test(recoveryUrl)) {
        				const recoveryLabel = String(conflict?.recoveryLabel || '').trim()
        					|| t('dutycheck', 'Open ArbeitszeitCheck');
        				const recoveryLink = create('a', {
        					class: 'button primary',
        					href: recoveryUrl,
        					text: recoveryLabel,
        					attrs: {
        						target: '_blank',
        						rel: 'noopener noreferrer',
        						'aria-label': recoveryLabel,
        					},
        				});
        				actions.appendChild(recoveryLink);
        			}
        			const assignmentIds = Array.isArray(conflict?.assignmentIds) ? conflict.assignmentIds : [];
        			if (conflict?.type === 'absence_collision' && assignmentIds.length) {
        				const focusBtn = create('button', {
        					type: 'button',
        					class: 'button',
        					text: t('dutycheck', 'Show assignment'),
        				});
        				focusBtn.addEventListener('click', () => {
        					revealAssignmentRow(Number(assignmentIds[0]));
        				});
        				actions.appendChild(focusBtn);
        			}
        			li.appendChild(actions);
        			list.appendChild(li);
        		}
        	}
        
        	async function acknowledgeConflict(conflictId) {
        		const reason = await C.promptReason({
        			title: t('dutycheck', 'Confirm this exception'),
        			label: t('dutycheck', 'Briefly explain why you are allowing this (minimum 10 characters)'),
        			confirmLabel: t('dutycheck', 'Confirm'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			minLength: 10,
        		});
        		if (reason === null) return;
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/conflicts/${conflictId}/acknowledge`, { reason });
        			render(response?.data || {});
        			const ackMsg = t('dutycheck', 'Exception confirmed. Planning checks are updated.');
        			Msg.announce(ackMsg, 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	const VIRTUAL_FALLBACK = {
        		DEFAULT_ROW_HEIGHT: 44,
        		DEFAULT_OVERSCAN: 6,
        		UNSIZED_WINDOW_ROWS: 24,
        		visibleRange(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			const rowHeight = 44;
        			return {
        				start: 0,
        				end: 0,
        				padBefore: 0,
        				padAfter: 0,
        				totalHeight: total * rowHeight,
        				rowHeight,
        			};
        		},
        		scrollTopToRevealIndex() {
        			return 0;
        		},
        		pageStride() {
        			return 1;
        		},
        		windowCaption(opts) {
        			const total = Math.max(0, Math.trunc(Number(opts && opts.total) || 0));
        			if (total <= 0) {
        				return { mode: 'empty', from: 0, to: 0, total: 0 };
        			}
        			return { mode: 'all', from: 1, to: total, total };
        		},
        	};
        
        	function virtualApi() {
        		const api = window.DutyCheckVirtualWindow;
        		if (api
        			&& typeof api.visibleRange === 'function'
        			&& typeof api.scrollTopToRevealIndex === 'function'
        			&& typeof api.windowCaption === 'function') {
        			return api;
        		}
        		return VIRTUAL_FALLBACK;
        	}
        
        	function cellSelectionKey(employeeId, dutyDate) {
        		return `${Number(employeeId)}|${String(dutyDate)}`;
        	}
        
        	function prefersReducedMotion() {
        		try {
        			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        		} catch (_) {
        			return false;
        		}
        	}
        
        	function measureHeight(el) {
        		if (!(el instanceof HTMLElement)) {
        			return 0;
        		}
        		const h = el.getBoundingClientRect().height;
        		return Number.isFinite(h) && h > 0 ? h : 0;
        	}
        
        	function assignmentIndex(assignments) {
        		const byKey = new Map();
        		for (const a of assignments || []) {
        			const key = cellSelectionKey(a.employeeId, a.dutyDate);
        			if (!byKey.has(key)) {
        				byKey.set(key, []);
        			}
        			byKey.get(key).push(a);
        		}
        		return byKey;
        	}
        
        	function setAssignmentsEmptyPanelVisible(showEmptyPanel) {
        		const emptyPanel = document.getElementById('dc-roster-assignments-empty');
        		if (emptyPanel) {
        			emptyPanel.hidden = !showEmptyPanel;
        		}
        	}
        
        	const gridState = {
        		view: 'grid',
        		selected: new Set(), // `${employeeId}|${dutyDate}`
        		focusRow: 0,
        		focusCol: 0,
        		paintAll: false,
        		rowHeight: 44,
        		headerHeight: 44,
        		windowStart: -1,
        		windowEnd: -1,
        		paintedEmployeeCount: -1,
        		paintedDateCount: -1,
        		remeasuring: false,
        	};
        
        	const listState = {
        		windowStart: -1,
        		windowEnd: -1,
        		rowHeight: 48,
        		paintedCount: -1,
        		remeasuring: false,
        	};
        
        	function renderAssignments(assignments) {
        		paintListWindow({ assignments: assignments || [], force: true });
        		renderRosterGrid(assignments || []);
        		updateAddAssignmentControl();
        	}
        
        	function buildAssignmentRow(a, periodOpen) {
        		const overnightHintText = t('dutycheck', 'Continues into the next day.');
        		const startClock = (s) => (D?.formatClock24FromTimeString(s) || String(s ?? ''));
        		const tr = create('tr');
        		if (a?.id != null) {
        			tr.dataset.assignmentId = String(a.id);
        			tr.setAttribute('tabindex', '-1');
        		}
        		const date = D?.formatDisplayDate(a.dutyDate) || a.dutyDate;
        		const tdDate = create('td', { text: date });
        		tdDate.dataset.cell = t('dutycheck', 'Date');
        		tr.appendChild(tdDate);
        
        		const tdStart = create('td');
        		tdStart.dataset.cell = t('dutycheck', 'Start');
        		tdStart.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.startTime) }));
        		tr.appendChild(tdStart);
        
        		const tdEnd = create('td');
        		tdEnd.dataset.cell = t('dutycheck', 'End');
        		tdEnd.appendChild(create('span', { class: 'dc-time-cell__value', text: startClock(a.endTime) }));
        		if (D?.isOvernightWallClockShift?.(a.startTime, a.endTime)) {
        			tdEnd.title = overnightHintText;
        			tdEnd.appendChild(create('span', { class: 'dc-row-meta', text: overnightHintText }));
        		}
        		tr.appendChild(tdEnd);
        
        		const rest = [
        			{ label: t('dutycheck', 'Employee'), value: a.employeeName || '' },
        			{ label: t('dutycheck', 'Location'), value: a.locationName || '' },
        			{ label: t('dutycheck', 'Break'), value: t('dutycheck', '{n} min').replace('{n}', String(a.breakMinutes ?? 0)) },
        			{ label: t('dutycheck', 'Note'), value: a.note || '' },
        		];
        		for (const cell of rest) {
        			const td = create('td', { text: String(cell.value ?? '') });
        			td.dataset.cell = cell.label;
        			tr.appendChild(td);
        		}
        		const tdActions = create('td');
        		tdActions.dataset.cell = t('dutycheck', 'Actions');
        		tdActions.className = 'dc-table__actions';
        		if (periodOpen && a?.id) {
        			const editBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Edit'),
        			});
        			editBtn.setAttribute('aria-label', t('dutycheck', 'Edit assignment'));
        			editBtn.addEventListener('click', () => openAssignmentEditModal(a, editBtn));
        			const cancelBtn = create('button', {
        				type: 'button',
        				class: 'button button--text',
        				text: t('dutycheck', 'Cancel shift'),
        			});
        			cancelBtn.setAttribute('aria-label', t('dutycheck', 'Cancel this assignment'));
        			cancelBtn.addEventListener('click', () => cancelAssignmentRow(a, cancelBtn));
        			tdActions.appendChild(editBtn);
        			tdActions.appendChild(cancelBtn);
        		} else {
        			tdActions.appendChild(create('span', { class: 'dc-row-meta', text: t('dutycheck', 'Read-only') }));
        		}
        		tr.appendChild(tdActions);
        		return tr;
        	}
        
        	function createListSpacer(px) {
        		const tr = create('tr', { class: 'dc-virtual-spacer' });
        		tr.setAttribute('aria-hidden', 'true');
        		tr.style.height = `${Math.max(0, px)}px`;
        		const td = create('td');
        		td.colSpan = 8;
        		td.style.height = `${Math.max(0, px)}px`;
        		td.style.padding = '0';
        		td.style.border = '0';
        		td.style.lineHeight = '0';
        		tr.appendChild(td);
        		return tr;
        	}
        
        	function updateListWindowStatus(range, total) {
        		const el = document.getElementById('dc-roster-list-status');
        		if (!el) {
        			return;
        		}
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		if (cap.mode === 'all') {
        			el.textContent = t('dutycheck', 'All {total} shifts are on screen.').replace('{total}', String(cap.total));
        			return;
        		}
        		el.textContent = t('dutycheck', 'Showing shifts {from}–{to} of {total}. Scroll to see everyone.')
        			.replace('{from}', String(cap.from))
        			.replace('{to}', String(cap.to))
        			.replace('{total}', String(cap.total));
        	}
        
        	function paintListWindow(options) {
        		const tbody = document.getElementById('dc-assignments-table-body');
        		if (!tbody) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		if (!assignments.length) {
        			tbody.replaceChildren();
        			setAssignmentsEmptyPanelVisible(true);
        			listState.windowStart = 0;
        			listState.windowEnd = 0;
        			listState.paintedCount = 0;
        			updateListWindowStatus({ start: 0, end: 0 }, 0);
        			return;
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const scroller = document.getElementById('dc-assignments-table-wrap');
        		const VW = virtualApi();
        		const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const viewportHeight = scroller && gridState.view === 'list' ? scroller.clientHeight : 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: assignments.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& listState.windowStart === range.start
        			&& listState.windowEnd === range.end
        			&& listState.paintedCount === assignments.length) {
        			updateListWindowStatus(range, assignments.length);
        			return;
        		}
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const periodOpen = canAddAssignment();
        		const frag = document.createDocumentFragment();
        		if (range.padBefore > 0) {
        			frag.appendChild(createListSpacer(range.padBefore));
        		}
        		const windowRows = assignments.slice(range.start, range.end);
        		for (const a of windowRows) {
        			frag.appendChild(buildAssignmentRow(a, periodOpen));
        		}
        		if (range.padAfter > 0) {
        			frag.appendChild(createListSpacer(range.padAfter));
        		}
        		tbody.replaceChildren(frag);
        		listState.windowStart = range.start;
        		listState.windowEnd = range.end;
        		listState.paintedCount = assignments.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        		}
        		const sample = tbody.querySelector('tr:not(.dc-virtual-spacer)');
        		const measured = measureHeight(sample);
        		if (measured > 0 && Math.abs(measured - rowHeight) > 1 && !listState.remeasuring) {
        			listState.rowHeight = measured;
        			listState.remeasuring = true;
        			paintListWindow({ assignments, force: true });
        			listState.remeasuring = false;
        			return;
        		}
        		if (measured > 0) {
        			listState.rowHeight = measured;
        		}
        		updateListWindowStatus(range, assignments.length);
        	}
        
        	function selectedPeriodId() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const fromSwitcher = Number(switcher?.value || 0);
        		if (Number.isInteger(fromSwitcher) && fromSwitcher > 0) {
        			return fromSwitcher;
        		}
        		return Number(state.lastRosterData?.selectedPeriodId || 0) || null;
        	}
        
        	function periodDateList() {
        		const periodId = selectedPeriodId();
        		const period = (state.periods || []).find((p) => Number(p.id) === Number(periodId));
        		if (!period?.startDate || !period?.endDate) {
        			return [];
        		}
        		const out = [];
        		const cursor = new Date(`${period.startDate}T00:00:00Z`);
        		const end = new Date(`${period.endDate}T00:00:00Z`);
        		while (cursor <= end) {
        			out.push(cursor.toISOString().slice(0, 10));
        			cursor.setUTCDate(cursor.getUTCDate() + 1);
        			if (out.length > 62) {
        				break;
        			}
        		}
        		return out;
        	}
        
        	function updateGridWindowStatus(range, total, dayCount) {
        		const el = document.getElementById('dc-roster-grid-status');
        		if (!el) {
        			return;
        		}
        		const days = Number.isFinite(dayCount) ? dayCount : periodDateList().length;
        		const cap = virtualApi().windowCaption({ start: range.start, end: range.end, total });
        		if (cap.mode === 'empty') {
        			el.textContent = '';
        			return;
        		}
        		let text = '';
        		if (cap.mode === 'all') {
        			text = t('dutycheck', 'All {total} people are on screen.').replace('{total}', String(cap.total));
        		} else {
        			text = t('dutycheck', 'Showing people {from}–{to} of {total}. Scroll to see everyone.')
        				.replace('{from}', String(cap.from))
        				.replace('{to}', String(cap.to))
        				.replace('{total}', String(cap.total));
        		}
        		if (days > 10) {
        			text += ' ' + t('dutycheck', 'Scroll sideways to see every day in this period.');
        		}
        		el.textContent = text;
        	}
        
        	function createGridSpacer(px) {
        		const el = create('div', { class: 'dc-virtual-spacer dc-roster-grid__spacer' });
        		el.setAttribute('aria-hidden', 'true');
        		el.setAttribute('role', 'presentation');
        		el.style.height = `${Math.max(0, px)}px`;
        		return el;
        	}
        
        	function setGridEmptyState(root, message) {
        		root.removeAttribute('aria-rowcount');
        		root.removeAttribute('aria-colcount');
        		root.removeAttribute('tabindex');
        		root.style.removeProperty('--dc-roster-day-count');
        		root.style.removeProperty('--dc-roster-day-min');
        		root.classList.remove('dc-roster-grid--long', 'dc-roster-grid--month');
        		root.setAttribute('role', 'status');
        		root.setAttribute('aria-live', 'polite');
        		root.replaceChildren(create('p', {
        			class: 'dc-roster-empty-state__text',
        			text: message,
        		}));
        	}
        
        	function prepareGridForRows(root, employees, dates) {
        		root.setAttribute('role', 'grid');
        		root.removeAttribute('tabindex');
        		root.removeAttribute('aria-live');
        		root.setAttribute('aria-rowcount', String(employees.length + 1));
        		root.setAttribute('aria-colcount', String(Math.max(1, dates.length + 1)));
        		root.style.setProperty('--dc-roster-day-count', String(Math.max(1, dates.length)));
        		const dayMin = D?.rosterDayColumnMin?.(dates.length) || (dates.length > 21 ? '3.25rem' : (dates.length > 10 ? '3.75rem' : '4.5rem'));
        		root.style.setProperty('--dc-roster-day-min', dayMin);
        		root.classList.toggle('dc-roster-grid--long', dates.length > 10);
        		root.classList.toggle('dc-roster-grid--month', dates.length >= 28);
        	}
        
        	function buildColHead(day, colIdx, dayMeta) {
        		const parts = dayMeta || D?.formatRosterColumnParts?.(day);
        		const full = parts?.full || D?.formatDisplayDate?.(day) || day;
        		let ariaLabel = parts?.ariaLabel || full;
        		if (parts?.isToday) {
        			ariaLabel = `${ariaLabel} — ${t('dutycheck', 'Today')}`;
        		}
        		const weekdayShort = parts?.weekdayShort || '';
        		const dayNum = parts?.day || '';
        		const classNames = ['dc-roster-grid__colhead'];
        		if (parts?.isWeekend) {
        			classNames.push('dc-roster-grid__colhead--weekend');
        		}
        		if (parts?.isToday) {
        			classNames.push('dc-roster-grid__colhead--today');
        		}
        		const children = [];
        		if (weekdayShort) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-wd', text: weekdayShort }));
        		}
        		if (dayNum) {
        			children.push(create('span', { class: 'dc-roster-grid__colhead-day', text: dayNum }));
        		}
        		const attrs = {
        			'data-col': String(colIdx),
        			'aria-colindex': String(colIdx + 2),
        			title: full,
        			'aria-label': ariaLabel,
        		};
        		if (parts?.iso) {
        			attrs['data-duty-date'] = parts.iso;
        		}
        		if (!children.length) {
        			return create('div', {
        				class: classNames.join(' '),
        				role: 'columnheader',
        				text: full,
        				attrs,
        			});
        		}
        		return create('div', {
        			class: classNames.join(' '),
        			role: 'columnheader',
        			attrs,
        		}, children);
        	}
        
        	function buildDutyDayMeta(dates) {
        		const map = new Map();
        		(dates || []).forEach((day) => {
        			map.set(day, D?.formatRosterColumnParts?.(day) || null);
        		});
        		return map;
        	}
        
        	function buildGridRow(emp, rowIdx, dates, byKey, dayMetaByDate) {
        		const row = create('div', {
        			class: 'dc-roster-grid__row',
        			role: 'row',
        			attrs: { 'aria-rowindex': String(rowIdx + 2) },
        		});
        		const prefs = state.preferencesByEmployee[String(emp.id)] || [];
        		const prefChips = prefs.slice(0, 2).map((p) => {
        			const band = String(p.band || '');
        			const label = band === 'early'
        				? t('dutycheck', 'Früh')
        				: (band === 'late' ? t('dutycheck', 'Spät') : band);
        			return create('span', {
        				class: 'dc-pref-chip dc-pref-chip--static',
        				text: label,
        				attrs: { title: label },
        			});
        		});
        		const headKids = [
        			create('span', { class: 'dc-roster-grid__rowhead-name', text: String(emp.name || emp.displayName || emp.id) }),
        		];
        		if (prefChips.length) {
        			headKids.push(create('span', { class: 'dc-roster-grid__pref-chips', attrs: { 'aria-label': t('dutycheck', 'Wishes') } }, prefChips));
        		}
        		row.appendChild(create('div', {
        			class: 'dc-roster-grid__rowhead',
        			role: 'rowheader',
        			attrs: { 'aria-colindex': '1' },
        		}, headKids));
        		dates.forEach((day, colIdx) => {
        			const key = cellSelectionKey(emp.id, day);
        			const cellAssignments = byKey.get(key) || [];
        			const dayParts = dayMetaByDate?.get?.(day) || D?.formatRosterColumnParts?.(day);
        			const cell = create('div', {
        				class: 'dc-roster-grid__cell',
        				role: 'gridcell',
        				attrs: {
        					tabindex: rowIdx === gridState.focusRow && colIdx === gridState.focusCol ? '0' : '-1',
        					'data-employee-id': String(emp.id),
        					'data-duty-date': day,
        					'data-row': String(rowIdx),
        					'data-col': String(colIdx),
        					'aria-colindex': String(colIdx + 2),
        					'aria-selected': gridState.selected.has(key) ? 'true' : 'false',
        				},
        			});
        			if (dayParts?.isWeekend) {
        				cell.classList.add('dc-roster-grid__cell--weekend');
        			}
        			if (dayParts?.isToday) {
        				cell.classList.add('dc-roster-grid__cell--today');
        			}
        			if (cellAssignments.length) {
        				cell.classList.add('dc-roster-grid__cell--filled');
        				const first = cellAssignments[0];
        				const label = `${String(first.startTime || '').slice(0, 5)}–${String(first.endTime || '').slice(0, 5)}`;
        				cell.appendChild(create('span', {
        					class: 'dc-roster-grid__shift',
        					text: label,
        				}));
        				const editLabel = canAddAssignment()
        					? t('dutycheck', 'Edit assignment')
        					: t('dutycheck', 'View assignment (read-only)');
        				cell.setAttribute('aria-label', `${editLabel}: ${label}`);
        				if (cellAssignments.length > 1) {
        					cell.appendChild(create('span', {
        						class: 'dc-roster-grid__more',
        						text: `+${cellAssignments.length - 1}`,
        					}));
        				}
        			} else {
        				cell.classList.add('dc-roster-grid__cell--empty');
        				const blackoutKey = `${emp.id}|${day}`;
        				const hasBlackout = !!state.blackoutsByEmployeeDate[blackoutKey];
        				if (hasBlackout) {
        					cell.classList.add('dc-roster-grid__cell--blackout');
        					cell.appendChild(create('span', {
        						class: 'dc-blackout-mark',
        						attrs: { title: t('dutycheck', 'Kann nicht') },
        					}, [
        						create('span', { class: 'dc-blackout-mark__icon', attrs: { 'aria-hidden': 'true' }, text: '⊘' }),
        						create('span', { class: 'dc-blackout-mark__text', text: t('dutycheck', 'Kann nicht') }),
        					]));
        					cell.setAttribute('aria-label', t('dutycheck', 'Cannot work (blackout)'));
        				} else {
        					cell.setAttribute(
        						'aria-label',
        						canAddAssignment()
        							? t('dutycheck', 'Empty cell — Space to select for bulk fill')
        							: t('dutycheck', 'Empty cell'),
        					);
        				}
        			}
        			if (gridState.selected.has(key)) {
        				cell.classList.add('is-selected');
        			}
        			row.appendChild(cell);
        		});
        		return row;
        	}
        
        	function syncVisibleGridChrome() {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		root.querySelectorAll('[role="gridcell"]').forEach((cell) => {
        			const empId = cell.getAttribute('data-employee-id');
        			const day = cell.getAttribute('data-duty-date');
        			const key = cellSelectionKey(empId, day);
        			const selected = gridState.selected.has(key);
        			cell.classList.toggle('is-selected', selected);
        			cell.setAttribute('aria-selected', selected ? 'true' : 'false');
        			const rowIdx = Number(cell.getAttribute('data-row'));
        			const colIdx = Number(cell.getAttribute('data-col'));
        			const isFocus = rowIdx === gridState.focusRow && colIdx === gridState.focusCol;
        			cell.setAttribute('tabindex', isFocus ? '0' : '-1');
        		});
        	}
        
        	function focusGridCellIfVisible(root) {
        		const node = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        		if (node instanceof HTMLElement) {
        			node.scrollIntoView({
        				block: 'nearest',
        				inline: 'nearest',
        				behavior: 'auto',
        			});
        			node.focus();
        		}
        	}
        
        	function activateGridCell(cell) {
        		if (!(cell instanceof HTMLElement)) {
        			return;
        		}
        		const rowIdx = Number(cell.getAttribute('data-row'));
        		const colIdx = Number(cell.getAttribute('data-col'));
        		if (Number.isInteger(rowIdx) && rowIdx >= 0) {
        			gridState.focusRow = rowIdx;
        		}
        		if (Number.isInteger(colIdx) && colIdx >= 0) {
        			gridState.focusCol = colIdx;
        		}
        		const empId = cell.getAttribute('data-employee-id');
        		const day = cell.getAttribute('data-duty-date');
        		const key = cellSelectionKey(empId, day);
        		const filled = cell.classList.contains('dc-roster-grid__cell--filled');
        		if (!filled) {
        			if (canAddAssignment()) {
        				toggleGridSelection(key);
        				syncVisibleGridChrome();
        				updateBulkBar();
        			}
        			return;
        		}
        		const byKey = assignmentIndex(state.assignments || []);
        		const cellAssignments = byKey.get(key) || [];
        		if (canAddAssignment() && cellAssignments[0]) {
        			openAssignmentEditModal(cellAssignments[0], cell);
        			return;
        		}
        		Msg.announce(t('dutycheck', 'This period is read-only. Re-open it from Periods to edit shifts.'), 'info');
        	}
        
        	function ensureGridFocusVisible() {
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const employees = state.employees || [];
        		if (!scroller || !employees.length) {
        			return;
        		}
        		const VW = virtualApi();
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = Math.max(rowHeight, scroller.clientHeight - headerHeight);
        		const next = VW.scrollTopToRevealIndex({
        			index: gridState.focusRow,
        			rowHeight,
        			viewportHeight,
        			scrollTop: scroller.scrollTop,
        			total: employees.length,
        		});
        		if (Math.abs(next - scroller.scrollTop) > 0.5) {
        			scroller.scrollTop = next;
        		}
        	}
        
        	function renderRosterGrid(assignments) {
        		paintGridWindow({ assignments: assignments || state.assignments || [], force: true });
        	}
        
        	function paintGridWindow(options) {
        		const root = document.getElementById('dc-roster-grid');
        		if (!root) {
        			return;
        		}
        		const assignments = options && Array.isArray(options.assignments)
        			? options.assignments
        			: (state.assignments || []);
        		const force = !!(options && options.force);
        		const employees = state.employees || [];
        		const dates = periodDateList();
        		const byKey = assignmentIndex(assignments);
        
        		if (!employees.length || !dates.length) {
        			setGridEmptyState(
        				root,
        				t('dutycheck', 'Add employees and an open period to use the planning grid.'),
        			);
        			gridState.windowStart = 0;
        			gridState.windowEnd = 0;
        			gridState.paintedEmployeeCount = employees.length;
        			gridState.paintedDateCount = dates.length;
        			updateGridWindowStatus({ start: 0, end: 0 }, 0, 0);
        			updateBulkBar();
        			return;
        		}
        
        		prepareGridForRows(root, employees, dates);
        		gridState.focusRow = Math.max(0, Math.min(employees.length - 1, gridState.focusRow));
        		gridState.focusCol = Math.max(0, Math.min(dates.length - 1, gridState.focusCol));
        
        		const VW = virtualApi();
        		const scroller = document.getElementById('dc-roster-grid-scroller');
        		const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        		const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        		const viewportHeight = scroller && gridState.view === 'grid'
        			? Math.max(0, scroller.clientHeight - headerHeight)
        			: 0;
        		const scrollTop = scroller ? scroller.scrollTop : 0;
        		const range = VW.visibleRange({
        			total: employees.length,
        			rowHeight,
        			viewportHeight,
        			scrollTop,
        			overscan: VW.DEFAULT_OVERSCAN,
        			paintAll: gridState.paintAll === true,
        		});
        		if (!force
        			&& gridState.windowStart === range.start
        			&& gridState.windowEnd === range.end
        			&& gridState.paintedEmployeeCount === employees.length
        			&& gridState.paintedDateCount === dates.length) {
        			syncVisibleGridChrome();
        			updateGridWindowStatus(range, employees.length, dates.length);
        			updateBulkBar();
        			return;
        		}
        
        		const savedTop = scroller ? scroller.scrollTop : 0;
        		const savedLeft = scroller ? scroller.scrollLeft : 0;
        		const dayMetaByDate = buildDutyDayMeta(dates);
        		const frag = document.createDocumentFragment();
        		const header = create('div', {
        			class: 'dc-roster-grid__row dc-roster-grid__row--head',
        			role: 'row',
        			attrs: { 'aria-rowindex': '1' },
        		});
        		header.appendChild(create('div', {
        			class: 'dc-roster-grid__corner',
        			role: 'columnheader',
        			text: t('dutycheck', 'Person'),
        			attrs: { 'aria-colindex': '1' },
        		}));
        		dates.forEach((day, colIdx) => {
        			header.appendChild(buildColHead(day, colIdx, dayMetaByDate.get(day)));
        		});
        		frag.appendChild(header);
        		if (range.padBefore > 0) {
        			frag.appendChild(createGridSpacer(range.padBefore));
        		}
        		const windowEmployees = employees.slice(range.start, range.end);
        		windowEmployees.forEach((emp, offset) => {
        			frag.appendChild(buildGridRow(emp, range.start + offset, dates, byKey, dayMetaByDate));
        		});
        		if (range.padAfter > 0) {
        			frag.appendChild(createGridSpacer(range.padAfter));
        		}
        		root.replaceChildren(frag);
        		gridState.windowStart = range.start;
        		gridState.windowEnd = range.end;
        		gridState.paintedEmployeeCount = employees.length;
        		gridState.paintedDateCount = dates.length;
        		if (scroller) {
        			scroller.scrollTop = savedTop;
        			scroller.scrollLeft = savedLeft;
        		}
        		const headerEl = root.querySelector('.dc-roster-grid__row--head');
        		const bodyRow = root.querySelector('.dc-roster-grid__row:not(.dc-roster-grid__row--head)');
        		const measuredHeader = measureHeight(headerEl);
        		const measuredRow = measureHeight(bodyRow);
        		if (((measuredHeader > 0 && Math.abs(measuredHeader - headerHeight) > 1)
        			|| (measuredRow > 0 && Math.abs(measuredRow - rowHeight) > 1))
        			&& !gridState.remeasuring) {
        			if (measuredHeader > 0) {
        				gridState.headerHeight = measuredHeader;
        			}
        			if (measuredRow > 0) {
        				gridState.rowHeight = measuredRow;
        			}
        			gridState.remeasuring = true;
        			paintGridWindow({ assignments, force: true });
        			gridState.remeasuring = false;
        			return;
        		}
        		if (measuredHeader > 0) {
        			gridState.headerHeight = measuredHeader;
        		}
        		if (measuredRow > 0) {
        			gridState.rowHeight = measuredRow;
        		}
        		bindGridInteractions(root);
        		updateGridWindowStatus(range, employees.length, dates.length);
        		updateBulkBar();
        	}
        
        	function bindGridInteractions(root) {
        		if (!(root instanceof HTMLElement) || root.dataset.dcGridBound === '1') {
        			return;
        		}
        		root.dataset.dcGridBound = '1';
        		root.addEventListener('click', (ev) => {
        			const cell = ev.target.closest('[role="gridcell"]');
        			if (!(cell instanceof HTMLElement) || !root.contains(cell)) {
        				return;
        			}
        			activateGridCell(cell);
        		});
        		root.addEventListener('keydown', (ev) => {
        			const employees = state.employees || [];
        			const dates = periodDateList();
        			const rowCount = employees.length;
        			const colCount = dates.length;
        			if (!rowCount || !colCount) {
        				return;
        			}
        			const gridKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Enter', ' ', 'Spacebar', 'Home', 'End', 'PageUp', 'PageDown'];
        			if (!gridKeys.includes(ev.key)) {
        				return;
        			}
        			ev.preventDefault();
        			if (ev.key === 'ArrowUp') {
        				gridState.focusRow = Math.max(0, gridState.focusRow - 1);
        			} else if (ev.key === 'ArrowDown') {
        				gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + 1);
        			} else if (ev.key === 'ArrowLeft') {
        				gridState.focusCol = Math.max(0, gridState.focusCol - 1);
        			} else if (ev.key === 'ArrowRight') {
        				gridState.focusCol = Math.min(colCount - 1, gridState.focusCol + 1);
        			} else if (ev.key === 'Home') {
        				gridState.focusCol = 0;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = 0;
        				}
        			} else if (ev.key === 'End') {
        				gridState.focusCol = colCount - 1;
        				if (ev.ctrlKey || ev.metaKey) {
        					gridState.focusRow = rowCount - 1;
        				}
        			} else if (ev.key === 'PageUp' || ev.key === 'PageDown') {
        				const VW = virtualApi();
        				const scroller = document.getElementById('dc-roster-grid-scroller');
        				const rowHeight = gridState.rowHeight > 0 ? gridState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        				const headerHeight = gridState.headerHeight > 0 ? gridState.headerHeight : rowHeight;
        				const viewportHeight = scroller
        					? Math.max(rowHeight, scroller.clientHeight - headerHeight)
        					: rowHeight;
        				const stride = typeof VW.pageStride === 'function'
        					? VW.pageStride({ rowHeight, viewportHeight })
        					: 1;
        				if (ev.key === 'PageDown') {
        					gridState.focusRow = Math.min(rowCount - 1, gridState.focusRow + stride);
        				} else {
        					gridState.focusRow = Math.max(0, gridState.focusRow - stride);
        				}
        			} else if (ev.key === 'Enter') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				activateGridCell(cell);
        				focusGridCellIfVisible(root);
        				return;
        			} else if (ev.key === ' ' || ev.key === 'Spacebar') {
        				ensureGridFocusVisible();
        				paintGridWindow({ force: false });
        				const cell = root.querySelector(`[data-row="${gridState.focusRow}"][data-col="${gridState.focusCol}"]`);
        				const empId = cell?.getAttribute('data-employee-id');
        				const day = cell?.getAttribute('data-duty-date');
        				const isEmpty = cell?.classList.contains('dc-roster-grid__cell--empty');
        				if (empId && day && isEmpty && canAddAssignment()) {
        					toggleGridSelection(cellSelectionKey(empId, day));
        					syncVisibleGridChrome();
        					updateBulkBar();
        				}
        				focusGridCellIfVisible(root);
        				return;
        			}
        			ensureGridFocusVisible();
        			paintGridWindow({ force: false });
        			focusGridCellIfVisible(root);
        		});
        	}
        
        	function toggleGridSelection(key) {
        		if (gridState.selected.has(key)) {
        			gridState.selected.delete(key);
        		} else {
        			gridState.selected.add(key);
        		}
        	}
        
        	function updateBulkBar() {
        		const bar = document.getElementById('dc-roster-bulk-bar');
        		const countEl = document.getElementById('dc-roster-bulk-count');
        		if (!bar || !countEl) {
        			return;
        		}
        		const n = gridState.selected.size;
        		if (n < 1 || !canAddAssignment()) {
        			bar.hidden = true;
        			return;
        		}
        		bar.hidden = false;
        		countEl.textContent = t('dutycheck', '{n} cells selected').replace('{n}', String(n));
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (sel && !(sel.options && sel.options.length)) {
        			populateBulkTemplates();
        		}
        	}
        
        	async function populateBulkTemplates() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		if (!sel) {
        			return;
        		}
        		await loadTemplates();
        		sel.replaceChildren();
        		const templates = state.templates || [];
        		if (!templates.length) {
        			sel.appendChild(create('option', { value: '', text: t('dutycheck', 'No templates yet') }));
        			return;
        		}
        		for (const tpl of templates) {
        			sel.appendChild(create('option', {
        				value: String(tpl.id),
        				text: `${tpl.name} (${tpl.startTime}–${tpl.endTime})`,
        			}));
        		}
        	}
        
        	function revealAssignmentRow(id) {
        		const assignmentId = Number(id);
        		const assignments = state.assignments || [];
        		const idx = assignments.findIndex((a) => Number(a.id) === assignmentId);
        		if (idx < 0) {
        			return;
        		}
        		applyRosterViewChrome('list');
        		const run = () => {
        			const scroller = document.getElementById('dc-assignments-table-wrap');
        			const VW = virtualApi();
        			const rowHeight = listState.rowHeight > 0 ? listState.rowHeight : VW.DEFAULT_ROW_HEIGHT;
        			if (scroller) {
        				scroller.scrollTop = VW.scrollTopToRevealIndex({
        					index: idx,
        					rowHeight,
        					viewportHeight: Math.max(rowHeight, scroller.clientHeight),
        					scrollTop: scroller.scrollTop,
        					total: assignments.length,
        				});
        			}
        			paintListWindow({ assignments, force: true });
        			const row = document.querySelector(`[data-assignment-id="${assignmentId}"]`);
        			if (row instanceof HTMLElement) {
        				row.scrollIntoView({
        					block: 'nearest',
        					behavior: 'auto',
        				});
        				row.focus();
        				row.classList.add('dc-row--flash');
        				window.setTimeout(() => row.classList.remove('dc-row--flash'), 1600);
        			}
        		};
        		window.requestAnimationFrame(run);
        	}
        
        	function setRosterPaintAll(paintAll) {
        		gridState.paintAll = paintAll === true;
        		paintListWindow({ force: true });
        		paintGridWindow({ force: true });
        	}
        
        	function wireRosterVirtualization() {
        		const gridScroller = document.getElementById('dc-roster-grid-scroller');
        		const listScroller = document.getElementById('dc-assignments-table-wrap');
        		let gridRaf = 0;
        		let listRaf = 0;
        		if (gridScroller) {
        			gridScroller.addEventListener('scroll', () => {
        				if (gridRaf) {
        					return;
        				}
        				gridRaf = window.requestAnimationFrame(() => {
        					gridRaf = 0;
        					paintGridWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		if (listScroller) {
        			listScroller.addEventListener('scroll', () => {
        				if (listRaf) {
        					return;
        				}
        				listRaf = window.requestAnimationFrame(() => {
        					listRaf = 0;
        					paintListWindow({ force: false });
        				});
        			}, { passive: true });
        		}
        		const resize = () => {
        			if (gridState.view === 'grid') {
        				paintGridWindow({ force: true });
        			} else {
        				paintListWindow({ force: true });
        			}
        		};
        		if (typeof ResizeObserver === 'function') {
        			const ro = new ResizeObserver(resize);
        			if (gridScroller) {
        				ro.observe(gridScroller);
        			}
        			if (listScroller) {
        				ro.observe(listScroller);
        			}
        		} else {
        			window.addEventListener('resize', resize);
        		}
        		window.addEventListener('beforeprint', () => setRosterPaintAll(true));
        		window.addEventListener('afterprint', () => setRosterPaintAll(false));
        		if (typeof window.matchMedia === 'function') {
        			try {
        				const printMq = window.matchMedia('print');
        				const onPrintMq = (ev) => setRosterPaintAll(!!ev.matches);
        				if (typeof printMq.addEventListener === 'function') {
        					printMq.addEventListener('change', onPrintMq);
        				} else if (typeof printMq.addListener === 'function') {
        					printMq.addListener(onPrintMq);
        				}
        			} catch (_) {
        				/* print media query is not available in every test/jsdom shim */
        			}
        		}
        	}
        
        	function applyRosterViewChrome(view) {
        		gridState.view = view === 'list' ? 'list' : 'grid';
        		const gridWrap = document.getElementById('dc-roster-grid-wrap');
        		const listPanel = document.getElementById('dc-roster-list-panel');
        		const gridBtn = document.getElementById('dc-roster-view-grid');
        		const listBtn = document.getElementById('dc-roster-view-list');
        		if (gridWrap) {
        			gridWrap.hidden = gridState.view !== 'grid';
        		}
        		if (listPanel) {
        			listPanel.hidden = gridState.view !== 'list';
        		}
        		if (gridBtn) {
        			gridBtn.setAttribute('aria-pressed', gridState.view === 'grid' ? 'true' : 'false');
        		}
        		if (listBtn) {
        			listBtn.setAttribute('aria-pressed', gridState.view === 'list' ? 'true' : 'false');
        		}
        	}
        
        	function setRosterView(view) {
        		applyRosterViewChrome(view);
        		window.requestAnimationFrame(() => {
        			if (gridState.view === 'list') {
        				paintListWindow({ force: true });
        			} else {
        				paintGridWindow({ force: true });
        			}
        		});
        	}
        
        	async function applyBulkFromTemplate() {
        		const sel = document.getElementById('dc-roster-bulk-template');
        		const tplId = Number(sel?.value || 0);
        		const tpl = (state.templates || []).find((t) => Number(t.id) === tplId);
        		if (!tpl || gridState.selected.size < 1) {
        			Msg.announce(t('dutycheck', 'Pick a template and select empty cells first.'), 'warning');
        			return;
        		}
        		if (!Number(tpl.locationId || 0)) {
        			Msg.announce(t('dutycheck', 'This template has no location. Edit the template in Settings, then try again.'), 'warning');
        			return;
        		}
        		const periodId = Number(selectedPeriodId() || 0);
        		let ok = 0;
        		let fail = 0;
        		for (const key of Array.from(gridState.selected)) {
        			const sep = key.indexOf('|');
        			if (sep < 1) {
        				fail += 1;
        				continue;
        			}
        			const employeeId = Number(key.slice(0, sep));
        			const dutyDate = key.slice(sep + 1);
        			if (!Number.isInteger(employeeId) || employeeId < 1 || !dutyDate) {
        				fail += 1;
        				continue;
        			}
        			const locationId = Number(tpl.locationId || 0);
        			if (!locationId) {
        				fail += 1;
        				continue;
        			}
        			try {
        				await submitAssignment({
        					periodId,
        					employeeId: Number(employeeId),
        					locationId,
        					dutyDate,
        					startTime: String(tpl.startTime).slice(0, 5),
        					endTime: String(tpl.endTime).slice(0, 5),
        					breakMinutes: Number(tpl.breakMinutes || 0),
        					note: '',
        					assignmentId: 0,
        				}, true);
        				ok += 1;
        			} catch {
        				fail += 1;
        			}
        		}
        		gridState.selected.clear();
        		await loadRoster(periodId || null);
        		Msg.announce(
        			t('dutycheck', 'Bulk fill finished: {ok} saved, {fail} skipped.')
        				.replace('{ok}', String(ok))
        				.replace('{fail}', String(fail)),
        			fail ? 'warning' : 'info',
        		);
        	}
        
        	function uniqueConflictTypes(conflicts) {
        		const types = new Set();
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity === 'soft' && conflict?.type) {
        				types.add(String(conflict.type));
        			}
        		}
        		return Array.from(types);
        	}
        
        	function softConflictSummary(conflicts) {
        		const lines = [];
        		for (const conflict of conflicts || []) {
        			if (conflict?.severity !== 'soft') {
        				continue;
        			}
        			const raw = String(conflict?.message || '');
        			const text = raw ? translateConflictMessage(raw) : t('dutycheck', 'Unknown conflict');
        			if (text && !lines.includes(text)) {
        				lines.push(text);
        			}
        		}
        		return lines.join('\n');
        	}
        
        	function errorCodeFrom(err) {
        		return String(err?.code || err?.payload?.error?.code || '');
        	}
        
        	function syncAssignmentPeriodFromSwitcher() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (!switcher || !periodHidden) {
        			return;
        		}
        		const selected = Number(switcher.value);
        		if (Number.isInteger(selected) && selected > 0) {
        			periodHidden.value = String(selected);
        		}
        	}
        
        	function announceAssignmentSaveError(err) {
        		clearAssignmentFormSuccess();
        		const friendly = errorMessageFor(err);
        		const message = friendly || t('dutycheck', 'Something went wrong. Please try again, and contact an administrator if it keeps happening.');
        		updateAssignmentFormFeedback(message);
        		Msg.announce(message, 'error');
        	}
        
        	async function submitAssignment(payload, retryWithAck = true) {
        		const editId = Number(payload.assignmentId || state.editingAssignmentId || 0);
        		try {
        			if (editId > 0) {
        				return await Api.put(`/apps/dutycheck/api/assignments/${editId}`, payload);
        			}
        			return await Api.post('/apps/dutycheck/api/assignments', payload);
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are scheduling anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Save with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acknowledgements = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return submitAssignment({ ...payload, acknowledgements }, false);
        			}
        			if (retryWithAck && code === 'BLACKOUT_CONFLICT' && !String(payload.blackoutOverrideReason || payload.blackout_override_reason || '').trim()) {
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Employee cannot work (blackout)'),
        					label: t(
        						'dutycheck',
        						'This employee marked that they cannot work during this time. To schedule anyway, type a short planning reason (at least 10 characters). Do not enter medical details.',
        					),
        					confirmLabel: t('dutycheck', 'Schedule anyway'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        					maxLength: 200,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				return submitAssignment({ ...payload, blackoutOverrideReason: reason.slice(0, 200) }, true);
        			}
        			throw error;
        		}
        	}
        
        	function setAssignmentFormBusy(form, busy) {
        		if (!form) return;
        		const submit = form.querySelector('button[type="submit"]');
        		const clear = document.getElementById('dc-assignment-form-clear');
        		if (submit) {
        			submit.disabled = busy || submit.dataset.dcLockedBySetup === '1';
        			if (busy) {
        				submit.setAttribute('aria-busy', 'true');
        			} else {
        				submit.removeAttribute('aria-busy');
        			}
        		}
        		if (clear) {
        			clear.disabled = busy;
        		}
        	}
        
        	function renderSetupCallout(data) {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		const list = document.getElementById('dc-roster-setup-checklist');
        		const form = document.getElementById('dc-assignment-form');
        		if (!callout || !list || !form) return;
        		const urls = readUrls();
        		const missing = [];
        		if (!state.periods.length) {
        			missing.push({
        				text: t('dutycheck', 'Create a planning period.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		if (!state.employees.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active employee.'),
        				url: urls.employees,
        				cta: t('dutycheck', 'Go to Employees'),
        			});
        		}
        		if (!state.locations.length) {
        			missing.push({
        				text: t('dutycheck', 'Add at least one active location.'),
        				url: urls.locations,
        				cta: t('dutycheck', 'Go to Locations'),
        			});
        		}
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data?.selectedPeriodId));
        		const periodIsOpen = (selectedPeriod?.status || '') === 'open';
        		if (state.periods.length && data?.selectedPeriodId && !periodIsOpen) {
        			missing.push({
        				text: t('dutycheck', 'The selected period is read-only. Pick an open period or re-open this one.'),
        				url: urls.periods,
        				cta: t('dutycheck', 'Go to Periods'),
        			});
        		}
        		list.replaceChildren();
        		if (!missing.length) {
        			callout.hidden = true;
        			form.querySelectorAll('input, select, button[type="submit"]').forEach((el) => {
        				if (el.dataset.dcLockedBySetup === '1') {
        					el.disabled = false;
        					delete el.dataset.dcLockedBySetup;
        				}
        			});
        			updateAddAssignmentControl();
        			return;
        		}
        		for (const item of missing) {
        			const li = create('li');
        			li.appendChild(document.createTextNode(item.text + ' '));
        			if (item.url) {
        				li.appendChild(create('a', {
        					class: 'button',
        					href: item.url,
        					text: item.cta,
        				}));
        			}
        			list.appendChild(li);
        		}
        		callout.hidden = false;
        		form.querySelectorAll('input:not([type="hidden"]), select, button[type="submit"]').forEach((el) => {
        			if (!el.disabled) {
        				el.dataset.dcLockedBySetup = '1';
        				el.disabled = true;
        			}
        		});
        		updateAddAssignmentControl();
        	}
        
        	function readUrls() {
        		try {
        			const raw = document.getElementById('app-content')?.dataset?.dcUrls;
        			if (!raw) return {};
        			return JSON.parse(raw) || {};
        		} catch (_) {
        			return {};
        		}
        	}
        
        	function restoreAssignmentFormPanel() {
        		const host = document.getElementById('dc-assignment-form-host');
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (host && panel && panel.parentElement !== host) {
        			host.appendChild(panel);
        		}
        	}
        
        	function selectedPeriodFromState() {
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		const raw = switcher?.value || document.getElementById('dc-assignment-period')?.value;
        		const periodId = Number(raw);
        		if (!Number.isInteger(periodId) || periodId <= 0) {
        			return null;
        		}
        		return state.periods.find((p) => Number(p.id) === periodId) || null;
        	}
        
        	function isAssignmentSetupBlocking() {
        		const callout = document.getElementById('dc-roster-setup-callout');
        		return !!(callout && !callout.hidden);
        	}
        
        	function canAddAssignment() {
        		if (!state.canCreateAssignments) {
        			return false;
        		}
        		if (!state.periods.length || !state.employees.length || !state.locations.length) {
        			return false;
        		}
        		const period = selectedPeriodFromState();
        		return !!(period && String(period.status || '').toLowerCase() === 'open');
        	}
        
        	function addAssignmentDisabledReason() {
        		if (!state.periods.length) {
        			return t('dutycheck', 'Create a planning period first.');
        		}
        		if (!state.employees.length) {
        			return t('dutycheck', 'Add at least one active employee before planning shifts.');
        		}
        		if (!state.locations.length) {
        			return t('dutycheck', 'Add at least one active location before planning shifts.');
        		}
        		const period = selectedPeriodFromState();
        		if (!period) {
        			return t('dutycheck', 'Select a period before adding assignments.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!state.canCreateAssignments) {
        			return t('dutycheck', 'You cannot add assignments for this period.');
        		}
        		return t('dutycheck', 'Adding assignments is not available right now.');
        	}
        
        	function updateAddAssignmentControl() {
        		const allowed = canAddAssignment();
        		const reason = addAssignmentDisabledReason();
        		document.querySelectorAll('.dc-roster-add-assignment-trigger').forEach((btn) => {
        			// Prefer omit over disable (CORE §7.1) — hide when the period cannot accept writes.
        			btn.hidden = !allowed;
        			btn.disabled = !allowed;
        			btn.setAttribute('aria-disabled', allowed ? 'false' : 'true');
        			btn.classList.toggle('dc-is-disabled', !allowed);
        			if (allowed) {
        				btn.removeAttribute('title');
        			} else {
        				btn.setAttribute('title', reason);
        			}
        		});
        		const emptyHint = document.getElementById('dc-roster-empty-add-hint');
        		if (emptyHint) {
        			emptyHint.textContent = allowed
        				? t('dutycheck', 'Use “Add assignment” above to plan the first shift.')
        				: reason;
        		}
        	}
        
        	function clearAssignmentsSectionSuccess() {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.hidden = true;
        		el.textContent = '';
        		el.classList.remove('dc-roster-flash--visible');
        	}
        
        	function showAssignmentsSectionSuccess(message) {
        		const el = document.getElementById('dc-roster-assignments-success');
        		if (!el) {
        			return;
        		}
        		el.textContent = String(message);
        		el.hidden = false;
        		el.classList.add('dc-roster-flash--visible');
        	}
        
        	function prepareAssignmentFormForModal() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		clearAssignmentFormSuccess();
        		updateAssignmentFormFeedback(null);
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate && period?.endDate) {
        			dateInput.min = String(period.startDate);
        			dateInput.max = String(period.endDate);
        			if (!dateInput.value) {
        				dateInput.value = String(period.startDate);
        			}
        		}
        		refreshAssignmentFormEligibility();
        		applyDefaultBreakToForm();
        		wireBreakPrefillHint();
        		fillTemplateSelect();
        		D?.applyLocaleToTemporalInputs(form);
        	}
        
        	function resetAssignmentFormAfterSave(data) {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return;
        		}
        		form.reset();
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden && data?.selectedPeriodId) {
        			periodHidden.value = String(data.selectedPeriodId);
        		}
        		applyDefaultBreakToForm();
        		const period = selectedPeriodFromState();
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput && period?.startDate) {
        			dateInput.value = String(period.startDate);
        		}
        		D?.applyLocaleToTemporalInputs(form);
        		updateAssignmentFormFeedback(null);
        	}
        
        	async function performAssignmentSave() {
        		syncAssignmentPeriodFromSwitcher();
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) {
        			return false;
        		}
        		if (isAssignmentSetupBlocking()) {
        			const msg = t('dutycheck', 'Complete setup before adding assignments. See the checklist in this dialog.');
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(msg);
        			Msg.announce(msg, 'error');
        			return false;
        		}
        		const payload = readForm(form);
        		const validation = validate(payload);
        		if (validation) {
        			clearAssignmentFormSuccess();
        			updateAssignmentFormFeedback(validation);
        			Msg.announce(validation, 'error');
        			focusFirstInvalidAssignmentField(payload);
        			return false;
        		}
        		updateAssignmentFormFeedback(null);
        		setAssignmentFormBusy(form, true);
        		try {
        			const response = await submitAssignment(payload, true);
        			render(response?.data || {});
        			const savedMsg = buildAssignmentSavedMessage(payload);
        			clearAssignmentFormSuccess();
        			showAssignmentsSectionSuccess(savedMsg);
        			Msg.announce(savedMsg, 'success');
        			rememberBreakMinutes(payload.breakMinutes);
        			resetAssignmentFormAfterSave(response?.data || {});
        			return true;
        		} catch (err) {
        			announceAssignmentSaveError(err);
        			return false;
        		} finally {
        			setAssignmentFormBusy(form, false);
        		}
        	}
        
        	function syncAssignmentModalPrimary(instance) {
        		if (!instance?.primaryBtn) {
        			return;
        		}
        		const blocked = isAssignmentSetupBlocking() || !canAddAssignment();
        		instance.primaryBtn.disabled = blocked;
        		if (blocked) {
        			instance.primaryBtn.setAttribute('title', addAssignmentDisabledReason());
        		} else {
        			instance.primaryBtn.removeAttribute('title');
        		}
        	}
        
        	async function openAssignmentCreateModal(triggerEl) {
        		if (!canAddAssignment()) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        			return;
        		}
        		state.editingAssignmentId = null;
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = '';
        		}
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		renderSetupCallout(state.lastRosterData || {});
        		const instance = C.openModal({
        			title: t('dutycheck', 'Create assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save assignment'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => {
        				const ok = await performAssignmentSave();
        				return ok;
        			},
        			onClose: (result) => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				if (!result) {
        					clearAssignmentFormSuccess();
        					return;
        				}
        				const addBtn = document.getElementById('dc-roster-add-assignment');
        				if (addBtn && typeof addBtn.focus === 'function') {
        					addBtn.focus();
        				}
        			},
        			onCancel: () => {
        				clearAssignmentFormSuccess();
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        		const form = document.getElementById('dc-assignment-form');
        		if (form && !form.dataset.dcModalSyncWired) {
        			form.dataset.dcModalSyncWired = '1';
        			form.addEventListener('change', () => {
        				if (assignmentModalInstance?._open) {
        					syncAssignmentModalPrimary(assignmentModalInstance);
        				}
        			});
        		}
        	}
        
        	async function openAssignmentEditModal(assignment, triggerEl) {
        		if (!canAddAssignment() || !assignment?.id) {
        			Msg.announce(addAssignmentDisabledReason(), 'warning');
        			return;
        		}
        		state.editingAssignmentId = Number(assignment.id);
        		state.editingAssignmentVersion = Number(assignment.version ?? 0);
        		await ensureAssignmentModalPrereqs();
        		prepareAssignmentFormForModal();
        		const idHidden = document.getElementById('dc-assignment-id');
        		if (idHidden) {
        			idHidden.value = String(assignment.id);
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		if (dateInput) {
        			dateInput.value = String(assignment.dutyDate || '');
        		}
        		refreshAssignmentFormEligibility();
        		const emp = document.getElementById('dc-assignment-employee');
        		if (emp) {
        			emp.value = String(assignment.employeeId || '');
        		}
        		const loc = document.getElementById('dc-assignment-location');
        		if (loc) {
        			loc.value = String(assignment.locationId || '');
        		}
        		const start = document.getElementById('dc-assignment-start');
        		if (start) {
        			start.value = String(assignment.startTime || '').slice(0, 5);
        		}
        		const end = document.getElementById('dc-assignment-end');
        		if (end) {
        			end.value = String(assignment.endTime || '').slice(0, 5);
        		}
        		const br = document.getElementById('dc-assignment-break');
        		if (br) {
        			br.value = String(assignment.breakMinutes ?? 0);
        		}
        		const note = document.getElementById('dc-assignment-note');
        		if (note) {
        			note.value = String(assignment.note || '');
        		}
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		clearAssignmentsSectionSuccess();
        		const panel = document.getElementById('dc-assignment-form-panel');
        		if (!panel) {
        			return;
        		}
        		const instance = C.openModal({
        			title: t('dutycheck', 'Edit assignment'),
        			dialogClass: 'dc-modal__dialog--roster-assignment',
        			primaryLabel: t('dutycheck', 'Save changes'),
        			cancelLabel: t('dutycheck', 'Cancel'),
        			render: () => panel,
        			onSubmit: async () => performAssignmentSave(),
        			onClose: () => {
        				assignmentModalInstance = null;
        				state.editingAssignmentId = null;
        				restoreAssignmentFormHost();
        				clearAssignmentFormSuccess();
        				if (triggerEl && typeof triggerEl.focus === 'function') {
        					triggerEl.focus();
        				}
        			},
        		});
        		assignmentModalInstance = instance;
        		syncAssignmentModalPrimary(instance);
        	}
        
        	async function cancelAssignmentRow(assignment, triggerEl) {
        		if (!assignment?.id || !canAddAssignment()) {
        			return;
        		}
        		const ok = window.confirm(
        			t('dutycheck', 'Cancel this shift? It will be removed from the open period and staff will not see it after publish.'),
        		);
        		if (!ok) {
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/assignments/${assignment.id}/cancel`, {});
        			render(response?.data || {});
        			showAssignmentsSectionSuccess(t('dutycheck', 'Shift cancelled.'));
        			Msg.announce(t('dutycheck', 'Shift cancelled.'), 'success');
        		} catch (err) {
        			Msg.handleApiError(err);
        		} finally {
        			if (triggerEl && typeof triggerEl.focus === 'function') {
        				triggerEl.focus();
        			}
        		}
        	}
        
        	let templatesInflight = null;
        
        	async function ensureAssignmentModalPrereqs() {
        		const jobs = [loadTemplates()];
        		if (state.planningDefaultsFresh !== true) {
        			jobs.push(refreshPlanningDefaultFromServer());
        		}
        		await Promise.all(jobs);
        	}
        
        	async function loadTemplates() {
        		if (state.templatesLoaded) {
        			fillTemplateSelect();
        			return;
        		}
        		if (templatesInflight) {
        			await templatesInflight;
        			fillTemplateSelect();
        			return;
        		}
        		templatesInflight = (async () => {
        			try {
        				const response = await Api.get('/apps/dutycheck/api/templates', {});
        				state.templates = Array.isArray(response?.data) ? response.data : [];
        				state.templatesLoaded = true;
        			} catch (_) {
        				state.templates = [];
        				state.templatesLoaded = false;
        			} finally {
        				templatesInflight = null;
        			}
        		})();
        		await templatesInflight;
        		fillTemplateSelect();
        	}
        
        	function fillTemplateSelect() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select) {
        			return;
        		}
        		const current = select.value;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'No template — enter times manually') }));
        		for (const tpl of state.templates) {
        			const label = `${tpl.name || ''} (${String(tpl.startTime || '').slice(0, 5)}–${String(tpl.endTime || '').slice(0, 5)})`;
        			select.appendChild(create('option', { value: String(tpl.id), text: label }));
        		}
        		if (current && [...select.options].some((o) => o.value === current)) {
        			select.value = current;
        		}
        	}
        
        	function applySelectedTemplate() {
        		const select = document.getElementById('dc-assignment-template');
        		if (!select || !select.value) {
        			return;
        		}
        		const tpl = state.templates.find((t) => String(t.id) === String(select.value));
        		if (!tpl) {
        			return;
        		}
        		const start = document.getElementById('dc-assignment-start');
        		const end = document.getElementById('dc-assignment-end');
        		const br = document.getElementById('dc-assignment-break');
        		const loc = document.getElementById('dc-assignment-location');
        		if (start) start.value = String(tpl.startTime || '').slice(0, 5);
        		if (end) end.value = String(tpl.endTime || '').slice(0, 5);
        		if (br) br.value = String(tpl.breakMinutes ?? 0);
        		if (loc && tpl.locationId) loc.value = String(tpl.locationId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        		refreshAssignmentFormEligibility();
        	}
        
        	function refreshAcknowledgeStats(periodId) {
        		const el = document.getElementById('dc-roster-ack-stats');
        		if (!el) {
        			return;
        		}
        		const id = Number(periodId);
        		if (!Number.isInteger(id) || id <= 0) {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === id);
        		const status = String(period?.status || '').toLowerCase();
        		if (status !== 'published' && status !== 'closed') {
        			el.hidden = true;
        			el.textContent = '';
        			return;
        		}
        		const rows = Array.isArray(state.assignments) ? state.assignments : [];
        		let total = 0;
        		let acked = 0;
        		for (const row of rows) {
        			if (String(row?.status || '').toLowerCase() === 'cancelled') {
        				continue;
        			}
        			total += 1;
        			if (row?.acknowledged || row?.acknowledgedAt) {
        				acked += 1;
        			}
        		}
        		const pct = total === 0 ? 0 : Math.round((acked / total) * 1000) / 10;
        		el.hidden = false;
        		el.textContent = t('dutycheck', 'Staff seen: {acked}/{total} ({pct}%)')
        			.replace('{acked}', String(acked))
        			.replace('{total}', String(total))
        			.replace('{pct}', String(pct));
        	}
        
        	function fillCopySourceSelect(selectedPeriodId) {
        		const select = document.getElementById('dc-roster-copy-source');
        		const wrap = document.getElementById('dc-roster-copy-period');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!select || !wrap) {
        			return;
        		}
        		const period = state.periods.find((p) => Number(p.id) === Number(selectedPeriodId));
        		const open = period && String(period.status || '').toLowerCase() === 'open';
        		wrap.hidden = !open;
        		state.copyPreviewReady = false;
        		if (applyBtn) applyBtn.disabled = true;
        		select.replaceChildren();
        		select.appendChild(create('option', { value: '', text: t('dutycheck', 'Choose a source period…') }));
        		for (const p of state.periods) {
        			if (Number(p.id) === Number(selectedPeriodId)) {
        				continue;
        			}
        			const label = `${p.startDate || ''} → ${p.endDate || ''} (${p.status || ''})`;
        			select.appendChild(create('option', { value: String(p.id), text: label }));
        		}
        	}
        
        	async function runCopyPeriod(dryRun) {
        		const targetId = Number(document.getElementById('dc-roster-period-switcher')?.value || 0);
        		const sourceId = Number(document.getElementById('dc-roster-copy-source')?.value || 0);
        		const status = document.getElementById('dc-roster-copy-status');
        		const applyBtn = document.getElementById('dc-roster-copy-apply');
        		if (!targetId || !sourceId) {
        			Msg.announce(t('dutycheck', 'Choose a source period first.'), 'warning');
        			return;
        		}
        		try {
        			const response = await Api.post(`/apps/dutycheck/api/periods/${targetId}/copy`, {
        				sourcePeriodId: sourceId,
        				dryRun: !!dryRun,
        			});
        			const data = response?.data || {};
        			const created = Number(data.created ?? data.wouldCreate ?? data.count ?? 0);
        			const skipped = Number(data.skipped ?? 0);
        			const msg = dryRun
        				? t('dutycheck', 'Preview: {created} would be copied, {skipped} skipped. Review, then Apply copy.')
        					.replace('{created}', String(data.wouldCreate ?? data.previewCreated ?? created))
        					.replace('{skipped}', String(data.wouldSkip ?? skipped))
        				: t('dutycheck', 'Copied {created} assignment(s). Conflicts were recomputed.')
        					.replace('{created}', String(created));
        			if (status) {
        				status.hidden = false;
        				status.textContent = msg;
        			}
        			Msg.announce(msg, dryRun ? 'info' : 'success');
        			if (dryRun) {
        				state.copyPreviewReady = true;
        				if (applyBtn) applyBtn.disabled = false;
        			} else {
        				state.copyPreviewReady = false;
        				if (applyBtn) applyBtn.disabled = true;
        				render(data.roster || data || {});
        				if (!data.roster && data.assignments) {
        					render(data);
        				} else if (!data.roster) {
        					await loadRoster(targetId);
        				}
        			}
        		} catch (err) {
        			Msg.handleApiError(err);
        		}
        	}
        
        	function updateAdminExportLinks(selectedPeriodId) {
        		const csv = document.getElementById('dc-roster-export-csv');
        		const printLink = document.getElementById('dc-roster-export-print');
        		if (!csv || !printLink) {
        			return;
        		}
        		const urls = readUrls();
        		const baseCsv = String(urls.rosterExportCsv || '');
        		const basePrint = String(urls.rosterPrint || '');
        		const id = Number(selectedPeriodId);
        		const ok = baseCsv !== '' && basePrint !== '' && Number.isInteger(id) && id > 0;
        		if (!ok) {
        			csv.href = '#dc-roster-period-section';
        			printLink.href = '#dc-roster-period-section';
        			csv.setAttribute('aria-disabled', 'true');
        			printLink.setAttribute('aria-disabled', 'true');
        			return;
        		}
        		const q = new URLSearchParams({ periodId: String(id) }).toString();
        		csv.href = baseCsv + (baseCsv.includes('?') ? '&' : '?') + q;
        		printLink.href = basePrint + (basePrint.includes('?') ? '&' : '?') + q;
        		csv.removeAttribute('aria-disabled');
        		printLink.removeAttribute('aria-disabled');
        	}
        
        	function wireAdminExportGuards() {
        		const guard = (event) => {
        			const el = event.currentTarget;
        			if (el && el.getAttribute('aria-disabled') === 'true') {
        				event.preventDefault();
        				Msg.announce(
        					t('dutycheck', 'Choose a period in the selector above before exporting or printing.'),
        					'warning',
        				);
        			}
        		};
        		document.getElementById('dc-roster-export-csv')?.addEventListener('click', guard);
        		document.getElementById('dc-roster-export-print')?.addEventListener('click', guard);
        	}
        
        	let rosterPhpL10nCache;
        	function getRosterPhpL10n() {
        		if (rosterPhpL10nCache !== undefined) {
        			return rosterPhpL10nCache;
        		}
        		const el = document.getElementById('dc-roster-php-l10n');
        		const raw = el && el.dataset ? el.dataset.l10n : '';
        		if (!raw) {
        			rosterPhpL10nCache = null;
        			return null;
        		}
        		try {
        			rosterPhpL10nCache = JSON.parse(raw) || null;
        		} catch (_) {
        			rosterPhpL10nCache = null;
        		}
        		return rosterPhpL10nCache;
        	}
        
        	function translateConflictMessage(message) {
        		const raw = String(message || '');
        		if (!raw) {
        			return t('dutycheck', 'Unknown conflict');
        		}
        		const fromPhp = getRosterPhpL10n()?.conflictMessages?.[raw];
        		if (fromPhp) {
        			return fromPhp;
        		}
        		return t('dutycheck', raw);
        	}
        
        	function render(data) {
        		state.lastRosterData = data;
        		if (data.defaultBreakMinutes !== undefined && data.defaultBreakMinutes !== null) {
        			state.defaultBreakMinutes = clampBreakMinutes(data.defaultBreakMinutes);
        			state.planningDefaultsFresh = true;
        		}
        		state.periods = data.periods || [];
        		state.employees = data.employees || [];
        		state.locations = data.locations || [];
        		state.assignments = data.assignments || [];
        		state.absenceBlocks = data.absenceBlocks || [];
        		state.canCreateAssignments = Boolean(data.canCreateAssignments);
        		fillPeriodSwitcher(state.periods, data.selectedPeriodId);
        		const periodHidden = document.getElementById('dc-assignment-period');
        		if (periodHidden) {
        			periodHidden.value = data.selectedPeriodId ? String(data.selectedPeriodId) : '';
        		}
        		const dateInput = document.getElementById('dc-assignment-date');
        		const selectedPeriod = state.periods.find((p) => Number(p.id) === Number(data.selectedPeriodId));
        		if (dateInput && selectedPeriod?.startDate && selectedPeriod?.endDate) {
        			dateInput.min = String(selectedPeriod.startDate);
        			dateInput.max = String(selectedPeriod.endDate);
        			if (!dateInput.value && state.canCreateAssignments) {
        				dateInput.value = String(selectedPeriod.startDate);
        			}
        		} else if (dateInput) {
        			dateInput.removeAttribute('min');
        			dateInput.removeAttribute('max');
        		}
        		fillSelect('dc-assignment-location', state.locations, {
        			emptyText: t('dutycheck', 'No active locations yet'),
        		});
        		updateLocationTimezoneHint();
        		refreshAssignmentFormEligibility();
        		renderAssignments(data.assignments || []);
        		renderConflicts(data.conflicts || []);
        		renderSetupCallout(data);
        		updateAddAssignmentControl();
        		updateAdminExportLinks(data.selectedPeriodId);
        		fillCopySourceSelect(data.selectedPeriodId);
        		refreshAcknowledgeStats(data.selectedPeriodId);
        		D?.applyLocaleToTemporalInputs(document.getElementById('dc-assignment-form') || document);
        	}
        
        	function selectedPeriodIdFromUrl() {
        		const params = new URLSearchParams(window.location.search || '');
        		const raw = params.get('periodId');
        		const parsed = Number(raw);
        		return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
        	}
        
        	function updateUrlPeriodId(periodId) {
        		const params = new URLSearchParams(window.location.search || '');
        		if (periodId && Number(periodId) > 0) {
        			params.set('periodId', String(periodId));
        		} else {
        			params.delete('periodId');
        		}
        		const query = params.toString();
        		window.history.replaceState({}, '', query ? `${window.location.pathname}?${query}` : window.location.pathname);
        	}
        
        	async function loadRoster(periodId) {
        		if (C.isModalOpen?.()) {
        			C.dismissOpenModal();
        		} else {
        			restoreAssignmentFormPanel();
        		}
        		setAssignmentsEmptyPanelVisible(false);
        		const tbody = document.getElementById('dc-assignments-table-body');
        		C.setLoadingRow(tbody, 7);
        		try {
        			const response = await Api.get('/apps/dutycheck/api/roster', { periodId });
        			render(response?.data || {});
        			updateUrlPeriodId(response?.data?.selectedPeriodId || null);
        			void loadRosterSignals(response?.data || {});
        			void loadCoverageStrip(response?.data?.selectedPeriodId);
        			return response?.data || {};
        		} catch (err) {
        			const code = String(err?.payload?.error?.code || err?.code || '');
        			if (code === 'PERIOD_NOT_FOUND' && periodId != null) {
        				try {
        					const fallback = await Api.get('/apps/dutycheck/api/roster', {});
        					render(fallback?.data || {});
        					updateUrlPeriodId(fallback?.data?.selectedPeriodId || null);
        					void loadRosterSignals(fallback?.data || {});
        					void loadCoverageStrip(fallback?.data?.selectedPeriodId);
        					Msg.announce(
        						t('dutycheck', 'That planning period is not available. Showing the roster for the current period instead.'),
        						'warning',
        					);
        					return fallback?.data || {};
        				} catch (retryErr) {
        					Msg.handleApiError(retryErr);
        					C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        					return null;
        				}
        			}
        			Msg.handleApiError(err);
        			C.renderTableFetchError(tbody, 8, t('dutycheck', 'Could not load the roster. Reload the page or contact an administrator if this keeps happening.'));
        			return null;
        		} finally {
        			C.clearLoadingRow(tbody);
        		}
        	}
        
        	function dateOverlapsBlackout(day, blackout) {
        		const start = String(blackout.startAt || blackout.start_at || '').slice(0, 10);
        		const end = String(blackout.endAt || blackout.end_at || '').slice(0, 10);
        		if (!start || !end) return false;
        		return day >= start && day <= end;
        	}
        
        	async function loadRosterSignals(data) {
        		state.preferencesByEmployee = {};
        		state.blackoutsByEmployeeDate = {};
        		const employees = data.employees || state.employees || [];
        		const period = (data.periods || state.periods || []).find(
        			(p) => Number(p.id) === Number(data.selectedPeriodId),
        		);
        		const from = period?.startDate || period?.start_date || '';
        		const to = period?.endDate || period?.end_date || '';
        		const ids = employees.map((e) => Number(e.id)).filter((n) => n > 0);
        		if (!ids.length) return;
        		try {
        			const res = await Api.get('/apps/dutycheck/api/roster/signals', {
        				employeeIds: ids,
        				from,
        				to,
        			});
        			const prefs = res?.data?.preferences || [];
        			const blackouts = res?.data?.blackouts || [];
        			prefs.forEach((p) => {
        				const eid = String(p.employeeId || p.employee_id || '');
        				if (!eid) return;
        				if (!state.preferencesByEmployee[eid]) state.preferencesByEmployee[eid] = [];
        				state.preferencesByEmployee[eid].push(p);
        			});
        			const days = [];
        			if (from && to) {
        				const cur = new Date(from + 'T12:00:00');
        				const end = new Date(to + 'T12:00:00');
        				while (cur <= end) {
        					const pad = (n) => String(n).padStart(2, '0');
        					days.push(`${cur.getFullYear()}-${pad(cur.getMonth() + 1)}-${pad(cur.getDate())}`);
        					cur.setDate(cur.getDate() + 1);
        				}
        			}
        			blackouts.forEach((b) => {
        				const eid = Number(b.employeeId || b.employee_id || 0);
        				if (!eid) return;
        				days.forEach((day) => {
        					if (dateOverlapsBlackout(day, b)) {
        						state.blackoutsByEmployeeDate[`${eid}|${day}`] = true;
        					}
        				});
        			});
        			if (Object.keys(state.preferencesByEmployee).length || Object.keys(state.blackoutsByEmployeeDate).length) {
        				paintGridWindow({ force: true });
        			}
        		} catch (_) {
        			// Optional enrichment — ignore failures.
        		}
        	}
        
        	async function loadCoverageStrip(periodId) {
        		const strip = document.getElementById('dc-coverage-strip');
        		const text = document.getElementById('dc-coverage-strip-text');
        		if (!strip || !text) return;
        		const id = Number(periodId || 0);
        		if (!id) {
        			strip.hidden = true;
        			return;
        		}
        		try {
        			const res = await Api.get(`/apps/dutycheck/api/periods/${id}/publish-readiness`);
        			const d = res?.data || {};
        			const hard = Number(d.hardConflicts ?? d.blockingConflicts ?? d.hard ?? 0);
        			const soft = Number(d.softConflicts ?? d.ackRequired ?? d.soft ?? 0);
        			const ready = d.ready === true || (hard === 0 && soft === 0 && d.blocking !== true);
        			let message;
        			if (ready || (hard === 0 && soft === 0)) {
        				message = t('dutycheck', 'Ready to publish — no blocking issues.');
        				strip.classList.remove('dc-coverage-strip--warn', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--ok');
        			} else if (hard > 0) {
        				message = t('dutycheck', '{n} must-fix issues before publish.')
        					.replace('{n}', String(hard));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--warn');
        				strip.classList.add('dc-coverage-strip--critical');
        			} else {
        				message = t('dutycheck', '{n} items need confirmation.')
        					.replace('{n}', String(soft));
        				strip.classList.remove('dc-coverage-strip--ok', 'dc-coverage-strip--critical');
        				strip.classList.add('dc-coverage-strip--warn');
        			}
        			text.textContent = message;
        			strip.hidden = false;
        		} catch (_) {
        			strip.hidden = true;
        		}
        	}
        
        	function currentSuggestLocationId() {
        		const params = new URLSearchParams(window.location.search || '');
        		const fromUrl = Number(params.get('locationId') || 0);
        		if (Number.isInteger(fromUrl) && fromUrl > 0) {
        			return fromUrl;
        		}
        		const filterEl = document.getElementById('dc-roster-location')
        			|| document.getElementById('dc-assignment-location');
        		const fromUi = Number(filterEl?.value || 0);
        		if (Number.isInteger(fromUi) && fromUi > 0) {
        			return fromUi;
        		}
        		return null;
        	}
        
        	async function runSuggestFill() {
        		const periodId = Number(selectedPeriodId() || 0);
        		if (!periodId) {
        			Msg.announce(t('dutycheck', 'Pick an open period first.'), 'warning');
        			return;
        		}
        		const btn = document.getElementById('dc-roster-suggest-fill');
        		if (btn) {
        			btn.disabled = true;
        			btn.setAttribute('aria-busy', 'true');
        		}
        		const locId = currentSuggestLocationId();
        		const suggestBody = { locationId: locId || null };
        		try {
        			const preview = await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-preview`, suggestBody);
        			const d = preview?.data || {};
        			if ((d.created ?? 0) === 0 && (d.skippedNoPattern ?? 0) === 0) {
        				const noLoc = Number(d.skippedNoLocation ?? 0);
        				Msg.announce(
        					noLoc > 0
        						? t('dutycheck', 'No shifts to fill. Patterns need a default location on working days (or pick a location filter).')
        						: t('dutycheck', 'No shifts to fill. Check that patterns have a default location and working days with times.'),
        					'warning',
        				);
        				return;
        			}
        			const body = create('div', { class: 'dc-suggest-preview' }, [
        				create('p', {
        					text: t('dutycheck', 'Would create {n} shifts from patterns.')
        						.replace('{n}', String(d.created ?? 0)),
        				}),
        				create('ul', { class: 'dc-suggest-preview__counts' }, [
        					create('li', { text: t('dutycheck', 'Skipped (already filled): {n}').replace('{n}', String(d.skippedExisting ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (absence): {n}').replace('{n}', String(d.skippedAbsence ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (kann nicht): {n}').replace('{n}', String(d.skippedBlackout ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no pattern): {n}').replace('{n}', String(d.skippedNoPattern ?? 0)) }),
        					create('li', { text: t('dutycheck', 'Skipped (no location): {n}').replace('{n}', String(d.skippedNoLocation ?? 0)) }),
        				]),
        			]);
        			const confirmed = await new Promise((resolve) => {
        				let settled = false;
        				const finish = (value) => {
        					if (settled) return;
        					settled = true;
        					resolve(value);
        				};
        				C.openModal({
        					title: t('dutycheck', 'Suggest fill preview'),
        					primaryLabel: t('dutycheck', 'Apply suggest fill'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					render: () => body,
        					onSubmit: async () => {
        						finish(true);
        						return true;
        					},
        					onCancel: () => finish(false),
        					onClose: () => finish(false),
        				});
        			});
        			if (!confirmed) return;
        			if ((d.created ?? 0) < 1) {
        				Msg.announce(t('dutycheck', 'Nothing to fill.'), 'info');
        				return;
        			}
        			await Api.post(`/apps/dutycheck/api/periods/${periodId}/suggest-confirm`, suggestBody);
        			Msg.announce(t('dutycheck', 'Suggest fill applied.'), 'success');
        			await loadRoster(periodId);
        		} catch (err) {
        			const code = String(err?.code || err?.payload?.error?.code || '');
        			if (code === 'ROTATION_DISABLED') {
        				Msg.announce(t('dutycheck', 'Turn on Muster in Duty & team settings first.'), 'warning');
        			} else if (code === 'SUGGEST_LOCATION_REQUIRED') {
        				Msg.announce(
        					t('dutycheck', 'Suggest fill needs a location. Set a default location on pattern working days, or filter the roster by location.'),
        					'warning',
        				);
        			} else if (code === 'RATE_LIMITED') {
        				Msg.announce(t('dutycheck', 'Too many suggest-fill requests. Wait a minute and try again.'), 'warning');
        			} else {
        				Msg.handleApiError(err);
        			}
        		} finally {
        			if (btn) {
        				btn.disabled = false;
        				btn.removeAttribute('aria-busy');
        			}
        		}
        	}
        
        	function clearAssignmentFormFields() {
        		const form = document.getElementById('dc-assignment-form');
        		if (!form) return;
        		const periodHidden = document.getElementById('dc-assignment-period');
        		const periodVal = periodHidden ? String(periodHidden.value || '') : '';
        		form.reset();
        		if (periodHidden && periodVal) {
        			periodHidden.value = periodVal;
        		}
        		applyDefaultBreakToForm();
        		D?.applyLocaleToTemporalInputs(form);
        		refreshAssignmentFormEligibility();
        		const clearedMsg = t('dutycheck', 'Form cleared. You can enter another assignment.');
        		if (document.getElementById('dc-assignment-form-panel')?.closest('.dc-modal')) {
        			showAssignmentFormSuccess(clearedMsg, 'info');
        		}
        		Msg.announce(clearedMsg, 'info');
        	}
        
        	function readForm(form) {
        		const data = new FormData(form);
        		const rawStart = String(data.get('startTime') || '');
        		const rawEnd = String(data.get('endTime') || '');
        		const startTime = (D?.formatClock24FromTimeString(rawStart) || rawStart).trim();
        		const endTime = (D?.formatClock24FromTimeString(rawEnd) || rawEnd).trim();
        		const assignmentId = Number(data.get('assignmentId') || state.editingAssignmentId || 0);
        		const out = {
        			periodId: Number(data.get('periodId')),
        			employeeId: Number(data.get('employeeId')),
        			locationId: Number(data.get('locationId')),
        			dutyDate: String(data.get('dutyDate') || ''),
        			startTime,
        			endTime,
        			breakMinutes: Number(data.get('breakMinutes') || 0),
        			note: String(data.get('note') || '').trim(),
        			assignmentId: Number.isInteger(assignmentId) && assignmentId > 0 ? assignmentId : 0,
        		};
        		if (out.assignmentId > 0 && state.editingAssignmentVersion !== null && state.editingAssignmentVersion !== undefined) {
        			out.expectedVersion = Number(state.editingAssignmentVersion);
        		}
        		return out;
        	}
        
        	/**
        	 * Match server RosterService::effectiveMinutes — overnight when end <= start wall time.
        	 */
        	function effectiveWorkMinutes(startTime, endTime, breakMinutes) {
        		const start = D?.wallClockMinutesFromTimeString?.(startTime) ?? null;
        		const end = D?.wallClockMinutesFromTimeString?.(endTime) ?? null;
        		if (start === null || end === null) return null;
        		let endAdj = end;
        		if (endAdj <= start) {
        			endAdj += 24 * 60;
        		}
        		return (endAdj - start) - breakMinutes;
        	}
        
        	function validate(payload) {
        		if (!Number.isInteger(payload.periodId) || payload.periodId <= 0) {
        			return t('dutycheck', 'Select a period before saving.');
        		}
        		const period = selectedPeriodForPayload(payload);
        		if (!period) {
        			return t('dutycheck', 'That planning period was not found.');
        		}
        		if (String(period.status || '').toLowerCase() !== 'open') {
        			return t('dutycheck', 'The selected period is not open for planning.');
        		}
        		if (!Number.isInteger(payload.employeeId) || payload.employeeId <= 0) {
        			return t('dutycheck', 'Select an employee.');
        		}
        		if (!Number.isInteger(payload.locationId) || payload.locationId <= 0) {
        			return t('dutycheck', 'Select a location.');
        		}
        		if (!payload.dutyDate || !payload.startTime || !payload.endTime) {
        			return t('dutycheck', 'Date, start time, and end time are required.');
        		}
        		if (period.startDate && period.endDate
        			&& (payload.dutyDate < String(period.startDate) || payload.dutyDate > String(period.endDate))) {
        			return t('dutycheck', 'Date must fall within the selected period.');
        		}
        		if (payload.startTime === payload.endTime) {
        			return t(
        				'dutycheck',
        				'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        			);
        		}
        		if (!Number.isFinite(payload.breakMinutes) || payload.breakMinutes < 0 || payload.breakMinutes > 720) {
        			return t('dutycheck', 'Break minutes must be between 0 and 720.');
        		}
        		const effective = effectiveWorkMinutes(payload.startTime, payload.endTime, payload.breakMinutes);
        		if (effective === null) {
        			return t('dutycheck', 'Please check the dates and times.');
        		}
        		if (effective <= 0) {
        			return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        		}
        		if (isDateInAbsenceBlock(payload.employeeId, payload.dutyDate)) {
        			return t('dutycheck', 'This employee has approved leave on the selected date.');
        		}
        		if (findOverlapAssignment(payload)) {
        			return t('dutycheck', 'This employee already has an overlapping assignment for these times.');
        		}
        		return null;
        	}
        
        	function errorMessageFor(error) {
        		const code = errorCodeFrom(error);
        		switch (code) {
        			case 'PERIOD_NOT_OPEN':
        				return t('dutycheck', 'The selected period is not open for planning.');
        			case 'PERIOD_NOT_FOUND':
        				return t('dutycheck', 'That planning period was not found.');
        			case 'DATE_OUTSIDE_PERIOD':
        				return t('dutycheck', 'Date must fall within the selected period.');
        			case 'EMPLOYEE_NOT_FOUND':
        				return t('dutycheck', 'Employee not found or no longer available.');
        			case 'LOCATION_NOT_FOUND':
        				return t('dutycheck', 'Location not found or no longer available.');
        			case 'EQUAL_DUTY_TIMES':
        				return t(
        					'dutycheck',
        					'Start and end time must be different. For overnight shifts, set the end time earlier than the start (e.g. 22:00–06:00).',
        				);
        			case 'INVALID_SHIFT_LENGTH':
        				return t('dutycheck', 'Shift length is invalid after break (check overnight times and break minutes).');
        			case 'INVALID_BREAK_MINUTES':
        				return t('dutycheck', 'Break minutes are out of the allowed range.');
        			case 'NOTE_TOO_LONG':
        				return t('dutycheck', 'Note is too long (maximum 512 characters).');
        			case 'ASSIGNMENT_OVERLAP':
        				return t('dutycheck', 'This employee already has an overlapping assignment.');
        			case 'ASSIGNMENT_DUPLICATE_SLOT':
        				return t('dutycheck', 'This exact assignment already exists. Reload the page to see the latest roster.');
        			case 'STALE_VERSION':
        				return t('dutycheck', 'Someone else changed this assignment. Reload the roster and try again.');
        			case 'CONFLICT_ACK_STALE':
        				return t('dutycheck', 'Someone else already confirmed this exception. Reload to see the latest checks.');
        			case 'COMPANY_MEMBERSHIP_REQUIRED':
        				return t('dutycheck', 'Your account is not on a company yet. Ask an administrator to add you under Settings → Companies.');
        			case 'EXPECTED_VERSION_REQUIRED':
        				return t('dutycheck', 'This edit is out of date. Reload the roster and open the assignment again.');
        			case 'ABSENCE_CONFLICT':
        				return t('dutycheck', 'This employee has an approved absence on that date.');
        			case 'BLACKOUT_CONFLICT':
        				return t('dutycheck', 'This employee marked that they cannot work during this time.');
        			case 'CONFLICT_ACK_REQUIRED':
        				return t('dutycheck', 'A planning rule needs your confirmation before this shift can be saved.');
        			case 'REASON_TOO_SHORT':
        				return t('dutycheck', 'Acknowledgement reason must contain at least 10 characters.');
        			case 'INTERNAL_ERROR':
        				return t('dutycheck', 'The server could not save this assignment. Reload the page and try again, or contact an administrator.');
        			case 'QUALIFICATION_MISSING':
        				return t('dutycheck', 'This employee is missing a required qualification for that location.');
        			case 'ASSIGNMENT_NOT_FOUND':
        				return t('dutycheck', 'That assignment no longer exists. Reload the page.');
        			case 'SCHEMA_NOT_READY':
        				return t('dutycheck', 'DutyCheck database upgrade is incomplete. Ask an administrator to run occ upgrade.');
        			case 'INVALID_DATE':
        			case 'INVALID_TIME':
        				return t('dutycheck', 'Please check the dates and times.');
        			case 'PERIOD_ID_REQUIRED':
        				return t('dutycheck', 'Select a period before saving.');
        			case 'EMPLOYEE_ID_REQUIRED':
        				return t('dutycheck', 'Select an employee.');
        			case 'LOCATION_ID_REQUIRED':
        				return t('dutycheck', 'Select a location.');
        			default:
        				return null;
        		}
        	}
        
        
        	async function loadPendingSwaps() {
        		const list = document.getElementById('dc-swap-list');
        		const empty = document.getElementById('dc-swap-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/swaps');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				li.appendChild(create('p', {
        					text: row.toEmployeeId
        						? t('dutycheck', 'Assignment #{id} · from #{from} → employee #{to}')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId))
        							.replace('{to}', String(row.toEmployeeId))
        						: t('dutycheck', 'Assignment #{id} · from employee #{from} → open pool')
        							.replace('{id}', String(row.assignmentId))
        							.replace('{from}', String(row.fromEmployeeId)),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'approved' });
        						Msg.announce(t('dutycheck', 'Swap approved.'), 'success');
        						await loadPendingSwaps();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/swaps/${row.id}/review`, { decision: 'rejected' });
        						Msg.announce(t('dutycheck', 'Swap rejected.'), 'info');
        						await loadPendingSwaps();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load swap requests.');
        			}
        		}
        	}
        
        	async function approveOpenShiftClaim(openShiftId, acknowledgements = [], retryWithAck = true) {
        		try {
        			return await Api.post(`/apps/dutycheck/api/open-shifts/${openShiftId}/approve`, {
        				acknowledgements,
        			});
        		} catch (error) {
        			const code = errorCodeFrom(error);
        			if (retryWithAck && code === 'CONFLICT_ACK_REQUIRED') {
        				const conflicts = error?.payload?.error?.conflicts || [];
        				let conflictTypes = uniqueConflictTypes(conflicts);
        				if (!conflictTypes.length) {
        					conflictTypes = ['rest_time_violation'];
        				}
        				const summary = softConflictSummary(conflicts);
        				const reason = await C.promptReason({
        					title: t('dutycheck', 'Planning rule needs your confirmation'),
        					label: summary
        						? t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).\n\n{details}')
        							.replace('{details}', summary)
        						: t('dutycheck', 'Briefly explain why you are approving this claim anyway (at least 10 characters).'),
        					confirmLabel: t('dutycheck', 'Approve with confirmation'),
        					cancelLabel: t('dutycheck', 'Cancel'),
        					minLength: 10,
        				});
        				if (reason === null || reason.length < 10) {
        					throw error;
        				}
        				const acks = conflictTypes.map((type) => ({ conflictType: type, reason }));
        				return approveOpenShiftClaim(openShiftId, acks, false);
        			}
        			throw error;
        		}
        	}
        
        	async function loadPendingOpenClaims() {
        		const list = document.getElementById('dc-open-claim-list');
        		const empty = document.getElementById('dc-open-claim-empty');
        		if (!list) return;
        		list.replaceChildren();
        		try {
        			const res = await Api.get('/apps/dutycheck/api/open-shifts/pending');
        			const rows = Array.isArray(res?.data) ? res.data : [];
        			if (empty) empty.hidden = rows.length > 0;
        			for (const row of rows) {
        				const li = create('li', { class: 'dc-conflicts__item' });
        				const when = D?.formatDisplayDate?.(row.dutyDate) || row.dutyDate;
        				li.appendChild(create('p', {
        					text: t('dutycheck', '{date} · employee #{emp}')
        						.replace('{date}', String(when))
        						.replace('{emp}', String(row.claimedByEmployeeId || '—')),
        				}));
        				const approve = create('button', { type: 'button', class: 'button primary', text: t('dutycheck', 'Approve claim') });
        				const reject = create('button', { type: 'button', class: 'button', text: t('dutycheck', 'Reject claim') });
        				approve.style.minHeight = '44px';
        				reject.style.minHeight = '44px';
        				approve.addEventListener('click', async () => {
        					try {
        						await approveOpenShiftClaim(row.id);
        						Msg.announce(t('dutycheck', 'Claim approved — assignment created.'), 'success');
        						await loadPendingOpenClaims();
        						await loadRoster(selectedPeriodFromState()?.id || null);
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				reject.addEventListener('click', async () => {
        					try {
        						await Api.post(`/apps/dutycheck/api/open-shifts/${row.id}/reject`, {});
        						Msg.announce(t('dutycheck', 'Claim rejected.'), 'info');
        						await loadPendingOpenClaims();
        					} catch (err) {
        						Msg.handleApiError(err);
        					}
        				});
        				li.appendChild(approve);
        				li.appendChild(reject);
        				list.appendChild(li);
        			}
        		} catch (_) {
        			if (empty) {
        				empty.hidden = false;
        				empty.textContent = t('dutycheck', 'Could not load pending claims.');
        			}
        		}
        	}
        
        	function wireMarketplace() {
        		const toggle = document.getElementById('dc-open-shift-create');
        		const form = document.getElementById('dc-open-shift-form');
        		toggle?.addEventListener('click', () => {
        			if (!form) return;
        			form.hidden = !form.hidden;
        			const loc = document.getElementById('dc-os-location');
        			if (loc && state.locations?.length) {
        				loc.replaceChildren();
        				for (const l of state.locations) {
        					loc.appendChild(create('option', { value: String(l.id), text: String(l.name || l.id) }));
        				}
        			}
        			const period = selectedPeriodFromState();
        			const date = document.getElementById('dc-os-date');
        			if (date && period?.startDate) date.value = String(period.startDate);
        		});
        		document.getElementById('dc-os-save')?.addEventListener('click', async () => {
        			const period = selectedPeriodFromState();
        			if (!period?.id) {
        				Msg.announce(t('dutycheck', 'Select a period first.'), 'warning');
        				return;
        			}
        			try {
        				await Api.post('/apps/dutycheck/api/open-shifts', {
        					periodId: Number(period.id),
        					locationId: Number(document.getElementById('dc-os-location')?.value || 0),
        					dutyDate: String(document.getElementById('dc-os-date')?.value || ''),
        					startTime: String(document.getElementById('dc-os-start')?.value || ''),
        					endTime: String(document.getElementById('dc-os-end')?.value || ''),
        					breakMinutes: 0,
        				});
        				Msg.announce(t('dutycheck', 'Open shift posted.'), 'success');
        				if (form) form.hidden = true;
        			} catch (err) {
        				Msg.handleApiError(err);
        			}
        		});
        	}
        
        	document.addEventListener('DOMContentLoaded', async () => {
        		D?.applyLocaleToTemporalInputs(document);
        		wireBreakPrefillHint();
        		window.addEventListener('dc-planning-defaults-changed', (event) => {
        			const minutes = Number(event?.detail?.defaultBreakMinutes);
        			if (Number.isFinite(minutes)) {
        				state.defaultBreakMinutes = clampBreakMinutes(minutes);
        			}
        		});
        		wireAdminExportGuards();
        		wireMarketplace();
        		setRosterView('grid');
        		wireRosterVirtualization();
        		document.getElementById('dc-roster-view-grid')?.addEventListener('click', () => setRosterView('grid'));
        		document.getElementById('dc-roster-view-list')?.addEventListener('click', () => setRosterView('list'));
        		document.getElementById('dc-roster-bulk-clear')?.addEventListener('click', () => {
        			gridState.selected.clear();
        			syncVisibleGridChrome();
        			updateBulkBar();
        		});
        		document.getElementById('dc-roster-bulk-apply')?.addEventListener('click', () => {
        			void applyBulkFromTemplate();
        		});
        		document.getElementById('dc-roster-suggest-fill')?.addEventListener('click', () => {
        			void runSuggestFill();
        		});
        		await Promise.all([
        			loadRoster(selectedPeriodIdFromUrl()),
        			loadPendingSwaps(),
        			loadPendingOpenClaims(),
        		]);
        
        		const switcher = document.getElementById('dc-roster-period-switcher');
        		switcher?.addEventListener('change', async () => {
        			clearAssignmentsSectionSuccess();
        			const periodId = Number(switcher.value);
        			await Promise.all([
        				loadRoster(Number.isInteger(periodId) && periodId > 0 ? periodId : null),
        				loadPendingSwaps(),
        				loadPendingOpenClaims(),
        			]);
        		});
        
        		document.getElementById('dc-roster-assignments-section')?.addEventListener('click', (event) => {
        			const btn = event.target.closest('.dc-roster-add-assignment-trigger');
        			if (!btn) {
        				return;
        			}
        			openAssignmentCreateModal(btn);
        		});
        
        		document.getElementById('dc-assignment-form-clear')?.addEventListener('click', () => {
        			clearAssignmentFormFields();
        		});
        
        		document.getElementById('dc-assignment-template')?.addEventListener('change', () => {
        			applySelectedTemplate();
        		});
        		document.getElementById('dc-roster-copy-preview')?.addEventListener('click', () => {
        			runCopyPeriod(true);
        		});
        		document.getElementById('dc-roster-copy-apply')?.addEventListener('click', async () => {
        			if (!state.copyPreviewReady) {
        				Msg.announce(t('dutycheck', 'Run Preview copy first.'), 'warning');
        				return;
        			}
        			await runCopyPeriod(false);
        		});
        		document.getElementById('dc-roster-copy-source')?.addEventListener('change', () => {
        			state.copyPreviewReady = false;
        			const applyBtn = document.getElementById('dc-roster-copy-apply');
        			if (applyBtn) applyBtn.disabled = true;
        		});
        
        		const form = document.getElementById('dc-assignment-form');
        		const scheduleEligibilityRefresh = () => {
        			clearAssignmentFormSuccess();
        			clearAssignmentsSectionSuccess();
        			refreshAssignmentFormEligibility();
        		};
        		document.getElementById('dc-assignment-date')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-employee')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-start')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-end')?.addEventListener('change', scheduleEligibilityRefresh);
        		document.getElementById('dc-assignment-location')?.addEventListener('change', scheduleEligibilityRefresh);
        		form?.addEventListener('submit', (event) => {
        			event.preventDefault();
        			const dialog = form.closest('.dc-modal__dialog--roster-assignment');
        			if (!dialog) {
        				return;
        			}
        			const primary = dialog.querySelector('.dc-modal__actions .button.primary');
        			if (primary && !primary.disabled) {
        				primary.click();
        			}
        		});
        	});
        })();
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:248:10)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: does not paginate listAssignments
    ok 3 - does not paginate listAssignments
      ---
      duration_ms: 0.31294
      type: 'test'
      ...
    # Subtest: assigns grid role only when rows exist; empty shell stays role=status
    ok 4 - assigns grid role only when rows exist; empty shell stays role=status
      ---
      duration_ms: 0.416343
      type: 'test'
      ...
    # Subtest: exposes a granny-readable status and a bounded scroller
    ok 5 - exposes a granny-readable status and a bounded scroller
      ---
      duration_ms: 1.504004
      type: 'test'
      ...
    1..5
not ok 5 - roster.js virtualizes grid and list without paginating the API
  ---
  duration_ms: 5.309326
  type: 'suite'
  location: '/home/alex/Development/nextcloud-dev/nextcloud/apps/dutycheck/tests/js/roster-virtualization.test.mjs:230:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 23
# suites 5
# pass 22
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 92.371581
killed fallback_paints_nothing
Roster virtualization mutation gauntlet OK (15 mutants killed).
